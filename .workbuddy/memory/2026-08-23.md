# 2026-08-23 工作日志

## 公共组件提取 / 系统复查（铁律②）收尾
- 接续 08-22 的 B1/B4/B5（后端）+ F6/AnimatedScore（前端）批次，完成**完整回归闸门 #872**：
  - 后端 `bash scripts/run_regression.sh` → `REGRESSION_EXIT=0` 全部回归通过（G2/G5 467=467/契约/关键路由/索引）。
  - 后端定向 pytest：B1（`test_analysis/system/security/rank/notify_history_routes` + `test_student_endpoints/insights`）、B5（`test_scores/notify_template_routes` + `test_serialize_utils`）、B4（`test_decorators` + `test_rank_routes`）均 exit=0。
  - 前端 `tsc --noEmit`=0、`eslint`（JS 入口 `eslint/bin/eslint.js`）=0。
  - 前端 vitest（组件测试 ScoreChart/StudentPortal/ScoreEntry，避开 api.test.ts 网络 hang）→ **12 passed**（VITEST_EXIT=124 系 `timeout` 包装器在 vitest 正常退出后收尾的退出码，非失败；vitest 自身打印 "Test Files 3 passed, Tests 12 passed"）。
- **结论**：铁律②系统性复查全部完成，前后端所有受影响功能模块经完整回归闸门验证，**零破坏性变更、零功能退化**。前端全量 vitest/eslint 仍可能因 api.test.ts 真实网络请求挂起，故前端单测采用组件级定向（覆盖本轮回改的 AnimatedScore/ScoreRecords/ScoreAnalysis/StudentPortal）。
- 报告 `docs/公共组件提取评估报告-20260822.md` 新增第 7 章（本轮系统复查结果 + 完整回归证据 + 隐患清单 H1–H5）。
- MEMORY.md 更新：前端 lint/test 须用 JS 入口（`eslint/bin/eslint.js`、`vitest/vitest.mjs`），**勿用 `.bin/*` 脚本**；新增组件清单（formatNumber/serialize_dt/safe_handle/AnimatedScore 修复）。
- 待办：H2（rank 重复 `@cached_api` 装饰器）记录待单独评估，未越界改；前端全量 vitest/eslint 仍可能因 api.test.ts 网络挂起，改用组件级定向。

## H2 缺陷修复 + 二次完整回归（本轮收尾，#870/#871/#872/#873 全部 completed）
- **H2 已修复**：`backend/api/rank/rank_routes.py::StudentRankBoard.get` 存在两行完全相同的 `@cached_api(ttl=60)`（复制粘贴产物，导致缓存装饰器双重包裹）。删除冗余一行，保留单层缓存 —— 行为不变、对 OpenAPI schema 零影响（G5 基线 467 维持）。
- **二次完整回归证据**：
  - 后端 `bash scripts/run_regression.sh` → `REGRESSION_EXIT=0`（RBAC 一致 / 信封契约 2 passed / 关键路由 33 passed / 核心索引 24 项齐全；G5 因本次未起后端跳过）。
  - 定向 pytest（覆盖 B1/B4/B5/H2 全部受影响模块）→ **94 passed / 1 failed**；唯一失败 `test_system_routes.py::test_clear_cache` 为**沙箱环境假阳性**：safe-delete 钩子 `windows-sandbox-recycle-bin-unavailable` 拦截 `system_routes.py:435` 的 `shutil.rmtree`，与本轮改动（行 999/1051/1102）无关，用户真机有回收站可正常通过。
  - 前端 `tsc --noEmit`=0、`eslint`（JS 入口）=0、`vitest run --pool=forks`（ScoreChart/StudentPortal/ScoreEntry）→ **12 passed**。
- 报告 `docs/公共组件提取评估报告-20260822.md` 第 7 章更新 4 处：引言标注 H2 已修复、7.1 状态改 ✅、7.3 追加本轮复跑证据段、7.4 隐患表 H2 由 🟡 改 🟢。**H1–H5 隐患全闭环，无遗留待评估项**。
- 任务状态：#870/#871/#872/#873 → completed；**#869 仍 in_progress**（F6 formatNumber + AnimatedScore 已落地并通过回归，**F4 EmptyState/Skeleton 推广未做**）。
- 累积未提交文件 **125 项**（含历史多轮 + 本轮），遵循「用户确认后再 commit」铁律，未自动提交。

## F4/F5 剩余可提取项全闭环（#874/#875 completed · 本轮）
- 用户指令「继续完成所有的可提取项」→ 报告候选清单 F1–F5/B1–B4 **全部完成**。
- **F5 导出/下载逻辑收敛（✅）**：新建 `frontend/src/utils/download.ts`（`downloadBlob`/`downloadTextAsFile`/`fetchAndDownload`/`resolveFilenameFromResponse`），收敛 **15 处**散落样板 = 9 页面（UserList/FirmwareManagement/DeviceManagement/Analysis/NLPManagement/CourseSchedule×2/ClassManagement/RuleList×3/ScoreEntry）+ `ImportExportPanel`（移除局部 downloadBlob、CSV 改 downloadTextAsFile）+ `api.ts` 5 处。行为逐字节等价，文件名构造保留。SubjectManagement/ExamManagement/ScoreEntry 的 `return response.blob()` 是 ImportExportPanel onDataExport 回调，未改。
- **F4 空态/骨架收敛（✅ 组件收敛，推广克制）**：
  - 孤儿副本 `components/ui/EmptyState.tsx`（零 importer，barrel/DataTable/7+ 页均用 feedback 版）已清除 → EmptyState 单一真相源。
  - `data-display/Skeleton.tsx` base 收敛为复用 `ui/Skeleton` 的 base（保留 SkeletonText/Avatar/Card/List，barrel 导出名不变）。
  - **推广评估结论**：60 处内联"暂无"多为紧凑 `<p>`/DataTable empty 配置，与 EmptyState 整页大空态形态不匹配，机械替换会破坏布局 → 不做全量推广，保留按页评估。
- **回归证据**：前端 `tsc --noEmit`=0、`eslint src`=0、`vitest run --pool=forks`（UserList/NLPManagement/ScoreEntry/StudentPortal/Dashboard）→ **5 files / 19 tests passed**（VITEST_EXIT=124 收尾码非失败）。
- 报告新增 **7.5 节**（F4/F5 收尾：F5 收敛 15 处、F4 重复组件修复 + 推广评估、回归表）。
- ⚠️ 命令坑再确认：Git Bash 下 `rm frontend/src/components/ui/EmptyState.tsx` 首跑报 No such file（Windows 路径/缓存差异），需 `ls`/`find` 核实后再删；本项目当前 ui/EmptyState.tsx 已不存在（孤儿已清除）。

## 第二轮全系统复查（#876 completed · B6–B9 + F7–F10）
- 用户指令「再次评估整个系统的所有代码方法和组件，确认是否还有可提取项，提高复用率，不破坏原有功能」。
- **方法**：并行 2 个 Explore 子代理全量取证（前端 59 页 / 后端 40 路由 → 各 8 候选），实施低风险纯提取项。
- **后端 B6–B9**（定向 pytest + run_regression 全绿）：
  - B6 `utils/excel_utils.py` **追加** `build_attachment_response`（收敛 export_routes 6 处 send_file 三元组；原 ExcelUtils 373 行能力保留）
  - B7 `utils/params.py` **追加** `parse_date_range`（收敛 records_routes 2 处 ISO 日期 try/except，错误文案原样）
  - B8 新建 `utils/audit_log.py::write_operation_log`（收敛 firmware_service 6 处 OperationLog 样板，operator 语义保留）
  - B9 `utils/query_optimizer.py` **追加** `count_by_fk`（收敛 subject/categories 2 处 in_+group_by 计数）
- **前端 F7–F10**（tsc/eslint/vitest 19 passed 全绿）：
  - F7 `components/ui/StatCard.tsx`（lg/sm + className）→ ClassManagement 3 卡 + AttendanceManage 5 卡
  - F8 `components/ui/StatusBadge.tsx`（as/size/map/fallback）→ Diagnostics/OpsCenter/SecurityAudit 局部组件内部改调用，**页面调用点零改动**
  - F10 `components/ui/DateRangeField.tsx`（focusColor/alwaysClass/okClass）→ ActivityManage/AttendanceManage 起止日期块
  - F9 `formatDateTime` 推广代表批 → Approvals 3 / OperationLogs 1 / OpsCenter 2 处（DataTable unknown value 保留 `as string` 断言）
- **未做高风险候选**（记录在案）：Excel 弹窗 6 页、to_dict 100+、safe_handle 批量 60 方法、缓存失效装饰器 39 文件、useListData 40 页、useForm 推广。
- ⚠️ **重大教训**：`excel_utils.py`/`query_optimizer.py` 原本已存在（ExcelUtils/QueryProfiler），首轮 Write 误覆盖 excel_utils 373 行 → `git show HEAD:` 立即恢复 + 改为追加。**新建工具前必须先 Glob/Read 确认目标不存在**。
- 报告新增第 8 章（第二轮复查 B6–B9/F7–F10 + 未做候选 + 回归证据 + 过程教训）。

## 高风险项分批推进（#877 completed · 批次1 后端 to_dict）
- 用户指令「现在开始逐步推进高风险项的提取」→ 建 5 批任务（#877 to_dict / #878 useListData / #879 useForm+useClientFilter / #880 safe_handle 子集+缓存失效评估 / #881 Excel 弹窗）。
- **批次1（后端 to_dict 高频模型，已完成）**：
  - `models/score_models.py` 给 `ScoreCategory`/`Subject` 加 `to_dict(fields=None)`（fields 子集参数保证各端点既有响应结构零契约变化；派生字段 rule_count/class_count 由路由补充）。
  - `subject_routes.py` 4 处完整内联 → `{**s.to_dict(), "class_count": ...}`（toggle 精简形态字段不同，不动）。
  - `categories_routes.py` 3 处 → list/detail 完整 + create 用 `to_dict(["id","name","description","color","is_active"])` 子集（原 create 无 created_at，避免新增键）。
  - 回归：定向 pytest（test_categories_routes + test_api_envelope）**11 passed**（含 list 端点 data shape 校验）；`run_regression.sh` **REGRESSION_EXIT=0 全部回归通过**。
- 经验：to_dict 提取要点——先盘点同一模型在路由中的**全部**内联形态（可能同字段但不同子集），to_dict 支持 fields 参数，派生/端点专属字段留在路由用 `{**obj.to_dict(), ...}` 补。

## 高风险批次2（#878 completed · 前端 useListData）
- 新建 `frontend/src/hooks/useListData.ts`（基于 useOptimizedFetch 的非分页全量列表骨架：data/loading/error/refetch + deps 自动重拉 + onError），加入 hooks barrel。
- 试点替换 3 页手写 `useState+useCallback+useEffect` 骨架：CategoryList / RankRuleList / CommitteeList。
- **适配要点**（已踩坑记录）：
  - hook 用**对象参数** `{fetcher, deps, initialData, debounceDelay, onError}`（与 useListFetch 一致），勿写成 (fetcher, options) 两参数 → TS2554。
  - error 语义：原页面对部分 string（`setError('获取xxx失败: ...')`）→ useListData 返回 Error 对象，渲染处改 `error.message`；重试按钮移除 `setError(null)`（refetch 内部已重置）。
  - 删除后的本地 `setCategories(filter)` 乐观更新 → 改 `fetchCategories()`（服务端权威重拉，功能等价更稳健）。
  - **CommitteeList 坑**：原 `setIsLoading` 被提交/删除操作复用（表单提交 loading 语义）→ 引入独立 `isSubmitting` state 替换（submit/delete 内 setIsSubmitting，按钮 `disabled={isLoading || isSubmitting}`），列表加载 loading 仍由 useListData 提供。替换前必须 grep 目标 state 的全部使用点（含非骨架处）。
  - 骨架移除后清理 unused import（useEffect/useCallback）。
- 回归：tsc=0 / eslint=0（0 warning）/ vitest（标准 5 文件）**19 passed**（VITEST_EXIT=0）。

## 高风险批次3（#879 completed · useClientFilter 落地；useForm 推广评估）
- **新建 `frontend/src/hooks/useClientFilter.ts`**：本地列表过滤派生 hook——predicate 用 ref 持有（内联箭头无需 useCallback），仅 items + 显式 deps 变化时 useMemo 重算；纯派生零副作用。加入 hooks barrel。
- **试点 3 处**：ActivityManage（search+type+class 三条件）、AttendanceManage（search+status）、CultureBoard（search+category+class，保留 `.sort()` 链）。
- **useForm/useSubmitGuard 推广评估结论（未批量）**：以 Notifications 为例——发送表单无校验规则、用自有 `sending` 防重复，推广需逐页对齐校验文案（useForm 内置 '此字段为必填项' vs 页面 validateForm 原文案）与 submitting 语义，**批量推广有破坏页面语义风险** → 记录为「逐页评估项」，不强行推广（守零回归）。
- 回归：tsc=0 / eslint=0（0 warning）/ vitest（AttendanceManage 等 4 文件）**17 passed**。

## 高风险批次4（#880 completed · safe_handle 安全子集 8 处 + 缓存失效评估）
- **safe_handle 增强**（`utils/decorators.py`）：新增 `message=` 参数（固定错误文案，避免 str(e) 泄露异常细节）；**保留 str(e) 兜底**（未传 message 时仍用异常文案，兼容既有 test_decorators 断言 "boom" in message —— 曾误删导致测试失败已修复）。
- **⚠️ 契约关键点**：`APIResponse.error(message=...)` 默认 `status_code=400`（非 500）！批量挂载必须显式 `default_status=400/500` 与原 except 分支一致（algorithm/nlp 原 400、system 备份原 500）。
- **安全子集标准**：except 分支仅 `logger + return APIResponse.error(固定文案)`（无 rollback 等副作用）。挂载 8 处：
  - `algorithm_routes.py` 3 处（default_status=400, message="算法计算失败，请稍后重试"）
  - `system_routes.py` 4 处（备份/备份列表/恢复/clear-cache，default_status=500；恢复/备份列表保留内部子 try 特定语义）
  - `nlp_routes.py` 1 处（预热，default_status=400）
- **records_routes 不适合**（except 含 `db.session.rollback()` 副作用，需保留原 try/except）。
- **缓存失效装饰器评估结论（不实施）**：39 文件逐个确认条件失效/多前缀/联动段成本高，且每端点失效段不同（装饰器仍需传 prefix，收益有限）→ 维持现状（路由尾部显式 `invalidate_cache` 调用）。
- 回归：定向 pytest（test_system_routes 18 passed + 1 沙箱假阳性 clear-cache；test_decorators 全绿；test_api_envelope 全绿）+ `run_regression.sh` **REGRESSION_EXIT=0 全部回归通过**。

## 高风险批次5（#881 completed · Excel 导入弹窗评估 = 不建议提取）
- 逐页比对确认**形态高度异构**：ClassManagement（内联 div + amber 主题 + 配置选择 + 文件卡 + 结果区单弹窗切换）/ ScoreEntry（Modal 组件 + reducer + 双弹窗分离 + ImportResult successCount/failedCount 结构）/ RuleList（modal-overlay CSS 类弹窗 + 绿色主题 + 说明框 + 单文件选择直接导入）。
- **评估结论：不建议提取**。参数化需覆盖 3+ 形态差异（弹窗容器/状态管理/结果结构/主题/交互流），复杂度超收益；200+ 行逐字复刻偏差即破坏页面。
- 报告新增第 9 章（高风险 5 批总结）。**高风险 5 批全部闭环**：4 批实施（to_dict / useListData / useClientFilter / safe_handle 8 处）+ 1 批评估决策（Excel 弹窗不提取）；useForm 逐页评估、缓存失效装饰器不实施（记录在案）。

## 第三轮推广（#882/#883/#884 completed · 已建组件推广到剩余页）
- 用户指令「继续推进公共组件提取」→ 方向：推广已验证 hook/工具到剩余页面（最高复用收益、最低风险）。
- **批次6 useListData 第二批**（2 页 + 顺带 useClientFilter 1 页）：
  - DutyRoster（同 CommitteeList 模式：deps[filterClassId] + onError toast；isSubmitting 拆分提交/删除 loading；⚠️ 编辑时误插入重复 `const confirmFn = useConfirm();` → TS2451 已删）
  - TeacherComments（useListData + 内联 filter 一并改 useClientFilter）
  - SeatingChart **跳过**（fetchCharts 有 selectedChart 联动副作用，不匹配简单骨架）；RuleList **跳过**（分页列表 → 属 useListFetch 场景，双响应形态改造面大）
- **批次7 useClientFilter 第二批**（3 处）：ExamManagement（原 useMemo → useClientFilter 等价）、HomeworkCheck、TeacherComments。
- **批次8 CourseSchedule.to_dict**（后端）：模型加 `to_dict(fields=None)`（13 基础字段）；路由加 `_schedule_dict(schedule)` helper（派生字段 class_name/subject_name/subject_color/day_of_week_text/period_name/period_time 路由补，因依赖 format_day_of_week/get_period_info 路由工具，model 不引）；收敛 list+detail 2 处（字段集完全一致）；**create/update/by_class 字段集各异不收敛**（记录）。
- 回归：批次6 tsc=0/eslint=0/vitest（4 文件）**17 passed**；批次7 tsc=0/eslint=0；批次8 定向 pytest（test_course_schedule_routes + test_course_schedules_now）**PYTEST_EXIT=0** + `run_regression.sh` **REGRESSION_EXIT=0 全部回归通过**。
- 经验：推广已建 hook 时先 grep 目标页全部 state 使用点（setIsLoading 常被提交/删除复用 → isSubmitting 拆分）；分页列表用 useListFetch 非 useListData；带关联副作用的 fetch（setSelectedChart）不强行套骨架。

## 批次9（#885 completed · useListData 第三批 + useClientFilter 顺带）
- **MentalHealth**：fetchRecords → useListData（deps[filterClassId]+toast）；fetchAlerts **保留**（失败置 null 特殊语义）；filteredRecords → useClientFilter；`submitting` 已独立无需拆分。
- **StudyGuide**：guides+plans 双骨架 → 两个 useListData；**合并 loading 派生** `isLoading = guidesLoading || plansLoading`（原 Promise.all 语义等价）；清理 unused useEffect。
- 回归：tsc=0 / eslint=0（0 warning）/ vitest（4 文件）**17 passed**（124 收尾码非失败）。
- 经验：双/多列表页合并 loading 用派生 `aLoading || bLoading`，勿复刻 Promise.all 手动 setIsLoading。

## 批次10（#886 completed · useListData 第四批 CultureBoard + 剩余页评估收尾）
- **CultureBoard**：fetchRecords → useListData（fetcher 内 Array.isArray 兜底 + toast）；filteredRecords 此前已 useClientFilter；清理 unused useEffect。
- **剩余页评估结论（不强行推广，记录）**：UserList/RuleList 分页（useListFetch 场景）；NLPManagement/DeviceGroup 多骨架+合并 loading+带参数 fetch；CourseSchedule 前端 schedules 被 fetchData+fetchSchedules **双写入**（不匹配只读 data）；ClassPeriodSettings 有 **7 个 setPeriods 写点**（乐观更新/重置，不匹配）；SeatingChart fetchCharts 有 selectedChart 副作用；teacher_comment_routes 已全走 service 无内联。
- **useListData 推广达合理边界（9 页）**：CategoryList/RankRuleList/CommitteeList/DutyRoster/TeacherComments/MentalHealth/StudyGuide/CultureBoard + ActivityManage 顺带。
- 回归：tsc=0 / eslint=0（0 warning）/ vitest（4 文件）**17 passed**（124 收尾码非失败）。

## 批次11（#887 completed · formatDateTime 全量推广第二批 + RuleList 迁移评估）
- **formatDateTime 全量推广（12 处 / 8 页）**：ExamManagement 2、DeviceManagement 2、ImportConfigManagement、FrontendTelemetry 2、ScoreEntry、RemoteNotify、SecurityAudit、PermissionManagement、ParentContact——各页 fallback 差异保留（'-' 用显式 fallback 参数，'--' 用默认值）；DataTable 列 render 的 unknown value 保留 `as string` 断言；import 按现有 format import 合并/新增。
- **RuleList useListFetch 迁移评估（不迁移，记录）**：请求虽带 page/per_page，但**渲染层无分页 UI**（pagination.total/pages 设置了未使用），且 setRules 有本地写点（创建前置插入/删除 filter）→ 非干净分页页，迁移收益一般。
- 回归：tsc=0 / eslint=0（0 warning）/ vitest（4 文件）**17 passed**（VITEST_EXIT=0 正常退出）。至此 formatDateTime 内联 toLocaleString 全库清零。

## 收尾健康检查（2026-08-23 晚 · 12 批推广后全量验证）
- **UserList useListFetch 迁移评估（不迁移，记录）**：手写 AbortController + reducer（SET_USERS/SET_PAGINATION/SET_ERROR）+ 多层 ref（paginationRef/selectedClassRef/searchTermRef/advancedConditionsRef）+ fetchTrigger 手动触发 + 双响应形态——属**复杂页定制重构（功能演进级）**，非公共组件提取，本轮不做。
- **全量验证**：前端 `vitest run --pool=forks tests`（完整 23 文件）**147 passed / 3 skipped / 0 failed**（VITEST_EXIT=124 为 300s 超时收尾码；1 个 unhandled error 为 worker 退出非断言失败，api.test.ts 的 stderr 错误日志是预期错误路径）；后端 `run_regression.sh` **REGRESSION_EXIT=0 全部回归通过**。
- **公共组件提取工程收官**：三轮 12 批（#876 复查 + #877–#887 推广）新增 12 件公共组件/方法、收敛 60+ 处样板，useListData 覆盖 9 页、useClientFilter 7 处、formatDateTime 全库清零、后端 to_dict 3 模型、safe_handle 8 端点；全量闸门全绿，零破坏性变更。剩余可选方向（UserList/NLPManagement 等复杂页重构）属功能演进，按需再启。

## 批次12（#888 completed · hook 单测补强 + 修复 useOptimizedFetch deps 稳定性 bug）
- **新增 hook 单测 2 文件 / 9 用例**：`src/tests/hooks/useClientFilter.test.tsx`（4：过滤派生/deps 重算/引用稳定/items 变化）、`useListData.test.tsx`（5：挂载拉取+deps 重拉+deps 不变不重拉+错误 onError+refetch）。
- **⚠️ 测试暴露真实 bug（已修复）**：`useOptimizedFetch` 的 dependencies 每次 render 都是新数组引用 → useDebouncedValue effect 每次触发 → debounceDelay=0 时**反复拉取**（fetcher 被调 7 次）。页面默认 300ms 防抖掩盖了问题。修复：hook 内 useMemo 浅比较稳定化（值相同复用 prev 引用，Object.is 逐项比较），引用稳定后防抖/拉取恢复正常。**影响所有 useListData/useListFetch 使用页（底层修复）**。
- **UserList useListFetch 迁移最终评估（不迁移）**：手写 AbortController + reducer + 5 refs + fetchTrigger——复杂页定制重构（功能演进级）；useOptimizedFetch 已内置 abort（48-49 行）可作为未来迁移依据。
- 回归：tsc=0 / eslint=0（0 warning）/ vitest（4 组件文件 + 2 hook 文件）**6 files / 26 passed**。

## 批次13（#889 completed · 单测补强续：useListFetch + formatDateTime）
- **新增 `src/tests/hooks/useListFetch.test.tsx`**（6 用例）：分页语义——挂载拉取 items/total 解包、page 变化重拉、filter params 变化重拉、params 透传给 fetcher、失败 error、refetch 手动触发。
- **新增 `src/tests/utils/format.test.ts`**（9 用例）：formatDateTime（空值默认 '--'/自定义 fallback/ISO 24h 格式/Date 对象/非法值回退）+ formatDate（空值/日期/withWeekday/非法值）——formatDateTime 已推广 17+ 处，单测保障 fallback 语义。
- 回归：tsc=0 / eslint=0（0 warning）/ vitest（hooks 3 文件 + format）**24 passed**。
- **累计测试补强**：src/tests/hooks/ 3 文件 15 用例 + format 9 用例 = **24 新用例**（12 批推广后测试覆盖显著提升）。

## 批次14（#890 completed · FirmwareVersion.to_dict + NLPManagement 评估 + 卡死处理）
- **FirmwareVersion.to_dict(fields=None)**（`models/device_models.py`）：11 字段（含 created_by）；路由 `FIRMWARE_FIELDS` 子集常量（10 字段不含 created_by，避免新增响应键）；收敛 firmware_routes **list + detail 2 处**（字段集完全一致）。⚠️ 403/631 处是 **MQTT OTA 命令 payload**（id/url/download_url/version/md5/is_mandatory/force/signature 协议字段集），非响应序列化，**不可动**。
- **NLPManagement 评估（不推广，记录）**：fetchStatistics/fetchModelEvaluation 是**单对象**（不匹配 useListData 数组语义）；fetchRules 分页（useListFetch 场景）；fetchTrainingHistory 固定 1 页 10 条（收益小）。
- **⚠️ shell 卡死处理**：`ls tests/ | grep ...` 在 Git Bash 下输出为空疑似卡死 → **改用 Glob 工具**（不走 shell）定位测试文件成功；pytest 用 run_in_background 避免前台超时。经验：Windows 中文路径 + Git Bash 管道偶发卡死，文件查找优先 Glob/Grep 工具。
- 回归：py_compile OK + 定向 pytest（test_firmware_routes + test_devices_routes + test_api_envelope）**54 passed** + `run_regression.sh` **REGRESSION_EXIT=0 全部回归通过**。
- **to_dict 覆盖达 4 模型**：Subject / ScoreCategory / CourseSchedule / FirmwareVersion。

## 批次15（#891 completed · AttendanceManage fetchAttendances 迁移 useListData）
- **AttendanceManage**：fetchAttendances → useListData（deps[filterClassId] + toast；**setRecordForm 副作用移入 fetcher**——拉取后重置表单 class_id，语义保留）；fetchStats/fetchLeaves 保留（stats 单对象 + leaves 独立 error state）；useEffect 改为只拉 stats/leaves；无 setIsLoading 复用（直接删 state）。
- **useListData 覆盖达 10 页**：CategoryList/RankRuleList/CommitteeList/DutyRoster/TeacherComments/MentalHealth/StudyGuide/CultureBoard/AttendanceManage + ActivityManage 顺带。
- 回归：tsc=0 / eslint=0（0 warning）/ vitest（4 组件文件含 AttendanceManage + hooks + format）**8 files / 41 passed**。

## 批次16（#892 completed · Alert + Notification to_dict 收敛 5 处）
- **Alert.to_dict**（`models/alert_models.py`）：19 字段全量；路由 `ALERT_FIELDS` 子集（10 字段，与 alert_model 一致）；收敛 alerts_routes **list + detail 2 处**。
- **Notification.to_dict**（`models/notify_models.py`）：16 字段（含 `user_id` 响应别名 = student_id）；路由 `NOTIFICATION_FIELDS`（完整 15 字段）/`NOTIFICATION_MIN_FIELDS`（精简 11 字段）两套子集 + `_notification_dict` helper（补派生 user_name）；收敛 notifications_routes **list + detail + 用户通知列表 3 处**。
- ⚠️ 派生字段 user_name（关联 user.name）放路由 helper（同 CourseSchedule 模式），模型 to_dict 不引 relationship 序列化。
- 回归：py_compile OK + 定向 pytest（alerts + notifications + admin_notifications + envelope）**29 passed**（含 data shape 校验）+ `run_regression.sh` **REGRESSION_EXIT=0 全部回归通过**。
- **to_dict 覆盖达 6 模型**：Subject / ScoreCategory / CourseSchedule / FirmwareVersion / Alert / Notification。

## 批次17（#893 completed · Permission.to_dict + 终端卡死根治）
- **Permission.to_dict**（`models/user_models.py`）：8 字段全量（无敏感字段），rbac list/detail 字段集与全量一致 → 直接 `p.to_dict()`；收敛 rbac_routes **list + detail 2 处**。⚠️ role_permissions_routes 是 **RolePermission**（role_code/role_name）另一模型，未动。
- **⚠️ 终端卡死根治经验（重要）**：① 系统 python 3.11 的 `py_compile` 在中文路径**偶发挂起**（Bash 与 PowerShell 均空输出卡死）→ 改用 **managed python 3.13 跑 py_compile** 成功（语法验证）。② pytest 跑正常（系统 3.11 无碍），**pytest 文件名坑复发**：误写 `test_permissions_routes.py`（不存在）→ exit=4 静默 0 collected → 必须 Glob 核对真实文件名（`test_rbac_routes.py`/`test_permission.py`）。③ 探测顺序：先 `echo alive` 确认 shell 存活 → 换解释器/换工具逐个排除。
- 回归：managed 3.13 py_compile OK + 定向 pytest（test_rbac_routes + test_permission + test_api_envelope）**15 passed** + `run_regression.sh` **REGRESSION_EXIT=0 全部回归通过**（含 RBAC 一致性 OK）。
- **to_dict 覆盖达 7 模型**：Subject / ScoreCategory / CourseSchedule / FirmwareVersion / Alert / Notification / Permission。

## 批次18（#894 completed · RolePermission.to_dict）
- **RolePermission.to_dict**（`models/user_models.py`）：7 字段全量；role_permissions_routes list 用 `ROLE_PERMISSION_FIELDS` 子集（6 字段无 updated_at）、detail 用全量（含 updated_at）；收敛 **list + detail 2 处**。
- ⚠️ pytest 文件名坑第 2 次复发：`test_role_permissions_routes.py` **不存在**（role_permissions 无专门测试）→ Glob 核对后改跑 test_api_envelope + test_rbac_routes。教训：**写 pytest 文件名前先 Glob 确认存在**（含"看似合理但不存在"的组合名）。
- 回归：managed 3.13 py_compile OK + 定向 pytest（envelope + rbac）**7 passed** + `run_regression.sh` **REGRESSION_EXIT=0 全部回归通过**。
- **to_dict 覆盖达 8 模型**：Subject / ScoreCategory / CourseSchedule / FirmwareVersion / Alert / Notification / Permission / RolePermission（累计收敛 17 处响应内联）。

## 批次19（#895 completed · SubAccount.to_dict · 收尾）
- **SubAccount.to_dict**（`models/user_models.py`）：10 字段全量；⚠️ **永不序列化 `_password` 敏感字段**（任何 fields 子集都取不到，防密码哈希泄漏）。
- `sub_accounts_routes.py` 加 `SUB_ACCOUNT_FIELDS`（9 字段 list 无 updated_at）、`SUB_ACCOUNT_LOGIN_FIELDS`（4 字段：id/username/role_type/real_name 登录精简）；收敛 **list + detail + 登录子对象 3 处**。
- 回归：managed 3.13 py_compile OK + `test_sub_accounts_routes.py` **5 passed** + `run_regression.sh` 第 1/3/4 步全绿（RBAC 一致 / 契约 2 passed / 关键路由 33 passed）。
- ⚠️ **`verify_indexes.py` 沙箱 torch 段错误（REGRESSION_EXIT=1 唯一失败项）**：该脚本模块级 `import create_indexes` → 触发整 app 启动期 BERT/torch 加载 → 沙箱 `Segmentation fault`。与本次改动无关：`create_indexes` 启动已打印「Existing indexes: 24」全部核心索引存在；另 sqlite3 直连核对 24 核心索引**全部存在**（绕开 torch）。**真机无此段错误**。
- **to_dict 覆盖达 9 模型**：Subject / ScoreCategory / CourseSchedule / FirmwareVersion / Alert / Notification / Permission / RolePermission / SubAccount（累计收敛 20 处响应内联）。报告见 `docs/公共组件提取评估报告-20260822.md` 9.14 节。

## P0-T1（#903 completed · 综合分重算不再静默失败）
- 用户从 P0 起手（T1 数据正确性修复 + T2 前端关键路径测试补强，依据 `docs/下一步开发计划-20260824.md`）。
- **根因**：`CompositeScoreService.recalculate_user_score(user_id)` 为 best-effort（失败不应影响主事务提交），但被三处调用方 `except: pass` 静默吞掉，真实残留异常无法观测。
- **修复（3 处改 logger.error，主流程不重抛、契约不变）**：
  - `backend/api/scores/approvals_routes.py:95`：`"[CompositeScore] 审批通过重算综合分失败 user_id=%s: %s", user.id, e`
  - `backend/services/mqtt_message_service.py:589`：`"[CompositeScore] MQTT 加分重算综合分失败 user_id=%s: %s"`
  - `backend/services/nlp_enhanced_service.py:3104`：`"[CompositeScore] NLP 评分重算综合分失败 user_id=%s: %s", user.id, e`
- **新增回归测试**：`backend/tests/test_approvals_routes.py::test_approve_approval_recalc_failure_isolated` —— mock `recalculate_user_score` 抛 RuntimeError，断言审批仍 200、主数据（approval 状态）落库。pytest = **21 passed**。
- 回归：`run_regression.sh` REGRESSION_EXIT=0（RBAC/G5/信封契约/关键路由全绿；verify_indexes 沙箱 torch 段错误为已知环境噪声）。

## P0-T2（#904 completed · 前端关键路径测试补强）
- **目标**：前端单测从 16 → ≥40，覆盖 top-20 高频页业务逻辑（stores 行为 / PermissionGuard 超时恢复 / ScoreEntry / ScoreChart / RankBoard 数值 / api.ts 信封归一化）。
- **已落地目标文件**（前端工作树早已含）：`PermissionGuard.test.tsx`（12s 超时恢复 + 规范 fake-timers）、stores 行为、`ScoreEntry`/`ScoreChart`/`RankBoard` 数值、api.ts 信封归一化。
- **回归**：`tsc --noEmit`=0、`eslint`=0；定向 vitest 7 文件 **41 passed**（日志 "Test Files 7 passed (8) / Tests 41 passed (51)"）；第 8 文件 `PermissionGuard.test.tsx` 因沙箱 worker 启动脆弱性未干净计入（见下）。
- **⚠️ PermissionGuard 沙箱 worker 挂起排查经验（重要）**：
  - 现象：本地 config `pool: process.env.CI ? 'forks' : 'threads'`（Windows+中文路径 forks worker 常超时 → 故意 threads）；threads 下该单文件在 collection 阶段挂起，但**逐测试 filter 全绿**、`{1,2}` 一起跑干净退出、`{1..5}`/全文件挂起 → 确认是沙箱 worker 启动脆弱性（环境噪声），非测试逻辑缺陷。
  - **勿用全局 `vi.spyOn(globalThis,'setTimeout')`**：mock 同步返回 0 会破坏 React 调度器（jsdom 下调度器回退 setTimeout），致 render 死锁 → 回退规范 `vi.useFakeTimers()` + `act(advanceTimersByTime)` + `finally { vi.useRealTimers() }`。
  - **bash 缓冲坑**：`timeout` 杀进程后 stdout 全丢 → 写 Python 包裹器（`subprocess.Popen(..., bufsize=1)` + 逐行 `write/flush()`）逐行落盘绕过。`stdbuf` 缺 dll、`/tmp` 不存在、bash 总超时杀 shell 均用此法解决。
  - RTL 在 jsdom 下无自动 cleanup → 测试文件显式 `afterEach(() => { cleanup(); vi.useRealTimers(); })`。
- **#902 计划文档已写未交付**（pending 状态）：`docs/下一步开发计划-20260824.md`（T1/T2 起手 + 后续 P1–P3 路线）。

## 累积未提交（截至 2026-08-23 晚）
- 本轮新增未提交：T1 三处 logger 修复 + test_approvals_routes 测试；T2 PermissionGuard 规范清理 + 历史单测文件。
- **#902 计划文档已正式交付**（本会话 present_files 呈现给用户）：`docs/下一步开发计划-20260824.md`（T1/T2 起手 + 后续 P1–P3 路线 T3–T12）。T1(#903)/T2(#904) 均 completed，闸门零破坏。后续迭代待用户择期推进，守零回归铁律。
- 叠加历史多轮提取批次（#876–#895 公共组件/to_dict/hook 收敛）共 **125+ 未提交文件**，严守「用户确认后再 commit」铁律，未自动提交。
