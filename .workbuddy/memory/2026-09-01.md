# 2026-09-01 工作日志

## #982 P3-b 座次表拖拽（统一 Pointer Events ✅）

- 勘察发现 `frontend/src/pages/SeatingChart.tsx` 早已实现拖拽（原生 HTML5 DnD），但**触屏/移动端原生 DnD 不触发**，与已收口的 P3-④ 移动端响应式矛盾。
- 用户拍板路线：**统一 Pointer Events**（桌面+触屏单路径，不引入第三方 DnD 库）。
- 实施：移除 `draggable/onDragStart/onDragOver/onDrop`，改为 `onPointerDown/Move/Up` + `setPointerCapture`；8px 阈值进入拖拽；松手用 `document.elementFromPoint` + `data-seat` 命中目标座位完成双人互换。
- 复用既有 `handleSeatDrop` 的"乐观更新 + 失败回退"逻辑（源座位改为显式入参，规避 setState 异步读不到源座位）；落库仍走 `api.seating.updateSeat` ×2，后端零改动。
- 触屏防滚动用 Tailwind `touch-none`（仅拖拽座位）；拖拽中固定跟随 ghost（`pointer-events-none`，避免 elementFromPoint 误命中自身）；`isSwapping` 期间 `pointer-events-none` 天然防重入。
- 回归闸门：`tsc --noEmit` exit 0；`eslint src/pages/SeatingChart.tsx` exit 0。无新增单测（沿用 13 子页既有策略：tsc+eslint 零错误 + 数据落库路径未变）。
- 仓库纪律：改动**未 git commit / 未 push**，待用户明确指示。

## 当前状态

- 班主任工作台 P3 系列（P3-a 操作反馈 #981 / P3-e 导航 #980 / P3-③ 无障碍 #985 / P3-④ 响应式 #986 / P3-b 拖拽 #982）全部 completed。
- 待办方向：git 提交推送（需代理），或推进其他非 P3 任务（用户未指示）。

## #F1–F6 六项 High 级缺陷修复 + 零回归闸门全绿 ✅

- 前次会话已完成 6 处代码 Edit（F1 App.tsx 登录解析白屏 / F2 useOptimizedFetch AbortController 竞态 / F3 DataTable 分页收敛+虚拟切片 / F4 create_score_entry 原子累加钳制 / F5 delete_record 回滚钳制 / F6 composite_score_service 全局进度按 task_id 隔离）。
- 本会话执行零回归闸门：
  - 前端 `tsc --noEmit`（managed Node 22.22.2）：**TSC_EXIT=0** ✅
  - 前端 `eslint src --ext .ts,.tsx`（managed Node 22.22.2）：**ESLINT_EXIT=0**（0 error，2 既有 warning 与本次无关）✅
  - 后端 `py_compile -q .`（managed Py3.13.12）：**PYCOMPILE_EXIT=0** ✅
  - 后端定向 pytest（系统 Py3.11，F4/F5/F6 受影响模块 `tests/test_records_routes.py`）：**62 passed, 1 warning, exit 0** ✅
  - 后端全量回归 `bash scripts/run_regression.sh`（系统 Py3.11，后台 ~2m50s）：**REGRESSION_EXIT=0**，5 步闸门全过（RBAC 一致 OK、OpenAPI 漂移因后端未启动跳过非阻塞、契约 2 passed、关键路由 33 passed、核心索引齐全）✅
- 任务 #1007(F1)/#1008(F2)/#1009(F3)/#1010(F4)/#1011(F5)/#1012(F6) 全部标记 completed。
- Medium 待确认项（composite_score_service:511 无 rollback、algorithm_service:231 N+1、score_record_service:297 班级隔离、多处 get_json KeyError、列表无分页、VirtualList keyExtractor 默认 Math.random、api.ts:1051 取消覆盖、useOptimisticUpdate:169、DataTable 多处 Low 项、后端 permission 缓存 TTL、approval_service 无 rollback 等）**仍按约定待用户确认是否纳入下一轮，本轮未动**。
- 仓库纪律：所有改动**未 git commit / 未 push**，待用户明确指示。

## Medium 轮缺陷修复推进（2026-09-01 续）

- 计划文档 `docs/Medium轮缺陷修复计划-20260831.md` 已落盘（M1–M11 定级：5 直接修 / 1 关闭 / 2 标红 / 3 延后）。
- **已直接修复（无业务逻辑/契约/权限歧义，安全项）**：
  - **M1** `frontend/src/components/data-display/VirtualList.tsx`：keyExtractor 默认回退从 `Math.random()` 改为稳定绝对索引 `vl-idx-${index}`（签名扩 `(item, index?)`，两处调用点传 `index`/`startIndex+localIndex`）。
  - **M5** `frontend/src/services/api.ts`：`registerAbortController` 注册前若同 key 旧 controller 仍存活先 `abort()`，消除请求泄漏。
  - **M2** `backend/services/composite_score_service.py`：增量更新 commit 外包 `try/except: db.session.rollback(); raise`（F17 下沉模式）。
  - **M3** `backend/services/approval_service.py`：create/update/delete/approve/reject 五处 commit 均外包 rollback 守卫，零契约影响。
  - **M4** `backend/services/algorithm_service.py`：`get_student_data_for_analysis` 的 N+1 改为单次 `GROUP BY student_id` 聚合预取平均分字典（等价性已论证：SQL AVG 跳过 NULL = 原 `[s for s if s is not None]` 过滤；全 NULL 学生两路径均被末行 `df.dropna()` 剔除）。同步更新 `test_algorithm_service.py` 的 mock（由 `Score.query.filter_by` 改为 `db.session` 聚合 mock），断言行为不变。
- **闸门证据**：
  - 前端 `tsc --noEmit`（managed Node 22.22.2）exit 0 ✅；`eslint`（同一 Node）0 error（2 既有无关 warning）✅；`vitest` api 用例 **34 passed** ✅。
  - 后端 `py_compile`（managed Py3.13.12）三文件 OK ✅；定向 pytest：approval+composite **34 passed**、algorithm **17 passed** ✅。
  - 全量 `bash scripts/run_regression.sh`（系统 Py3.11）**后台运行中**，待收口。
- **判定关闭**：**M6** 110 处裸 `get_json()` 因 Flask-RESTX 全局 errorhandler 已兜底 400、裸调用不返 None、silent=True 处均有守卫，判定非缺陷，避免零收益改动。
- **⚠️ 标红待用户拍板（未动）**：**M7** `score_record_service:316` today_count 绕过班级隔离（班主任见全校今日数）；**M8** `permission.py:286` 缓存 TTL 硬编码 30s 不可调。二者均涉数据可见范围/权限语义，须用户确认方案后再定稿。
- 仓库纪律：所有改动**未 git commit / 未 push**，待用户明确指示。
