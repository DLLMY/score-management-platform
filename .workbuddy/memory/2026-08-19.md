# 2026-08-19

## 第五次全面评估（用户指令"再次评估整个项目"）

- **方法升级**：pyflakes 全类别扫描（上次仅 imported-but-unused）→ 发现 **50 处 undefined name**，逐一验证确认 **5 个真实 P1 NameError 缺陷**（2 个运行时复现）：
  1. utils/validation.py::get_user_fields — fields 未导入（get_common_fields 有局部导入，get_user_fields 没有）→ 实测 NameError
  2. utils/cache.py::cached().invalidate — lambda 引用 wrapper 局部 cache_key（作用域外）→ 实测 NameError
  3. utils/batch_writer.py — datetime 未导入（MQTT 批量日志）
  4. utils/diagnostics.py — time 未导入（诊断 timing；psutil 有 PSUTIL_AVAILABLE 保护非缺陷）
  5. services/nlp_enhanced_service.py:2607 — corrected_pos 未定义（学名修正路径）
- **P2**：export_routes student_id 重复键；users_routes io 重复导入；mqtt_manager 62 + redis_cache 28 处 print 未走 logger；15 处 f-string 无占位符；scripts/ 4 文件缺导入（shutil/argparse/subprocess/statistics）；test_nlp_service_comprehensive get_nlp_service 未导入（try/except 掩盖为 skip）。
- **backups/ 1.2G 膨胀根因**（上次 616MB→1.2G）：① clean_old_backups 仅备份任务触发时调用，BACKUP_ENABLED 默认 false → 保留策略永不执行；② test_create_backup 写真实 backups/（3×189M test zip）；③ retention_days=30 与 BACKUP_MAX_COUNT=10 不一致。
- **闸门全绿**：G2 68 ✅ / G5 461 零漂移 ✅ / 全量 157 文件 ~1971 用例 0 FAILED ✅ / tsc 0 / eslint 0 / build 0 / vitest 164 ✅ / models __all__ 83 ✅ / 56 页面全注册 ✅ / unlock+backup 未认证 401 ✅。
- **工具链限制**：npm audit 无法执行（npmmirror 不支持 audit API）。
- **评分**：8.6/10（↓0.1，因发现测试盲区内 NameError；安全 ↑0.5 因 unlock 认证修复生效）。报告：reports/PROJECT_FIFTH_EVALUATION_REPORT_2026-08-19.md。
- **pytest 退出阶段异常记录**：全量 100% 后 summary 未输出（进程消失、无 FAILED/ERROR），重定向句柄卡住后台任务——退出阶段偶发，不影响结果判定。

## 第五次评估修复执行（用户选择"修复全部 P1+P2"）

- **提交 0952897 已推送**（amend 修正行尾后 force push，23 文件 +197/-20）。
- **P1 修复明细**：validation.py get_user_fields/get_score_fields/get_pagination_fields 补 from flask_restx import fields（3 函数，get_common_fields 此前有局部导入所以没暴露）；cache.py cached().invalidate 改 wrapper 记录 _last_cache_key + _invalidate 闭包方法；batch_writer 补 from datetime import datetime；diagnostics 补 import time + **修复 try 块从未 import psutil 的更深缺陷**（PSUTIL_AVAILABLE 恒 True，所有 psutil 调用必崩——比 pyflakes 报的 time 更严重）；nlp_enhanced_service:2607 补 corrected_pos = text.find(corrected_name)。
- **P2 修复**：export_routes 删重复 student_id 键；users_routes 删重复 import io；15 处 f-string 去 f 前缀；scripts 4 文件补导入；test_nlp_service_comprehensive get_nlp_service 提升模块级（**从假 skip 恢复真执行，单跑 71s**）；data_sync_validation 补 NLPScoringRule。
- **验证**：pyflakes undefined name 50→1（余 1 为 test_concurrent_operations 闭包误报）/ f-string 15→0 / repeated 2→0；定向 78 passed；全量 157 文件 0 FAILED 0 ERROR；G2 OK。
- **行尾教训第三次升级**：f-string 批量修复脚本用 text-mode 写文件，把 6 个 **HEAD 为 LF** 的文件（mqtt_manager/joint_test_v2/update_db_schema/verify_class_time_lock/migrate_merge_wol_device/create_test_accounts）转成 CRLF → 1962/1290/286/396/234/168 行假 diff 进提交 → **必须用 git cat-file 对比 HEAD 实际行尾 + amend + force push**。规律：**python text-mode 写文件在 git-bash 下 LF→CRLF**（与之前 api.ts CRLF 化同类）；写文件应 open(f,'w',newline='') 或写后统一按 HEAD 行尾转换。
- **全量 pytest teardown 偶发 logging 死锁**（nlp warmup/parser daemon 线程累积）→ 100% 后 summary 偶发缺失，用例 0 FAILED 不受影响；已知限制记录在案。
- **待排期**：mqtt_manager 62 + redis_cache 28 处 print→logger；backups/ 1.2G 治理（clean 调度独立化 + 测试 tmp_path + 统一保留策略）；npm audit 需官方 registry。

## 排期修复执行（用户指令"开始排期修复"，提交 6037dbc + 29f82bc 已推送）

- **排期1 print→logger（90 处）**：mqtt_manager 62 + redis_cache_service 28 处 print 全转 logger（error/warning/info 按内容关键词三级映射；多行 print 收敛单行；flush=True 移除；模块级 logger 定义）。注意：redis_cache 的 logger 曾被脚本插进 docstring，手工修正。验证：MQTT/Redis/cache 相关 144 passed。
- **排期2 backups 治理**：① clean_old_backups 加 max_count 参数（30 天保留期 + 数量上限双策略，config.BACKUP_MAX_COUNT=10 首次真正生效）；② service_init 注册独立每天 3:00 清理任务（不再依赖备份创建成功）；③ SystemBackup.post 改读 Config.BACKUP_DIR（此前硬编码路径，BACKUP_DIR 配置从未生效）；④ test_utils.test_backup_utils 用 tmp_path 实例化 BackupManager（此前 create_backup("test") 每次全量写 189M）、test_system_routes.test_create_backup monkeypatch BACKUP_DIR。验证：25 passed，backup_test 不再新增。
- **存量清理（用户授权"按策略清理"）**：25 个 1.6G → **10 个 601M**（删 5 个 backup_test 945M 测试污染 + 10 个超量/过期 backup_full）。
- **排期3 npm audit**：npmmirror 不支持 audit API → 官方 registry 跑通。**11 漏洞 → 3**：socket.io-client 4.6.0→4.8.3（连带修 engine.io-client/ws）、vite 5.4→6.4.3（dev server 高危）、react-router-dom 6.30→7.18.2（移除 v7 废弃 HashRouter future 属性，App.tsx:347）、overrides socket.io-parser ^4.2.7（运行时高危，client 锁 4.2.6）。余 3（brace-expansion/js-yaml/nanoid）全 dev-only（eslint/postcss 生态，构建产物不含）且父包锁版本无干净升级路径 → 记录为已知工具链风险。验证：tsc 0 / build 0 / vitest 164 / eslint 0。
- **经验**：① npm audit 用 `--registry=https://registry.npmjs.org/` 可绕过 npmmirror 限制；② vite 6 / react-router 7 大版本升级对 declarative 模式项目兼容良好（仅 HashRouter future 属性移除需删）；③ 备份保留策略必须独立于备份创建调度，否则保留逻辑永不触发。


## 第六次全面评估（用户指令再次评估整个系统）

- 结果：8.9/10（历史最高，↑0.3），生产就绪，无新增 P1/P2 缺陷。
- 上次遗留全部闭环验证：5 个 P1 NameError 未复发（pyflakes undefined name 保持 1=已知闭包误报）；print→logger 90 处落地日志体系正常；backups 601M 稳定；依赖漏洞 11→3 稳定（dev-only）；teardown 死锁本次未复现。
- 闸门全绿（连续第三次）：全量 157 文件 0 FAILED / 0 ERROR / 无 Timeout；G2 68；G5 461 零漂移；tsc 0 / eslint 0 / build 0 / vitest 164；vite dev 200（react-router 7 运行时正常）；backup/unlock/export 401 + 审计日志。
- 新维度：npm audit 官方源稳定 3（brace-expansion/js-yaml/nanoid dev-only，父包锁版本无干净升级路径）；e2e 9 spec。
- 已排除非缺陷：_admin noqa（F17 404 语义）、config_validator result noqa、mqtt last_err（except 绑定局限）、global CLASS_ID 死声明（脚本级）。
- 过程教训：wmic 全量 kill python 误杀后台 pytest 且清不干净多实例 → 清理进程须按 PID/CommandLine 精确筛选，全量 kill 曾致 3 个 run.py 竞争。
- 报告：reports/PROJECT_SIXTH_EVALUATION_REPORT_2026-08-19.md


## 第七次评估：产品级（代码质量/架构/功能完整性/性能/UX）+ 开发路线图

- 视角切换：前六次做的是"工程质量闸门"（已 8.9/10 连续三次全绿），本次**不重复闸门**，改评"老师用起来顺不顺"。产出 reports/PROJECT_SEVENTH_EVALUATION_AND_ROADMAP_2026-08-19.md（只读评估，未改任何代码）。
- 评分：工程 8.9/10 维持；**产品成熟度首次量化 7.2/10**；**UX 6.5/10 = 最大短板**；性能 7.8（↓1.2，结构性风险）、架构 8.5（↓0.8，两处裂缝）。
- **架构裂缝 1（P1，根因级）**：前端"组件库与页面脱节"——36 处原生 `<table>` vs antd Table 仅 4 处、`pagination` 属性仅 1 处、`scroll={{x}}` 0 处；`VirtualList.tsx`/`OptimizedList.tsx` 已实现但**0 页面引用**；Skeleton 仅 6/56 页面落地、`loading=` 仅 9 处。→ loading/空态/分页/横滚/虚拟滚动五件事在 56 页面重复缺失。**修统一 DataTable 一处收益覆盖全站**。
- **架构裂缝 2（P2，最低成本最高收益）**：221 个索引全部由 `backend/scripts/create_indexes.py` **手动**创建，全仓无任何启动/部署脚本调用（仅 docs/STARTUP_GUIDE.md:156 列为手动工具）→ 新环境漏跑则索引全失且**测试不失败、闸门不报警**（静默性能崩塌）。建议改幂等 + 启动校验 + 新增 verify_indexes.py 纳入 run_regression.sh。
- **UX/安全 P1**：后端 **150 处** 响应 message 回传 `str(e)`（exam_import_routes:199/329/390 等），而前端 `services/api.ts:446-454` getErrorMessage **优先返回后端原始 message**、友好文案表仅兜底 → 老师直接看到 IntegrityError/SQL 表名。修法：反转优先级（按 error_code 查文案，后端 message 仅 user_facing:true 才展示）。
- **UX P1 其余**：`window.confirm` 56 处/34 文件 与自研 `useConfirmDialog`（已用于 11 页面）**双标准并存、同页混用**；长操作无真实进度（ImportExportPanel:105-554 进度条是**模拟**的）；`useAutoSave` 仅 UserList:296、`useSubmitGuard` 仅 ScoreEntry/ExamManagement → 全站几乎无草稿保护；5xx/网络无重试（api.ts:689/773 仅 401/419）。
- **性能实测基线**：首屏 gzip ≈452KB（antd 245 + vendor 128 + index 55 + css 24；dist 3.3M，56 页面全懒加载 ✅）；开发库 105 表/221 索引，user 94/score_record 3118/operation_log 3615；查询计划——时间倒序命中覆盖索引 1.2–2.2ms ✅，但 `description LIKE '%开A箱%'` **全表 SCAN 20.4ms@3.6k**（operation_logs_routes:96/98，10 万行推算 550ms+）；后端冷启动 **25–55s**（MQTT 连接确认超时 5s+10s 串行阻塞在启动路径 + jieba/BERT）。
- **性能结构性风险**：per_page 上限保护仅 **7/27 = 26%**（users_routes:138 无上限，可 ?per_page=100000 dump 全表）；`@cached_api` 仅 **12/461 = 2.6%**，且 api_cache_middleware:152 `invalidate_cache()` 无参 → 一次写清空全部 api:*；设备域 devices_routes:83/468 `sum(... for d in Device.query.all())` 全表内存过滤、:513 列表无分页、device_service:130-205 导入行级 N+1×3；NLP 推理 nlp_routes:517/539/560/653/675 在 HTTP 线程同步（Celery 已接入 15 处 delay 却未卸载 NLP）。
- **功能缺口（非模块缺失，56 页面业务闭环完整、业务代码 TODO≈0）**：请假审批无批量（Approvals:101/120 仅单条）；批量通知/成绩确认无真实进度；无草稿；管理端**移动端 0 适配**（pages/ 中 useDeviceDetection/isMobile 引用 0 次）；设备解锁两步弹窗；导入无行级错误定位；anomaly_service:445 积分归因占位。
- **路线图（4 阶段）**：一 体验基座 M1 统一 DataTable / M2 错误体系 / M3 确认+草稿统一（M1+M2 是一切前置，资源有限只做这两件）；二 高频业务 M4 批量审批+键盘流 / M5 成绩录入 / M6 通知 / M7 设备 / M8 导入导出；三 防规模悬崖 M9 分页上限+缓存扩面至15%+失效精准化 / M10 NLP异步化+MQTT移出启动路径 / M11 索引闸门；四 M12 移动适配（依赖 M1）/ M13 首屏瘦身+归因补全+无障碍。
- **纪律**：M1 迁移 36 处 table 须"先 5 页试点→跑闸门→再批量"（沿用 F17 渐进纪律）；M2 改 150 处文案每批必跑 G5 --strict（只改 message 内容不改结构）；缓存只加在容忍秒级延迟的只读聚合，成绩明细不加。
- **方法论教训（重要）**：子代理初查有 3 处误判被抽查纠正——① "OperationLog.created_at 缺索引"实为存在（真问题是索引脚本未纳入部署）；② "useAutoSave/useSubmitGuard 零调用"实为 1–2 页在用（真问题是推广不足）；③ "useConfirmDialog 未使用"实为 11 页在用（真问题是双标准并存）。**结论：子代理结论必须抽查复核后才入报告**，尤其"某物不存在"类断言要用运行态/DB 实测验证。
- 过程：为取性能数据启动过后端（admin/admin123 密码不对，改用 sqlite EXPLAIN QUERY PLAN 取证），评估后按 CommandLine 精确清理进程。


## M1 统一 DataTable 落地（用户指令"按照报告开始排期修复"，头号优先 M1）

### M1-1 基础组件（新建 4 文件 + 3 处导出 + App 根挂 Provider）
- `frontend/src/components/data-display/DataTable.tsx`：antd 兼容 `columns`/`dataSource` 接口，内置五件套——① loading 骨架（TableSkeleton）② 空态（feedback/EmptyState 接 empty prop）③ 受控/非受控分页（20/50/100/200，**硬上限 200**，为 A4 预埋）④ `overflow-x-auto` 横滚 ⑤ 行数 ≥ virtualThreshold(默认 200) 且非受控时自动切 `VirtualList`（定高 rowHeight 默认 52）。另支持排序（sorter/受控 sortField+onSortChange）、选择（selectable/selectedRowKeys/onSelectChange）、rowActions、onRowClick、rowClassName、error/empty prop。
- `frontend/src/hooks/useTableUrlState.ts`：URL query 持久化 page/pageSize/sort（报告"排序态持久化 URL"要求）。
- `frontend/src/components/ui/ConfirmDialog.tsx`：`ConfirmProvider` + `useConfirm(options)=>Promise<boolean>`（type: danger|warning|info|success），**Provider 未挂载时降级 window.confirm**（防御性，App 根已挂 → 生产永不触发）。ConfirmProvider 挂到 `App.tsx` 根。
- `DataTable.test.tsx` 7 用例（loading/空态/分页/内部排序/虚拟滚动自动切换）全绿。
- ⚠️ lint 教训：`useConfirm` 返回稳定函数但 exhaustive-deps 与"不加 deps"规则矛盾 → **用 `const confirmRef = useRef(confirmFn); confirmRef.current = confirmFn;` 包裹**，回调里 `await confirmRef.current({...})`，ref 不进 deps。

### M1-2 迁移（5 试点 + 批量 19 页，全部通过）
- 试点：OperationLogs（服务端受控）、Approvals（卡片列表→表格+2 confirm）、DeviceManagement（客户端+1 confirm）、UserList（选中+排序+1 confirm，columns useMemo 必须放 early return **之前**）、ScoreRecords（无表→学生列表区改 DataTable onRowClick+rowClassName 高亮；Excel 导出 HTML 字符串里的 `<table border='1'>` 是假阳性勿动）。
- 批量（6 并行代理，每代理只碰分配文件+各自跑 tsc/eslint）：ClassManagement/CommitteeList/CourseSchedule(课表矩阵→DataTable)/TimeRuleList/HomeworkCheck/RemoteNotify/ExamManagement/FirmwareManagement(2)/ScoreEntry(动态科目列)/MentalHealth/AttendanceManage/Diagnostics/OpsCenter/RankBoard(2)/SecurityAudit/FrontendTelemetry(2)/AlgorithmAnalysis(4)/NLPManagement(4)/PermissionManagement(4)。
- **批量坑**：① 并行代理互相看到对方写到一半的文件 → 交叉 tsc 报错是瞬态，最终以本人全量重跑为准；② 受控分页下后端固定 per_page 与 DataTable 页大小选择器不一致会分页错乱（ClassManagement 加 perPage 参数 / RemoteNotify `pageSizeOptions={[20]}` 锁死）；③ `useConfirmDialog` 死代码（deps 引用但未调用）换 useConfirm 后必须连带删除；④ `empty.icon` 枚举无 'calendar' 用 'file'。

### M1-3 闸门与验收指标
- **全量闸门全绿**：tsc 0 / eslint 0 / vite build 0 / vitest **171 passed 0 failed**（20 文件，含覆盖已迁移页面的既有测试）/ DataTable 7/7。
- **验收指标**：原生 `<table>` 文件 36 → **3**（DataTable.test.tsx 断言 + OptimizedList 遗留组件 0 页面引用仅再导出 + ScoreRecords Excel 字符串假阳性），**达标 ≤5**；`window.confirm` 56 → **1**（仅 ConfirmDialog 兜底，生产不触发）；DataTable 覆盖 **24/56 页面**（内置骨架屏 → 骨架覆盖面 ≥30/56 达标）；`loading=` 显式 19 页（列表页达标，非列表页 37 页以 early-return loading 为主，覆盖率指标待 M1 收尾核查）。
- 未动：OptimizedList（遗留组件，可后续删除或内部改 DataTable）；M2（错误文案 150 处）起待用户排期。

## M1 遗留清理 + M2 错误文案链路（用户指令"先处理遗留问题，然后继续推进M2"）

### M1 遗留清理
- **OptimizedList.tsx 删除**（死代码：仅 components/index.ts:90 + data-display/index.ts 再导出，0 页面/测试引用；ColumnDef 类型与 types/index.ts 独立定义无关）→ 原生 `<table>` 文件 36→**2**（DataTable.test.tsx 断言 + ScoreRecords Excel 字符串假阳性）。
- **useConfirmDialog 双标准清零**：7 文件 11 处全部是死代码——CategoryList/DeviceGroup/FirmwareManagement 裸调用；ExamManagement/ScoreEntry 的 `show()` 从未调用（仅 deps 引用）；ClassManagement 的 Modal 从未被打开（未解构 show、confirm() 无调用方）。全部删除（含 ClassManagement 1224-1303 整块死弹窗 UI + deps 清理）。教训：**useModal 的 useConfirmDialog 是 state-only 无 UI 的半成品，页面用它必然死代码；统一用 ConfirmDialog 的 useConfirm**。

## M3 确认+草稿统一 + M9 API 保护与缓存（用户指令"按报告继续 M3 和 M9"）

### M3（确认统一已闭环；草稿/防重落地）
- **useAutoSave 扩展**：新增 `draftAvailable`（挂载检测可恢复草稿）+ 内置 `beforeunload` 离开拦截（hasUnsavedChanges||isDirty||draftAvailable 时阻止）；clearDraft/restoreDraft 同步清标志。API 向后兼容（UserList 不受影响）。
- **4 录入页草稿接入**：ScoreEntry(key=score-entry，draftData=考试/班级/成绩/pendingChanges/batchSubject，恢复按字段 dispatch 填回，空草稿静默清理，提交/批量确认成功 clearDraft——**验收"成绩录入中途刷新可恢复"达成**）、ExamManagement(exam-form)、AttendanceManage(attendance-entry)、RemoteNotify(remote-notify)。统一 amber 恢复提示条（恢复/放弃）。
- **useSubmitGuard 推广**：13 页接入（Modal 表单提交/批量操作/主 CRUD：CategoryList/RankRuleList/TimeRuleList/RuleList/CourseSchedule/CultureBoard/HomeworkCheck/StudyGuide/ImportConfigManagement/DeviceGroup/DeviceManagement/PermissionManagement/SubjectManagement）；15 页已有自身防重跳过（ActivityManage/MentalHealth/StudyGroups/Notifications/FirmwareManagement/CommitteeList/DutyRoster/ParentContact/SeatingChart/ClassManagement/DataSyncPage/WakeOnLan/Settings/Approvals/OperationLogs）。

### M9（分页上限 100% + 缓存 15%+ + 失效精准化）
- **`utils/pagination.py::get_pagination(default, max_per_page=200)`** 新建；`api/` 41 处手写解析替换 + student_routes 2 处保留 page_size 别名但钳制 1-200；per_page 上限保护 26%→**100%**（system_routes 原 500 上限收紧为 200）。
- **缓存扩面**：`@cached_api` 12 → **96 处**（41 文件，列表 ttl=30/聚合 ttl=60；强一致单条详情/成绩明细/导出/实时端点不加；写路径补约 40 处 `invalidate_cache("api:/api/<域>/*")`）。
- **失效精准化（真实 bug 修复）**：`api_cache_middleware.invalidate_cache` 曾把前缀拼成 `api:api:/api/users/*`（键实为 `api:/api/users/*:{hash}`）→ **按前缀失效从未命中**，全靠 TTL 兜底。修复：pattern 已带 `api:` 前缀则不重复拼接；`invalidate_user/device/rule_cache` 统一传 `/api/<域>/*` 形态。
- **预加载**：审批列表（Approval.user）、设备列表（class_info/admin）已存在 joinedload，无需改。
- **闸门**：G5 --strict 461/461 零漂移；定向 pytest 75 passed（分页域）+ P2 自验 50+253 passed；前端 tsc 0 / eslint 0 / vitest 176 / build 0。

### M2 错误文案链路（报告 P1：后端 str(e) 直吐用户）
- **前端防御层（核心，一处覆盖全部）** `frontend/src/services/api.ts`：
  - 新增 `errorCodeMessages` 友好文案表（BAD_REQUEST/NOT_FOUND/UNAUTHORIZED/FORBIDDEN/INTERNAL_ERROR/...15 个码）；
  - 新增 `TECHNICAL_ERROR_PATTERNS` 启发式（Python 异常/SQL/文件路径/Traceback/IP）`looksLikeTechnicalError()`；
  - `getErrorMessage` 优先级反转：① error_code 命中文案表 → 展示 ② 后端 message 非技术 → 透传 ③ 技术报错 → 屏蔽 + logger.warn，HTTP 状态兜底。
  - `getErrorMessage` 导出 + 新增 `src/services/__tests__/getErrorMessage.test.ts` 5 用例。
- **后端批改（message 内容只改结构不变，每批跑 G5 --strict）**：
  - `algorithm_routes.py` 66 处 `message=str(e)` → `message="算法计算失败，请稍后重试"` + `logger.error`（脚本化，保留行尾 CRLF/LF）；
  - 其余 21 个路由文件 62 处 `f"前缀: {str(e)}"` → 保留前缀剥技术后缀 + logger；`api/` 响应内 str(e) 210→**1**（algorithm_routes:1441 业务 ValueError"不支持的导出类型"保留透传）。
  - **脚本坑（重要）**：logger 行插入逻辑用"首个非 import 行"定位 import 块结尾，遇**多行 import**（`from x import (\n a,\n)`）会把 logger 插进括号内 → SyntaxError，热重载崩后端。涉及 devices_routes/import_export×3/firmware_routes/notify_template_routes/rules_routes 共 7 处手工修复。**教训：批改脚本必须先 py_compile 全部改动文件，不能只信单文件 diff**。
- **闸门**：G5 --strict **461/461 零漂移**（改完重启后端后跑）；定向 pytest 52+93+算法全过；前端 tsc 0 / eslint 0 / vitest **176 通过**（+5 新用例）。
- 剩余：services/utils/models 里 str(e) 多为日志/再抛（非响应 message）无需改；前端 user_facing 字段方案放弃（G5 结构约束，用 error_code+启发式替代）。

## M10 异步化与启动优化 + M11 索引闸门（用户指令"提交里程碑 commit，然后继续 M10 和 M11"）

- **里程碑 commit 01587fc**：M1-M9 全部工作（116 文件 +10396/-7333，pre-commit 自动 py_compile 53 文件通过）。

### M10（冷启动 55s → **16s** <25s 验收达成 ✅）
- **NLP 预热移出启动路径**：`service_init.py::init_nlp_service` 改用已存在的 `nlp_service.async_warmup()`（daemon 线程）+ 解析器预加载移入后台线程。首请求早于预热完成则懒加载（一次性代价，可接受）。
- **MQTT 连接等待收紧**：`mqtt_manager._create_and_connect_client` 等待上限 min(cfg timeout, 5)s（connect_async + loop_start 已带 paho 自动重连，无需同步等 15s）。
- **torch 链懒加载（最大头）**：实测 `services.nlp_ml_service` import 23.7s、`nlp_enhanced_service` 冷导入 21.8s（同一 torch 链）——`nlp_routes.py` 模块级 import 触发 → 路由注册时 torch 加载。改 `_get_parser()`/`_get_ml_service()` helper 函数内 import，13+12 处调用替换。**冷启动 41s→16s**。
- **NLP 推理并发闸门**：`nlp_routes.py` 模块级 `threading.BoundedSemaphore(4)` + `inference_slot_guard` 装饰器（7 个推理端点：/parse /execute /batch-parse /sentiment /model/predict /predict-multi /ensemble-predict），槽位耗尽立即 503 `INFERENCE_BUSY`（不排队占满 worker）。
- 验证：NLP 定向 24 passed；MQTT 测试过；G5 461/461 零漂移。

### M11（索引纳入部署闸门 ✅）
- `scripts/create_indexes.py` 重构：抽共享清单 `get_all_indexes()`（create/verify 单一来源）+ 新增 `verify_indexes()`（只读核对，返回缺失）+ 入口 `--verify`（缺失 exit 1）。**修复脚本失效 import**：`from app import app, db`（app 包已不导出 db）→ `from app import app` + `from models import db`——脚本此前从未被调用、早已 ImportError，正是"索引与部署脱钩"裂缝的实锤。
- **补建开发库缺失 2 个索引**：alert.ix_alert_device_resolved / ix_alert_created_desc（F9-A 合并后新增、开发库漏建）——闸门价值直接兑现。
- 新脚本 `scripts/verify_indexes.py`（包装 verify + exit code）；`scripts/run_regression.sh` 新增 **5/5 索引闸门步**（注意：步骤 3 已 cd backend，相对路径用 `scripts/verify_indexes.py`，别写 `backend/scripts/`——第一次跑就踩了这个坑）。
- **启动校验**：`service_init.py::init_index_check` 启动时抽查 10 表 14 个核心索引，缺失打印醒目告警"请运行 python scripts/create_indexes.py --create"。
- 回归全绿：run_regression.sh 五步全过（G2 68 / G5 零漂移 / 契约 2 / 关键路由 33 / 索引 OK）。
- 未做（报告"顺带"项）：operation_logs 前置通配 LIKE 优化（FTS 已有基础设施，风险中等，留后续）。
- 后端当前未运行（测试临时进程已清理），用户按需 `python run.py --env development` 启动。

## M4 请假审批模块（用户指令"提交后继续，先不进行移动端适配"——提交 2f44dd1 后推进阶段二首个模块）

- **后端批量端点**（api/scores/approvals_routes.py）：
  - 抽取共用 helper `_execute_approve/_execute_reject`（单条/批量同源：权限+pending 校验、service 事务、学生通知 create_approval_result_notification、MQTT 推送、ClassTimeChecker 上课时间拦截、admin 通知、缓存失效）；单条端点改为薄包装（响应逐字节不变，测试确认）。
  - 新增 `POST /api/approvals/batch-approve`、`POST /api/approvals/batch-reject`（body {ids, comment?}，逐条 try/except 隔离，返回 {results:[{id,success,message}], success_count, failed_count}，上限 100 条）。
  - OpenAPI 快照更新（461→463 端点，--update 自动备份 .bak_）；test_approvals_routes.py 追加 4 用例（批量成功/拒绝理由落库/部分失败/空 ids）。
- **前端 Approvals.tsx**（代理交付）：DataTable selectable 批量选择 + 批量通过/拒绝按钮；批量拒绝 Modal 带 6 条常用理由模板下拉（即选即填可编辑）；失败详情条（成功 X/失败 Y + 失败项一键重试）；键盘流 J/K 移动（rowClassName 高亮）+ Y/N 通过/拒绝（忽略输入框聚焦）。
- **闸门**：run_regression.sh 五步全过；G5 463/463 零漂移；approvals 测试（含 4 新用例）通过；前端 vitest 176 / build 0 / tsc / eslint 0。
- 后端测试进程已清理（5000 空闲）。

## M5 成绩录入模块（用户指令"继续按照建议推进"）

- **真实进度+分批提交**（ScoreEntry.tsx，代理交付）：
  - 通用 `runBatched(items, fn)`：每批 20 条 Promise.allSettled 并发、逐批更新进度、cancelBatchRef 可中断；4 个提交函数（saveAll/batchConfirm/batchDelete/batchReset）全部接入。
  - 新增 reducer action `REMOVE_PENDING_CHANGE`：保存成功逐条移除并同步本地 scores（带回 id），失败项保留在 pendingChanges 供重试；toast + 前 5 条失败红色详情条（可关闭）；有失败不刷新（避免清掉待重试项）。
  - 进度条 UI（保存条卡片内）：processed/total + 预计剩余秒数 + 取消按钮。
- **录入体验**：
  - 跳格：input 加 data-sid/data-subject + `focusCell`（CSS.escape 规避科目特殊字符）；Enter/Tab 保存后跳下一格（末列→下一学生首列）、↑↓ 切相邻学生同科目。
  - 粘贴填充：onPaste 按 `\r?\n` / `\t|,|，` 拆分，向右向下批量填充，逐格 0-100 校验复用 handleScoreBlur。
  - 越界即时红框：onInput classList.toggle('border-red-500')（保持非受控 defaultValue 模式）。
  - 键盘提示行：`Tab/Enter 跳格 · ↑↓ 切学生 · 粘贴可批量填充`。
- 后端无改动（前端分批调现有 create/update）；闸门：vitest 176 / build 0 / tsc / eslint 0。

## M6 通知下发模块（用户指令"继续推进所有修复"）

- **发送前预览（防误发）**：后端新增 `GET /api/remote_notify/preview`（notification.send 权限，只读：设备总数 / 在线数 last_heartbeat 5 分钟且 is_active / 最近活跃前 20 台 device_id+class_name+last_seen）；前端 RemoteNotify device/broadcast 发送前调 preview（try/catch 静默降级）→ 确认弹窗展示"设备总数 N · 当前在线 M"+ 展开名单（≤20 台）+ 上课拦截/强制发送提示；确认才发。
- **失败重发**：发送失败 toast 带原因 → confirmRef"是否重试"，重试复用同一 notifyData 直发（不再弹预览）。仅 device/broadcast 路径。
- **偏差说明**：报告原案"批量发送改 Celery 后台任务+进度轮询"——现状 MQTT publish 单次广播（毫秒级、无逐台循环），Celery 化反而绕路，故以预览+重发落地（scheduled_notify 已走 Celery 后台）。
- api.ts 加 remoteNotify.preview()（skipCache:true）；G5 快照 464 端点零漂移；闸门：run_regression.sh 五步全过 + vitest 176 / build 0 / tsc / eslint 0。

## M7 设备管理模块（用户指令"继续推进所有修复"）

- **stats 在线数 count() 化**（devices_routes.py:465 区域）：`online = sum(1 for d in Device.query.all() if d.is_online)` 全表内存遍历 → `Device.query.filter(Device.last_heartbeat >= now-60s).count()`（带 ix_device_last_heartbeat 索引；阈值与 is_device_online 一致 60s）；offline 加 max(0,) 防负。
- **导入 N+1 → 批量预取**（device_service.import_devices，代理交付）：all_rows 预扫描收集 device_ids/class_names/admin_names（strip 去重）→ 三 map（device_map/class_map/admin_map，admin 先 real_name.in_ 再 username.in_ setdefault 补齐保持语义）→ 循环内 map.get 替代逐行 3 查询；返回结构/错误消息/校验链不变。
- **解锁一步到位**（DeviceManagement.tsx，代理交付）：抽 `performAction(action, device)`（调 api.devices.remoteControl + toast）；行内新增 开A箱(绿)/开B箱(红) 快捷按钮 → confirmRef 二次确认 → 一步开箱（权限 device.edit 照抄行内）；远程操作 Modal 四选项保留（高级入口）。
- 报告原案"列表加分页/joinedload"已存在（后续批次已做）；WebSocket 实时性现状已有（未做前端轮询替代改造，说明现状合理）。
- 闸门：run_regression.sh 五步全过；devices pytest 27 passed；vitest 176 / build / tsc / eslint 0。

## M8 导入导出收口 + M13 首屏瘦身（用户指令"继续推进所有修复"）

### M8（现状收口，无代码改动）
- 报告点名的 ImportExportPanel（模拟进度条）已不存在；导入导出分散到各域页面。
- 已覆盖：openpyxl read_only 流式（非 pandas）；行级错误 {row, message, row_data, error_fields}（ClassManagement/ScoreEntry 等展示 + 一键导出错误行 api.export.errors）；无模拟进度条。
- 大导出后台化：数据量 3k 级秒级完成，收益低，**说明现状合理不改造**（与 M6 同款偏差处理）。

### M13 首屏瘦身（gzip 452KB → **~213KB**，降 53%，达成 ≤380KB ✅）
- 根因：DashboardSkeleton/UserListSkeleton 用 antd（Skeleton/Card/Table/Statistic）→ 首页共享链强制拉 antd 785KB（manualChunks 整包）。
- 改法：两个骨架屏改为纯 div + tailwind animate-pulse 自研（src/components/skeletons/*.tsx）；antd 只剩 ImportConfigManagement/SemesterReport 两个懒加载页面引用（页面级加载，不再进首屏）。
- 实测首屏 gzip：index.html 1 + index 54 + vendor 127 + Dashboard 6 + css 23 ≈ **213KB**。
- 闸门：tsc / eslint / vitest 176 / build 0 全绿。

## 剩余小项收口（用户指令"除了移动适配，完成剩余小项"）

- **A18 异常积分归因补全**（anomaly_service.py:445 区域）：sudden_change 用 z_score（原有）；**trend_anomaly 用 total_change（连续同向总变化，带符号）**；**group_anomaly 用 deviation（与班级均值标准差偏差）**——全部 int() 归一防前端 NaN。测试 0 FAILED。
- **M11 顺带 operation_logs LIKE 优化（收口说明，无改动）**：`@cached_api(ttl=30)` 已在列表端点（M9 已覆盖高频筛选）；前置通配 LIKE 在 SQLite 无索引手段（FTS5 porter 不支持中文分词，fulltext_search.py 自身注释确认），预聚合计数字段需新增 result 列（模型无）——技术局限，不改，避免过度工程。
- **A19 无障碍基线**（代理交付）：Header 5 个纯图标按钮补 aria-label（清除搜索/切换主题/通知/通知设置/用户菜单）；ScoreEntry 成绩单元格输入框补 `aria-label={学生名 的 科目 成绩}`。只加属性零逻辑改动。
- 闸门：anomaly pytest 0 FAILED；run_regression.sh 五步全过；vitest 176 / tsc / eslint 0。

## 第八次项目评估（用户指令"现在再次评估整个项目"）

- 产出 reports/PROJECT_EIGHTH_EVALUATION_REPORT_2026-08-19.md：综合评分 8.0→**9.1**（架构 9.2/性能 9.3/功能 9.0/工程 9.0/UX 8.8）。
- 验收指标 12 项全部达成/超出：confirm 3（兜底）/原生 table 4/str(e) 1/per_page 100%/cached_api 96(20.7%)/冷启动 **10s**/首屏 gzip **190KB**/索引闸门 ✅/五步回归全绿/vitest 176。
- 运行态：4 条关键查询 EXPLAIN 全部索引命中；G5 464/464 零漂移；DB 105 表/207 索引。
- 规模：后端 476 .py/11.4 万 LOC；前端 192 .tsx/.ts。
- 遗留（全 P3）：M12 移动端（用户跳过）、RemoteNotify 原生 table 规范偏差 1 处（M6 引入）、useConfirmDialog hook 定义残留（页面零用）、antd 785KB chunk（仅 2 懒加载页）、operation_logs LIKE（SQLite 技术局限）、FTS 中文受限。
- 评估用后端进程已清理；报告未提交（待用户决定是否 commit）。

## 遗留问题全清零 + 第八次报告提交（用户指令"解决全部遗留问题，然后提交报告，一并 commit"）

- **RemoteNotify 预览名单原生 table → div 网格**（grid-cols-12 三列，样式等价）：原生 table 文件 4→3（仅 DataTable 自身+测试断言+Excel 假阳性，全部合理）。
- **useConfirmDialog 死代码清理**：useModal.ts（UseConfirmDialogResult+函数）、useUndoRedo.tsx（ConfirmDialogOptions+函数）删除，hooks/index.ts 两行导出移除；README.md 同步清理（useConfirmDialog 节 + OptimizedList 节 + 目录树 + changelog + 性能建议改 DataTable）——文档残留也归零。
- **antd chunk 按需化（重要配置经验）**：vite manualChunks 去掉 antd 强制整包。**踩坑**：直接删分支 → antd 落入 'vendor' 兜底 → vendor 414→1203KB、首屏 gzip 190→436KB（污染首屏）。**正解**：antd 分支 `return undefined`（既不入 antd 整包、也不入 vendor，交给 rollup 自动分包）→ 首屏保持 190KB + antd 共享 chunk 785→**384KB**（tree-shake 生效，-51%，仅 2 懒加载页加载时拉取）。
- 闸门：tsc/eslint/vitest 176/build 0 全绿；残留计数复验归零。

## 第九次项目评估（用户指令"现在再次评估整个项目"）

- 产出 reports/PROJECT_NINTH_EVALUATION_REPORT_2026-08-19.md：综合评分维持 **9.1**，第八次遗留全清零确认。
- 复验：confirm 3（兜底）/原生 table 3（全合理）/str(e) 1（业务保留）/cached_api 96(20.7%)/get_pagination 41/TODO 0/useConfirmDialog+OptimizedList 0/冷启动 **10s**/首屏 **214KB**（含 CSS）/G5 464/464/五步回归全绿/vitest 176/build 0/git 干净（15 commit 闭环）。
- 无 P0-P3 待办（仅 M12 移动端用户跳过）；建议接 CI + 生产部署前 create_indexes + 五步回归。
- 评估用后端进程已清理。

## CI 接入 + UX 优化（用户指令"接入 CI 固化回归基线，然后继续完成用户体验优化"）

- **CI 接入**：
  - `scripts/run_regression.sh` 改可移植：PYTHON_BIN 环境变量优先 → 本机默认系统 3.11 → python3/python 探测；本地改造后五步回归全绿复验。
  - 新建 `.github/workflows/ci.yml`：backend job（ubuntu + py3.11 + redis service + pip install requirements + create_app 建表 + create_indexes --create + run_regression.sh【PYTHON_BIN=python】）+ frontend job（node20 + npm ci + vitest + build + lint）。**不装 torch**（requirements 中为可选注释；M10 懒加载后契约/路由测试无需 torch）；G5 走"跳过不阻塞"分支由 test_api_envelope 兜底。
- **UX 优化（3 项，tsc/eslint/vitest/build 全绿）**：
  - **修复失效的 Shift+? 快捷键**：新建 `KeyboardShortcutHelp.tsx`（data-help-modal + 快捷键列表 Modal：Esc 关弹窗 / Shift+? 帮助 / Ctrl+⌘K 搜索 / J K Y N 审批 / Tab Enter 跳格 / ↑↓ 切学生 / 粘贴填充）；挂 App 布局（App.tsx + layout/index + components/index 导出）。
  - **动态标签页标题**：Header unreadCount → document.title（"(N) 积分管理平台"，N>9 显示 9+）。
  - **Header 帮助按钮**（? 图标，data-help-modal 触发，可发现性）。

## CI 首跑排障 + 全绿（推送 origin-ssh 验证）

- **push origin-ssh refactor/F17**（SSH-over-443，gh CLI 已认证 DLLMY），CI 实际跑起来。
- **首跑失败 3 问题**（884356f 修复）：
  1. **create_indexes.py 清单列名与模型漂移 4 处**（score_record.user_id→student_id、scores.subject→subject_id×2、notification.user_id→student_id）——本地库索引为历史版本所建掩盖缺陷，CI 干净库 CREATE INDEX 失败（no such column）；已在本地模拟干净库（删索引→重建→verify 全绿）验证修复。**教训：脚本路径从未在干净库验证过，本地 verify 通过≠脚本可用。**
  2. **pytest-timeout 缺失**（CI 无插件，--timeout 参数报错）→ requirements.txt 补 `pytest-timeout>=2.3`。
  3. **vitest forks pool 在 Node 20 崩溃**（webidl.markAsUncloneable）→ ci.yml setup-node 20→22（与本地 managed 22.22.2 一致）。
- **重跑全绿**：run 32271796600 ✓ 前端 1m11s（vitest/build/lint）+ ✓ 后端 3m53s（依赖/建表建索引/五步回归 G2-G5-契约-关键路由-索引闸门）。
- CI 回归基线正式固化（push refactor/* 自动触发）。

## 第十次项目评估（深度审查六大维度，用户指令"全面审查"）

- 产出 reports/PROJECT_TENTH_EVALUATION_2026-08-19.md。六维评分：代码质量 8.8 / 架构 9.0 / 功能 9.2 / 安全 8.6 / 性能 9.2 / 可维护 8.9。
- 无 P0/P1；P2×2：① token 存 localStorage → 建议 HttpOnly cookie（XSS 面）；② services→api 反向依赖 2 处（academics_service 懒 import exam_import_routes 的 ScoreImportHelper、score_record_service 懒 import rank_routes——辅助函数应下沉 services）。
- P3 若干：export/devices 3 处 Device.query.all()+is_online 全表遍历（M7 只修了 stats）；长函数 course_schedule.post 487 行；any 94 处（api.ts 44 合理）；pyflakes 3 处；opencv/numpy 版本不匹配（pip check）；npm audit 因 npmmirror 不支持无法跑。
- 亮点：eslint 全量 0 错；0 循环依赖；裸 except 仅 scripts 工具；SQL 全参数化；权限装饰器 581 处；CI 全绿。
