# 2026-08-13

- 前端算法分析页增强：新增「学生画像」Tab（用户选「完善算法相关 → 前端算法分析页」方向）。
  - 背景：AlgorithmAnalysis.tsx 原有 8 个 Tab 全部只消费**批量**接口（按班级）；上一轮修复的 7 个单用户算法接口（getPrediction/getScorePredict/getRiskPredict/getUserAnomaly/getSuddenChange/getTrendAnomaly/getGroupAnomaly）此前完全未被页面使用，属"假契约"隐患。
  - 改动（frontend/src/pages/AlgorithmAnalysis.tsx）：
    - TABS 数组加 `studentProfile`（图标 UserCircle）。
    - 新增状态 students / selectedProfileUserId / profileLoading / profileError / studentProfile；类型导入补 PredictionResult/ScorePredictResult/RiskPredictResult/AnomalyResult。
    - `loadStudents`（api.users.getAll 映射，复用 ruleApplication 的 studentList 写法）、`loadStudentProfile`（Promise.all 并行调 7 个单用户接口、按 predictionDays/recommendDays/anomalyDays 传参）。
    - `renderStudentProfile()`：学生下拉选择器 + 积分预测/成绩预测卡（复用 getTrendIcon/getTrendColor）+ 风险评估卡（风险等级/因子/建议）+ 异常检测 4 卡（综合/突变/趋势/群体偏离，对"无异常"优雅显示"正常"）。
    - 进 Tab 时经独立 useEffect 加载学生列表；内容区加分发。
  - 验证：eslint 0 error（仅既有 no-console 警告）；`vite build` 成功（BUILD_EXIT=0，AlgorithmAnalysis chunk 73.87kB）。
  - 提交 b78297d 并推送（用户确认"提交并推送"）。

- 新增算法维度：成绩波动归因（用户选「新增算法维度 → 健壮性/边界 → 单元测试」第一步）。
  - 后端 `backend/services/attribution_service.py`（新）：
    - 纯函数核心 `attribute_score_change(score_before, score_after, factor_values, sensitivities, factor_labels)`：无 DB 依赖，将成绩净变化按各维度影响(灵敏度加权)分配到贡献度；处理 None 成绩/影响相互抵消(等分到净变化)/空因子/浮点漂移兜底(贡献度和≈净变化)。返回 has_data/confidence/factors/summary。
    - DB 包装 `analyze_score_attribution(user_id, days=30)`：近/前期双窗口取 学业成绩(Score⊕Exam.date 均值)、行为积分(ScoreRecord 日均)、出勤率(Attendance)、作业提交率(HomeworkSubmission)；置信度按数据充分度估算。
    - 灵敏度 SENSITIVITY={behavior:1.5, attendance:30, homework:25}（仅相对权重，非因果，结果属相关性归因）。
  - 路由 `backend/api/algorithm_routes.py`：`/api/algorithm/attribution/<int:user_id>`（requires_permission algorithm.view），import AttributionService。
  - 前端：types 加 ScoreAttributionResult/ScoreAttributionFactor；api.ts 加 getScoreAttribution + normalizeScoreAttribution；AlgorithmAnalysis 学生画像页加「成绩波动归因」卡（总变化/前后分/置信度 + 各因子贡献度条形）。loadStudentProfile 并行多取 1 路。
  - 健壮性：后端纯函数/DB 层对缺数据、单条、除零、影响抵消、空因子均防御；前端 normalize 兜底。
  - 测试 `backend/tests/test_attribution_service.py`：10 passed（纯函数 6 + DB 集成 3 + 路由 1）。覆盖正常归因、缺成绩、影响抵消、空因子、单因子负向、集成(含 FK 父表 ClassInfo/HomeworkAssignment)、学生不存在、无考试数据、路由 200。
  - 验证：py_compile OK；pytest 10 passed；eslint 0 error（清掉未用导入 ScoreAttributionFactor）；vite build 进行中。
  - 未提交（待用户确认）。

- 预测置信区间（用户选「新增算法维度 → 健壮性/边界 → 单元测试」第二步）。
  - 关键发现：后端两 service **早已实现** confidence_interval——score_predict 返回 `[lower,upper]` list（L199-233，std_dev=5+score_std*0.5，2σ 带宽）；prediction 返回中点累计水平区间 tuple（L125-132，2σ√n 带宽）。前端 PredictionResult/ScorePredictResult 与 normalize 均未消费 → 经典"假契约"。本轮=前端真正接入区间可视化（非新增后端算法）。
  - 前端改动：
    - types：PredictionResult/ScorePredictResult 加 `confidence_interval?: [number, number]`。
    - api.ts：normalizeUserPrediction/normalizeUserScorePredict 解析 raw.confidence_interval（list/tuple 长度2）→ `[number,number]` 或 undefined，缺失/长度不符兜底。
    - AlgorithmAnalysis：renderScoreCard 加 `interval` 参数 + 区间带可视化（浅蓝带宽 + 预测蓝点，量程包住预测值与区间端点）；学生画像两处调用传 `prediction/scorePredict.confidence_interval`。
  - 健壮性已在后端内联（方差0→区间收敛单点 lower==upper；历史<7→ 不返回该键）；前端 normalize 兜底 undefined。
  - 测试 `backend/tests/test_prediction_confidence.py`：5 passed（成绩预测区间结构 predicted 落在区间内 / 积分预测正常 lower<=upper / 不足数据无键 / 零方差收敛单点 / 路由返回）。坑：ScoreRecord 无 rule_name 字段（初版误加致 TypeError，已移除）。
  - 验证：py_compile OK；pytest 5 passed；eslint 0 error（仅既有 no-console）；vite build 进行中。
  - 未提交（待用户确认）。

- 多维风险分（用户选「继续完善算法 → 多维风险分」方向，本轮第三步）。
  - 关键发现：后端 `risk_predict_service.predict_risk` **早已在 risk_details 内计算三维子风险**（academic/behavior/attendance，各带 risk_level/risk_score/factors），综合分由三者加权得到（L580-636）。但前端 RiskPredictResult 与 normalizeUserRiskPredict 只消费 overall，三维子维度完全未接到页面 → 经典"假契约"。路由 `/api/algorithm/risk-predict/<id>` 已返回 risk_details（data 信封内），**无需改后端 schema/路由**。
  - 前端改动：
    - types：新增 `RiskSubRisk{key,name,level,score,factors}` 接口；`RiskPredictResult` 加可选 `sub_risks?: RiskSubRisk[]`。
    - api.ts：import 补 RiskSubRisk；`normalizeUserRiskPredict` 按固定顺序映射 `raw.risk_details`(academic/behavior/attendance)→ sub_risks 数组（每个子项 level 默认 low、score 经 toNumSafe、factors 取 description/factor/name），缺失 risk_details 兜底空数组。
    - AlgorithmAnalysis 学生画像风险卡：在等级徽章下方加「多维风险分」区块，三维横向条形（红=高/黄=中/蓝=低，宽度按 score 封顶 100%），无 sub_risks 优雅跳过。
  - 健壮性已在后端内联（各子维度 level 默认 low、score 默认 0）；前端 normalize 全面兜底。
  - 测试 `backend/tests/test_risk_details.py`：5 passed（结构三维齐全+level 合法+score 非负 / 无记录用户不崩且结构完整 / 用户不存在优雅返回 error / 健康活跃用户三维均 low / 路由返回 200 且 data.risk_details 含三维）。
    - 坑1：初版断言"无记录用户三维均 low"失败——后端对**完全不活跃**用户按设计将 behavior/attendance 升高（inactivity 是真实风险信号），故改为只断言结构与 level 合法。
    - 坑2：Attendance.class_id 为 NOT NULL 外键，健康用户测试需先建 ClassInfo 行并传 class_id（同 attribution 测试）。
  - 验证：py_compile OK；pytest 5 passed；eslint 0 error；vite build 成功（BUILD_EXIT=0，AlgorithmAnalysis chunk 78.36kB）。
  - 未提交（待用户确认）。

- Tab 自动滚动修复（方案 C，用户截图反馈"找不到学生画像 Tab"）。
  - 根因：AlgorithmAnalysis.tsx 的 TABS 共 9 个，学生画像排第 9（最右），Tab 容器 `overflow-x-auto` 把其裁出可视区，用户误以为页面不存在。
  - 改动（frontend/src/pages/AlgorithmAnalysis.tsx）：React 导入补 `useRef`；新增 `tabNavRef` + `useEffect`（activeTab 变化时 `scrollIntoView` 平滑水平居中）；Tab 容器加 `ref={tabNavRef}`，每个 button 加 `data-tab={tab.id}` 锚点。
  - 验证：eslint 0 error（仅既有 no-console）；vite build 成功（BUILD_EXIT=0，仅无关 CSS 拼写警告）。

- 后端修复：异常检测接口 numpy 类型序列化 500（用户实跑学生画像触发）。
  - 根因：`AnomalyService.detect_group_anomaly` 用 `np.mean`/`np.std`（返回 numpy.float64）做比较，`abs(deviation) > threshold` 得到 `numpy.bool_`，Flask 默认 JSON provider（Flask 2.x 已弃用 `app.json_encoder`，`LazyJSONEncoder` 实际未生效）无法序列化 → `/api/algorithm/anomaly/group/<id>` 与 `/anomaly/<id>`(detect_all_anomalies) 均 500，致学生画像 Promise.all 整体 reject 白屏。
  - 修复（backend/services/anomaly_service.py）：detect_group_anomaly 把 class_mean/class_std 转 `float(...)`、deviation 转 `float(...)`、is_anomaly 转 `bool(...)`，返回字段全为原生类型（与 rule_recommendation_service 处理 ScoreCategory 同一思路：service 层规整）。
  - 测试（backend/tests/test_anomaly_service.py）：新增 test_detect_group_anomaly_serializable / test_detect_all_anomalies_serializable（mock 构造 len(classmate_totals)>1 走 np.std 分支，断言 json.dumps 不抛且 has_anomaly/deviation/class_mean/class_std 为原生 bool/float）→ 2 passed。
  - 验证：pytest 2 passed。
  - 生效条件：线上后端跑旧代码，须强杀占用 5000 的 python 进程并重启（Flask 不热重载）才生效。
  - 未提交（待用户确认）。

- 前端算法页实跑验证（用户指令"验证刚才优化的前端算法页面"）。
  - 环境：强杀旧后端 5000（PID 23744）→ 用修复后代码后台重启（RJMJZ0 后台 task）；managed workspace 已装 playwright 但缺 chromium，h1eoZI 后台 install chromium（exit 0，4s 完成）。
  - 后端 smoke：curl 登录 admin/123456 拿 access_token → 带 token 触发之前 500 的 `/api/algorithm/anomaly/group/72?days=30` 与 `/api/algorithm/anomaly/72?days=30` → **均 HTTP 200**，numpy 修复确认生效。
  - Playwright 实跑（脚本 `C:/Users/53527/.workbuddy/binaries/node/workspace/verify_algo.js`，chromium headless 1920x1080）：
    - **坑1**：前端是 **HashRouter**（App.tsx L282），所有 SPA 路由走 hash。`page.goto('/algorithm-analysis')` 只设 path，HashRouter 解析 hash 为空 → 回退 `index` 路由 → Dashboard（首页仪表盘），故脚本初版截图捕到仪表盘而非算法页。**修复**：goto 必须 `/#/algorithm-analysis`。同理 Login 用 path `/login`（path 也注册了，未受保护，可直接渲染登录页）。
    - **坑2**：登录 click 后 `waitForURL` 失效——HashRouter navigate('/') 让 URL 变成 `http://localhost:3000/login#/`（pathname 仍是 /login），`(u)=>!u.toString().includes('/login')` 永远 false。**修复**：不依赖 URL，登录 click → 等 networkidle + 1s → goto `/#/algorithm-analysis`（重新加载 SPA，路由守卫读 localStorage.admin 同步放行）。
    - **坑3**：初版脚本 catch 截图捕到"加载学生画像中..." spinner（截图时机太早）。**修复**：在 screenshot 前 `getByText('风险分').waitFor({timeout:15000})` 等 riskPredict 完全渲染。
  - 验证结果（最终脚本）：
    - TAB_IN_VIEWPORT=true（学生画像 Tab boundingBox x=1595.86，水平居中靠右 1920 viewport 内）→ **Tab 自动滚动方案 C 生效**。
    - PICKED_STUDENT_ID=66（DebugTestUser_xyz，一年级1班）。
    - RISK_VISIBLE=1、PROFILE_ERROR=0、RENDER_FULL=1（"风险分"文本可见，riskPredict 完整渲染）。
    - **API_5xx=0**（零 5xx，numpy 修复 + 整体链路通）。
    - CONSOLE_ERRORS=1（仅 WebSocket Invalid frame header 警告，与本次验证无关，socket.io 跨域/代理常见）。
  - 截图 `C:/Users/53527/Desktop/algo_profile_verify.png` 展示完整学生画像：学生画像 Tab 在最右侧自动滚动到位并选中；积分预测 47.0 +9.4 置信度 75% + 95% 区间带 41.2~51.1；成绩预测 81.0 +2.1 置信度 58% + 区间 75.4~95.8；风险评估「低风险」徽章 + 风险分 0.0 + **多维风险分**（学术/行为/出勤三维蓝色低风险条 low 0.00）；异常检测 4 路（综合/突变/趋势/群体偏离）正常显示；成绩波动归因「数据较少考试数据，无法对比归因」正常提示。本轮两轮算法优化（置信区间+三维风险）视觉效果已端到端验证通过。
  - 关键经验：**SPA HashRouter 路由必须用 hash 形式访问**（`page.goto(BASE + '/#/路由'）`，path-only 会回退到 index/Dashboard）；该坑极隐蔽，仅 playwright 实跑才能暴露，build/lint/vitest 全不报。

- 新增算法维度：参与度指数（Engagement Index，用户说"进行下一步，先不推送"，方向=参与度指数(推荐)）。
  - 需求与算法（纯 Python，无 numpy 复用风险）：
    - 综合 4 维度：出勤率（Attendance status 加权 present=1/late=0.5/leave=1/absent=0）、作业提交率（HomeworkSubmission 在窗口内 assigned_date 的作业中按时比例，迟交半价）、积分活跃度（ScoreRecord 事件数 / expected=days//3，封顶 1）、请假天数（已批准 LeaveApplication 与窗口重叠天数）。
    - 权重：attendance 0.4 / homework 0.3 / activity 0.3，缺失维度按权重重归一化。
    - 请假扣分：min(15, leave_days * 1.5)，作为负贡献 factor。
    - 等级：>=70 high / >=45 medium / else low。
    - 健壮性：activity_rate 在事件数=0 时记为 **None**（同 attendance/homework），保证全维度无数据时 present 空 → has_data=False、score=0、level=low；任一字段缺失返回 None；所有数值走 float()/int() 转原生类型（防 numpy 序列化回归）。
  - 后端 `backend/services/engagement_service.py`（新）：模块函数 `calculate_engagement(user_id, days=30)` + 类 `EngagementService(staticmethod(calculate_engagement))`（与 AnomalyService 等保持一致接口）。常量 ATTENDANCE_WEIGHT/WEIGHTS/LEVEL_HIGH/LEVEL_MEDIUM/LEAVE_PENALTY_PER_DAY/CAP 集中定义。
  - 路由 `backend/api/algorithm_routes.py`：`/api/algorithm/engagement/<int:user_id>` GET（requires_permission algorithm.view，days 默认 30），import EngagementService。
  - 前端：types 加 EngagementFactor/EngagementResult；api.ts 在 '../types' 导入加 EngagementResult，algorithm 对象接口声明 `getEngagement(userId, days?)`，实现请求 `/api/algorithm/engagement/<id>?days=<d>` 经 `normalizeUserEngagement` 归一化（factors 数组元素 name/value/weight/contribution 兜底；components 四个字段；level 白名单；has_data 强制布尔）；AlgorithmAnalysis studentProfile state 加 `engagement?`，loadStudentProfile 的 Promise.all 新增第 9 路 getEngagement(anomalyDays)；renderStudentProfile 在风险卡后插入「参与度指数」卡（emerald 主题，level 徽章 高/中/低+不同底色，参与度分 + 可选请假天文本 + 三条 emerald 组件条，仅渲染 rate≠null 的维度，无数据/无 engagement 优雅降级到提示文案）。
  - **关键坑**（已修）：
    1. 路由 import `from services.engagement_service import EngagementService` 要求**类**，而我初版写的是模块函数 → ImportError → 整个 algorithm 命名空间被 conftest 跳过注册 → 路由 404。**修复**：函数保留，同时加 `class EngagementService: calculate_engagement = staticmethod(...)` 导出。
    2. `activity_rate` 初版始终 min(1.0, event_count/expected)（=0 当无事件），导致「无数据」分支 present 永远非空（activity 算 0.0）→ has_data 误判 True。「权重重归一化」测试（仅出勤、无积分事件）得分 57.1 而非预期 100。**修复**：event_count==0 时 activity_rate=None，参与权重重归一化分支。
    3. 无数据分支/主分支 `float(activity_rate)` 在 activity_rate=None 时 TypeError。**修复**：所有 activity_rate 走 `float(...) if ... is not None else None`。
    4. 单元测试 `test_renormalize` 断言 `activity_rate==0.0` 与新设计不符 → 改为断言 `is None`。
  - 测试 `backend/tests/test_engagement_service.py`（新）：5 passed
    - structure_and_serializable：建 ClassInfo+User+10 条 attendance present+1 条 HomeworkAssignment+已提交+5 条 ScoreRecord，断言 has_data=True、score 0-100、level 合法、components 键集、attendance_rate==1.0、homework_rate==1.0、json.dumps(result) 不抛（防 numpy 序列化回归）。
    - no_data：仅 ClassInfo+User 无任何记录，断言 has_data=False、score=0.0、level=low、attendance_rate/homework_rate 为 None。
    - renormalize_on_missing_dimension：仅 8 条 attendance present 无作业无积分，断言 homework_rate/activity_rate=None、engagement_score==100.0、level=high。
    - leave_penalty：8 present + 2 absent + 10 天批准请假，断言 leave_days>=10、factors 含 name=='leave' 且 contribution<0、engagement_score<=80（出勤贡献上限）。
    - route_registered：client GET `/api/algorithm/engagement/1?days=30` 无 token 期望 200/401（已注册非 404）。
    - 复用 conftest `app` fixture（sqlite :memory: + StaticPool + db.create_all()）真实插入，ClassInfo+User+Attendance/HomeworkSubmission/HomeworkAssignment/ScoreRecord 满足 NOT NULL FK；每个 User 的 card_id 用 `CARD_TEST_<sid>` 唯一（user.card_id unique 约束）。
  - 验证：py_compile OK（engagement_service + algorithm_routes）；pytest 5 passed；eslint 0 error（仅既有 24 个 no-console 警告）；vite build 成功（1m24s，exit 0，仅既有 CSS 拼写警告 backgroundposition）。
  - Playwright 实跑（verify_algo.js 增强：增加 "参与度指数" 文案等待 + ENGAGEMENT_VISIBLE 计数）：admin/123456 登录 → `/#/algorithm-analysis` → 学生画像 Tab（自动滚动 x=1595.86，方案 C）→ 选学生 66（DebugTestUser_xyz）→ 等渲染 → **ENGAGEMENT_VISIBLE=1、RISK_VISIBLE=1、RENDER_FULL=1、PROFILE_ERROR=0、API_5xx=0**。截图 `algo_profile_verify.png` 展示新「参与度指数」卡：中参与度 50.0 / 作业提交率 0% / 积分活跃度 100%（出勤率因该生无 attendance 记录按设计被排除+渲染时跳过）。后端 curl 验证 `/api/algorithm/engagement/66?days=30` 返回 data.level=medium, score=50.0, components.attendance_rate=None（权重归一化到 homework 0.5 + activity 0.5），has_data=True，factors 含 homework/activity 贡献度。
  - 未推送（用户说"先不推送"）；**待用户决定是否本地提交**。

- 新增算法维度：批量归因路由（用户说"继续推进"，方向=批量归因路由）。
  - 需求与算法：按班级一次性跑全班成绩波动归因，单生异常隔离不影响整体；前端新增「班级归因」Tab（接全局 selectedClass 下拉），展示全班汇总卡片 + 每生归因表 + 异常隔离区。
  - 后端 `backend/services/attribution_service.py`：DB 包装层加 `AttributionService.batch_analyze(class_name, days=30)`：查 `User.class_name == class_name` → 遍历逐生调用 `analyze_score_attribution`，try/except 隔离单生异常进 `failed_students`；返回 `{class_name, days, total, analyzed, with_data, failed, students:[...], failed_students:[...]}`，全部 int/float/str 原生类型（np.mean 已 float()）。
  - 路由 `backend/api/algorithm_routes.py`：`/api/algorithm/attribution/batch` GET（requires_permission algorithm.view，class_name 可选 + days 默认 30）。注册在 `ScoreAttribution`（`/attribution/<int:user_id>`）之前，避免 flask-restx 路由顺序歧义。
  - 前端：types 加 `BatchAttributionStudent`/`BatchAttributionResult`；api.ts 导入行加 `BatchAttributionResult`；algorithm 对象接口声明 `getBatchAttribution(className?, days?)`；实现 `encodeURIComponent(className)` 拼接；`normalizeBatchAttribution` 全面兜底（students/failed_students 数组、factors 白名单、direction 三值、summary、error）。
  - AlgorithmAnalysis.tsx：lucide 导入加 `Users`；TABS 加 `{ id:'batchAttribution', label:'班级归因', icon:Users, new:true }`；state 加 `batchAttribution`/`batchAttributionDays=30`/`batchAttributionLoading`/`batchAttributionError`；`loadBatchAttribution` useCallback（无 selectedClass 弹 warning toast + 直接返回）；useEffect 在 `activeTab==='batchAttribution' && selectedClass` 时自动加载（selectedClass/batchAttributionDays 变化也触发）；`renderBatchAttribution`：控制区（班级/days input/「生成全班成绩波动归因」按钮）+ 4 汇总卡片（班级人数/有效归因/缺数据/异常隔离）+ 表格（学生/成绩变化+/-色/主要归因 top2 拼接/置信度条/状态徽章）+ 异常隔离区（仅 failed.length>0 时渲染）；分发 `activeTab==='batchAttribution' && renderBatchAttribution()`。
  - 健壮性：单生异常隔离（service try/except）；空 class_name / 不存在的 class 返回 total=0 空结果；normalize 全面兜底；前端 selectedClass 空时按钮 disabled + 「请先选择班级」提示；loading/empty/error 三态区分；表格行 key=user_id ?? idx 避免 key 冲突。
  - 测试 `backend/tests/test_attribution_batch.py`（新）：4 passed
    - structure_and_serializable：ClassInfo+2 学生同班，断言 total=2/analyzed=2/failed=0/students 长度 2，`json.dumps(result)` 不抛（防 numpy 序列化回归）。
    - empty_class：class_name="根本不存在的班" → total=0/students=[]/failed=0 不抛。
    - partial_failure_isolated：`unittest.mock.patch` 替换 `services.attribution_service.AttributionService.analyze_score_attribution` side_effect 让 sid=1 抛 RuntimeError，断言 failed=1/analyzed=1/failed_students 含「测试生1」+ error 原生 str/user_id 为 int。
    - route_registered：client GET `/api/algorithm/attribution/batch?class_name=测试班1&days=30` 无 token 期望 200/401（已注册非 404）。
  - 验证：py_compile OK（attribution_service + algorithm_routes）；pytest 4 passed；eslint 0 error（仅既有 no-console）；vite build 成功（52s, exit 0，仅 CSS `backgroundposition` 拼写警告）；后端重启后 curl `/api/algorithm/attribution/batch?class_name=__none__&days=30` 带 admin token 返回 HTTP 200（路由生效）。
  - Playwright 实跑（`verify_batch.js`：登录 admin/123456 → `/#/algorithm-analysis` → 点班级归因 Tab → 遍历 11 个班级 option 对每个 selectOption + 等批量归因返回 → 自动定位首个 rows>0 的班并截图停留）：找到「一年级1班」含 3 行（吴芳/吴娜/DebugTestUser_xyz），汇总 班级人数 3 / 有效归因 0 / 缺数据 3 / 异常隔离 0（该班学生无考试数据 → has_data=False → 表格行渲染但归因字段"—"+状态徽章"缺数据"，符合设计）；**API_5xx=0 / CONSOLE_ERRORS=0 / 路由 200**。截图 `algo_batch_verify.png` 展示新「班级归因」Tab（自动滚动到最右侧 + NEW 徽标）+ 控制区 + 汇总卡片 + 3 行表格。
  - 关键经验：
    1. flask-restx 字面路由 `/attribution/batch` 与动态 `/attribution/<int:user_id>` 因字面段优先匹配安全共存，但放在动态路由前更明确（避免未来加 `/attribution/<int:user_id>/something` 时歧义）。
    2. 批量 service 遍历单生 + try/except 是"单点故障不影响整体"的标准模式（与 notification.send 单生异常隔离同理）。
    3. Playwright 验证功能时**遍历真实数据维度**比 hardcode 单值更稳健——本次遍历 11 班级自动定位到有数据的「一年级1班」，避免 hardcode 班级名导致空态误判。
    4. dev server HMR 对新增 lucide import + 新 TABS 项 + 新 render 函数完全热更新，无需重启；构建仅作编译验证。
  - 未推送（按用户约定不主动推送）；**待用户决定是否本地提交**。

- **班级归因 Tab UI 修复**（用户截图反馈"无法选择班级"）：
  - **根因**：`renderBatchAttribution` 控制区 1938-1940 行 `<span>{selectedClass || '全部班级'}</span>` 是只读文本；唯一选择器是顶部"全部班级"下拉框（无 label 标识），用户进 Tab 后看不到可交互控件→"无法选择"。
  - **修复**（frontend/src/pages/AlgorithmAnalysis.tsx，1 file +55/-14）：
    1. 班级归因 Tab **内嵌 select**（data-testid="batch-attribution-class-select"）：`<Users /> 选择班级:` label + `<select>`（min-w-[140px]、focus:ring-purple-500、复用 classes 列表）+ 已选时显示 "清除" 按钮；归因窗口 input 也补 htmlFor label。
    2. 顶部下拉框加 `<span class="hidden sm:inline">班级:</span>` label（sm 以下隐藏避免挤占）+ `aria-label="按班级筛选"` + `data-testid="global-class-filter"`。
    3. 空态文案改写："请先在上方【选择班级】下拉框中选择班级后开始归因" + 副提示"（页面顶部"班级"下拉框与此处同效，二选一即可）"，明确指向新加控件。
  - **验证**：eslint 0 error（仅既有 no-console 警告）；Playwright `verify_all_tabs.js` 实跑——10 个 Tab 全部 `5xx=0/console=0/pageerror=0`、班级归因 Tab 选"一年级1班"渲染 3 行学生、3 张汇总卡（班级人数 3/有效归因 0/缺数据 3/异常隔离 0）。截图 `Desktop/algo_class_before.png`（空态）+ `algo_class_after.png`（选中后）。
  - 提交 aee928a（pre-commit 通过）。未推送。
  - **经验**：
    1. **Tab 内 vs 全局下拉框**：批量型 Tab（按班级过滤）应同时提供"全局顶部"+"Tab 内"两个入口并互相同步——顶部让多 Tab 切换时保留选择，Tab 内让"本 Tab 立刻可操作"。仅放顶部会让人误以为无可点控件。
    2. **必填项视觉强化**：硬要求 selectedClass 的按钮用 `disabled={!selectedClass} + opacity-50` 是"隐式提示"，显式更优：加"必选"标识或内嵌一个必选下拉框就近引导。
    3. **空态文案要指路**：不要只说"请先在 X"，而要说"请先在 [控件名] 选 [内容] 后 [操作]"——用户能照着按。

- **参与度指数可视化（继续推进方向）**：
  - **背景**：`engagement_service.calculate_engagement` 单用户已有但**前端无 Tab**，后端参与度维度（出勤/作业/积分活跃度/请假）是平台特色数据但只能"知道有、看不到"。批量排名 + 周趋势是填补此空白的自然下一步。
  - **后端**（+165/-2，`backend/services/engagement_service.py` + `backend/api/algorithm_routes.py`）：
    1. `calculate_engagement(user_id, days, end_date=None)` 加 `end_date` 可选参数——窗口变为 `[end_date-days, end_date]` 而非固定 `[now-days, now]`，让 weekly_trend 可向前滚动构造各周窗口。
    2. `EngagementService.weekly_trend(user_id, weeks=8)` 按周循环：每周 `end_date = now - (weeks-1-i)*7`、`days=7` 调 `calculate_engagement`，过滤 has_data 后返回 `series` + `trend`（基于首末两周均值的 up/down/flat）+ 全部原生类型。
    3. `EngagementService.batch_rank(class_name, days=30)` 遍历 `User.class_name` 全班——单生异常 try/except 隔离进 `failed_students`，每个学生 `engagement_score + rank + level + components`——rank 按 score 倒序（None 排最后）。
    4. 路由 `/api/algorithm/engagement/batch`（前置注册避免与 `/<int:user_id>` 歧义）+ `/engagement/<int:user_id>/weekly-trend`。
  - **前端**（+489，`frontend/src/types/index.ts` + `services/api.ts` + `pages/AlgorithmAnalysis.tsx`）：
    1. 新类型 `EngagementRankResult/EngagementStudentRank/EngagementTrendResult/EngagementTrendPoint`。
    2. `api.algorithm` 加 `getEngagementRank(className?, days?)` + `getEngagementTrend(userId, weeks?)`，配 `normalizeEngagementRank/Trend` 兜底（含 `level` 三值、series 数组、weeks 数字）。
    3. TABS 加 `engagement`（label "参与度分析"，icon `LineChart`）；state `engagementRank/engagementTrend/engagementTrendUserId/engagementRankDays=30/engagementTrendWeeks=8/loading/error`；`loadEngagementRank` useCallback + `loadEngagementTrend` useCallback；useEffect 在 activeTab==='engagement' && selectedClass 时自动加载排名；useEffect 在 `engagementTrendUserId` 变化时加载趋势。
    4. `renderEngagement`（~230 行）：内嵌班级下拉框（data-testid="engagement-class-select"，含 label、清除）+ 统计天数 input + 「生成全班参与度排名」按钮；4 汇总卡（班级人数/有效参与度/高参与度/异常隔离）；排名榜表格（排名/学生/参与度/等级[徽章]/出勤率/作业率/活跃度/请假/周趋势[查看]），**行可点击** → 滚到趋势区并设 `engagementTrendUserId`；`EngagementTrendChart` 组件渲染 SVG 折线图（720×240 viewBox、0/25/50/75/100 网格、面积+折线+数据点+周标签 W26-W32），趋势色 up=绿/down=红/flat=紫，"近 [4/6/8/12 周] ▼" 切换。
    5. 顶部班级下拉框的 onChange 已绑 `setSelectedClass`，与 Tab 内下拉框双向同步（顶部改 → Tab 内自动更新）。
  - **测试**（6 passed，`backend/tests/test_engagement_batch_trend.py`）：
    1. `calculate_engagement_end_date` —— 历史 end_date 窗口含数据、now 窗口无数据→ has_data 翻转。
    2. `weekly_trend_length_and_native` —— series 长度=weeks、score 全是原生 float。
    3. `weekly_trend_direction_up` —— 后周均分 > 前周 → trend='up'。
    4. `batch_rank_structure_and_serializable` —— total=2/analyzed=2/failed=0/rank 倒序/json.dumps 不抛。
    5. `batch_rank_empty_class` —— 不存在班名 total=0。
    6. `batch_rank_partial_failure_isolated` —— mock patch side_effect 让 sid=1 抛 RuntimeError → failed=1/analyzed=1/failed_students 含 uid+name+error（全是原生类型）。
  - **验证链**：py_compile OK → pytest 6 passed（既有 test_engagement_service.py 5 passed 未回归）→ eslint 0 error → vite build exit 0 → curl `/engagement/batch` 401（路由注册成功，需鉴权而非 404）→ Playwright `verify_engagement.js` 实跑——11 个 Tab 全部 5xx=0/console=0/pageerror=0、一年级1班 3 行渲染（吴芳 50.0/DebugTestUser_xyz 50.0/吴娜 45.0，全部 medium、活跃度 90-100%、请假 0）、点「查看」加载周趋势 SVG（7 周数据点、紫色平稳线、W26-W32 标签）。截图 `algo_engagement.png`（排名榜）+ `algo_engagement_trend.png`（周趋势）。
  - 提交 ac601a1（pre-commit 通过）。未推送。
  - **经验**：
    1. **维度数据缺失的 UI 处理**：参与度 4 个维度（出勤/作业/活跃度/请假）独立计 has_data，整体 `has_data = 任一维度有数据`。前端对 `components.attendance_rate == null` 渲染 "—" 而非 "0%"——避免误导（0% 表示"全缺勤"，null 表示"无考勤数据"）。
    2. **end_date 注入让"周趋势"实现干净**：`calculate_engagement(days, end_date)` 是周窗口的天然基础；不加 end_date 就得复制代码或重构为滑动窗口函数，**这是"参数化窗口"原则的胜利**。
    3. **Playwright `:contains()` 不可用**：原生 CSS 不支持 `:contains`，Playwright 也没扩展。要统计"含某文本的行数"，应用 `page.evaluate` 在浏览器内 `Array.from(querySelectorAll(...))` + 检查 `cells[2].textContent`，比 `tbody tr:has(...)` 系列伪类选择器稳健。
    4. **行可点击 + 列内按钮共存**：排名榜表格整行 `onClick` 触发趋势区定位，但「查看」按钮用 `e.stopPropagation()` 阻止冒泡——避免点击按钮时同时触发行点击导致重复 setState。两者交互独立、互不打架。

- **全 11 算法 Tab 完整可信验证**（用户"继续推进"选"全页面完整可信验证"，呼应之前"确保分析所有页面完整可信可用"）：
  - **方法**：写 `verify_all_tabs_audit.js`——登录后遍历 11 Tab，每 Tab 截图 + 导出 DOM 文本/错误文本/空态关键词到 `algo_audit_report.json`（**因当前模型无法 read 图片**，改用 DOM 文本 JSON 判断可信度，而非看截图）。先设顶部全局班级=一年级1班，再逐 Tab 收集 trs/btns/sels/cards/errTexts/grayTexts。
  - **发现（首轮）**：9 个 Tab 有真实数据、全部 5xx=0/console=0；batchAttribution/engagement 因审计只设了**顶部全局下拉框**（非内嵌控制点）才显空态——本身可用（内嵌 select 才是控制点，已在 verify_batch/verify_engagement 证过）；ruleApplication 主区裸显"暂无数据"像坏了；scorePredict "涉及科目 0" 像 bug（实际是预测均为"综合"无科目拆分）。
  - **修复**（AlgorithmAnalysis.tsx，2 处纯文案/显示，+7/-3）：
    1. **scorePredict「涉及科目」**：`summary.subjects` 为空时显示"综合"/"综合评分"替代"0"/"—"（无科目拆分时预测为综合分，显示 0 像 bug）。重跑审计确认渲染"涉及科目 综合 综合评分"。
    2. **ruleApplication「评分分布统计」空态**：把裸 `<p>暂无数据</p>` 改为引导文案"暂无评分分布数据 / 需学生已有积分·成绩记录；可在右侧「规则应用控制」选择学生执行规则，或点击「调整分布」生成"。重跑确认渲染。
  - **二轮回测（修正交互）**：对 batchAttribution/engagement 用**内嵌 data-testid 下拉框**选一年级1班、对 studentProfile/ruleApplication 用「含学生名 option 的 select」选吴芳，确认真实渲染：batchAttribution 3 行（吴芳/吴娜/吴静「数据不足」缺数据）、engagement 3 行（吴芳 50.0/DebugTestUser_xyz 50.0/吴娜 45.0）、studentProfile 选学生后渲染 4 面板（异常检测文本出现在 DOM）、ruleApplication 引导空态。
  - **最终结论**：11 Tab 全部 5xx=0/console=0；9 个有真实数据；2 个（batchAttribution/engagement）需内嵌选班、2 个（studentProfile/ruleApplication）需选学生——均有清晰引导，非缺陷。
  - 提交 432fb54（pre-commit 通过）。未推送。
  - **经验**：
    1. **模型不能看图时改用 DOM 文本审计**：Playwright `page.evaluate` 导出 `document.body.innerText` + 错误/灰字元素文本到 JSON，比截图更可机读、可 diff、可 grep。截图只给人看，文本给模型看。
    2. **全局 vs 内嵌下拉框的审计陷阱**：批量型 Tab（班级归因/参与度）的控制点是**内嵌** select（data-testid 标注），顶部全局班级下拉框不驱动它们。审计若只用全局下拉框会误判"空数据"——必须操作正确的内嵌控件。
    3. **空态文案要"指路+解释原因"**：裸"暂无数据"让人以为功能坏了；应说明"为什么空 + 怎么产生数据"（如规则应用：需学生有积分记录/选学生执行/点调整分布）。
    4. **"0" vs "综合" 的语义**：聚合指标无明细维度时（如成绩预测无科目拆分），显示"综合"比"0"更可信——0 像缺数据，综合像有意聚合。

  - **代码健康清理（2026-08-13，commit c771c6b）**：消除评估遗留的仓库噪音待办。
    - 归档（移动非删除，保留本地）到 `backend/scripts/archive/`：孤儿调试/验证/探针脚本共 **24 个**（根散落 verify_algo*.py/verify_risk_batch.py/test_*.py/debug_nlp_rules_tab.py/probe_*.py/inspect_db_schema.py/fix_local_imports.py/verify_all_algo.py + scripts/verify_algo_fix.py）；根散落 NLP 调试截图 **12 张 PNG**；instance/ 备份库 **21 个**（13 个 .db.bak_* + 2 个损坏库 *_corrupt.db/*_rebuilt_broken.db + 6 个 .db.*_bak 变体）。
    - `instance/` 现仅剩 `score_management.db` + shm/wal + risk_model_config.json + 原始数据/；根 .py 仅剩核心（app/celery/config/run/wsgi/repair*/rebuild_db/seed_*/sweep_endpoints）。
    - `.gitignore` 追加 `backend/scripts/archive/`（整归档目录忽略）+ `backend/*.png`（防根再散落截图）。`backend/instance/` 与 `*.bak` 早已有忽略规则。
    - 验证：`git rm --cached` 移出跟踪同时 `mv` 保留工作树；pytest `--collect-only` 正常（根 test_*.py 移走后净化测试收集）；git status 干净。
    - **经验**：① 仓库噪音清理三步走「git rm --cached + mv 归档 + .gitignore 忽略归档目录」——既清出 git 跟踪又保留本地可溯源；② .gitignore 路径必须用**相对仓库根全路径**（曾误写 `scripts/archive/` 漏 `backend/` 前缀未生效，改 `backend/scripts/archive/` 才生效）；③ 根散落 `test_*.py` 会被 pytest 自动收集，是噪音危害点，移出即净化测试环境。

  - **算法洞察业务闭环（2026-08-13，commit 待定）**：把算法结果反推到业务端，形成分析→行动闭环。
    - 后端 `api/student/student_routes.py` 新增 `GET /api/student/insights`（requires_student）：聚合 `calculate_engagement(uid)` + `RiskPredictService.predict_risk(uid)` + 近 N 周积分趋势（`_build_score_trend` 拉全量 ScoreRecord 内存按周分组，规避 SQLite DateTime 列与 date 边界比较不一致）。单维异常隔离：参与度/风险/趋势各自 try/except 降级，不影响其余返回；全部原生类型。
    - 前端学生端 StudentPortal 新增第 6 个 Tab「我的成长」（TrendingUp 图标）：参与度指数卡片（分数+等级+出勤/作业/活跃度进度条）+ 风险预警卡片（级别徽章+干预建议前 3 条）+ 近 8 周积分变动柱状图（绿涨红跌，`maxAbs` 归一化高度）。
    - 教师端 TeacherTools 顶部新增「算法洞察」入口卡片：班级归因一键查看→`/#/algorithm-analysis?tab=batchAttribution`、参与度排名榜→`?tab=engagement`。AlgorithmAnalysis 加 `useSearchParams` 读 `?tab=` 参数初始化 activeTab（校验合法性，默认 statistics）。
    - 测试 `tests/test_student_insights.py` 5 passed（结构/可序列化/趋势长度/鉴权 401/单维异常隔离/建议字段）；eslint 0 error；vite build exit 0；Playwright `verify_insights.js` ALL PASS（学生端成长页 3 卡 + 8 根柱子、教师端入口 + 双向跳转直达，5xx=0 console=0）。
    - **经验**：① 双 JWT 隔离决定学生端洞察必须走 `requires_student` 新接口，不能复用 `algorithm.view`（否则 401）；② Playwright 内 `page.evaluate` 里 fetch 相对路径需先 `goto` 建立同源（空白页 fetch 报 "Failed to parse URL"）；③ 中文参数必须 `encodeURIComponent`（直接拼中文班级名 → data.students 空数组）；④ Tailwind `max-w-[18px]` 编译为 CSS class 而非 inline style，`querySelectorAll('div[style*="max-width"]')` 匹配不到，用 `div.rounded-t` 类选择器数柱子；⑤ `calculate_engagement` 返回 key 是 `engagement_score` 非 `score`（batch_rank/weekly_trend 均用 engagement_score）。


  - **补全算法单测边界（2026-08-13，commit 待定）**：修复 3 个 predict_batch 缺单生异常隔离 + 补边界单测。
    - **真实修复**：`risk_predict_service` / `score_predict_service` / `prediction_service` 三处 `predict_batch` 循环内原来**无 try/except**——单生预测抛异常会中断整个班级批处理。已统一加隔离：失败学生追加 `failed_students[{user_id,name,class_name,error}]`，返回追加 `failed` 计数（与 attribution/engagement 批量标准模式对齐，前端 normalize 兜底兼容）。
    - 新测试 `tests/test_algorithm_batch_isolation.py` **10 passed**：三处 batch 单生异常隔离（mock 单生抛异常→failed=1 其余正常）、risk 空班级不炸、全负积分极端数据 predict_risk→high/medium、composite `_calculate_scores` 权重敏感性（A 行为高/学术低 vs B 行为低/学术高，权重 [0.8,0.1] vs [0.1,0.8] 排名翻转）、entropy_weight 归一化/零方差列/全零回退等权。
    - 回归：既有 4 个相关测试文件 82 passed + 契约回归 test_api_envelope 2 passed（改后端必跑）。
    - **运维教训（重要）**：Flask **debug reloader 会 fork 多进程**（日志 "Restarting with stat"），改后端后**仅杀 netstat 显示的子进程不够**——reloader 父进程还在、且 Windows netstat/Get-NetTCPConnection 常显示 **stale PID**（进程已死表未刷新）。正确流程：① `Get-Process -Name python*` 列出真实进程 → 全杀（含 reloader 父进程）→ ② **用 curl 探测**（connection refused=已释放）而非 netstat 判断端口 → ③ nohup 重启（启动约 30-40s，MQTT 初始化慢）→ ④ 用 `?skipCache=1` curl 验证新字段（列表 GET 有持久化缓存，不带参数会命中旧响应误判"未生效"）。

  - **学期报告嵌算法摘要（2026-08-13，commit 待定）**：让算法洞察沉淀到正式导出文档。
    - 新建 `services/report_summary_service.py`：`build_class_summary(class_name, days)` 聚合三维——参与度（batch_rank 均值/等级分布/有效人数）、风险（predict_batch 高/中名单 top10）、归因（batch_analyze 学生 factors 按名聚合 top5）；三维各自 try/except 隔离（单维失败置 None 不影响其余与主表格）；`summary_to_rows` 转 [项目,内容] 摘要行。
    - `api/reports/report_routes.py` /class-semester 接入：Excel 加「算法摘要」sheet（headers=[项目,内容]）；CSV 顶部加 `# ` 注释式摘要行（BOM 放文件最开头，meta utf-8 无 BOM，正文剥 BOM 拼接）。摘要整体 try/except 兜底，失败不影响主表格导出。
    - 测试 `tests/test_report_summary.py` **6 passed**（摘要结构/单维隔离 mock/空班级/转行格式/Excel 含摘要 sheet/CSV 含摘要行）；既有 test_reports 5 passed 无回归；契约回归 2 passed。
    - 冒烟（真实数据一年级1班）：Excel sheets=['学期报告','算法摘要']，摘要 8 行（参与度均值 48.3、等级分布高0中3低0、有效 3/3、风险高0中1 名单吴娜）；CSV 开头 `# 算法摘要...` 后接表格。
    - **经验**：① seed 摘要测试必须同时设 `User.class_name`（batch 接口按 class_name 过滤）与 `role="student"`（risk/score_predict batch 按 role 过滤），只设 class_info_id 会查空；② CSV 嵌多行摘要用 `# ` 注释行 + BOM 统一放文件最开头（正文剥 BOM 再拼），Excel 打开正常；③ 冒烟脚本用 Python urllib 替代 bash 嵌套引号更稳（bash $() 内嵌 python -c 带括号必炸）。

  - **算法结果导出 Excel（2026-08-13，commit 待定）**：算法 Tab 一键导出，教师拿走即用。
    - 后端 `api/algorithm_routes.py` 新增 `GET /api/algorithm/export?tab=engagement|attribution|risk&class_name=&days=`（requires_permission algorithm.view）：`_build_export_rows` 调对应 batch service 组装二维数组——engagement:[排名,姓名,班级,参与度,等级,出勤率,作业率,活跃度,请假天数]、attribution:[姓名,班级,成绩变化,主要因子,置信度,状态]、risk:[姓名,班级,总体风险,风险分,风险因素]，`ExcelUtils.export_to_excel` 生成 xlsx send_file（filename 含班级名+sheet名，安全替换 /）。
    - 前端 `api.algorithm.exportExcel(tab,className?,days?)`：fetch blob + a.download（仿 exportClassSemester 模式，getBearerToken 注入）。AlgorithmAnalysis 三 Tab 控制区加「导出 Excel」按钮（Download 图标、导出中禁用显示"导出中..."）：班级归因/参与度需选班级；风险评估允许不选班级导出全部（handleExport 中 `!selectedClass && tab!=='risk'` 才拦截）。
    - 测试 `tests/test_algorithm_export.py` **6 passed**（三 tab 各导出 200+openpyxl 验证 sheet/表头/行数、非法 tab 400、student token 401、无 class_name 导出全部）；契约回归 2 passed；eslint 0 error；vite build exit 0。
    - Playwright `verify_export.js` **ALL PASS**：三 Tab 导出按钮可用，点击真实下载 3 个文件（算法attribution_一年级1班.xlsx / 算法engagement_一年级1班.xlsx / 算法risk_全部.xlsx），页面 console 0（仅过滤 Socket.IO ws 长连接噪声"Invalid frame header"——后端重启后旧 sid 失效的既有环境噪声，非页面 JS 错误）。
    - **经验**：① HashRouter 下同一路径仅 query 变化（`?tab=a`→`?tab=b`）**不会重挂载组件**，activeTab useState 保持旧值——Playwright 切 Tab 用 URL 时必须 `page.reload()` 强制重新初始化；② 后端重启后前端 socket.io 报 "Invalid frame header" 是旧 sid 失效的瞬态噪声，验证脚本需过滤 `/socket\.io|Invalid frame header/i` 再判 console 错误，勿误判为页面 bug；③ 导出文件下载用 fetch blob + a.click（不走 request 信封，getBearerToken 手动注入），与学期报告导出同一模式。

  - **OpenAPI 快照更新（2026-08-13，commit 待定）**：消除契约漂移。
    - `verify_openapi_contract.py` 检出 7 个新增端点未入快照（/algorithm/attribution/batch+{user_id}、/algorithm/engagement/batch+{user_id}+{user_id}/weekly-trend、/algorithm/export、/student/insights——全部为本轮迭代新增）。
    - `--update` 用实时 swagger 覆盖快照：451→458 路径，重跑 verify 0 漂移；旧快照自动备份 `openapi.json.bak_<ts>`（移入 backend/scripts/archive/ 保留，git 历史本就有旧版）；JSON 有效性校验通过。
    - **经验**：新增/改名后端端点后应跑 `scripts/verify_openapi_contract.py --update` 同步快照（否则 CI --strict 会失败）；脚本自动生成的 `.bak_<ts>` 备份不受既有 ignore 规则覆盖，需手动移入 archive（或追加 ignore 规则）。

  - **模块2+3：前端组件测试补全 + 教师端风险名单卡片（2026-08-13，commit 待定）**。
    - **模块2（vitest 组件测试）**：
      - 新增 `StudentPortal.test.tsx`（4 用例）+ `AlgorithmAnalysis.test.tsx`（4 用例）。StudentPortal 验证 6 Tab 渲染 + 默认积分显示 + **切「我的成长」渲染参与度/风险/趋势卡片（TDZ 回归防护——历史上 TDZ 白屏 bug 未被 vitest 覆盖）** + 排名 Tab；AlgorithmAnalysis 验证 11 Tab 导航 + 默认统计分析 Tab + 班级归因内嵌下拉框 + 参与度控制区。
      - **关键坑：vitest 4 下 `jest.mock` 兼容层 hoisting 失效（mock 不生效、页面走真实 api）→ 必须用 `vi.mock`**（vi.mock 探针证实 isMocked false→true）。既有 Dashboard.test.tsx 的 jest.mock 实际也失效（页面容错才通过），属遗留隐患。
      - **真实健壮性修复**：AlgorithmAnalysis line 54 `el?.scrollIntoView(...)` → `el?.scrollIntoView?.(...)`（jsdom 无 scrollIntoView 导致渲染崩溃——生产浏览器有该 API 才未暴露）。
      - 全量 vitest **157 passed | 3 skipped**（原 149 + 新 8，无回归）。
    - **模块3（教师端风险名单卡片）**：TeacherTools 新增「班级风险名单」卡片——选班级 → `api.algorithm.getBatchRiskPredict` 显示高风险/中风险计数 + 名单（姓名/等级徽章/风险分）+ 空态"暂无中高风险学生"。Playwright verify_risk_card.js ALL PASS（一年级1班 高风险0/中风险1 吴娜 0.67，与参与度数据一致）。
    - eslint 0 error；vite build 通过。
    - **经验**：① vitest 4 用 `vi.mock` 而非 `jest.mock`（兼容层 hoisting 失效）；② jsdom 无 scrollIntoView，组件里调用需 `?.()` 可选调用（也是真实健壮性提升）；③ 页面级组件测试用 module 级普通函数 mock（非 jest.fn），避免 clearAllMocks 清掉。

  - **CI 测试集扩充（2026-08-13，commit 待定）**：新增算法/报表/学生测试纳入 CI 防线。
    - 确认 11 个测试文件（test_algorithm_batch_isolation/export、test_report_summary、test_student_insights、test_engagement_batch_trend、test_attribution_batch、test_anomaly_service、test_score_predict_service、test_risk_predict_service、test_prediction_service、test_composite_score_service）仅 import models，**无 BERT/transformers/torch 依赖** → CI 无模型环境可跑。
    - 本地合集验证 **150 passed in 94s**（11 文件）。
    - ci.yml backend job 加 step「跑算法/报表/学生新增测试组（不依赖模型）」pytest 指定 11 文件。YAML 校验通过（step 0 checkout 无 name 属正常，勿误判）。
    - **经验**：CI 只跑固定子集，新增测试默认不进 CI——新功能落地后应同步扩充 CI 固定子集（先 grep 确认无模型依赖，再本地合集验证，最后加 ci.yml step）。

  - **e2e 固化新功能（2026-08-13，commit 待定）**：本轮新功能的临时 Playwright 脚本固化为正式 spec。
    - tests/e2e/ 新增 3 个 spec（均 serial + 复用 smoke 登录模式 + admin 登录用 input[type=text]/password）：
      - `teacher-risk-card.spec.ts`：教师端风险名单卡片渲染 + 选班评估出高/中风险计数（注意 getByText('中风险') 命中计数卡+名单徽章两处需 .first()）；
      - `algorithm-export.spec.ts`：三 Tab（班级归因/参与度/风险）「导出 Excel」真实触发 download 事件，文件名含 .xlsx（参与度/风险 Tab 用 URL 后需 page.reload() 强制 activeTab 重初始化——HashRouter 同路径 query 变化不重挂载）；
      - `student-growth.spec.ts`：学生端「我的成长」Tab 渲染参与度/风险/趋势卡片（用 page.request 动态取真实学生凭据 admin→classes→student login→localStorage 注入）+ 教师端「算法洞察」入口→班级归因一键查看跳转 tab=batchAttribution。
    - 单独跑新 spec **6/6 passed**；全量 27 用例 19 passed / 3 failed / 5 did not run——3 个失败（basic/dashboard/algorithm-export）**单跑均通过，系全量并行下的资源 flake**（27 用例并行挤占后端 BERT/算法重负载页，smoke 注释已记录"并行多 context 关闭易超时误报"），非代码问题。
    - **经验**：① getByText 精确匹配在文本出现多处时 strict mode violation → 用 .first() 或 filter 限定；② 学生端 e2e 用 page.request（baseURL+proxy）动态取真实凭据，比 hardcode 凭据稳；③ 全量 e2e 并行下偶发失败先单跑该 spec 确认（单跑过=flake，勿改代码）。

  - **完善已有功能：成长页补参与度周趋势（2026-08-13，commit 待定）**。
    - 缺口：学生端 insights 无参与度周趋势（只有参与度快照+风险+积分趋势），成长页只有积分柱状图。
    - 后端 `student_routes.py` insights 加 `participation_trend`：复用 `EngagementService.weekly_trend(uid, weeks)`（与算法 Tab 同口径），异常隔离降级 {"series":[]}。
    - 前端：`StudentInsight` 类型加 `participation_trend`（StudentParticipationTrend/Point）；StudentPortal 成长页参与度构成卡与风险卡之间加「参与度周趋势」SVG 折线卡——网格(0/25/50/75/100)+面积+折线+数据点(空点小圆)+周标签，趋势徽章 上升绿/下降红/平稳紫（无有效数据显空态）。
    - 测试 test_student_insights.py 加 test_insights_participation_trend（weeks=4→series 4 点+原生类型+trend 三值），**6 passed**；eslint 0 error；Playwright verify_trend.js ALL PASS（真实学生 8 周 8 数据点 SVG 渲染，console 0）。

  - **彻查不真实显示+异常提示（2026-08-13，commit 待定）**：全局审计前端显示真实性。
    - **子代理审计** 52 个页面：3 处硬编码假数据、2 处假徽章、15 处"0 冒充真实值"、1 处假时间、4 处 catch 吞错。
    - **修复（10 个页面）**：
      - 硬编码假数据移除：Analysis 假"近8周积分趋势"图→诚实空态（需真实周数据）；ScoreAnalysis 模拟相关系数 0.68→"暂无相关数据"；Profile 伪造登录记录→空态+引导"安全事件可在系统日志查看"；Diagnostics 连接池硬编码"正常"→"未检测"。
      - 0 冒充修复（缺省无数据→'—'，合法 0 值保留）：Analysis 基础统计无学生时 null→'—'、correlation 徽章 null→"暂无关联数据"（不再误判负相关）；AlgorithmAnalysis 6 处（avg_behavior/academic/correlation/avg_current/predicted/risk_score）；Dashboard 假更新时间 null→'—'；StudentPortal 当前积分 2 处；AttendanceManage 出勤/缺勤/迟到/请假/出勤率 stats null→'—'；MentalHealth 平均心情/压力/睡眠；ScoreRecords 平均成绩。
      - 吞错修复：AttendanceManage fetchStats catch 加 setStats(null)+showToast。
      - **判断原则**：合法 0 值（置信度 0%、异常数 0、风险分 0、优秀率 0%）不改——0 是真实语义；仅"缺省无数据"场景改 '—'。ScoreAnalysis 考试统计 0 人/0% 合法不改。
    - **设备状态审计**：后端无 mock，Device.status 由 MQTT 心跳维护（真实链路），/api/devices/online 的 is_online:True 语义正确（查询即在线过滤）；开发环境无真实设备时 status 可能陈旧——已记录待后续增强"最近心跳时间"显示。
    - 验证：eslint 0 error；Playwright verify_honest.js——5 关键页无 ErrorBoundary/无 5xx/无 console，Analysis/ScoreAnalysis/Diagnostics/Profile 诚实文案命中；vite build 通过。
    - **经验**：① 前端"0 冒充真实值"是可信度最大杀手——无数据必须显示 '—'/'暂无'，但**合法的 0（置信度 0%、异常数 0）不能改**，判断依据是字段是否为"缺省 null"而非"真实 0"；② 子代理审计比手工遍历高效（52 页面一次出清单）；③ 假数据修复优先选"置空+诚实空态"而非"造真实数据"（避免二次造假）。

  - **彻查第二轮+全部修复（2026-08-13，commit 待定）**：P0/P1/P2 级不真实显示修复。
    - **P0（失败伪装安全/成功，最危险）修复 6 处**：Analysis/ScoreAnalysis 风险预警卡 warnings 为 null（接口失败）时灰"—无法获取"而非绿"无预警"；DataSyncPage healthy 三态（null 灰"未确认"，不再初始 true 绿"良好"）；MentalHealth alerts 三态（null 灰"预警加载失败"而非绿"所有预警已处理"）；Dashboard warnings null→'—'、dashboardData 失败→顶部琥珀警示条"数值可能不完整"。
    - **P1（操作假成功）修复 2 处**：courseSchedules.export / classes.export 从 `window.open`（无 token、无法感知失败）改为 fetch blob + a.download（带鉴权、校验 res.ok），CourseSchedule/ClassManagement handleExport 改 await + try/catch + 成功/失败 toast。
    - **P2（失败冒充空/加载中）修复**：Dashboard "算法数据加载中..."→"暂无算法数据"、lastUpdateTime 仅接口成功才更新；Diagnostics 4 个 fetch 加 loadError 状态+顶部警示条；OperationLogs 失败→"日志加载失败请刷新"（不再伪装"暂无操作记录"）。
    - **判断为误标不改**：DeviceManagement lastUpdateTime 成功路径才设+null 不渲染（无假时间）；ScoreAnalysis 考试统计 0 人/0% 合法。
    - 验证：eslint 0 error；vite build exit 0；Playwright verify_honest2.js——7 个改动页（analysis/score-analysis/dashboard/diagnostics/operation-logs/data-sync/class-management）无 ErrorBoundary/无 5xx/无 console。
    - **剩余 P4（低影响 catch 吞错 ~18 处）**：RemoteNotify/DeviceGroup/SubjectManagement/ImportConfigManagement/TimeRuleList/UserDetail/WakeOnLan/Profile/RuleList/NLPManagement/ClassCompare/Settings 等页面 API catch 仅 console.error——失败显示空态但有空态文案，影响低，记为待办（后续批量加 error 态）。
    - **经验**：① "失败伪装成安全"是最高危模式（预警/健康/一致性类徽章必须 API 成功才显示绿态）；② window.open 导出无法感知失败且不带 token，统一改 fetch blob；③ 三态（null=未确认/true=好/false=坏）比布尔两态更诚实。

  - **P4 级 catch 吞错全部修复（2026-08-13，commit 待定）**：上轮遗留的 ~18 处低影响吞错清零。
    - **扫描分类**：12 个页面的 catch 用脚本按行为分类（toast/setError=OK 不改、仅 console.error=P4 修复、EMPTY 实际是 showToast 误判）——真正 P4 约 16 处。
    - **重大新发现**：`WakeOnLan.tsx` API 失败时 **fallback 到硬编码假设备**（办公室电脑/服务器/00:1A:2B...）——上轮"彻查不真实显示"漏网的假数据！已移除：失败 → setDevices([]) + setLoadError(true)，渲染区分三态（加载失败红条/无设备空态/设备列表）。Playwright 验证无假设备出现。
    - **12 个页面加统一 loadError 模式**（state + catch 置 true + 成功置 false + 顶部琥珀警示条"XX加载失败，请刷新重试"）：RemoteNotify（模板/定时/历史/统计 4 处）、DeviceGroup（分组/统计/设备 4 处）、SubjectManagement（科目/教师/班级/关联 4 处）、ImportConfigManagement、TimeRuleList、UserDetail（recordsError 独立标记，因已有 error 是整页级）、Profile、RuleList（分类/模板，`loadError && !error` 避免与规则列表错误重复）、NLPManagement（算法列表/分析数据）、ClassCompare、Settings。
    - **顺手修复**：ImportConfigManagement antd Modal `destroyOnClose` 弃用警告 → `destroyOnHidden`（Playwright 发现的 2 条 console error 的唯一来源，antd 新版 API）。
    - 验证：eslint 0 error（12 页）；vite build exit 0（25s，仅既有 CSS 警告）；Playwright verify_p4.js **ALL PASS**——11 页无崩溃/无 5xx/console 0，WakeOnLan 无假设备+诚实空态。
    - **经验**：① catch 块分类脚本（按 toast/setError/console 关键字）比肉眼快且准——"EMPTY"类实际是 showToast 被正则误判，需人工复核；② **API 失败 fallback 假数据是"不真实显示"审计里最隐蔽的一类**（仅在失败路径执行，正常路径看不见），审计要专门搜 `catch.*set[A-Z]|fallback|default.*devices` 模式；③ 统一错误警示条模式（loadError + 琥珀条 + 成功置 false）已成 P4 修复标准范式。
