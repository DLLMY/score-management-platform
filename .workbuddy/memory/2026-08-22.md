# 2026-08-22 工作日志

## 按《全面代码审查报告-20260821.md》推进后端+前端整改（P0→P3）

依据报告 §9 优先级逐项落实，遵守项目铁律：G2 RBAC 一致性、G5 OpenAPI 零漂移、不自动提交、后端改动强杀重启、F17 防腐层只迁写入/事务路径。

### 后端（#840–#845，已落地）
- #840 records_routes 综合分重算 3 处 `except:pass` → logger（修复数据正确性隐患，中危）。
- #841 security_routes 默认密钥缺失即 `raise RuntimeError`（fail-closed）。
- #842 删死依赖 xlrd/xlwt（utils/excel + requirements）/ web-vitals（package.json）。
- #843 测试内 `except:pass` 改 `pytest.fail`（3 文件）；并暴露+修复 `test_get_user_analysis_empty` 误用不存在 user_id 的"假绿"缺陷 → 14 passed。
- #844 print→logger 收口 + 基础设施/审计 `except:pass` 日志化（structured_logger.py:102 递归陷阱刻意不改）。
- #845 package.json 删 CRA `eslintConfig` 残留。

### 前端关键路径测试（#846，P0，本次完成）
- 扩展 `src/services/__tests__/api.test.ts`：信封归一化（success/data 提取、success:false 业务失败抛错）+ `getErrorMessage`（error_code 优先 / 技术错误屏蔽 / 状态码兜底），14 tests。
- 新增 `src/tests/components/PermissionGuard.test.tsx`：superadmin 直通、加载中转圈、12s 超时恢复 UI、空权限+已登录恢复、空权限+未登录跳转、单/多权限 allow-deny、fallback，11 tests。
- 新增 `src/stores/permissions.test.ts`：hasPermission/hasAny/hasAll、角色标志、fetchAndApply 空权限兜底（admin→['all']、teacher→16 项默认集，修复登录后卡死锁）、toast/theme store，10 tests。
- 新增 `src/tests/components/ScoreChart.test.tsx`：趋势百分比（涨+ / 跌- / 单点--）、标题，5 tests。
- 新增 `src/tests/components/RankBoard.test.tsx`：班级榜总分/平均分/班级数、学生榜积分与剩余开锁 x/y，2 tests。

### 验证闸门（#847，本次完成）
- 前端 vitest 全量：**24 文件 / 217 tests 全绿**（报告基线 16 单测 → ≥40 达成）。
- `tsc --noEmit` 干净；ESLint 改动文件干净；`vite build` 成功。
- 后端 G2：DB 权限目录 70 条，seed 66 条，`一致性 OK, 无缺失需补齐`。
- G5 OpenAPI 未跑全量（#846/#847 仅改前端测试文件，无后端契约变更，不受影响）；如需硬证可另跑 `run_validation.py`。

### 待协调后续（未自动启动）
- 报告 P1 分页整改（per_page 统一等）因 G5 契约漂移风险，需与用户协调后再做。
- 未提交；遵循"用户确认后再 commit 并 push"规范。

---

## P1 列表分页整改（#848–#853，本次完成；用户确认范围：Tier1 规模化表 + 后端+前端分页控件）

### 关键机制澄清（#848）
- **G5 闸门只比对 paths（路径+HTTP方法）**，不比对 response schema → 分页整改（不改路径）必然零漂移。
- `APIResponse.success(data, **kwargs)` 的 `kwargs` 会作为信封**兄弟键**附加（如 `pagination`）；`APIResponse.pagination(data,...)` 则返回**嵌套** `{data, pagination}`（/api/student/records 用嵌套）。
- `unwrapEnvelope`（api.ts:695）对标准信封返回 `rawData.data` → **兄弟键 pagination 会被丢弃**，嵌套形态才保留（getRecords 模式）。

### 后端落地（#851，Agent 完成，11 处）
- devices_routes 667/700（/class/<id>、/admin/<id>，data=数组+兄弟键）、exam_routes 83/203/479（/exams、/scores、/score-analysis/rankings/<id>）、user_management 75/231（黑名单/状态列表）、subject_routes 87、course_schedule_routes 499、security_routes 346/389（suspicious-ips/rate-limit-status 裸字典内追加 pagination）。
- 跳过 10 处：导出/聚合/OTA 广播预筛选/内部反查/.limit().all()。479 排名全局序号用 `(page-1)*per_page+idx+1` 保留。
- **注意**：GET /api/users（users_routes.py UserList）**历史已分页**（default=100，data 嵌套含 page/per_page/pages）→ UserList 前端分页本已正常，无需改。

### 前端落地（#853，本次完成）
- **防截断回归修复（关键）**：后端 5 端点新分页默认 20 条 → api.ts 方法显式 `per_page=200`（M9 上限）：`subjects.getAll` / `courseSchedules.getAll` / `exams.getAll`（rest.per_page 未传才补）/ `scores.getAll` / `devices.getAll`（新增 page/per_page 参数 + interface 同步）。
- **UserList**：已 DataTable 受控（total/page/pageSize/onPageChange + reducer 分页），后端 data 嵌套形态 → 零改动达标。
- 下拉/引用型（subjects/exams/classes/admins）与轮询页（DeviceManagement）保持全量（per_page=200），不接真分页（客户端筛选+轮询与服务端分页语义冲突）。
- rankings/suspicious-ips/rate-limit-status **无前端消费者** → 前端跳过。
- 验证：tsc 0 错、eslint 0 错、api.test.ts 14 过、全量 vitest **203 passed/3 skipped**（唯一 error 为 getErrorMessage.test.ts 的 vitest 4.1.11 teardown 竞态 `Closing rpc while onUserConsoleLog was pending`，**单独跑稳定通过，属环境噪音非代码回归**）、pytest 关键子集（devices/exam/course_schedule/security）全过、G5 467=467 零漂移、G2 通过。

### 未提交
- 所有改动（含历史批次）未 commit；遵循"用户确认后再 commit 并 push"。

---

## 报告 P2/P3 续批（2026-08-22 晚，用户"根据报告继续完成优化"）

### 已完成（低风险批量收口）
- **P2 ML 重依赖瘦身**：requirements.txt 删 `catboost`+`gensim`（nlp_ml_service 均为 try/except 可选导入，CATBOOST_INSTALLED/GENSIM_INSTALLED 降级完备；torch 顶层硬依赖/transformers/xgboost/lightgbm/shap 保留）。
- **P2 print→logger**：`app/config_init.py` 15 处 print 全改 logger（RBAC 检查/R1 Redis/CSRF/配置校验/三中间件），顶部加 logger import、删 validate_secret_keys 内冗余局部 import。
- **P2 配置源收敛（文档化落地）**：`config.py` 头部标注配置体系职责边界——Config=Web 权威源（灌 app.config）；config/__init__=兼容层；config_loader=**NLP/算法专属参数源**（intent_words/position_weights，algorithm_service+nlp_enhanced_service 活依赖，勿删）；密钥三点（config.py/security.py/config_init.py）同源 env + 均 fail-closed。激进重构（pydantic-settings 单 Config）风险高收益低，未做。
- **P3 缓存层日志化**：`services/redis_cache_service.py` 4 处 except:pass → logger（_register/打开日志文件失败=warning、close 失败=debug、_store_tags 标签登记失败=warning——缓存失效不完整风险）；就绪探测轮询（预期失败）加注释保留 pass 防刷日志。
- **P3 N+1 修复**：`exam_routes.py` scores 列表（to_dict 访问 subject_rel.name）+ `course_schedule_routes.py` 课表列表（循环访问 class_info/subject）补 `joinedload`（含 import）。

### 验证
- pytest：test_exam_routes + test_course_schedule_routes + test_course_schedules_now 全过（EXIT=0）。
- G5：467=467 零漂移（exit 0）。后端重启冒烟 swagger 200。
- 铁律：不自动提交。

### 未做（需用户确认范围，高风险/高工作量）
- **P3 大路由瘦身**：records_routes(~1050 行)/algorithm_routes(~1242 行) 读路径下沉 service——F17 风格新批次，工作量大，本轮仅规划。
- **P2 [ml] extras 完整拆分**：torch 顶层 import 需 lazy 改造，高风险，未动。
- **配置源激进重构**：同上。

---

## 报告 P3 大路由瘦身（2026-08-22 深夜，用户"继续"推进）

### records_routes 读路径下沉（第一批，3 个 GET 收口）
- **service 追加**（`services/score_record_service.py`，读路径下沉区）：
  - `query_score_records(user_id, rule_id, start_dt, end_dt, allowed_classes, page, per_page)` —— 分页查询（joinedload user/rule + 筛选 + 隔离），返回 Pagination 对象
  - `serialize_score_record(r)` —— 行序列化（列表/按用户共用，逐字段与原一致）
  - `get_score_statistics(user_id, class_name, start_dt, end_dt, allowed_classes)` —— 聚合统计（total_records/total_add/total_subtract/net_change/today_count，today_count 仅按 user_id 过滤、不套隔离——与原语义一致）
  - `_apply_isolation_filter(query, allowed_classes)` —— 隔离过滤（None=不过滤/[]=无可见/[names]=exists 白名单）
- **路由改薄**（`api/scores/records_routes.py`）：RecordList.get / RecordByUser.get / RecordStatistics.get 改为参数解析+ISO 校验(bad_request)+`_resolve_allowed_classes()` 隔离解析+调 service+响应构造；删除 `_apply_score_data_isolation`（语义迁入 service）。
- **设计约定**：service 无请求上下文依赖；隔离白名单由路由解析传入（与 F17 写路径"路由保留校验"一致）；缓存读写（statistics 的 cache_key get/set）保留路由。

### 验证
- 基线先跑（test_records_routes 16 tests 绿）→ 改造后 test_records_routes + test_cache_invalidation 全过（EXIT=0，零契约漂移）。
- G5：467=467 零漂移（exit 0）。后端强杀重启冒烟 swagger 200。
- records_routes 1058 行 → ~860 行（第一批减 ~200 行）；ScoreEntryResource.get（~280 行大编排）与 algorithm_routes 留待后续批次。

### 未提交
- 铁律：不自动提交。

---

## 大路由瘦身第二批 + 收口（2026-08-22 深夜）

### records_routes 第二批：ScoreEntryResource.get 下沉
- service 追加 `get_score_entry_data(allowed_classes=None)`（活跃规则+活跃学生表单数据；allowed_classes None/空=不过滤、非空=白名单——与原语义一致）；路由保留 score_entry_data 5 分钟缓存读写。
- ScoreEntryResource.post（~220 行）**保持不动**——已是 F17 达标模式（校验+隔离+create_score_entry service 事务+POST-COMMIT 副作用 MQTT/通知/缓存/综合分）。
- RecordResource.get 保持原样（序列化字段与 serialize_score_record 有差异——无 student_id，直接用会加字段破坏契约）。
- records_routes 累计：1058 → **949 行**（4 个 GET 下沉：RecordList/RecordByUser/RecordStatistics/ScoreEntryResource；service +~180 行）。

### algorithm_routes 判定：**无需下沉（已薄）**
- 抽查确认：全部端点（statistics/cluster/composite-score/warning/prediction/anomaly 等 ~30+）均为统一薄转发模式（参数解析 → AlgorithmService/ClusterService/CompositeScoreService/PredictionService/AnomalyService 调用 → try/except → APIResponse）。算法逻辑早已收口 services，1460 行主要是端点数量+docstring+模型定义。报告"内联逻辑"判断基于历史状态，已不成立。**不强行重构**（无下沉对象，显式 try/except 可读性更好）。

### 验证
- pytest：test_records_routes + test_score_ecosystem_service 全过（EXIT=0）。
- G5：467=467 零漂移（exit 0）。后端重启冒烟 swagger 200。
- 铁律：不自动提交。

### 结论：报告 P3 大路由瘦身收口
- records_routes 完成（1058→949，读路径全部下沉 service）；algorithm_routes 判定为薄无需改。
- 剩余可选：无（P3 大路由项已闭环）。

---

## 报告全部收口 + 全量回归（2026-08-22 深夜，最终）

### 收口核对
- 报告 §3 `log_permission_action` 3 处 except:pass（sub_accounts/rbac/admins）**已为 logger.warning**（#844 已修，报告基于 08-21 旧状态）——无需再改。
- 报告 §3/§5 其余项（db_session teardown 可接受、query_optimizer 表名内部可控、records 审计/缓存告警已日志化）均核对通过。

### 全量回归（本次会话所有改动）
| 闸门 | 结果 |
|---|---|
| pytest 关键子集（records/exam/course_schedule/devices/security/cache_invalidation/score_ecosystem） | ✅ EXIT=0 |
| G5 OpenAPI | ✅ 467=467 零漂移 |
| 后端重启冒烟 | ✅ swagger 200 |
| tsc --noEmit | ✅ EXIT=0 |
| eslint | ✅ EXIT=0 |
| vite build | ✅ EXIT=0（**坑**：vite 清空 dist 触发 safe-delete 钩子（>50 文件）→ 需 `vite build --emptyOutDir false` 或先手动清 dist） |
| vitest api.test.ts | ✅ 14 过（全量 203 过，唯一 error 为 getErrorMessage teardown 竞态环境噪音） |

### 状态
- 88 个未提交文件（分页整改/评语模块/缓存一致性/前端测试/F17 迁移/报告 P0-P3 全批次累积）。
- **未提交**：遵循"用户确认后再 commit 并 push"。git-bash 内 commit 注意 git 配置。

---

## 前后端公共组件提取（2026-08-22 晚，用户新任务）

### 评估（#860/#861/#862 完成）
- 报告：`docs/公共组件提取评估报告-20260822.md`。
- 前端基础设施已成型（DataTable/Modal/useConfirm 覆盖率高）；**最大重复：格式化函数（43 处散落 + 8 页内联）与状态徽标（55 处内联色 span）**。
- 后端分页/权限/响应已统一；**最大重复：参数解析（60 处裸 int()/type=int）与访问控制样板（12 文件 30 处）**。

### 第一批提取（#863/#864 部分，已完成并验证）
- **F1 `frontend/src/utils/format.ts`**（新建）：`formatRelativeTime/formatDateTime/formatDate/formatUptime/formatFileSize/formatHourMinute`。替换 7 页内联：FirmwareManagement/DeviceManagement/Dashboard/RemoteNotify/Notifications/PhoneBoxPolicy/ClassPeriodSettings。注意同名不同义：DeviceManagement/Notifications 的 formatTime=相对时间→formatRelativeTime；FirmwareManagement/RemoteNotify 的 formatTime/formatLastSeen=绝对→formatDateTime；PhoneBoxPolicy/ClassPeriodSettings 的 formatTime=HH:mm→formatHourMinute。fallback 按原页语义保留（'-'/'—'/'--'）。
- **F2 `frontend/src/components/ui/StatusTag.tsx`**（新建）：`StatusTag` + `STATUS_TAG_CLASS`/`STATUS_TONE_MAP` 导出（components/ui/index.ts + components/index.ts）。试点替换 Approvals（switch 审批状态）、ExamManagement（getStatusBadge 映射对象）。
- **B1 `backend/utils/params.py`**（新建）：`get_int_arg/get_float_arg/get_str_arg`（缺失/非法回退 default，可选 min/max 钳制）。试点替换 `algorithm_routes` 31 处裸 `int(request.args.get(...))`（修复非法输入 500 隐患）。
- 验证：tsc 0 错、eslint 0 错、pytest（analysis/records/exam）全过、G5 待跑（路径未变应零漂移）。

### 后续批次（未做）
- F2 推广剩余 ~50 处内联状态 span（逐页语义核对）；F3 `useListFetch` 试点；B2 `can_access_student` 收敛 utils/permission；B3 序列化统一。
- 未提交。

---

## 公共组件提取第二批（2026-08-22 深夜，"继续推广"）

### F2 推广（收尾）
- **UserList** 账号状态徽标 → StatusTag（黑名单=danger/启用=success/禁用=neutral）。累计 3 页落地（Approvals/ExamManagement/UserList）。
- **推广边界判定**：剩余 ~100 处内联色多为**按钮色/图表色/角色色卡/操作类型标签**（PermissionManagement 含 pink 色板、NLPManagement/AlgorithmAnalysis/ScoreAnalysis 为图表密集页）——语义多样不宜机械替换，**保留现状**；StatusTag 组件已就绪供新代码使用。评估报告 F2 标记"试点高频页"达成。

### B2 访问控制收敛
- `utils/permission.py` 新增 **`can_access_student(user_id)`**（含 None 前置检查 + 完整语义注释）。
- 三路由内联删除并统一调用（共 17 处）：
  - records_routes `_can_access_student`（删 def，5 处调用）
  - approvals_routes `_can_access_approval_user`（删 def，6 处）
  - users_routes `_can_access_user`（删 def，6 处）
- 全仓确认无 `def _can_access` 残留；get_by_id/User import 仍被使用无未使用告警。

### 验证
- pytest：test_approvals_routes + test_user_routes + test_users_routes + test_records_routes 全过（EXIT=0）。
- tsc 0 错、eslint 0 错（UserList StatusTag）。
- G5：467=467 零漂移（后端重启冒烟）。
- 未提交。

### 剩余候选（第三批可选）
- F3 `useListFetch` 列表页 fetch 骨架 hook 试点；B3 高频实体（User/ScoreRecord）序列化统一；B4 错误处理装饰器。
