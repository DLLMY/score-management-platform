# 2026-09-16 工作日志

## 49 页浏览器实跑 + 验收报告（收官）

### 后端 500 健壮性修复（已 commit 4eaf431）
- `services/device_service.py::create_device`：捕获 `IntegrityError` → 抛 `ValueError("设备ID ... 已存在")`。
- `api/devices/devices_routes.py::DeviceList.post`：捕获 `ValueError` → `bad_request`。
- `api/system/admins_routes.py::AdminList.post`：捕获 `IntegrityError` → `bad_request("用户名已存在")`。
- 根因：UNIQUE 冲突在 commit/flush 处未捕获，被全局 handler 吞成通用 500。改后转 400，属后端健壮性缺陷修复。

### harness 改进（run15.js，已 commit）
- 注入 `RUN_TAG=Math.random().toString(36).slice(2,8)`，marker 由 `PBT<idx>` 改为 `PBT<idx>_<RUN_TAG>`，消除跨运行确定性值碰撞（此前导致设备/分类/班级/管理员创建 500/400）。
- `smartFill`：card_id/学号 填 8-16 位数字、phone 填合法手机号（避免校验 400）。
- `pickSubmit` 放宽：扫描范围扩到 form 外（antd modal 提交按钮常在 form 外），submit 正则新增「上传/发送/下发」。
- 新增 `fillCourseSchedule`：班级×星期×节次 组合轮询找空闲时段，真正创建课程表（避开「该班级此时段已有课程」冲突校验）。

### 全量重跑结果（run15_full8，EXIT=0，5m1s）
- **49 页：28 真实落库(POST 2xx) / 16 只读展示页 / 5 harness 缺口 / 0 失败**。
- 5 个缺口（idx 7/19/26/28/41）为 harness 无法判定新增按钮/非表单类操作页，非应用缺陷。
- 报告：`gen_report.js` → `.workbuddy/browser_test/report_49pages.html`（已 commit）。

### DB 清理
- 备份 DB：`apps/backend/instance/score_management.db.bak_20260916_104154`。
- 清 PBT 测试数据：`PRAGMA foreign_keys=OFF` 后按文本列 `LIKE 'PBT%'` 删；FTS 虚拟表 `user_search_idx` 走虚拟表 DELETE 自动维护 `_content/_docsize/_idx` 影子表（**不可直删影子表**）。
- 结果：删 130 行（122 + FTS 8），`REMAINING_PBT=0`，`integrity_check=ok`。

### 提交范围纪律
- commit 4eaf431 只含：3 后端修复 + run15.js/gen_report.js/report_49pages.html。
- 未混入前序遗留改动：committee_service.py、Header.tsx、Pagination.tsx、api.ts、stores/index.ts、memory 文件（按规范待用户确认后再处理）。
- 未 push（需本地代理）。

### 经验沉淀
- 浏览器实跑 PBT 标记必须用每次运行唯一后缀，否则确定性值必与历史数据撞 UNIQUE。
- FTS5 影子表（_content/_docsize/_idx）只能经虚拟表 DELETE 维护；直删会破坏索引一致性。
- sqlite 列引用要用双引号，`WHERE 'col' LIKE` 会把列名当字符串字面量恒为 0（本轮踩过的坑）。

### A+B+C 计划收官（full11，本轮）
- **A 缺口页定向打通**：5 个缺口页（idx 7 智能评分 / 19 考勤 / 26 手机箱策略 / 28 成绩录入 / 41 数据同步）全部真正驱动写入：
  - idx7→POST /api/nlp/rules 200；idx19→POST /api/attendance/records 201；idx26→PUT /api/phonebox-policy 200；idx28→先自建 published 考试再 POST /api/scores 200；idx41→POST /api/consistency/fix 200。
  - 关键修复：NLP 限定 `div.fixed.inset-0` 弹窗内填首个 text+textarea（之前选到页面搜索框致关键词空被校验拦）；考勤轮询班级直到有可选学生（原生 select）；成绩录入用绝对 URL + `data:JSON.stringify` + Content-Type 头（之前相对路径 Invalid URL / `json:` 选项不生效）。
- **B 编辑/删除回归**：`verifyEditDelete` 只在含 `PBT` 文本的行点编辑/删除，绝不碰真实种子数据；未发现 PBT 行则跳过并记 note。
- **C 最终交付物**：`report_49pages.html`（full11 数据）。
- **全量重跑 full11（EXIT=0，~4m47s）结论**：
  - **30 已真实落库（写接口 2xx，含 PUT/PATCH/DELETE） / 0 提交被拒(4xx/5xx) / 16 只读展示页 / 3 其他**。
  - 3 个非落库页均为 harness 时序/级联依赖问题，非应用缺陷、无任何 4xx/5xx：
    - idx4 积分规则：no-submit（新增表单级联/校验未驱动）；idx10 课程表：submit-no-db（弹窗出现时 weekday/period 选项尚未从 API 加载完，下拉空）；idx36 审批管理：no-submit。
- **gen_report.js 修正**：`classify()` 将 PUT/PATCH/DELETE 2xx 也计为「已真实落库」（此前 idx26 手机箱策略 PUT 200 被误标「已提交未落库」）；副标题/统计标签同步更新为 30 页。
- **清理标记说明（待用户决策）**：`PBT` 标记仅嵌入 NLP 关键词、考试名；考勤记录/成绩行引用种子 ID 但不含 PBT 文本 → 朴素 `LIKE '%PBT%'` 清理会留孤儿行（attendance 指向已删学生、scores 指向已删考试）。如需彻底清理需按 FK 删除子行，或留待用户决定是否清理。

### 提交纪律（重申）
- 本次 run15.js + gen_report.js + report_49pages.html 改动**未 commit**（用户未显式要求；且工作区另有前序遗留无关改动 committee_service.py/Header.tsx/Pagination.tsx/api.ts/stores/index.ts 未处理）。
- 仅 commit 不 push（push 需本地代理）。

### 致命坑：dev 库 admin 密码与 run15.js 不一致 → 全量 401 假阴性（full12 教训）
- 现象：full12 全 49 页 0 ok / 33 no-submit / 16 readonly，idx28 报 `examPOST=401 未提供有效的认证令牌`，idx4/36 报「未找到添加规则/创建申请按钮」。
- 根因：run15.js 登录用 `admin / Test@123456`，但 dev 库 `admins` 表的 admin 密码 bcrypt 哈希已不再是 Test@123456（疑似前序会话跑了 `__reset_pw_tmp.py` 带自定义 RESET_PW，或库被备份还原覆盖）。登录整体失败 → 所有页落到未登录态，按钮不存在 → 全 no-submit。
- 验证：`curl -X POST :5000/api/auth/login -d '{"username":"admin","password":"Test@123456"}'` 返回 `用户名或密码错误` 即确诊。
- 修复：`apps/backend/.venv/Scripts/python.exe __reset_pw_tmp.py`（默认 RESET_PW=Test@123456）重置即可，`RESET_OK username=admin role=admin`。
- 铁律：**跑 run15 全量前先 curl 验登录**；若全量出现「全页 no-submit + 个别 401」，先查 admin 密码是否被改/库是否被还原，不要盲改 harness。
- 后续 full13 已重置密码后重跑（进行中）。

### 3 缺口页修复（idx4/10/36 专属驱动，待 full13 验证）
- 新增 `fillPointsRule`（idx4 积分规则）：点「添加规则」→ 在 `.modal-overlay` 内填 规则名称(text)/积分值(number=5)/描述(textarea)/分类(首个真实 option) → 提交「添加规则」。
- 新增 `fillApproval`（idx36 审批管理）：点「创建申请」→ 在 `div.fixed.inset-0` 内选 学生(首个 value≠0 的 option，StudentSelect 原生 select)/标题(text)/积分变化(5)/说明(textarea) → 提交「创建申请」。
- 重写 `fillCourseSchedule`：先轮询等 班级/科目/星期/节次 选项从 API 加载完（星期/节次无占位符，length>=1 即代表已加载），再循环组合避「该班级此时段已有课程」冲突；星期=day_of_week、节次=period_number、教室 placeholder=`输入教室`、提交按钮=「保存」、班级/科目原生 select。
- GAP 映射补 `'积分规则':fillPointsRule,'审批管理':fillApproval`；课程表仍走 line ~580 的专属分支调用 `fillCourseSchedule`。

### dev 库模式漂移修复（让 idx32/idx34 两个 500 落地，full15 关键修复）
- 表现：`POST /api/devices → 500`、`POST /api/firmware/upload → 500`（返回"服务器内部错误 / INTERNAL_ERROR"），但干净 payload 也 500 → 非 harness 问题，是后端真实未捕获异常。
- 根因：`sqlite3.OperationalError: no such column: device.device_secret`。`Device`/`FirmwareVersion`/`SystemConfig` 模型有差异 #1/#4 新增列，但 dev 库 `device`/`firmware_versions`/`system_config` 表是**旧 schema 创建的**——`db.create_all()` 只建缺失表、不改已有表，所以这些列一直缺失，所有触到该列的 INSERT/SELECT 都 500（`create_device` 只捕获 IntegrityError，OperationalError 漏出）。
- 漂移清单（dev 库缺列）：`device` 缺 device_secret/secret_issued_at/last_seen_ts；`firmware_versions` 缺 device_type/is_stable/rollback_to；`system_config` 缺 device_whitelist_enabled。
- 修复：非破坏性 `ALTER TABLE ADD COLUMN`（用模型列类型、置可空规避 SQLite NOT NULL 限制）+ 对有 server_default 的列回填（device_type='phonebox'、is_stable=0、device_whitelist_enabled=0）。脚本 `.workbuddy/browser_test/fix_schema_drift.py`（先备份 DB：instance/score_management.db.bak_20260916_2032），诊断脚本 `diag_schema_drift.py`。修复后重跑诊断 `NO DRIFT`，curl 复验设备 201 / 固件 200。
- 铁律：**全量 500 先看后端 traceback / curl 复验**，确认是"代码引用了库里不存在的列/表"还是 harness 填值错；dev 库被重置后极易出现此类模式漂移，先用 `diag_schema_drift.py` 扫一遍。

### 全量回归结果（浏览器实跑 49 页）
- full14（seed 前置数据后）：ok=28 / fail=2(idx32设备,idx34固件 500) / readonly=16 / no-submit=3(idx5排名规则,idx37远程通知,idx40权限管理)。3 缺口页(idx4/10/36) 已 `201` 真实落库。
- full15（schema 修复后）：**ok=31 / fail=0 / readonly=16 / no-submit=2(idx5,idx37)**。idx32/idx34 转 ok、idx40 也转 ok（idx40 是时序抖动）。
- 剩余 2 no-submit（idx5 排名规则 / idx37 远程通知）：诊断证实点"新增/添加等级"后 `div.fixed.inset-0` 弹窗 + 4/5 字段表单**能正常打开且可被 findModalContainer 识别** → 纯属 harness 点"新增"后首检过早/点击偶发被吞的时序抖动。
- 修复 harness：点"新增"后若弹窗未出现则**重标按钮并重试点击**（最多 3 轮，每轮轮询 6×700ms）。full17 全量重跑验证中（预期 idx5/idx37 转 ok，目标 ok≥33）。
- 注意：`run15.js` 启动自清空 `15_perpage.jsonl`；用 `PAGES=5,37` 部分跑会清空全量 jsonl，务必先备份或跑完再生成报告（本轮一度误清，已用 full17 全量重跑补全）。

### idx37 远程通知 no-submit 根因与专属驱动修复（full18 验证）
- 根因：remote-notify 页有两个"新建"按钮（TemplatesPanel「我的模板」/ ScheduledPanel「定时通知」）+ 右侧"发送通知"（PermissionButton，MQTT 发送无 HTTP 写）。`pickAddBtn` 正则同分下取首个"新建"（TemplatesPanel），连续跑时首检过早/点击偶发被吞 → 弹窗未被 `findModalContainer` 检出 → no-submit。单页 fresh-load 诊断能打开弹窗，证实非后端/前端缺陷，纯 harness 时序。
- 修复：新增 `fillRemoteNotify` 专属驱动（`GAP['远程通知']`），精确命中「我的模板」卡片内 h3→card 的"新建"按钮（避开另一"新建"与"发送通知"），轮询等待 `div.fixed.inset-0` 弹窗 12×600ms（比通用 6×700ms 更久），填模板名称(PBT模板+marker)/内容 → 点"保存"（handleSaveTemplate → `api.notifyTemplates.create` → `POST /api/notify_templates/ 200`，真实落库）。
- 定向 `PAGES=37` 验证：`add=True / modal=True / submit=True / POST /api/notify_templates/ 200`。

### idx21 心理健康 回归与修复（full19 收口）
- 回归现象：full17 该页 `创建第一条记录`（空态按钮）落库成功 → 在 dev 库留下心理健康记录；full18 该页变为列表态，空态"创建第一条记录"消失，仅剩顶部"快速记录"按钮 → `pickAddBtn` 正则**未含"快速记录"** → found=false → no-submit（full18 ok 总数不变，只是 idx37↔idx21 互换）。
- 本质：浏览器实跑创建的真实数据会污染 dev 库，改变后续运行的页面状态（空态↔列表态），是 **run-to-run 状态依赖**，非本次 idx37 改动所致。
- 修复：`pickAddBtn` 正则新增 `快速记录` 并归入 score=3 优先组（与添加/新建/新增/创建/发送同权）。修复后 list 态也能定位并点击"快速记录"→ 开表单 → 填 → 保存（`POST /api/mental-health/records 201`）。
- 铁律补充：**勿用 `cleanup_pbt.py`（删所有 %PBT% 行）清库**——它会连 `seed_prereqs.py` 插入的 8 名 PBT 标记种子学生一起删掉，破坏考勤/成绩等依赖学生的页。正确做法是让专属驱动/正则覆盖页面两种状态，而非依赖清库。

### 最终交付（full19，EXIT=0，5m21s）
- **49 页：33 已真实落库（写接口 2xx）/ 0 失败 / 16 只读展示页 / 0 其他**。idx37、idx21 双双转 ok，全量零 no-submit、零 4xx/5xx。
- 报告：`gen_report.js` → `report_49pages.html`（full19 数据；副标题已更新含 idx37 专属驱动 + idx21 快速记录识别）。
- 提交范围纪律：仅 `run15.js`/`gen_report.js`/`report_49pages.html`/`seed_prereqs.py`/`diag_schema_drift.py`/`fix_schema_drift.py`/`diag_nosubmit.js`/`_parse_full15.py` + `15_perpage.jsonl`(full19 证据) + 今日记忆；**未卷入** committee_service.py/Header.tsx/Pagination.tsx/api.ts/stores/index.ts 等无关改动（按规范待用户确认后再处理）。仅 commit 不自动 push（push 需本地代理）。

### StrictMode 双调用 null 崩溃修复批次（收官，commit 4a0a65d，已 push）
- 根因：React18 StrictMode 开发期双调用使 `api.ts::registerAbortController` 二次注册时 **abort 掉首个请求的 controller** → fetch 被取消返回 null → 各数据层直接 `result.xxx` / `setX(null)` 引发 `Cannot read properties of null` 系统性崩溃（Header 通知、权限 store、Workbench 等）。
- 修复（5 文件，+22/-11）：
  - `api.ts`：根因修复——`registerAbortController` 不再主动 abort 旧 controller，让其自然完成（路由切换仍由 `abortAllRequests()` 统一取消所有在途请求，不受影响）。
  - `Header.tsx` / `stores/index.ts`：对 `result` 为 null 兜底（空数组 / 抛错），避免 StrictMode 双调用时崩溃。
  - `Pagination.tsx`：页码 `Array.from(new Set(...))` 去重 + ellipsis `key={'ellipsis-'+index}` 唯一化，修复 React key 重复告警。
  - `committee_service.py`：`term_start/term_end` 经 `parse_date` 解析（已 import），覆盖创建与更新两条路径。
- 验证（四闸门全绿）：backend `py_compile` OK；frontend managed Node 22.22.2 直调 `tsc --noEmit` EXIT=0 / `prettier --check` 0 / `eslint` 0。
- 提交 4a0a65d（5 文件），push 至 `main`（72b581d..4a0a65d）。该批次独立于浏览器测试提交，未相互混入。
