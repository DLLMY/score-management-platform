# 2026-08-24 工作日志

## B3 收尾：User.to_dict（#897，批次 21）
- 收敛 16 处响应体内联 dict 序列化（users/user_management/student/box/classes/export/exam 路由）。
- 修复真实 bug：User.to_dict 误用 `weekly_unlock_limit`，真实列名 `weekly_unlock_count`（模型 line 35），已对齐。
- 回归：managed 3.13 py_compile 10 文件全绿；定向 pytest **95 passed**；G2 `verify_rbac_consistency.py --check-only` 一致 OK（70 目录 / 66 seed / teacher 30）；G5 OpenAPI 路径/方法级零漂移（仅内部序列化变更）。
- B3 战役累计：11 个高频模型（Subject/ScoreCategory/CourseSchedule/FirmwareVersion/Alert/Notification/Permission/RolePermission/SubAccount/ScoreRecord/User），**收敛 41 处**响应内联。
- 刻意不收敛：审计快照、MQTT 负载、导入预览（结构不同/需上下文）。
- 报告 9.16 节已写入 docs/公共组件提取评估报告-20260822.md（line 428-442）。
- 已清理 backend/ 下 4 个临时日志（openapi_check/pytest_b3/rbac_check/regression_b3）。

## 新任务：全面项目评估与下一步开发计划
- 用户要求：评估整体架构、各模块功能完成度、代码质量与可维护性、识别缺陷，输出结构化下一步计划（功能清单+完成状态、主要问题/风险、按优先级排序任务、目标/验收标准、实施顺序与时间安排）。
- 已建任务 #898（后端架构探索）、#900（前端架构探索）、#901（代码质量/技术债）、#899（编写计划）、#902（呈现交付）。
- 已有《全面代码审查报告-20260821.md》锚定 P0–P3 优先级基线。

## 评估复核关键更正（重要 · 推翻早期一轮误判）
- ❌ 早期误判「项目无 CI、无统一回归门禁、scripts/run_regression.sh 不存在」——**本轮重新取证已推翻**：
  - ✅ `.github/workflows/ci.yml` + `deploy.yml` 均存在；`ci.yml` 后端 job 跑 `bash scripts/run_regression.sh` + 前端 vitest/build/lint；`deploy.yml` 为发版 CD（Windows 部署 `if: false` 暂禁用）。
  - ✅ `scripts/run_regression.sh` 真实存在（在**仓库根** `scripts/`，非 `backend/scripts/`——早期 Glob `backend/scripts/*` 漏掉所以误判不存在）。5 步闸门：RBAC 一致 / OpenAPI 契约(未起后端则跳过) / 契约测试 / 关键路由 / 索引闸门。
- 量化复核（本轮实跑）：`tsconfig.json` `strict:false`（L12）；前端全仓 `any` **60 处**（api.ts 占 44）；巨型页 AlgorithmAnalysis 3938 / NLPManagement 2835 / RemoteNotify 2273 行；静默失败页 4 个（OpsCenter/SystemMetrics/SecurityAudit/FrontendTelemetry 均含 `return null`）。
- 计划文档已写入 docs/下一步开发计划-20260824.md（T1–T12，承接 2026-08-21 P0–P3 基线 + B3 战役闭环），**不再把"缺 CI/缺门禁"列为风险**。
- 遗留真实风险：CI 后端 job 不启动服务 → G5 OpenAPI 在 CI 中"跳过不阻塞"，契约由 test_api_envelope 兜底（建议后续起后端让 G5 真正生效）。
