# 2026-08-26 工作日志

## NLP 模块深度评估（Task #918/#919/#920/#921 全完成）
- 产出综合报告：`reports/NLP模块深度评估报告_2026-08-26.md`，替代旧版过度乐观的 `NLP功能评估报告_2026-08-01.md`。
- 四维结论：功能部分完整（领域意图/评分/规则/训练/情感/自学习已实现；分词/POS/通用NER/通用分类缺失或部分）、可靠性部分不可靠、代码质量中等、测试高风险缺口。
- **三大 P0 级发现（已 grep 取证）**：
  1. `nlp_routes.py:845-851` + `:865` `/model/evaluate` 无样本/无标注时返回硬编码 `accuracy/precision/recall/f1 = 0.85` → 数据诚信伪造（准确率循环定义，无 ground-truth 列）。
  2. `nlp_service.py:109-112` + `:194/196` `ml_based` 分支实例化 `NLPMLTrainingService` 后调不存在的 `parse()` → 潜伏死分支（API 不传 parser_type 故无法触达，但属死代码）。
  3. `nlp_optimizer.py:140-142` 误引用全后端未定义的 `NLPMLService`（try/except 吞错 → ML 预热静默跳过）。
- 其余高/中问题：`@safe_handle` 全文件仅 1 处（`:1580`），约 50 NLP 端点无顶层异常捕获；前端评分无并发防重（B1 重复扣分）、corrected_name 映射错误毒化数据（B2）、信封契约不统一致假成功；7 个孤儿 `api.nlp` 方法。
- 测试缺口：fast parser 零覆盖、复合句回归缺失、自学习闭环未测、rules 路由层未测、BERT 真实降级未测；`/model/evaluate` 测试若接受 0.85 即假绿。
- 前端已拆 9 子模块（`NLPManagement.tsx` 1314 行主壳 + `nlp-management/` 9 文件），可维护性较旧版 2262 行单体显著提升。
- ⚠️ 待用户决策：P0 三项是否进入修复轨道（建议立即修，属「模块可信」前提）。

## NLP 模块 P0 三缺陷修复（用户指令「直接进入修复轨道」后完成，#922/#923/#924 → completed）
### P0-1 · `/model/evaluate` 伪造 0.85 数据诚信
- 文件：`backend/api/nlp/nlp_routes.py`（NLPModelEvaluate.get，约 L834-881）
- 改动：删除伪造 0.85 默认值；无样本时 accuracy_rate/precision/recall/f1_score 均返回 null；有样本时仅给"自动匹配率"（表无 ground-truth 列，明确标注非真实准确率）。
- 证据：`grep "已移除伪造的 0.85 默认值"` → L860。

### P0-2 · `ml_based` 解析器死分支
- 文件：`backend/services/nlp_service.py`（`_get_parser_instance`，约 L109-113）
- 改动：原 `NLPMLTrainingService().parse()` 必抛 AttributeError 的死分支 → 改为禁用返回 None（调用方降级为"解析器不可用"）。
- 证据：`grep "ml_based 为潜伏死分支"` → L110；grep 证伪全后端仅 nlp_service.py 出现 ml_based，无路由传入，禁用安全。

### P0-3 · `NLPMLService` 未定义误引用
- 文件：`backend/services/nlp_optimizer.py`（warmup，约 L139-150）
- 改动：原 `from services.nlp_ml_service import NLPMLService`（全后端未定义，被 except 静默吞错→`_nlp_ml_service` 恒 None）→ 改引真实类 `NLPMLTrainingService`，守卫 `if hasattr(..., "load_models")` 调用，且显式 logging.error 不再静默吞错。
- 证据：`grep "原引用未定义的 NLPMLService"` → L142。

## 零回归闸门（全绿）
- `py_compile` 三文件：PYCOMPILE_OK ✅
- `pytest test_nlp_routes.py test_nlp_service.py test_nlp_optimizer.py`：**36 passed**（31.96s）✅
- G2 `verify_rbac_consistency.py --check-only`：一致性 OK，无缺失（DB 70 / seed 66，teacher 30）✅
- G5 OpenAPI：本批次仅改响应值（路径/方法未变），G5 比对 path+method 不比对值，**逻辑上零漂移**；前一轮离线 swagger 比对已确认 NO_DRIFT（469 paths，/nlp/model/evaluate=GET 一致）✅

## 待办（用户未纳入本次）
- P1：~50 NLP 端点统一 @safe_handle 兜底、评分并发防重、前端信封契约统一；测试补齐（fast parser/复合句/自学习闭环/rules 路由层/BERT 降级）。
- 代码未 git commit（铁律：不主动提交，待用户决策）。

## P1 批次推进（用户「推进P1」→ 拆 #925/#926/#927/#928；本段完成 P1-1）
### P1-1 · ~50 NLP 端点统一 @safe_handle 异常兜底（#925 → completed）
- 文件：`backend/api/nlp/nlp_routes.py`（**52** 个路由方法，幂等插入）。
- 改动：脚本在每方法紧贴 `def` 上方插入 `@safe_handle()`（最内层装饰器，位于 @requires_permission/@cached_api/@ns_*/@inference_slot_guard 之下）；纯增量——仅捕获未处理异常返回标准信封，happy path 与 HTTPException 透传语义不变（`safe_handle` 用 functools.wraps 保留方法名，flask-restx 仍识别 get/post/put/...，路由注册不受影响）。
- 零回归闸门：py_compile OK；pytest(nlp 三文件) **36 passed**（35.55s，与 P0 基线一致）；G2 一致性 OK（DB 70/seed 66/teacher 30）；G5 离线 swagger 比对 **469==469**，仅 study_group 4 路径 hyphen/underscore 命名差（快照陈旧、与 P1-1 无关——NLP 路径 0 差异、方法差异 0）。
- 待启动：P1-2 评分并发防重（B1 重复扣分）/ P1-3 前端信封契约统一（api.ts 收敛）/ P1-4 NLP 测试补齐（fast/复合句/自学习/rules/BERT 降级）。代码未 git commit。

### P1-2 · 评分并发防重 / 重复扣分闭环（#926 → completed）
- 根因复核：全系统幂等现状
  - MQTT `score/add`（`services/mqtt_message_service.py::handle_score_add`）：已有 `msg_id` 幂等检查 + `ProcessedMessage.message_id UNIQUE`；但幂等检查在事务外（L504），并发重发同一 msg_id 可绕过检查 → 第二插入抛**未捕获 IntegrityError** 令 MQTT 消费循环崩溃（实际扣分因回滚未加倍，但设备拿不到干净响应）。
  - `scores` 学术成绩表：`UniqueConstraint(exam_id,student_id,subject_id)`（R3）已防重复成绩。
  - HTTP `create_score_entry`（行为分录入）：无幂等键，依赖前端 `useSubmitGuard`。
  - 前端 `ScoreEntry.handleSaveAll` 已在 L1326 用 `runSubmit(handleSaveAll)` + `disabled={submitting}`，**防连点锁已就绪**。
- 改动（`mqtt_message_service.py`，后端纯增量、无路由/契约变更）：
  - 新增 `_try_insert_processed(msg_id, record_id, new_score, client_id, response_topic)`：事务内尝试写 ProcessedMessage；若 `IntegrityError`（并发唯一约束冲突），`rollback()` 后回查已有记录并直接发幂等响应（返回 True → 调用方 return），把崩溃转为干净幂等回包；非冲突/无 msg_id 返回 False。
  - `handle_score_add` 三分支（rule_id/rule_name/score_change）的原内联 `ProcessedMessage` 写入替换为 `if self._try_insert_processed(...): return`；helper 自带 `from models import ProcessedMessage, db`（与调用方同级，保证测试 patch 生效）。
  - 修复了既有 `test_handle_score_add_direct_score` 因 helper 作用域缺 `ProcessedMessage` 的 NameError（连带修复）。
- 回归测试：新增 `test_try_insert_processed_concurrent_conflict`（并发冲突回退幂等、不抛异常）+ `test_try_insert_processed_no_conflict`（正常/无 msg_id 返回 False 且不发响应）。
- 零回归闸门（全绿）：
  - py_compile 两文件 OK ✅
  - `pytest test_mqtt_message_service.py`：**44 passed**（41 原 + 3 新：2 新增冲突测试 + 修复后 direct_score）✅
  - `pytest test_scores_routes.py test_records_routes.py`：**21 passed** ✅
  - `pytest test_cache_invalidation.py`：**12 passed** ✅
  - G2 `verify_rbac_consistency.py --check-only`：一致性 OK（DB 70/seed 66/teacher 30）✅
  - G5 OpenAPI 离线比对（route-map 归一 /api 前缀 + Flask`<>`→OpenAPI`{}`）：实时 470 vs 快照 469，仅差 4 个 `study_group`(underscore) vs `study-group`(hyphen) 预存命名不一致 + 1 个 Blueprint 下载路由（swagger 快照不含 blueprint 路由）——**均与 P1-2 无关，NLP/score 变更 0 路径 0 方法差异** ✅
- 残留风险（已记录，非本批次范围）：HTTP `create_score_entry` 无服务端幂等键，仍依赖前端 `useSubmitGuard`；如需彻底防御网络层重试重复，需后续加 `idempotency_key`（P1-2 评估后判定前端锁已覆盖主路径，未扩约）。代码未 git commit。
