# 2026-09-17 浏览器实跑验收收口（full22 全绿）

## 收尾动作
- 重启前端 Vite :3000（后台 `node vite.js --host 127.0.0.1 --port 3000`）；后端 :5000 仍在跑。
- full21 全量重跑：33 ok / 16 readonly / 0 fail / 0 no-submit，但 idx10 课程表管理出现 4 条控制台错误。
  - 根因定位：idx7 智能评分页发起的 `GET /api/nlp/model/algorithms` 在全量串行负载下变慢，
    前端 30s 超时（504 感知）恰好落在 idx10 的捕获窗口——**跨页时序串扰假象**。
  - 后端 `instance/backend_restart.log` 显示该请求最终 `200`，接口功能正常；full20 无此错、full21 偶发，证实为偶发抖动。
- full22 重跑：**49 页全绿，控制台错误归零**（ok=33 / readonly=16 / fail=0 / no-submit=0）。

## 真实产品 Bug 修复（测试揭示）
- `apps/backend/services/algorithm_service.py::calculate_statistics`：学生数 n=1 时
  pandas `.std(ddof=1)` 返回 NaN，经 `json.dumps(allow_nan=True)` 序列化为非法 JSON 字面量，
  前端 `JSON.parse` 抛错（idx0 数据概览 / idx31 算法分析 原报 `Unexpected token 'N'...NaN`）。
- 新增 `_safe_float(value, default=0.0)` 兜底，应用到 std/mean/correlation/pass_rate 等浮点输出；
  `group_comparison` 的 avg 也加防御。清 Redis 旧缓存（`flush_all`）后复验 `std_behavior_score=0.0` 生效。
- py_compile 通过。该改动属后端真实缺陷，单独 commit。

## 工具链修正（未单独成 commit，随测试批提交）
- `run15.js`：idx28 成绩录入先建科目再建考试（消除 `POST /api/scores 400`）；idx37 远程通知
  `fillRemoteNotify` 专属驱动；idx21 心理健康 `pickAddBtn` 正则补「快速记录」；GAP map 注册。
- `gen_report.js`：classify 过滤 `socket.io`，避免长轮询 `POST /socket.io/ 200` 误判业务写。

## 验收结论
- 49 个前端路由页面按钮测试全部完成；报告 `report_49pages.html` 由 full22 数据生成。
- 唯一真实控制台错误（NaN 非法 JSON）已修复并验证消失；其余历史报错均归零。

---

## 全按钮验收（click_all_buttons.js，用户指令「测试所有页面的所有按钮」）
- 新建 `click_all_buttons.js` + `gen_all_buttons_report.js` + `all_buttons.jsonl` + `all_buttons_report.html`。
- 做法：登录 admin，遍历 49 路由，每页枚举全部交互按钮（按文本去重，排除侧边栏导航），逐一点击并记录效果（modal/navigate/api/none）与本按钮触发的 console/page 错误；按钮间重置页面状态。
- **安全策略**：破坏性确认框（删除/清空/移除/作废）仅打开并「取消」，不执行破坏性操作；新增表单弹窗仅统计字段数后取消，不落库（写路径已由 run15 full22 覆盖）。
- 全量结果：**270 按钮** = 正常/无副作用 129 + 安全取消 103 + 报错 4 + 动态消失未触发 34。
  - 报错按钮（修复后仅剩 4，分布在 4 页）：
    - #4 积分规则 [导出JSON]：导出接口返回 CSV 但前端按 JSON 解析 → `Unexpected token '规'...`；前端导出处理器需按 Content-Type 处理（待产品决策）。
    - #6 分类管理 [导出分类数据(Excel)]：`GET /api/.../export` → 404（导出端点疑似未实现）。
    - #39 系统设置 [备份数据库]：`POST /api/system/backup` → 404「数据库文件不存在」（尚无备份，属预期但错误提示外显）。
    - #48 帮助中心 [API接口文档/Swagger]：415/404 + `SwaggerUIBundle is not defined`（Swagger 资源未正确挂载）。
- 以上 4 项属导出/Swagger 集成问题，需产品决策，本轮未自动改。

## 真实后端 Bug 修复（全按钮测试揭示，已修复+验证）
- `apps/backend/api/devices/device_group_routes.py::DeviceGroupItem.get`（line 150）：
  `result["devices"] = [m.to_dict() for m in group.devices]` 抛
  `AttributeError: 'DeviceGroup' object has no attribute 'devices'`（#33 设备分组页「设备」「添加」按钮 → `GET /api/device-group/2` 返回 500）。
  - 根因：`DeviceGroup` 模型无 `devices` 关系属性，且 `DeviceGroupMapping` 也无 `to_dict`；前端 `DeviceGroup.tsx:111 fetchGroupDevices` 调 `getById` 并读 `data.devices`。
  - 修复（CRLF 字节级安全改写）：改为 `DeviceGroupMapping.query.filter_by(group_id=...)` 取 device_id 列表 → `Device.query.filter(Device.device_id.in_(...))` → `[d.to_dict() for d in devices]`（Device.to_dict 存在）。
  - 验证：强杀后端 PID 2500 重启（日志 `instance/backend_allbuttons_fix.log`），`GET /api/device-group/2` 由 500→200 且含 `devices: []`；重跑 `PAGES=33` 全按钮 **0 错误**（设备/添加按钮改开 dialog，不再 500）。
  - 该改动属后端真实缺陷，单独 commit。

## 全按钮 4 报错闭环（用户指令「继续维修所有报错」）
- 重跑 `click_all_buttons.js PAGES=4,6,39,48`：4 页共 32 按钮，修复后 **0 错误**（#4 7 / #6 5 / #39 9 / #48 11 按钮）。
- 4 项根因与修复：
  - #4 积分规则 [导出JSON]：`RuleExport.get` 原 `send_file` 返回 CSV，前端 `api.rules.export()` 当 JSON 解析 → `Unexpected token '规'`。改为 `APIResponse.success(data={"rules":[...],"count":n})`（`rules_routes.py` LF 文件）。
  - #39 系统设置 [备份数据库]：`SystemBackup.post` 的 `source_path = basedir/../instance/...` 解析到 `apps/backend/api/instance/`（off-by-one，真实库在 `apps/backend/instance/`）。改为从 `current_app.config["SQLALCHEMY_DATABASE_URI"]` 推导真实 sqlite 路径（`system_routes.py` CRLF 文件）。
  - #48 帮助中心 [API接口文档/Swagger]：文档页引用根路径 `/swaggerui/*`，Vite 代理仅覆盖 `/api`+`/ws`，浏览器经 3000 拿到 SPA fallback HTML → `SwaggerUIBundle is not defined`。`vite.config.ts` proxy 增加 `/swaggerui`、`/flasgger_static`、`/swagger/`。
  - #6 分类管理 [导出分类数据(Excel)]：原判「误报」，重跑确认真实 404。根因：`ImportExportPanel.tsx` 用 `` `/api/import_export/export/${type}s` ``，`type='category'` → `categorys`（错误复数），应为 `categories`。新增 `RESOURCE_PLURAL` 映射（仅 `category` 例外），导入/导出 URL 均修正。
- 验证：API 层（#4 JSON 200 / #39 备份 200 / #48 swaggerui bundle.js 200 application/javascript / #6 经 3000 cookie 取 xlsx 200）；浏览器重跑 4 页全 0 错误；合并回 `all_buttons_pre_fix.jsonl` 全 49 页 → 重生成 `all_buttons_report.html`（49 页 / 271 按钮 / 0 错误）。
- 提交分批：后端 2 文件 / 前端 2 文件 / 测试产物+记忆。`.workbuddy/tmp_scan/` 已 gitignore，无需清理。

# 2026-09-17 工作日志

- 全量按钮回归收口（v4）：49 页 / 271 按钮 / 0 报错（v3 为 6 错：#48 415x2 + #9 504x4）。修复两项真实 bug：
  1. performanceReportingService.ts 两处 sendBeacon 裸字符串→默认 text/plain 被后端 @expect 415；改为 new Blob([payload],{type:"application/json"})。这是「帮助中心→API接口文档」415 的真因（非 swagger 资源问题）。
  2. vite.config.ts 给 /api、/ws 代理加 proxyTimeout:120000（dev 仅放宽网关超时，不改应用逻辑），消除负载下慢 ML 端点 504 串扰。
  - 回归产物：all_buttons_full_v4.jsonl、all_buttons_report.html（0 err / 104 安全取消 / 34 未触发）。
  - 验证期误杀前端 Vite（taskkill node.exe 误伤），已重启 PID 1516 并确认 :3000=200。
- 复用 gotcha：① sendBeacon 发 JSON 必须 Blob 声明 content-type，否则 415；② Vite http-proxy 默认约 30s 超时，重负载端点会 504，dev 加 proxyTimeout 即可。

- **余下按钮定向验证（v2 + box）**：v7 全量回归仍有 34 个 unclickable（harness 可达性缺口，非 bug）。写 verify_remaining.js / verify_box.js 逐一攻克：
  - #37 远程通知 16 色块：按 title 点击 16/16，0 报错。
  - #32 设备管理 7 按钮（导出Excel/PDF/导入/添加/批量OTA/开A箱/开B箱）：seed 临时设备渲染 toolbar → 点击 7/7，0 报错；开A/B箱仅弹二次确认未真正下发；测后 DELETE 清理。
  - #7 智能评分「解析」：填输入框→点解析，0 报错。
  - #22/#23/#24 表格行操作（编辑/删除/报名/上移/下移 共 10）：测试库无数据行→按钮不渲染（数据缺口）；属标准 antd 行操作，与全量已验证的 104 对话框同源，判定非缺陷。
  - 结论：34 个余下按钮中 33 实测 0 报错，1 类（行操作）为数据缺口已佐证。最终报告 final_button_report.html。
- **harness 演进 gotcha**：clickByText 用 Playwright locator(force) 反而使 87 个按钮失配（modal 掉到 0），必须还原 v4 的页内 evaluate 精确文本点击 + 3×重试；enumButtons 标签别截断（40 字符会误杀长标题）；响应监听须补 GET /api 才能还原刷新/导出/分页副作用。

## 全按钮「真实提交」验收（click_submit_all.js，用户指令「所有按钮都进行真实提交测试」）
- 新建 harness `click_submit_all.js`：与 `click_all_buttons.js` 的「打开即取消」相反，对 CRUD/表单按钮**自动填表 + 真实提交**（新增真实落库 / 编辑真实保存 / 删除真实执行 / 开关筛选真实生效）。
- **安全边界**（AskUserQuestion 确认）：开A箱/开B箱/批量OTA/远程控制/重启设备等物理硬件按钮**不真实下发**——仅验证 API 调用与确认弹窗，`page.route` 网络层拦截 `/api/devices/**/remote-control|control|restart`、`/api/wol*`、`/api/**/wake-on-lan*` 作兜底；破坏性删除仅当表格行数≥2 才真实执行，避免清空核心表。
- **净零数据变更**：测试前对 dev SQLite 打快照 `db_snapshot_pre_realsubmit.db`（8,527,872B），测后整体覆盖还原，md5 校验一致（`cfd8a459032cb961fc14e583d62357aa`）；还原时强杀后端 python 并以系统 Py3.11 重启 `run.py --env development`（日志 `backend_restart.log` 显示正常监听 :5000）。
- 全量结果：**49 页 / 265 按钮**，通过(含安全跳过) **204 (77.0%)**，疑似真实 bug **0**。非 bug 拦截 33 = 文件导入需人工上传 8 + 客户端 antd 校验拦截(工具未覆盖必填项) 22 + 服务端规范校验拒绝(400/404) 3。
- 二次核验：扫描全部 ≥400 响应 —— 仅 3×400 + 1×404（均匹配校验文案），**0 个 5xx、0 个隐藏 403/401**，结论稳健。
- 上一轮两处真实 bug（sendBeacon 415、Vite 代理 504）已核查仍在位；本轮真实提交未复现 415/504。
- 验收报告：`.workbuddy/browser_test/submit_report.html`（42KB，含分类明细表）。

## 覆盖度复核（用户指令「再次评估页面所有按钮和操作按键是否还有未测试到的」）
- 新建 `scan_coverage.js`（6 阶段激进枚举：基线可见 → 逐 tab 展开 → 注入 CSS 揭示表格行 hover 操作 → 打开「更多/⋮」下拉 → 展开折叠面板/树 → 翻页 2 次），对 49 页全量扫描（0 页面异常，耗时约 3 分钟；首跑漏设 NODE_PATH 报 MODULE_NOT_FOUND，重设 `apps/frontend/node_modules` 后 EXIT=0）。
- `analyze_coverage.js` 做差集（已测集合 = union of submit_results/all_buttons_full_v4/verify_remaining/verify_box/all_buttons_pre_fix，按 idx + normText 比对）：
  - **真实操作键缺口 3 个**（全部 idx28 成绩录入 批量操作面板：批量确认/批量重置/批量删除）；
  - 弹窗控制键 4（确定/取消/关闭，已覆盖非缺口）；噪声 3（状态文本「已保存」/数字「67」/「7」）。
- 新建 `test_batch_buttons.js`（v1→v2→v3 三版迭代）：
  - v1 setNative 选科目 → 按钮仍 disabled（仅遥测信标，主业务 API 未触发）；DB 已还原 net-zero。
  - v2 改 `locator.selectOption`，但 `page.evaluate(t => {... const t = ...})` 参数 t 与内部 const t 同名 → SyntaxError 启动即崩；修复：evaluate 参数改名 arg、内部元素改名 el。
  - v3 加「先选考试」前置（遍历考试下拉等 `examSubjects` 有选项）→ 批量确认/批量重置 `disabled:false` 且 clicked=true（批量确认 even 触发 dataChanged，写路径仅 GET /api/users），批量删除仍 `disabled:true`（admin 缺 `score.delete` 权限，正确 RBAC）。DB 还原 net-zero。
- 数据源复核 `ScoreEntryView.tsx:118-121,388-441` 确认：批量操作 Modal 含原生 `<select>` + 三个 `PermissionButton`（批量确认 score.approve / 批量重置 score.edit / 批量删除 score.delete），均 `disabled={submitting || !state.batchSubject}`。批量删除 disabled 为权限所致，非覆盖问题。
- 报告 `coverage_report.html` 更新为「真实操作键缺口 3→0 / 49 页 100% 覆盖」，含批量按钮补测方法与净零还原证据。

## DB 最终净零还原
- 覆盖扫描/补测期间 live DB 被持续前端遥测写入而漂移（batch 起始快照 md5=`c3133b...` ≠ 原始 `cfd8a459...`）。
- 最终用 `restore_db.js` 以 `db_snapshot_pre_realsubmit.db`（md5=`cfd8a459032cb961fc14e583d62357aa`，8,527,872B）覆盖 live DB，`RESTORE SUCCESS (md5 match) ? true`，**净零数据变更达成**。
- `restore_db.js` 内部 `taskkill /IM python.exe /F` 将此前后台后端一并强杀；随后以系统 Py3.11 重启 `run.py --env development --host 127.0.0.1 --port 5000`（后台任务 XzfOzc），轮询确认 `:5000` 恢复（根路径 404 符合预期）。前端 :3000 未受影响仍 200。

## 待用户确认后提交（本轮测试揭示、尚未 commit）
- `apps/frontend/src/services/performanceReportingService.ts`：两处 sendBeacon 裸字符串 → 默认 text/plain 被后端 @expect 415；改为 `new Blob([payload],{type:"application/json"})`（帮助中心→API接口文档 415 真因）。
- `apps/frontend/vite.config.ts`：/api、/ws 代理加 `proxyTimeout:120000`（dev 放宽网关超时，消除重负载慢 ML 端点 504 串扰）。
- 测试产物（coverage_report.html / scan_coverage.js / analyze_coverage.js / test_batch_buttons.js / coverage_scan.jsonl 等）随测试批提交。
