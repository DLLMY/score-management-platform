
## 科目管理空列表 + POST 误报"科目名称已存在" 双 bug
- 用户报告：科目管理页 "暂无科目数据" + 3 个统计卡全 0，但点"添加第一个科目"提交 → POST /api/subjects 400 "科目名称已存在"
- 真根因：`backend/api/academics/subject_routes.py:101` GET 列表返回 `APIResponse.success(data={"items": result})`（envelope 剥后是 `{items: [...]}`，非数组），而 `frontend/src/services/api.ts:3089-3090` 的 `subjects.getAll()` 用 `Array.isArray(result) ? result : []` → 永远空数组 → 6 处消费者（SubjectManagement/CourseSchedule/ClassTimeSettings/ExamManagement/ScoreEntry）都看不到任何科目 → 用户以为没数据。
- 触发链：列表显示空 + 3 卡全 0 → 用户点"添加第一个科目" → DB 实际有 12 条（含 `t`、`冒烟科目`、`__TEST_SUBJECT__` 测试脏数据）→ 应用层查重 `Subject.query.filter_by(name=...).first()` 撞到 `t` → 400 "科目名称已存在"（即使名字根本不是用户想的那个）
- 修复：
  1. **后端 GET 改返回数组**（subject_routes.py:101）：`data={"items": result}` → `data=result`，与 `/api/classes`、`/api/scores` 列表端点形状一致。6 处消费者已假定 `Subject[]`，自动恢复。
  2. **清理 3 条测试脏数据**（id=10 `t`、11 `冒烟科目`、12 `__TEST_SUBJECT__`）：备份 `instance/score_management.db.test_subj_bak`。
- **踩坑（勿回退）**：曾尝试给 POST 查重加 `is_active=True` 过滤（想"已禁用允许重建"），结果**从 400 变成 500**。因为 `subject.name` 列本身有 **DB 层 UNIQUE 约束** —— 应用层查重被绕过后 DB 拦截抛 `sqlite3.IntegrityError`。查重必须保持原样 `filter_by(name=data["name"]).first()`，且 PUT 那处（subject_routes.py:209/214）也不要加 `is_active`，否则会引发同样的 UNIQUE 报错。
- **重要踩坑**：Flask-SocketIO `socketio.run(app, debug=True)` 在本项目**不会自动 reload**（实测代码改了 subject_routes.py 后，进程还在 LISTENING 但 curl 仍命中旧逻辑）。改完 backend 代码必须**强杀全部 python.exe 后重启**才生效。kill 用 `Get-Process python | Stop-Process -Force`（Git Bash 的 `kill -9` / `taskkill //F` 在 Git Bash 下参数被吃）。
- 验证：GET 返回 9 项数组；POST 新名 201；POST 同名/种子已存在 400。

## 删除按钮无反应真凶：useConfirmDialog 缺 UI 渲染
- 用户报告：规则管理点击删除 → 无反应、无网络请求
- 根因：hooks/useModal.ts::useConfirmDialog 只 setState，**没有任何全局 <ConfirmDialog /> 渲染这些 state** → show() 返回的 Promise 永远 hang → await showConfirm(...) 阻塞 → DELETE 不发送
- 修复：RuleList.tsx::handleDelete 改用 window.confirm() 直接确认，跳过坏掉的 hook
- 同 bug 影响范围：所有使用 useConfirmDialog 的页面（CategoryList/CourseSchedule/ClassManagement/DeviceGroup/FirmwareManagement/DeviceManagement/RankRuleList/RemoteNotify/TimeRuleList 等）—— 删除/敏感操作都会卡住，待统一修复
- 真正修复方向：需要写一个全局 <ConfirmDialog /> 订阅 store 中的对话框 state，或在每个页面本地渲染一个对话框组件

## 其他本轮修复
- RuleList.tsx:608 textarea null 警告：编辑规则时 rule.description 为 null → ?? ''
- api.ts::cacheDependencyMap 补充 5 组缺失映射：rules/attendance/duty/committee/records（解决删除后列表缓存不清导致重复点击 404）
- Vite dev server 已用 --force 重启 + 清空 .vite 缓存

## NLP 解析 500：datetime - str
- 报错：POST /api/nlp/parse → 500 `unsupported operand type(s) for -: 'datetime.datetime' and 'str'`（NLPManagement 智能解析触发）
- 根因：`models/__init__.py` 中 `NLPScoringRule.last_used_at` 声明为 `db.String(50)`，SQLAlchemy 从 String 列加载恒返回字符串；但 `services/nlp_enhanced_service.py::_compute_rule_stats` 原写 `(datetime.now() - rule.last_used_at)` 直接做减法 → 崩溃。缓存失效（cache_time 过期）且某规则 last_used_at 非空时必现。
- 复现数据：DB 中 rule 2 的 last_used_at = '2026-08-03 20:55:07.380018'（字符串）。
- 修复：新增模块级 `_coerce_dt(value)`，把 str/date/datetime 统一解析为 datetime（多格式 strptime + fromisoformat 兜底），`_compute_rule_stats` 改为先 coerce 再减。并给 import 补 `date`。
- 验证：① DB 真实字符串值经 `_coerce_dt` 解析成功（recency=0.870，无异常）；② 实测 POST /api/nlp/parse 返回 200。
- 顺带清理：端口 5000 上有 7 个遗留 `run.py --debug` 进程（reloader 父进程），已全部 kill，restart 单一干净后端（run.py --env development --host 127.0.0.1 --port 5000 --debug）。保留 backend/test_nlp_http.py 作冒烟脚本。

## NLP 反馈记录 400：NLPCorrection 模型/代码三重不匹配（同一天第二轮）
- 报错：POST /api/nlp/feedback/record → 400 `记录失败: 'original_text' is an invalid keyword argument for NLPCorrection`（NLPManagement 点击反馈/纠正确认触发）
- 根因三层：① 模型 `NLPCorrection` 只有 8 个旧列，缺 original_text/field_type/original_value/corrected_value/status/confidence_after/learn_count/last_learned_at/verified_at 共 9 列，而路由+服务大量读写这几列；② 路由构造 NLPCorrection 只传 original_value，漏 field_type/corrected_value，且没设 input_text（模型 NOT NULL）→ INSERT 先触发 IntegrityError；③ nlp_routes.py 根本没 `import g`，`g.current_user.id` 抛 NameError 被 except 吞，corrected_by 恒 null。
- 修复：① models/__init__.py 补 9 列；② DB `ALTER TABLE nlp_corrections` 补同 9 列（先备份 instance/score_management.db.nlp_corr_bak_*）；③ nlp_routes.py 构造补 input_text/original_text/field_type/original_value/corrected_value；④ nlp_routes.py 加 `from flask import request, g`；⑤ utils/permission.py 的 requires_permission/requires_role 校验出 admin 后 `g.current_user = admin`。
- 验证：反馈 200 + 纠正落库 + corrected_by=1（admin id）；GET /api/nlp/corrections 200 且新列全部可读。保留 backend/test_nlp_feedback.py 作回归冒烟。
- 注：requires_permission 此前从不设 g.current_user，因此所有经它鉴权的端点里读取 g.current_user 都会失败（被防御性 except 吞掉）。本次一并修通，其他端点若依赖 g.current_user 现在也能用。

## NLP 解析后顶部红框 toast：重复渲染 + 错误颜色（同一日下午）
- 现象：NLPManagement 点击「解析」后，顶部中央出现红底 toast「已识别姓名/行为/意图，但未匹配到现有规则；已展示**相近**规则推荐，可一键应用或手动修正。」，右下角同时出现蓝底同义 toast「已**提示**相似规则推荐...」。用户问"为什么红框错误提示"。
- 真相：NLP 解析完全成功（姓名/行为/意图/置信度均正常返回）。**红框不是真错误**，是 bug。
- 根因：项目里有两个 toast 渲染器共存，共用 `useToast()` 状态：
  - **新版** `context/ToastContext.tsx` 内部渲染器（右下角，按 type 映射蓝/琥珀/红/绿）✅
  - **旧容器** `components/feedback/ToastContainer.tsx`（顶中央，写死 `type === 'success' ? #22c55e : #ef4444`）❌：所有 info/warning/error 都被强制渲染成红底+白字+AlertCircle 图标。`App.tsx` 仍挂着 `<ToastContainer />`。所以一次 `showToast('info', ...)` 弹出两条同义 toast（顶部红 + 右下角蓝）。
- 修复：`App.tsx` 移除 `<ToastContainer />` 与对应 import；`ToastContainer.tsx` / `ToastContainer.jsx` 文件保留（仍被 components/index.ts 导出，测试和别处可能引用），但不再挂载。
- 验证：硬刷新后顶部红框消失，仅保留右下角正确着色的同义提示。
- 文字措辞细微差异（"已展示相近" vs "已提示相似"）说明两条 toast 来自不同时机的代码——红框很可能是早先一次解析（HMR/缓存中旧版本）残留；现在两条合并为一条，问题不复现。

## NLP 智能评分「规则管理」列表空白：envelope 双重剥离（同一日第三轮）
- 现象：用户进 NLP 智能评分页面 → 切到「规则管理」tab → 表格表头齐全（关键词/描述/分数/类型/标签/使用次数/准确率/操作）但 tbody 完全空。DevTools 控制台**只**看到 `GET /api/nlp/model/evaluate 25.80ms` 和 `GET /api/nlp/rules/statistics 54.5ms`，**没有** `GET /api/nlp/rules` 请求。`nlp_scoring_rules` 表实测有 53 条，`/api/nlp/rules?page=1&per_page=20` 200 + 20 items。
- 根因：**`api.ts::request()` 在 691-697 行已经自动剥离 envelope**（`rawData.success !== undefined && rawData.data !== undefined` → `data = rawData.data`），但 `NLPManagement.tsx` 全部 17 处用 `if (response.success) { ... response.data.X ... }` **二次剥离**——`response.success` 恒为 `undefined` → 静默不进 if → `setRules` 不被调用 → rules state 永远 `[]` → 表格空。这是项目里典型的"前端自己又解包一次"模式。
- 注意：`api.auth.login` 等少数端点用 `skipDataExtract: true` 保留原始 envelope；其余端点都已剥。判断方式：搜索 `if (response.success)` + 紧跟 `response.data.X` 即为此 bug。
- 修复：`NLPManagement.tsx` 全量替换：22 处 `if (response.success)` → `if (response)`，9 处 `response.data.X` → `response.X`；`showToast('error', response.message)` → `showToast('error', '操作失败')`（因为 message 在 envelope 层，剥了后没有）。备份在 `.tsx.bak`（已删）。
- 验证：Vite HMR 已热加载修复后代码（`curl /src/pages/NLPManagement.tsx?t=...` 看到 `if (response)` + `setRules(response.items)`）。用户**Ctrl+Shift+R 硬刷新**即可看到 53 条规则。
- 影响范围：除 `NLPManagement.tsx` 外，其他页面若有 `if (response.success) { ... response.data.X ... }` 模式（特别是同样调 `api.X.Y()` 的页面），也会同样静默失败。建议全仓搜 `if (response.success)\b.*\n.*response\.data\.` 排查。

## 全仓 envelope 双重剥离 bug 扫尾修复（同一日晚间，用户"全部修复吧"）
- 用户意图：把上一轮警示的"前端自己又解包一次 envelope"模式全仓扫一遍并修。
- 机制确认：`api.ts::request()` 仅在 `rawData.success !== undefined && rawData.data !== undefined` 时剥离（`data = rawData.data`），否则原样返回。所以后端返回 `{success,data}` 的端点被剥；返回 `{success,message,topic}`（无 data）的端点不被剥。
- 真正 bug（已修）：
  1. `ImportConfigManagement.tsx` 5 处 `if (response.success) { response.data }` → `if (response) { setConfigs(response) }`（list 返回剥离后的数组）；delete/setDefault/update/create 改为依赖 request() 抛错判断成败，toast 用占位文案（message 已被剥）。
  2. `ClassTimeSettings/ClassManagement/CourseSchedule.tsx` 的 `openImportModal` 各 1 处 `res.data.map(...)` 崩溃（res 已是数组）→ `res.map(...)`。
  3. `api.ts` 的 `importConfig.*` 与 `analysis.getClassCompare` 接口/实现返回类型从"envelope 形状"改成"剥离后的真实形状"（ImportConfig[] / ImportConfig / unknown）。消费者仅上述 5 文件，安全。
- 非 bug（已核对，勿动）：
  - `RemoteNotify.tsx`：后端返回 `{success,message,topic}` 无 data 字段 → 不剥离 → `result.success` 有效。
  - `RuleList.tsx`：两处显式 `skipDataExtract:true`，保留 envelope，有效。
  - `SubjectManagement/ExamManagement` 的 `handleImportComplete(result)`：是 ImportExportPanel 子组件回调参数（import 结果对象），非直接 api 响应，有效。
  - 导入类端点（`DeviceManagement/DataSyncPage/CourseSchedule/ClassTimeSettings/ClassManagement/ImportExportPanel`）的 `result.success`+`success_count/failed_count`：返回的 data 负载自带 success/统计，剥离后仍有效。
  - `ClassCompare.tsx`：原误判为 bug。实测 `/api/analysis/class-compare` 返回**嵌套** `{success,data:{success,period,days,data:[...]}}`，外层剥后内层仍含 success+data，`data.success`/`data.data` 正确 → 已回退改动保持原样。
- 顺带修后端 DB 漂移：`/api/import/configs` 实测 500 `no such column: import_configs.import_type`。模型 `ImportConfig` 有 14 列、DB 表仅 13 列，缺 `import_type`（models/__init__.py:743 `db.String(50), index=True`）。已 `ALTER TABLE import_configs ADD COLUMN import_type VARCHAR(50)` + 建索引 `ix_import_configs_import_type`；备份 `instance/score_management.db.import_type_bak`。复测端点 200、data 为数组、count=0（暂无种子数据）。此修复同时让上面 3 个导入弹窗可用。
- 类型检查：6 个改动文件 0 错误；全仓剩余 4 个 tsc 错误均为 tsconfig.json 选项版本不兼容（moduleResolution:bundler 等），与本次无关，由 Vite 自带工具链处理。

## NLP 智能评分整页崩溃：algorithms.map (response.data 整值误用，同一日晚)
- 报错：`NLPScoringManagement` 渲染即崩 `Uncaught TypeError: Cannot read properties of undefined (reading 'map')` @ NLPManagement.tsx:1407（`algorithms.map`）。ErrorBoundary 捕获，整页白屏。
- 根因：上一轮扫尾只修了 `response.data.X`（带点的二次解包），**漏了 `response.data` 作为整值**的用法（正则 `response.data\.` 不匹配无点情况）。`api.ts::request()` 已把 `/api/nlp/model/algorithms` 的 `{success,data:[...]}` 剥成数组本身，但 `fetchAlgorithms` 仍写 `setAlgorithms(response.data)` → `algorithms` 被设成 `undefined`（数组无 `.data` 属性）→ 渲染时 `.map` 崩。
- 排查扩展：全仓 `response\.(message|code|success|data)` 在 NLPManagement.tsx 还暴露 7 处同类整值误用（均为 `request()` 已剥 envelope、却仍读 `response.data`/`response.message`）：
  - 442 `setStatistics(response.data)` / 453 `setModelEvaluation(response.data)` / 554 `setTrainingResult(response.data)` / 574 `setTrainAllResult(response.data)` / 594 `setEvaluationAllResult(response.data)` / 748 `setBenchmarkResults(response.data)` / 763 `setOptimizerConfig(response.data)`；外加 555/575 `showToast('success', response.message)`（message 已被剥 → 显示 "undefined"）+ 375 `response.message || '应用失败'`（有兜底，安全）。
- 修复（api.ts 不动，避免破坏正在跑的 parse/rule 流程）：NLPManagement.tsx 全部改为用剥离后的 `response` 整值，局部类型不同处加 `as unknown as <本地类型>` 转换：
  - `setAlgorithms/Statistics/ModelEvaluation/TrainingResult/TrainAllResult/EvaluationAllResult(response as unknown as X)`；benchmark/optimizer 两处 state 是 `any` 直接 `setBenchmarkResults(response)` / `setOptimizerConfig(response)`；
  - 555→`'模型训练成功'`、575→`'全部模型训练成功'`、375→`'应用失败'`（去掉被剥的 message）。
  - `trainingHistory`(464) 与 `corrections` 原本就用 `response.items`（正确，未动）。
- 验证：`/api/nlp/model/algorithms` 返回 14 项数组（svm/random_forest...）→ 修复后 `algorithms` 有值，`.map` 不再崩。Vite HMR 已热加载，用户**Ctrl+Shift+R 硬刷新**即可。
- 说明：`stores/asyncStore.js` 也有 `response.data` 整值误用，但全仓无文件 import 它（死代码），不在本次范围，未动。
- **ADD 2026-08-04 PM**：ClassTimeChecker 已存在（`backend/services/class_time_checker.py`）但基于 `TimeRule`（全局，非按班级）；下发渠道 5+4 = 9 条（MQTT/score/approve/user 已接，WOL/定时/Celery/OTA/模板 未接）；无审计、`/course-schedules/now` 不存在。用户选了 B+C 组合方案（按真实课表反查 + super_admin 唯一 force_send + 实时徽章），Phase1+2 后端先开。
- **ADD 深夜 23:47 风险答疑后拍板**（上课时间下发互斥方案最终决策）：①**自习课也拦**——课表空格（subject_id 空=自习）视为上课，该班当天 course_schedules 有任意记录且当前时间落入 class_period 窗即拦截；②**改课表立即生效**——实现优先直查 DB 天然即时，若引入缓存则改课表端点加刷新钩子；③**TimeRule 由排课教务角色维护**（非仅超管，需新增权限授该角色）。逃生舱仍仅 super_admin 可 `notification.force_send`。三项已同步进 MEMORY.md「上课时间下发互斥」节，方案定稿，待用户确认开工 Phase 1。
- **ADD 23:59 手机箱开箱纳入**：用户要求上课时间默认不允许学生开箱拿手机。调研确认开箱已有 `check_time_valid`(services/mqtt_message_service.py:15) 基于 TimeRule.allow_unlock 全校拦截（reason=not_in_time），但全校一刀切、不按课表。方案调整为：开箱与学生刷卡→班级课表挂钩，上课中（含自习）硬拦，复用升级后的统一 ClassTimeChecker；**仅拦学生自助刷卡（`phonebox/unlock` → handle_unlock_message），管理员远程开锁（unlock_a/unlock_b、/api/mqtt/unlock、devices command）不受限**（用户拍板：班主任紧急随时可开）**。判定优先级：TimeRule 允许开锁时段优先放行 → 否则课表上课中拒绝（reason=CLASS_IN_SESSION）。已同步 MEMORY.md。


## 科目删除异常：软删除改真删除+级联（当晚）
- 用户报告："科目删除异常"
- 根因：DELETE /api/subjects/<id>（subject_routes.py）原是**软删除**（仅 `subject.is_active=False`），带来两异常：①删后科目记录仍在库，`name` 仍占 DB UNIQUE，导致无法重建同名科目（POST 报"科目名称已存在"），且在"含停用"视图/导出里依旧可见；②`SubjectClass.subject_id` 与 `CourseSchedule.subject_id` 外键均 `NOT NULL` 且无 ON DELETE CASCADE（models/__init__.py:701/720），软删后这些关联记录成孤儿悬空引用。
- 修复：DELETE 改为**真正级联删除**——先 `cascade_delete_related_records(Subject, id)`（清理 SubjectClass 等 NOT NULL 子表、study_guide/homework 可空外键置 NULL），再 `db.session.delete(subject)`。subject_routes.py 顶部 import 加 `cascade_delete_related_records`。备份 instance/score_management.db.subj_del_bak_20260804。
- 验证（端到端，Python311 起后端）：建测试科目→造 SubjectClass 关联→DELETE 返回 200 "科目已删除"→subject_classes 关联计数 0（级联生效）→同名重建 201（UNIQUE 释放）。无 500。
- 副作用：真删科目会**连带删除其课程表排课（course_schedules）**，符合"删除科目"语义且与其他删除端点（exam 等）一致；若需保留排课需另行处理。
- 注意：POST/PUT 查重仍保持 `filter_by(name=...).first()`，勿加 is_active（见上轮 UNIQUE 踩坑）。
