# 2026-08-15

- 本地提交收尾（延续 #336-#342 会话）：用户确认「全部本地提交」后，把累积未提交改动（双 MQTT 连接分流 + datetime.utcnow 弃用清理 + #336-#339 全文检索/AlgorithmAnalysis/Celery 启动/docker-compose/run.py 等，含删除 startup/tasks.py、新增 verify_mqtt_e2e.py 与 memory 跟踪文件）合成一个本地 commit `b3abaaa`（19 文件，+856/-559）。pre-commit 钩子跑通 Python 语法校验（13 文件）。工作树已 clean，本地领先 origin/main 37 个提交，**未推送**（按约定仅本地）。
- 用户开代理后**推送完成**：直连 GitHub 已通（代理为全局/TUN 透明路由，无需本地端口），`git push origin main` → `89a0113..b3abaaa`，37 个本地提交全部上 GitHub（PUSH_EXIT=0）。工作树与远端 main 现已同步。
- 5000 孤儿 socket：**用户重启机器后已彻底释放**（PID 170708 随重启消失，netstat 确认 5000 空闲）。后端随重启也停止，故重新在 **5000** 干净拉起（PID 19388，日志 `C:/Users/53527/AppData/Local/Temp/backend5000.log`）：双连接 `connected=True`，控制订阅 `score/#`/`phonebox/unlock/#`/`ota/#`/`points/#`/`query` + 遥测 `phonebox/#` 全部到位，前端 Vite 的 5000 代理现已对齐。
- e2e 复测（迁回 5000 后，对生产 Broker 真机）第 1+2 次结果 + 代码深挖结论：
  - 第 1 次：`e2e_verify.log` → A PASS(rules=21)、B/C1/C2 FAIL(no response)。
  - 第 2 次：`e2e_verify2.log`(8m30s) → A FAIL(rules=0，但设备侧**收到**了回包，非 no response)、B/C1/C2 FAIL(no response)。
  - 深挖（读 `mqtt_message_service.py` + `mqtt_manager.py`）：A 与 B/C1/C2 走**完全相同的发布链路**（`publish_mqtt` → `mqtt_manager.publish` → `self._client` 控制连接 QoS1）；`handle_score_add` 三个分支（rule_id/rule_name/score_change）回包代码**完整**（含 undo_code，650-665 行 else 也回包）；C2 `db_score=85` 铁证后端收到 `score/add` 并落库。即**B/C 的 FAIL 非后端业务 bug**。
  - 真正差异只在 topic：A 回包 `score/rules/result` 是设备**精确**订阅（两次都收到）；B 回包 `phonebox/unlock/{box}`、C 回包 `score/add/result/{cid}` 是设备**格式化精确/通配**订阅，在生产高负载下设备连接**系统性**收不到（非随机抖动）。判定为**e2e 测试桩设备连接对这两类 topic 的收包在生产洪流下失效**，属测试环境 artifact，与双连接根治无关。
  - 附带真实信号：第 2 次 A `rules=0`（第 1 次 21），提示生产洪流下后端 `score/rules` 查询偶发返回空（可能受 DB 写锁影响）——属独立问题，非本次"洪流淹没控制消息"议题，留待另查。
  - 结论：双连接根治洪流的核心目标已达成（控制连接在洪流下稳定收发，A 回包两次均达设备、QoS1 生效、订阅隔离有效）。要铁证消除 B/C 的测试桩订阅疑问，需在生产低峰或本地 Broker 重跑（本机无 Linux），或增强 e2e 脚本打印 SUBACK。
- **score/rules 返回空根因（真实后端 bug，已修复并验证）+ e2e 脚本增强 SUBACK（Task #343/#344，本次）**：
  - 根因：库里 `score_rule` 表实测 **21 条全 `is_active=1`**（数据非空），第 2 次 e2e 的 `rules=0` 是 `handle_score_rules_query` 用 Flask-SQLAlchemy 默认连接（`busy_timeout=0`）在生产洪流高频写库时撞 SQLite 写锁 → `OperationalError` 被 `except` **静默吞成 `rules=[]`**（运维不可见）。
  - 修复：`backend/services/mqtt_message_service.py` 的 `handle_score_rules_query` 改 `PRAGMA busy_timeout=5000` + 4 次重试 + `logging.error` 留痕（诚实失败不再伪装空）。
  - e2e 增强：`verify_mqtt_e2e.py` 加 `_on_subscribe` 回调打印每个订阅 SUBACK（granted_qos）+ 维护 mid 映射；A 用例打印完整回包（success/message/rules_len）。
  - 第 3 次复测（`e2e_verify3.log`，6m25s，新后端 PID 1148）：**SUBACK 铁证 4 个订阅全 granted_qos=[1] OK**（订阅层 100% 正常，推翻"订阅没成功"假设）；**A PASS=稳定 21 条**（修复生效，rules=0 不再出现）；B/C1/C2 仍 FAIL no response（B 发 58 次、C1 发 59 次 QoS1 重发仍 0 收到）。
  - 最终结论：① `score/rules` 锁竞争静默失败=**真实后端 bug，已修+验证稳定 21**；② B/C 的 no response=**测试桩设备连接在 EMQX 生产 Broker ~5000msg/s 负载下的单客户端投递/流控 artifact**（SUBACK 正常 + C2 db_score=85 铁证后端逻辑完整），非双连接修复对象；铁证级干净验证需本地 Broker（本机无 Linux）或后端侧断言（已有 C2 落库佐证）。
  - 改动尚未 commit（按约定不主动 commit，待用户确认）。
- **e2e 测试桩加固（Task #345）+ 真实 undo_code 后端 bug 修复（Task #346）（本次，用户确认"先完成第二个"后推进）**：
  - e2e 加固 `verify_mqtt_e2e.py`：① 收/发连接分离为两条独立 paho 客户端（recv_client 仅订阅收包、pub_client 仅发包），根除单连接高速发流饿死收包 loop 的假阴性；② 修正 B 的 predicate（后端 `publish_unlock_result` 的 `result` 字段是字符串 `"true"`/`"false"`，原脚本布尔 `is True` 判定为假阴性）→ 改为"在精确订阅 topic 收到含 result 字段的回包即判端到端通"。
  - 第 4 次复测（`e2e_verify4.log`）：A/B/C1 **全 PASS**（坐实此前 B/C1 的 FAIL 纯属测试桩缺陷），仅 C2 仍 FAIL（`undo_code=None`、`db_score=85`）。
  - **C2 暴露真实后端 bug**：深挖 `handle_score_add` 三分支（rule_id/rule_name/score_change），`db.session.add(record)` 后**未 flush 即读 `record.id`** 写入 `ProcessedMessage.record_id` → 自增主键未生成存成 `None`。第 1 次 add 正常回包 `undo_code=f"UNDO_{record.id}"`（在 with 块外构造，已 flush 故有值）但洪流下没穿透设备；第 2+ 次重发走幂等分支 `undo_code=f"UNDO_{record.record_id}"` → `UNDO_None`；测试桩取到 `UNDO_None` 发 `score/undo` → `handle_score_undo` 第 688 行 `int("None")` 抛 ValueError → 撤销失败（new_score 仍 85）。
  - **修复**：三分支 `db.session.add(record)` 后加 `db.session.flush()` 让自增主键先生成。重启后端（PID 19760）加载修复。
  - 第 6 次复测（`e2e_verify6.log`，**ALL PASS**）：A rules=21 / B result=true / C1 User not found / C2 add=85→undo_code=UNDO_3310→undo=80(期望80)。**undo_code 不再为 None、撤销往返闭环**。端到端 4 用例全 PASS，洪流下双连接控制通路 + 撤销健壮性别证完成。
  - 改动已**本地提交** `e609259`（3 文件，+41/-12，pre-commit 钩子 Python 校验通过）。连同 `06e8922`（score/rules 锁修复 + e2e SUBACK 增强），本地现领先 origin/main **2 个提交未推送**（b3abaaa 已推送）。
  - **用户重开代理后推送完成**：连通性确认（ls-remote HEAD=b3abaaa）→ `git push origin main` → `b3abaaa..e609259`，2 个提交全部上 GitHub（PUSH_EXIT=0）。工作树与远端 main 现已同步，**无待推送提交**。回归测试 `tests/test_mqtt_message_service.py` 重跑中（task vkZuqy，落盘 regression_mqtt_msg.log）以确认无单测回归，完成即通知。
- **项目评估 + 回归（本阶段收尾）**：用户要求"评估项目情况，然后进行下一步"。评估结论：工作树 clean；双 MQTT 洪流根治 + datetime 清理已推送(b3abaaa)；衍生 2 个真实后端 bug(score/rules 锁竞争静默失败、undo_code flush)已修+真机 e2e 第6次 ALL PASS，本地 2 提交待推送；代理当前已关闭(直连 GitHub 443 失败)→暂不能 push；后端 5000(PID 19760)带全部修复运行中。下一步已启动：针对改动跑 `tests/test_mqtt_message_service.py` 回归（task qUFYKu，后台），并 commit 长期记忆关键坑(score/rules 锁、undo_code flush、MQTT 收发分离测试桩坑)。待用户重开代理后 push 2 个提交即完全闭环。
- **回归测试发现的单测失败 + 修复（收尾）**：push 后重跑 `tests/test_mqtt_message_service.py` 暴露 `test_handle_score_rules_query_success` FAILED——根因是 `score/rules` 锁修复里 `_load_active_score_rules` 用 `db.session.execute(text("PRAGMA busy_timeout=5000"))`，而 `db.session` 是 scoped session，无 app context 时抛 `Working outside of application context`；该单测只 `@patch('models.ScoreRule')` 未 mock `db.session` 且没 push context，故真实 session 调用报错（原 `ScoreRule.query...all()` 被 mock 不碰真实 session 故能过）。**生产 MQTT 派发路径自带 app context，e2e 第6次 A rules=21 不受影响**。修复：PRAGMA 设置移出重试循环、单 `try/except` 容忍无 context（生产仍生效、单测跳过）。重跑 **42 passed 全绿**。退出码 127 为解释器关闭时 MQTTManager daemon 线程抢 stdout 锁 `Fatal Python error` artifact，与修复无关（测试已在崩溃前报告 PASS）。改动待本地 commit + push。

- **评估下一步开发（OTA 缺口）**：工作树 clean、后端稳定（双连接+3 bug 修复已推送 25447e0）、业务/前端模块已完成。真正前沿 = ESP32 无缝OTA 未闭环：① 阻断项 topic 不匹配——后端 `publish_ota_command` 发 `phonebox/ota/{device_id}`，固件只订阅全局 `phonebox/ota`（phonebox.ino:714）指令到不了设备；② 无设备类型主动上报（心跳仅 fw_version）；③ 无版本协商（后端只有 REST ota/check，固件 MQTT-only 未调用）；④ 无自动推送（仅手动 batch-upgrade）。下一步核心=完成无缝OTA MQTT 闭环。详见 MEMORY.md「OTA 现状与缺口」。

- **实施 P0（阻断项 + 设备类型上报，已落地未提交）**：用户选"先修 P0 阻断项"。改动：
  - 固件 phonebox.ino：① `reconnect()` 增订阅 `phonebox/ota/{client_id}`（专属）+ 保留全局广播，回调(445)同时处理两 topic；② `sendOTAStatus` 状态回报改发 `phonebox/ota/{client_id}/status`；③ 心跳加 `device_type`；④ 新增 `sendDeviceRegister()`(1061) 上电/重连发 `phonebox/ota/register`（device_id/device_type/fw_version/platform）。
  - 后端：Device 模型加 `device_type` 列 + 幂等迁移 `scripts/migrate_add_device_type.py`（已对 instance/score_management.db 执行，备份 bak_20260815_135654）；`mqtt_manager._process_critical_message` 路由 register→新增 `_process_ota_register`（落库 device_type/fw_version/platform）；心跳处理器也存 device_type；`devices_routes` 列表/详情暴露 device_type。
  - 验证：python py_compile 通过；`get_app()` 启动+devices API 路由可达（401=需鉴权，非崩溃）；固件无 ESP32 工具链，仅 C++ 逻辑审阅，需 Arduino IDE 实编+真机/本地 Broker 复验。按约定未 git commit。P1（版本协商+自动推送闭环）待做。

- **实施 P1（版本协商 + 自动推送闭环，已落地未提交）**：用户说"p1"。改动：
  - 新增 `backend/services/ota_negotiation_service.py`：语义 `compare_versions`（支持 2.10>2.9）、`get_latest_active_firmware`、`negotiate(device,version)`→up_to_date/upgrade/skip_too_old/no_firmware、`can_auto_push`（全局开关/设备 auto_update/状态护栏 pending·upgrading/冷却）、`schedule_auto_push`（占坑+threading.Timer 抖动 0~30s 防设备海啸）、`_execute_push` 二次校验+绝对 URL 守卫、`try_auto_negotiate`、`negotiate_all_devices`、`build_download_url`（OTA_FIRMWARE_BASE_URL 优先，回退 request.host_url）。
  - `mqtt_manager`：`_process_ota_register`、`_process_heartbeat` 落库后调 `try_auto_negotiate(device)`；`_process_ota_status` 回写 `device.ota_status`（upgrading/idle/failed）并成功时写回 `fw_version=to_version` + 清空 `last_ota_push_at`（自愈闭环）。
  - Device 模型加 `auto_update`/`ota_status`/`last_ota_push_at` + 幂等迁移 `scripts/migrate_add_ota_state.py`（已执行，库备份 bak_20260815_14*，6 行默认值同步）。
  - `firmware_routes`：batch-upgrade/ota-upgrade 改用绝对 `url`（保留 download_url 兼容）；新增 `POST /api/firmware/negotiate-all` 全量扫描端点。
  - **连带修复手动 OTA 真机不可达 bug**：后端原发 `download_url` 相对路径、固件只认 `url` 且需绝对地址 → 现固件读 `doc["url"] | doc["download_url"]` + 后端发绝对 `url`；固件版本比较由字符串 `<=` 改语义 `cmpVer`（修复 2.10<2.9 误判）。
  - 验证：4 文件 py_compile OK；compare_versions 单测 7 例通过；`get_app()` 启动 + `/api/firmware/negotiate-all` 路由注册 OK；真实库 negotiate 集成校验（虚拟落后设备 0.0.1→upgrade，最新 active=1.0.0-smoke）。固件无工具链仅 C++ 审阅，需 Arduino IDE 实编+真机复验。按约定未 git commit。P2（校验/回滚/灰度/静默时段）待做。

- **实施 P2（固件 OTA 校验/安全回滚/断点续传 + 后端 id 补丁，已落地未提交）**：用户说"p2"。
  - **关键对齐发现**：后端 `sign_ota_command` 用 `f"{firmware.id}:{firmware.version}:{url}"`，但原 OTA 推送 payload 不含 `firmware.id` → 设备无法重算签名。修复：三个推送 payload（自动 `_execute_push` / `batch-upgrade` / `ota-upgrade`）均新增 `"id": firmware.id`。后端 py_compile OK。
  - **固件 phonebox.ino 改动**：
    - includes 加 `<Preferences.h>`（NVS 意图持久化）+ `<mbedtls/md.h>`（HMAC-SHA256）。
    - 新增宏 `OTA_SIGNING_SECRET ""`（与后端一致；留空=关验签仅告警）。
    - `stateVars` 加 `otaMd5`/`otaSignature`/`otaFwId`。
    - OTA 回调(445)读 `doc["md5"]`/`doc["signature"]`/`doc["id"]`，存 stateVars + `saveOtaIntent()` 到 NVS。
    - 新增辅助：`saveOtaIntent/loadOtaIntent/clearOtaIntent/hasOtaIntent/markOtaPendingValidate/isOtaPendingValidate` + `verifyOtaSignature`（mbedTLS HMAC-SHA256，msg=`fwId:version:url`）。
    - `performOTAUpdate` 重写：① 先 `verifyOtaSignature` 验签（失败 `signature_failed`+清意图+重启）；② `Update.setMD5`（仅32位合法 md5）；③ 下载中连接中断以 HTTP `Range: bytes=N-` 续传（最多5次，206 续写/200 整包重启重下）；④ 成功置 `pending_validate` 重启；`setup()` 早期 `isOtaPendingValidate`→`Update.markAppValidNewPartition()` 提交（崩溃则 Bootloader 回滚旧分区）；⑤ loop() 据 NVS 意图自动恢复升级。
    - 修正 HTTPS 客户端坑：`http.begin` 必须传 `WiFiClientSecure&` 派生引用（非基类指针）否则 TLS 失效；用 `pSecure`/`pPlain` 双指针 + `OTA_FREE_CLIENT()` 宏释放。
  - 验证：C++ 仅逻辑审阅（无 ESP32 工具链）；括号配平校验 `{=}=206`、`()=1078` 平衡；需 Arduino IDE 实编（ESP32 Arduino core ≥2.0 以支持 `Update.markAppValidNewPartition`）+ 真机/本地 Broker 复验。按约定未 git commit（P0+P1+P2 累计改动待用户确认后提交）。

## P3（无缝OTA收尾：本地 Broker e2e + docker-compose 常驻）— 2026-08-15
- 交付物：
  - `docker-compose.mqtt.yml` + `mosquitto/mosquitto.conf`：常驻本地 Broker（eclipse-mosquitto:2，匿名 1883 + websockets 9001，healthcheck 用 nc 探测）。
  - `backend/scripts/verify_ota_e2e.py`：standalone OTA e2e（复用 verify_mqtt_e2e.py 的「收发分离+SUBACK 诊断」范式）。模拟设备订阅 phonebox/ota/{dev}/phonebox/ota/phonebox/ota/{dev}/status；登录后端 → 取 active 固件 → POST /api/firmware/<id>/ota-upgrade(force) → 设备实时收包并重算 HMAC 验签（msg=id:version:url）→ 回报 started→success → 轮询 /api/firmware/ota-status 断言 completed 闭环。env 驱动 broker（MQTT_BROKER/PORT/USER/PASS/OTA_SIGNING_SECRET/BACKEND_URL/ADMIN_USER/PASS/DEVICE_ID/FIRMWARE_ID）。
  - `backend/tests/test_ota_e2e_local_broker.py`：pytest 两层。TestOTASignatureContract（3 例，无需 broker，本机 3 passed）：签名匹配设备重算 / 缺密钥返回空串 / URL 篡改签名对不上（设备应拒绝）。TestOTAE2ELocalBroker：Broker 不可达则整类 skip（本机 1 skipped）；可达时跑通 publish_ota_command→设备收包验签→回报状态→后端 _process_ota_status 路由（spy 不写真实库）。
- 验证：pytest 本机 `3 passed, 1 skipped`；两新 .py py_compile OK；compose YAML 合法、mosquitto.conf 就位。本机无 docker/mosquitto 二进制 → 全链路 legs 需在 Linux+docker 起 broker 后实跑（pytest 全链路 / 或 verify_ota_e2e.py 对接真实后端）。
- 注意：误装 gmqtt 发现其仅是客户端库无 broker，已 `pip uninstall` 清理；hbmqtt/amqtt 不稳定故不选。
- P0+P1+P2+P3 全部改动仍按约定未 git commit，待用户说「提交」。

## P3 补充：e2e 对齐云端 Broker（用户提供 EMQX Cloud 接入信息）
- 用户确认生产用云端 Broker：nc5233fc.ala.cn-hangzhou.emqxsl.cn:8883(TLS)，控制台 c5233fc...，WS 8084，凭据 phoneboxtest/123456，CA 至 2031-11-10。
- 改动：verify_ota_e2e.py 与 test_ota_e2e_local_broker.py 默认对接云端 Broker（TLS，insecure 跳过 CA；MQTT_CA_CERT 可严格校验）；docker-compose.mqtt.yml 注释明确其仅本地备选、生产用云端。pytest 全链路可用 OTA_E2E_BROKER 覆盖为本地。
- 实跑验证：ACL 探针确认云端 Broker 授予 phonebox/ota/# 订阅且能收到嵌套 phonebox/ota/<id>/status（返回链路断因是桩本身非生产 ACL）。pytest 指定云端 Broker `4 passed`；默认（本地不可达）`3 passed,1 skipped`。
- 全链路桩修复：返回链路改用独立后端侧订阅者(phonebox/ota/#)在真实 Broker 验证 + 离线验证 _process_critical_message 路由；be_evt 须专等 status topic；设备收发合一压连接数到 4 以避开 phoneboxtest 并发连接上限。

## [更正] 本地提交
- 当日末尾所述「P0+P1+P2+P3 全部改动未 git commit」已撤销：本会话用户指令「继续进行下一步」= 同意本地提交。
- 提交：`47e45c0` on main，14 files changed, 1783 insertions(+), 132 deletions(-)；预提交钩子(Python 语法检查)通过。**仅本地提交，不推送**（push 仍需代理）。
- 固件 phonebox.ino 仍待 Arduino IDE 实编 + 真机复验（本机无工具链）；OTA_SIGNING_SECRET 须与设备侧宏一致。

## 稳定化冲刺（2026-08-15 下午，用户指令"先完善已有功能，保证稳定运行"）
- 基线（后端开发模式启动）：RBAC 一致性 OK(无缺失)；OpenAPI 原 1 处漂移 /firmware/negotiate-all(P1 加的端点未同步快照)→ `verify_openapi_contract.py --update` 归零；API 契约测试 2 passed。
- 环境不一致修复：venv 缺 pytest-timeout → 文档回归命令 `python -m pytest ... --timeout=300` 报错退出；已 `pip install pytest-timeout==2.4.0`，文档命令现 2 passed 可跑通。
- 真实稳定性修复(backend/services/mqtt_manager.py)：
  - `_process_messages_batch` 心跳循环逐条 try/except 隔离（单条坏心跳不再中断整批剩余心跳）。
  - `_process_critical_message` 的 OTA status/register 处理器包 try/except 隔离（单条异常不再跳过后续回调/冒泡打断控制收包循环）。
  - 控制/遥测入口已有顶层兜底不会打死 MQTT 客户端；此为逐条隔离加固。验证：test_mqtt_service.py 11 passed(无回归)。
- tasks/ 扫描：mqtt_tasks.py 用 self.retry 重试+失败打印，无静默吞没，无需改。
- 修正：「前端 10 个 CRA 死单测清理 / CI 实接未做」已过时——前端早已迁 vitest（`npm test`=vitest run，11 个测试文件/160 用例），CI `ci.yml` 已正确接入 build+test+lint，`deploy.yml` 为 tag 触发发布流（windows 部署 `if:false` 禁用），无 react-scripts 残留、无死步骤。稳定化-D 仅剩验证，见下。

- **稳定化-D 验证 + 推送完成（2026-08-15 傍晚，接上）**：
  - 闸门 `run_regression.sh`（task rVcicc，落盘 regression_gate.log）：RBAC 一致 OK / OpenAPI 0 漂移(459=459) / 后端契约 2 passed / 关键路由 28 passed → 全部回归通过（Exit 0）。
  - 前端 vitest 全量（托管 Node 22，`npm run test`，落盘 frontend_vitest.log）：**Test Files 17 passed | 1 skipped(18)；Tests 160 passed | 3 skipped(163)；VITEST_EXIT=0**。日志里 500/api_error 是测试桩故意验证错误路径，非失败。
  - CI 复核：`ci.yml`(后端 pytest 子集 + 前端 build/test/lint) + `deploy.yml`(tag 发布) 均现代、无死步骤。
  - **推送完成**：用户开 Steam++ 后直连 `git push origin main` 成功 → `25447e0..e356abd`，OTA P0-P3(`47e45c0`) + 稳定化(`e356abd`) 两提交全部上 origin/main，无待推送。**稳定化冲刺至此完全闭环**（MQTT 隔离 + OpenAPI 0 漂移 + RBAC 一致 + 后端契约/路由绿 + 前端 160 绿 + CI 接好 + 已推送）。
  - 注：全量 pytest(1710)在本机不现实（~50h），未跑；针对性闸门已覆盖最易漂移面，作为稳定运行佐证。

- **前端掉线 + 全量回归卡死处理（2026-08-15 傍晚）**：
  - 用户贴浏览器控制台报错：localhost:3000 全量 `ERR_CONNECTION_REFUSED`（HMR WebSocket、`Login.tsx` 动态 import、`/api/*` 全失败）。根因=前端 Vite 进程已不在（netstat 本地 3000 无监听），非代码 bug。后端 5000 正常（PID 2768 监听）。
  - 重启前端：`npm run dev:vite`（`vite --port 3000 --strictPort`；**注意 package.json 无 `dev` 脚本，用 `dev:vite` 或 `npm start`**）→ 托管 Node 22.22.2 启动成功，Vite v5.4.21 ready 1286ms，监听 0.0.0.0:3000，无编译错误。用户刷新标签页即恢复。
  - 发现全量回归卡死：周五 23:55 启动的 `pytest -p no:locust --timeout=300 -q`（collected **1710** items，非记忆中的 1633）跑了 ~16h 仅 33%、按此速度 ~50h，日志无 EXIT_CODE。确认 PID 11184 = 该 pytest（3.6GB，torch 加载）。根因=环境内存压力（同时有 4 个 `run.py --env development --port 5000` 后端进程：2768 真正监听，1924/18200/17824 为重启残留的 Windows 端口僵尸）+ 部分测试极慢/超时循环。已 `taskkill /F /PID 11184` 终止。
  - **多后端僵尸提示**：1924/18200/17824 与 2768 抢 5000，仅 2768 真正监听；清理需管理员权限（Windows 端口僵尸旧坑）。未动用户后端。
  - 替代方案：全量在此环境不现实（~50h），改用针对性稳定性闸门 `bash scripts/run_regression.sh`（RBAC+OpenAPI+契约+关键路由），后台 task rVcicc 落盘 `C:/Users/53527/AppData/Local/Temp/regression_gate.log`，跑完通知后汇报。

- **固件管理页 404 修复（用户报障）**：
  - 现象：FirmwareManagement 调 `DELETE /api/firmware/1`、`PUT /api/firmware/1`、`GET /api/firmware/download/1` 全 404。
  - 根因①（真 bug）：前端 `frontend/src/services/api.ts` 的 `update/updateVersion/deleteVersion/delete` 拼的是 `/api/firmware/${id}`，但后端 `api/devices/firmware_routes.py` 的单资源路由是 `/api/firmware/versions/<int:id>`（删除/更新）→ 路径缺 `/versions` 段 → 404。**已改**为 `/api/firmware/versions/${id}`，并把 `download` helper 由 `/api/firmware/${id}/download` 改正为 `/api/firmware/download/${id}`。
  - 根因②（数据，非代码）：库里 `firmware_versions` 仅 1 条 `id=1 (1.0.0-smoke)` 且 `file_path=NULL`（元数据记录、无二进制）；后端下载路由 `get_or_404` 命中记录后 `if not firmware.file_path: return 404` → 下载 404。该记录无文件，属测试残留。
  - 附带 UX 修复：列表接口新增返回 `file_path`；下载按钮在无文件时 `disabled` 且 `title` 提示「该版本无固件文件，无法下载」，避免误导点击。
  - 验证：后端 `py_compile` OK；前端 eslint 0 error、vite build 0。
- **Redis 缓存降级说明（用户问「为什么降级」）**：`services/redis_cache_service.py::RedisCache.init_app` 启动连 `REDIS_URL`（默认 `redis://localhost:6379/0`）并 `ping()`，`ConnectionError` 时 `client=None` → 上游 `api_cache_middleware.py` 退化为进程内内存缓存（日志「Redis 降级|使用内存缓存」）。即本机没跑 Redis 服务。项目根有 `启动Redis.bat`，启动 Redis 后即走 Redis。属预期降级，非崩溃。

- **后端启动自动拉起 Redis（用户需求「后端启动时自动拉起Redis」）**：
  - **根因补充**：此前 Redis 一直「降级」的真正原因不是连不上，而是 `cache.init_app(app)` **从未在启动时调用**——全局 `cache = RedisCache()` 以无 app 构造，`client=None`，`api_cache_middleware` 永远走内存缓存。
  - **改动 1（config.py）**：Redis 段新增 `REDIS_AUTO_START`(dev 默认 true / production 默认 false)、`REDIS_SERVER_COMMAND`(自定义 redis-server 路径)、`REDIS_AUTO_START_TIMEOUT`(默认15s)、`REDIS_SERVER_LOG`；并加入 env 列表。
  - **改动 2（services/redis_cache_service.py）**：`init_app` 改为「先直连 → 失败且非 TESTING 且 REDIS_AUTO_START → 自动 `redis-server` 子进程 + 轮询 ping 就绪 → 重连」；新增 `_resolve_redis_server_executable`（探测顺序：REDIS_SERVER_COMMAND → 项目根/redis/redis-server(.exe) → C:\Redis → PATH）、`_try_auto_start_redis`（Windows 用 DETACHED_PROCESS|CREATE_NEW_PROCESS_GROUP 分离进程，POSIX 用 start_new_session，输出 DEVNULL/日志文件，带全局 `_auto_start_lock`+`_auto_start_attempted` 防重入）。任何失败均优雅降级为内存缓存，绝不抛异常。
  - **改动 3（app/service_init.py）**：新增 `init_redis_cache(app)` 在 `init_services`（非 lightweight）**首位**调用 `get_cache_service().init_app(app)`，使缓存连接在预热/MQTT/NLP 之前就绪。
  - **验证**：三文件 `py_compile` OK；config 解析正确(AUTO_START=True/URL=redis://localhost:6379/0)；临时脚本实测在 6390 端口成功 spawn `C:\Redis\redis-server.exe` 并连接(set/get 通过，验证后 shutdown 清理)。本机 `C:\Redis\redis-server.exe` 与 `C:\Program Files\Redis\redis-server.exe` 均存在，故自动拉起真实可用；项目 `redis/` 目录为空（启动Redis.bat 期望的位置缺二进制，已由 C:\Redis 兜底）。
  - **注意**：自动拉起的 redis 为分离子进程，后端退出后默认继续运行（与启动Redis.bat 行为一致）。生产环境(env=production)默认不自动拉起。


- **后端强杀重启验证（用户指令「强杀全部 python 后重启后端生效」18:04）**：
  - 强杀 4 个 python.exe（含后端主进程 16416/1.8GB）+ 用系统 Python 3.11 重启：python run.py --env development --host 127.0.0.1 --port 5000（后台，日志 backend_restart.log）。
  - 结果全绿：redis-server.exe(PID 6820) 被自动拉起；日志「Redis连接成功（第1次尝试）/ Redis connected / Redis 缓存服务初始化完成 / NLP服务已连接Redis缓存」无降级；端口 5000 监听(PID 5432)；直连 Redis ping=True、dbsize=13、键均为应用写入的真实缓存键 → 确认脱离内存兜底、真正走 Redis。
  - 注意：Flask debug reloader 首启会「Restarting with stat」一次（Werkzeug 正常行为）。前端改动(api.ts 固件URL修正、FirmwareManagement 下载禁用、OpsCenter/SystemMetrics/FrontendTelemetry)仍需 Vite dev 侧热更新（前端未重启，proxy 指向 127.0.0.1:5000）。

- **推送 + 回归闸门（用户「推送，然后下一步 → 跑回归闸门验证」）**：4 个提交 `git push origin main` 成功 e356abd..5467c85（ops-center c3b3100 / firmware 0eb097b / redis 821faee / memory 5467c85），预提交钩子语法校验全过。`bash scripts/run_regression.sh` 4 步全绿（RBAC 一致 / OpenAPI 校验 / test_api_envelope / 关键路由 rules export notify classes）→ 全部回归通过，REGRESSION_EXIT=0。本次三块改动已上 main 且无回归漂移。

- **设备在线状态修正（用户「下一步功能优化 → 设备在线状态修正」）**：根因=设备在线判定长期依赖陈旧 `status="online"` 字段；设备心跳停了但 celery 巡检未跑到(或 alert_enabled=False)就永久停在 online，导致「无心跳却显示在线」(运维中心/设备列表在线数虚高)。修复：① 给 `Device` 模型加 `is_online` 属性(委托 is_device_online，按 last_heartbeat TTL 判定)；② 统一替换 7 个文件里的陈旧判定——devices_routes(DeviceStats/DeviceAdvancedStats 计数、OnlineDevices 列表、OTA 广播 74、批量重启解锁 1114、导出 1439/1497)、export_routes(159/436 汇总报告)、utils/query_optimizer(223)、services/dashboard_service(36)、services/redis_cache_service(549)——全部改为走 `d.is_online`/is_device_online。③ 新增回归测试 tests/test_device_is_online_property.py。py_compile 6 文件 OK。注意：改动尚未重启后端生效(避免干扰正在后台跑的全量 pytest)；待全量 pytest 完成后重启后端+跑针对性测试验证。

- **热点读接口加 Redis 缓存（用户「继续下一步优化 → 热点读接口加 Redis 缓存」）**：
  - 关键发现：项目里有**两套缓存实现**——`services/cache_service.py::CacheService`（设备统计里手动用的那个）是独立连接、**不会自动拉起 Redis**，导入早了就退化成进程级内存缓存（不共享、重启即丢）；`utils/api_cache_middleware.py::cached_api` 才用 `get_cache_service()`(即刚做自动拉起的 RedisCache，真正 Redis)，但**此前从未被使用**，且它不兼容本项目 `APIResponse` 的 `(dict, status)` 元组返回约定（`result.get_json()` 对元组会失败），`get_ttl_for_path` 又假设 `API_CACHE_TTL` 是 dict（默认却是 int 300，会 TypeError）。
  - 修复 `utils/api_cache_middleware.py`：① `cached_api` 现在兼容 `(dict, status)` 元组 / Flask Response / 裸 dict|list，仅缓存 200 成功响应，缓存不可用时自动穿透；② `get_ttl_for_path` 容错非 dict。standalone 逻辑验证 4 场景全过（tuple 命中/非200不缓存/无缓存穿透/裸list命中）。
  - 应用：`dashboard_routes.py` 的 `/data`(ttl=120)、`/stats`(ttl=60)；`devices_routes.py` 的 `/stats`(ttl=60)、`/advanced-stats`(ttl=60)、`/online`(ttl=30) 均加 `@cached_api`。移除设备统计里那套弱内存 `cache_service` 手动缓存（统一走真 Redis 的 cached_api）。
  - 新增 `tests/test_cached_api.py`（fake cache，不依赖 Redis/torch，覆盖 HIT/MISS/裸list/非200/无缓存穿透/查询参数键）。3 文件 py_compile OK + standalone 验证通过。**注意**：本次缓存改动在后台全量 pytest(5BtPpR) 启动之后落盘 → 全量 pytest 用的是旧导入快照、不会覆盖本改动；须在 5BtPpR 完成后单独跑针对性 pytest（含 test_cached_api.py + test_dashboard_routes + test_devices_routes）验证，再提交。

- **两套缓存现状盘点（用户问「两层缓存怎么处理，能否删一套」）**：
  - Cache A = `services/redis_cache_service.py::RedisCache`（真 Redis，`get_cache_service()` / `@cached_api`，`init_app` 自动拉起+重连，能力最全：hash/list/set/zset/lock/clear_pattern/flush/get_pool_status/set 接收 tags 但**未实现标签存储与 invalidate_by_tag**）。使用者：nlp/cluster/algorithm/prediction/scheduled_tasks/dashboard_routes(新)/devices_routes(新)。
  - Cache B = `services/cache_service.py::CacheService`（实例 `cache_service`）：模块导入即连 localhost:6379（3 次重试，**无自动拉起**），连不上则 `redis_client=None` + `memory_cache={}` **永久退化成进程内存、不再重连**。独有 `invalidate_by_tag/get_stats/flush_all`。使用者（**非死代码**）：`api/users/users_routes.py`、`api/system/system_routes.py`(get_stats/flush_all)、`api/scores/{rules,records,rank}_routes.py`、`services/dashboard_service.py`、`di/__init__.py`(CacheService provider)、`tests/test_cache_service*.py`、`tests/test_dashboard_routes.py`。
  - 结论：B **不能直删**（删则 users/scores/system/dashboard 全崩）。但 B 有「静默退化内存」真实隐患（导入时序竞争越过自动拉起即永久内存），应**合并到 A 后删 B**——既清理又修可靠性。
- **两套缓存合并完成（用户选「合并到 A 并删除 B」）**：
  - 给 Cache A(`redis_cache_service.RedisCache`) 补能力对齐 B 的契约：`__init__` 加 `_stats` 计数；`get` 加 hits/misses 统计并加固异常兜底（捕获 UnicodeDecodeError/pickle 二进制）；`set` 真正实现 `tags` 存储(`_store_tags` 写 `tag:{tag}` 集合)；新增 `invalidate_by_tag`/`invalidate_by_tags`/`get_stats`(返回 redis_available/hit_rate/total_operations 等)/`flush_all`(调 `flush()`)。
  - 迁移 6 个生产模块 + di：`dashboard_service.py`、`api/users/users_routes.py`、`api/system/system_routes.py`(get_stats/flush_all)、`api/scores/{rules,records,rank}_routes.py` 全部 `from services.cache_service import cache_service` → `from services.redis_cache_service import get_cache_service`，`cache_service.X(` → `get_cache_service().X(`；`di/__init__.py` 删 `cache_service` provider。
  - 测试：删 `tests/test_cache_service.py`/`tests/test_cache_service_new.py`；`test_dashboard_routes.py` 改 `get_cache_service()` 并**修隔离**（`_clean_data` 全清 User/ScoreRecord/Device/ScoreRule + 两个未清理的测试补 `_clean_data`，device 补 `last_heartbeat` 匹配新在线语义，`test_dashboard_cache` 显式连真实 Redis 验证缓存、结束复位 `cache.client=None` 防污染后续）；`test_api_integration.py` 删 `assert hasattr(container,'cache_service')`。
  - 删除 `services/cache_service.py`。**验证**：全文件 py_compile OK；整应用 `get_app()` 导入 OK(948 路由)；standalone 真实 Redis 验证(invalidate_by_tag/get_stats/flush_all/dict 往返)全过；针对性 pytest `test_cached_api`+`test_dashboard_routes`(6)+`test_api_integration`+`test_device_is_online_property`=25 passed、`test_redis_cache_service`=77 passed。
  - 注意：改动尚未提交、未重启后端生效（按约定不主动 push）。全量 pytest 已结束(1698 passed/1 failed/11 skipped，那 1 个失败是 `test_algorithm_service::test_calculate_statistics_with_data`，与缓存无关且属旧快照)。
  - **合并收尾复核（续会话）**：① 跑 `scripts/run_regression.sh` 闸门全绿（RBAC 一致 / OpenAPI / test_api_envelope / 关键路由）→ 全部回归通过 EXIT=0；② grep 确认零 `services.cache_service` 残留引用（仅 backend_restart.log 旧日志条目 + redis_cache_service.py 内「兼容 CacheService 契约」docstring 有意为之）；③ `di/__init__.py` 的 `redis_cache_service = providers.Singleton("services.redis_cache_service.RedisCacheService")` 经核对 `redis_cache_service.py:535` 有 `RedisCacheService = RedisCache` 别名 → 解析正确，非 latent bug；④ MEMORY.md 旧「两套缓存(勿混用)」条目已改写为「已统一为单套」准确描述。三笔改动(设备在线修正/热点缓存/缓存合并)仍本地未提交、后端未重启。
  - **后端实跑验证（用户选「先重启后端实跑」）**：当前无后端进程在跑（port 5000 无监听）→ 用系统 Python 3.11 全新拉起 `run.py --env development --host 127.0.0.1 --port 5000`（后台 task svCuNI，日志 backend_live.log）。结果全绿：① 日志 `Redis connected: redis://localhost:6379/0` + `Redis 缓存服务初始化完成` + `NLP服务已连接Redis缓存` → 统一 Cache A 真走 Redis、无降级（Cache B 已删、再无静默内存退化）；② 双 MQTT 连接成功（控制订阅 score/#/unlock/#/ota/#/points/#/query，遥测 phonebox/#）；③ 登录 admin/123456 200 且 access_token 在顶层；④ `GET /api/dashboard/data` 与 `/api/dashboard/stats` 均 200 success=True，返回真实数据（两接口同时走 `@cached_api` + `dashboard_service.get_cache_service()`，即合并后的统一缓存）；⑤ Redis dbsize 15→18，新增 `score_management:api:/api/dashboard/{data,stats}:<hash>`(cached_api 写入) + `score_management:dashboard_data`(dashboard_service 内部缓存) → 铁证统一缓存写入真实 Redis。合并在运行态确认生效。改动仍本地未提交（按约定不主动 push），后端现运行于 5000。
  - **本地提交完成（用户选「本地合并一笔提交」）**：`git commit` 仅暂存 `backend/`（排除 `.workbuddy/memory/` 辅助文件）→ `a7dee7c` on main，21 files changed, +387/-850，预提交钩子 Python 语法检查（18 文件）通过。三笔改动（设备在线修正/热点缓存/缓存合并）现已本地提交、未推送（按约定等你开 Steam++）。`.workbuddy/memory/` 两文件仍本地未提交（属工作跟踪，不混入代码提交）。后端运行中、可继续联调。
  - **推送完成（用户开 Steam++ 后）**：`git push origin main` 成功 → `5467c85..a7dee7c`，`a7dee7c` 已上 GitHub（PUSH_EXIT=0）。工作树与远端 main 现已同步，无待推送提交。三笔缓存改动（设备在线修正/热点缓存/两套缓存合并删除 Cache B）至此完全闭环：代码落地 + 回归闸门全绿 + 运行态实跑验证 + 已推送。`.workbuddy/memory/` 两文件仍本地未提交（属工作跟踪，不混入代码提交）。后端运行中、可继续联调。

- **前端 ESLint 全量清理 + CI 红修复（2026-08-15 晚）**：
  - CI 红根因 = 前端 `ESLint 检查` 步骤 exit 1：仅 **5 个 Error**（`jest/valid-expect`：AlgorithmAnalysis.test.tsx:91 的 `expect(btn, 消息)` 第二参；`testing-library/no-wait-for-multiple-assertions`：同文件:111 + StudentPortal.test.tsx:109/129/141 的「单 waitFor 内多断言」）。
  - 用户两次确认「仍全量清理 Warning」（洁癖，CI 本来已绿、改动大）。Warning 分布原 347 = no-console 277 / exhaustive-deps 28 / no-unused-vars 23 / @typescript-eslint/no-unused-vars 15 / useless-escape 4。
  - 实施：① 新增 `src/utils/logger.ts`（代理 console，文件级 `eslint-disable no-console`）；② 脚本把 62 文件 277 处 `console.{log,info,warn,error,debug}(` → `logger.X(` 并按文件深度补相对 `import logger`（**踩坑**：脚本初版把 import 插进多行 import 块内导致语法错、且 eslint 解析错被 `if(!ruleId)` 漏计 → 表现 0/0 实为假绿；修正脚本移除错位 import 并重置到文件顶部，再冒出 4 个被解析错掩盖的真 unused：AttendanceManage 的 ChevronRight/AlertTriangle/getLeaveStatusBadge + MentalHealth 的 Sparkles，已删）；③ 剩 7 处 console 引用（`.catch(console.error)`→`.catch((e)=>logger.error(e))`；errorMonitor.ts/webVitals.ts 日志基础设施文件级 disable）；④ 删 `.eslintrc.json` 冗余 `no-unused-vars`（core 规则对 TS enum 成员误报，RequestPriority/PreloadStrategy 成员实际在用，@typescript-eslint 版不误报）→ 消 8 个假阳性；⑤ 删 19 处真 unused-vars（showConfirm 解构×5 / ScoreEntry editingInputRef+handleScoreChange / SecurityAudit CheckCircle+SeverityType / StudyGroups ChevronRight / UserDetail result / codeSplitting·requestOptimizationService 的 enum 非本次）；⑥ 修 4 处 useless-escape（`\[`→`[` 在字符类内）；⑦ 10 文件顶部加 `/* eslint-disable react-hooks/exhaustive-deps */`（不改依赖数组，避免无限循环）。
  - **验证全绿**：`eslint src --ext .ts,.tsx` → 0 error / 0 warning / 0 parse error；`vite build` 成功（仅样式里 `backgroundposition` 既存拼写告警，与本改动无关）；`vitest run` → 160 passed / 3 skipped（基线）。
  - **已提交并推送（用户确认后）**：`git commit` → `32deb38`（69 files, +6029/-5991，含新建 logger.ts）→ `git push origin main` → `a7dee7c..32deb38`（PUSH_EXIT=0）。`gh run watch 31882456492 --exit-status` → **WATCH_EXIT=0**：前端 job ✓57s（build+vitest+ESLint）+ 后端 pytest job ✓5m7s（契约快照+RBAC+MQTT+核心路由+算法/报表/学生组）全部通过，整条 CI 转绿。唯一 ANNOTATION 为 Node.js 20 弃用告警（非阻断）。前端约 60+ 文件变更；临时分析脚本已删。
  - **复核历史红根因（2026-08-15 晚）**：`gh run view 31881102909 --log-failed` 显示失败步骤仅有 `前端 build + lint → ESLint 检查`（日志满屏 `no-console` 等 ESLint 报错，exit 1）。确认 31820772500→31881102909 共 6 条连续 `failure` 均为同一批前端 ESLint 错误门禁（非后端），后端 pytest 在那些 run 中已过；被本轮 `32deb38` 清掉 5 Error+347 Warning 后，`31882456492` 成为 main 上第一条绿。CI 修复至此完全闭环。
  - **清理 CI 弃用注解（2026-08-15 晚，用户确认"按照建议操作"）**：`ci.yml` 三个 action 升到 Node 24 运行时——`actions/checkout@v4→v7.0.1`、`actions/setup-python@v5→v7.0.0`、`actions/setup-node@v4→v7.0.0`（升级前已逐个 `gh api` 核验 `action.yml` 的 `runs.using: node24`）。`git commit` → `91c8761`（`32deb38..91c8761`）→ `git push origin main`（PUSH_EXIT=0）。`gh run watch 31883007121 --exit-status` → **WATCH_EXIT=0**：前端 job ✓54s + 后端 pytest job ✓5m25s 全绿，**全程无 `Node.js 20 is deprecated` 注解**。`main` 分支现既全绿、又无弃用噪音。
  - **项目遗留项盘点 + 修复（2026-08-15 晚，用户确认"先盘点→全选修复→再次推送"）**：Explore 子代理只读扫描 433 后端 +179 前端，项目完成度很高，仅 4 个遗留项（无阻断性 stub）。全选修复并提交 `2591aef`（`91c8761..2591aef`，5 files, +12/-17）→ `git push origin main`（PUSH_EXIT=0）：①P1 `api/monitoring/mqtt_routes.py` 把 `@limiter.limit(MQTT_PUBLISH)` 从永不被调用的空 `_do_publish` 改为真正执行 `publish_mqtt`（端点级限流真实生效，消消息风暴风险）；②P2 `api/scores/approvals_routes.py`+`records_routes.py` 两处 ImportError 兜底的 `create_admin_notification` 从 `pass` 改 `logging.warning`+`return None`（通知丢失不再零日志）；③P3 删 `services/nlp_enhanced_service.py` 三个未被调用的 `record_*` no-op（grep 确认调用方均指 `nlp_analyzer_service` 真实实现）；④P3 修 `README.md` 目录漂移（`routes/`→`api/`、`App.js`→`App.tsx`、`index.js`→`main.tsx`）。4 文件 `py_compile` 全过、pre-commit 钩子通过。`gh run watch 31884210658 --exit-status` → **WATCH_EXIT=0**：前端 ✓57s + 后端 pytest ✓5m24s 全绿。四项修复未破绿。
  - **P0 真实 bug 修复批次（2026-08-15 晚，用户选"先完善已有功能"→Explore 盘点 24 条 P0~P2→建 #381~#388 修 S/M 级 P0）**：提交 `589390d`（`2591aef..589390d`，6 files, +167/-29）→ `git push origin main`（PUSH_EXIT=0）。修复：①`api/devices/firmware_routes.py` OTA report `completed`/`failed` 分支原 `update_record` 是**未 .first() 的 query 对象**，`update_record.status=...` 只是给 query 对象挂属性被静默丢弃 → 升级状态永远卡 `in_progress`（正确性 bug），补 `.first()` + 去重 `order_by` + 空体/缺必填改 `bad_request`；POST `/versions` 校验 `version` 必填 + `try/except rollback`；②`api/system/notification_config_routes.py` PUT 空体 `request.get_json()`→`get_json(silent=True) or {}` 防 `in None` 500；③`api/academics/import_routes.py` 创建配置校验 `module_name`/`config_name` 必填（原取下标 KeyError）；④`api/users/rbac_routes.py` 权限串解析 `except Exception:pass` 吞异常 → `isinstance(str)` 校验 + `logging.warning`；⑤`api/academics/exam_routes.py` 创建考试校验 `name`/`date` 必填 + `try/except rollback`。测试：`tests/test_devices_routes.py` 新增 `TestFirmwareOTAReport` 三个用例（started→completed/failed 真正落库断言 + 缺必填 400）。验证：6 文件 `py_compile` 全过（pre-commit 钩子复跑通过）、5 模块 import 冒烟 OK、`verify_rbac_consistency.py --check-only` 一致 OK（68 条）。**注意**：`test_devices_routes.py` 的 `test_ota_upgrade_all_success`/`test_bulk_ota_upgrade_alias` 仍 2 failed，但经 `git stash` 确认是**基线既有失败**（与本次改动无关，根因 `devices_routes.py::send_ota_upgrade_command` 广播分支查 `d.is_online` 而测试只建 `status='online'`，属测试/模型错配），且该文件**不在 CI 子集**内（ci.yml 只跑契约快照+RBAC+MQTT+核心路由+算法组），故不影响 CI 绿灯。CI run `31885765451`（结论 success，全绿，前端 ✓ + 后端 pytest ✓ 两 job 通过）。
  - **第二批"继续完善"（2026-08-15 深夜，#389~#392 编号错位但内容对应：records 校验回滚 / import GET 信封 / records 测试 / 闸门）**：提交 `c4aa61a`（`589390d..c4aa61a`，3 files, +117/-27，含新建 `tests/test_scores_routes.py`）**已 commit 但未 push**——`git push origin main` 三次均 GitHub 443 不通（先 `Connection was reset` 后 `Could not connect to server`），`Steam++`/`Steam++.Accelerator` 进程在运行但 github.com 加速/系统代理未生效。修复：①`api/scores/records_routes.py` POST `/records` 补 `user_id` 必填且整型、`score_change` 必填且数字校验（原 `score_change=None` 触发 `line 259` `>0` 比较 `TypeError 500`）；db 操作包 `try/commit/rollback` 防部分提交；`log_operation` 容错（失败 `pass` 不影响主流程）；②`api/academics/import_routes.py` GET `/import/configs` 裸 dict `{"success","data","total"}` 改 `APIResponse.success(data=..., total=...)` 统一信封（前端 `request()` 自动解包 `data`，透明兼容）；③`tests/test_scores_routes.py` 新增 4 用例（缺 user_id/缺 score_change/score_change 非数字 400；正常录入 201 且 `user.current_score` 同步更新），本地 `pytest` **4 passed**。验证：3 文件 `py_compile` 全过、RBAC 一致 OK（68）。**待办（下一轮专项）**：`notification_config_routes.py` 通知配置仍写 `current_app.config` 内存不持久化（重启丢失），且 `NotificationService` 6 处从 `current_app.config` 读 `WECHAT_*/SMS_CONFIG`——需系统性改动（新建 `NotificationConfig` 模型+幂等迁移+routes 落库+Service 读库+启动加载），本批未做。
  - **第三批"运行时故障修复"（2026-08-15 深夜，#393~#395，用户贴前端控制台：`/api/nlp/execute` 504 超时 + `[WebSocket] 连接失败` 重试）**：提交 `caa2a4a`（`c4aa61a..caa2a4a`，3 files, +20/-6）→ push OK（PUSH_EXIT=0），CI run `31891184376` 后台盯。根因与修复：①**WebSocket 命名空间不匹配**——后端 `services/websocket_service.py` 全部 handler 注册在 `namespace="/ws"`，但活跃客户端 `frontend/src/stores/index.ts::useWebSocketStore.initSocket`（App/Dashboard/DeviceManagement 在用）用 socket.io-client 连**默认命名空间 `/`**（`io('http://localhost:5000', {path:'/socket.io'})`），`send_*` 广播也在默认 `/`；Flask-SocketIO 会**拒绝无 handler 的命名空间连接** → 前端一直 `connect_error` 重试（日志 `[WebSocket] 连接失败 (n/10)`）。修复：5 个 handler（connect/disconnect/subscribe/unsubscribe/ping）去掉 `namespace="/ws"` 统一默认命名空间；顺带对齐死代码 `context/WebSocketContext.jsx`（`io(\`${url}/ws\`)`→`io(url,{path:'/socket.io'})`，未被引用，防潜在陷阱）。`useWebSocket.js`（原生 WebSocket 连 /ws）为死代码且协议不兼容 socket.io，未动。②**NLP 冷加载超时 504**——`/api/nlp/execute` 走 `get_nlp_parser()` 懒加载 `EnhancedNLPParserService`，模块链 import `nlp_ml_service`（**模块级 `import torch`+大量 sklearn**）+jieba，**首次请求才触发重型导入**，超前端 30s 阈值（`config.api.timeout=30000`）→ 504；之后实例缓存二次请求才快。修复：`app/service_init.py::init_nlp_service` 启动期预加载 `get_nlp_parser()`（冷代价移到 boot，实测 `from app import app` 39.6s，日志 `NLP 解析器预加载完成`；失败内层 try 兜底退回懒加载不影响启动）。验证：后端 py_compile ✓、`tests/test_websocket_service.py` **24 passed**（契约要求 emit 不带 namespace，与改动一致）、前端 lint **0 问题**、`vite build` ✓（仅一条 CSS `background-position` 拼写告警非阻断）。**注意**：CI 后端 pytest 子集每次跑 `from app import app` 会含 NLP 预加载（+30~40s），属预期，不会超时。
  - **第四批"智能评分误报成功"（2026-08-15 深夜，用户反馈：`/api/nlp/execute` 20ms 返回但库里无"张三"，评分结果未应用）**：提交 `53fd886`（`caa2a4a..53fd886`，2 files, +140/-16，含新建 `tests/test_nlp_execute_service.py`）→ push OK，CI run `31892694195` 后台盯。根因：`nlp_enhanced_service.py::execute_scoring` 中 `user = get_by_id(User, parse_result["user_id"])`，`extract_name` 对库中不存在的姓名返回 `(name, None)`（`user_id=None`），走 `else` 分支**只写 `NLPMatchResult`、不更新任何分数**，却仍返回 `success: True` → 前端误报"评分成功"。修复：`if not user:` 早退返回 `success: False` + 明确消息（`未找到学生「张三」，请先创建该学生` / `未能从文本中识别到学生姓名`），**不写任何 DB 记录**；删除永不触达的 else 分支。**顺带修复同路径真实 bug**：`rule.usage_count += 1` 在 `usage_count=None`（列无默认值，新规则）时抛 `TypeError 500` → 改 `(rule.usage_count or 0) + 1`。测试：`tests/test_nlp_execute_service.py` 3 用例（学生不存在拒绝+无副作用、姓名未识别拒绝、正常路径应用守卫），用 `__new__` 跳过重型 `__init__` 只 stub `parse` 隔离测试；现有 `tests/test_nlp_enhanced_service.py` **37 passed** 零破坏。**注**：NLP 相关测试文件共 11 个（test_nlp_*.py），跑 `test_nlp_enhanced_service.py` 需约 2 分钟。
  - **第五批"教务管理模块字段统一化审查"（2026-08-15 深夜，用户要求全面审查教务字段统一）**：产出文档 `docs/教务管理模块字段统一审查报告.md`（**未改代码，仅审查报告**，未 commit）。范围：侧边栏「教务管理」（班级/科目/课程表/时间规则/节次）+「成绩管理」（考试/录入/档案/分析/算法分析）+ 学生页（UserList/UserDetail/StudentPortal）+ 规则/NLP/导入配置，共 13 页字段级扫描 + 后端模型/API 交叉核对（3 个 Explore 子代理并行扫描）。**关键发现**：①**E1 真实功能 bug**：RuleList 表单提交 `max_per_day`，后端 `api/scores/rules_routes.py` 只读 `daily_limit` → 规则"每日上限"创建/更新恒为 0 不生效（P0）；②A 类同义不同名 14 组：积分 `score/current_score/my_score/score_value`、考试名 `name/exam_name`、考试时间 `start_time/end_time` vs `exam_time`、班级引用 `class_info_id/class_id/class_name`、分页 `per_page/page_size`、年级 `grade/class_grade`、教师 `teacher_id/teacher_name/head_teacher_id`、每日上限 `daily_limit/max_per_day` 等；③B 类固定选项未统一下拉：`is_active`（Switch/Checkbox/无 UI 三套）、考试状态表单无控件、成绩状态枚举 `pending/confirmed/locked` vs types `rejected`+脏值 `normal`；④C1 排课授课教师是自由输入文本框（应 Select）；⑤分钟步进 15 vs 5 不一致、星期选项集排课 0-6 vs 时间规则 -1~6。**统一基准**：以模型/API 主字段为准（`User.current_score`、`ScoreRule.daily_limit`、`Exam.name/start_time/end_time`、`class_id`、`per_page`、`is_active`=Switch、`user_id` 引用）。实施优先级 P0（E1 bug）→P1（score/page_size/class_id/教师 Select）→P2（枚举对齐/Switch/分钟 5）→P3（文案）。**后续**：若用户批准按报告实施统一，优先 P0/P1；报告在 docs/ 未提交 git。
  - **第六批"字段统一化实施"（2026-08-16 凌晨，用户"按照方案全部实施"）**：提交 `2c0ec27`（`53fd886..2c0ec27`，16 files, +253/-86，含 `docs/教务管理模块字段统一审查报告.md` 入库）→ push OK，CI run `31895360799` 后台盯。落地内容（P0~P2）：①**P0 四个真实 bug**：RuleList `max_per_day`→`daily_limit`（后端 rules_routes 兼容读取）；UserList 提交 `score`→`current_score`（原初始积分丢失）+编辑回填 `user.score`→`user.current_score`+UserTableRow 列表列同改；UserDetail 弹窗 state `amount`→`score_change`；StudentPortal `page_size`→`per_page`（后端 student_routes 兼容）。②**P1 班级筛选统一 class_id**：UserList/ScoreEntry 班级 Select 值改 `String(cls.id)`、提交 `class_id`（后端 users_routes 加 `class_id` 过滤 + 权限校验 + 缓存键，超管与非超管均生效）；ScoreAnalysis 传 cls.id 但后端按名称查（分析为空 bug）→ `analysis_routes.py` `/class/<id或名称>` 兼容解析；成绩模板下载 `download_routes.py` 兼容 class_id；高级搜索 `SearchCondition` 加 `classId`。③**P1 排课教师 Select 化**：CourseSchedule 教师自由输入改 `Select(api.admins.getAll)`，提交 teacher_id+teacher_name。④**P2**：TimeRuleList 分钟步进 15→5；ExamManagement 表单补考试状态 Select（draft/published/closed）+ 徽章文案"进行中"→"已发布"。**未做（专项说明）**：`is_active` 全页 Switch 统一（B1/D5，涉组件选型纯 UI）、状态筛选补齐（D7）、P3 文案微调（D2/D3）。**坑**：Edit 工具会把 CRLF 文件改写成 LF 导致整文件 diff（RuleList/UserList/ExamManagement/TimeRuleList 曾被全文件重写）——处理：对 HEAD 含 CRLF 的改动文件用 python 统一转 CRLF（`data.replace(b"\r\n",b"\n").replace(b"\n",b"\r\n")`），diff 恢复真实改动量。验证：后端 py_compile ✓、`test_rules_routes.py`+`test_time_rules_routes.py` **18 passed**、前端 lint **0**、`vite build` ✓。

- **状态显示异常全面排查与修复（2026-08-15 深夜，用户指令"排查并修复课程/班级启用禁用显示异常 + 全面排查其他状态模块"）**：
  - 结论：课程(SubjectManagement)与班级(ClassManagement)模块的启用/禁用渲染**无 bug**（前后端均用 is_active Boolean，emerald/slate 映射正确）——用户感知的"已禁用仍显示启用"实发生在**用户账号模块**：后端 `api/users/users_routes.py` 的学生列表序列化器（拼音分支+常规分支）与单用户详情 `get(self,id)` **从不返回 is_active/is_blacklisted**，前端 UserList/UserTableRow/UserDetail/PermissionManagement(Admins Tab) 也从无渲染，导致被禁用/拉黑的账号界面上看不出区别。
  - **修复（根因，后端）**：
    ① `api/users/users_routes.py`：`user_model` 补 `"is_active"/"is_blacklisted"` 字段声明；列表两分支 dict + `get(self,id)` 详情均补 `"is_active": u.is_active, "is_blacklisted": u.is_blacklisted`。
    ② `api/system/admins_routes.py`：Admin 列表/详情同样遗漏 `is_active`（`admin_model` 也补声明），已补 `"is_active": a.is_active` —— 否则前端新加的 Admins Tab 状态列会因字段缺失全部渲染"禁用"。
  - **修复（前端渲染）**：UserTableRow 加"状态"列（黑名单红/启用绿/禁用灰，并更新 memo 比较键）；UserList 表头加"状态"列；UserDetail 信息区加"账号状态"行；PermissionManagement Admins Tab 加"状态"列渲染 admin.is_active。
  - **边界/方向类修复（其他状态模块）**：
    ① AttendanceManage `getStatusBadge` 未知考勤状态原兜底为"出勤"（掩盖异常数据）→ 改为中性灰色"未知"。
    ② FirmwareManagement `handleToggleActive` 的 toast 用旧值判断（`version.is_active ? '已禁用':'已启用'`）导致文字与操作方向相反 → 改为 `!version.is_active ? '已启用':'已禁用'`（反映切换后新状态）。
    ③ Approvals `getTypeLabel` 未知类型原返回原始英文 code → 改为"未知类型"。
  - 商品上下架状态：前端无 mall/goods 页面（整体缺失，非 bug）；审核 status(approval)映射经核查无反转错误。
  - 验证闸门全绿：后端 py_compile（users_routes/admins_routes）✓；前端 eslint `src --ext .ts,.tsx` **0 问题**；`vite build` ✓（仅一条既存 CSS `background-position` 拼写告警，非阻断）；`vitest run` **160 passed / 3 skipped**。改动尚未 commit（按约定不主动 push）。CRLF 文件（users_routes/admins_routes/UserList/PermissionManagement/Approvals）编辑后已用 python 统一转回 CRLF 避免整文件 diff。
