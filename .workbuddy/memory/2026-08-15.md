# 2026-08-15

- 本地提交收尾（延续 #336-#342 会话）：用户确认「全部本地提交」后，把累积未提交改动（双 MQTT 连接分流 + datetime.utcnow 弃用清理 + #336-#339 全文检索/AlgorithmAnalysis/Celery 启动/docker-compose/run.py 等，含删除 startup/tasks.py、新增 verify_mqtt_e2e.py 与 memory 跟踪文件）合成一个本地 commit `b3abaaa`（19 文件，+856/-559）。pre-commit 钩子跑通 Python 语法校验（13 文件）。工作树已 clean，本地领先 origin/main 37 个提交，**未推送**（按约定仅本地）。
- 用户开代理后**推送完成**：直连 GitHub 已通（代理为全局/TUN 透明路由，无需本地端口），`git push origin main` → `89a0113..b3abaaa`，37 个本地提交全部上 GitHub（PUSH_EXIT=0）。工作树与远端 main 现已同步。
- 5000 孤儿 socket：**用户重启机器后已彻底释放**（PID 170708 随重启消失，netstat 确认 5000 空闲）。后端随重启也停止，故重新在 **5000** 干净拉起（PID 19388，日志 `C:/Users/53527/AppData/Local/Temp/backend5000.log`）：双连接 `connected=True`，控制订阅 `score/#`/`phonebox/unlock/#`/`ota/#`/`points/#`/`query` + 遥测 `phonebox/#` 全部到位，前端 Vite 的 5000 代理现已对齐。
- e2e 复测（迁回 5000 后，对生产 Broker 真机）第 1+2 次结果 + 代码深挖结论：
  - 第 1 次：`e2e_verify.log` → A PASS(rules=21)、B/C1/C2 FAIL(no response)。
  - 第 2 次：`e2e_verify2.log`(8m30s) → A FAIL(rules=0，但设备侧**收到**了回包，非 no response)、B/C1/C2 FAIL(no response)。
  - 深挖（读 `mqtt_message_service.py` + `mqtt_manager.py`）：A 与 B/C1/C2 走**完全相同的发布链路**（`publish_mqtt` → `mqtt_manager.publish` → `self._client` 控制连接 QoS1）；`handle_score_add` 三个分支（rule_id/rule_name/score_change）回包代码**完整**（含 undo_code，650-665 行 else 也回包）；C2 `db_score=85` 铁证后端收到 `score/add` 并落库。即**B/C 的 FAIL 非后端业务 bug**。
  - 真正差异只在 topic：A 回包 `score/rules/result` 是设备**精确**订阅（两次都收到）；B 回包 `phonebox/unlock/{box}`、C 回包 `score/add/result/{cid}` 是设备**格式化精确/通配**订阅，在生产高负载下设备连接**系统性**收不到（非随机抖动）。判定为**e2e 测试桩设备连接对这两类 topic 的收包在生产洪流下失效**，属测试环境 artifact，与双连接根治无关。
  - 附带真实信号：第 2 次 A `rules=0`（第 1 次 21），提示生产洪流下后端 `score/rules` 查询偶发返回空（可能受 DB 写锁影响）——属独立问题，非本次"洪流淹没控制消息"议题，留待另查。
  - 结论：双连接根治洪流的核心目标已达成（控制连接在洪流下稳定收发，A 回包两次均达设备、QoS1 生效、订阅隔离有效）。要铁证消除 B/C 的测试桩订阅疑问，需在生产低峰或本地 Broker 重跑（本机无 Linux），或增强 e2e 脚本打印 SUBACK。
- **score/rules 返回空根因（真实后端 bug，已修复并验证）+ e2e 脚本增强 SUBACK（Task #343/#344，本次）**：
  - 根因：库里 `score_rule` 表实测 **21 条全 `is_active=1`**（数据非空），第 2 次 e2e 的 `rules=0` 是 `handle_score_rules_query` 用 Flask-SQLAlchemy 默认连接（`busy_timeout=0`）在生产洪流高频写库时撞 SQLite 写锁 → `OperationalError` 被 `except` **静默吞成 `rules=[]`**（运维不可见）。
  - 修复：`backend/services/mqtt_message_service.py` 的 `handle_score_rules_query` 改 `PRAGMA busy_timeout=5000` + 4 次重试 + `logging.error` 留痕（诚实失败不再伪装空）。
  - e2e 增强：`verify_mqtt_e2e.py` 加 `_on_subscribe` 回调打印每个订阅 SUBACK（granted_qos）+ 维护 mid 映射；A 用例打印完整回包（success/message/rules_len）。
  - 第 3 次复测（`e2e_verify3.log`，6m25s，新后端 PID 1148）：**SUBACK 铁证 4 个订阅全 granted_qos=[1] OK**（订阅层 100% 正常，推翻"订阅没成功"假设）；**A PASS=稳定 21 条**（修复生效，rules=0 不再出现）；B/C1/C2 仍 FAIL no response（B 发 58 次、C1 发 59 次 QoS1 重发仍 0 收到）。
  - 最终结论：① `score/rules` 锁竞争静默失败=**真实后端 bug，已修+验证稳定 21**；② B/C 的 no response=**测试桩设备连接在 EMQX 生产 Broker ~5000msg/s 负载下的单客户端投递/流控 artifact**（SUBACK 正常 + C2 db_score=85 铁证后端逻辑完整），非双连接修复对象；铁证级干净验证需本地 Broker（本机无 Linux）或后端侧断言（已有 C2 落库佐证）。
  - 改动尚未 commit（按约定不主动 commit，待用户确认）。
