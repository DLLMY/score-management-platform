# 2026-09-06 工作日志

## B3 扩展③✅：ScoreRankRule 收敛完成
- 取证：模型 `ScoreRankRule`（score_models.py:192，12 列、无关联派生、无敏感字段）无 to_dict；内联序列化 **3 处**——rank_routes.py 列表 11 字段 / 详情 12 字段 + `cached` 同级（注意：列表 `cached` 在 result 层，详情 `cached` 与 12 字段同级）/ **rank_service.py 活跃规则 9 字段（服务层带缓存，实为 API 消费路径）**。
- 落地：模型层 `ScoreRankRule.to_dict(fields=None)`（默认=详情 12 字段契约，serialize_dt）；rank_routes.py `RANK_RULE_LIST_FIELDS`(11) + 详情 `{**rule.to_dict(), "cached": False}` 保键序；rank_service.py `RANK_RULE_ACTIVE_FIELDS`(9)（LF 文件可 Edit；rank_routes/score_models 均 CRLF → 二进制 heredoc 行列表 join）。
- 证据：py_compile OK + 定向 pytest **12 passed**（test_rank_routes/test_rank）+ run_regression **REG_EXIT=0 全部通过**。
- 残留：仅 `redis_cache_service` warmup 预热扁平结构（非 API 序列化），沿用留档判定。
- 踩坑：**bytes 正则 `re.M` 下 `.*$` 会把行尾 CR 一起吃掉** → 锚 `ns_line` 末尾含 `\r`，`ns_line + EOL` 变 `\r\r\n` 导致 count=0 断言失败；修法 `m.group(0).rstrip(b'\r')`。
- **B3 全战役终态（5 实体）**：User/ScoreRecord（08-23 就绪）+ ScoreRule + Admin + ScoreRankRule，累计 **9 文件 +254/−170**，各域残留均为审计快照/MQTT 协议消息/引擎内部 DSL/预热缓存等非序列化点。未 git commit（HEAD 526f6e3 == 远端 main）。

## T2（#968）全量 vitest 收口✅
- 全量 vitest（线程池，managed Node 22.22.2）：**EXIT=0、Errors 0、Test Files 38 passed | 1 skipped (39)、Tests 271 passed | 3 skipped（274）**，2m08s（较历史 ~8min 快，线程池+缓存命中）。
- 三要素核对全过：退出码 0 / Errors 0 / **reported 39 == disk 39 且 comm 差集空**（提取脚本须 `s/\\/\//g` 归一反斜杠 + 正则含 `jsx`，否则漏抓 6 个 `src/components/__tests__/*.test.jsx` 造成"缺失"假象——09-05 已踩过同类坑）。

## useListFetch `enabled` 专项 ✅（2026-09-06 收口）
- 底层 `useOptimizedFetch` 加 `enabled`（默认 true）：false 时挂载/deps 变化不自驱、中止在途，手动 refetch 仍可触发；`useListFetch`/`useListData` 透传。
- 验证：tsc/eslint 0；定向 vitest 10 passed（含 enabled 三用例）。
- 吃掉 3 处条件加载留档（deps 字段名对外不变，子组件零改动）：
  - StudentPortal notifications → `useListFetch({enabled: tab==='notifications'})`；共享 loading 保留，刷新走 refetch。
  - NLPManagement corrections → `useListFetch({enabled: showCorrectionList})`；保留 `fetchCorrections(page?)` 签名（翻页走 setCorrectionsPage 触发 params 自动重拉）。
  - RemoteNotify history → `useListFetch`(列表) + `useOptimizedFetch`(stats 标量)，均 `enabled: showHistory`；删 loadHistory* 与两个 useEffect。
- ClassManagement `fetchClasses(page,keyword,skipCache,perPage)` 为真命令式（skipCache 绕后端缓存），`enabled` 语义与之正交 → 留作 intentional exception（需 hook 扩展 skipCache 透传，独立更大改动）。
- 前端全量回归三要素全过：Test Files 38 passed|1 skipped(39)；Tests **274 passed|3 skipped(277)**；EXIT=0；报告文件 39==磁盘 39 差集空（较基线 271→274，+3 即 enabled 用例）。

## useListFetch `skipCache` 透传专项 ✅（收口，吃掉 ClassManagement 真命令式）
- 根因：`skipCache` 是 API 层 `request()` 的 GET 响应缓存绕过选项（api.ts:4752 classes 已支持）；`useOptimizedFetch` 原声明了死字段 `skipCache` 选项但从未使用，fetcher 签名 `()=>Promise<T>` 无下发通道。
- 落地：
  - `useOptimizedFetch`：移除死 `skipCache` 选项；fetcher 改为 `(ctx:{skipCache:boolean})=>Promise<T>`；新增 `skipCacheRef`，`refetch({skipCache})` 经 refetch 选项下发；旧 `()=>Promise<T>` 调用方类型兼容。
  - `useListFetch`：`ListFetchParams` 加可选 `skipCache`（瞬时、非依赖项）；新增 `RefetchOptions {skipCache?, params?}`；`refetch(opts)` 支持 `{skipCache:true}` 与 `{params:{page:1}}` 单次覆盖（paramsOverrideRef 合并后仅当次生效，不改动外部受控 params）；`dependencies` 排除 skipCache；`index.ts` 导出 `RefetchOptions`/`ListFetchParams`。
  - 测试：src/tests/hooks/useListFetch.test.tsx 加 3 用例（refetch 收 skipCache / params 覆盖 / 普通 refetch 不带）。
- 迁移 ClassManagement：
  - 删 classes/pagination/isLoading(列表态)/fetchClasses loader/初始+搜索两个 useEffect/handlePageChange；改 `useListFetch<ClassInfo>({params:{page,pageSize,keyword}})`。
  - mutation 调用点：`classList.refetch({skipCache:true})`（创建/删除/分配/移除）；导入/撤销重置首页 `setPage(1); classList.refetch({skipCache:true, params:{page:1}})`。
  - 渲染：classes→items、pagination.*→page/pageSize/classList.total、loading→classList.loading（mutation busy 保留独立 isLoading）。删除未用 import `ClassListResponse`。
  - 修复 `OperationLogs` 把 `refetch` 直接当 onClick 的破例（包箭头）。
- 验证闸门：tsc 0｜eslint 0（hooks+pages，消 5 个 exhaustive-deps 警告：deps 改 `classList` + 移除冗余 `searchInput`）｜全量 vitest **三要素全过**：EXIT=0 / failed=0 / reported 39==disk 39 差集空；**Tests 277 passed | 3 skipped (280)**（较 274 基线 +3 即 skipCache 用例）。
- git 全程未 commit（HEAD == 远端 main）。


## 待办（需拍板）
- 班主任 C 批交互增强：指标卡下钻已落地（C-1/C-2 完），其余交互增强待定。
- 后端未改动；git 全程未 commit（HEAD == 远端 main）。

## P3 统一异常处理风格 ✅（收口，全仓零裸吞）
- 评估 `docs/全面代码审查报告-20260821.md` P0–P3 历史优化债：P0 前端测试（16→280 vitest 已大幅覆盖）、P1（T1 综合分重算/T3 列表分页/T6 security fail-closed 均已完成）、P2 卫生（xlrd/xlwt/web-vitals/CRA eslint/print→logger 经取证已不存在，说明后续已清）——仅 P3 有真实残留。
- 真裸吞 `except Exception: pass` 全仓 9 处，全部在 NLP 模块：nlp_service×1、bert_service×1、nlp_enhanced_service×1、nlp_optimizer×3、nlp_ml_service×2、nlp_routes×1。报告 §3 点名的基础设施/缓存/审计处（response.py/error_handler.py/redis_cache_service.py 等）经取证已无裸吞。
- 修复：保留 `pass`（行为零变化）前提下插入 `logging.getLogger(__name__).warning("NLP best-effort operation failed; exception previously swallowed silently", exc_info=True)`；缺 `import logging` 的 3 文件（nlp_service/bert_service/nlp_ml_service）补导入。共 +12 行（9 日志+3 导入）。
- 验证：managed Py3.13.12 `py_compile` 6 文件全过；全仓行解析扫描裸吞残留=0（其余 109 处裸 except 均有真实处理体）。
- 坑：① 沙箱系统 Py3.11 的 pytest 环境本身坏（缺 typing_extensions、flask/werkzeug 版本冲突 ImportError），与改动无关；装 typing_extensions 后仍卡 werkzeug，属环境故障，未强修。② CRLF 文件正则 `except Exception:/s*/n` 匹配诡异失败，改用按行解析（strip 比对）才命中。③ Edit 工具在限流时对多编辑批次只落前 1–2 处，本批改用 Python 二进制读写一次性应用。
- git 全程未 commit（HEAD == 远端 main）。当前未提交文件含：B3 战役后端 11 文件 + A 轨/钩子前端 9 文件 + 本次 NLP 6 文件。

## P2 ML 重依赖解耦 ✅（收口：一等公民化 ml extra）
- 评估 `docs/全面代码审查报告-20260821.md` P2「ML 重依赖解耦」：经取证**代码层 + 清单层已基本完成**（前序 T7 torch 懒加载 + 优雅降级已落地），唯一缺口是 pyproject 未声明 `ml` optional extra。
- 现状取证：
  - 运行时代码层：`nlp_ml_service.py`/`bert_service.py` 对 torch/transformers/xgboost/lightgbm/shap/catboost/gensim 全部模块级 `try/except ImportError` + `_INSTALLED` 标志；调用点确用标志检查（XGB_INSTALLED 用 8 次、CATBOOST 6 次、line 360 `if not TRANSFORMERS_INSTALLED or not TORCH_AVAILABLE`、line 178/1363/1563 返回 `{"error":"算法依赖未安装"}`）——降级真实有效，非伪解耦。
  - 清单层：`requirements.txt` 基础依赖已排除 torch/transformers/xgboost/catboost/gensim（仅留 jieba/pypinyin/scikit-learn 轻量）；`requirements-ml.txt` 独立承载 xgboost/lightgbm/shap/torch/transformers。
- 本批收口（唯一真实缺口）：`backend/pyproject.toml` 新增 `[project]`（name/version/requires-python=>=3.11）+ `[project.optional-dependencies].ml`，镜像 requirements-ml.txt；`requirements-ml.txt` 头部补 `pip install .[ml]` 等价说明；`requirements.txt` 注释补指向。使解耦一等公民化（计划原话"拆 optional extras"）。
- 零风险依据：backend `Dockerfile` 用 `pip install -r requirements.txt`（非 `pip install .`）、无 CI 引用 pyproject，新增 `[project]` 仅元数据，不触发任何打包构建。
- 验证：managed Py3.13 `tomllib` 解析 pyproject 合法、`ml` extra 可读、`[tool.black]` 保留；基础 requirements 不含重包；全仓无函数内无守卫硬 import 重包。
- 范围克制：未把 numpy/scipy/sklearn 改懒加载（已在基础依赖、体量可控、改之收益低）；未做 ml_deps 中心化（触及多文件、pytest 沙箱坏，风险>收益）。
- git 全程未 commit（HEAD == 远端 main）。

## 收口与提交 ✅（累积改动已 commit，未 push）
- 评估《全面代码审查报告》P0–P3 历史优化债：P0 测试覆盖 / P1 T1-T3-T6 / P2 卫生项+ML解耦 / P3 裸吞清零 **全部收口**。
- 3 项"架构级债"复核结论（经取证，非盲目强推）：
  1. 配置源收敛：config.py 已是中央 Config 类（docstring 标"P2 收敛说明"），三处密钥读取点值同源且 fail-closed → **已超前置完成**。
  2. 大路由瘦身：路由最大 1655 行但均 service 编排层、已用 joinedload 规避基础 N+1；盲目下沉 20+ 文件属高异构低确定收益 → 按铁律**拒绝**。
  3. N+1 复审：需 DB/EXPLAIN 巡检，沙箱不可实操 → **暂缓**。
- 提交：commit `04ee9d9`（31 文件，+2265/−1873），含 B3 后端 9 / 前端钩子+5 页+测试 9 / NLP 6 / ML pyproject 3 / 记忆日志 3。pre-commit 钩子未挂接（run_validation.py 仅 compile 语法检查），后端 py_compile 已全绿，提交未被阻断。
- **未 push**：用户未显式要求；且本地 `origin/main` ref 陈旧（`git status -sb` 显 ahead 71 为假象，须 `git ls-remote` 核实）。push 走代理（Clash/v2rayN）+ origin，待用户授权。
- 当前工作树干净（无未提交改动）。

## 报告 §5 写路径同步重算→异步 收口 ✅（最后残留补齐）
- 复核发现 `services/score_recalc.py` 已实现「Celery 异步优先、同步兜底」封装 `enqueue_or_recalc_user_score`（文档明言「统一收口 records_routes 多处 recalculate_user_score 调用」），但**热路径未全量接线**——records_routes.py:620 等 4 处活跃调用点仍直连 `CompositeScoreService.recalculate_user_score`（同步阻塞）。
- 本批收口：4 个活跃调用点（api/scores/records_routes.py:620 / api/scores/approvals_routes.py:93 / services/mqtt_message_service.py:581 / services/nlp_enhanced_service.py:3436）全部改为走 `enqueue_or_recalc_user_score`，import 同步替换（每文件 `CompositeScoreService` 仅 import+call 两处，替换后无悬空引用）。records_routes 其余 28/260/776/893 此前已走封装，620 为最后残留。
- 零回归验证：managed Py3.13 `py_compile` 5 文件全过；同步兜底模式下 wrapper 返回 recalc 结果 dict，`result`/status 语义与改动前一致；仅显式开启 `CELERY_ASYNC_SCORE_RECALC`+broker 可达时走异步入队（写路径立即返回，报告 §5 期望优化生效）。
- 合法保留：`score_recalc.py:37` 自身同步兜底、`tasks/score_tasks.py:25` Celery 任务体、`tests/*` mock 引用、`backups/refactor_F17/*` 备份分支中的直连调用——均不动。
- 至此《全面代码审查报告》P0–P3 全部优化项经新证据复核**确认收口**：P0 前端关键路径测试(含 api/PermissionGuard/stores/ScoreChart 等点名模块，vitest 280 passed) / P1 T1-T3-T6 / P2 卫生项+ML解耦 / P3 裸吞清零+写路径异步化。
- git：本批 5 文件未提交（连同此前记忆日志，均待用户授权 commit）。

## P3 裸吞修复补全 ✅（全仓生产代码裸吞=0，复核扫描修正前误判）
- 前轮「全仓=0」结论**不准确**：当时扫描漏了 `except ValueError: pass`、`# noqa: BLE001` 形态及部分 best-effort 站点。本轮回溯式全仓精确扫描（非 test/migration 共 601 个 except 块）：裸吞 18 处 =
  - 8 处 tests/conftest（测试夹具，可接受）
  - 5 处入口/teardown best-effort `# noqa: BLE001`（run.py/startup/service_init，可接受）
  - 5 处生产 best-effort：risk_predict_service×2、score_tasks×1、classes_routes(审计)×1、datetime_utils×2
  - 1 处 nlp_enhanced_service:1869 `except ValueError`
- 经上下文确认仅 3 站点是真残留（应补日志）：`risk_predict_service.py:62,74`（模型配置 load/save best-effort 失败静默）、`classes_routes.py:43`（审计失败被吞）。其余均正确模式不动：score_tasks.py:32（Celery self.retry 失败时 line 28 已 logger.error）、nlp_enhanced_service:1869（集合遍历逐条 ValueError 跳过坏项）、datetime_utils:61,68（多格式回退链）。
- 修复：`risk_predict_service.py` 补 `import logging` + 2 处 `logging.getLogger(__name__).warning(..., exc_info=True)`（CRLF 文件，Python 二进制读写，避开 Edit 不稳定与缩进损坏）；`classes_routes.py` 用已有 `logger` 补审计失败 warning。保留 `pass`（行为零变化）。
- 验证：managed Py3.13 `py_compile` 两文件全过；最终全仓扫描生产裸吞残留=**0**（已排除 test 夹具/入口 teardown/已知正确模式）。
- 坑：Edit 工具在限流/多编辑批次下不稳定，CRLF 文件须用 Python 二进制读写；插入日志后必须确认缩进（except 块内应多 4 空格），否则 IndentationError。
- git：本批 2 文件 + 写路径异步化 5 文件 共 7 改动待 commit（连同记忆日志）。

## 后端 pyflakes 卫生审计 ✅（修复 2 真实 BUG + 死导入清零）
- 动机：后端无 flake8/pyflakes 闸门，死导入/未用变量会累积（报告 §7 规范化主题）。用 system Py3.11 的 pyflakes（仅 AST 解析、不导入代码，坏掉的 flask 沙箱环境无碍）全量审计 app/services/api/utils/models（排除 backups/migrations/refactor_F17/tests）。
- 真实 BUG 修复（undefined name）：
  - services/nlp_optimizer.py：P3 编辑插入 logging.getLogger 但 import logging 仅存在于某方法内（函数级作用域不跨方法），模块级引用未定义 → 补模块级 import logging。
  - api/academics/course_schedule_routes.py：_parse_course_import_input 直接引用 field_mappings/default_values（定义在 _load_course_import_config 局部），调用方未传入 → 加参数并透传。
- 死导入清理（零副作用，CRLF 文件用二进制安全 Python 注入）：
  - 批次A：mqtt_message_service 移除未用 IntegrityError（消 redefinition）、approvals_routes 删函数内冗余 import logging、route_registry 去冗余 f-string 前缀、nlp_optimizer 删未用 log_debug。
  - utils.logger.* 纯函数死导入 22 处批量移除。
  - 批次B：sqlalchemy.func×2 / serialize_dt / get_nlp_parser(局部) / joinedload / db_session_scope / invalidate_cache×2 / categories_routes 的 db 等 9 处零副作用死导入移除。
- 刻意保留（# noqa: F401 副作用导入，删之破坏启动/注册）：score_recalc.py 的 celery_app（fail-fast 探测）、models/__init__.py 的 TeacherComment（模型注册）。
- 范围克制：15 处未用局部变量（_admin/subject/last_err/e）含 DB 查询等赋值副作用，盲删改行为，按零回归铁律不自动清（纯 lint 噪音，零运行时影响），记为 deferred。
- 验证：managed Py3.13 py_compile 全量 24 改动文件通过；pyflakes 从 58 处收敛至 17 处（剩余=2 刻意副作用导入 + 15 未用局部变量）。
- 提交：3b2d040（24 文件，+24/−30）。未 push（用户未显式要求；本地 origin/main ref 陈旧，push 需代理待授权）。
