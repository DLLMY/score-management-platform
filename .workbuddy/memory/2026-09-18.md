# 2026-09-18 分批提交并推送（按钮覆盖度复核 + 真实提交验收）

## 提交与推送（用户指令「按项目规范分批提交并推送」）
- 分批策略：前端 2 文件（独立 bug fix）/ 测试产物+记忆（覆盖度复核+真实提交验收）/ gitignore 安全（独立）。共 3 个 commit，全部经代理推送 origin main 成功（ls-remote 核实 HEAD=6fccc86 一致）。
- `e11ae3e` fix(frontend): performanceReportingService.ts（sendBeacon 两处改 Blob application/json，修 415）+ vite.config.ts（/api、/ws 代理加 timeout/proxyTimeout=120000，消 504 串扰）。
- `de8f2dc` test(browser): 覆盖度复核（scan_coverage.js / analyze_coverage.js / test_batch_buttons.js / coverage_report.html）+ 全按钮真实提交验收（click_submit_all.js / submit_report.html / verify_remaining·verify_box.js / final_button_report.html）产物与数据 + 记忆 2026-09-17.md。共 36 文件。
- `6fccc86` chore: .gitignore 排除含密钥开发计划目录 `下一步开发计划-2026-09-15（含，请勿外传key在）/`、临时脚本 `apps/backend/__*_tmp.py`、`apps/backend/api/uploads/`（OTA 固件）、`.workbuddy/healthcheck.txt`。

## 安全核查
- 提交前逐文件排除：调试脚本(debug*/diag*/run*)、截图(*.png)、固件上传、临时脚本、含密钥目录均**未入库**（git status 复核敏感项 = 6 个 ?? 全为未跟踪，未进任何 commit）。
- 注：`docs/reports/*20260915*.md`（覆盖度复核旧草稿）与大量 browser_test 调试噪音(*.png/debug*/diag*/run*)保持未跟踪，未强制入库。

## 遗留未跟踪（本地保留，不入库）
- `.workbuddy/browser_test/` 下调试脚本/截图/中间数据（debug*/diag*/run*/ok_*.png 等）；`docs/reports/` 两个 20260915 旧报告草稿；`下一步开发计划-2026-09-15（含，请勿外传key在）/` 已 gitignore。

## 清理调试噪音 + 旧草稿去留（用户指令「都需要」）
- 对 `.workbuddy/browser_test/` 未跟踪文件做**分类删除**：保留真实交付物、删除 155 个调试噪音文件 + `shots/` 目录（含 ~100 张 jpeg 截图）+
  6 个中文名 `shot_*.png`。噪音类型 = 截图 PNG/JPEG、console/result 文本 dump、debug/diag/run/triage 脚本、临时 py、中间数据 `all_buttons_full_backup.jsonl`、`dummy_fw.bin`、`01_login.html` 等。
- 关键坑：git status 对含非 ASCII 文件名输出**八进制转义 + 引号包裹**，直接拼路径 `fs.statSync` 会 ENOENT；修正 = 先 `rel.replace(/^"|"$/g,'')` 再解析，且中文名截图改用 `fs.readdirSync` 真实名匹配删除。
- `docs/reports/` 两个 `*-20260915.md` 旧草稿 → **丢弃**（已被已提交 HTML 报告 submit/coverage/final_button/分页面 取代，冗余）。
- `分页面验收报告.html`（逐页验收 HTML，内嵌截图，与已提交报告同系列，此前漏提交）→ **保留并补提交** `ece992a`，推送 origin main 核实 HEAD 一致。

## 净结果
- `git status --short -- .workbuddy/browser_test/ docs/reports/` 现已**无未跟踪残留**；远程 HEAD=ece992a = 本地，全部推送核实通过。
- 真实交付物在 git 中完整：coverage_report.html / submit_report.html / final_button_report.html / 分页面验收报告.html / all_buttons_report.html / report_49pages.html + 各 harness 脚本 + 数据。

## 前后端按钮·API对接·字段统一 真实写入复核（用户指令「再次...要求进行真实写入」）
- 改方案：静态契约 agent 触发频限(429)，改用**运行时请求/响应 round-trip 比对**更准——真实写入时同时捕获前端发出的请求体字段与后端返回的持久化字段，按 `camelCase↔snake_case` 归一化后逐值比对。
- 新增 `verify_fields.js`（真实写入 + 字段捕获 harness，含硬件控制网络层 abort）+ `analyze_fields.js`（初版有 100 误报）+ `gen_verify_report.js`（修正版报告）。
- 首轮全量跑因 `newFields` 作用域 bug（按钮循环内声明、循环外引用）致 36-48 页 PAGE_EXCEPTION；修复（提升至按钮循环作用域）后**追加模式**补跑 36-48 页（PAGES 环境变量），与已有 0-35 合并。
- **数据**：59 真实写调用 = 15 成功业务写 + 5 非2xx + 39 遥测写；覆盖 34 页真实业务写（其余 15 为只读/展示/导航页或仅遥测，非缺口）；14 个去重业务端点。
- **结论**：所有成功业务写（classes/subjects/activity/culture/study-guide/seating/notify_templates/system config·backup·clear-cache/consistency-fix/class-periods/reset 等）请求字段与后端响应字段**完全一致，0 真实字段统一问题**。
- 5 个非2xx 全部为 harness 自动填充的**测试数据**被后端正确拒绝：`/api/approvals`(user_id:"0" 学生不存在→"学生ID不能为空")、`/api/admins`(弱口令→"密码强度不足")、`/api/system/restore`(不存在的备份名→404)——字段名前后端一致，证明校验+信封错误层正常，非对接缺陷。
- 1 个 `member_ids`「未回显」属误报：后端 `group_model` 已声明 `member_ids`，差异仅「创建响应回显 members/member_count 形态(请求/响应形态差)」+ harness 发占位值未实际入组，非命名不一致。
- 72 条初版 FIELD_DROPPED 误报来自 `frontend-performance` 遥测写（202/204 不回显体，预期），已剔除。
- 净零：测试后 `restore_db.js` 还原至基线 `cfd8a459…` md5 一致；还原后 live 应用自身遥测漂移非测试残留。最终报告 `verify_fields_report.html`。
- 注：本任务产物（verify_fields.js / analyze_fields.js / gen_verify_report.js / field_capture.jsonl / submit_results.jsonl 等）尚未提交，待用户确认后入库。

## 续：gitignore 泄漏黑洞修复 + 调试噪音二次清理（用户指令「继续推进」）
- **严重发现**：`6fccc86` 写入的 gitignore 模式 `下一步开发计划-2026-09-15*` **与实际目录名不符**——真实敏感目录名为 `生成时间：2026-09-15` 与 `生成时间：2026-09-15（二轮·修复提交点击与重复key后）｜`（全角冒号 U+FF1A，第二个含 "key"）。旧模式（半角连字符 + 错误前缀）匹配不到任何真实目录，**两个含密钥目录此前完全未被 gitignore 保护**，存在入库泄漏风险。
- 修复：`.gitignore` 第 194 行改为 `生成时间：2026-09-15*`（全角冒号）。已用 node 直读真实目录名 + `execFileSync('git',['check-ignore', name])` 复核，两个真实目录均 **PROTECTED**（旧模式校验用错名曾假阳性"已忽略"，本次以真实磁盘名为准）。
- 同时新增 `.workbuddy/_trash_*/` 忽略（回收站不入库）。
- 调试噪音二次清理：将 26 个 429-pivot 死稿（err*.txt / diag*.py / analyze_fields.js / contract.json / backend_raw.json / build_contract.py 等）移至 `.workbuddy/_trash_20260918/`（可恢复），保留 verify_fields/gen_verify_report/harness 与各 HTML 报告。
- 现状：`git status` 仅 `.gitignore` 为 ` M`，其余干净；无代码改动。本 gitignore 安全修复**未提交**（等用户确认 commit+push）。

## 前后端 API 契约静态 diff（用户指令「1」= api_versioning.py 路由表 vs 前端 api.ts）
- 用户从「下一步可选」里选了 ① 静态契约 diff，作为实时按钮测试的补充交叉验证。
- 后端事实来源：用 venv python 加载 Flask `app.url_map`（避开外部服务连接：`create_app(lightweight=True)` 后手动 `api_version_manager.init_app(app)`，只注册路由不连 MQTT/Redis/Celery）。导出 959 条 `/api` 规则 → 归一化后 **485 个唯一资源路径**（已含 `/api` 与 `/api/v1` 双前缀，去 `<converter:name>`/`<name>` 为 `{}`）。
- 前端事实来源：扫描整个 `apps/frontend/src` 下 **173 个 .ts**（不只 api.ts，因 request 被多 service 文件 import）的全部 `request(...)` 调用；方法用**括号匹配精确锁定每个调用作用域**再取（初版在 800 字符窗口误归因 method 致 58 假"方法不匹配"）。
- 归一化坑（已修）：① 查询串构造 `${query ? '?'+query : ''}`/`${params.toString()}`/`${qs}` 应整段移除而非转 `{}`；② `encodeURIComponent(className)` 函数包裹变量应判为路径参数 `{}`；③ 嵌套模板 `${query ? \`?${query}\` : ''}` 用括号匹配彻底删除；④ 纯变量名在 QUERY_VAR 集(query/qs/params...)→移除，其余→`{}`。
- **结果**：410 个前端唯一调用 ↔ 485 后端路径：**匹配 410 / 孤儿 0 / 方法不匹配 0**。前端每个 REST 调用均有对应后端路由且方法一致。
- 180 条"后端无前端调用"为信息性（devices/MQTT/diagnostics/migration/infra/swagger + 同一 ns 下 UI 未暴露的额外 CRUD 变体），非契约断裂。
- 产物（位于已 gitignore 的 `.workbuddy/tmp_scan/`）：`dump_routes.py` / `backend_routes.json` / `contract_diff.js` / `contract_diff.json` / `contract_diff_report.html`。未提交（tmp_scan 本就忽略）。
- 结论闭环：实时按钮真实写入(271按钮/49页/净零) + 本静态契约 diff 互相印证——前后端 API 对接与字段统一**无断裂**。

## 前后端匹配 + 功能统一（用户指令「进行下一步前后端匹配和功能的统一，尤其是使用逻辑要符合一般人的使用习惯」）
- 方向：在前两轮（实时真实写入 + 静态契约 diff，已证明路由/字段/方法无断裂）基础上，转向**页面级 UX 语义统一**，重点查"破坏性操作确认一致性 / 增改闭环一致性 / 前后端语义错配"。
- 基础设施核验（已统一，非本任务目标）：错误信封 `unwrapEnvelope` 单一真相源(HTTP200+success:false 抛 ApiError)、分页 `per_page` 前后端一致、确认组件 `ConfirmDialog`+`useConfirm()`、反馈 `showToast` 唯一原语(1007处)、401 自动刷新+重定向。
- 子代理因 429 频限不可用 → 改用直跑静态扫描脚本 `ux_audit.js`/`ux_gap.js`（Node 全 src 扫描，括号匹配锁定函数体）量化一致性信号。
- 真实缺口（仅 1 处）：用户列表页批量删除/批量评分 `useUserListScore.ts` 的 `handleBatchDelete`/`handleBatchScore` **无任何确认弹窗**——单删 `useUserListCrud.ts:177` 已确认，根因是组合根 `useUserListLogic.ts` 创建了 `confirmRef` 却只传给 crudDomain、**漏传 scoreDomain**。
- 修复（低风险、零逻辑破坏、沿用模块既有 `confirmRef.current({type:'danger'/'warning'})` 写法）：`useUserListScore.ts` 注入 `confirmRef` 接口 + 两处批量操作补确认（批量删除 type:'danger'「将删除 N 名学生，不可恢复」、批量评分 type:'warning'）；`useUserListLogic.ts:66` 组合根补传 `confirmRef` 给 scoreDomain。
- 误报消除：初版按文件计"缺确认"误报 52 处（types.ts 字段名/api.ts 批量封装/View 透传 prop/antd `<Popconfirm>` 声明式）——经逐类核验均为误报，收窄到唯一真实缺口。
- **零回归闸门全绿**：tsc=0 / eslint(改动文件)=0 error / prettier 全绿 / vitest **276 passed / 3 skipped / 0 failed**（=基线）。四闸门守门通过。
- 产物：`unify_audit_report.html`（含五项证据表 + 修复说明 + 闸门状态）。位于已 gitignore 的 `.workbuddy/tmp_scan/`，未提交。
- 待评估（未动手，按"低风险高价值优先"原则）：若需进一步收紧，可人工复核仍用裸 `request()` 直接写且未确认的边界页面——但本仓 CRUD 已高度走 `useConfirm + wrapAsync + showToast` 统一范式。

## 继续优化（用户指令「提交推送后继续优化」）
- 先提交推送上轮 userList 批量删修复：`eee6300`（含 2 修复文件 + 记忆日志），`git ls-remote` 核实远程 HEAD=本地 ✅。
- 继续审计（确认覆盖/提交防抖/增改范式全健康，铁证如下）：
  - 确认弹窗覆盖：全 src 扫描 `confirmRef.current(` 66 处 + antd `<Popconfirm>` 67 处 = 133 处破坏性确认，广泛统一。
  - 提交防抖：ruleList 用 `useSubmitGuard`+`runSubmit(handleSubmit)`(RuleModal:53)+`disabled={submitting}`(RuleModal:223)，scoreEntry 同款——初版"函数体内无防抖"检测是误报（防抖在 View 层），实际健康。
  - 增改范式：ruleList/scoreEntry 均 `useModal` + 成功 `closeModal()` + 刷新 + toast，统一。
- 新发现真实不一致（已修复，未提交）：确认弹窗 `type:'danger'` 语义。全 63 个确认弹窗中 43 个破坏性语义、41 个已 danger(93%)，仅 2 个破坏性确认用 `type:'warning'`（视觉警示弱于约定）：
  - `ClassPeriodSettings.tsx:142`（重置并删除所有自定义节次）→ 改 `type:'danger'`
  - `useScoreEntryActions.ts:381`（清空所有学生某科成绩）→ 改 `type:'danger'`
  - 纯 UI 提示色改动，零逻辑影响。零回归闸门全绿：tsc=0/eslint=0/prettier全绿/vitest 276·3·0。
- 产物：`ux_gap2.js`/`ux_gap3.js`/`ux_confirm_audit.js`（脚本）+ `unify_status_report.html`（综合状态报告）。均在 `.workbuddy/tmp_scan/`（gitignore），未提交。
- 可选后续（需用户拍板，涉及语义/多文件，未擅自动手）：a. 两套防抖 wrapAsync vs useSubmitGuard 收敛；b. 自定义 validateForm(ruleList) vs antd Form rules 校验范式统一；c. 确认文案风格统一（"不可恢复"警示）；d. 列表空/加载/错误状态一致性审计。

## OpenAPI 契约漂移修复（CI 失败项）
- 现象：CI `verify_openapi_contract.py --strict` 失败（exit 1）。实时后端 swagger 路径 471，快照 `docs/api/openapi.json` 路径 473，快照多 2 个已消失端点：`/role-permissions` 与 `/role-permissions/{id}`（旧 RBAC 设计遗留，整数 id 风格）。后端现仅剩 `/api/rbac/role-permissions/<string:role_code>`（`api/users/rbac_routes.py:457`），无顶层 `/role-permissions`。
- 修复：脚本按 key 精确删除这 2 个路径块 + 孤立 `role-permissions` 标签（`scripts/verify_openapi_contract.py` 的 `--update` 同款格式：indent=2/ensure_ascii=False，故 diff 仅 56 行删除、0 插入，无重排）。路径数 473→471 与实时一致。
- 验证：diff 仅 56 deletions；JSON 合法；逻辑上 only_snap 由 2→0（only_live=0、method_diffs=0 不变）→ 漂移归零。本地 swagger 提取 harness 因 `__init__.py` 模块级 `get_app()` 已全量初始化致蓝图重复注册而报错，改用逻辑推理 + git diff 确认。
- 提交：`docs/api/openapi.json` + 本记忆日志，单原子提交（不与 UX danger 修复混批）。UX danger 修复 2 文件（ClassPeriodSettings.tsx / useScoreEntryActions.ts）仍按规范未提交，待用户确认。

## 四统一任务 a/b/c/d（用户指令「四个同步推进」）
- a. 收敛 wrapAsync 与 useSubmitGuard：userList 此前仅用 wrapAsync 做 loading/错误编排，缺双击防护。在组合根 `useUserListLogic.ts` 用全站标准 `useSubmitGuard().run` 包裹 6 个变更处理器（submit/handleDelete/handleToggleActive/handleQuickScore/handleBatchDelete/handleBatchScore），通过 guarded* 版本注入 props 与 buildUserColumns（表格行删除/启停亦受防护）。保留 wrapAsync 乐观更新/撤销/onSuccess/onError 不变。
- b. ruleList 校验范式统一：删除独立的数组格式 `validationRules` + `validateForm` 调用，改复用 `useForm` 的 `validateAll()`；因 useForm 的 object 规则原生不支持 integer，新增模块级 `integerRule` 校验函数承载整数校验（等价原 'integer' 规则）。handleSubmit 直接 `if (!validateAll()) return;`。
- c. 确认文案风格统一：破坏性确认统一为「此操作不可恢复。」。4 个 outlier 规范化——CategoryList:104 / ruleList/useRuleListLogic:210 / RankRuleList:113 的「不可撤销」+ deviceManagement useDeviceCrudDomain:43 的「无法撤销」→「不可恢复」。Settings:190「覆盖当前数据」语义不同保留。
- d. 列表 空/加载/错误 状态一致性审计（审计为主，未大规模重构）：基础设施已就位——`DataTable` 内建三态（loading→TableSkeleton / error→ErrorState+重试 / empty→EmptyState），`useListFetch`/`useListData` 提供 loading/error/items/total/mutate。采用度：DataTable 被 ~41 文件、useListFetch/useListData 被 ~40 文件使用，约 26 列表页已迁移到标准组合。不一致集中在少数未迁移页：① UserListView 仍自绘 loading skeleton/error/empty 三态并提前 return，DataTable 内建三态失效（line 180-256）；② ImportConfigManagementView:279 直接 antd `<Table>` 遗留；③ RankRuleList:68-80 仍数组格式 validateForm 双轨（与 b 同源，建议纳入同款收口）；④ 空态文案散落（默认"暂无数据" vs 语义化"暂无搜索结果"）。结论：非 26 页普遍问题，建议以"页面级迁移战役"收口，不逐页小改。
- 闸门：tsc 0 / eslint 0 / prettier 全绿；vitest 全量 276 passed / 3 skipped / 0 failed（基线一致）。改动未提交（待用户确认 commit+push）。

## 19:22 补充：RankRuleList 校验双轨收口 + 全量提交
- 用户确认「提交推送 + RankRuleList 同款收口」都要执行。
- RankRuleList.tsx 同 b 模式收口：删除独立 `validationRules`（数组格式）+ `validateForm` 调用，改 `useForm` 的 `validateAll()`；`min_score/max_score` 的 integer 语义用模块级 `integerRule`（`validate` 函数）承载；`handleSubmit` `if (!validateAll()) return;` 后保留 `min_score>=max_score` 自定义 range 校验。
- 零回归复跑：tsc 0 / eslint 0 / prettier 全绿 / vitest 276 passed / 3 skipped / 0 failed（与基线一致）。
- 提交分组（原子）：① UX danger 修复 `ClassPeriodSettings.tsx` + `useScoreEntryActions.ts`；② 四项统一 + RankRuleList 收口 5 文件（`useUserListLogic.ts`/`useRuleListLogic.tsx`/`CategoryList.tsx`/`RankRuleList.tsx`/`useDeviceCrudDomain.ts`）；③ 本 memory 日志。经代理 push，push 后 `git ls-remote` 核实。

## 19:50 列表三态一致性迁移战役（任务 d 落地）
- 战役目标：所有"表格型列表"统一走 DataTable 三态（loading→TableSkeleton / error→ErrorState+重试 / empty→EmptyState），消除自绘与遗留 antd Table 分支。
- 现状基线：DataTable + useListFetch/useListData 已就位；约 34 个表格页已合规；残留"绕过标准"仅 2 处（已本批次收口）。
- 批次1（已完成，commit `38a9073`，已 push 核实）：
  - `UserListView`：移除 loading/error/empty 三个提前 return（手绘 TableSkeleton/EmptyState 旁路了 DataTable 内建三态），改把 `error={message,onRetry}` 与条件 `empty`（暂无搜索结果/清除筛选）交给已用的 DataTable；删除不再使用的 EmptyState/TableSkeleton 导入。
  - `ImportConfigManagementView`：原生 antd `<Table>` 换用 DataTable，下沉 loading/error/empty，移除手动错误横幅；列定义用一次 `as ColumnType<ImportConfig>[]` 断言过检（运行时与 DataTable render 契约一致）。
  - 闸门：tsc 0 / eslint 0 / prettier 全绿 / vitest 276 passed / 3 skipped / 0 failed。
- 批次2（已调研+收尾）：先 grep 全 16 处 `<EmptyState>` 文案 + 交叉比对 DataTable 使用清单，结论：① 16 处全部是 card/grid/图表非表格页、均不在 DataTable 清单内，**不存在"表格绕过标准三态"或"DataTable+自绘 EmptyState 双重渲染"**；文案已语义化（"暂无X数据/记录/文章/计划"），本就合规。② 仅 2 处纯文案 outlier 值得对齐：`TopUsersRanking.tsx:99` "暂无数据"→"暂无学生数据"（与全站"暂无X数据"风格一致且更准确）；`SystemMetrics.tsx:329` desc 被截断（缺"请稍候或检查后端采样服务"），补齐与 272 行一致。③ `UserDetailView:240-252` 是自定义卡片列表（非 DataTable），其错误态用手绘 amber 横幅无重试——属错误态不一致但需加 retry 回调（中等风险，非纯文案，本批不动，列为后续可选）。
  - 闸门：tsc 0 / eslint 0 / prettier 全绿 / vitest 276 passed / 3 skipped / 0 failed。
- **列表三态一致性迁移战役 → 实质收官**：表格型三态已在批次1收口（UserListView/ImportConfigManagementView 下沉 DataTable），非表格页经调研已合规，仅 2 处最小对齐。战役核心目标达成，不建议为"统一"批量改已合理的语义化文案（会破坏语境，违背"符合常人习惯"）。
