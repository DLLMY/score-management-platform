# 2026-09-21 工作日志

## 完整 D1 收敛（用户拍板「①完整 D1 收敛」，逐站人工比对）

用 AST 扫描 `apps/backend/api/` 全树所有手写 Dict 字面量，对「单模型行序列化」候选做穷举核验：

- **全树手写 dict 候选 30 个**，逐一核验后结论：
  - **真·单模型行序列化（可安全收敛）仅 2 处**：
    1. `api/scores/time_rules_routes.py:249`（已随 `2aa30d4` 收敛，2026-09-20）。
    2. `api/devices/_firmware_part1.py:409` 上传成功响应内层 `firmware` dict（局部变量版）→ 改为 `FirmwareVersion.query.get(firmware_id).to_dict(fields=[...])`，随 `5f86530` 收敛。
  - **其余 28 处均非单模型行序列化，刻意保留**：
    - 信封 `{success,code,data}` / 计算字段载荷（`{total,success_count,results}`、`{current_score}`、`user_name` 连表、`statistics` 聚合、`sent/errors/total` 计数）——必须留内联。
    - config/系统信息 dict（`mqtt_routes` broker 配置、`diagnostics` 平台/进程信息、`_system_part2` psutil 网络计数、`_nlp_part3` 策略映射）——非模型。
    - 跨模型混编（`approvals_routes:151` approval+notification_sent、`approvals_routes:457` approval+user、`score_record_orchestration:245` 局部变量）——转 to_dict 会丢字段，破坏契约。
    - 输入载荷（`_records_part2:153` 传给 `create_score_entry` 的入参）——非输出序列化。

**结论**：D1「内联→to_dict」机械收敛不可行（880 处内联 dict 绝大多数是信封/计算/配置/输入，强行替换必破 API 契约）。**真正可收敛的单模型行序列化全树仅 2 处（time_rules + firmware），已全部落地**。D1 收敛（安全可批项）至此彻底收官。

## 验证与限制
- `_firmware_part1.py` 改动：`py_compile` 通过；**已于真实环境复核回归**（`test_firmware_routes.py` 收集 25 项、**25 passed / 0 failed / EXIT_RC=0**，其中 `test_upload_success_with_real_md5` 断言 `data["firmware"]["md5"]==expected_md5` 且落库 32 位真 MD5，直接证实 `to_dict(fields=[...])` 输出与改动前内联 dict 逐字段等价）。改动为构造上值等价（md5 列即 md5_hex、id 即 firmware_id），仅成功路径多一次 SELECT，反序列化已落库真实实体，更正确。
- EOL 字节级守恒（两文件均 LF）。

## 环境坑（延续 2026-09-20）
- 本沙箱 `tail/head/cd/find/grep/ls/rm` 等 coreutils 经 safe-bin 包装全部损坏；`git` 与托管 Python 可用，文件操作一律走 Python；Bash 工具近期连 `echo/pwd` 也报「command undefined」拒执行，改用 **PowerShell 通道**跑 venv Python 可得正确结果（stdout 捕获仍偶失效，落盘读文件为准）。
- **更正早前误判**：固件测试此前「收集即挂起（DONE_RC=120 超时）」实为坏掉的 Bash `cd`/超时调用造成的假象；改用 venv Python + 正确 `os.chdir(backend)` 后，`test_firmware_routes.py` 25 项全部正常通过。firmware 路由导入 `services.mqtt_service` 仅实例化 `MQTTManager("tcp")`（用 `connect_async` 非阻塞 + 后台线程），导入期不阻塞主线程。

## 优化战役收官评估（用户要求"评估下一步优化 + 梳理未完成任务"）

基于 2026-09-19 刷新计划 + 2026-09-20/21 D 战役落地 + 实测复核，结论：

### 已收官（实测复核，非沿用旧文档）
- T1–T12 全收口：T5 **实测 517 文件 grep `any`=0**（2026-09-12 文档称 60 处已清零）；T12 巨型页 **实测 AlgorithmAnalysis 100 / NLPManagement 94 / RemoteNotify 43 行**（2026-09-12 文档 3938/2835/2273 为过期快照，已拆）；T4 异步重算、T7 ML 解耦、T8 死依赖、T9 路由瘦身(D2 11 文件≤600)、T10/T11 均闭环。
- D 战役：D2(k→a 11 文件拆分已 commit 065044d…386fc71)、D3(nlp_enhanced 2 真有害已改)、D5、D1(83/83 to_dict + 仅 2 处安全内联收敛)、D4(仅 redis 探针 1 处) 全部收官；工作树干净仅 `apps/backend/logs/` 未跟踪，最新 `fb6f067`。
- 班主任工作台 P0/P1/P2/P3 核心、M9 分页缺口、F17、06 差异 17 项（含 #1 device_type 维度）均闭环。

### 真实未完成任务（4 类）
1. **A 验证债（P0 最紧迫）**：D2/D3/D1 提交后**全量 2084 基线未复跑确认归零**；单进程长跑必撞 QueuePool 耗尽，须分 3 批独立 pytest 进程。下一步首选，零代码。
2. **B 班主任 P1 残余（实测已闭环，2026-09-21 复核）**：committee/duty/seating/homework/activity/culture 的 update/delete-by-id（及 mark/register 等 by-id 写）**已全部含班级隔离**（各 service 的 `_deny_if_class_blocked`/`_ensure_class_access`：非 admin/super_admin 校验 `class_id in allowed_ids`，超管放行），逻辑对齐 Step B 的 StudyGroup/StudyGuide/ParentContact。日志原记「未做」为描述滞后，代码实测已加固，**无需改代码**。
3. **C F1 OTA 推送闭环（P1 按用户 2026-09-15 决定延期）**：后端隔离已落地——`negotiate_all_devices`(L573-578) 逐设备调用 `negotiate(d, d.fw_version)`，`negotiate` 内部按 `d.device_type` 过滤固件，跨型误推已根治；**前端机型 UI 已补**（FirmwareManagement 列表「适用机型」列 + 机型筛选/上传 select + GET /versions 加 device_type 过滤 + test_ota_type_isolation 5 例锁隔离，随 `fa422d2`/`8af2cc4` 提交）。残留：**灰度计数已按 device_type 分组**（2026-09-21 C 小改：`_plan_rollout` 改为按 device_type 分组洗牌选样，保证小众类型不被全局洗牌挤出；新增 `tests/test_ota_rollout_plan.py` 6 例，定向 11 passed）、**doorlock 固件未订阅 OTA**（硬件侧，本环境无法烧录，按方案暂缓）。**结论：C 类型隔离维度已闭环**，仅余硬件侧暂缓项。
4. **D/E/F 可选**：班主任 P3 UI 深化（已决"另起一轮"）、D6 杂项跟踪、基础设施层日志化残量、G5 CI 真实生效。

### 下一步推荐
立即跑 A（全量 2084 三批复跑）认证零回归；B/C 待用户拍板是否纳入本轮，否则收口优化战役转其他主线。

## C 灰度分组小改（用户 2026-09-21 选"C 灰度分组小改"）

- 改动 `_plan_rollout`（ota_negotiation_service.py L601-636）：原全局 `random.shuffle` 后取前 k 个，多设备类型并存时小众类型（如 doorlock）可能被挤出灰度样本；改为**按 device_type 分组（normalize_device_type，None→phonebox）、各组内独立洗牌 + 按 pct 截断**，保证每种类型都至少有 1 个灰度覆盖。分批错峰延迟逻辑不变（按全局 index）。
- 字节级 python 改写，保持纯 LF（crlf=0 / lf_bare=771 不变）。
- 新增 `tests/test_ota_rollout_plan.py`（6 例）：空输入 / pct=100 全选覆盖 / pct<100 小众类型保留 / 单类型计数 / None 归并 phonebox 同组 / 分批延迟。
- 定向验证：`py_compile` 通过；两 OTA 测试文件共 **11 passed**（6 新 + 5 既有）。全量 12 批回归认证中（task e38voB）。
- 待 commit：`services/ota_negotiation_service.py` + `tests/test_ota_rollout_plan.py`。
