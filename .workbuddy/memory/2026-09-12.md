# 2026-09-12 工作日志

## C901 战役 — #120（21+ 桶）收口 + 战役可安全推进面收尾

### #120 `_validate_course_import_item` (c=22 → 0) — 收口
- 文件：`apps/backend/api/academics/course_schedule_routes.py`（纯 CRLF，改写后 1214/1214 行）。
- 改写：orchestrator + 4 个模块级 helper（`_validate_text_field` / `_validate_day_of_week` / `_validate_period_number` / `_validate_teacher_role`）。仅 `teacher_name` 分支含**只读** `Admin.query` 两次（无写、无事务）。
- 等价证明：差分 harness（HEAD 原版 vs 工作区新版+helpers，打桩只读 Admin）**18/18 + SELF_CHECK_OK**（覆盖各字段空/格式错/超长/有效、day str/int/越界/float、period none/str/zero/over、teacher 角色 ok/错/未找到/格式错、classroom 超长/none、多错误组合）。
- 闸门：py_compile OK · 文件 C901 仅剩 `post`(16) · 后端 C901 **55**（−1）· 默认 **2258 中性** · 纯 CRLF 保真 · 针对性 pytest **9 passed** · 全量回归 **REG_EXIT=0**（待日志确认）。
- 工具：`refactor_validate_course_item.py`（二进制安全，边界断言 start=259 end=342）· `snap_validate_course_item.py` · HEAD 取自 `git show HEAD:...` 存 `.workbuddy/tmp_scan/course_schedule_routes.head.py`。

### #120 21+ 桶剩余 18 项审定结论（按宁跳不强推纪律全部跳过）
全量扫描带路径 21+ 桶共 19 项，逐一审定：
- 写路径/DB 事务：`users_routes.post`(45/25) · `execute_subject_import`(37) · `import_devices`(31) · `subject_routes.post`(23) · `resolve_relations`(22) · `execute_scoring`(21)
- NLP 语义敏感：`nlp.parse`(36) · `extract_behavior`(29) · `parse_without_correction`(28) · `extract_name`(26) · `deep_semantic_match`(24) · `determine_intent`(22)
- 安全/基础设施：`security.validate`(35) · `configure_rate_limits`(26)
- 测试/工具/脚本（不该动）：`tests.run_tests`(27) · `collect_security_metrics`(24) · `verify_rbac_consistency.run_check`(22)
→ **均待用户单独拍板，本战役在「机械低风险可自动安全推进」层面已收口**。

### 本轮（含 2026-09-11 末）累计改动
- #119：`user_list_service.get_user_list_view`(20) + `course_schedule_routes._parse_course_import_input`(20)
- #120：`course_schedule_routes._validate_course_import_item`(22)
- 后端 C901 基线：**58 → 55**（−3）· 默认计数 **2258 中性** · 工作区累积未 commit（HEAD 仍 `8b45381`）。

### 收口 commit（用户"继续完成"授权）
- 工作区跨会话累积 35 处改动（32 代码 + 3 memory）+ 196 个 `.workbuddy/tmp_scan/` 临时产物。
- 按 dirty-worktree-commit-split SOP：无中间快照 → 批次级切分不可行 → **路径前缀分层 4 桶提交**（绝不 `git add -A .`，排除 tmp_scan）：
  - `6db27da` `refactor(services)`: 20 文件
  - `64828e8` `refactor(api)`: 8 文件
  - `cf67bc8` `refactor(utils/tools)`: 4 代码文件 + `.gitignore` 追加 `.workbuddy/tmp_scan/` 排除
  - `0d477d9` `docs(memory)`: 3 文件
- `git status --porcelain -uall` = 0（干净；tmp_scan 已被忽略）。**未 push**（用户未要求）。
- 整体已全量回归 REG_EXIT=0 背书（含全部累积改动）。

## C901 战役 — #121（「可安全机械收口」子集首批 2 项）收口

对 55 项余量重扫并按风险审定，结论：**可机械收口子集仅 4 项**（纯注册/纯提取），其余 51 项全属写路径·DB 事务 / NLP 语义 / 安全 RBAC / 测试脚本工具，按「宁跳不强推」纪律跳过。

### #121-1 `error_handler.register_error_handlers` (c=11 → 1) — 收口
- 文件：`apps/backend/utils/error_handler.py`（纯 CRLF，改写后 211/211）。
- 改写：9 个嵌套 `@app.errorhandler(X)` handler 提为模块级 `_handle_*` 函数；注册函数瘦身为 10 条顺序注册语句。注册语义 `app.errorhandler(X)(f)` ≡ 原装饰写法；9 个 handler 仅依赖模块级 `make_error_response`/`logger`/`request`/`_record_server_error`，**无闭包依赖 app**，故可安全外提。
- 等价证明：差分 harness（HEAD vs 新版，**共享 stub 类对象**保证注册 key 可按身份比较）→ **10 个注册 key 集合一致 + 逐个 handler 响应逐字节一致**。
- 闸门：py_compile OK · 文件 C901 归零 · 纯 CRLF 保真。

### #121-2 `rule_engine_service.execute_rules` (c=11 → 0) — 收口
- 文件：`apps/backend/services/rule_engine_service.py`（纯 CRLF，改写后 362/362）。
- 改写：抽 `_is_daily_limit_reached` / `_is_min_interval_violated` 两个**模块级纯读** helper（仅 `ScoreRecord.query` 查询，无写）；循环体改为 `if daily_limit and _is_daily_limit_reached(...)` / `if min_interval and _is_min_interval_violated(...)`。helper 置于 `class RuleMatcher:` 之前。
- 等价证明：差分 harness（HEAD vs 新版，打桩 ScoreRecord+FakeColumn/db_session_scope/get_by_id/User/db）→ **6 场景等价**（no_limits/daily_hit/daily_ok/min_hit/min_ok/both）+ 自检（命中场景确实产生 skip）。
- 闸门：py_compile OK · 文件 C901 归零 · 纯 CRLF 保真 · 针对性 pytest **58 passed**（含 `test_execute_rules_*` 三项直测）。

### 本批合计
后端 C901 **55 → 53**（−2）· 默认 **2258 中性** · 全量回归 5 闸门全绿（见下）。

### ⚠️ 本轮环境故障与兜底（重要，已同步 MEMORY.md）
- `bash scripts/run_regression.sh` **被沙箱拒绝执行**：日志仅 `Bash/CallMsi/E_ACCESSDENIED`，`REG_EXIT=1` 且**无任何闸门输出** → 纯环境问题，非代码失败。
- 兜底：PowerShell 直跑脚本内部 5 道闸门 → RBAC EXIT=0（空目录跳过）· OpenAPI EXIT=2（后端未起，非阻塞）· 契约 **2 passed** · 关键路由 **33 passed** · 索引 `[OK] 核心索引全部存在`。与历史基线一致。
- bash 会话 PATH 损坏（`dirname`/`grep`/`tail` not found）→ 后续改用 PowerShell 执行。
- 回归日志含 null 字节（Read/Grep 判二进制）→ `[System.IO.File]::ReadAllBytes` 剔 `\0` 再 UTF8 解码；产出文件用无 BOM UTF8 写。

## C901 战役 — #122（「可安全机械收口」子集收官 2 项）

### #122-1 `mqtt_message_service.handle_mqtt_message` (c=11 → 1) — 收口
- 文件：`apps/backend/services/mqtt_message_service.py`（纯 LF，改写后 932 行）。
- 改写：9 路 `elif` topic 分派链 → **模块级「方法名字符串表」`_MQTT_TOPIC_HANDLERS` + `_dispatch_mqtt_topic(service, topic, data)`（`getattr` 调用）**，块插在 `class MQTTMessageService` 之前。
- 等价性关键：各 topic 纯等值互斥 ⇒ 顺序无关；**`phonebox/unlock` 前缀分支（`==` 或 `startswith`）必须前置为 `if ... return`**，否则 dict 查不到会静默漏派发。
- 等价证明：差分 harness **14/14 + 自检**（query/unlock 精确+带 device_id/heartbeat/points×3/score×3/unknown/空 topic/畸形 JSON/非 dict；用 `sys.modules` 注入假 logging 捕获 warning 文本）。

### #122-2 `websocket_service.register_handlers` (c=12 → 1) — 收口
- 文件：`apps/backend/services/websocket_service.py`（纯 CRLF，改写后 204/204）。
- 改写：**保留嵌套 `def` + `@sio.on(...)` 注册语句原样不动**，仅把 5 个 handler 的分支体抽为模块级 `_ws_handle_connect/_ws_handle_disconnect/_ws_handle_subscribe/_ws_handle_unsubscribe/_ws_handle_ping(svc, ...)`（闭包捕获的 `self` 改显式传 `svc`）。
- ⚠️ 决策规则：**不用 lambda/partial 外提注册对象** —— 会改 `__name__` 与签名，flask-socketio 按签名注入参数，语义会静默变化。
- 等价证明：差分 harness **12/12 + 自检**（connect/disconnect 已订阅·未订阅/subscribe 新·第二房间·空 room·无 room 键/unsubscribe 已订阅·未订阅·空/全流程/ping），比对注册事件集合 + `socketio`/`_handlers_registered` flags + emit 序列 + join/leave 序列 + log + `client_rooms` 终态。

### 本批闸门
py_compile OK · 两文件 C901 全清 · 后端 C901 **53 → 51** · 默认 **2258 中性** · 行尾保真（LF 932 / CRLF 204）· 针对性 pytest **88 passed**（mqtt×4 + websocket）· 全量回归 5 闸门全绿（RBAC 0 / OpenAPI 2 跳过 / 契约 2 passed / 路由 33 passed / 索引 OK）。

### 战役收官结论
「可安全机械收口」子集 **7 个函数全数收口**：#119(2) + #120(1) + #121(2) + #122(2)，C901 **58 → 51**。剩余 **51 项**全属写路径/DB 事务、NLP 语义、安全 RBAC、测试脚本工具 —— 动前须用户逐项拍板。

### 本轮新增踩坑（已并入 skill `c901-cyclomatic-refactor`）
- 函数体内 `import logging` 遮蔽模块级 stub → 须 `sys.modules["logging"]` 注入才能比对 warning 文本。
- PowerShell 工具**默认 2 分钟超时**杀长任务（duration 卡 2m1s、后台输出文件为空）→ 必须显式 `timeout`；`cmd /c` 被安全策略禁用；`Tee-Object` 后台可能整体丢输出。
- 全量 `pytest tests`（2k+ 用例含 NLP/torch）**可跑 20 分钟以上** → 别用默认超时，否则拿到空日志假失败。

### 全量回归实证（用户要求"务必回归"）
- **全量 `pytest tests` 串行：6 failed / 2078 passed / 7 skipped**（22m35s）。
- 6 个失败**全部为预存在**，用 `git stash push -- apps/backend` 回退到 HEAD 后跑同一批文件逐字复现：
  - `test_security_core` + `test_security_utils` :: `test_is_strong_password`（2）
  - `test_regression_20260817` :: `test_ota_failed_statuses_mapped` / `test_export_has_class_scope`（2）
  - `test_query_optimizer` :: `test_invalidate_user_cache` / `test_invalidate_all_cache`（2）
- 另跑 `pytest -n 4` 并行：8 failed / 2076 passed —— 多出的 2 个是 `test_nlp_performance`（warmup 计时/成功率阈值）**并行干扰**，串行下全过 ⇒ **全量勿用 xdist**。
- 结论：**本轮改动零回归**（失败集合 == 基线 6 项，无新增）。已写入 MEMORY.md 长期判据。

### 收口 commit + push（用户"按照建议全部进行推进"授权）
- `cb474b0` `refactor(backend)`: 4 个代码文件（mqtt_message_service / websocket_service / rule_engine_service / error_handler）
- `97b4176` `docs(memory)`: 项目记忆更新
- **已 push**：`8b45381..97b4176 main -> main`（远端 SSH 443 直连成功，无需额外代理配置）；`git ls-remote origin refs/heads/main` = `97b4176`，与本地 HEAD 一致；`git status -sb` = `## main...origin/main`（无 ahead）。

## C901 战役 — #123 试点：`app/service_init.py::init_scheduler` (c=11 → 2) — 收口

剩余 51 项里按「非测试 / 非脚本 / 非 NLP / 非安全」筛选出的最低风险试点（用户「按建议继续推进」授权）。

- 文件：`apps/backend/app/service_init.py`（纯 CRLF，改写后 352/352）。
- 手法（**新确认的可安全收口类别：注册/初始化型函数**）：3 个嵌套闭包（`scheduled_backup` / `scheduled_cleanup_backups` / `scheduled_heartbeat_check`）**保留 def 与 `scheduler.add_job(...)` 注册语句原样**，仅把闭包体逐字搬到模块级 `_scheduled_*_job()` helper（闭包捕获的 `app` 改为显式参数）。收口后仅剩尾部 try/except + `contextlib.suppress`。
- 等价证明：差分 harness **9 场景 + 逐 job 真实执行 + 自检** 全等（观测 jobs/flags/logs/bm/hb/tasks/job_ran）；覆盖 backup 成功·失败·抛异常、cleanup 有删除·无删除·抛异常、heartbeat 有超时·无超时·None·抛异常、tasks.scheduler import 失败（注册数 5 vs 3）。
- harness 两个硬坑：① 闭包体是**函数内 `from x import y`** → 必须 `sys.modules` 注入假模块（含父包 `utils`/`services`/`tasks`）；② `exec` 出的函数 `__globals__` 绑定 exec 时那个 dict，**stub 必须写回同一 dict**（`nsl = dict(ns)` 再注入 → NameError）。
- 闸门：py_compile OK · 文件 C901 全清 · 后端 C901 **51 → 50** · 默认 **2258 中性** · 纯 CRLF 保真 · 针对性 pytest **158 passed**（7 个 service_init 消费方测试）· 回归 5 闸门全绿 · **全量 pytest 分 3 批：568 + 776 + 734 = 2078 passed / 7 skipped / 6 failed，失败集合 == 基线 6 项 ⇒ 零回归**。

### 待办
- 剩余 **49 项 C901**：多数属写路径/DB 事务、NLP 语义、安全 RBAC、测试脚本工具，按「宁跳不强推」纪律，动前需您逐项拍板。
  - **已识别的下一批同类低风险候选**（同为注册/初始化型，可照 #123 手法批量收口）：`service_init.init_cache_warmup`、`redis_cache_service._try_auto_start_redis`(14)、`startup/start_celery.main`(12)。
- 剩余 **51 项 C901**：全属写路径/DB 事务、NLP 语义、安全 RBAC、测试脚本工具，按「宁跳不强推」纪律跳过，需逐项目单独拍板。

## C901 战役 — #124 B档试点：`api/data/export_routes.py::ExportErrors.post` (c=12 → 0) — 收口

用户「先开始1」= 授权按 B 档（写路径试点）推进，首选 `export_routes.post`。实测**比预估更安全**：目标不是数据导出主接口，而是 `/export/errors` 的 `ExportErrors.post` —— **纯 openpyxl 构建 + send_file 下载，零 DB 写、零事务**。

- 文件：`apps/backend/api/data/export_routes.py`（**纯 LF**，改写后 585 行）。
- 改写：装饰器（`@ns_export.doc/response` + `@requires_permission("report.export")`）与 `def post(self):` **原样保留**，仅替换函数体；抽 4 个模块级 helper 追加至文件末尾：
  `_collect_error_data_columns`（汇总并排序 row_data 键）· `_write_error_headers`（表头+Font/PatternFill/Alignment 样式）· `_write_error_rows`（数据行）· `_autosize_worksheet_columns`（列宽估算，含 logger.debug 降级注释）。
  openpyxl 的 `Workbook/Font/PatternFill/Alignment` **仍在函数内 import 并以形参传给 helper**（不提到模块级，避免改启动期 import 行为）。
- 等价证明：差分 harness **10/10 + SELF_CHECK_OK**（观测：单元格值+字体/填充/对齐 repr、表头顺序、列宽、wb.save 次数、send_file 参数（时间戳正则归一）、logger.debug 次数、异常形态）。用例覆盖 无错误/无 errors 键/单条/多条混合含缺 row_data·row_data 非 dict·缺 row/缺 module/中文键排序/超长值截断/空 row_data。

### ⚠️ harness 连中 3 次「假等价」——全靠自检拦下（重要方法论）
1. `ns["datetime"] = datetime` 注入了**模块**而非类（源码是 `from datetime import datetime`）→ 两版一起 `AttributeError: module 'datetime' has no attribute 'now'`，10/10 是**空转等价**。改为 `datetime.datetime`。
2. `slice_method` 取 `next(def post(self...)` → 切到文件里 **line 92 的另一个 post**，两版跑同一无关函数。改为**按函数体 marker（`from openpyxl import Workbook`）选取**，并断言新版源码含 helper 名。
3. `column_dimensions` 用普通 dict → 真实 openpyxl 是**按需自动创建** Dimension，两版一起 `KeyError: 'A'`，绕过列宽对比。改为 `collections.defaultdict(FakeDim)` 且快照取 `.width`。
> 判据：**差分 harness 必须先自检「真实路径确实被走到」（返回值/写入数/表头/save 次数），否则 10/10 无意义**。本轮若无自检会直接交付一个未被真正验证的改写。
- 另一坑：`norm()` 把 tuple 归一为 list，断言须用 list 形态。

### 闸门
py_compile OK · 文件 C901 全清（该文件的 c=12 唯一项归零）· 后端 C901 **50 → 49** · 默认 **2258 中性** · 纯 LF 保真（585）· 针对性 pytest **70 passed**（export_routes / export_service / import_export_routes / filter_import_export_e2e）· 回归 5 闸门全绿（RBAC 0 / OpenAPI 2 跳过 / 契约 2 passed / 路由 33 passed / 索引 OK）· **全量 pytest 分 3 批：568 + 776 + 734 = 2078 passed / 7 skipped / 6 failed，失败集合 == 基线 6 项 ⇒ 零回归**。

## C901 战役 — #125 B档续：`api/academics/exam_import_routes.py::DownloadTemplate.get` (c=13 → 0) — 收口

B 档（写路径/导出类）同构试点，与 #124 同类：目标 `exam_import_routes.DownloadTemplate.get` —— **纯「查询 + openpyxl 构建 + send_file」下载，零 DB 写、零事务**。

- 文件：`apps/backend/api/academics/exam_import_routes.py`（**纯 LF**，改写后 510 行）。
- 改写：装饰器（`@ns_exam_import.route("/template")` + `@requires_permission("score.entry")`）与 `def get(self):` 原样保留，仅瘦身；抽 2 个模块级 helper 追加至**文件末尾**：`_build_notes_sheet(sheet_notes, exam)`（说明表+考试信息）· `_build_data_sheet(sheet_data, students, subjects)`（成绩导入表+表头样式+列宽+数据验证+冻结+筛选+条件格式）。
- 等价证明：差分 harness（备份原版 vs 工作区新版+helpers，打桩 FakeWB/FakeSheet/FakeUser + 假 `openpyxl`、`flask.send_file`）**5/5 + SELF_CHECK_OK**（用例：无考试/无班级/无学生、考试无时间/2学生、考试带时间/班级/1学生、考试带时间/无学生、无考试/班级/3学生3科；自检：数据表/说明表存在、`as_attachment`、mimetype、表头 `["学号","姓名","班级","科目","分数","满分","备注"]`、数据行数=len(students)*len(subjects)、考试信息行、cf 规则=2、validation=1、freeze="A2"）。

### ⚠️ 重大新失效模式：装饰器捕获陷阱（结构性，等价 harness 覆盖不到）
- 初版把 helper 插在 `DownloadTemplate` 类**之前**（`prefix + helper + mid + ...`）→ 实测 `test_get_import_history` **404**（其余 9 通过），stash 实证 clean 文件 pass、我的改动 fail。
- URL map dump 确认：clean=5 路由全注册；我的版本**仅 /validate /preview /execute 3 个，/template 与 /history 双双缺失**。
- `probe_routes.py` 铁证：`@ns_exam_import.route("/template")` 被绑到插入的 helper `def` 而非 `DownloadTemplate`（Variant D 插入空 dummy `def` 同样 404 ⇒ 结构性）。
- **根因**：flask-restx 路由类上方的 `@ns_X.route(...)` 装饰器会绑到紧邻其前的模块级 `def`；等价 harness 只证函数体等价，**完全覆盖不到模块级装饰器绑定**这一结构性问题。
- **修复**：helper 改置**模块最末尾**（其后无 `@ns.route` 装饰器），即 `out = prefix + mid + new_get + tail + [""] + helper + [""]`。修复后全量验证全绿。
- **判据更新**：路由文件抽取后，等价 harness 即便 N/N 通过也**不能**证明路由注册正确 —— 必须追加 **URL map dump / monkeypatch `Namespace.add_resource` 打印注册 / 针对性 pytest（含该路由测试）** 三重兜底。

### 闸门
py_compile OK · 文件 C901 仅剩 `post`(c=12)（`get` 归零）· 后端 C901 **49 → 48** · 默认 **2258 中性** · 纯 LF 保真（510）· 针对性 pytest `test_exam_import_routes.py` **7 passed**（含此前 404 的 `test_get_import_history`）· URL map 确认 5 路由全注册 · **全量 3 批回归：568 + 776 + 734 = 2078 passed / 7 skipped / 6 failed，失败集合 == 预存在 6 项（test_security_core / test_security_utils::test_is_strong_password、test_query_optimizer::test_invalidate_user_cache/test_invalidate_all_cache、test_regression_20260817::test_ota_failed_statuses_mapped/test_export_has_class_scope）⇒ 零回归**。
- commit `b0c32cc` → 已 push 至 origin/main（SSH over 443，`git ls-remote` 核实 `b0c32cc...` == 本地 HEAD）。
- skill `c901-cyclomatic-refactor` 已补「装饰器捕获陷阱」铁律与探测手法（修正原 #6「插在 class 前」写法）。
- 临时 `tests/test_tmp_urlmap.py` 已删除（commit 前清理）。
- ⚠️ 工具链坑（本轮复现）：① PowerShell `Set-Content -Encoding utf8` 写 **BOM** → 首行文件名 `tests/ï»¿...` 找不到；② Python 文本模式写把 `\n`→`\r\n`；③ bash 路径里 `\a` 被转义成告警符；④ 批跑清单 `cat|tr` 须 `tr -d '\r'`。三批均需 `tr -d '\r'`。

## C901 战役 — #126 安全子集收口（2 项基础设施型，48→46）— 完成

收口 2 个已验证安全的「基础设施/启动」型函数（非写路径/DB 事务/NLP/安全）：
- `services/redis_cache_service.py::_try_auto_start_redis` (c=14→<10)：抽取 `_open_redis_log(self, log_path)` 与 `_wait_redis_ready(self, host, port, db, timeout)` 两个私有方法，原函数退化为薄编排器（保留 global 锁、早返回、spawn_kwargs 分支、Popen try/except/finally 关闭 logf、超时探测）。
- `startup/start_celery.py::main` (c=12→<10)：抽取 `_build_parser()` 与 `_launch_processes(args)` 两个模块级 helper。

手法同 #119–#125：二进制安全改写保行尾（redis 保 LF / start_celery 保 **CRLF**，禁 Edit 直改 CRLF），差分等价 harness 证明零行为变更。

harness 要点（本轮新坑已规避）：
- start_celery harness：argparse/start_worker/start_beat/sys.exit/time.sleep 全 stub；场景 worker+beat / none 双用例，断言 start_worker 调用参、sys.exit(1)+print_help 分支一致。
- redis harness：subprocess.Popen/redis.from_url/time/os/open/logger 全 stub；`_auto_start_attempted` global + `_auto_start_lock` fake；5 场景（nonlocal/noexe/ok/popenfail/timeout）全 SAME；`time` stub 须返回 [0,0,1000] 否则 while 探测循环首检即退出致 seen_true 失败（已修）。
- 类方法 harness 拼接坑：block 已是 4 空格基准，**直接**拼到 `class _H:` 下即可，勿再 `+ "    "` 前缀（会把方法嵌进前置 `_resolve` 方法体内）。

闸门：py_compile OK · EOL（redis LF 727 / start_celery CRLF 135 保真）· 目标文件 C901 各归零 · 后端 C901 **48→46** · 默认 **2258 中性** · 两 harness SELF_CHECK_OK+EQUIV_OK · **全量 3 批回归 568+776+734 = 2078 passed / 7 skipped / 6 failed，失败集合 == 预存在 6 项（test_query_optimizer×2、test_regression_20260817×2、test_security_core、test_security_utils）⇒ 零回归**。
- commit `fbce10f` → push `origin/main`（`git ls-remote` 核实 `fbce10f...` == 本地 HEAD）。

### 剩余 C901 判决
48→46 后**剩 46 项全部为高风险**（写路径/DB 事务 users/post×3·execute_subject_import·import_devices·resolve_relations·execute_score_import；NLP 语义 nlp_enhanced_service×9；安全 configure_rate_limits/requires_permission；测试夹具 conftest.app 改全崩）。**均无安全机械收口空间，按"宁跳不强推"整体跳过，待逐项拍板**。一次性维护脚本（create_missing_indexes/verify_ota_e2e.main/migrate_*.main 等）改动价值低、不在请求路径，亦不收口。

### B3 / 公共组件 扫描结论（用户要求 123 全推进）
- **B3 已成熟**：`to_dict(fields=None)` 已覆盖 User/ScoreRecord/Device/System/Alert/Notify/Class/Subject 等高频实体；仅 16 个低频二级模型（activity/attendance/committee/culture/duty/homework/mental_health/nlp_models/notification_config/parent/seating/study_group/study_guide/teacher_comment/archive）缺 to_dict。**继续扩展 ROI 低**（极少内联序列化），建议判 B3 实质完成。
- **公共组件已成熟**：前端 `useListData`/`useClientFilter`/`useListFetch`、后端 `safe_handle`、`formatDateTime`（仅 `utils/format.ts` 单点定义，无重复）**均已存在并广泛使用**。剩余仅是 api/ 下 103 处裸 `except Exception` 可逐步套 `safe_handle`（已由 `route-try-except-to-decorator` skill 覆盖，增量推进）。


## 预存在 6 项 pytest 失败清零（2026-09-12，commit 67f6fd3）

对长期基线里的 6 个预存在失败逐项定性并修复 → **全量回归 2078 passed/7 skipped/6 failed ⇒ 2084 passed/7 skipped/0 failed（失败清零，零回归）**。

### 真实代码 bug（2 类，命中 4 项测试）
- `utils/security.py::is_strong_password`：末行 `return re.search(r"\d", password)` 返回 **`re.Match` 而非 `bool`**（签名与 docstring 均声明 bool）。→ 改 `return ... is not None`。调用方 `admins_routes.py:190/425` 均 `not is_strong_password(...)` 仅用真值，**行为逐字一致**。（命中 `test_security_core` + `test_security_utils` 两处）
- `utils/query_optimizer.py::CachedQueries.invalidate_user_cache / invalidate_all_cache`：`for k in self.cache_manager:` —— `CacheManager` **未实现 `__iter__`** → `TypeError: 'CacheManager' object is not iterable`。→ 改 `for k in list(self.cache_manager.keys())`（`.keys()` 返回 `list(self._store.keys())` 快照，**遍历中删除安全**）。
  - ⚠️ 若写 `for k in self.cache_manager.keys():` 会触发 ruff **SIM118**（把 CacheManager 误当 dict 建议直接迭代）；而**直接 `for k in cm:` 会在删除时抛 `RuntimeError: dict changed size during iteration`**。正解 = `list(cm.keys())`（快照 + 无告警）。仓库无 noqa 风格，不用 noqa。

### 重构滞后测试（2 项：C901 抽取致源码文本断言失效）
- `test_ota_failed_statuses_mapped`：失败码表已下沉为**类常量** `MQTTManager._OTA_FAILURE_STATUSES`（C901 services 批次 **6db27da**），测试仍 `inspect.getsource(_process_ota_status)` 后断言字符串。→ 升级为**对象级断言** `code in MQTTManager._OTA_FAILURE_STATUSES` + 校验方法源码引用该常量。
- `test_export_has_class_scope`：`_admin_scope` 已下沉到 `_export_users_data/_export_devices_data/_export_records_data` 三个 helper，`ExportData.post` 仅分派。→ 升级为 **helper 级断言**（逐个 `inspect.getsource(helper)`）+ 校验 `post` 确实分派到三个 helper。
- **判据（通用）**：`git log -S "<被搬走的字符串>" -- <src>` vs `git log -- <test>` 比对先后；源码字符串移位提交晚于测试最后改动 ⇒ 重构未同步测试，非功能回归。
- **正解原则**：把文本断言**升级**为对象级/helper 级断言（更强，能防「表存在却未被用」「helper 存在却未被调用」），**不是**把逻辑塞回原方法去迁就测试。

### 闸门
针对性 4 文件 **86 passed**（原 80/6）· 全局 default lint **2258 中性** · C901 **46 不变** · EOL 保真（`security.py`/`query_optimizer.py` **CRLF** 二进制安全改写；测试 LF）· 全量 3 批 **568+776+740 = 2084 passed / 7 skipped / 0 failed** · commit `67f6fd3` 已 push（`git ls-remote` 核实 remote==local==`67f6fd3`）。
- skill `c901-cyclomatic-refactor` 已补 **§9「重构后必须复扫源码文本类测试」** + SIM118 误报判据。
- ⚠️ **回归基线口径已更新**：今后判回归「失败项必须为 0」，不再是「失败集合 ⊆ 6 项」。

## 特性任务调研 + 优化方案（2026-09-12）

产出 `docs/特性任务优化方案-20260912.md`。方法=实测取证（Grep/Read/只读查库），不采信旧文档快照。

### 关键纠正（旧文档已过时，勿再引用为待办）
- `docs/下一步开发计划-20260824.md` 的 T7/T8/T10/T11 **实测均已闭环**：T7 = `requirements.txt:53-54` 已把 `torch/transformers/xgboost/lightgbm/shap` 移至 `requirements-ml.txt`、`catboost/gensim` 移除；T8 = requirements 无 `xlrd/xlwt`、package.json 无 `web-vitals`；T10 = package.json 无 `react-app`；T11 = 4 页 `return null` 计数 = 0。
- `docs/班主任工作台优化方案-待审核.md` 的 P3 A 批（P3-2/3/4/5/6/7）**实测均已闭环**：`CurrentClassLabel` 已覆 13 页；`StatCard` 10 页 / `EmptyState` 16 页 / `LoadingSpinner` 11 页；`pages/SeatingChart.tsx:49/54` 已 `useWorkbenchClass` + `getAll(filterClassId)`。P3-1（PageHeader 抽取，**组件尚不存在**）与 P3-8（总览卡补全）未做。

### 唯一实证真实缺口：OTA 多设备类型隔离（拟主推 F1）
- `models/device_models.py:135-148` `FirmwareVersion` **无 `device_type` 维度**；`version` 为全局 unique。
- `services/ota_negotiation_service.py:94-102` `get_latest_active_firmware()` 取全局最新 active，**不按类型过滤**；`negotiate()` 与 `/api/firmware/ota/check`（firmware_routes.py:208-237）、`negotiate_all_devices` 同病。
- 但 `Device.device_type`（device_models.py:81）已存在且注册链路已落库（`mqtt_manager.py:430/448-449`）→ 类型信息已有，固件侧未消费。
- 后果：doorlock 设备一旦注册即被判"需升级"→ `ota_status=pending` → 下发 **phonebox 固件**；若门铃订阅 OTA 会刷错固件。
- `apps/firmware/esp32/doorlock/doorlock.ino:975-993` 心跳有 `fw_version` 无 `device_type`，全文无 `TOPIC_OTA`/`verifyOtaSignature`/`ota/register` → doorlock 未接 OTA。
- 窗口期最佳：dev 库 `device`=0 行、`firmware_versions`=0 行，**无存量迁移风险**。
- 5 阶段方案（A 模型+B 协商+C 路由+D 固件+E 前端）；唯一契约面变更 = `/ota/check`、`/versions` 可能新增 `device_type` 键 → 须 G5 `--update` 快照并备份，或保持响应键集不变（待拍板）。
- 备选 F2 = T12 巨型页拆分（AlgorithmAnalysis ~3938 / NLPManagement ~2835 / RemoteNotify ~2273，纯 churn，建议暂缓）；F3 = doorlock 接 OTA（依赖 F1 A–C）。

### 工具链复用
- 只读查库用 `sqlite3.connect("file:...?mode=ro", uri=True)`；注意表名是 **`device`**（单数）非 `devices`。
- `&&` 链在 grep 无命中（exit 1）时会截断后续 → 复核实测一律用 `;` 分隔并 `|| echo NO_XXX`。

## 优化批次：safe_handle 收敛 7 处路由手写 try/except（#128）

用户指示「OTA 先延期，继续其他优化」。经实测复核，代码质量 backlog 已枯竭（B3/公共组件/F17/C901安全子集/预存在6失败 均已闭环），唯一仍有真实增量的方向是 **`api/` 下裸 `except Exception` 逐步收敛到 `@safe_handle`** → 本轮执行第一批。

### 收敛面（7 处 / 5 文件）
| 文件 | 方法 | 原兜底 | 状态码 |
|---|---|---|---|
| `api/academics/exam_import_routes.py` | ValidateImportFile.post | 验证失败 | 500 |
| 同上 | PreviewImportData.post | 预览失败 | 500 |
| 同上 | ExecuteImport.post | 导入失败 | 500 |
| `api/academics/import_routes.py` | ImportTemplate.get | 生成模板失败 | 500 |
| `api/academics/subject_routes.py` | SubjectOrder.put | 操作失败，请稍后重试 | **400**（原 `APIResponse.error` 默认） |
| `api/scores/rules_routes.py` | ApplyRuleTemplate.post | 应用模板失败 | 500 |
| `api/data/download_routes.py` | download_score_template 内嵌 generate_template | 生成模板失败 | 500 |

### 硬拒项（逐条定性，不清算）
- **server_error 组 12 处**（export_routes×6 / devices_routes×2 / import_export_routes×4）：`APIResponse.server_error` 展开含 `error_code="INTERNAL_ERROR"`，`safe_handle` 不透传 `error_code` → 收敛必丢字段。若要动，须先给公共装饰器加 `error_code` 透传参数（**独立评审**）。
- **handler 带副作用 2 处**：`firmware_routes.post`（`os.remove(file_path)` 清理暂存）、`scheduled_notify_routes.post`（`rollback_scheduled_session()`）→ 业务性 try。
- **动态文案泄露 2 处**：`phonebox_policy_routes.post`（`f"一键放行失败: {e}"`）、`report_routes.get`（`f"生成报表失败: {exc}"`）→ 属有意契约变更，需批准。
- **体量 >100 行 1 处**：`devices_routes.get`（117 行）。
- **审计/通知「只 log 不 return」7 处**：`classes_routes._audit_class_op`、`nlp_routes.save_context_memory`、`records_routes._notify_score_change`、`admins/rbac/sub_accounts::log_permission_action` 等 → 语义是「失败不阻断主流程」，收敛会改成返回错误信封 → 必错。

### 零漂移前置核验（缺一不可）
1. 原 `APIResponse.error` 默认 **status_code=400**（`utils/response.py:22`）→ `subject_routes.put` 必须 `default_status=400`；其余原写死 500 → `default_status=500`。
2. 调用链无 HTTP 异常：7 处 try body + `excel_service/academics_service/subject_service/score_rule_service/score_import_helper/excel_utils` 全文 grep `abort(|_or_404|HTTPException` = **clean**（404 语义均在路由层 try 之外）。
3. `utils/error_handler.py` 的 `APIError/NotFoundError/...` 带 `status_code`/`error_code`，但**仅 tests 引用、产线未 raise** → 装饰器的 `getattr(e,'status_code')` 透传分支不触发。
4. 装饰序：`@safe_handle` 恒为**最内层**（`@ns.doc` / `@requires_permission` 在外）。
5. EOL：`exam_import_routes`/`rules_routes` 为 **LF**；`import_routes`/`subject_routes`/`download_routes` 为 **CRLF** → 全走二进制安全改写（逐行保 EOL）。

### 闸门（全绿）
py_compile 5/5 OK · ruff 5 文件 14 err（vs 备份 16，**净 −2**：BLE001/G201 各 −1，新增 F401 已清）· 全局 default lint **2258 → 2256** · 全局 C901 **46 不变** · EOL 全保真 + 尾字节与备份一致 + ≥3 连空行数与基线一致（`rules_routes.py` 的 1 处 3 连空行为**基线既有**）· 装饰器最内层 0 违规 · 7 处 `@safe_handle` · 残留 `except Exception` = 0 · 针对性 pytest 4 文件 **47 passed** · 全量 3 批回归 **2084 passed / 7 skipped / 0 failed**。

### 工具链新坑（已入 skill `route-try-except-to-decorator`）
1. **一阶 AST 判据严重漏检**：严格档「docstring 后仅一条 Try」命中 8 项（真可收敛仅 1）；放宽为「Try 是 body **最后一条语句** + pre 属安全类（请求解析/纯计算/早退 return）」后命中 **26 项**。pre 安全时异常行为等价（早退非 raise；`get_json()` 坏 JSON 抛 werkzeug BadRequest=HTTPException，装饰器 re-raise 交 Flask，与原一致）。
2. **import 锚点必须限定缩进 0**：`line.strip()==anchor` 会命中**函数体内懒加载**的同名 import（`download_routes.py:26` 缩进 4），把列 0 的新 import 插进函数体中段 → `IndentationError`。
3. **删 handler 致 F401**：仅在该 handler 用过的 import（`APIResponse`）变未用 → 必须 `ruff --select F401` 清掉。
4. **EOL 往返公式**：`dom.join(raw.split(dom)) == raw` 恒成立，勿再 `+ dom`（多补换行致误报）；文件尾可能**本就 `\n\n`** → 闸门改为**与备份尾字节比对**，不强求「单 EOL 结尾」。

## #129 safe_handle 加 error_code 透传 + server_error 组收敛（4 处）

按「推荐 A」执行：给公共装饰器 `utils/decorators.py::safe_handle` 加**尾置默认参** `error_code=None` 并透传
（`APIResponse.error(..., error_code=error_code)`），从而解锁原「硬拒」的 `server_error` 组。
既有 **163 处** `@safe_handle(` 不传该参 ⇒ 响应体键集逐字不变（`APIResponse.error` 内 `if error_code:` 才写键）。

### 收敛 4 处（可证明零漂移）
| 方法 | message |
|---|---|
| `api/data/export_routes.py::ExportData.post` | 导出失败，请稍后重试或联系管理员 |
| `api/devices/devices_routes.py::DeviceImport.post` | 设备操作失败，请稍后重试 |
| `api/devices/devices_routes.py::DeviceExport.get` | 设备操作失败，请稍后重试（**128 行、严格形态**：Try 是 body 唯一语句 → 无 pre 漂移）|
| `api/data/import_export_routes.py::DeleteBackup.delete` | 删除失败 |

统一写法 `@safe_handle(message="<原文案>", default_status=500, error_code="INTERNAL_ERROR")`，恒为**最内层**
（`@ns_*.doc` / `@requires_permission` 在外层）。

### 硬拒 6 处（契约可见差异，待用户拍板）
`export_routes` 的 ExportUsers / ExportRules / ExportDevices / ExportRecords / ExportCategories / ExportSummary
六个 `get` —— **pre 语句含 DB 调用**（`User.query.all()` / `ScoreRule.query.all()` / `_admin_scope()` / `.count()`）。
- 装饰器包**整个方法** → pre 也被纳入捕获；而原实现 pre 在 try **之外**。
- pre 抛非 HTTP 异常时：原 → 全局 `register_errorhandler(Exception)` → `server_error(str(e))`（message=**str(e)**）；
  装饰器 → **固定文案**。status/code/error_code/键集全同，**仅 message 文本不同** → 属契约可见，
  按「宁跳不强推」排除（**勿自行放宽，先问**）。
- pre 抛 HTTPException 时两者一致（都向外冒泡 / re-raise），非阻塞项。

### 另 9 处 grep 命中但非可收敛形态
`devices_routes.py:90/121/798`（函数体内**无 try**，inline if-return）；
`import_export_routes.py::_do_import_users` 与两个 `post`（handler 返回 `data={"errors":[str(e)]}` 自定义 payload，
装饰器无法复现）。

### 零漂移核验（本轮新判据）
- 4 处 try body 与被调 service 链 `grep -E "abort\(|_or_404|HTTPException"` = **clean**
  （装饰器 re-raise HTTPException 而原 `except` 会吞 → 必须无此风险）；
- 调用链仅 `raise ImportError/ValueError`（内置，**无 `.code`/`.status_code`**）→ 装饰器 `getattr`
  透传分支不触发，仍得 `code=-1` / `status=500`；
- **机制级微证明**（新手法，比逐端点 harness 便宜且更强）：`@safe_handle(message=..., default_status=500,
  error_code="INTERNAL_ERROR")` 产出信封 vs `APIResponse.server_error(same)` **逐键相同**（除 `timestamp`/`path`）；
  不传 `error_code` 时**无该键**；`HTTPException` 仍 re-raise。

### 闸门
py_compile 3/3 · EOL 保真（`export` LF / `import_export` LF / `devices` **CRLF**，逐行二进制安全改写，
尾字节与备份一致）· 装饰器最内层 0 违规 · 函数体内无 Try 残留 · `@safe_handle(` **163 → 167** ·
三文件 ruff 统计 new == bak（27/27，净中性）· 全局 default lint **2256 不变** · C901 **46 不变** ·
针对性 pytest `test_decorators` + `test_export_routes` + `test_devices_routes` + `test_import_export_routes`
+ `test_regression_20260817` = **63 passed** ·
**全量 3 批回归 568 + 776 + 740 = 2084 passed / 7 skipped / 0 failed（零回归）**。
- commit `03ea4b3` → push origin/main（`git ls-remote` 核实远端 == 本地 HEAD）。
- skill `route-try-except-to-decorator` 已更新：`error_code` 解锁配方 + 「pre 含 DB 调用」判据（三类特殊形态 #4）
  + §1 前置核验第 6 条 + 踩坑 #10（公共装饰器加参的零影响证明）+ 验证闸门新增「机制级等价微证明」。

## safe_handle 收敛 — #130 `api/data/export_routes.py` 6 处 GET 导出端点

用户批准上轮「推荐 A 的延伸」—— 即接受「500 错误路径 message 由全局兜底 `str(e)` 改为固定文案」的契约可见差异（status / code / error_code / 键集全不变，与项目既有「不直返异常细节」策略一致）。

- 文件：`apps/backend/api/data/export_routes.py`（**纯 LF**，改写后 564 行 / 23781 字节）。
- 收敛 6 处：`ExportUsers.get` / `ExportRules.get` / `ExportDevices.get` / `ExportRecords.get` / `ExportCategories.get` / `ExportSummary.get`，统一 `@safe_handle(message="导出失败", default_status=500, error_code="INTERNAL_ERROR")`（恒最内层）。原 handler 均为 `logger.exception("导出失败")` + `return APIResponse.server_error(message="导出失败")`。
- 手法：AST 定位（Try 为 body 末句）→ 删 try 行 → body dedent 4 → 删 except+handler → def 前插装饰器；从后往前处理避免行号偏移；二进制安全改写保 LF。
- 闸门：py_compile · EOL(LF) + 尾字节 + ≥3 连空行与备份一致 · 6 处装饰器最内层 0 违规 · 函数体内无 Try 残留 · `@safe_handle` 167→**173** · 本文件 ruff 统计 new==bak（14/14 净中性）· 全局 default lint **2256 不变** · C901 **46 不变** · `api/` 裸 `except Exception` 103→**86**（累计 −17）· 针对性 pytest **32 passed** · **全量 3 批 568+776+740 = 2084 passed / 7 skipped / 0 failed**。
- 机制级微证明：装饰器信封 vs `APIResponse.server_error(message="导出失败")` 逐键相同（除 timestamp/path）；早退 `return APIResponse.bad_request`（400）未被改写。
- 提交：`f9dbb7d`（代码）+ `c1af622`（docs 入库），已 push（`git ls-remote` 核实 `c1af622` == 本地 HEAD）。
- 文档入库：`docs/特性任务优化方案-20260912.md`（上轮交付物，149 行）。

### 剩余面精扫结论（下一批依据）
AST 精扫 `api/` 45 个候选（判据：Try 为 body 唯一条/末条 + handler 单一 `Exception`）：
- **真实可收敛仅 2 处**：
  - `mqtt_routes.MQTTConnect.post` —— handler `APIResponse.error(message="MQTT connection failed", status_code=500)` → `default_status=500`；⚠️ 方法内另有**内层 try 降级**（payload 解析失败沿用 DB 配置），收敛外层时内层须原样保留。
  - `notify_template_routes.TemplateUse.post` —— `APIResponse.error(message="发送失败")` **未写 `status_code` → 默认 400** → 须显式 `default_status=400`（`safe_handle` 默认 500）。
- 硬拒分布：LOGONLY 21（审计/通知「只 log 不 return」）、CUSTOM 9（`data={"errors":[str(e)]}` 自定义 payload）、OTHER(try-not-last) 17、SIDEFFECT 2（`os.remove` / `rollback_scheduled_session`）、DYNMSG 2。

### 工具链坑（本轮新增）
- `ast.unparse` 把字符串字面量规范化成**单引号** → 闸门断言勿用带双引号的整串（`assert 'error_code="INTERNAL_ERROR"' in d` 必失败），须拆成「关键词 in 串」。
- f-string 内不能含反斜杠（Py3.11）→ 本轮连踩 3 次（bash heredoc + 脚本 print），一律改 `%` 格式化或预计算变量。
- AST 扫描：`ast.Try` 在「`try/finally`（无 except）」时 `handlers` 为空 → 访问 `handlers[0]` 前必须先判 `len(h.handlers) != 1`。
- 扫描判据补充：判「handler 能否被装饰器复现」时必须检查 `data=` 关键字参数（自定义 payload 无法复现），只看 `message` 会严重高估可收敛面（本轮 5 → 2）。

## #131 safe_handle 线收官 + T12 巨型页拆分方案立档

用户指令：「要（收口剩余 2 处），然后给出下一步计划，我建议 T12 巨型页拆分，OTA 相关继续延期」。

### #131 收口 `mqtt_routes.MQTTConnect.post`
- 文件 `apps/backend/api/monitoring/mqtt_routes.py`（**纯 CRLF**，438 行 → 432 行，二进制安全改写）。
- 形态：`body == [logger.info(...), Try]`，整方法体被单一 try 包裹，handler `APIResponse.error(message="MQTT connection failed", status_code=500)`。
- 收敛为 `@safe_handle(default_status=500, message="MQTT connection failed")`（**刻意不传 `error_code`**，因原兜底无该键 → 响应键集恒等），置于 `@requires_permission` 之后 = 最内层。方法内**内层 try（payload 解析降级）原样保留**。
- 零漂移证明：`grep -n "raise \|status_code\|\.code" services/mqtt_manager.py` **全空** → 装饰器 `getattr` 恒取默认值；机制级微证明 `MECH_PROOF_OK`（信封逐键同 / 无 `error_code` / HTTPException re-raise / 正常返回透传）。
- 闸门：py_compile・pyflakes 0 ｜ EOL 全 CRLF(432/432) ｜ 尾字节与备份一致 ｜ `@safe_handle` 3→4 ｜ 残留（`MQTT connection failed: {type(e).__name__}` / `logger.error("%s: %s", "MQTT connection failed"`）清零 ｜ ruff 同目录对照 HEAD 版 **3 → 2（G201 −1）**。

### 二阶 AST 收官扫描（13 项候选逐项分类）——仅 1 项可收敛
12 项硬拒：裸 dict 兜底 **5**（`remote_notify_routes` 的 5 个 post 返回非信封字典）、handler 带 `data=` **2**（`import_export_routes.ImportRules/ImportCategories`，`if data:` 取值敏感且装饰器无 data 透传口）、pre 含副作用 **1**（`firmware_routes.FirmwareUpload.post` 的 `ensure_upload_folder()` 建目录）、pre 含 DB＋f-string 泄漏 **2**（`phonebox_policy_routes`、`report_routes`）、非整方法体 try **1**、pre 含 DB＋HTTPException **1**（`scheduled_notify_routes.ScheduledTrigger`）。
- ⚠️ **判据升级（改判）**：`notify_template_routes.TemplateUse.post` 上一轮被列为「可收敛」；本轮按技能判据复核后**改判硬拒** —— try 只包尾部 publish+落库（业务性 try）、pre 含 `NotifyTemplate.query.get_or_404(id)` 与 `_resolve_class_from_device`/`ClassTimeChecker` 等 DB/service 调用，装饰器包整个函数会令 pre 段异常从「全局 errorhandler 500 + str(e)」变为「400 + 固定文案」→ **status 与 message 双漂移**。

### 闸门与提交
- 后端全量 3 批：**568 + 776 + 740 = 2084 passed / 7 skipped / 0 failed**（与基线逐项一致，零回归）。
- 针对性 pytest（`test_mqtt_routes` / `test_mqtt_service` / `test_mqtt_management_service` / `test_mqtt_message_service`）**64 passed**。
- ruff 全域：默认 **2259**（2260→2259，本文件净 −1）／C901 **46 不变**。
- 前端四闸门（T12 起跑基线）：tsc 0 ／ eslint 0 ／ prettier 0 ／ vitest **37 passed + 1 skipped（38 文件）= 276 passed / 3 skipped**。
- ⚠️ 工具链纠错：prettier 二进制实际是 `node_modules/prettier/bin-prettier.js`（写成 `node_modules/bin-prettier.js` 会 MODULE_NOT_FOUND）；已同步修正 MEMORY.md。
- skill `route-try-except-to-decorator` 已更新：硬拒三类（裸 dict 兜底 / handler 带 `data=`·`errors=` / pre 含副作用）＋ 特殊形态 #5（pre 仅日志的严格形态，MQTTConnect 实例）与 #6（非整方法体 try，TemplateUse 实例）＋ 新增「收官扫描」小节（逐项分类表＝收官凭证）。

### T12 方案交付
- `docs/T12-巨型页拆分方案-20260912.md`：判据＝**单文件行数**（非目录总行数）；`pages/` 下 >600 行 **24** 个、≥850 行 **9** 个（P0）；沿用既有「装配层 + 实现目录」范式；三拆法 A（Panel 切）/B（View 区块切）/C（god hook 按域切）；10 批计划；四闸门；不拆清单。
- 关键实测：`pages/*/index.*` **不存在**，入口壳靠 `App.tsx` 的 `createLazyComponent(() => import('./pages/Xxx'))` 解析 → 拆分**不得改入口路径**，原 Sections 文件宜退化为 re-export 兼容层。
