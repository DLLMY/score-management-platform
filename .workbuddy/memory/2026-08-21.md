# 2026-08-21 工作日志

## 班主任工作台全量修复（#813–#816）—— 完成

### #813 工作台级共享班级（useWorkbenchClass）
- 新建 `frontend/src/hooks/useWorkbenchClass.ts`：模块级 store + `useSyncExternalStore`（React 18），
  跨 12 个兄弟路由持久；sessionStorage 落盘（刷新不丢，不跨标签页）。0 = 全部班级。
  导出 `useWorkbenchClass / getWorkbenchClassId / setWorkbenchClassId / ALL_CLASSES`，hooks/index.ts 已导出。
  选模块 store 而非 Context：工作台页是 App.tsx `<Routes>` 兄弟节点，无共同父组件，避免改 App.tsx 包 Provider。
- 8 个页面接入共享 `filterClassId`（视图筛选）+ 本地 `selectedClassId`（弹窗表单）双态：
  已修复页 CultureBoard/ActivityManage/StudyGuide 本地 state → 共享 hook；
  新增视图筛选：DutyRoster（服务端 class_id 过滤 + 统计收敛到可见组）、CommitteeList、
  HomeworkCheck、AttendanceManage（getStats 也带 class_id）、StudyGroups、ParentContact、MentalHealth。
  新建弹窗默认带入当前筛选班级；空态文案区分"该班级暂无…"。
- DutyRoster 修 `defaultDutyForm.class_id` 硬编码 1 → 0（由 ClassSelect 自动默认），并显示 student_name（后端已返回，前端类型补字段）。

### #814 后端班主任班级数据隔离（隐私）
- `parent_service.list_contacts`：非 admin/super_admin join User 按 `User.class_info_id.in_(allowed_ids)` 收敛；`class_id` 参数按学生所属班级过滤；`list_contact_logs`、`get_contact`（403 越权）同样隔离。
- `mental_health_service.list_records / list_alerts`：同口径隔离 + class_id 过滤。
- **关键坑**：隔离分支与 class_id 分支各自 join(User) → SQL 重复 JOIN user → SQLite `ambiguous column name: user.class_info_id`。已合并为单次 join（is_scoped or class_id → join 一次，再分别加 filter）。
- 路由透传 class_id（parent_routes / mental_health_routes）；**G5 红线**：class_id 仅加 doc params（不新增响应字段）→ OpenAPI 464 零漂移。mental_health_routes 的 Namespace 显式 `path="/mental-health"`（与 api_versioning add_namespace path 一致），修复 conftest 动态注册 URL 变下划线的问题。
- 前端 api.ts：parent.getAll(studentId?, classId?)、mentalHealth.getRecords(studentId?, classId?)、getAlerts(studentId?, isResolved?, classId?)。
- 新建 `tests/test_workbench_isolation_smoke.py`（6 passed）：admin 全量/class_id 过滤、teacher 只可见关联班级（parent/mental/alert）、单查越权 403。

### #815 工作台总览页
- 新建 `frontend/src/pages/WorkbenchOverview.tsx`：登录落地页。顶部班级筛选（共享 hook）+ 刷新；
  今日概览指标（出勤率/待完成作业/未处理预警/小组/活动/值日组/迟到请假，Promise.allSettled 各路独立容错）；
  12 子功能入口卡片（按权限过滤）。
- App.tsx 注册 `/workbench` 路由（class.view 守卫）+ lazy import；Sidebar teacherWorkbench 组首位加"工作台总览"（LayoutDashboard 图标，import 已补）。
- **登录默认落地页**：原 `/` index 固定 Dashboard（score.view，班主任无权限会落"权限不足"页）→ 新增 `HomeRedirect`：有 class.view → `/workbench`，否则 `/dashboard`。

### #816 DataTable 迁移评估结论
- 保留卡片式：DutyRoster（组+任务两级）、StudyGroups（组+成员）、ParentContact（可展开日志）均为嵌套数据形态，
  拍平到 DataTable 会损失信息层级，属过度设计；本轮已完成这些页的筛选与隔离（原缺陷根因）。
- CommitteeList/HomeworkCheck/AttendanceManage/MentalHealth(records) 已用 DataTable；MentalHealth alerts 卡片保留。

### 验证
- 前端：tsc --noEmit ✅、eslint ✅（全部改动零告警）。
- 后端：py_compile ✅；G2 RBAC 68/68 ✅；G5 OpenAPI 464 零漂移 ✅（重启后复验）；`tests/test_workbench_isolation_smoke.py` 6/6 ✅；`scripts/run_regression.sh` 全绿（关键路由/索引/契约）✅。
- 真实后端冒烟：admin token 调 `/api/parent/contacts?class_id=1`、`/api/mental-health/records?class_id=1` 均 200（dev 后端加载 .env 的 JWT_SECRET_KEY，非 .env.development）。
- 后端已强杀重启（Flask-SocketIO 不 reload），当前 5000 在线。

### 备注
- run.py 只 load_dotenv(basedir/.env)，`--env development` 不切换 .env.development（与直觉相反）；外部脚本签 token 须用 .env 的 key。

## 班主任工作台全面评估（2026-08-21 晚）
- 核验方法：swagger 464 路径 vs 前端 api.ts 155 条 URL 机器比对（工作台相关 41 条）。
- **P0**：学法指导-改进计划 编辑/删除 404 —— 后端无 `/study-guide/plans/<id>` PUT/DELETE（仅 /plans 列表/创建 + /plans/{id}/progress），service 无 update_plan/delete_plan。
- **P1**：① 评语管理全项目缺失（前端/后端/模型均无）；② 值日轮转 `POST /api/duty/rotate` 后端完整但页面未接；③ 班委任期 `GET/POST /api/committee/terms` 后端完整但页面未接。
- **P2/P3**：总览页缺班级管理/学生/成绩/通知入口卡；空考勤显示 0%（应"—"）；/parent/logs 的 parent_id 参数未登记 OpenAPI doc。
- 报告存 `docs/班主任工作台评估报告-20260821.md`。

## 评估问题全量修复（2026-08-21 深夜，P0–P3 全部落地）
- **P0 改进计划 404**：`study_guide_service` 补 `update_plan/delete_plan/_ensure_plan_access`（越权 403 + 进度防负收敛 [0,100]）；`study_guide_routes` 补 `/plans/<int:plan_id>` PUT/DELETE；`update_plan_progress` 同步加隔离+防负。Namespace 显式 `path="/study-guide"`。
- **P1 值日轮转**：api.ts 补 `duty.rotate`，DutyRoster 加"轮转值日"按钮（需先选班级 + 确认弹窗）。
- **P1 班委任期**：api.ts 补 `committee.getTerms/createTerm`（types 加 CommitteeTermCreateInput），CommitteeList 加"任期管理"弹窗（列表+新建+设当前任期）。
- **P1 评语管理（全栈新增）**：模型 `models/teacher_comment.py`（TeacherComment，student_id/term/comment_type/rating/content）；服务 `services/teacher_comment_service.py`（班级隔离 join User + 越权 403，口径同 parent/mental）；路由 `/api/teacher-comments` GET/POST、/{id} GET/PUT/DELETE；seed_rbac 加 `comment.view/comment.edit`（admin+teacher）；G2 --apply 补齐（DB 70 条/teacher 30）；前端 TeacherComments.tsx（DataTable+弹窗+共享班级筛选+搜索+星级评分）、App.tsx 路由、Sidebar 入口、总览入口卡。
- **P2 总览入口**：WorkbenchOverview 加"常用全局模块"（学生/班级/成绩/通知，按权限过滤）。
- **P3**：总览出勤率/迟到请假在无考勤记录时显示"—"；parent/logs 补 OpenAPI doc 参数。
- **验证**：前端 tsc/eslint/vite build 全绿；G2 一致性 OK；G5 快照更新为 **467 路径**零漂移（3 新增端点：plans/{id}、teacher-comments 两段）；pytest 12/12（隔离+评语 CRUD+计划 CRUD）；回归闸门全绿；真实后端冒烟（teacher-comments 200、plans/999 404"计划不存在"正确）。后端已强杀重启。
- 新表 `teacher_comment` 需在目标环境建表：dev 重启自动 create_all（SQLite）；生产需迁移（同 models 新增表的既有流程）。

## 班主任工作台「再次评估」复评（2026-08-21 深夜，实测核验）
- 用户要求不采信前报告结论、重新实测复评。方法：逐行读后端路由+service（teacher_comment/study_guide/committee/duty/mental_health/parent 6 个 module）+ 前端 api.ts/useWorkbenchClass.ts/13 页面 + 跑闸门 + 对运行后端(5000)真实探测。
- 实测结论全部绿灯：G2 RBAC 70/teacher 30 一致；G5 OpenAPI **467 路径零漂移**；pytest 工作台隔离 **12/12**；前端 tsc/eslint/vite build 全绿。
- 实时探测 10 条关键路由（无认证）全部 **401（非 404）**，含 P0 的 `PUT/DELETE /api/study-guide/plans/999` → 401，证明资源+方法已真实注册（P0 闭环）。
- 隐私隔离逐行确认：teacher_comment/study_guide(计划)/parent/mental_health 四模块均为"合并单次 JOIN User.class_info_id"，跨班 403。
- 观察（非缺陷）：SeatingChart 用本地 ClassSelect（图表级 class_id），未接全局共享班级 Hook，属设计差异。
- 报告：`docs/班主任工作台复评报告-20260821.md`（功能完整/前后端对接/数据流转对比表）。
