# 2026-09-08 工作日志

## 薄路由战役 · 第 6 批（rules，已落地，全量回归后台 BeEGVl）
- **背景**：用户选「继续推进其他低风险项」。全量盘点发现除 nlp/system 高风险文件外，仍有大量低风险厚 GET 候选（data/import_export、reports、scores/rules、student、scores/approvals、academics/exam_import 等）。
- **剔除项**：`exam_import_routes.DownloadTemplate.get`(175行) 是 openpyxl Excel 生成+send_file（文档生成非查询序列化，按「高风险提取宁可跳过」跳过）；`report_routes.ClassSemesterReport.get`(103行) 含 S3 安全权限校验+Excel 生成（安全敏感+文件生成，跳过）；`rules_routes.RuleExport.get`(CSV生成)/`RuleTemplate.get`(静态)/`RuleResource.get`(已薄) 均不动。
- **范围收窄（铁律：写/MQTT/安全敏感/文件生成零触碰）**：`api/scores/rules_routes.py`(LF 691) 仅下沉 2 个纯只读 GET 到新建 `services/score_rule_query_service.py`：
  - `RuleList.get`(35行, 过滤+分页+手动缓存+序列化) → `get_rule_list_view(page,per_page,category_id,is_active)`；is_active 原始字符串在 view 内做 `(is_active.lower()=="true")` 判定，契约逐字一致。
  - `RuleStatistics.get`(63行, 按 rule_id 聚合+详情关联+排序+总计) → `get_rule_statistics_view()`；返回 {statistics, summary} 结构不变。
- **零漂移要点**：`@cached_api(ttl=30)` + 手动缓存(ttl=300,tags=["rules"]) 双缓存层**全留路由层**，view 只返回裸 data dict；缓存键 `rules_list:{page}:{per_page}:{category_id}:{is_active}` 与写回行为与原实现一致；`@requires_permission` 装饰器未动。
- **清理**：`RULE_LIST_FIELDS`/`RULE_STAT_FIELDS` 常量随逻辑迁入 service（仅此 2 个 GET 用，已 grep 确认）；`RULE_CREATE_FIELDS` 留路由（POST 仍用）；`from models import db` 因视图下沉后 routes 不再用 db 而删除（ScoreRule/ScoreCategory/get_by_id 写端点仍用，保留）。
- **验证**：双文件 py_compile 全绿；routes 内 `RULE_LIST_FIELDS`/`RULE_STAT_FIELDS` 残留=0；定向 `test_rules_routes.py` = **17 passed**；路由 691→603 行（−88）。
- **铁律守门**：未自动 commit/push。全量回归 BeEGVl 已启动但**日志文件损坏（197609字节却 wc -l 报 0 行，疑似会话重载占位损坏），无法采信**；改为后续合并回归背书。

## 薄路由战役 · 第 7 批（approvals，已落地，合并回归 fbBhz3 后台）
- **背景**：用户「继续推进下一个低风险文件」。先评估 student_routes（GET 已全薄壳委托 analysis/engagement/risk service，try/except 隔离语义刻意，跳过）→ 选 approvals_routes 为低风险候选。
- **范围收窄**：`api/scores/approvals_routes.py`(LF 539) 下沉 3 个纯只读 GET 到新建 `services/approval_query_service.py`：
  - `ApprovalList.get`(45行) → `get_approval_list_view(page,per_page,status)`；
  - `PendingApprovals.get`(14行) → `get_pending_approvals_view(page,per_page)`；
  - `ApprovalResource.get`(24行) → 保留 `can_access_student` 403 校验在路由，`get_approval_detail_view(id)` 做 joinedload+get_or_404（404 优先），`_serialize_approval` 序列化。
  - 3 处重复的审批序列化字典收敛为单一 `_serialize_approval(detail=False/True)`；原模块级 helper `_apply_approval_data_isolation`（仅 2 处 GET 用）随查询迁入 service。
- **零漂移要点**：`paginate` 沿用原默认 `error_out=True`（不传参，保持 404 行为）；`@cached_api(ttl=30)` + `@requires_permission` 留路由层；列表 12 字段 / 待审 10 字段 / 详情 12 字段契约逐字一致。
- **清理**：删除路由层 `joinedload`/`get_current_admin`/`get_allowed_classes` import（仅隔离 helper 用），`User`/`can_access_student`/`request`/`datetime` 写路径仍用保留；`from services.approval_service` import 块保留（写路径用）。
- **验证**：双文件 py_compile 全绿；routes 内 4 个符号残留=0；定向 `test_approvals_routes.py` = **21 passed**；路由 539→437 行（−102）。
- **铁律守门**：未自动 commit/push。

## 全量回归（合并，fbBhz3 后台运行）
- 覆盖前六批 + rules + approvals + 8 僵尸删除。金标准零漂移背书。完成后自动通知。
- 注：BeEGVl（rules 单独回归）日志损坏未采信，本次合并回归一并验证 rules 批次。
- **scheduled_notify 批次在 fbBhz3 运行期间落地（CRLF 二进制改写 + service 追加）**：fbBhz3 启动时已收集旧版，故它只背书前七批；scheduled_notify 的零漂移由紧随其后的新全量回归（fbBhz3 后）兜底。

## 薄路由战役 · 第 8 批（scheduled_notify，已落地，待最终回归）
- **背景**：fbBhz3 运行期间用户连续「继续推进」。评估 exam_import_routes（仅 ImportHistory.get 纯读、DownloadTemplate 是 Excel 生成跳过，价值小）→ 选 scheduled_notify_routes 为更优候选（2 个纯读 GET + 已 import 同 service）。
- **范围**：`api/scores/scheduled_notify_routes.py`(CRLF 342) 下沉 `ScheduledList.get`(41行)/`ScheduledDetail.get`(26行) 到 `services/scheduled_notify_service.py`（LF，原文件追加 `_serialize_scheduled`/`get_scheduled_list_view`/`get_scheduled_detail_view`）。两处逐字相同的 21 字段序列化字典收敛为单一 `_serialize_scheduled`。
- **零漂移要点**：列表 GET 原 `APIResponse.success({items,total,...})` → 路由 `return APIResponse.success(get_scheduled_list_view(...))`；详情 GET 原**裸 dict 无 APIResponse 包裹** → 路由 `return _serialize_scheduled(get_scheduled_detail_view(id))`，契约逐字一致；`@requires_permission` 装饰器保留；`get_or_404` 保留在 service 的 detail view。
- **改写法**：CRLF 文件用二进制脚本按类名定位 `def get` 替换（装饰器保留），写后归一纯 CRLF（285 CRLF / 0 lone LF）。
- **验证**：双文件 py_compile 全绿；routes 内 `repeat_day_of_week` 内联序列化残留=0；定向 `test_scheduled_notify_routes.py` = **2 passed**；路由 342→285 行（−57）。
- **铁律守门**：未自动 commit/push。

## 薄路由战役累计（均未 commit/push，按铁律待用户确认）
| 批次 | 改动 | 定向测试 | 全量回归 |
|---|---|---|---|
| course_schedule | +service / routes 1435→1078(CRLF) | 12 ✅ | 背书 ✅ |
| exam | +service / LF routes | 37 ✅ | 背书 ✅ |
| subject | +service / CRLF routes | 20 ✅ | 背书 ✅ |
| users | +service / UserList.get 166→30 | 40 ✅ | 背书 ✅ |
| devices | +service / CRLF routes 1461→1255 | 27 ✅ | 背书 ✅ |
| rules | +service / LF routes 691→603 | 17 ✅ | **fbBhz3 合并背书** |
| approvals | +service / LF routes 539→437 | 21 ✅ | **fbBhz3 合并背书** |
| scheduled_notify | +service / CRLF routes 342→285 | 2 ✅ | **fbBhz3 后补跑** |
| + 删除 8 个僵尸备份 | | | 背书 ✅ |
| **合计定向** | | **176 ✅** | **0 真实失败** |

## 低风险候选盘点（待续）
- 已确认低风险 LF 纯读 GET：scores/approvals_routes(46/39行)、student/student_routes(63行)、data/import_export_routes(72/56/44行)、academics/exam_import.ImportHistory(48行)、scores/scheduled_notify(41行)、scores/rules 已做。
- 中风险（CRLF/需谨慎）：monitoring/operation_logs(76/58行,CRLF)、system/security_routes(61/54/44/41行,CRLF,T6 fail-closed 安全敏感)、users/rbac_routes(49行,CRLF,RBAC敏感)、devices/firmware_routes(53/46/44行,LF)。
- 明确跳过：nlp_routes(NLP核心)、system_routes(线程死锁)、各类 Excel/CSV 生成端点、含 S3 安全权限校验的 report_routes。
- flaky 类型库（4 类，均凭「隔离必过+未触碰相关代码」定性，非回归）：clear_cache 顺序依赖 / nlp_performance×2 负载敏感 / correction_update_status 顺序依赖 / system_routes 线程死锁。

## ⚠️ 并发回归争用事故与清场（关键）
- **现象**：fbBhz3（合并 rules+approvals+前6批+僵尸，8:08 启动）跑 24min+ 仍无结果，远超历史 15-17min。进程诊断发现**两组共 4 个 pytest 进程并发**：
  - PID 1808(venv)+23036(系统3.11)= 8:01 启动 = **BeEGVl 残留**（rules 单独回归，日志损坏但进程实际未死，占 CPU 拖慢全局）
  - PID 35588(venv)+6596(系统3.11)= 8:08 启动 = **fbBhz3**
- **根因**：BeEGVl 日志损坏被误判为「已结束」，实际进程仍在后台跑全量回归，与 fbBhz3 并发争用 CPU → 双慢 + flaky 放大。
- **处置**：PowerShell `Stop-Process -Id 1808,23036,35588,6596 -Force` 全杀；`Get-Process python` 确认清零。
- **重启单一干净回归**：`ycfcwF` = `.venv/Scripts/python.exe -m pytest -p no:locust --timeout=600 -q -p no:cacheprovider`（覆盖**全部8批+rules/approvals/scheduled_notify 首次合并背书**），单一进程无并发，后台运行，预计 15-17min。
- **工作树清单已核对（18 改动 + 8 删除）**：
  - 8× M routes（course_schedule/exam/subject/devices/scheduled_notify 为 CRLF；users/rules/approvals 为 LF）
  - 1× M service（scheduled_notify_service.py 追加）
  - 7× 新建 service（course_schedule/exam/subject/user_list/device_query/score_rule_query/approval_query）
  - 8× D 僵尸备份（refactor_F17/*）
- **下一步（用户「按照建议操作」确认）**：ycfcwF 零回归后执行 **commit（不 push）检查点**，按 `git ls-remote` 取远端真值复核差额。

## ✅ 最终全量回归 ycfcwF 零回归 + commit 检查点完成
- **ycfcwF 结果（单一进程无并发，21m15s）**：`2078 passed, 7 skipped, 0 failed`（对比基线 2076/2，连 nlp_performance×2 负载敏感 flaky 也稳定通过）。**8 批薄路由 + 8 僵尸删除 = 零真实失败，全量绿灯**。
- **远端复核**：`git ls-remote origin refs/heads/main` = `8625185`，本地 HEAD 同值，差额 0（本地与远端本就同步；工作树为未提交改动）。
- **commit（不 push）**：`0c67670` — `refactor(routes): 薄路由只读查询下沉 8 批 + 删除 8 僵尸备份`，26 files changed, +1791/−7663。提交后工作树干净，本地领先 origin/main 1（按铁律未 push）。pre-commit 钩子正常通过。
- 用户「继续完成优化」→ 战役收口决策点：低风险纯读大块已全部覆盖，剩余候选风险/收益比不划算（见下）。

## 薄路由战役收口盘点（待用户拍板后续）
- **已收口（8 批，零回归）**：course_schedule/exam/subject/users/devices/rules/approvals/scheduled_notify + 8 僵尸删除。定向合计 176 passed。
- **剩余候选与评级**：
  - 低风险但价值小：academics/exam_import.ImportHistory（LF 48行，单 GET，其余 GET 是 Excel 生成跳过）——收益不足以支撑一轮全量回归。
  - 中风险（CRLF/敏感，需用户拍板）：monitoring/operation_logs、system/security_routes（T6 fail-closed 安全敏感）、users/rbac_routes（RBAC 敏感）、devices/firmware_routes。
  - 铁律跳过：nlp_routes（NLP 核心）、system_routes（线程死锁）、report_routes（S3+Excel）、所有 Excel/CSV 生成端点。
- **建议**：薄路由战役在此自然收口；后续可 ① push 当前检查点 ② 转其他优化线（前端格式化/命名整治、F17 写路径迁移、公共组件提取）③ 仅当用户显式拍板才动中风险敏感文件。

## ✅ push 检查点 + 薄路由收口尾批（exam_import.ImportHistory）
- **push（用户「push」）**：本地领先 origin/main 1（commit `0c67670` 薄路由 8 批检查点 + 未提交工作日志 `842891f`）。GitHub 直连可达（`ssh.github.com:443` 通，`git ls-remote` 取真值 `8625185`，无需本地代理），强杀/代理均未触发。`git push origin main` 成功 → `8625185..842891f main -> main`，origin/main 现 `842891f`，工作树与远端同步（无 ahead）。
- **收口尾批（用户「继续完成其他你内容」）**：落地此前 flagged「低风险价值小」的 `exam_import.ImportHistory.get`：
  - 新建 `services/exam_import_query_service.py`（LF，与路由及兄弟 service 一致）：`get_import_history_view(exam_id,page,per_page)` 逐字搬运原 40 行读路径（Score 分页 + User/Exam/Admin/subject_rel 序列化，契约 `{"data","total","page","per_page","pages"}` 不变）。
  - `api/academics/exam_import_routes.py`（**LF**，Edit 直改安全）：ImportHistory.get 40 行 → 4 行薄壳；同文件 145 行 `DownloadTemplate.get` 为 openpyxl Excel 生成+send_file，按铁律冻结不动。
  - 新增 `test_exam_import_routes.py::test_get_import_history`（制造 entered_by 记录后 GET /history 校验契约结构 + ≥1 行），锁定下沉零漂移。
  - **验证**：双文件 py_compile 全绿；定向 `test_exam_import_routes.py` = **7 passed**（6 旧 + 1 新）；routes 542→505 行（−37）。未自动 commit（铁律）。
- **⚠️ EOL 检测更正（关键，纠正本日志前文）**：本日志前文的 CRLF/LF 判定基于 `grep -c $'\r'`，该命令在 Git Bash 下 `$'\r'` 展开异常导致**误判为全 CRLF**。用 Python 字节计数（`b.count(b"\r\n")` vs `b.count(b"\n")`）复核真实 EOL：
  - **实为 LF（此前误判 CRLF）**：exam_import_routes / firmware_routes / remote_notify_routes / 兄弟 service（device_query_service 等）。
  - **确为 CRLF**：course_schedule_routes / device_group_routes / admins_routes / mqtt_routes。
  - **MIX**：classes_routes（349 CRLF + 1 lone LF）。
  - 结论：后续薄路由批次若再启，须按 Python 字节计数判定 EOL，LF 文件可安全 Edit 直改，CRLF 文件仍走二进制改写，MIX 文件禁用 Edit。

## 薄路由战役最终状态
- **已收口（9 批含尾批，定向合计 176+7=183 passed）**：course_schedule/exam/subject/users/devices/rules/approvals/scheduled_notify + exam_import.ImportHistory + 8 僵尸删除。**全部零漂移**。
- **真正穷尽**：其余未做 GET 均为 ① 已薄壳委托 service（exam_routes/admins GET 等）② Excel/CSV 生成端点（冻结）③ 高风险敏感（system/security_routes T6 fail-closed、users/rbac_routes RBAC、monitoring/operation_logs，需用户显式拍板）④ nlp/system/report 铁律跳过。
- **下一步（转「其他优化线」）**：战役已自然收口。可推进的锁定决策（此前已批准）：① 命名 outliers 整治 ② 前后端格式化（后端 black 26.5.1 / 前端 prettier+eslint，Node 20）③ 根目录按域深度重组 ④ 公共组件提取（前端 useListData/useClientFilter/useListFetch 等）。四项均为大面改动，按铁律需用户确认范围后再启，避免巨量 diff 与并发回归争用。

## 下一步选型：用户选「前端公共组件提取」→ 实质已收口
- **盘点结论**：该线（useListData / useClientFilter / useListFetch / formatDateTime）已实质完成：
  - `useListFetch` 被 25 个页面采用；`useListData`/`useClientFilter` 已用于多页；`src/utils/format.ts`（F1 公共组件提取 2026-08-22）集中 formatDateTime/formatDate/formatRelativeTime/formatUptime/formatFileSize/formatHourMinute，**18 页采用**。
  - 仅剩 4 个页面仍直连 `fetchJson`：OpsCenter / SecurityAudit / SystemMetrics + FrontendTelemetry 残留。前三均为 dashboard / 多资源并行拉取（health+performance+mqtt+stats 等）或单点 stats/metrics，非「服务端分页列表」语义 → 按 A 轨 SOP「❌ 真命令式跳过，不在本专项强推」**正确跳过**。
  - 其余含 useEffect 的页面（ScoreRecords/ExamManagement/DeviceManagement/PermissionManagement 等）均为「多独立资源 + 多 useState」CRUD/看板，强迁会高回归风险，跳过。
  - 内联 `toLocale*` 仅 11 文件、~13 处，多为特殊场景（文件名/input value）或语义差异（前导零、秒位），归一改 display 属决策②格式化范畴，不建议在「提取」线强做。
- **结论**：前端公共组件提取线自然收口，无安全拟合的剩余提取项。

## 后端格式化评估（决策② 候选，待拍板）
- **工具**：`.venv` 内 black 26.5.1（CPython 3.11.7 编译版）；项目 `.flake8` 设 `max-line-length=120`，故 black 用 `--line-length 120`。
- **read-only 干跑（`black --check --line-length 120 backend`）**：**884/1205 文件将被重排（73%），321 不变**。即后端近乎从未 black 格式化。
- **风险（铁律视角）**：① 巨量 diff（884 文件）远超「小批可审阅」原则；② black 在 Windows 下可能归一 EOL（CRLF→LF），触发此前 CRLF 噪音陷阱与未来 Edit 全文件 diff；③ 需全量 pytest（15–21min）背书零回归。
- **待用户拍板**：全量一次应用 / 按模块分批小步（每批独立 pytest）/ 暂不做、改做命名 outliers 整治（决策①，更小更安全）。

## ✅ 后端 black 格式化 · 按模块分批落地（决策② 收口）
- **用户选型**：「按模块分批应用」。
- **⚠️ 配置更正（纠正上段）**：项目 `.flake8` 的 `max-line-length=120` 仅作用于 flake8；真正的 `pyproject.toml [tool.black]` 为 `line-length=100` + `skip-string-normalization=false` + `target-version=['py311']`。故实际用项目默认配置（line-length 100，单→双引号规范化），未用 120 覆盖。
- **关键结论（消除最大担忧）**：black **按文件保留 EOL**——CRLF 文件格式化后仍 CRLF，LF 仍 LF。两个 pilot 验证（celery_app CRLF 不变 / filters LF 不变 / 4 个 utils CRLF 文件重排后仍 CRLF）。
- **MIX 文件处理（20 个，含 1 个 bare-LF 多数）**：先按「多数行尾」归一再做 black。
  - 正确多数判定：`bare_lf = lf - crlf`；`crlf > bare_lf` → CRLF，否则 LF。**切勿用 `crlf > lf`**（lf 内含 CRLF 的 \n，会误判）。
  - 19 个为「多数 CRLF + 1~4 stray LF」→ 归一 CRLF；1 个 `tests/test_approvals_routes.py` 为 312 CRLF + 118 bare-LF → 仍 CRLF（crlf 312 > bare_lf 118）。
  - 归一后全后端 **MIX 文件 20→0**。
- **6 批落地（每批 py_compile 全绿 + HEAD-vs-工作区 EOL 逐文件验证，0 意外翻转）**：
  | 批次 | 范围 | reformatted/总 | 意图内 MIX→clean |
  |---|---|---|---|
  | B1 | utils | 9/36 | 0 |
  | B2 | models+app | 5/29 | 0 |
  | B3 | config/di/middleware/startup/tasks/scripts/tools | 15/ | 5 |
  | B4 | services | 40/99 | 11 |
  | B5 | api | 23/83 | 3 |
  | B6 | tests | 17/167 | 1 |
- **验证**：全后端 `py_compile` 全绿；EOL 逐文件比对（每批）0 个被改动行尾；MIX 归零。纯格式调整，逻辑未变。
- **提交（不 push，铁律）**：`41b9a9e`(exam_import 收口) + `9bf1a71`/`4bb384c`/`3ea0265`/`284708c`/`439ede9`/`79196aa`(6 格式批)。本地领先 origin/main 7。
- **零回归闸门**：全量 pytest 后台运行中（task `BCpH5d`，系统3.11，`-p no:locust --timeout=600 -q`），基线 2066 passed/7 skipped，完成后自动通知。

## 决策① 命名 outliers 整治 · 实证盘点 + 首个 outlier 收口（2026-09-08 续）
- **先实证再动手（铁律：字段调整须用户审核）**：重新实跑扫描，纠正报告陈旧：
  - 后端应用代码 camelCase 标识符 outliers：**0**（排除 .venv/tests 后完全干净，black+既有纪律已覆盖）。
  - P0-5（前端 `read`/`is_read` 双轨）：实测 `is_read` 已全量统一（api.ts:1625/1774/2324、types/index.ts:242 均 `is_read`；`/read` 仅 HTTP 动作路径）——该项已自洽，报告陈旧。
  - F12（裸信封）：窄模式仅命中 5 文件少量；报告「algorithm/exam/devices/firmware/nlp 共百余处」明显夸大/口径不同 → 属大批量契约归一，非 outlier。
- **唯一清晰「真正命名 outlier」（单端点偏离全局约定）**：`remote_notify` 预览端点（`api/scores/remote_notify_routes.py:160`）发 `last_seen`，而全局 Device API 用 `last_heartbeat`。前端 `SendForm.tsx:598` + 类型 `api.ts:1672` 照此消费。测试字段白名单 `tests/data_sync_validation.py:257` 也列 `last_seen`。
  - 修复（4 处，零逻辑变更）：后端键名→`last_heartbeat`；前端类型/组件同步；测试白名单同步。`data_sync_validation.py` 为 CRLF，走二进制改写保留 EOL（558=558）。
  - 验证：前后端 `last_seen` 残留 0；`py_compile` OK；pytest 19 passed（remote_notify_class_time + data_sync_service）；前端 `tsc --noEmit` OK。
  - 提交 `46ac76d`（领先 origin/main 9，未 push）。
- **剩余范围判定（待用户审核，按铁律不能擅自批量动）**：报告 F-table/P0-P2 中，符合「仅修真正 outliers」且低风险纯重命名的已基本清零；其余均属：
  - 结构合并/数据迁移（F5/F9/F10/F11 已做迁移；P0-4 审批合并、P0-6 预警合并、§4 服务合并）——明确排除。
  - 契约级引用/枚举统一（F4 `class_info_id`、F8 RBAC、F13 路由版本化、P0-1 `student_id/user_id`、P0-2 `subject_id`、P0-3 RBAC 双源清理）——触碰 API 契约，须逐条确认。
  - 大批量信封归一（F12）——机械但触及返回形状。
  - 纯命名零风险（P2-7 误导性路由类名 `wol_routes`/`admin_notifications_routes`/`DeviceAlerts`）——低价值，可做可不做。
- **下一步**：向用户确认决策①是否在此收口（outliers 已清），或继续契约级项（F4/F8/F12/F13/P0-1/2/3）需逐条拍板。

## 决策② 零回归闸门 + 收口推送（2026-09-08 终）
- **首轮全量回归（task BCpH5d，EXIT=1）**：尾部 1 个 `F`（`test_system_routes.py`），但隔离复跑该文件 **14 passed** + black 语义惰性 → 判定为长跑资源争用 flaky；且清理阶段触发批量删除安全拦截，汇总行被截断，计数不可信。
- **干净全量重跑（task 6XG9w1，junit-xml 落盘）**：**tests=2086 / failures=3 / errors=0 / skipped=7**。基线 2066 passed → 现 2076 passed（多 10 来自新增测试），skipped 同 7。
  - 3 失败全部为已知负载敏感 flaky NLP 测试：`test_nlp_performance::test_nlp_warmup_improves_first_call`、`test_nlp_parse_success_rate`、`test_nlp_routes::test_correction_update_status`。
  - **隔离复跑 3× 全部 PASS** → 确认是 load/timing flaky，**非我的改动所致**（black 仅空白/引号；last_seen 改名在 remote_notify_routes，非 NLP）。errors=0。
- **决策①按用户选择收口**；决策②零回归闸门以「2076 passed / 0 真实失败 / 3 已知 flaky」结论收口。
- **推送（铁律：用户已确认 push）**：先 `git ls-remote` 核实 origin/main=842891f 与本地一致；清理本地回归产物 3 文件；`git push origin main` → `842891f..4505986 main -> main`。
- 当前 `origin/main`=4505986，本地 0 ahead，工作树干净。10 个 commit 已落地（exam_import 尾批 + 6 批 black + last_seen outlier + 2 日志）。
- **结论**：决策②（后端 black 分批）+ 决策①（命名 outliers 实证收口）全部完成并推送。剩余报告项均为结构合并/契约级，非「仅修 outliers」范畴，留待后续逐条确认。

## 决策③ 根目录按域深度重组（2026-09-08 续）
- **先出迁移计划**：调研全仓库路径引用断点地图（docker-compose / Dockerfile / ci.yml / deploy.ps1|*.py / .gitignore / README / DEVELOPMENT_GUIDELINES 均硬引用 backend/frontend/infra/deploy 当前扁平布局）。
- **关键约束发现**：`api-docs/` 不是纯文档目录——被钉死在 G5 OpenAPI 契约闸门：`backend/scripts/verify_openapi_contract.py:24 DEFAULT_SNAPSHOT`、`backend/tools/project_evaluation.py:353`、`scripts/run_regression.sh:55`，迁移必须同步改这 3 处否则 G5 真验崩。
- **用户选 Phase 1 + Phase 2**：
  - **Phase 1（零风险）**：`reports/`→`docs/reports/`（23 份）、2 份根清理报告 md→`docs/cleanup/`、删 2 个陈旧 `t4_*.log`（未跟踪磁盘残留）、README 树/链接更新。已提交。
  - **Phase 2（中风险·含 G5 闸门）**：`api-docs/`→`docs/api/`（openapi.json+备份+swagger/redoc 全保留）；改 3 处代码引用（verify_openapi_contract / project_evaluation / run_regression.sh）+ ci.yml 注释、.gitignore(`api-docs/*.bak_*`→`docs/api/*.bak_*`)、RELEASE_GUIDE/upgrade_execution_log/PROJECT_TERTIARY/API_DOCUMENTATION 文档引用。验证：py_compile 两 .py OK、bash -n run_regression.sh OK、功能路径 api-docs 残留 0（仅 .workbuddy 历史 memory 日志保留）。已提交。
- **待办（未做，按铁律需确认）**：
  - Phase 2 的 **G5 实时真验未跑**（需起后端 5000 + MQTT 连通；变动仅路径字符串，已 py_compile 背书，建议下次跑 `bash scripts/run_regression.sh` 顺带覆盖）。
  - **可选 deploy+infra 折叠**（compose+infra+deploy 收进 infra/，中风险）——用户选项里标「可选」，本批未做。
  - **Phase 3 高风险（backend/frontend/firmware→apps/）**——需重写 compose/Dockerfile/CI/3 部署脚本/.gitignore/README/DEVELOPMENT_GUIDELINES 全部路径，用户显式确认才做。
- 当前本地领先 origin/main 2（Phase1+Phase2），**未推送**（铁律：推前需用户确认；且 G5 实时真验建议补跑）。

## 决策③ Phase B（deploy+infra 折叠）+ Phase 3（apps 收编）· 已落地未提交（2026-09-08 续）
- **Phase B（deploy+infra→ops/）**：`git mv deploy ops/deploy`、`git mv infra ops/infra`。修复：
  - `docker-compose.yml`：`dockerfile: ops/infra/Dockerfile`、`./apps/backend/.env` 挂载(3 处)、`./apps/frontend/build`、`./apps/frontend/nginx.conf`；`docker-compose.mqtt.yml`：`./ops/infra/mosquitto/mosquitto.conf`。
  - `ops/infra/Dockerfile`：4 处 `COPY frontend/`→`COPY apps/frontend/`、`COPY backend/`→`COPY apps/backend/`（目的 `./backend/` 容器内部不动，保 gunicorn `backend.wsgi` 可解析）。
  - `ops/deploy/*.ps1`：`$projectDir` 改为 `Parent.Parent`(repo 根)、`Join-Path ... "apps/backend"|"apps/frontend"`。
  - `ops/deploy/check_deploy.py`：`project_dir=dirname(dirname(script_dir))` 回升 repo 根；config_files 元组 `apps/backend/.env.example`/`apps/frontend/...`/`ops/deploy/one_click_deploy.py` 等。
  - `ops/deploy/one_click_deploy.py`：同上 + `backend_dir=join(project_dir,'apps','backend')`。
  - `ops/deploy/start_server.bat`、`service_manager.py`、各 `*.md`/`*.bat`：令牌替换 `backend/`→`apps/backend/` 等（二进制安全）。
- **Phase 3（backend/frontend/firmware→apps/）**：`git mv backend apps/backend`、`frontend apps/frontend`、`firmware apps/firmware`。修复：
  - `pytest.ini`：`testpaths = apps/backend/tests`；`scripts/run_regression.sh`：verify_rbac/verify_openapi 路径→`apps/backend/scripts/`、`cd $ROOT/apps/backend`、`cd $ROOT/apps/frontend`；`scripts/p2_5_print_to_logger.py`：`"backend/`→`"apps/backend/`。
  - `.github/workflows/ci.yml`+`deploy.yml`：`working-directory: apps/backend|apps/frontend`、requirements/package-lock 缓存路径同步。
  - `apps/backend/tools/project_evaluation.py`：PROJECT_ROOT 由 3 层 dirname→4 层(repo 根)，BACKEND_DIR/FRONTEND_DIR 用 `("apps","backend"/"frontend")`；`generate_api_docs.py`：output 多一层 dirname 回到 repo 根。
  - `redis_cache_service.py` 容器内 `backend/redis`（原本就不存在，非本次引入，未动）。`tests/data_sync_validation.py:64 "../frontend/..."` 相对路径随兄弟结构保留在 apps/ 下仍成立（已确认 apps/frontend/src/types/index.ts 存在）。
- **EOL 守门（铁律）**：全部走 Python 字节级改写（读→replace→写回字节），**零 EOL 翻转**；git 显示 rename（R），历史保留。
- **验证**：① 全仓 grep `(?<!apps/)(?<!ops/)(backend|frontend|infra|deploy)/` 过滤 `/app/backend/` 后**为空**（无残留旧路径）；② 改动 .py 全 `py_compile` 绿；③ `pytest --collect-only`（apps/backend）**2086 tests collected** exit 0（与基线一致，导入/路径完好）；④ 全量 pytest 后台运行（task sBjmvu，系统3.11，`-p no:locust --timeout=600`），完成后通知。
- **未提交/未推送**（铁律）：本次 B+C 改动未提交；叠加此前领先 origin/main 3 commit。G5 闸门路径（docs/api）未动，仍绿。
- **下一步**：全量回归 sBjmvu 零回归后，按铁律待用户确认再 commit（建议合并 B+C 为 2–3 个语义 commit：apps 收编 / ops 折叠 / 文档同步）与 push。
## G5 实时真验（13:3x）
- 起后端 5000（apps/backend/run.py --env development）：SWAGGER_READY 276KB。
- **修复 Phase3 收编引入的隐藏 bug**：`apps/backend/scripts/verify_openapi_contract.py` 的 `DEFAULT_SNAPSHOT` 原写 `BASE_DIR.parent/"docs"/"api"/"openapi.json"`，backend 移入 apps/ 后 BASE_DIR.parent=apps/ 导致解析成 `apps/docs/api/openapi.json`（文件不存在，G5 误判 skip）。改为 `BASE_DIR.parent.parent/...`（= 仓库根）。该文件本就 LF，Edit 未翻 EOL。
- **G5 真验结论：PASS**（exit 0）。快照 469 路径 = 实时 469 路径；新增/消失/方法差异 均 0，契约零漂移。
- 后台全量 pytest sBjmvu 仍运行中（~28%），完成自动通知；后端 38d4YT 仍在线 5000。

## 提交 B+C + bug 修复（语义 commit，未推送）
- 无激活 pre-commit 钩子（.git/hooks 仅 .sample），提交不被 run_validation.py 拦截。
- Commit1 `0996955` refactor(repo): 根目录按域重组 Phase3 应用收编 + 运维折叠（899 文件变更；backend/frontend/firmware->apps/，deploy/infra->ops/；路径引用全同步）。
- Commit2 `2db39b4` fix(repo): 修复 OpenAPI 契约快照路径（DEFAULT_SNAPSHOT 因 backend 移入 apps/ 失效）。diff 仅 1 行。
- 提交策略：verify_openapi_contract.py 在 Commit1 暂以「移动后未修复」态入库（broken 中间态，不推送无碍），Commit2 补回修复，使 bug 修复独立可见。
- 当前领先 origin/main 5（未推送）。未提交仅余 .workbuddy/memory/2026-09-08.md（当日日志，已排除）。
- 铁律：未自动 push，待用户确认。

## 推送（13:4x）
- 用户确认「push所有」。无本地代理监听（7890/1087 等均未起），但 `ssh://git@ssh.github.com:443` 直连成功（SSH over 443，无需 HTTP 代理）。
- `git push origin main` 成功：`75b6dc4..2db39b4 main -> main`，PUSH_EXIT=0。
- 权威核验：`git ls-remote origin refs/heads/main` = `2db39b4...`；本地 HEAD 同值；`git rev-list --count origin/main..HEAD` = 0（零领先）。
- 未提交仅余 `.workbuddy/memory/2026-09-08.md`（本日工作日志，按惯例不纳入代码交付 commit，保持未提交）。

## T5 前端 any 收敛 · 实测已收口（用户选 T5 后发现早已完成）
- 用户「继续完成优化工作」→ 选 T5 前端 any 收敛。实跑盘点：T5 **早已收口**（前序会话完成，未在本日日志体现）。
- 证据：全仓 type-level `any` 扫描（`: any|as any|<any>|any[]|Promise<any>|Record<string,any>|any =>|...`）= **0 匹配**；残留 3 处 `any` 全在注释（env.ts:11 / codeSplitting.tsx:22 / Dashboard.test.tsx:14）。
- `apps/frontend/src/services/api.ts`(6361 行) 已用 `RawJson=Record<string,unknown>` + `RawAlg{[key:string]:unknown}` + `normalize*` 运行时归一化替代 any；注释明写「T5: 后端信封/算法接口返回的原始 JSON（结构在运行时校验）。用于替代 any」「未知字段经索引签名为 unknown，避免 any 扩散」。
- 推论：P0/P1 代码质量项 **T1/T2/T3/T5/T6 全部收口并已提交**（score_recalc.py + 33 测试文件含 permissions.test.ts/ScoreEntry.test.tsx/RankBoard.test.tsx/api.test.ts + M9 分页 + security_routes raise RuntimeError 为证）。
- 真正剩余的优化前沿：T4 综合分异步化 / T7 ML 解耦 / T8 死依赖 / T9 大路由瘦身 / T10 CRA eslint 残留 / T11 静默失败页 / T12 巨型页拆分 + 决策②前端 prettier+eslint 半（未执行）。
- 待用户确认下一步（T5 无需再做）。

## 优化工作收官：前端 prettier 格式化（决策②前端半）· 已落地未提交
- 用户「继续完成优化工作」连选 T5、P2卫生包，实测**两者早已收口**（前序会话完成）：T5 全仓 any=0（api.ts 用 unknown+normalize*）；T8 死依赖已删（requirements.txt:53-54 catboost/gensim 移除、torch/transformers 移 requirements-ml.txt、web-vitals 不在 package.json）；T9 except:pass 已日志化、路由已下沉；T10 .eslintrc.js 内联 react-app 规则去 CRA 依赖（注释明写 T10 #960）；T4 enqueue_or_recalc_user_score 已 Celery 入队；T7 ML 解耦完成；T11 四页 return null=0；T12 巨型页≤1435 行。
- **结论：T1–T12 路线图 + 决策①②③ + 薄路由战役 + 架构债兜底收敛 全部已完成并提交**；唯一未做的是**决策②前端 prettier+eslint 半**。
- 执行：`.prettierrc` 与锁定决策一致（printWidth100/singleQuote/semi/lf）。`prettier --check` 实测 80 文件不合规 → `prettier --write` 格式化全部 80 文件 → 复检 `All matched files use Prettier code style!`。
- **回归闸门全绿**：`tsc --noEmit`(managed Node22.22.2) exit 0；`eslint src --ext .ts,.tsx` exit 0（0 error，9 个 pre-existing no-unused-vars warning，无新增）；`vitest run` **277 passed / 3 skipped(280)**，exit 0。
- 纯格式调整，零逻辑变更；endOfLine=lf 按决策统一。未提交（铁律），待用户确认 commit/push。
- **管理平台优化工作至此实质收官**。后续若继续，可转其他并行线程：F17 写路径迁移(~106处)、班主任工作台交互修复、手机箱 OTA。

## 重要校正：F17 与班主任工作台实为已完成（用户点名两线程均已收口）
- 用户「继续完成优化工作」点名 F17 写路径迁移（约106处）+ 班主任工作台交互修复。实测 + 文档复核：两者均**早已终态收口**。
- **F17**：`docs/F17_路由服务化重构_终态汇总与收尾记忆.md`（#629，2026-08-30）明写「真正终态闭合」；全局 grep `api/` 写路径 `db.session.(commit|rollback|add|delete|flush|merge|refresh|execute|bulk_save)` 零残留（仅 nlp 一处只读 SELECT）。本回合实测 `api/` 仅 9 处 `db.session` 全为读/统计；`services.` 调用 372 处 → 路由已薄壳化。内存「约106处」为陈旧数据。
- **班主任工作台**：`pages/WorkbenchOverview.tsx`(527行) 聚合落地页已存在（内存「无聚合首页」陈旧）；`docs/Medium轮缺陷修复计划-20260831.md` 中 M1-M5 直接修、M6 判定关闭、M7/M8 按方案A实施✅、M9-M11 延后/观察。
- **推论**：管理平台优化（T1-T12 + F17 + 决策①②③ + 薄路由 + 架构债兜底 + Medium缺陷）已全收口并提交。唯一未交付为**前端 prettier 80 文件改动（tsc/eslint/vitest 全绿，未 commit）** + `composite_score_service.py:526,532` 两处 `print()`→`logger`（Medium计划§六提及，零功能影响，可选）。
- 待用户重定向（铁律：班主任页改动须用户审核，不静默重构）。

## 重扫管理平台新债（用户选「重扫管理平台新债」）
- 全仓 fresh 扫描结论：**主体已终态洁净**。洁净项：console.* 无散落；TODO/FIXME 后端0/前端1(测试内)；any 0；print 仅 CLI tools/scripts；硬编码 endpoint 无；F17 写路径 0 残留。
- 新鲜债项（已入库 docs/代码债重扫清单-20260908.md + 6 个 task D1-D6）：
  D1 exhaustive-deps 抑制集群(~25+处,NLPManagement 9处)·Medium·需逐处核验；
  D2 巨型页>1000行(DeviceManagement 2031 等 8+ 页)·P3·班主任页须铁律③审核；
  D3 debounce/throttle 重复(mobileUtils vs requestOptimizationService)·Low/Med；
  D4 batch_writer.py:447 print→logger·Low；
  D5 后端 broad except 补 logger.exception·Low；
  D6 formatNumber 合并到 format.ts·Low。
- 处置建议：D1/D2 先规划；D3-D6 小批量低风险直收，每批零回归后提交。铁律：不主动 commit/push。
- 当前工作树：前端 prettier 80 文件仍待提交（D系列未动代码）。

## D系列逐步优化启动（2026-09-08 续，按用户「按照建议逐步优化」）
- **D4 完成**（backend `utils/batch_writer.py:447` print→logger.info）：CRLF 文件用 Python 字节级改写（`str.encode('utf-8')` 定位，保留 CRLF），diff 仅 1 行；`py_compile` 全绿；引用方 `utils/__init__.py:34` 导入符号未变，逻辑零影响。
- **⚠️ 灾难与恢复**：做 D6 时 `git rm apps/frontend/src/utils/formatNumber.ts` 命令被 SIGTERM 中断，残留 `.git/index.lock`，并导致 `apps/frontend/src` 下 **267 个文件被从磁盘物理删除**（git status ` D`）。已 `rm -f .git/index.lock` + `git checkout HEAD -- apps/frontend/src` 从 HEAD 全量恢复（DELETED 267→0），`backend` D4 改动未受影响。根因疑为中断 git 进程在中文路径下的异常 → 后续**禁用 git rm**，改用不删除文件方案。
- **D6 重做（re-export 桥接，零删除）**：`format.ts` 并入 formatNumber/formatPercent/formatCurrency/formatCompact/formatScore；3 个引用页（ScoreRecords/ScoreAnalysis/StudentPortal）import 改为 `'../utils/format'`；`formatNumber.ts` 改为纯 re-export 桥接（保留文件，调用方仍能收敛到 format）。`tsc --noEmit` exit 0；`prettier --write` 全量成功（修正入口：用 `./node_modules/.bin/prettier` shim，非不存在的 `bin/prettier.cjs`）。
- **✅ D6 闸门通过**（task Wfqobd）：`vitest run` = **277 passed / 3 skipped**(exit 0)；`format.test.ts(9 tests)` 通过=合并+桥接无回归；tsc/eslint 全绿。D4+D6 均零逻辑变更、零删除。
- 待续：D6 通过后继续 D3/D5(低风险 backend) → D1(exhaustive-deps 核验) → D2(巨型页，含班主任页须铁律③审核)。铁律：不主动 commit/push。

## D3/D5/D1 收口（2026-09-08 终）
- **D3 = false positive（跳过）**：`mobileUtils.debounce/throttle`(通用 timing，`(func,wait)=>wrappedFunc` 无 key) vs `requestOptimizationService.debounceRequest/throttleRequest`(基于 `private debounceMap/throttleMap` 的请求级 keyed 去重，`(key,request,options)=>Promise<T>`)。语义根本不同、无实现重复，强行合并收益低 → 跳过（已写回清单）。
- **D5 安全子集完成**（backend `api/student/student_routes.py` 4 处）：原 analytics 端点 engagement/risk/score_trend/participation_trend 异常**服务端完全无日志、静默吞入返回 dict**。补模块级 `logger` + 4 处 `logger.exception(...)`（HTTP 响应契约逐字节不变）。文件 LF（Edit 安全），diff 6 行追加。scheduled_notify_routes 2 处外层已有聚合日志，不强行加。
  - **真实范围修正**：`# noqa: BLE001` 的 broad except 全仓 **55+ 处、30+ 文件**（含 conftest/run.py/service_init/startup_cleanup/decorators@safe_handle 核心/services/脚本）；远超原描述。其余 50+ 处按铁律延后（启动期 logger 未初始化、`@safe_handle` 核心、跨系统大面改动风险）。
  - **闸门全绿（task vJCe31）**：py_compile OK + 定向 `test_student_insights.py` 通过 + 全量 `bash scripts/run_regression.sh` **REG_EXIT=0**（2m48s，全量回归通过）。
- **D1 逐处核验完成（零改动）**：全仓 grep 实测 **22 处**（非原估 25+）= 11 file-level 整文件抑制（Analysis/DeviceManagement/Dashboard/CourseSchedule/ExamManagement/FirmwareManagement/NLPManagement/RemoteNotify/RuleList/ScoreEntry/UserList 顶部 `/* eslint-disable */`）+ 11 单行。原清单「NLPManagement 9 处」为误报，实为 1 file-level。
  - 11 单行全部合法有意省略（ref 捕获最新值 / 稳定 setState / 挂载期 poll / trigger 驱动），无需补全依赖；11 file-level 为开发期有意全文件约定，逐页移除会爆大量 warning 且无功能收益 → **保留**。
  - **闸门（eslint 22 文件）**：`node node_modules/eslint/bin/eslint.js` 跑 22 处所在文件 = **ESLINT_EXIT=0** 全绿。
- **D系列最终状态**：D1✅核验零改动 / D2📋规划完成待逐页认领 / D3❌跳过 / D4✅ / D5✅子集 / D6✅。前端 prettier 80 文件 + 债务清单/日志未提交。
- **铁律遵守**：本轮所有代码改动（D4/D5/D6 + prettier）均**未 commit/push**：未提交项 = ① 前端 prettier 80 文件；② `docs/代码债重扫清单-20260908.md`；③ 本日 memory 日志。待用户明确指示再 commit。

## D2 巨型页拆分 · 启动执行（2026-09-08 续）
- **首拆 Analysis.tsx（1091 行 → 逻辑层 ~230 行）**：
  - 新建 `src/pages/analysis/AnalysisSections.tsx`：9 个纯展示子组件（BasicStatsGrid/AlgorithmInsightCards/ScoreDistributionPanel/ClusterDistributionPanel/RiskWarningPanel/TopUsersRanking/ScoreTrendPanel/CorrelationPanel/NeedAttentionPanel）+ 导出共享类型( UserWithCluster/AlgorithmData/ScoreDistributionItem/ClusterPieItem/WeeklyDataItem/BasicStat/AlgorithmStat )与常量(CLUSTER_COLORS/RISK_COLORS/getClusterColor)。
  - `Analysis.tsx` 重写为「逻辑层 + 子组件调用」，全部 state/effect/handler/useMemo 逐字节保留，仅把渲染 JSX 换成子组件。
  - **关键坑（已踩已修）**：① 子文件在 `pages/analysis/` 下，`'../types'/'../components'` 应是 `'../../types'/'../../components'`（路径少一级导致 `User` 导入失败、级联出 15+ 属性错误）；② 子文件漏导入 `BarChart3`（eslint react/jsx-no-undef 报错）；③ Analysis 侧移除已迁走的 recharts/多余 lucide 导入，补齐 basicStats/algorithmStats 仍用的 Users/Award/TrendingUp/TrendingDown/Activity/Zap/GitBranch/Shield。
  - **闸门全绿**：`tsc --noEmit` EXIT=0；`eslint` 两文件 EXIT=0；`vitest run` = **277 passed / 3 skipped** EXIT=0（Analysis 无专项测试，全量覆盖）。
- **下一步执行顺序**：RuleList(1100)→ScoreAnalysis(1104)→SubjectManagement(1198)→AlgorithmAnalysis(1431)→Dashboard(1170)→DeviceManagement(2031)/PermissionManagement(1657)/ScoreEntry(1571)/UserList(1490)/CourseSchedule(1476)/NLPManagement(1358)；班主任页 🔒ClassManagement/AttendanceManage/StudyGuide 须铁律③审核后执行。
- **铁律遵守**：D2 改动（Analysis 拆分）+ 此前 D4/D5/D6/prettier 全部**未 commit/push**，待用户明确指示。

### D2 第二拆：RuleList.tsx（2026-09-08 续）
- 抽 `src/pages/rule/RuleSections.tsx` 导出 `RuleView`：纯展示 + 三类模态框（规则增改 / Excel 导入 / 模板应用），全部 state/effect/handler 留在 `RuleList.tsx`。
- 共享接口（Rule/Category/FormData/FormErrors/RuleTemplate）留在 `RuleList.tsx`，`RuleSections.tsx` 用 `import type { ... } from '../RuleList'` 引入——类型循环导入安全（仅类型，运行期无环）。
- `RuleList.tsx` 重写为逻辑层 + `return <RuleView {...35个props}/>`，全部 state/effect/handler/useMemo 逐字节保留，删除渲染专用导入（lucide 图标、components 的 Button/Skeleton/SearchFilter/EmptyState/PermissionButton）。
- **闸门**：`eslint` 两文件 **EXIT=0**（已验证）。`tsc --noEmit` / `vitest run` 因本轮回话末段 Bash/PowerShell/TaskOutput 工具层集体临时故障（连 `echo ok` 都被拒）**未复跑**——预期零回归（逻辑未变），待工具恢复后补验。
- 经验：high-prop-count 视图（35 props）用单一 `RuleView` 整体抽取比 Analysis 多子组件更直接，类型接口集中、风险可控。

## D2 第五拆：AlgorithmAnalysis.tsx（2026-09-08 续）
- 抽 `src/pages/algorithm-analysis/AlgorithmAnalysisShell.tsx` 导出 `AlgorithmAnalysisShell` + `AlgorithmAnalysisShellProps`。原文件 12 个 Tab 子组件已在 `./algorithm-analysis/`，本拆仅把"主壳 JSX（头部/搜索/班级筛选/刷新/标签导航/5 个特殊控制栏/警示/错误/内容分发/加载遮罩）+ 4 个列 useMemo（predictionDetailColumns/scorePredictColumns/attributionColumns/engagementColumns）"移到视图层。
- 逻辑层保留全部 state/effect/handler/useMemo，仅 `return <AlgorithmAnalysisShell {...shellProps}/>`。`shellProps` 透传 `AlgorithmAnalysisDeps`（不含列定义）+ 壳层专用 setter/回调（activeTab/setActiveTab/tabNavRef/setSearchKeyword/setPredictionDays/setAnomalyDays/setRecommendDays/loadPrediction/loadAnomaly/loadStatistics）；Shell 内部 memo 列定义后合并回 `deps` 下发给各 Tab（与原闭包行为一致）。`anomalyDays`/`recommendDays` 非 `AlgorithmAnalysisDeps` 字段，需在 ShellProps 额外声明。
- **坑（已修）**：① Shell 组件是 default export，主文件误用 named import 致 TS2724 → 改 `import { default as AlgorithmAnalysisShell, type ... }`；② `anomalyDays`/`recommendDays` 不在 deps 接口 → 补到 ShellProps；③ 4 个 `studentProfile` 相关类型（RiskPredictResult/AnomalyResult/ScoreAttributionResult/EngagementResult）在 Shell 未命名引用 → 移除未用导入消除 eslint warning。
- **闸门全绿**：`tsc --noEmit` EXIT=0；`eslint` 两文件 EXIT=0（仅 1 条与原始一致的 exhaustive-deps 警告）；`vitest run` = **277 passed / 3 skipped** EXIT=0。
- **D2 进度（5/多）**：Analysis ✅ + RuleList ✅ + ScoreAnalysis ✅ + SubjectManagement ✅ + AlgorithmAnalysis ✅；下一站 Dashboard(1170)；班主任页 🔒铁律③。

## D2 第六拆：Dashboard.tsx（2026-09-08 续）
- 抽 `src/pages/dashboard/DashboardView.tsx` 导出 `DashboardView` + `DashboardViewProps`。本页结构异于前几页：逻辑层用 `useReducer`（`DashboardState`/`DashboardAction`/`dataReducer`/`initialState`），且 `UserCard`/`DeviceCard` 原定义在 `Dashboard` 函数体内、闭包捕获 `getLevel`/`getRankColor`/`getScoreColor`/`getUserCluster`/`CLUSTER_COLORS`。
- 视图层收口 5 个 memo 组件（`AnimatedNumber`/`StatCard`/`UserCard`/`DeviceCard`/`LiveClock`）+ 5 个纯函数/常量 helper + 主壳 JSX；`UserCard` 的 `getUserCluster(state.algorithmData.clusters, user.id)` 重构为模块级 `(clusters, userId)` 入参，由逻辑层经 `state.algorithmData.clusters` 下发给 `UserCard` 的 `clusters` prop。逻辑层导出 `DashboardState`/`ClusterData`/`ClusterStudent` 供视图 `import type`（同 RuleList 的 `import type` 循环安全模式）。`loading` 骨架屏判断移到视图层顶部。
- **坑（已修）**：① 视图层在 `src/pages/dashboard/` 下，`'../components'`（DashboardSkeleton）应是 `'../../components'`——原误写 `'../components'` 致 TS2307，已改 `../../`；② 视图层误用 named 之外的 hooks（`useRef`/`useEffect`）→ 补到 react 导入；③ 逻辑层 `ID`、视图层 `Notification` 拆分后无命名引用 → 移除未用类型导入（eslint 零 warning）。
- **闸门全绿**：`tsc --noEmit` EXIT=0；`eslint` 两文件 EXIT=0；`vitest run` = **277 passed / 3 skipped** EXIT=0。拆分后 Dashboard(逻辑)480 行 + DashboardView 752 行。
- **D2 进度（6/多）**：Analysis ✅ + RuleList ✅ + ScoreAnalysis ✅ + SubjectManagement ✅ + AlgorithmAnalysis ✅ + Dashboard ✅；下一站余量大页 DeviceManagement(2031)/PermissionManagement(1657)/ScoreEntry(1571)/UserList(1490)/CourseSchedule(1476)/NLPManagement(1358)；班主任页 🔒铁律③。

## D2 第七拆：DeviceManagement.tsx（2026-09-08 续）
- 用户选「子模块拆分（推荐）」。本页 2031 行、异构度最高（5 useForm / 9 模态框 / list+monitor 双 Tab / WebSocket 心跳订阅 / 导出导入 / 批量OTA轮询），单逻辑/视图二分会产生 ~1500 行视图层，故采用子模块拆分。
- 抽 `src/pages/deviceManagement/` 共 16 文件：
  - `types.ts`（DeviceStats/AdvancedStats/NewDeviceForm/BindForm/DeviceSettings/OTAForm/OTAProgressData/ClassItem/AdminItem/DeviceImportResult 接口）
  - `helpers.tsx`（`getSystemStateText`/`getSignalStrength`/`getSeverityIcon` 提升为模块级）
  - `DeviceColumns.tsx`（`DeviceRow` 类型 + `createDeviceColumns(handlers)` 模块级列工厂，逻辑层 useMemo 内调用并注入 5 个 handler）
  - `StatisticsCards.tsx`（4 张渐变统计卡）、`DeviceListView.tsx`（列表 Tab）、`DeviceMonitorView.tsx`（监控 Tab）
  - 9 个模态框（`AddDeviceModal`/`DeviceDetailModal`/`BindDeviceModal`/`ControlDeviceModal`/`DeviceSettingsModal`/`OTAModal`/`BulkOTAModal`/`OTAProgressModal`/`ImportModal`），各自承接 props
  - `DeviceManagementView.tsx`（顶视图：header + 统计卡 + Tab 分发 + 9 模态框组合，`DeviceManagementViewProps` 单一大接口）
- `DeviceManagement.tsx` 重写为纯逻辑层（**802 行**，原 2031）：全部 state/effect/handler/useMemo/useForm/useModal/useSubmitGuard/WebSocket 订阅零变更，仅 `return <DeviceManagementView {...props}/>`。`submitting` 守卫 + `runSubmit` 包装为 `submitAddDevice/submitBindDevice/submitRemoteControl/submitUpdateSettings/submitOTAUpgrade/submitBulkOTAUpgrade` 下发；`useForm` 泛型 `handleChange` 经 `(field, value: unknown)` 适配器（DeviceSettings/OTA 两异构表单需包装，NewDevice/Bind 直接透传）透传视图层；`closeDetailModal` 复合关闭回调。
- **坑（已修）**：① `getSignalStrength` 助手名误写成 `getSignal` → 改 `getSignalStrength` + 导入；② 工作树 `DataTable.tsx` 已删 `ColumnType.fixed`，原 `fixed:'right'` 属死配置，移除（视觉行为不变）；③ `Device` 类型来自 `'../../types'` 非 `'../../services/api'`（Alert/Heartbeat 才来自 services/api），修正 3 个子文件 import；④ `signalDistribution` useMemo 推断为 `Record<string,number>` 与视图层 `{excellent,good,fair,poor}` 结构类型不匹配 → 显式标注分布对象类型。
- **闸门全绿**：`tsc --noEmit`(managed Node22.22.2) EXIT=0；`eslint` 全绿 EXIT=0（清理 1 个未用 `AlertTriangle` 导入 warning）；`vitest run` = **277 passed / 3 skipped**(280) EXIT=0（全量回归，2m01s）。
- **D2 进度（7/多）**：Analysis ✅ + RuleList ✅ + ScoreAnalysis ✅ + SubjectManagement ✅ + AlgorithmAnalysis ✅ + Dashboard ✅ + DeviceManagement ✅；下一站余量大页 PermissionManagement(1657)/ScoreEntry(1571)/UserList(1490)/CourseSchedule(1476)/NLPManagement(1358)；班主任页 🔒铁律③。
- **铁律遵守**：D2 拆分 + 此前 D4/D5/D6/prettier 全部**未 commit/push**，待用户明确指示。

## D2 第八拆：PermissionManagement.tsx（2026-09-08 续）
- 用户「继续完成其他大页」。本页 1657 行、5 Tab（用户/班级/角色/权限/日志）+ 5 模态框，采用与 DeviceManagement 同策略的子模块拆分（逻辑层 + 视图层）。
- 抽 `src/pages/permissionManagement/` 共 3 文件：
  - `types.ts`（PermissionLog/AdminFormData/ClassFormData/RoleFormData/PermissionFormData 接口；`ClassInfo` 引自 `'../../services/api'`，非 `'../../types'`）
  - `helpers.tsx`（**含 JSX 故必须 `.tsx`**：`getRoleLabel`/`getRoleBadgeColor`/`PERMISSION_CATEGORIES` 模块级 + 4 列工厂 `createAdminColumns(adminRolesMap,roles)`/`createClassColumns`/`createRoleColumns`/`createLogColumns` + `filterPermissions`/`groupPermissions` 纯派生）
  - `PermissionManagementView.tsx`（**default export** → 主文件 `import { default as PermissionManagementView }`；tab 导航 + 5 Tab + 5 模态框，单一 `PermissionManagementViewProps` 接口含 25+ props）
- `PermissionManagement.tsx` 重写为纯逻辑层（**614 行**，原 1657）：全部 state/effect/handler/useMemo 零变更，`getRoleLabel`/`getRoleBadgeColor`/`PERMISSION_CATEGORIES` 的 useMemo/const 降为模块级，仅 `if(loading) return <LoadingSpinner/>` + `return <PermissionManagementView {...props}/>`。`submitting`/`runSubmit` 经 props 下发视图层模态框保存按钮。
- **坑（已修）**：① `helpers.ts` 含 JSX 编译报 TS1005 → 改 `helpers.tsx`（`mv`）；② `ClassInfo` 不在 `'../../types'` 而在 `'../../services/api'`（原 `import api, { ClassInfo }` 印证）→ 三文件 import 改 `'../../services/api'`；③ 视图层 default export 误用 named import → 改 default import。
- **闸门全绿**：`tsc --noEmit`(managed Node22.22.2) EXIT=0；`eslint` 全绿 EXIT=0；`vitest run` = **277 passed / 3 skipped**(280) EXIT=0（全量回归，1m48s）。
- **D2 进度（8/多）**：Analysis ✅ + RuleList ✅ + ScoreAnalysis ✅ + SubjectManagement ✅ + AlgorithmAnalysis ✅ + Dashboard ✅ + DeviceManagement ✅ + PermissionManagement ✅；下一站余量大页 ScoreEntry(1571)/UserList(1490)/CourseSchedule(1476)/NLPManagement(1358)；班主任页 🔒铁律③。
- **铁律遵守**：D2 拆分全部**未 commit/push**，待用户明确指示。债清单（D2 段落 + 汇总表）与 memory 已同步。

## D2 第九拆：ScoreEntry.tsx（2026-09-08 续）
- 用户「继续完成其他大页」。本页 1571 行、useReducer 异构页（超动态 `columns` useMemo 含输入/粘贴/方向键导航），采用「逻辑层 + 单一 View 大 Props」范式（避免 DeviceManagement 16 文件过碎）。
- 抽 `src/pages/scoreEntry/` 共 2 文件：
  - `types.ts`（完整接口：ClassInfo/Exam/ScoreItem/ExamData/PendingChange/ScoreEntryDraft/ImportResult/CellPosition/ScoreEntryState/ScoreEntryAction/BatchFailure/ScoreEntryViewProps）
  - `ScoreEntryView.tsx`（**default export**：草稿恢复条/header 操作区/筛选 Card/进度保存全部确认区/batchProgress 条/batchFailures 条/DataTable/3 个 Modal(导入/批量操作/导入结果) 全部 JSX；批量保存「取消」经 `onCancelBatch` 回调跨层触发逻辑层 `cancelBatchRef.current=true`）
- `ScoreEntry.tsx` 重写为纯逻辑层（**1176 行**，原 1571）：保留 `scoreEntryReducer`/`initialState`、全部 stateful handler（`handleScoreBlur`/`focusCell`/`runBatched`/`handleSaveAll`/`handleImport`/`handleConfirmAll`/`handleBatchDelete`/`handleBatchReset`/`handleBatchConfirm`/`exportTemplate`/`getStatusBadge`/`handleKeyDown` 等）、`columns` 动态 useMemo（依赖 stateful handler 保留在逻辑层构造后下发，保证键盘/粘贴交互逐字节不变）、`examSubjects`/`visibleSubjects`/`getEntryProgress`/`filteredStudents` useMemo，以及 `cancelBatchRef`/`batchProgress`/`batchFailures` 本地状态；仅 `return <ScoreEntryView {...props}/>`。
- **坑（已修）**：① 逻辑层 `ColumnType` 导入路径 `'../../components/data-display/DataTable'` 应为 `'../components/data-display/DataTable'`（ScoreEntry 在 `src/pages/` 一级）→ 改 `../`；② eslint 首轮 5 个 unused-var（逻辑层 `classes`/`loading`/`selectedExamData`；视图层 `ColumnType`/`openImportResultModal`）→ 删解构 + 删 `selectedExamData` 整行 + 删 `types.ts` 接口 `openImportResultModal` 字段 + 逻辑层 return 删对应 prop；③ 删 `ColumnType` 首次编辑未生效（行 5 残留）→ 重新定位行 5 删除，eslint 终 EXIT=0 全清。
- **闸门全绿**：`tsc --noEmit`(managed Node22.22.2) EXIT=0；`eslint` 全绿 EXIT=0；`vitest run` = **277 passed / 3 skipped**(280) EXIT=0（全量回归后台 orIA1f，2m21s）。
- **D2 进度（9/多）**：Analysis ✅ + RuleList ✅ + ScoreAnalysis ✅ + SubjectManagement ✅ + AlgorithmAnalysis ✅ + Dashboard ✅ + DeviceManagement ✅ + PermissionManagement ✅ + ScoreEntry ✅；下一站余量大页 UserList(1490)/CourseSchedule(1476)/NLPManagement(1358)；班主任页 🔒铁律③。
- **铁律遵守**：D2 拆分全部**未 commit/push**，待用户明确指示。债清单（D2 段落 + 汇总表）与 memory 已同步。

## D2 第十拆：UserList.tsx（2026-09-08 续）
- 用户「继续」。本页 1490 行、useReducer 页（列表/批量操作/编辑/快捷评分），采用「逻辑层 + 单一 View 大 Props」范式。
- 抽 `src/pages/userList/` 共 2 文件：
  - `types.ts`（PaginationState/FormData/Rule 接口）
  - `UserListView.tsx`（**default export**：header/搜索+班级筛选/高级筛选卡/BatchActionBar/DataTable/3 个 Modal(增改/导入/快捷评分) 全部 JSX；`batchActions`/`advancedSearchFields` useMemo 下沉视图层）
- `UserList.tsx` 重写为纯逻辑层（**1009 行**，原 1490）：保留 `useReducer`+`State`/`Action`/reducer、全部 stateful handler（`handleOpenModal`/`handleSubmit`/`handleDelete`/`handleToggleActive`/`handleQuickScore`/`handleBatchDelete`/`handleBatchScore`/`handleExport`/`handleClearSelection`/`handleSelectionChange`/`handleClearFilters`/`handleRetry`/`fetchUsers`）、`userColumns` useMemo（闭包捕获 handler 保留在逻辑层构造后下发）、`classes`/`selectedUsersArray`/`selectedUsersData`/`debouncedSearchTerm` useMemo；仅 loading/error/empty 三分支 + `return <UserListView {...props}/>`。视图用 `autoSave.hasUnsavedChanges`（非独立变量）。新增 `handleClearFilters`（清搜索+班级+高级条件）、`handleRetry`（清 error+fetchTrigger++）两复合 handler 下沉视图（视图 loading/error/empty 的 onAction 用）；`State`/`Action` 经 `export type UserListState=State; UserListAction=Action` 供视图 `import type`。
- **坑（已修）**：① 视图层用 `ToggleSwitch` 未导入 → 补 `import { ToggleSwitch } from '../../components/form/ToggleSwitch'`；② `handleOpenQuickScore` 仅供逻辑层 `userColumns` 操作列用，视图不需要 → 从 props 接口/解构/逻辑层 props 对象三处删除；③ `advancedSearchFields` useMemo 视图层新文件无 file-level `eslint-disable` → 补 `dispatch` 到 deps 消除 exhaustive-deps 警告。
- **闸门全绿**：`tsc --noEmit`(managed Node22.22.2) EXIT=0；`eslint` 全绿 EXIT=0；`vitest run` = **277 passed / 3 skipped**(280) EXIT=0（全量回归后台 GkUdAv，1m50s）。
- **D2 进度（10/多）**：Analysis ✅ + RuleList ✅ + ScoreAnalysis ✅ + SubjectManagement ✅ + AlgorithmAnalysis ✅ + Dashboard ✅ + DeviceManagement ✅ + PermissionManagement ✅ + ScoreEntry ✅ + UserList ✅；下一站余量大页 CourseSchedule(1476)/NLPManagement(1358)；班主任页 🔒铁律③。
- **铁律遵守**：D2 拆分全部**未 commit/push**，待用户明确指示。债清单（D2 段落 + 汇总表）与 memory 已同步。

## D2 第十一拆：CourseSchedule.tsx（2026-09-08 续）
- 用户「继续」。本页 1476 行、**全 D2 最高「逻辑-in-JSX」耦合**（渲染块 784 行内嵌真实逻辑 + 14 setter 直连），采用「逻辑层 + 单 View」范式。
- 抽 `src/pages/courseSchedule/` 共 2 文件：
  - `types.ts`（FormData/ConflictResult/WeekDay/ImportResult/TeacherItem + 46 字段 CourseScheduleViewProps 接口）
  - `CourseScheduleView.tsx`（**default export**：header/统计卡/班级下拉/DataTable 时间表/增改 Modal(冲突警告+教师教室备注)/导入 Modal(模板下载+配置+策略 radio+文件拖拽+结果) 全部 JSX，verbatim 移动；`api`/`logger`/`downloadBlob`/`getAuthHeaders` 由视图直接 import）
- `CourseSchedule.tsx` 重写为纯逻辑层（**688 行**，原 1476，−53%）：保留全部 state（`showModal`/`closeModal`/`showImportModal` 经 useModal、`exportFormat`/`exporting`、13 setter）+ handler + **`columns` useMemo 保留在逻辑层**（闭包捕获 getPeriodTime/handleAdd/handleEdit/handleDelete/weekDays）+ activePeriods/filteredSchedules/统计 useMemo；仅末尾 `return <CourseScheduleView {...viewProps}/>`。
- **关键坑（已修）**：① 分析确认 JSX 内 `end`/`file`/`messages`/`ok`/`start`/`schedule`(仅 schedule.manage)/`period`/`subject`(map local) 均为 CSS 片段/局部变量 **非闭包引用**，真实闭包 props=46；② 逻辑层仍保留 `columns` useMemo（含 ColumnType/Clock/PermissionButton/Edit2/Trash2/User/MapPin/Plus/StatusTag）→ 这些 import 必须留逻辑层不能删；③ `FormData` 与全局 DOM FormData 冲突 → 必须 `import type { FormData }`（TS2866）；④ `handleSubmit` 签名为 `(e?: FormEvent)` → props 类型相应标注；⑤ 视图缺 `ClassPeriod`/`handleSubmit` 引用 → 补 import/destructure/props；⑥ prettier 用 `node_modules/prettier/cli.js`（`node_modules/.bin/prettier` 是 shell shim 会被 node 误执行 MODULE_NOT_FOUND）。
- **闸门全绿**：`tsc --noEmit`(managed Node22.22.2) EXIT=0；`eslint` 全绿 EXIT=0；`vitest run` = **277 passed / 3 skipped**(280) EXIT=0（全量回归后台 J7yhX4，1m57s）。
- **D2 进度（11/多）**：Analysis ✅ + RuleList ✅ + ScoreAnalysis ✅ + SubjectManagement ✅ + AlgorithmAnalysis ✅ + Dashboard ✅ + DeviceManagement ✅ + PermissionManagement ✅ + ScoreEntry ✅ + UserList ✅ + CourseSchedule ✅；下一站余量大页 NLPManagement(1358)；班主任页 🔒铁律③。
- **铁律遵守**：D2 拆分全部**未 commit/push**，待用户明确指示。债清单（D2 段落 + 汇总表）与 memory 已同步。

## D2 第十二拆核验：NLPManagement.tsx（T12 已拆，不再另拆）
- 核验结论：NLPManagement 在 T12 已按「主壳 + `NLPDeps` 透传」范式拆分完毕——ParseTab/RulesTab/TrainingTab/StatisticsTab/AnalysisTab + CorrectionModal/RuleFormModal/BatchImportModal/CorrectionsList 全部抽到 `./nlp-management/` 子模块（头部注释「T12 拆分」明示）。主文件 1358 行构成 = ~50 useState + ~35 useCallback handlers + 4 columns useMemo（ruleColumns/trainingResultColumns/performanceColumns/correctionColumns，闭包绑定 ~50 组件 state）+ ~100 行 `deps: NLPDeps` 装配 + **仅 ~60 行主壳导航 JSX**。
- **不再拆**：主壳 JSX 仅 60 行，无大块可下沉；剩余 bulk 是纯逻辑层（D2 铁律「逻辑保留零变更」范畴，不删）。强拆 columns 需绑定 50+ state 的宽泛 factory 接口、异构度高、收益低 → 违反「复杂度超收益则拒绝」，正确跳过。
  - **D2 非班主任 >1000 行大页至此全部处理完毕（十一拆 + NLP 核验）**，三闸门全绿。剩余：🔒 班主任页（ClassManagement/AttendanceManage/StudyGuide）铁律③。
  - **铁律遵守**：本轮 ScoreEntry/UserList/CourseSchedule 三拆 + 文档/记忆全部**未 commit/push**，待用户明确指示。

## D2 收口续：扩展 6 页闸门全绿 + 文档同步（2026-09-08 续）
- 用户「将 D2 范围扩展到 >1000 阈值外的『大页』」→ 锁定 6 候选：RemoteNotify(937)/DeviceGroup(909)/StudentPortal(894)/Approvals(823)/ClassPeriodSettings(710)/Diagnostics(709)。RemoteNotify/DeviceGroup 前序已拆验证；StudentPortal/Approvals/ClassPeriodSettings/Diagnostics 前序仅写码未闸门（Bash 故障期），本回 Bash 恢复后补 tsc/eslint/vitest 三闸门。
- **tsc 发现 5 处类型错误**，根因一致：视图层以 `setXxxForm({ field: v })` **Partial 形式**调用，但逻辑层直传原始 `useState` setter（`Dispatch<SetStateAction<T>>`）→ ① 类型不可 assign（`Partial<T>` 不兼容 `T`）；② 运行时整替换会丢字段。
- **修复**：4 处统一在逻辑层加合并包裹 `setXxxMerged = (data: Partial<T>) => setXxx(prev => ({ ...prev, ...data }))`，视图 props 类型维持 `(data: Partial<T>) => void`；+ ApprovalsView `sortOrder` 经 `sortOrder as SortOrder` 桥接（`SortOrder` 来自 `../../components/data-display/DataTable` 本地导出，非 antd）。另清 3 处 unused-import 警告（StatusTag/PeriodFormData/HealthCheck）。
- **三闸门全绿**：`tsc --noEmit` EXIT=0；`eslint` 8 文件 EXIT=0（0 problems）；`vitest run` = **277 passed / 3 skipped**(280) EXIT=0（与基线一致，零回归，后台 QIGLtk 1m47s）。
- **D2 全部收口**：非班主任大页 12 拆 + NLPManagement 核验 + 扩展 6 页；🔒 班主任页（ClassManagement/AttendanceManage/StudyGuide）铁律③三页全拆。债清单 D2 段落 + 汇总表已同步。
- **铁律遵守**：全部改动**未 commit/push**，待用户明确指示。
