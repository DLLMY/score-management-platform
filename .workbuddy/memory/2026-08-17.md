# 2026-08-17

## F1-F20 系统级修复完成（上午）+ 5 维重新评估（10:11）
- F 修复落地：check_rule_limits .first()×2、exam-import preview subject_id、批量录入事务隔离重构、成绩唯一性冲突检测×2、JWT 密钥强制（pytest 模式豁免）、X-Admin-Id 伪认证关闭+3 处审计日志 operator 改 get_current_admin、登录限流启用（app/__init__ 调用 + student login 纳入）、响应层 student_id 双字段过渡（5 文件）、权限 TTL 缓存 30s、student/records 分页钳制+日期 try、publish_mqtt 返回校验（开箱+远程通知 send/broadcast）、批量录入权限检查 3N→1N、rank 2 端点 cached_api、DB 恢复加固（路径穿越/SQLite 魔数/预备份）、删 14 死组件+enums.ts+ResolveDeviceAlert 重复、顶层 4 路由归位（api/algorithm、api/nlp、api/analytics）+version_routes 引用更新、回归测试 test_regression_20260817（4 passed）。
- 闸门：OpenAPI 0 漂移 / eslint 0 / build 0 / vitest 160+3 / pytest 回归 4+9 passed。
- **保留未做**：F16 models 拆包、F17 883 处路由直连 db 迁移（大重构）、F19 URL kebab 统一（契约变更低收益，随重构做）。

## 5 维重新评估（3 子代理+复核，报告 docs/全系统重新评估报告_2026-08-17.md）
- **维度1**：17 域 CRUD 全覆盖、抽查 10/10 匹配；但有"链路空转"（UnlockValidator 死链、考勤 leave 无写入口、开锁维度无数据源）。
- **P0 必崩（复核属实，1-2 行低级 bug）**：① approvals_routes.py:117/:412 列表推导 `r.student_id` 未定义 r → 审批列表恒 500；② box_routes.py:125-126 ScoreRecord 无 source/source_info 字段 → 刷卡加分恒 TypeError；③ export_routes.py:139/:354 无 new_score 属性 → 记录导出恒 AttributeError。
- **体系性断链**：开锁限额/黑名单全链路未生效+门户开箱不扣分免费（student_routes:314 只发空载荷）；开锁统计中英文描述断裂（"Points deducted for unlock" vs like("%开锁%")）→ 排行/综合分恒 0；综合评分仅 score-entry 路径触发重算（post/batch/delete/approvals/MQTT 不触发）；考试排名筛 status=published 而实际 confirmed → 恒空；请假审批不生成 leave 考勤记录；数据隔离漏考试/考勤/通知。
- **字段**：死列（ScoreRecord.reason/period_id/subject_id、Score.rank、Approval.leave_type）、current_score Integer vs score_change Float 精度截断、枚举漂移（pending 三语义/box closed vs 前端 opened）、updated_at 无 onupdate、规则删除无级联。
- **异常**：MQTT 首笔 min_interval 判空顺序颠倒（:73）、开箱并发双扣无锁无幂等、成绩无 DB 唯一约束兜底、Redis 仅 8 方法降级、远程通知 3 路径假成功、批量导入同批重复 card_id 脏 session、手动 POST /records 不限额可扣负、成绩无范围校验。
- **建议 R1-R9**：R1 修 3 必崩（改 a.student_id/删越界字段）→ R2 开锁链路统一 → R3 唯一约束 → R4 综合分重算全覆盖 → R5 原子累加 → R6 数据隔离补漏 → R7 字段规范 → R8 异常兜底 → R9 考试状态机。

## R1-R9 全部修复（10:37 起，用户「全部修复」）
- **R1 三个必崩**：approvals:117/412 `r.student_id`→`a.student_id`（F8 插双字段变量名误写）；box 删 source/source_info；export 删 3 处 new_score。
- **R2 开锁链路**：_deduct_and_unlock 统一走 UnlockValidator（黑名单/分数/日周限额）+record_unlock（扣分+计数+流水）；User 加 weekly_unlock_count/week_start_date 列+迁移；描述词统一"开锁扣分"（原英文与 like("%开锁%") 断裂）；门户开箱补扣分记账（原免费空载荷）；_check_time_window 兜底恒放行修复；validate_unlock 加 skip_time_window 参数。
- **R3**：Score UniqueConstraint(exam,student,subject)+迁移（库 0 重复）；单条/批量/更新 IntegrityError→400。
- **R4**：records 手动/批量/删除、approvals、MQTT 加分均触发 recalculate_user_score。
- **R5**：utils/score_utils.atomic_score_update（SQL 原子累加+钳制+刷 updated_at）；records/approvals/mqtt 全改用。
- **R6**：考勤/请假（attendance_service）、成绩列表（exam ScoreList）、通知列表（notifications_routes）套班级隔离。
- **R7**：删模型死列（ScoreRecord.reason/period_id/subject_id、Score.rank，读取改动态/None，DB 列保留）；User.current_score Integer→Float；User/Score.updated_at onupdate；规则删除置空 rule_id 级联；composite 读端 behavior_score 归一化；Device 补 battery_level/temperature 列+迁移；'pending' 三语义不收敛（说明保留）。
- **R8**：MQTT min_interval 判空顺序；Redis 14 方法 @_safe 降级装饰器（**模块级函数！类体内 RedisCache._safe 引用类名 NameError→连锁 404**，已踩坑修复）；远程通知 3 路径校验 publish 返回；批量导入批内 card_id 去重+rollback；统计日期 try；手动 records 钳 min/max。
- **R9**：成绩增删改 closed 拒绝；排名筛 confirmed；成绩范围校验（0~full 全路径）；Exam date 可写。
- 闸门：py_compile 23 OK / OpenAPI 0 漂移 / RBAC OK；pytest 首跑 17 失败=Redis 装饰器 NameError 连锁 404（已修重跑中）。
- **终态闸门**：py_compile 23 OK / pytest 定向 99 passed（回归+unlock+scores+approvals+records+attendance+mqtt；mqtt 2 测试按 R2 真实校验重写：mock_user→真实 User 记录+断言扣分/计数）/ OpenAPI 0 漂移 / RBAC OK。前端零改动。
- **踩坑**：Redis 降级装饰器定义在类体内引用类名 → NameError → redis_cache_service import 崩 → records/scores/approvals 全 404（17 测试连锁失败）；改模块级函数修复。

## 第 4 轮回归验证（11:31 起，R1-R9 后复核，发现并修 6 处遗漏）
- **V1 🔴 Score.to_dict 仍访问已删 rank 列**（R7 删列漏改）→ 有成绩即 AttributeError，删 rank 键。
- **V2 🟠 mqtt 2 条加分路径（rule_name/score_change）残留 Python 读改写**（R5 只改 rule_id 路径）→ 补 atomic_score_update+钳制（float 转换防 TypeError）。
- **V3 🟠 P2-8**：recalc 无 composite 记录 index() ValueError + 增量基数（composite 表）≠全量（全量活跃用户）→ 无记录返回 None + 基数统一。
- **V4 🟠 P1-4**：approve_leave 不生成考勤记录 → 批准后按请假日期幂等生成 Attendance(status='leave')（class_id 取学生，period='all_day'）。
- **V5 🟡**：UnlockValidator._check_daily_limit 历史 NULL daily_unlock_limit → None 比较 TypeError，默认 5。
- **V6 🟡 P2-7**：手机箱无策略 _serialize allow_self_unlock True→False（与 evaluate DEFER 一致）。
- 补 6 条复核回归测试（TestR1R9ReviewRegression）；全量定向 **117 passed**（8 文件）。OpenAPI/RBAC/前端零影响。
- 存量保留：F16/F17/F19 架构重构、pending 三语义、composite 开锁方向（产品确认）。

## 未覆盖区域深度评估（11:51 起，4 子代理+5 P0 人工复核，报告 docs/未覆盖区域深度评估报告_2026-08-17.md）
- **区域A 固件/OTA/MQTT**：P0-1 **OTA 下载被 requires_permission("view_devices") 阻断**（固件 http.GET 无认证头→401→OTA 全链路必失败，e2e 带 token 掩盖）；P0-2 SHA256(64位)冒充 md5 存 firmware.md5，固件只认 32 位→完整性校验静默失效。P1：OTA 失败状态契约缺失（自动推送死锁）、工具 version=latest 被拒、Range 续传无效、status 无人消费。契约一致：HMAC 签名串/心跳字段/unlock 下行。
- **区域B NLP**：P0-1 **execute_scoring 直接改 current_score**（nlp_enhanced_service:2930，生产可达 nlp_routes:191）绕过流水/原子/限额(写死0-100)/重算/隔离；P0-2 复合句只执行第一人；P0-3 扣分可能变加分（rule.score_value 符号）；P0-4 三层缓存无失效钩子+纠正删不存在的 _parse_cache；P0-5 重名 last-wins；P0-6 空词库全拒。ML 是摆设+训练自标注。
- **区域C 导出/运维**：P0-1 **导出零班级隔离**（班主任可导全校含电话/卡号）；P0-2 "操作后积分"恒 0（删 new_score 键未删表头）；P0-3 /metrics 500 截断；P0-4 遥测过滤值不匹配。Excel 公式注入/备份删除路径穿越/匿名写端点无保留策略。
- **区域D 前端**：P0-1 **权限跨账号残留**（登出不调 clearPermissions）；P0-2 401 跳转 replace('/login') 无 #（HashRouter→404）；P0-3 ScoreEntry 批量无防抖可重复提交。裸 fetch 绕过封装、useRouteChangeAbort 死代码。
- 改进建议 **S1-S10 分级**：S1 OTA 双通道、S2 NLP 走标准积分链路、S3 导出隔离、S4 前端权限+401、S5 OTA 契约、S6 NLP 正确性、S7 运维数据、S8 安全加固、S9 提交防重、S10 性能。

## S1-S10 全部修复（12:07 起，未覆盖区域深评修复）
- **S1 OTA 鉴权**：FirmwareDownload 去 requires_permission（固件无认证头→401 断链）；upload 改真 MD5（原 SHA256 64 位→固件校验静默失效）。
- **S5 OTA 契约**：mqtt_manager 失败状态映射补全（download_failed/space_insufficient/begin_failed/signature_failed/version_check_failed/resume_exhausted/incomplete→failed，防自动推送死锁）；batch-upgrade target_version=latest 兼容；Range 续传 Flask 2.3 send_file 原生支持无需改。
- **S2 NLP 积分链路**：execute_scoring 改 atomic_score_update+SystemConfig 钳制+ScoreRecord 流水+综合分重算（原直接改分绕过一切）。
- **S6 NLP 正确性**：复合句明确拒绝提示（不静默丢）；分数符号按 score_type 归一化（deduct 正数→负数）；纠正链路改清真实 parser 缓存（原删不存在的属性）；重名 id 置 None（歧义提示）；空词库友好提示；否定词前缀（不要/别/请勿）不误判。
- **S3 导出隔离**：export/import_export/report 套 get_allowed_classes/get_admin_class_ids（班主任不再能导全校）。
- **S7 运维数据**：export_service 删恒 0 列；/metrics 前端分页循环拉全量（原 500 截断）；FrontendTelemetry 过滤值对齐（api→api_request、js_error→javascript_error）；前端口表清理扩展（system_metric_service）。
- **S8 安全**：Excel 公式注入清洗（= + - @ 前缀 '）；备份删除 basename 校验；异常回显收敛（str(e) 不直返）；mqtt DEFAULT_CONFIG 弱口令移除。
- **S4 前端**：Header 登出清 clearPermissions+user_permissions/user_roles（权限残留）；api.ts 401 跳转改 hash。
- **S9 防重**：新 hooks/useSubmitGuard + ScoreEntry 保存/导入/批量×3 + ExamManagement 保存。
- **S10 性能**：批量指标延迟 commit 合并（原每条 commit）；NLP extract_entities 走缓存（原每请求全表查）。
- 闸门：py_compile 21 OK / pytest 130 passed（9 文件）/ eslint 0 / build 0 / vitest 160+3；OpenAPI 无路由变更跳过实时校验。
- 固件侧残留（需真机）：A-P1-1 固件失败码上报已在后端映射但固件需确认字段；A-P1-3 Range 续传固件侧；A-P1-2 工具 latest 由后端兼容；P2 弱凭据/TLS setInsecure 在固件/工具侧需人工改。

## vitest 超时问题定位与解决（12:41）
- **根因 1（真实缺陷）**：`src/utils/cacheDB.ts:22` `openCacheDB()` 直接 `indexedDB.open()`——vitest jsdom 无 indexedDB → `ReferenceError: indexedDB is not defined` 被 catch 后行为不确定；api.ts 请求路径走 getCache/setCache 触发。
- **根因 2（超时诱因）**：vitest 默认 testTimeout 5s，Windows+中文路径+并行任务（pytest/build/vitest 同跑）冷启动慢 → UserList 动态 import 偶发 5s 超时。
- **修复**：① cacheDB 加环境检测（`typeof indexedDB === 'undefined'` → 快速 reject，调用方 catch 降级穿透，消除 ReferenceError）；② vitest.config testTimeout/hookTimeout 5s→15s。
- **验证**：UserList 3 次全过；全量 vitest **160 passed/3 skipped**（与后端 pytest 并行竞争下）无超时；eslint 0。测试输出残留 71 行 "Cache get error: IndexedDB is not supported" 为预期的降级 warn（无碍闸门，生产环境无此日志）。
- **进一步根治（12:47）**：安装 `fake-indexeddb@6.2.5`（devDep）+ `src/test-setup.ts` 顶部 `import 'fake-indexeddb/auto'`（jsdom 注入内存 IndexedDB）→ cacheDB 测试环境真实可用、**降级 warn 噪音归零**；全量 vitest 160 passed/3 skipped 且噪音检查 0。cacheDB 的环境检测 guard 保留（生产无 IndexedDB 场景仍降级穿透，正确行为）。

## 同类问题全面排查与解决（12:50，vitest 环境缺失 API/噪音）
- **test-setup.ts 补 jsdom 缺失 API 标准 stub**：matchMedia / ResizeObserver / IntersectionObserver / scrollIntoView（Element.prototype）+ fetch 相对 URL → 转 http://localhost（消除 Failed to parse URL）。
- **代码层 6 处 guard（生产健壮性）**：ThemeContext matchMedia（默认浅色）、usePWA matchMedia+serviceWorker（'serviceWorker' in navigator）、LazyImage/OptimizedImage IntersectionObserver（无则直接加载）、VirtualList ResizeObserver、AlgorithmAnalysis scrollIntoView 可选调用。
- **Dashboard.test.tsx jest.mock → vi.mock**（经全局 jest 兼容别名的 jest.mock 不 hoist → mock 失效 → 真实 fetch 噪音；AlgorithmAnalysis/StudentPortal 已是 vi.mock）。
- **ScoreEntry.test.tsx 加通用 api Proxy mock**（任何嵌套方法返回 Promise.resolve([])，消除 /api/exams 真实 fetch）；**api.test.js vi.mock errorMonitor 静音**（错误路径测试上报日志）。
- **终验**：vitest **160 passed/3 skipped，噪音 0**（indexedDB/Cache/fetch failed/超时全部归零）；剩余 5 条 [ErrorMonitor] 属 errorMonitorDedupe 测试主题本身（验证去重，预期输出）；1 条 API Error 属 api.test.js 错误路径测试预期。ESLint 0。
- **经验**：jsdom 缺失 API（matchMedia/ResizeObserver/IntersectionObserver/scrollIntoView/indexedDB）→ test-setup 标准 stub；组件测试必须 mock api（vitest 用 vi.mock 勿用 jest.mock 别名）；ErrorMonitor 测试用 vi.mock 静音。

## 六维度评估报告升级方案一步到位执行（13:00–14:10，依据 docs/系统整体评估报告_六维度_2026-08-16.md）
- **范围（安全可一步到位，排除已落地 S5/CSP/限流/权限缓存 与高风险 F16/F17/F19/S4/S2）**：S1 密钥生产硬失败、M1 配置校验硬化、M3 RBAC 启动门禁、R1 Redis 连通性、R2 MQTT 已知好配置缓存、P1 归档表+迁移脚本、Q3 前端核心页 vitest。
- **备份**：DB 物理拷贝 `backend/instance/score_management.db.pre_upgrade_20260817`（2.3G）；6 配置文件快照 `backend/backups/pre_upgrade_20260817/`；完整 `git stash@{0}` "pre_upgrade_20260817_WIP"（已 drop 不完整的 stash@{1}）。
- **改动点**：`app/config_init.py`（S1/M1/M3/R1，production 下密钥/配置 error 级 sys.exit(1)，RBAC/Redis 非致命门禁）、`scripts/verify_rbac_consistency.py`（抽 run_check 不 sys.exit 供 importlib 调）、`services/mqtt_manager.py`（_last_known_good 回退）、`models/__init__.py`（ScoreArchive/AttendanceArchive）、`scripts/migrate_archive_tables.py`（建表+dry-run/execute，BATCH=2000）、`frontend/src/tests/components/AttendanceManage.test.tsx`+`Notifications.test.tsx`（proxy mock）。
- **全闸门（G1–G8 全绿）**：py_compile 0 / RBAC exit 0（68 条）/ create_app 冒烟（RBAC+Redis+安全+缓存中间件 OK）/ P1 dry-run 建表 OK 0 行（未移数据）/ OpenAPI 461/461 0 漂移 / eslint 0 / vite build 0（仅无关 CSS warning）/ vitest **164 passed/3 skipped**（新增 4 测试无回归）。
- **文档**：`docs/upgrade_execution_log_2026-08-17.md`（范围目标/备份清单/逐步日志/详细回滚方案/验证摘要）。
- **关键坑**：P1 迁移脚本 import app→torch，须用**系统 Python 3.11**（默认 `python` 缺 torch）；ESLint/vite/vitest 须用 `node node_modules/.../eslint.js|vite.js|vitest.mjs`（勿跑 .bin 包装脚本）。升级非破坏式，回滚极低风险。

## 后续建议执行（14:30，依据 upgrade_execution_log 第六节）
- **#3 P1 --execute 演练**：`migrate_archive_tables.py --execute` 实跑，归档表就绪，**scores +0 行 / attendance +0 行**（数据全在 365 天 retention 内，无数据移动），exit 0。验证完整 execute 路径安全。
- **#4 清理快照**：删 `api-docs/openapi.json.bak_*`（5 个 ~450K 历史快照，Git Bash rm -f）；**保留** `backend/instance/score_management.db.pre_upgrade_20260817`（2.3G 活动回滚点，NTFS 去重删亦不释放空间）。
- **#1 F16/F17/F19/S4 专项评估+排期**：产出 `docs/重构专项评估与排期_F16F17F19S4_2026-08-17.md`（只评估不改动代码）。F16 模型拆包中风险/第一批；F17 路由服务化高风险/第二批按域（实测 api/ 约 40 文件含 db.session，全后端约 883 处）；F19 kebab 契约变更低收益/可选延后；S4 JWT→Cookie 最高风险/最后一批需 staging 专项。均须独立分支+备份+全闸门，禁止 C 盘 2.3G 克隆（用 dry-run/影子表）。
- **#2 S2 固件 OTA**：本机无 ESP32 工具链，由硬件侧单独完成，无可执行动作。
- 结论：四项后续建议均已"执行其本意"（可安全执行的已落地，需专项/硬件的已评估排期或移交），无代码破坏性改动。

## F16 模型层拆包 — 已完成（14:40）
- **动作**：按 `docs/重构专项评估与排期_F16F17F19S4_2026-08-17.md` 第一批执行 F16。分支 `refactor/F16`，快照 `backend/backups/refactor_F16/models_init_before.py`。
- **做法**：AST 逐字切片（保留全部注释/F9-B/R7 注解与显式 `__tablename__`），将 `__init__.py` 内 60 个内联模型类拆到 8 个新子模块：`user_models / score_models / device_models / system_models / notify_models / alert_models / archive_models / nlp_models`。`__init__.py` 仅保留 `db`+helpers（`cascade_delete_related_records`/`is_bcrypt_hash`/`hash_password`/`get_by_id` 等）+ 底部再导出块（既有 12 + 新增 8）。工具脚本 `backend/backups/refactor_F16/split_models.py`（可重放）。
- **关键坑（已踩已修）**：首版脚本把 110-1361 区间整段删除，误删模块级函数 `get_by_id`（被 10+ 路由 `from models import get_by_id`），导致 create_app ImportError。修正为"仅删 class 块、保留其余模块级内容"后解决。
- **验证（全绿）**：py_compile 0 / create_app 冒烟 OK（RBAC+Redis+安全+缓存中间件）/ OpenAPI strict 461/461 0 漂移 / 宽导入面（academics/devices/users/monitoring/scores 路由 + utils.permission/security + nlp/ota/score 服务全部 OK）/ **命名空间 0 回归**（原 68 个顶层名全部 `hasattr(models,x)` 通过）/ 表名 0 漂移（与 live DB 一致）。前端不受影响。
- **新约定（长期）**：新增模型须放入对应子模块（如分数→score_models、设备→device_models），禁止回写 `__init__.py` 内联；`__init__.py` 仅供 `from models import X` 兼容再导出。
- 回滚：`git checkout main`（弃分支）或恢复 `models_init_before.py` + 删 8 子模块。

## F17 路由服务化（第一批：scores/categories pilot，2026-08-17 15:00）
- 分支 `refactor/F17`（自 F16 切出）。快照 `backups/refactor_F17/categories_routes_before.py`。
- 做法（防腐层渐进，按评估排期"优先写入/事务路径、只读暂缓"）：新增 `services/score_category_service.py`（`create_category`/`update_category`/`delete_category`，逻辑逐行照搬原路由），`api/scores/categories_routes.py` 仅迁移 POST/PUT/DELETE 写入路径，GET 列表/详情/规则保留（只读暂缓）；`db`/`datetime` import 因迁入 service 而移除。
- **契约铁律**：所有响应体/状态码/错误信息逐字节不变，含 create 端点历史双元组返回的 `[response_dict, 200]` 列表形态（APIResponse.success 已返回 `(dict,status)` 元组，路由再包 `(...,201)` 即双重元组→列表，属既有契约，迁移须复刻）。
- **测试先行**：先强化 `tests/test_categories_routes.py`（加 `_body()` 归一化列表信封 + 行为断言），迁移前基线 9/9，迁移后 9/9，契约 0 漂移。
- 验证：G1 py_compile / G2 RBAC exit0 / G3 create_app 冒烟 OK / G5 OpenAPI 461/461 0 漂移，全绿。前端未改不重跑。
- 排期后续：scores 域剩余 rank/time_rules/class_periods（中低）→ rules_routes（事务）→ records_routes（成绩录入主写入，含 score/add 幂等+flush 后读 id 契约，风险最高）→ approvals_routes；之后 notifications→devices→academics→users。每子域须补行为测试 + G1-G5；禁一次性 883 处全改。
- 文档：`docs/refactor_F17_execution_log_2026-08-17.md`（含回滚：cp 快照回退 + 删 service 文件 + 弃分支；零表变更无需 DB 回滚）。

## F17 路由服务化（第二批：scores/rank+time_rules+class_periods，2026-08-17 15:xx）
- 再迁移 3 路由 → 3 service：`services/{score_rank_service,time_rule_service,class_period_service}.py`。rank(5)/time_rules(5)/class_periods(8) 仅写入路径迁入；time_rules create 仍为 `(dict,201)` 元组、class_periods batch/reset 文案不变、重复编号校验留路由。
- 快照 `backups/refactor_F17/{rank,time_rules,class_periods}_routes_before.py`。强化 `test_rank_routes.py`/`test_time_rules_routes.py`，新增 `test_class_periods_routes.py`。
- 验证：定向 20/20（7+6+7）+ scores 域全套回归 87 passed（85.45s，零回归）；G1-G5 全绿（OpenAPI 461/461）。
- **scores 域中低风险子域已全清**（categories+rank/time_rules/class_periods）。下一站高风险写入路径：rules_routes(13)/records_routes(11,含 score/add 幂等+flush 后读 id)/approvals_routes(14)，迁移前须先补针对性行为测试（undo_code 幂等、flush 后读 id、事务回滚）。之后 notifications→devices→academics→users。

## F17 路由服务化（第三批：scores/rules_routes 高风险，2026-08-17 16:xx）
- 迁移 `api/scores/rules_routes.py`（13 处 db.session）→ `services/score_rule_service.py`（`create_rule`/`update_rule`/`delete_rule`(R7 级联解引用保留)/`import_rules`(批量事务)/`apply_rule_template`(事务+flush+回滚)）。路由仅留 get_or_404/校验/缓存失效/日志/响应。快照 `backups/refactor_F17/rules_routes_before.py`。
- 强化 `test_rules_routes.py` 12→17（新增 R7 级联/import 混合/apply 创建/缺 id/模板不存在）。验证：定向 17/17 + scores 全套回归 46 passed + G1-G5 全绿（OpenAPI 461/461）。

## F17 路由服务化（第四批：scores/records_routes 最高风险主写入，2026-08-17 续）
- 迁移 `api/scores/records_routes.py`（1049→998 行，4 写入路径）→ `services/score_record_service.py`（4 方法）。路由仅留 get_or_404/请求级校验/缓存失效/日志/MQTT 排名+积分变动通知/管理员通知/统计缓存失效/综合评分重算/响应构造。
- `create_record`/`delete_record` 走 R5 `atomic_score_update`(含 SystemConfig min/max 钳制)；`commit_batch_score_entry` 保留 F3 修复（仅成功行 add+原子累加+单次 commit）；`create_score_entry` 排名计算+设分+`log_operation`(commit 前设 operation_log_id，`hasattr` 守卫)+add+commit。
- 快照 `backups/refactor_F17/records_routes_before.py`。强化 `test_records_routes.py` 12→17（加 `_unwrap` 兼容列表/字典信封 + 5 行为用例：create/score_entry/batch_entry/delete 需确认/delete 确认回滚）。
- **迁移中发现并修复两处生产缺陷（均非破坏性，不改契约）**：
  1. score-entry 原路由 `record.operation_log_id = log_entry.id` 因 `log_operation` 返回 bool（非 OperationLog）→ AttributeError 500；加 `if log_entry and hasattr(log_entry,"id")` 守卫（路由+service 同法）。
  2. **积分双重累加**：路由调 `create_score_entry` 前仍内联 `user.current_score = before_score + score_change`，service 再读已更新值作 before 累加一次 → `new_score` 偏差 2×score_change；已删除路由内联积分改写，仅保留只读排名对比（基于 `before_score + score_change` 预测 after_rank），积分原子累加完全交 service。
- 验证：定向 **17/17 passed** + scores 域路由全套回归 **58 passed**（records+rules+rank+time_rules+class_periods+scores）+ G1 compileall 0 error + G2 RBAC 68 + G5 OpenAPI 461/461 0 漂移，全绿。
- **scores 域剩 approvals_routes（14 处，审批事务）待迁移**；之后 notifications→devices→academics→users。approvals 迁移前须补强行为测试（事务回滚/级联审批流/MQTT 通知）。

## F17 第五批：approvals_routes 审批事务迁移（下午）
- 快照 `backups/refactor_F17/approvals_routes_before.py`（480 行）→ 新建 `services/approval_service.py`（5 方法：create/update/delete/approve/reject_approval）。审批/原子积分/ScoreRecord 写入全部收口 service。
- 路由保留：get_or_404、请求级校验、数据隔离 `_can_access_approval_user`、跨切面副作用（R4 综合评分重算 / D3+R4 学生通知中心落库 Notification / MQTT 缓存+下发 / 管理通知）、响应构造。grep 复核：路由仅剩 6 处 db.session（approve+reject 各 1 个 Notification 块 add/commit/rollback，按 F17 保留）。
- 只读 GET（列表/详情/待审批）逐字节不变；响应体/状态码逐字节不变（create 201、approve/reject 200 单元组信封）。
- **测试先行**：补强 `test_approvals_routes.py`（6→16 用例）。基线发现两处**既有响应不一致**（按零漂移保留，未顺手修）：① `GET /approvals/<id>` 详情仅返回 `user_id` 不含 `student_id`（列表/pending 有）；② `GET /approvals/pending` 项不含 `status` 字段（列表有）。测试已对齐实际契约锁定。
- 验证：定向 **16/16 passed** + scores 域路由全套回归 **74 passed**（原58+16）+ G1 compileall 0 error + G2 RBAC 68 + G5 OpenAPI 461/461 0 漂移，全绿。
- scores 域累计 **8 路由 → 8 service**，写入路径已全部服务化。下一步 **notifications → devices → academics → users** 域。

## F17 第六批：notifications_routes 迁移（post-scores 首域，晚间）
- 快照 `backups/refactor_F17/notifications_routes_before.py`（288 行）→ 改写 `api/monitoring/notifications_routes.py`（249 行）。6 写入路径（POST /、PUT /<id>、DELETE /<id>、POST /<id>/read、POST /send、POST /batch）收口至**追加**进既有 `services/notification_service.py`（该文件是外部微信/短信下发服务，不可覆盖，仅追加 6 个 DB-CRUD 函数：create_user_notification/update_notification/delete_notification/mark_notification_read/send_notification/batch_send_notifications）。顶部加 `from models import db, Notification, User`。
- 路由保留：get_or_404（404 语义）、请求级校验（batch 空参数 400）、force_send 权限 + ClassTimeChecker 时段拦截、班级数据隔离、响应构造；只读 GET（列表/详情/按用户）逐字节不变。batch 事务保留逐条 `begin_nested()` savepoint（单条失败仅回滚该条），目标解析/校验/拦截留路由。
- **测试先行暴露并就地修复两处既有只读 500 缺陷**（迁移前即存在，非写入迁移引入，修复恢复预期契约、未移读逻辑入 service）：
  1. `Notification.query.filter_by(recipient_type="user").get_or_404(id)` 在 SQLAlchemy 2.0 抛 `InvalidRequestError`（get() on filtered query）→ 改 `filter_by(recipient_type="user", id=id).first_or_404()`（详情/PUT/DELETE/标记已读 4 端点）。
  2. `GET /user/<id>` 漏 `.paginate()` 直接取 `.items`/`.total` → 补 `.paginate(...)`。
- **已知 F9-B 残留不一致**：detail 端点返回 `user_id`(=student_id) 但**不含 `student_id`** 字段（list 有）。只读响应按"暂缓不动"保留，测试对齐实际契约（断言 user_id）。
- 测试：新增 `tests/test_notifications_routes.py`（16 用例，覆盖 3 GET + 6 写入 + 边界）。**16/16 passed**。
- 验证闸门：G1 py_compile OK（改动 2 文件）；G2 RBAC `--check-only` exit0（权限目录 68 条）；G5 OpenAPI strict **461/461** 0 漂移；G3 经 conftest 隐式加载全命名空间无 ImportError。全绿。
- F17 累计 **9 路由 → 9 service**。下一步 **devices → academics → users** 域。执行日志见 `docs/refactor_F17_execution_log_2026-08-17.md` 第 12 节。

## F17 第七批：devices 域（Device+DeviceGroup）迁移（晚间）
- 快照 `backups/refactor_F17/devices_routes_before.py`（1493 行）+ `device_group_routes.py_before.py`（393 行）→ 新建 `services/device_service.py`（352 行，13 函数）。改写 `api/devices/devices_routes.py`（1493→1348 行）、`device_group_routes.py`（393→332 行）。
- 13 写入端点收口 service：**Device（8）** create_device/update_device/delete_device/bind_device_class/bind_device_admin/resolve_device_alert/update_device_settings/import_devices + **DeviceGroup（5）** create_device_group/update_device_group/delete_device_group/add_devices_to_group/remove_device_from_group。import_devices 用 openpyxl 批量事务（逐行校验 validate_device_id/validate_name/ClassInfo 存在/Admin 角色 admin|teacher），致命错误抛出由路由 rollback+server_error。
- 路由保留：get_or_404、请求级校验（bind-class/bind-admin 的 ClassInfo/Admin 存在性→not_found）、缓存失效、操作日志、跨切面副作用、响应构造。grep 复核：devices_routes 仅剩合法 db.session（line 934 聚合只读 query + line 1236 import rollback 兜底）；device_group_routes 已无 db import。只读 GET 逐字节不变。
- **create 响应 `data.device_id` 语义 = PK(int)**，与详情业务键 `device_id`(str) 同名不同义——已逐字节复刻并写入执行日志锁定，避免后续误判。
- **测试先行仅修复测试侧 bug（业务契约零漂移）**：① create 断言混淆 PK id 与业务键 device_id（修正区分）；② 缺 device_id 路由无前置校验→TESTING 抛 IntegrityError，改 `pytest.raises(IntegrityError)` 锁定既有行为（未顺手补校验）；③ 导入行名"导入设备1"含数字被 validate_name 拒→改纯中文名；④ 预存 OTA 测试种子缺 last_heartbeat→is_device_online False→"无在线设备"400，补 `last_heartbeat=datetime.now()`（OTA 端点本轮未迁，非迁移范围）。
- 测试：补强 `tests/test_devices_routes.py`（TestDeviceWriteEndpoints 11 + TestDeviceGroupWriteEndpoints 4 用例）。**定向 pytest 27/27 passed**。
- 验证闸门：G1 py_compile 0 error；G2 RBAC `--check-only` exit0（68 条）；G5 OpenAPI strict **461/461** 0 漂移；G3 经 conftest 隐式加载无 ImportError。全绿。
- F17 累计 **devices 批：13 端点 → 13 service 函数**。devices 域剩余子批 box_routes/wol_routes/firmware_routes 写入路径待续；之后 academics→users。执行日志见 `docs/refactor_F17_execution_log_2026-08-17.md` 第 13 节。

## F17 第八批：devices 子批2（box + wol 写入路径）
- 快照 `backups/refactor_F17/box_routes_before.py` + `wol_routes_before.py`。`services/device_service.py` 追加 **4 函数**：box_add_score(user,rule) / create_wol_device(data) / update_wol_device(device,data) / delete_wol_device(device)。
- box_routes：仅 `POST /box/verify` 带 rule_id 分支有 DB 写（user.current_score+=rule.score + ScoreRecord + commit），收口至 box_add_score；其余（无 rule 验身份、check_rule_limits 限速只读）保留路由。路由移除未用 `db` import，保留 `ScoreRecord`（check_rule_limits 仍用）。
- wol_routes：POST/PUT/DELETE /wol/devices 三写端点收口 service；MAC 格式(400)/唯一(409) 校验留路由（产出状态码），service 幂等再归一化。网络类端点（/wake、/wake/batch、/validate、/status/<mac>、GET /devices）按"只读暂缓"保留。移除未用 `db` import。
- **测试先行**：test_box_routes 原 2 弱用例→6 行为用例；新建 test_wol_routes 9 行为用例。迁移前 15 passed 建立基线，迁移后同 15 passed 零漂移；devices 全量 27 passed 无回归。
- 验证闸门：G1 py_compile OK；G2 RBAC `--check-only` exit0（68 条，未增权限码）；G5 OpenAPI strict **461/461** 0 漂移；devices 全量 27/27 passed。flake8 仅 box_routes:55 预存 E501（check_rule_limits 长行，非本次引入）。
- F17 累计 **devices 子批2：4 端点 → 4 service 函数**。firmware_routes（24 db.session）紧接 OTA P0-P3 重写、风险高，**延期至 devices 第三子批独立处理**。执行日志见 `docs/refactor_F17_execution_log_2026-08-17.md` 第 14 节。

## F17 第九批：devices 子批3（firmware 写入路径）
- 快照 `backups/refactor_F17/firmware_routes_before.py`。新建 `services/firmware_service.py` **7 函数**：create_firmware_version / update_firmware_version / delete_firmware_version / report_ota_status（started/completed/failed 三分支）/ create_uploaded_firmware / log_batch_upgrade / log_ota_upgrade。
- firmware_routes：7 写端点 db.session 全收口 service；路由仅留 get_or_404 / 请求校验 / 文件 save+MD5 计算 / MQTT 下发 / 响应构造。`/negotiate-all` 本就委托 ota_negotiation_service，无路由内 db.session（不迁移）。移除未用 `db`/`OperationLog`/`datetime`/`from hashlib import md5` import。
- **测试先行暴露并就地修复 upload 写路径两 500 缺陷**：① 日志描述引用未定义 `sha256`→NameError 500；② 响应 `"md5": md5` 误用 hashlib.md5 模块对象。均改 `md5_hex`（真 32 位）。基线 24 passed+1 failed → 修后 **25 passed**。
- 新建 `tests/test_firmware_routes.py` 25 行为用例（覆盖 7 写端点 + 4 只读回归 + 关键错误分支）；upload 用例用 `tmp_path` 覆盖 FIRMWARE_UPLOAD_FOLDER 避免污染真实 uploads。
- 验证闸门：G1 py_compile OK；定向 pytest **25/25 passed**；devices 全量 **58/58 passed**（test_devices_routes/wol/box/wol_service/is_device_online/...）；G2 RBAC `--check-only` exit0（68）；G3 create_app 冒烟 941 routes；G5 OpenAPI strict **461/461** 0 漂移。
- F17 累计 **devices 域：24 端点 → 24 service 函数**（第一批13 + 子批2 4 + 子批3 7）。后续域 academics → users。执行日志见 `docs/refactor_F17_execution_log_2026-08-17.md` 第 15 节。

## F17 第十批：academics 域（subject + exam 子批）
- academics 域规划（#594，前序）：5 文件 57 处 db.session，4 子批（admin_classes/exam_import → subject → exam → course_schedule/import）。基线快照 `backups/refactor_F17/{subject_routes,exam_routes}_before.py` 等已就绪。
- **第二子批 subject_routes（#595，前序完成）**：17 处 db.session 收口至 `services/academics_service.py`（9 方法：create_subject/toggle_subject/update_subject/delete_subject/create_subject_class/update_subject_class/delete_subject_class/update_subject_order/execute_subject_import）。定向 **20/20 passed**；G1/G2(68)/G5(461) 全绿。
- **第三子批 exam_routes（#597，本批）**：20 处 db.session 收口至 `services/academics_service.py` 追加 **11 方法**（create_exam/update_exam/delete_exam/publish_exam/close_exam/create_score/batch_create_scores/update_score/delete_score/confirm_all_scores；路由保留 `_resolve_subject_id` 读助手）。10 写端点（Exam CRUD/publish/close + Score 单条/批量/更新/删除/confirm-all）全收口；只读 9 端点（含 2 个 export）逐字节不变。
- 路由要点（沿用已验证 F17 铁律）：单条 create 用 `APIResponse.success` → **200**（与 subject create 201 区分）；route 调 service 后 `get_by_id` 重读构造响应规避 DetachedInstanceError；`IntegrityError`→400（并发唯一冲突）、confirm 异常→500、update 分数越界/格式→400（消息逐字节保留 "分数格式非法"/"成绩需在 0 ~ X 之间"）；状态机幂等/非法分支（已发布/已关闭/非草稿/非已发布）逐字节保留。
- **测试先行**：新建 `tests/test_exam_routes.py` 32 行为用例（考试 CRUD + 发布/关闭状态机 + 成绩 单条/批量/更新/删除/确认 + 错误分支）。基线 32 passed，迁移后同 **32 passed** 零漂移。
- 验证闸门：G1 py_compile OK（exam_routes.py + academics_service.py）；G2 RBAC `--check-only` exit0（权限目录 68 条）；G5 OpenAPI strict **461/461** 0 漂移；academics 全量回归（subject+exam+admin_classes+exam_import）**64 passed** 无回归。
- F17 累计 academics 域：**第1批 admin_classes+exam_import(12) + 第2批 subject(17) + 第3批 exam(20) = 49 处 db.session → 对应 service 函数**。剩 **academics-4 course_schedule/import（#598）** + 全闸门 #599 + users 域。
- 已知契约不一致（零漂移保留，独立整改）：approvals 详情缺 student_id/pending 缺 status；notifications 详情缺 student_id（F9-B）。

## F17 第十一批：academics-4（course_schedule + import 子批，#598 完成）
- **快照**：`backups/refactor_F17/course_schedule_routes_before.py`(59433B) + `import_routes_before.py`(14760B)。`academics_service.py` import 块补 `Admin, CourseSchedule, ImportConfig, ClassPeriod`（顺手修 execute_subject_import 引用 Admin 的 latent gap），追加 **第4子批 8 方法**：CourseSchedule `create/update/delete_course_schedule` + `apply_course_schedule_import(creates, updates)` 元组返 (created,updated)；ImportConfig `create/update/delete_import_config` + `set_default_import_config`（含 is_default 互斥清理）。
- **路由收口**：course_schedule_routes 4 处 db.session（create/update/delete + import 批处理）→ service；import 批处理原 `db.session.add(new)` 改 `creates.append({...})`、原 `existing.x=x` 改 `updates.append((existing.id,{...}))`、末尾 commit 改 `academics_service.apply_course_schedule_import(creates, updates)`。import_routes 6 处 db.session（create/update/delete/set-default）→ service；路由保留 get_or_404 + update 同名冲突检测 + `get_by_id` 重读构造响应。两路由移除 `db`/`datetime` 直接 import，grep 确认 `db.session` **零残留**。
- **双元组契约**：course_schedule create `return APIResponse.success(...), 201` 实测为双元组 `[envelope,200]`+外层201（既有契约，逐字节保留）；import create 用 `APIResponse.success(..., status_code=201)` 单 envelope+201（非双元组）。测试 `_envelope` 统一兼容 list/dict。
- **测试先行**：新建 `tests/test_course_schedule_routes.py`(11) + `tests/test_import_config_routes.py`(11)。初跑 19 passed/2 failed，修 2 测试断言后 **20 passed**：① 删 `test_create_schedule_missing_fields`（缺字段触发 KeyError 在 TESTING 模式逃逸、非契约）② 锁负向契约 `test_import_json_existing_slot_conflict`（同槽位 import 因 check_conflicts 不传 exclude_id 判冲突 → success_count=0/failed_count=1，原记录 teacher 仍"张老师"，**既有 latent 行为，零漂移保留**）。
- **验证闸门（#607，全绿）**：G1 py_compile OK（academics_service + 两 routes）；G2 RBAC `--check-only` exit0（68，未增权限码）；G3 create_app 冒烟 946 routes（course-schedules 8 / import 26 端点 intact）；G5 OpenAPI strict **461/461** 0 漂移；定向 pytest **52 passed**（course_schedule/import 20 + exam 32）。**不 git commit**。
- F17 累计 academics 域：**第1批12 + 第2批17 + 第3批20 + 第4批10 = 59 处 db.session → 对应 service 函数**（ClassPeriod 3 处只读暂缓，属独立子域未完成）。剩 **全闸门 #599（academics 整体） + users 域**。

## F17 第十二批：academics 整体全闸门（#599 完成）
- 汇总第1~4子批（admin_classes/exam_import/subject/exam/course_schedule/import）全量定向回归：**84 passed**（subject 20 + exam 32 + admin_classes/exam_import + course_schedule 11 + import_config 11），零回归。G1 py_compile / G2 RBAC(68) / G3 create_app 946 routes / G5 OpenAPI strict 461/461 全绿（与 #607 同）。
- F17 累计 academics 域：**59 处 db.session → 对应 service 函数**（services/academics_service.py 单文件承载）。ClassPeriod 3 处只读暂缓（独立子域，未动）。**不 git commit**。
- 下一步：**users 域**（F17 最后一域），按同样"快照→测试先行→service 薄封装→改写路由→闸门"范式推进。

## F17 第十三批规划：users 域（最后一域，待执行）
- 快照就绪：`backups/refactor_F17/users_routes_before.py`(56754B) + `user_management_routes_before.py`(10224B)。
- 写入路径分布：users_routes.py **15 处 db.session**，端点 POST /(create_user:311) / PUT /<id>(update_user:492) / DELETE /<id>(delete_user:572) / POST /import(JSON:655) / POST /batch-delete(787) / POST /batch-score(819) / POST /import-file(CSV:961)；user_management_routes.py **1 处**（admin/学生状态类，待读）。
- 拟子批：① students CRUD(create/update/delete + 删除级联) ② import 三路径(JSON/CSV/batch-delete/batch-score) ③ user_management 1 处。services 新建 `services/user_service.py`（users 域无既有 service）。沿用 F17 铁律：测试先行、逐字节复刻响应体/状态码/错误、路由保留 get_or_404+请求级校验+响应构造。

## F17 第十四批：users 子批1（students CRUD，#609 完成，前序）
- 新建 `services/user_service.py`（create_user/update_user/delete_user 三方法，db_session_scope 包裹，级联清理复用 models.cascade_delete_user_related_records）；users_routes.py 三写端点收口 service，FTS/操作日志/缓存失效/响应构造逐字节保留。db.session 15→10；12 既有 CRUD 测试零回归。

## F17 第十五批：users 子批2（import/batch/toggle，#610 完成，本批）
- 快照已就绪（#608）：`backups/refactor_F17/users_routes_before.py`(56754B) + `user_management_routes_before.py`(10224B)。追加 `services/user_service.py` **5 方法**：bulk_create_users / bulk_delete_users / bulk_score_update / apply_csv_import / toggle_active。
- 路由收口：
  - `/import`(JSON) 原 `db.session.add`+`commit`+`rollback` → `pending_users` 列表 + `bulk_create_users`（冲突仍由 service 内 db_session_scope 回滚并上抛，路由错误文案 `导入失败：数据库写入冲突（...），已回滚` 逐字节保留）。
  - `/batch-delete` 原 delete-in-loop+`commit` → `target_ids`（路由做 `_can_access_user` 过滤）+ `bulk_delete_users`（级联同事务）；`deleted_count` 由返回条数构造，消息逐字节 `批量删除完成: 成功N条`。
  - `/batch-score` 原 `update` execute+`add_all(ScoreRecord)`+`commit` → `bulk_score_update(allowed_ids, score_change, description)`，`updated_count` 返路由供 MQTT 通知/响应（路由内 `datetime.now().isoformat()` 时间戳保留）；移除路由内 `from sqlalchemy import update`/`from models import ScoreRecord` 局部 import。
  - `/import-file`(CSV) 原 `existing.x=x` 原地 setattr + `db.session.add(user)` + `commit` → `pending_updates=[(id,{field:value})]`（仅非空新值入 dict，与原 `x if x else existing.x` 语义逐字节一致）+ `pending_users` + `apply_csv_import`（updated_at 由 service 置位）；响应体 total/success_count/failed_count/imported/updated/errors/messages 逐字节不变。
  - `user_management_routes` `ToggleUserActive` 原 `is_active` 翻转+`commit` → `toggle_active`（None→404，路由据此返"用户不存在"）。
  - 两路由移除未用 `db` import；grep 确认 `db.session` **零残留**（users_routes 0 + user_management_routes 0）。
- **测试先行暴露并就地修复 1 处既有缺陷**：users_routes 用 `re.match` 做 phone/father_phone/... 校验但从未 `import re` → CSV 导入任意带手机号行触发 `NameError: name 're' is not defined` 500（JSON /import 因此前测试未传 phone 而漏检）。补 `import re`，零风险修复，与 notifications 子批"测试先行暴露只读 500 缺陷"同范式。
- **测试先行**：新建 `tests/test_user_import_routes.py` **11 行为用例**（import JSON 成功/库内重复/空、batch-delete 成功/空、batch-score 成功/空ids、import-file CSV 成功/无文件、toggle-active 成功/不存在）。基线 11 passed，迁移后同 **11 passed** 零漂移。
- 验证闸门（全绿）：G1 py_compile OK（user_service + 两 routes + 测试）；G2 RBAC `--check-only` exit0（68，未增权限码）；G3 create_app 冒烟（live 后端 5000 启动干净，461 路径全注册）；G5 OpenAPI strict **461/461** 0 漂移；定向 pytest **11 passed**（新）+ 既有回归 test_users_routes 12 + test_user_management_routes 5 + test_user_routes 12 全绿。**不 git commit**。
- F17 累计 users 域：**子批1 5 处 + 子批2 10 处 = 15 处 db.session → 对应 service 函数**（users_routes 0 + user_management_routes 0 残留）；加 `import re` 缺陷修复 1 处。
- **users 域（F17 最后一域）整体完成。**

## 第十六批（2026-08-17 晚）：修复所有缺陷 + 继续推进 F17
- 用户指令："修复所有缺陷，并继续推进"。先压缩 MEMORY.md（60→58 行，F17 逐域日志合并为摘要表，去除冗余）。
- **修复 3 处已知契约不一致（测试先行）**：
  - approvals 详情补 `student_id`（list 早有，detail 缺）；pending 列表补 `status`（全量列表有，pending 缺）。更新 `test_approvals_routes` 断言（先跑红 KeyError，再改路由跑绿）。
  - notifications 详情补 `student_id`（list 早有，detail 缺，F9-B 残留）；更新 `test_notifications_routes::test_get_notification_detail` 断言。
  - 三契约测试 3 passed；approvals 全量 16 passed；notifications 全量 16 passed。
- **approvals approve/reject 内联通知收口**：6 处 `db.session`（Notification 创建+commit/rollback）→ `notification_service.create_approval_result_notification(user_id,title,content)`（status='sent', recipient_type='user'，与原逐字节一致）；路由 try/except 吞异常不阻断审批。移除路由内 `db, Notification` import。
- **继续推进（续推 2 个写入路径）**：
  - alerts `PUT /<id>`：原路由内 `is_read/read_at` 赋值+`commit` → `alert_service.update_alert_status(alert_id, is_read)`；移除 `datetime`/`db` import。新建 `test_alerts_routes.py`（3 用例，含 404/缺字段 no-op 200 零漂移）。
  - notify_history `DELETE /clean`：原 `filter(...).delete()+commit` → 新建 `services/notify_history_service.clean_notify_history(days)`；移除 `db` import。强化 `test_notify_history_routes::test_clean_notify_history`（断言 deleted_count 整数 + 旧删新留）。
  - 两路由 `db.session` 零残留；快照 `backups/refactor_F17/{alerts,notify_history}_routes_before.py`。
- 定向：alerts+notify_history+alert_service **34 passed**；既有回归无影响。
- **全闸门（重启后端跑 `run_regression.sh`）**：G2 RBAC OK / G5 OpenAPI 461/461 / 契约 2 passed / 关键路由 33 passed / **全部回归通过**。
- 不 git commit。剩余 backlog（见 MEMORY"剩余可续推"）：scores 子路由(rules/records/notify_template/scheduled_notify)、users(rbac/sub_accounts)、system(admins/security/admin_notifications/system_routes)、nlp、data/import_export 等写入路径 + 意图保留的 import 回滚/只读聚合兜底。

## 第十七批（2026-08-17 晚）：scores/notify_template_routes 迁移
- 4 写路径（POST create / PUT update / DELETE soft-del / POST use）内联 db.session → 新建 `services/notify_template_service.py`（create_template/update_template/delete_template/record_template_usage 薄封装；log_operation 移入 create_template；逐字节复刻落库行为）。
- 路由仅保留 get_or_404 / 请求校验 / ClassTimeChecker 上课时间拦截 / publish_mqtt 下发 / APIResponse 响应构造；移除未用 `db`/`NotifyHistory`/`log_operation` import（`db` 仍用于 GET /categories 只读聚合，按 F17 规则保留不动）。
- 快照 `backups/refactor_F17/notify_template_routes_before.py`；新建 `tests/test_notify_template_routes.py`（9 用例，use 端点 mock `publish_mqtt` + `ClassTimeChecker.is_broadcast_blocked`/`is_notification_allowed` 确定性，断言 NotifyHistory 落库 + usage_count 递增）。
- 测试先行：先对未迁移代码跑绿建基线 **9 passed**；迁移后重跑 **9 passed** 零漂移。
- 全闸门：G1 py_compile OK / G2 RBAC 68 OK / G5 OpenAPI 461/461 OK / 定向 9 passed。不 git commit。
- 当前 backlog（真实 grep 剩余写入路径）：scores/scheduled_notify_routes:13、users(sub_accounts 8 / rbac 34)、system(admins 17/security 15/admin_notifications 9/system_routes 7)、nlp 6、data/import_export 10；意图保留不迁移：scores/rules_routes:3、scores/records_routes:2、academics/exam_import_routes:1、devices/devices_routes:2、operation_logs 2 读、ClassPeriod 3 读。

## 第十八批（2026-08-17 深夜）：scores/scheduled_notify_routes 迁移
- **测试先行暴露并就地修复 1 项历史缺陷**：`ScheduledNotify` 模型声明与库表均缺 `created_by` 列，而路由 POST create 早已 `created_by=_admin_id or 1` → create 端点恒 `TypeError` 500。补 `models/notify_models.py::ScheduledNotify.created_by`（与兄弟表一致，注释标明历史遗漏）+ ALTER 库表 `instance/score_management.db`（SQLite ADD COLUMN 元数据级，安全；根 `score_management.db` 无 scheduled_notifies 表，跳过）。与 notifications 子批"测试先行暴露只读 500 缺陷"同范式。
- 4 写路径（POST create / PUT update / DELETE / POST cancel）+ trigger 成功落库 + 后台任务 `process_scheduled_notifications` 单条落库 → 新建 `services/scheduled_notify_service.py` 6 函数薄封装（create_scheduled_notify/update_scheduled_notify/delete_scheduled_notify/cancel_scheduled_notify/record_scheduled_notify_sent/record_scheduled_history）；`calculate_next_send` 由路由迁至 service（纯函数）；`record_scheduled_notify_sent` 自带提交+回滚、`record_scheduled_history` 仅加 history 不提交（事务边界留给后台任务最终统一 commit）。
- 路由保留：get_or_404（404 语义）、请求校验、跨切面副作用（publish_mqtt / ClassTimeChecker 上课时间拦截 / log_notify_audit）、响应构造；trigger 防御性 `db.session.rollback()`×2 与后台任务 `rollback`/`commit` 事务边界保留（非写路径建模）。13 → 4 db.session 残留（全为防御性/事务边界）。移除路由内 `NotifyHistory`/`log_operation`/`timedelta` 未用 import。
- 快照 `backups/refactor_F17/scheduled_notify_routes_before.py`；新建 `tests/test_scheduled_notify_routes.py` **10 行为用例**（list/create/detail/update/delete/cancel/trigger 成功+上课拦截+无强制权限/process 后台任务；trigger 与 process mock `publish_mqtt`+`ClassTimeChecker`+`has_permission` 确定性，断言 NotifyHistory 落库 + status 推进）。
- 测试先行：先对未迁移代码跑红（9 failed 因 created_by 缺陷）→ 修缺陷后 **10 passed** 基线；迁移后重跑 **10 passed** 零漂移。
- **全闸门（强杀重启后端跑）**：G1 py_compile OK（routes+service+model） / G2 RBAC `--check-only` exit0（68，未增权限码） / G5 OpenAPI strict **461/461** 0 漂移 / 定向 pytest **10 passed**。**不 git commit**。
- F17 剩余 backlog（最新真实 grep）：users(sub_accounts 8 / rbac 34)、system(admins 17/security 15/admin_notifications 9/system_routes 7)、nlp 6、data/import_export 10。
