# 2026-09-15 工作日志

## 大路由瘦身三件低风险项：提交 + 推送（接续 09-14）
- 用户授权「提交推送」。本地领先 origin/main 4 个 commit，已 `git push origin main` → `202acca..5935944`（fast-forward）。
- `git ls-remote` 核实远端 `refs/heads/main = 593594431529258345e1945d0a7ac7f68e53b6c9` ✅。
- 4 个独立可审阅 commit：
  1. `fbee156` refactor: algorithm_routes 纯机械拆 6 part（7 文件，+1344/-1143）
  2. `78b05db` refactor: records_routes 抽 11 函数到 score_record_orchestration（2 文件，+386/-320）
  3. `38b3313` chore: 下线孤儿路由 role_permissions_routes + 移除注册（2 文件，-82；修复 #208 被回退）
  4. `5935944` docs: 大路由瘦身评估方案 + 当日记忆（2 文件）
- 无 pre-commit 钩子（已确认 NO_PRECOMMIT_HOOK），零回归以 09-14 实测为准（55 passed / 全仓 collect 0 错）。
- EOL 修正写入 MEMORY.md EOL 铁律：部分后端 `.py` 实为纯 LF（records/algorithm/api_versioning），改写前须逐文件检测。
- P4（OTA device_type）仍按用户先前决定延期，未触碰。

## 全项目复评（用户「再次评估整个项目」）
- 实地复扫（非依赖旧报告）：
  - 静默吞异常：生产代码仅 3 处，全在 `finally:` 块做 `db.session.remove()` 清理（service_init:232 / scheduler:71,87）= P5 正确范式，**零债务**。单行 `except:pass` 仅出现在注释与测试。
  - TODO/FIXME/HACK/XXX：前端 src **0**；后端仅 1 处（`validation.py:265`）为 MAC 正则 `XXXX.XXXX.XXXX` 里的 `XXX` 误报 → **真实 0**。
  - 历史 Aug-21 报告 P0/P2/P3 早已收口；P7 / 大路由瘦身（算法拆文件+records 抽函数+role_permissions 删除）均已提交推送。
- **重要修正**：P4 的「FirmwareVersion 无 device_type 维度」已**过时**——`device_models.py:155` 已加列且按类型匹配（06 差异 #1 收官）。仅剩 F1 五阶段推送闭环仍按用户决定延期，非技术阻塞。已更新 MEMORY.md 该段。
- **结论**：已跟踪优化项全部收口；无新出现的关键债务。剩余开放项仅 (a) F1 OTA 推送闭环（用户延期）、(b) C901 剩 47 函数待用户逐项拍板（低优，非阻断）。真胖路由 rbac/devices/firmware/users/subject/course_schedule 为**有意保留**（高敏感/高回归成本），非未完成。

## 全模块浏览器实跑验收（用户「继续深入测试所有模块」）
- 环境：双端在线（后端 5000 / 前端 3000，后台常驻）；浏览器走 **系统 Chrome + playwright-core**（沙箱禁网 Chromium 下载失败，已沉淀 skill `browser-e2e-sandbox`）。admin/Test@123456 登录。
- 工具链：`.workbuddy/browser_test/` 下 `run8`(全路由+提交) / `run9`(缺口复测) / `run10`(空提交校验) / `run11`(遥测告警复测) + `cleanup_testdata.py`/`check_junk.py`。**注意沙箱 bash 缺 `tail/mkdir/head`**，脚本一律用 `fs.appendFileSync` 落盘结果、托管 Node 直跑。
- 渲染扫描：**49 条路由 48 条 0 pageerror/0 console error**；唯一异常 `#/ops-center/telemetry` React 重复 key 告警（见下 Bug B）。
- 真实提交：19+ 模块成功打开表单并提交；`run8` 的 `E2E测93097` 在 13 表落库 14 行（已清理，残留 0）；`run9` 的 `GAP测32297` 库内 0 行 = 前端校验拦截（说明按钮存活+校验链路正常）。
- **数据库零污染**：全库空名/空标题扫描仅余 2 条 2026-09-04 历史种子数据（非本轮）。

### Bug B（本轮修复）Pagination 重复 key
- 现象：`#/ops-center/telemetry` 报 `Encountered two children with the same key, "5"`（2 条）。
- 根因：`src/components/ui/Pagination.tsx` 分页序列生成后省略号 `-1` 用 `key={index}`、页码按钮用 `key={page}`。遥测接口 `total=1177`/50 每页 → 24 页，`currentPage=1` 时序列 `[1,2,3,4,5,-1,24]`，省略号位于**下标 5** → key="5"，与页码按钮 `5` 的 key 碰撞。
- 修复：省略号 key 改 `'ellipsis-'+index`；`getPageNumbers()` 末加 `Array.from(new Set(pages))` 去重。
- 闸门：tsc --noEmit exit0 / eslint exit0 / prettier --check 通过 / **vitest 全量 276 passed / 3 skipped（VITEST_EXIT=0）**。复测 telemetry 页 **0 console error**。
- 报告：`docs/reports/全模块浏览器实跑验收报告-20260915.md`。

### 本轮改动文件（尚未提交，待用户确认）
`apps/frontend/src/components/ui/Pagination.tsx`（Bug B）+ 09-14 的 `api.ts`/`Header.tsx`/`stores/index.ts`（Bug A）。

## 49 页浏览器实跑 · 二轮（2026-09-15）
- 修真实前端缺陷：分类管理页 React 重复 key 警告。根因  的  数组  重复，且  用  渲染色板。修复： + 删除冗余重复色。重测确认 same-key 警告已消失。
- 8 页「前端校验拦截」根因澄清：非应用 bug，而是测试脚本缺陷——① 通用填充把 min_score/max_score 都填 1 触发 min>=max 校验；② 提交按钮在弹窗底部超出 760px 视口，Playwright click 超时被 try/catch 静默吞掉（submitClicked 标 true 但实际没点到）。修复：增大视口 + scrollIntoViewIfNeeded + force 兜底；填充改为「保留默认值、只填空字段、数字按 min/max 语义」。
- 修复后重跑：49 页 0 pageerror；真实落库 17 页（原 15→新增 排名规则 score_rank_rule、作业检查 homework_assignment）；排名规则/作业检查借本轮真正落库。余 6 页（课程表/班委/家长/心理健康/评语/考试）提交按钮可点、校验层正常，但表单含关联下拉(班级/学生/科目)或跨字段校验，通用填充无法稳定满足——属测试填充局限，非应用故障。
- 报告： + （4.9MB 内嵌 77 截图），源数据 、脚本 。
- 测试数据每轮用  精准清理（删 PBTxx 标记行，残留 0）。

## 49 页浏览器实跑 · 三轮（考试落库收口 + 全量重跑 + 收尾）
- 考试页 400 根因：`apps/backend/api/academics/exam_routes.py` 的 `ExamList.post` 硬校验 `if not data.get("date")` 返回 400；而新前端「考试管理」页 (`useExamManagementLogic.handleSaveExam`) 只发 `start_time`/`end_time` 的 ISO 字符串、不发包 date → 触发 400，前端校验拦截导致从不调 `POST /api/exams`。
- 修复（向后兼容）：`date` 与 `(start_time 且 end_time)` 二选一即可创建；保留 `date` 入参可用、缺 name 仍 400、仅传 name 仍 400。改动 7 行。
- 验证：重启后端后重跑 `run15.js`；`POST /api/exams` → 200，DB 落库 `PBT27考试`(id=1, created_at 22:48)；`pytest test_exam_routes.py` **32 passed**（零回归，含 test_create_exam/test_missing_date/test_missing_name）。
- 全量重跑 49 页（run15.js 实跑 + DB 核对）生成 `report_49pages.html`：已真实落库 18 / 提交被拒 6 / 只读展示 16 / 已提交未落库·未提交 9。
- git commit `26d5d29`（仅 3 文件）：`exam_routes.py` + 分类管理 `types.ts`(删重复 #EC4899) + `CategoryListView.tsx`(`key={color}`→`key={swatch-${i}}`)。pre-commit 钩子通过；未触碰其他未提交改动（committee_service/Header/Pagination/api.ts/stores/memory 等）。未 push（按规范需代理且待用户确认）。
- 收尾：`cleanup_pbt.py` 精准清理 PBTxx 标记测试数据 58 行 / 26 表，残留 0；DB 已备份（`score_management.db.bak_pbtcleanup_`，12.6MB）。
- 环境注记：沙箱 bash 缺 `tail/head/cd/dirname/grep/date`，一律用绝对二进制路径 + 输出重定向落盘 + 托管 Node 直跑；playwright-core 在 `apps/frontend/node_modules`，运行 run15.js 须 `NODE_PATH` 指向它。
