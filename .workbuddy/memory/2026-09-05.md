# 2026-09-05 工作日志

## 班主任工作台（历史主线）· A/B 批推进
- **A 批（字段命名/骨架核对面）结论：无实质 outlier**（MentalHealth/TeacherComments/CommitteeList 抽样：多词键均 snake 对齐后端契约、camel 侧全单字词；骨架组件 useWorkbenchClass/CurrentClassLabel/WorkbenchBreadcrumb/Pagination 既有约定已覆盖）。**总览聚合页已存在**（WorkbenchOverview.tsx 498 行，6 路 Promise.allSettled 真实取数 + 12 卡片）——纠正"无聚合首页"旧记忆。
- **B 批第 1 条收敛落地**：Sidebar 菜单权限与路由守卫同域对齐（原 class.view 冗余差异）——实际 **5 项**非 4：study-groups→study_group.view、mental-health→mental_health.view、activity→activity.view、culture→culture.view、study-guide→study_guide.view（teacher-comments 本就 comment.view 一致）。权威核实 rbac_service head_teacher 兼有 class.view 与全部域 *.view → 纯前端属性一致性、零角色/后端影响。eslint+tsc 全绿。
- 遗留：B 批全量 13 页三方矩阵只读证据表未做；C 批交互增强待拍板。

- **B 批全量 13 页三方矩阵（只读证据表）完成 → 全部一致**：菜单(5cfe6e4 收敛后) == 路由守卫(PermissionGuard) == 后端域权限（api/class_management/*: seating/duty/committee/parent=class.view|edit；homework=homework.view/edit/check；attendance=attendance.view/edit/approve；study_group/mental_health/activity/culture/study_guide=对应 .view/.edit；teacher_comment=comment.view/edit；phonebox=phonebox.unlock.manage）。前端仅 gate view 级，写权限由后端 enforce——无越权空隙。

- **C-1 指标卡下钻落地**：WorkbenchOverview 7 张指标卡（出勤率/待完成作业/未处理预警/学习小组/文体活动/值日组/迟到请假）→ 对应功能页 Link（attendance/homework-check/mental-health/study-groups/activity/duty-roster），权限不足时不渲染链接（hasPermission 门控），hover 微交互+focus ring。eslint+tsc 全绿。（预筛选目标页参数留作后续增量。）

- **C-2 下钻带预筛选（作业页首个打通）**：HomeworkCheck.tsx 新增 `?status=pending|done` URL 参数初始化 statusFilter（useSearchParams），工具栏加 全部/进行中/已完成 分段 chips（aria-pressed 可达）；useClientFilter predicate 并入状态条件（deps 同步）。WorkbenchOverview「待完成作业」卡下钻改 `/homework-check?status=pending`。eslint 0 error/tsc 全绿（1 个既有 hwPages 未用变量警告未引入未处理）。其他 6 卡预筛选待后续按需扩展（模式已确立）。

- **C-2 扩展：心理健康「未处理预警」下钻**：MentalHealth.tsx 支持 `?view=alerts&resolved=0|1`——初始 tab=alerts、服务端 isResolved 过滤（getAlerts 第二参接通），alerts 内容区新增 全部/未处理/已处理 分段（aria 可达），切过滤重置分页。总览「未处理预警」卡下钻 `/mental-health?view=alerts&resolved=0`。eslint 0 error/tsc 全绿（recordPages/alertPages 2 个既有未用变量警告非本次引入）。⚠️ 补丁过程中 Overview 曾被整体翻 CRLF（LF 原文件），已还原，净 diff +1/−1。

- **C-2 扩展③：文体活动「已发布」下钻**：ActivityManage.tsx 支持 `?published=1|0` → 服务端 isPublished 过滤（getAll 第二参接通）+ 工具栏 全部/已发布/未发布 分段 + 切换回第 1 页。总览「文体活动」卡下钻 `/activity?published=1`。eslint 0 error/tsc 全绿（activityPages 既有未用变量警告非本次引入）。
- **C-2 四卡评估结论**：考勤(AttendanceManage 1056L 复杂日期/状态表单)、学习小组(仅 searchTerm)、值日(DutyRoster 无状态维度) → **无高价值预筛选维度，保持 C-1 纯导航**（已留档）；活动页因存在 isPublished 语义落地。C-2 全部 7 卡处理完：2 卡带参（homework/mental/activity 计 3 卡带参）、4 卡纯导航。
- **全量 pytest 基线（aQ60Zi，09-05）**：2066 passed / 7 skipped / 0 failed（25min，三要素通过；report 162 collectible 文件 == 磁盘 collectible，排除 __init__/data_sync_validation/class_management_api 脚本式三文件）。

- **A 轨试点✅：FrontendTelemetry 双列表迁 useListFetch**（净 −25 行）：移除手写 metrics/errors/perfTotal/errTotal/perfLoading/errError+两个 loadCB+两个 useEffect；perf/err 各接 useListFetch（params 注入 page/pageSize/过滤，debounce 250，error/refetch 透出给 DataTable）。eslint/tsc 全绿，残留引用 0。A 轨剩余 14 页手动分页可照此模式逐页小批推进（每页信封/过滤语义各异需单独适配）。

- **A 轨续批✅：FirmwareManagement versions 列表迁 useListFetch**：删 versions/setVersions/versionsTotal 手工态；getVersions 从 loadData 三路 Promise.all 拆出独立 hook（params page/pageSize），4 个刷新点补 versions.refetch（上传/删除/启停/手动刷新）；表格/分页/头部计数改 versions.items/.total（头部原显示当页 length→改 total 语义更准）；DataTable 补 loading。eslint/tsc 全绿。评估留档：Notifications 乐观更新密集（本地 total 调整+竞态序号）不适合本钩子，暂缓。

- **A 轨续批②：ActivityManage 列表迁 useListFetch**：删 activities/setActivities/activityTotal/activityPages/isLoading 手工态与 fetchActivities 手写体；hook params={page,pageSize:50,isPublished} 直通 getAll 第二参（失败 toast 保留在 fetcher 内 catch+rethrow）；既有 4 处 handler 经包装 fetchActivities=refetch 语义不变；filteredActivities 基座改 items；loading/分页/总数改 hook 输出。eslint/tsc 全绿。踩坑：ListFetchParams 值域宽→isPublished 需显式 Boolean 收窄；CRLF 文件禁用 Edit。

- **A 轨续批③：HomeworkCheck 作业列表迁 useListFetch**：删 assignments/hwTotal/hwPages/isLoading+fetchAssignments 手写体；hook params={page,pageSize:50,classId}（class_id 服务端过滤，0→undefined）；status/search 过滤保持页内 useClientFilter；统计(总数/已完成)/DataTable loading/antd 分页改 hook 输出；既有 handler 经包装 fetchAssignments=refetch。+29/−34。eslint/tsc 全绿。

- **A 轨续批④评估（Approvals/RuleList）→ 均驳回机械迁移，留档**：Approvals 审批乐观更新（status map）+ activeIndex 联动 + 批量失败 UI + 详情取数，迁移须行为妥协；RuleList 增删本地乐观（prepend/filter）+ 防御分支（Array.isArray 兜底）+ is_active 过滤特殊语义 + templates 模态多态。两页与 useListFetch「纯服务端取数」语义冲突，强行迁会改交互或引入复杂 onMutate。**结论：A 轨本轮收口于 4 个干净页**（FrontendTelemetry/Firmware/Activity/Homework），剩余重页如需迁移应先扩展 hook 支持 onMutate/replace 或以专项（带行为评审）推进。

- **A 轨续批⑤：ParentContact 家长联系人列表迁 useListFetch**：删 contacts/contactTotal/contactPages 手工态；class_id 服务端过滤入 params（0→undefined）；isLoading 拆两层——列表 loading 用 hook 输出、**共享 busy（表单提交/按钮禁用）保留独立 isLoading**（首次踩坑已修）；StatCard/空态门/映射/antd 分页改 hook；既有 handler fetchContacts=refetch 包装。+31/−33。eslint/tsc 全绿。

- **A 轨续批⑥评估（ClassManagement）→ 驳回**：fetchClasses 带命令式参（page/keyword/skipCache/perPage），import 需 skipCache 缓存失效 + pagination.pages 门控 + searchInput 独立触发；与 useListFetch 声明式 params 模型不匹配。**A 轨结案（2026-09-05）：5 个干净页已迁移**（FrontendTelemetry/FirmwareManagement/ActivityManage/HomeworkCheck/ParentContact），其余手动分页页均为重型/乐观/命令式（Approvals/RuleList/Notifications/ClassManagement/NLPManagement/RemoteNotify/Permission/WakeOnLan 等），需先扩展 hook（onMutate/replace/skipCache 语义）并以专项（带行为评审）推进。

- **hook 扩展专项✅（657ce41）**：useListFetch 新增乐观覆写 `setItems/setTotal/mutate`（override 层，服务端新 data 到达自动让位 via useEffect[data]）；单测覆盖覆写即时生效+refetch 让位。**修复 FrontendTelemetry fetchJson null 语义**：失败返回 null（非抛错）→ fetcher 转抛使 hook error 态可见（保 T11 错误分支测试），含一次误诊（先误判为 waitFor 时序，实为 null 语义）。全量 vitest 271 passed/3 skipped EXIT=0；tsc/eslint 全绿。**为乐观更新重页（Approvals/RuleList/Notifications）迁移扫清障碍**。

- **乐观更新页试点✅：Notifications 迁 useListFetch + mutate API**（-98/+58）：移除手写 pagination{page,per_page,total,pages} 状态/seq 竞态/loading；filter+admin_id 入 params（hook abort 竞态取代 seq）；markRead/markAllRead/delete(含末页回退)/send(前置插入+total+1) 全走 list.mutate/setTotal；totalPages 由 total 派生。中途踩坑：JSX 花括号残留、漏 write（两阶段脚本只 assert 未落盘重写）。eslint 0 error 0 warning + tsc 全绿 + 全量 vitest 271 passed EXIT=0。

- **重页迁移①✅：Approvals 迁 useListFetch + mutate**（+62/−62，827L 保持 LF）：移除 approvals/loading/pagination{page,per_page,total}（page/pageSize/sort 本就由 useTableUrlState 提供，pagination 仅承载 total）+ 手写 fetch/effect；params 声明式注入 page/pageSize/status/sortBy/sortOrd（fetcher 内完全从解构参取值，避开闭包 stale），total 保留后端 pagination.total 语义（含 Array 兜底分支）；approve/reject 乐观更新走 list.mutate，批处理仍走 loadApprovals=refetch 包装（返回 Promise<void> 以兼容 await）；activeIndex 收敛与键盘导航改 list.items。actionLoading 共享 busy 保留。eslint 0e0w + tsc + 全量 vitest 271 passed/3 skipped EXIT=0。

- **重页迁移②✅：RuleList 迁 useListFetch + mutate**（+48/−58，1110L→1100L 保持 LF）：取证发现本页**无分页 UI**（pagination 仅取数用、page 恒 1/per_page 恒 50、total/pages 存而不渲染）→ 删 rules/isLoading/pagination 三态 + Pagination 接口，改常量 RULE_PER_PAGE=50；fetcher 保留 Array.isArray 防御分支与 setError 文案（catch 后 rethrow 交 hook）；create→mutate 前置插入+total+1、delete→mutate 过滤+total-1；filteredRules 基座/骨架门/空态/刷新按钮改 hook 输出；分类过滤同时走服务端 category_id。eslint 0e0w + tsc + 全量 vitest 271 passed/3 skipped EXIT=0。踩坑：catch 内 showToast 缩进为 8 空格（非 6），锚点失配一次（未落盘即中止，无副作用）。

- **重页迁移③✅：SemesterReport 班级下拉迁 useListData**（+16/−17，144L 保持 LF）：该页非分页列表（一次性 200 条下拉选项）→ 用**全量钩子**而非 useListFetch；删 classes/loadingClasses 两态与首 useEffect，onError 回调复用页面 error 展示（导出错误态共用同一 Alert，行为不变）；selectedClass memo 基座/deps、Select 的 loading/placeholder/options 改 classes.data/.loading。eslint 0e0w + tsc + 全量 vitest 271 passed/3 skipped EXIT=0。**选型铁律补记**：分页列表→useListFetch；全量下拉/选项列表→useListData（data 恒数组，免判空）。
- **重页迁移盘点（本轮 3 页：Approvals/RuleList/SemesterReport）后剩余**：NLPManagement(1341L,mut4)、RemoteNotify(939L,mut4)、WakeOnLan(427L,mut5)、StudentPortal(891L,mut3) 为乐观更新重页（mutate 可覆盖，需逐页取证）；ClassManagement(1452L) 命令式 fetch（skipCache/keyword 参数式）暂不适用。

- **重页迁移④✅：WakeOnLan 迁 useListFetch + mutate**（+35/−40，427L→422L 保持 LF）：删 devices/isRefreshing/loadError/wolTotal 四态与 mount useEffect；`loadDevices(page)` 命令式参数 → `params{page:wolPage,pageSize:wolPageSize}` 声明式（handleWolPageChange 只剩 setWolPage，hook 自动重拉）；addDevice/deleteDevice 乐观增删走 mutate（含 total ±1）；loadError→list.error（诚实空态语义不变）、isRefreshing→list.loading，**isLoading 共享 busy（唤醒/批量/增删）保留**；漏改点：handleWakeAll 的 validMacs 仍读 devices（tsc 抓出）。eslint 0e0w + tsc + 全量 vitest 271 passed/3 skipped EXIT=0。**模式沉淀**：命令式 `load(page)` + 手工 `setPage + load(page)` 属"伪命令式"，可直接声明式化（与 ClassManagement 真正的 skipCache 语义不同）。

- **重页迁移⑤✅：RemoteNotify 双列表迁钩子**（+47/−46，939L→940L 保持 LF）：**模板（全量）→ useListData**、**定时通知（分页）→ useListFetch**；删 templates/templatesLoading/scheduledNotifications/scheduledTotal 四态；初始 effect 移除两路显式调用与依赖（hook 自驱）；Array/信封两种形态兼容分支、loadError 三路共享语义均保留（fetcher 内 setLoadError 成功 false/失败 true 后 rethrow）；输出对象字段改 `templates.data/.loading`、`scheduled.items`；分页 totalItems/totalPages 改 scheduled.total（漏改一处由 tsc 抓出）。**history 列表保持手写**：受 `showHistory` 条件拉取 + stats 联动，hook 无条件自驱会导致切 tab 闪空，需先给 hook 加 enabled/skip 语义再迁（已留档）。eslint 0e0w + tsc + 全量 vitest 271 passed/3 skipped EXIT=0。

- **A 轨收尾✅：NLPManagement + StudentPortal 迁移，A 轨终态闭合（2026-09-05 晚）**
  - **NLPManagement rules 迁 useListFetch**（1341L 主壳改 5 处，子模块 RulesTab/CorrectionsList **零改动**——二者经 deps 字段名解构，主壳 deps 里手写 state 换成 hook 输出即透传）：删 rules/rulesLoading/rulePage/ruleTotal 四态 + fetchRules 手写体 + mount useEffect；hook params={page:rulePage,pageSize:20,keyword,score_type,sort_by:'usage_count',sort_order:'desc'}（fetcher 内从解构参取值避闭包 stale）；keywordFilter/scoreTypeFilter 清空联动 setRulePage(1) 保留（声明式 page 变更自动重拉）；fetchRules 保留为 mutation 后重拉包装，**签名 `() => Promise<void>`（tsc 抓出：原 () => void 与 NLPDeps 契约不符，改 return rulesList.refetch()）**。**corrections 列表保持手写（留档）**：模态按需命令式 fetchCorrections(page)，hook 无条件自驱语义不符。
  - **StudentPortal records 迁 useListFetch**（891L→~870L）：删 records/setRecords/pagination{page,per_page,total,pages} 态；**loadScore 复合加载拆分**——score 主请求保持原 loadScore，records 拆独立 fetcher 入 hook params={page,pageSize:20}（fetch 信封 key：recRes.data 数组 + recRes.pagination.total 取 total，fetcher 内 res?.pagination?.total ?? 0）；tab 切回 score 时 setRecordsPage(1)（原 loadScore(1) 伪命令式 → 声明式 page 重置自动重拉）；刷新按钮 loading 改 scoreRecords.loading、onClick 改 scoreRecords.refetch；分页上下页按钮 page 源改 scoreRecords 输出、disabled 由 totalItems 派生；totalChange 统计基座改 scoreRecords.items。**notifications 保持手写（留档）**：无分页 UI 仅取首页 + 复合加载，收益低。⚠️ 主壳 `loadScore` 不再含 records 但函数名未改（含 score 主请求，语义仍准确）。
  - **回归证据**：eslint 两页 0e0w + 全量 tsc EXIT=0 + 全量 vitest **Test Files 38 passed/1 skipped(39)、Tests 271 passed/3 skipped、Errors 0、EXIT=0**——与 A 轨基线一致；三要素核对 reported=39 == disk=39，comm 差集空（踩坑：Windows vitest 日志路径为反斜杠 + 组件测试为 .test.jsx，提取正则须 s|\\\\|/|g 归一 + 含 jsx，否则误报 6 个"缺失"实为提取漏抓）。
  - **A 轨终态**：迁移收口于 13 页——FrontendTelemetry/FirmwareManagement/ActivityManage/HomeworkCheck/ParentContact/Notifications/Approvals/RuleList/SemesterReport(useListData)/WakeOnLan/RemoteNotify/NLPManagement/StudentPortal。留档待专项：ClassManagement（命令式 skipCache/keyword）、RemoteNotify history（需 enabled/skip 语义）、NLPManagement corrections、StudentPortal notifications（按需加载，同因）。**全工作区 git 仍 0 变更（仅本地文件改动未提交，HEAD 526f6e3 == 远端 main）**。

- **B3 战役终态确认（2026-09-05 晚，零代码改动）**：启动取证即发现 **B3 已在此前会话全部收口**，记忆状态滞后于代码。证据：① 模型层 User.to_dict(fields=None)(user_models.py:42) + ScoreRecord.to_dict(fields=None)(score_models.py:102，二级派生防 DetachedInstanceError) 08-23/08-29 已就绪；② 路由层 users/user_management/student/box/export/records 域全部 `x.to_dict(FIELDS)` 收敛，role/score 别名由路由补；③ serialize_score_record 已薄转发 `return r.to_dict()`。全量残留命中逐一判定为**非 B3 范畴**：users_routes before_data 审计快照 ×2、approvals/records 的 MQTT 设备协议消息 ×3（字段名 new_points 等协议专用）、course_schedule/subject 的 s 为 Subject 非 User ×2。**未新增任何代码** → 无需回归。已更新 MEMORY.md（勿再启动 B3 取证）。教训：跨会话恢复时，模型层/路由收敛类"战役状态"应先用 grep 验证代码实况再立项，勿信滞后记忆。

- **B3 扩展实体评估（2026-09-05 晚，只读）**：结论——**ScoreRule 首选收敛**（无 to_dict，rules_routes.py 内联 ≥4 变体：98 列表含 category_name /192 create 8 字段 /224 get /668 批量端点；模型 68-88 行字段全集小；⚠️ 列表契约**含** category_name → 默认 fields 须含之防漂移）；**Admin 次选**（无 to_dict，内联 4 处：auth_routes:78 登录子集 + admins_routes:142 列表含 class_count 派生/217 详情/313；⚠️ 永不输出 password、class_count=len(class_links) 派生仅显式 fields 访问）；**SubAccount 已有 to_dict(fields) 无需动作**；**Exam(19 调用)/Score(15 调用) 已全走模型层 to_dict 无内联残留、各端点全量同构 → 无需 fields，低优先**；ScoreCategory/Subject/CourseSchedule/Permission/ClassInfo 已 to_dict(fields)，categories_routes 外补 rule_count 属合理派生；ScoreRankRule(152 无 to_dict) 待查低优先。

- **B3 扩展执行✅：ScoreRule 收敛完成（2026-09-05 晚）**：① score_models.py:89 注入 `ScoreRule.to_dict(fields=None)`（CRLF 二进制，默认=详情 11 字段含 category_name，二级派生仅显式 fields 访问防 detach；serialize_dt 与手写 isoformat-if 语义等价）；② rules_routes.py 顶部 3 常量（RULE_LIST_FIELDS 10/RULE_CREATE_FIELDS 8/RULE_STAT_FIELDS 7）收 4 内联变体（列表/create/详情默认/统计）；③ export_routes.py RULE_EXPORT_FIELDS(10) 收 2 处（文件自含常量惯例）；④ categories_routes.py CATEGORY_RULE_FIELDS(7) 收 1 处（CRLF 二进制）。**踩坑**：python -c 双引号包裹注入 `\r\n` 混入字面 `/r/n` 致 AST 坏（已字节修复 1 处）；改用 heredoc + 行列表 join(EOL) 写法彻底规避；bytes 字面量禁含中文（须 str.encode）。**证据**：4 文件 +107/−87；py_compile OK；定向 pytest 76 passed（rules/categories/export/rule_engine）；run_regression REG_EXIT=0 全部通过。残留甄别为非 B3：TimeRule/NLP rule 其它实体、rule_engine_service 引擎内部 DSL 结构（category 别名/priority 派生）、redis_cache warmup 预热扁平结构（enabled 别名）——均非 API 响应序列化，留。**Admin 收敛（auth/admins_routes 4 内联）待执行**。

- **B3 扩展执行②✅：Admin 收敛完成（2026-09-05 晚）**：① user_models.py:111 注入 `Admin.to_dict(fields=None)`（CRLF 二进制，默认=详情 8 字段契约；password property 永不序列化；class_count=len(class_links) 二级派生仅显式 fields 访问，防 detach lazy）；② admins_routes.py 3 内联收敛（ADMIN_LIST_FIELDS 9 含 class_count/详情默认/ADMIN_LOGIN_FIELDS 5）+ auth_routes.py AUTH_ADMIN_FIELDS 5（键序 id,username,real_name,role,force_password_change 对齐 auth 原内联）。**踩坑**：Admin.to_dict 的 data dict **漏 force_password_change 键**（ADMIN_LOGIN_FIELDS 含该列但 data 未建键 → comprehension `if k in data` 静默丢弃 → test_admin_login_with_force_password_change 抓出契约漂移）——教训：显式 fields 里出现的每列必须都在 data dict 建键，补键后 38 passed/1 skipped。**证据**：3 文件 +77/−41；py_compile OK；定向 pytest 38 passed/1 skipped EXIT=0（admin/auth/rbac/sub_accounts）；run_regression REG_EXIT=0 全部通过。**B3 全战役终态**（User/ScoreRecord/ScoreRule/Admin 四实体累计 7 文件 +185/−128）：ScoreRule+Admin 残留 0（real_name/username 键手写空）；其余高频实体 Exam/Score 本就全收敛无需 fields、ScoreRankRule 待查低优先。git 未 commit（HEAD 526f6e3 == 远端）。
