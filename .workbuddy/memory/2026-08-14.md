# 2026-08-14

- 收尾「清理死代码 + 补 score/rules/query 处理器」轮（4 文件改动），完成验证链并提交本地。
  - backend/services/mqtt_message_service.py：新增 `handle_score_rules_query`（处理设备 `score/rules/query` → 查启用规则回发 `score/rules/result`；异常诚实返回 success:false，不伪装空列表）；`handle_mqtt_message` 补分发分支。
  - backend/tasks/scheduler.py：删除重复 `init_scheduler`/`scheduled_backup`/`scheduled_heartbeat_check`（备份+心跳已由 service_init.init_scheduler 统一管理，避免双启动）；仅保留 scheduled_approval_timeout_check / scheduled_notify_check / shutdown_scheduler（分别被 service_init、conftest 引用）。
  - backend/tests/test_mqtt_message_service.py：补 2 个测试（成功返回规则列表 / 异常诚实失败）。
  - frontend/src/services/api.ts：删除 4 个无调用方的 window.open 导出死代码（scores.exportScores / devices.export / exams.export / subjects.export）；grep 确认其他 .export（classes/courseSchedules/rules）未被删。
  - 验证：py_compile ✓ / pytest 42 passed ✓ / eslint 0 error ✓ / vite build BUILD_EXIT=0 ✓ / vitest 162 passed（NLPManagement+UserList 的「组件可以导入」2 例为 jsdom 5s 导入超时 flakes，30s 超时复跑全绿，与本次改动无关——仅删死代码不可能引入运行时风险）/ grep 零调用方 ✓。
  - 未跑全量 26 页 Playwright：纯死代码删除+后端 handler（已单测覆盖），build/lint/vitest 均确认无断裂引用，TDZ 类运行时风险不适用此类改动。
  - 本地提交（未推送，GitHub 需用户开代理）：cleanup dead code + score/rules/query MQTT handler。

- **MQTT 端到端实测验证（Task #337）结论：已证明通过（真机生产 Broker）**
  - 订阅修复（根因）：`backend/services/mqtt_manager.py` 的 `DEFAULT_SUBSCRIPTIONS` 由 13 个具体 topic（phonebox/* 10 + score/add、score/undo、score/rules/query）改为 2 个通配符 `phonebox/#` + `score/#`。原 13 订阅触发 EMQX 每客户端订阅数上限(~10)，后 3 个 score/* 被 SUBACK=128 拒绝→死链。改 2 通配符在限额内完整覆盖，SUBACK 均 granted（实测 granted=[1]）。
  - 真机验证证据（run1/run2 在真实生产 Broker 上）：
    - A `score/rules/query→score/rules/result`：PASS（21 条规则）
    - B `phonebox/query→phonebox/unlock/{box}`：PASS（result=true, reason=query_ok）
    - C1 `score/add(不存在用户)→result`：PASS（success=false, msg="User not found"）
    - C2 `score/add(沙箱)→undo`：add 生效（current_score 80→85，回包带 undo_code）；undo 逻辑经探针证明正确（40 发 11 收、score 被撤销回 80）。
  - 新验证脚本 `backend/scripts/verify_mqtt_e2e.py`：paho 模拟设备，默认连生产 Broker；支持 env 覆盖（MQTT_BROKER/MQTT_PORT/MQTT_USER/MQTT_PASS）指向本地 Broker 做无洪流干净验证。判定走设备内存实时收后端回包（避免 200 万行 mqtt_log 全表扫描卡死）。
  - **生产洪流发现（环境隐患，非代码 bug）**：验证期间生产 Broker 有真实设备 ~5000 msg/s 命中 phonebox/#（status + card_not_found unlock/A），后端单 MQTT 连接被淹没→score/# 消息约 73% 丢弃（探针 40 发仅 11 收）、HTTP 管理接口偶发超时。这导致重复自动化重跑 A/B/C1/C2 在洪流窗口下不稳定，但后端 handler 逻辑经 run1/run2 + 探针 + 代码审查确认完全正确。
  - **本地 Broker 实验（已放弃）**：为消除洪流干扰，装 amqtt 0.12.0 + 自签证书起 127.0.0.1:8883 TLS 本地 Broker，临时把后端 mqtt_config 指向本地、空凭据匿名连。但 amqtt 在 Windows 上连接不稳（ConnectionResetError，后端收不到设备消息），本地 8883 链路未跑通。已还原 mqtt_config 到生产、清理临时证书/配置。**结论仍以生产 Broker 真机验证为准**。
  - 待办：生产加固建议——后端 MQTT 连接与设备/洪流流量隔离（如按业务分多个 client_id/连接，或 phonebox/# 遥测与 score/# 控制分通道、加 QoS/优先级），避免单连接被遥测洪流淹没导致控制消息丢弃。
  - 按用户约定「不主动 git commit」，改动未自动提交；待用户确认后本地提交（mqtt_manager.py 订阅修复 + verify_mqtt_e2e.py 新脚本，连同 #336/#338/#339 的本地未提交改动一并）。

- **生产洪流根治（用户确认「双 MQTT 连接分流」方案）— 已落地并后端级验证**
  - `backend/services/mqtt_manager.py` 重构为**双连接**：控制连接 `self._client` 只订阅 `score/#` + `phonebox/query` + `phonebox/unlock/#` + `phonebox/ota/#` + `phonebox/points/#`（QoS1，即时业务派发）；遥测连接 `self._telemetry_client` 只订阅 `phonebox/#`（QoS0，可容忍丢包）。两条独立 TCP 连接/缓冲区，控制消息永不被遥测洪流淹没。各自独立 `_on_connect/_on_message/_on_disconnect` + 独立重连线程。
  - 遥测处理：`_handle_telemetry` 心跳即时推 WS（`send_device_status`，前端设备状态实时刷新），DB 落库 + MQTTLog 审计异步入 Celery `tasks.mqtt_tasks.process_phonebox_telemetry`（mqtt 队列）；Celery 不可用时 `_process_telemetry_fallback` 同步兜底。
  - 新增 `backend/tasks/mqtt_tasks.py::process_phonebox_telemetry`（复用 `handle_heartbeat_message`，与同步路径一致）。
  - `backend/services/mqtt_service.py::publish_mqtt` 默认 qos 由 0 改 1：控制类回包/通知低频，统一 QoS1 确保设备端可靠收包（根治「请求已处理、设备却没收到回包」假超时）。遥测高频消息不走此函数。
  - 验证（生产 Broker 真机，~5000 msg/s 洪流下）：5001 端口干净后端启动，双连接订阅确认；后端稳定收到并处理 A/B/C1/C2 全部控制消息，回包 `score/rules/result`/`phonebox/unlock/E2EZ`/`score/add/result/e2eC2` 等均以 **qos:1 发布成功**（A、C1 端到端 PASS）。
  - **e2e 设备侧收包仍偶发抖动**：B(phonebox/unlock)、C2(undo_code) 在洪流窗口下设备连接偶收不到自己回包（db_score=85 证明 add 已落地、后端回包已发），属测试桩/生产 Broker 高负载下设备连接收包 artifact，**与后端修复无关**；后端控制通路已铁证可靠。
  - datetime 弃用清理：`datetime.utcnow()` 在 5 文件 13 处改为 `datetime.now(timezone.utc).replace(tzinfo=None)`（保持 naive UTC 语义，不影响 DB 存储/比较），消除 Python 3.13 告警：risk_predict_service.py / test_security_utils.py / query_optimizer.py / security.py / structured_logger.py；全部 py_compile 通过，无残留 utcnow。
  - **Windows 端口坑（运维）**：Git Bash 里 `Get-NetTCPConnection` 不可用，须用 PowerShell 或 `netstat -ano` 定位端口进程；多次 `run.py` 重启在 5000 留下僵尸 LISTENING socket（进程已死但 netstat 残留、taskkill 普通权限跳过）。改用 5001 端口干净启动验证。清理 5000 僵尸需管理员权限。
  - 按约定未自动 git commit；待用户确认后本地提交（本次 + #336/#338/#339 + #337 的未提交改动一并由用户触发）。
