# 2026-08-25 工作日志

## T4 综合分重算异步化（Celery，#905 completed）
- 依据 `docs/下一步开发计划-20260824.md` P1-T4：批量录分/删除路径的同步 `CompositeScoreService.recalculate_user_score` 改为 Celery 异步入队。
- **实现**：
  - 新建 `backend/tasks/score_tasks.py::recalc_user_score`（`name=tasks.score_tasks.recalc_user_score`, `queue="score"`）：在 `flask_app.app_context()` 内调用 recalc，失败 `logger.error` + 轻量 `self.retry(countdown=5, max_retries=2)`。
  - 新建 `backend/services/score_recalc.py::enqueue_or_recalc_user_score`：读 `current_app.config["CELERY_ASYNC_SCORE_RECALC"]`（默认 False）→ True 且 broker 可达才 `.delay()` 异步；否则**同步回退**调用 recalc（失败向上抛，由调用方 `try/except` 记上下文，复用 T1 的 `logger.error` 约定）。
  - `backend/celery_app.py` 的 `include` + `autodiscover_tasks` 增加 `tasks.score_tasks`。
  - `records_routes.py` 三处结果未消费路径（单条创建 :261 / 批量循环 :772 / 删除 :890）改调 `enqueue_or_recalc_user_score`；单条更新 :617 保留同步（其消费 recalc 返回值设置 `composite_score_updated` 响应标记，契约不动）。
- **零回归设计**：默认 `CELERY_ASYNC_SCORE_RECALC=False` → 测试/沙箱/本地永远同步重算，绝不因「已入队无 worker」静默漂移；生产置 True + 起 worker 才真正异步。
- **回归证据**：py_compile(managed 3.13) OK；定向 pytest（scores_routes/records_routes/composite_score_service/approvals_routes）= **54 passed**；新增 `test_score_recalc_dispatcher_falls_back_to_sync`（断言异步关时同步调用一次）并入 composite_score_service 测试（13 passed）；`bash scripts/run_regression.sh` **REGRESSION_EXIT=0**（RBAC 一致 / 契约通过 / 关键路由通过 / 核心索引齐全；G5 后端未起跳过）。
- **生产启用**：后端 config 设 `CELERY_ASYNC_SCORE_RECALC=True` 并启动 worker 监听 score 队列（`celery -A celery_app.celery_app worker -Q score`）。
- 累积未提交：T4 新增 2 文件 + 改 records_routes/celery_app/测试，叠加历史批次共 130+ 未提交，守「用户确认后再 commit」。

## 进度衔接
- P0（T1/T2）已完成；P1 中 T3 分页、T5 any 收敛、T6 security fail-closed 此前已完成；本日完成 T4。P1 剩余：无（T4 为最后一项）。后续可推进 P2（T7 ML 解耦 / T8 死依赖 / T9 大路由瘦身 / T10 CRA eslint 清理，其中 T8/T10 已 completed）或 P3（T11 静默失败页 / T12 巨型页拆分）。

## T9 基础设施层日志化（P2 子项，`except:pass` → `logger.*`）
- 依据 `docs/下一步开发计划-20260824.md` P2-T9："基础设施/缓存/审计辅助函数 except:pass 改 logger.warning"。逐点评估 Grep 全仓 except:pass 基线后落地：
- **改 logger.warning（真实静默失败，需留痕）**：
  - `utils/api_cache_middleware.py`：缓存 get/set 失败降级回源/不阻断，但留警告（:108-111 / :132-135）。
  - `utils/security.py::get_request_data`：JSON 解析失败降级空 dict 留警告（:439-443）。
  - `utils/backup_utils.py::list_backups`：列举备份失败留警告（:205-206）。
  - `services/system_metric_service.py`：清理前端上报表失败 / 采样回滚失败均留警告（两处）。
  - `services/mqtt_service.py`：print→logger.error（2 处）+ rollback 兜底 logger.warning（2 处）。
  - `services/data_sync_service.py`：两处 rollback 兜底 logger.warning。
  - `services/mqtt_message_service.py`：PRAGMA busy_timeout 无 context 改 logger.debug（可预期分支，不刷屏）。
  - `services/score_rule_service.py::delete_rule`：解除流水引用失败留警告。
  - `api/devices/wol_routes.py`：arp 解析失败留警告。
  - `api/scores/notify_template_routes.py`：按设备解析班级失败留警告。
  - `api/devices/firmware_routes.py`：删除固件文件失败留警告。
  - `api/monitoring/mqtt_routes.py`：payload 解析失败降级 DB 配置留警告。
- **保留静默 + 显式注释（T9 评估结论，刻意不改）**：
  - `utils/structured_logger.py` formatter 内部（:100-105）——改 logger 会递归死循环，必须 pass。
  - `utils/excel_utils.py` + `api/data/export_routes.py` 逐 cell 列宽热循环（失败仅跳过该列估算）——改 logger 会刷屏，保留 pass 并注释。
- **未纳入本批（属 T7 ML 解耦专项）**：`api/nlp/*`、`services/bert_service.py`、`nlp_optimizer.py`、`nlp_ml_service.py`、`nlp_enhanced_service.py`、`nlp_service.py` 的 except:pass 触及 NLP 解析器内部行为，留待 T7 评估，不越界。
- **零回归证据**：py_compile(managed 3.13) 15 文件全 OK；定向 pytest（cache/security/data_sync/mqtt/wol/firmware/notify/export 共 15 文件）= **296 passed**；`bash scripts/run_regression.sh` 全绿（RBAC 一致 / 契约 / 关键路由 / 核心索引齐全）。
- 累积未提交：T9 日志化 14 文件改动，叠加 T4 + 历史批次共 130+ 未提交，守「用户确认后再 commit」。

## T9 路由瘦身（P2 子项，`records_routes`/`algorithm_routes` 读路径下沉）
- **现状复核（关键）**：两个路由文件此前 F17 迁移已高度服务化 —— `algorithm_routes.py` 内联 DB 操作 **0 处**（全部委托 AlgorithmService/CompositeScoreService/ScorePredictService 等 18 个 service 类）；`records_routes.py` 读路径已委托 `score_record_service`（query_score_records/get_score_statistics/get_score_entry_data/serialize_score_record）。即"读路径编排下沉"主体已完成，剩文件"大"是因端点数量多（algorithm_routes 75+ 端点、1453 行），非内联编排重。
- **本批唯一明确内联读编排**：`algorithm_routes.py::_build_export_rows`（算法导出三 tab 行列拼装，~78 行，纯数据整形）下沉到新建 `services/algorithm_export_service.py::build_algorithm_export_rows`（逐字节一致，无逻辑改动）。路由 `AlgorithmExport.get` 仅保留参数解析 + `ExcelUtils.export_to_excel` + `send_file` 薄壳；模块级函数已删除，无其它引用点（grep 确认）。
- **零回归证据**：py_compile(managed 3.13) OK；定向 pytest（test_algorithm_export + test_algorithm_service + test_algorithm_batch_isolation）= **33 passed**；`bash scripts/run_regression.sh` 全绿（RBAC/G5/契约/关键路由/索引）。
- **未做（评估结论，不强行拆）**：其余 ~74 个算法端点均为薄委托壳（14-21 行），无内联编排可抽；强行拆成"一个端点一个 service 方法"会增代码量、无收益、且违背 F17"读路径可暂缓"铁律。T9 路由瘦身到此收敛（日志化 + 唯一内联编排下沉）。
- P2 剩余真正待办：**T7 ML 重依赖解耦**（torch/transformers/xgboost/catboost/gensim 拆 extras 或 lazy import，主镜像去未用 ML 重依赖）。
- 累积未提交：T9 日志化 14 文件 + T9 路由瘦身 2 文件（algorithm_routes 改 + 新建 algorithm_export_service）+ T4 + 历史批次共 130+，守「用户确认后再 commit」。
