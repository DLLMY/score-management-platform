# 2026-09-04 工作日志

## P2 print→logger 收口（第二批 + 第三批 + logger 加固）
- 改动文件（纯 print→logger，无逻辑漂移）：`backend/app/service_init.py`(36 处)、`backend/app/db_init.py`(17 处)、`backend/utils/security_config.py`(4 处)；`traceback.print_exc()` 由 `log_error(..., exception=e)` 替代。
- `backend/utils/logger.py` 加固：抽 `_dump_extra` + `_emit` 统一 5 级别；`log_debug/info/warning/error/critical` 均支持 `exception` 参数（修复「log_warning 误传 exception 被 json.dumps 致 TypeError 崩溃」陷阱）；`json.dumps(..., default=str)` 容错不可序列化对象。
- `backend/tests/test_logger.py` 补 4 个回归守卫（warning/info/debug 传 exception 不崩 + 不可序列化 extra 不崩）。
- 验证闸门：`run_regression.sh` → `REG_EXIT=0` 全绿；`test_logger` 定向 17/17 通过。
- 全量 pytest（系统 Py3.11，剔除 WoL 网络测试后重跑）：`9 failed, 2062 passed, 8 skipped`。9 个失败均**非本次改动引入**：① `test_anomaly_types_thresholds`（阈值配置 3≠5，与 logger 无关）；② `test_system_routes::test_clear_cache`（沙箱 safe-delete 假阳性，已知）；③ `test_workbench_isolation_smoke` 7 例（班主任工作台班级隔离逻辑/信封 `data` 成字符串——属工作台代码既有问题，与 print→logger 无关）。首次全量跑 `test_wol_routes.py`(Wake-on-LAN 网络测试) 单测超时 600s 触发 pytest-timeout 终止，剔除后其余全绿。

## 严重事故：仓库 .git 损坏（已完成恢复）
- 现象：全量 pytest 运行期间 `.git/objects/pack/*.pack` 数据文件与 `.git/refs` 目录被环境删除，git 报 `bad object HEAD` / `not a git repository`。
- 影响：仅 `.git` 对象库损坏；**工作树改动（本回合 5 文件等）独立安全、未丢失**。
- 恢复：`git fetch --refetch origin` 从远端(ad259de)重新拉取全部对象，重建 `refs/{heads,tags,remotes/origin}`；`git status`/`diff`/`HEAD` 恢复正常。改动文件先备份至 `/tmp/workbackup/` 再操作。
- 残留：`.git/objects/pack/` 有孤儿 `.idx`（对应 .pack 已随 refetch 重建），`git fsck` 仍报若干 `failed to load pack entry`（非致命，status/commit 可用）。
- 发现：恢复后 `git diff origin/main` 显示**74 个文件未推送**，为多个会话累积（前端 M9/M11 润色、后端 P2、P1 无界 limit、测试、记忆文件），远超本回合 P2 范围。提交/推送范围需用户显式确认（仓库铁律：不主动 commit/push）。

## 提交/推送（已收口）
- 用户确认「提交全部未提交文件」。实测真实未提交仅 15 文件（此前「74 文件」是陈旧 `origin/main`(5da4546) ref 假象；真实远端=ad259de175）。
- `git commit` → a162724（15 文件：P1 无界 limit + P2 print→logger 两批 + logger 加固 + 测试 + 记忆）。
- 推送发现**分叉**：本地 4973ec9 与真实远端 ad259de175 自共同祖先 5da4546 起分叉（远端仅含 P2 首批 3 文件；本地含 M9/M11/P1/P2全批/测试等 19 提交）。本机 `origin/main` ref 陈旧(5da4546)，`git status` 读数失真，须用 `git ls-remote` 核实。
- 收口：`git merge --no-edit ad259de175`（远端 P2 首批内容本地已含，合并干净无冲突）→ `git push origin main` 成功。
- 远端主线 = 98db8b9，与本地 HEAD 一致，全量 P2 工作已上线。
- 注意：本机 `origin/main` 陈旧 ref 仍未实际写入（packed-refs bug），后续核对推送一律用 `git ls-remote`。

## 待办
- 可选：清理孤儿 pack .idx + multi-pack-index 使 `git fsck` 干净（已 rm 孤儿 .idx/multi-pack-index，fsck 应已无 failed-to-load）。

## P2-5 NLP/ML services print→logger 收口（第四批，services 下 56 处）
- 范围：services 下 NLP/ML 7 文件共 56 处 print → `log_info`/`log_warning`/`log_debug`：
  - `nlp_service.py`(17) / `textcnn_service.py`(15，demo 块 `__main__`→`log_debug`) / `bert_service.py`(8，含多行 `print`→`log_info`) / `nlp_optimizer.py`(5) / `nlp_ml_service.py`(5，L568 初始化未完成→`log_warning` 无 exception) / `nlp_fast_parser.py`(2) / `nlp_enhanced_service.py`(4，缓存命中/未命中→`log_debug`，batch_parse 失败→`log_warning(exception=e)`)。
- 方法：AST 脚本 `scripts/p2_5_print_to_logger.py`，规则——except 块→`log_warning(exception=<e>)`；`if __name__=="__main__"` 块→`log_debug`；显式 OVERRIDE 映射（非 except 告警/缓存调试行）；其余→`log_info`。零逻辑漂移。
- 踩坑（脚本已加固，下次复用直接可用）：
  1. ast 的 `col_offset/end_col_offset` 在本环境按 **UTF-8 字节**计（中文 3 字节/字），`len()` 按字符计 → 偏移错位吞掉下一行。修复：`off()` 把字节列经 `line.encode('utf-8')[:col].decode()` 转回字符列。
  2. `open(...,"w")` Windows 文本模式把 **LF** 文件翻成 CRLF（整文件 diff）。修复：二进制读写 `rb`/`wb` 精确保留行尾（CRLF/LF 各自不变）。
  3. 空 `print()` → `log_debug()` 会因 `message` 无默认值 `TypeError`；脚本置 `inner='""'`。
- 验证：7 文件 `py_compile` 全过；无残留 statement-level `print`；RBAC 一致性 OK；核心索引全在；`verify_indexes.py` 启动日志证实 logger 调用实跑（NLP 初始化/预热/parser 日志走 INFO，无崩溃）。
- 提交：`git diff --stat` 7 文件共 56 处变更（nlp_enhanced 仅 9 行因原 LF 行尾已保留）；**尚未 commit/push**（仓库铁律：须用户显式确认）。

## P2-5 提交/推送（✅ 已上线 b6c466d）
- 用户确认「提交并推送」→ `git commit` → **b6c466d**（9 文件 +285/−58：7 NLP/ML services + 脚本 scripts/p2_5_print_to_logger.py + 本日志）→ `git push origin main` 成功 `98db8b9..b6c466d`。
- `git ls-remote origin refs/heads/main` = b6c466d == 本地 HEAD，工作树干净，推送真实落盘。
- ⚠️ **remote 修正**：实测 `git remote -v` 当前**仅 `origin`** 一个 remote，URL=`ssh://git@ssh.github.com:443/DLLMY/score-management-platform.git`（历史记忆的 `origin-ssh` 名已不存在）。MEMORY.md「push 走 origin-ssh」已改「push 走 origin」。`origin-ssh` 作为 push 专用名可能曾存在后被移除/改名，勿再引用。

## P2-6 services 剩余 19 处 print→logger 收口（✅ 收尾批，services 清零）
- 范围：7 文件共 19 处 → 复用 `scripts/p2_5_print_to_logger.py`（TARGETS 换 7 文件 + OVERRIDE 扩 5 条）：
  - `composite_score_service.py`(4)：426/447 无记录需全量→`log_info`；440 学生不存在/停用→`log_warning` 无 exception；452 首增跳过常态噪音→`log_debug`
  - `websocket_service.py`(4)：连接/断开/订阅逐事件高频→`log_debug`；处理器注册一次→`log_info`
  - `notification_config_store.py`(4)：40/43 加载状态→`log_info`；45/66 except→`log_warning(exception=e)`
  - `mqtt_service.py`(2)：167 发 OTA 指令→`log_info`；209 except→`log_warning(exception=e)`
  - `mqtt_message_service.py`(2)：201/241 except→`log_warning(exception=e)`
  - `class_time_checker.py`(1)：243 except→`log_warning(exception=e)`
  - `wol_service.py`(2)：36 成功→`log_info`；40 except→`log_warning(exception=e)`
- 验证：7 文件 py_compile 全过；无残留 statement-level print（**services/ 全目录清零**）；主线程预热后 7 模块 import OK；RBAC 一致性 OK；核心索引全在。diff +26/−19 无行尾翻转。
- 提交推送待用户确认。
- ✅ 用户确认提交+推送：`git commit` → **7a258b9**（10 文件 +60/−32）→ `git push origin main` 成功 `b6c466d..7a258b9`；`git ls-remote origin refs/heads/main`=7a258b9==本地 HEAD，工作树 clean。**至此 services/ 全目录 print→logger 收口完毕（P2-5 56 处 + P2-6 19 处 + 前批 app/utils 57 处）。**

## P2-7 backend 运行时残留 print→logger 收口（✅ 12 文件 81 处，未提交）
- 方法升级：AST 实扫（**grep 会虚报 docstring 示例代码**——smart_reconnect/batch_writer 各 3/1 处 print 全在模块底部 docstring 里，0 真实，已剔除）；13 文件真实 print 81 处。
- 范围：脚本批量 11 文件 80 处（`api/devices/firmware_routes`1 + `api/scores/approvals_routes`4 + `rules_routes`1 + `system_routes`1 + `config/config_loader`10 + `middleware/__init__`27 + `utils/validation_middleware`1 + `tasks/mqtt_tasks`6 + `notification_tasks`3 + `scheduled_tasks`19 + `scheduler`7）+ 手工 `utils/security.py` 1 处（`print(..., file=sys.stderr)` 去流化 → `log_warning`，import 注入 `log_warning`）。
- 分类：except→`log_warning(exception=e)`（approvals 顶层 `except ImportError` 无 `as` → warning 无 exception）；逐消息噪音（mqtt_tasks 未匹配 2、scheduler 每轮"无超时审批"）→`log_debug`；middleware 27 条限流/CSRF 注册与 config_loader/scheduled 任务状态/完成→`log_info`。
- **脚本三处新加固（复用铁律）**：① import 注入锚点不能是首个 def/class——可能在**模块级 try/except 内（缩进 def）**或**带装饰器**（approvals/tasks 因此首跑语法错误）；改为"docstring 之后、前导 import 段整体结束后"插入；② 多行**括号 import 块**（`from x import (\n...`）须括号配平跳过续行，否则 import 插进 import 语句中间（firmware/approvals 因此二跑失败）；③ TARGETS 支持 `backend/` 前缀仓库根路径（脚本不再只限 services/）。
- 验证：12 文件 py_compile 全过；无残留 statement-level print；JWT_SECRET_KEY 环境变量下 12 模块 import OK（security fail-closed 需 env，非改动问题）；RBAC 一致性 OK；核心索引齐全；diff +159/−105 无行尾翻转。rules_routes 重复 utils.logger import 行已合并为 `log_info, log_operation`。
- 提交推送待用户确认。至此**backend 运行时代码（app/utils/services/api/config/middleware/tasks）print→logger 全清零**；剩余 print 仅在 scripts/（CLI 748）、tests/、migrations/、tools/（by-design 控制台）。

## P2-8 运行时 traceback.print_exc 折叠（✅ 3 文件 4 处，未提交）
- 全仓 traceback.print_exc 13 处（AST/位置定位）：tests 3 + migrations 3 + scripts 1 by-design 保留；runtime 5 处中 **service_init L178 仅注释**（代码早已 `log_error(exception=e)` 收口）。
- 真改 4 处：`services/mqtt_manager.py` L762/L870（连接异常/发布异常）、`services/redis_cache_service.py` L718（缓存预热失败）、`api/monitoring/mqtt_routes.py` L306（连接失败）。模式：删除局部/顶层 `import traceback`+`traceback.print_exc()`，给既有 `logger.error` 加 **`exc_info=True`**（单条日志带堆栈，替代直出 stderr 两段式）；mqtt_routes 顺带删模块级 `import traceback`（L2，唯一用途）。
- 验证：3 文件 py_compile 全过；runtime 目录（services/app/api/utils/config/middleware/tasks/models/startup）残留 traceback.print_* 为 **0**；3 模块 import OK；RBAC 一致；核心索引齐全；diff +10/−15 无行尾翻转。
- 至此运行时代码调试直出（print + traceback.print_exc）**双清零**。
- ✅ 用户确认推送：`git commit` → **591b43e**（3 文件 + 记忆 +17/−15）→ `git push origin main` 成功 `8dd4ed4..591b43e`；ls-remote 核实远端==本地 591b43e。
- **同类残留终扫（结论：运行时代码调试直出全家族归零）**：① 纯 `pass`/空体 except——AST 全 runtime 目录 **0 处**（前序 #844/#965 已清）；② 非行首 `print(` / `sys.stdout|stderr.write` / `pprint`——**0 处**；③ grep 命中的 batch_writer L447、smart_reconnect 7 处、models/migrations 27 处、startup/run.py、start_celery、run.py、seed_rbac 均**docstring 示例或 CLI/迁移/启动横幅（by-design）**，非真实运行时代码。**P 家族在 backend 运行时代码侧收口完毕。**

## #375 前端 console.log 收口（✅ 评估 + 尾部收口，未提交）
- **评估结论**：历史 277 处/62 文件的 #375 已被前序会话基本消化——`src/utils/logger.ts` 已存在（默认导出，含 log/info/warn/error/debug 五方法），残留 grep 命中仅 12 处且多为注释/docstring/logger.ts 自引用。
- **真实可替换仅 2 处**：`src/hooks/useApiFetch.ts` L30/L38（fetchJson 非 2xx 与 catch 异常日志，T11 静默失败改造遗留直接 console.error）→ 改 `logger.error` + 加 `import logger from '../utils/logger'`，docstring 同步措辞。
- **by-design 保留**：`src/utils/errorMonitor.ts` 3 行 console.error（**故意挂钩全局错误捕获**，替换会破坏其用途）；`services/api.ts` L652 仅注释。
- 验证：eslint 单文件过；`tsc --noEmit` exit 0；logger.error 仍透传 console.error → 既有 spy console.error 的测试不受影响。
- 收口后 src 真实 console 调用仅剩 errorMonitor 挂钩（by-design）。
- ✅ 用户确认推送：`git commit` → **c2d9048**（2 文件 +13/−3）→ `git push origin main` 成功 `591b43e..c2d9048`；ls-remote 核实远端==本地 c2d9048，工作树 clean。
- **遗留任务销账（代码取证）**：#1021/#1025（M9 P1 class_management 13 端点分页）——class_management 各域路由已用 `get_pagination`（activity/attendance/culture/classes 等），实际早已完成 → 销账；#1026（M11 DataTable a11y+斑马纹）——提交 `94a0409 "feat(a11y): DataTable 表头语义与斑马纹润色（M11）"` 已存在（含 aria-sort/scope='col'/斑马纹）→ 销账。**近期（≥#1000）任务板已清空无 open。**

## 全量零回归闸门（今日 P2-5~P2-8 + #375 五批改动后，✅ 全绿）
- `bash scripts/run_regression.sh`：**全部回归通过**（3m8s；RBAC 一致 / 契约 / 关键路由 / 24 核心索引齐全）。
- 前端 vitest 全量：**37 files passed | 1 skipped；270 tests passed | 3 skipped**（100s，threads 默认池）。
- 后端全量 pytest（系统 Py3.11，`-p no:locust --timeout=600`）：**9 failed / 2071 passed / 8 skipped**（1256s）。9 个失败与晨间基线**逐项一致、零新增**：① `test_anomaly_types_thresholds`（阈值配置 3≠5，预存）；② `test_clear_cache`（沙箱 safe-delete 假阳性，预存）；③ `test_workbench_isolation_smoke` 7 例（班主任工作台隔离/信封问题，预存）。今日改动文件（services NLP/7 + 非NLP 7 + api4 + utils/tasks/config/middleware + useApiFetch）无任何用例失败。**结论：今日五批推送零回归。**

## 修复 9 个预存 pytest failed（✅ 定向全绿，全量复核后台）
- **① workbench 隔离 7 例（test 侧契约漂移）**：M9/T3 强制分页后 4 列表端点（parent/contacts、mental-health/records、mental-health/alerts、teacher-comments）data 为分页信封 dict（list key 各为 contacts/records/alerts/comments + total/page/per_page/pages），旧测试按裸数组断言（len(dict)=5≠4）。修复：`test_workbench_isolation_smoke.py` 加 `_items(data,key)` 解包 helper（兼容新旧），7 处断言改解包；顺带把重复定义的 `_admin_headers`（类 staticmethod + 模块级两份）收敛为单一模块函数。**后端隔离逻辑本身正确，未动**。
- **② anomaly 阈值 1 例（test 侧过期契约）**：配置 `group_anomaly.threshold=3` 且 description 写明"偏离班级均值超过3个标准差"（与 sudden_change=3 同口径），测试断言 5 是旧值 → 改断言 5→3 并注释。
- **③ clear_cache 1 例（生产真实缺陷）**：`api/system/system_routes.py::SystemClearCache.post` 原 `shutil.rmtree` 删 `__pycache__`，任一目录失败（沙箱拦截 / **Windows 被占用 .pyc**）→ 整请求 500。修复：抽 `_clear_pycache(path)` 逐目录 try/except OSError + `log_warning` 跳过，其余继续，返回 success——Windows 生产环境同受益。
- 验证：定向 14 passed（12 workbench + anomaly + clear_cache）；`test_system_routes.py`+`test_anomaly_service_full.py` 全文件 22 passed；system_routes py_compile OK；run_regression 全部通过。全量 pytest（9EmlGE）后台复核中，预期 0 failed。改动 3 文件待提交推送。
- ✅ 全量复核：**2080 passed / 8 skipped / 0 failed**（931s）→ 提交推送 5753566（4 文件）。**全量 pytest 零失败达成。**
- ⚠️ **EOL 事故与恢复**：5753566 中 system_routes.py 被 Edit 工具整体翻成 LF（根因：P2-7 脚本注入 import 时固定 `"\n"`，在 CRLF 文件里混入 1 个 LF 行 → 文件变混合 → Edit 规范化整文件），产生 2315 行噪音 diff。处置：commit 679203e 统一回 CRLF（净 diff 仅 +10/−3）；并把 `scripts/p2_5_print_to_logger.py` import 注入改为**按源主行尾换行**（`nl = "\r\n" if "\r\n" in source else "\n"`）→ commit 4434b3c。经验固化：**CRLF 文件一律用二进制 IO 编辑，注入新行须继承主行尾**。
- 提交链：5753566(9-fix) → 679203e(EOL恢复) → 4434b3c(脚本加固)，全部推送并 ls-remote 核实（远端==本地 4434b3c）。工作树 clean。

## 建议三连逐步推进（① 审查报告遗留取证 / ② 前端健壮性续批 / ③ 归档域终扫，✅ 均完成取证）
- **① 《全面代码审查报告-20260821》P0–P3 遗留取证**：P0（前端关键路径测试 270+）✅；P1（综合分重算日志化/列表强制分页/security fail-closed）✅；P2（torch 懒加载+requirements-ml、死依赖清理、CRA eslint 残留、print→logger）✅；P3（except:pass→logger 清零、测试 except:pass→pytest.fail、N+1 joinedload 补齐）✅。**仅剩实质项：`api/algorithm/algorithm_routes.py` 1375 行 / `records_routes.py` 908 行大路由瘦身（P3 持续改进，异构高风险，记录待用户决策是否排期）**；配置源收敛已基本成型（env 读取集中于 utils 配置类/app/config_init/db_init，仅 services/ota_negotiation_service 等零星）。
- **② 前端健壮性续批扫查**：无残留 `Math.random` key（#1013 已清）；hooks 栈健全（useRouteChangeAbort/useOptimizedFetch/useSubmitGuard 等）；裸 fetch 集中于导出/下载/健康/错误上报类端点（历次审计已收口，非缺陷）。无新增实质项。
- **③ 归档域终扫（F9 alert/notification 合并、F10 WOLDevice、Leave→Approval）**：运行时无旧表/模型残留引用；DeviceAlerts/MentalHealthAlerts/WOLDevice 类名、`/api/admin_notifications/` 路径均为**保留的 API 兼容面**（内部分别走 alert/notification/device 新表与服务）；仅 backups/refactor_F17 快照含旧名（预期）。全量 pytest 0 failed 佐证。**合并域收口状态最终确认。**

## P3「大路由瘦身」评估 → **驳回结案**（2026-09-04）
- **algorithm_routes.py（1375 行 / 66 端点方法）**：`0` 处内联 db.session/.query、`4` 处序列化；handler 全部委托 10+ service（AlgorithmService/Cluster/Composite/Warning/Prediction/Anomaly…），≈21 行/方法为 flask_restx doc/model 正常密度 → **已是薄路由**，无对象可搬，收益≈0。
- **records_routes.py（908 行 / 9 端点方法）**：残余 db 访问 4 + 序列化 3，多属 F17 保留读路径/响应构造口径；score 域写路径 #629 已全收口。边际收益低。
- 判词同既有铁律「异构高/收益低即拒绝」。**此 P3 条目正式结案为"评估驳回"**。待评估的下一个候选：records_routes 内大段 Excel 导入/导出 helper 是否搬入 score_import_helper/工具层（纯搬移低风险）。
- **候选续评 → 亦驳回**：`records_routes`（908 行 / 9 方法）重型方法 ScoreEntryResource.post(222)/BatchScoreEntryResource.post(139) 等**已按 F17 委托 `score_record_service`**（源码注释"事务收口到 service：排名+积分+log_operation+add+commit"），路由仅保留读路径查询、缓存失效、编排壳与响应构造（F17 约定内合法）；Excel 导入/导出逻辑本就在 `services/score_import_helper.py` + `api/data/import_export_routes`。**"搬 Excel helper"前提不存在** → 与 algorithm_routes 一并结案。**P3 大路由瘦身（及其候选）全部评估驳回，任务关闭。**

## 模块深审（定时任务域）+ 全仓缺陷扫描（失败伪装/静默吞错）→ 修复 2+1
- **模块深审命中真缺陷：`tasks/scheduled_tasks.py::archive_operation_logs` 恒败**——OperationLogArchive 表结构为 original_id/admin_id/action/details(JSON)/archived_at，原代码按 OperationLog 字段（operation_type/target_type/...）构造 → **TypeError，日志从未归档**（主表无限膨胀）。修复：按新表结构映射（original_id=log.id、admin_id=log.user_id、action=operation_type、details=JSON 汇总、archived_at 默认 now），删除按已归档 id 精确匹配 + synchronize_session=False。**新增 `tests/test_scheduled_tasks_archive.py`（2 用例：超期归档/保留期内不动）→ 2 passed**。另查 sync_device_status "未提交"系误判（db_session_scope auto_commit=True 退出即提交），未动。
- **全仓 AST 扫描「except→return 假值且 handler 无日志」**：51→修正 Name 日志检测后 **36 处真静默**。分级：redis_cache_service 5 处 False = 设计性降级（client=None 内存回退，get 路径已有 logger.error）不动；sms/wechat_utils 为占位 stub（`try:return True`，加日志无意义，设计另议）；phonebox/_parse_class_id、datetime/entity_names/security 等为合法守卫/契约。**高信号修 2 处**：`utils/email_utils.py` 真实 SMTP 发送失败两处补 `log_warning(exception=e)`；`services/bert_service.py::_cosine_sim` numpy 异常静默归 0（评分正确性）补 `log_warning(exception=e)`。验证：py_compile + import 冒烟 OK。
- 改动 3 文件（scheduled_tasks.py、email_utils.py、bert_service.py）+ 新测试 1 文件 + 记忆，待提交推送。

## 继续会话收尾（建议直执 + 死代码清理）✅
- ① 建议执行：scheduled_tasks 修复 + email/bert 日志 + 新测试 → commit ce04928 推送（ls-remote==本地核实）。**EOL 二次事故**：ce04928 中 scheduled_tasks.py 被 Edit 工具整体 CRLF→LF（493 行噪音）→ commit 239c0fc 统一回 CRLF（净 diff 0）。**铁律再固化：CRLF 后端文件禁止 Edit 直改，一律二进制读改写/继承主行尾**（第一次 system_routes，第二次 scheduled_tasks，第三次 test_utils 已预防）。
- ② 死代码清理（用户批准直执）：删 `utils/sms_utils.py`+`wechat_utils.py`（假成功 stub、零运行时引用，仅 test_utils 冒烟）；保留真实 SMTP 的 email_utils；同步删 test_utils 两条冒烟 → 9 passed。commit 00c9dea 推送核实。
- ⚠️ **工作区异常处置**：清理前发现 8 个非本会话删除（utils/__init__、validation、structured_logger、validation_middleware、validators、smart_reconnect、startup_cleanup、transaction_retry，共 3006 行）——均有存活引用方（scores/users/device/import_export/run.py）+ 专属测试，**无 caller 迁移 = 半成品删除态**，判定他处会话/人工遗留 → `git restore` 回 HEAD，未卷入本提交。已在提交说明留档。若确属有意删除请另行下达（需同步迁移引用方）。
- 通知链路取证结论：生产通知走 NotificationService（微信 access_token HTTP / 阿里云短信签名真实实现），stub 不参与运行。

## 第三轮模块深审：notification_service 短信渠道 4 缺陷实锤并修复（待提交推送）
- **模块**：services/notification_service.py `_send_aliyun_sms`（阿里云短信，运行期复现确认）。
- **缺陷链（叠加 → 短信渠道从未成功发出）**：① `hashlib.hmac.new` 属性不存在（hashlib 无 hmac 导出）→ 每次调用 AttributeError 被 except 吞；② 即便修正，`.b64decode()` 也错，缺 digest→b64encode 步骤；③ string_to_sign 用 `quote("/")` 保留字面 '/'，阿里云 RPC 规范要求 %2F；④ 最终 URL `?Signature={sig}{canonicalized}` **缺 & 分隔符**，Signature 值吞并后续参数，服务端必拒。
- **修复**：顶部 import base64/hmac；签名= b64(hmac_sha1(secret&, sts))→quote；sts 用字面 `GET&%2F&`；URL 补 `&`。另澄清错误文案 SMS_PROVIDER→SMS_CONFIG.provider。CRLF 保持（二进制改写），净 diff +12/−6 无噪音。
- **新增 tests/test_notification_sms_signing.py（2 用例）**：独立算法路径（urllib.parse safe=''）重算期望签名逐字节比对 URL Signature（锁定 hmac/b64/%2F/& 全修复防回归）+ 未配置 provider 兜底文案。→ 2 passed。
- **附带观察（未改）**：_get_wechat_access_token 无缓存（微信 access_token 7200s 有效、日获取上限，群发通知多会触发 2000 次/日上限风险）；notify_device_offline 空管理员列表时返回 success:True（契约可议）。改动 2 文件待提交。

## 存量观察项修复：微信 access_token 进程内缓存（待提交推送）
- **背景**：access_token 7200s 有效、微信日获取上限；原实现每次发送现取 → 群发通知耗尽配额。
- **实现**：notification_service.py 模块级 `_WECHAT_TOKEN_CACHE`（appid 关联防多配置串用）+ `_WECHAT_TOKEN_REFRESH_SKEW=60` 提前刷新；成功取到写缓存（expires_in 缺省 7200）；接口错误/网络异常时按 appid 匹配**降级回退旧 token**；无配置仍返回 None。CRLF 二进制改写保持，+37/−6（含短信修复累计）。
- **测试**：新增 tests/test_notification_token_cache.py（7 用例：缓存命中零请求/过期刷新/接口错误降级/网络异常降级/无配置隔离/异 appid 不串用）。**四文件 44 passed**（含存量 test_notification_service(_new) 35）。
- **评估驳回项**：notify_device_offline 空管理员 success:True 为**既有契约**（test_notification_service.py:381 断言），fire-and-forget 语义，不改。
- 备注：后台全量 pytest（WZk7BP）为 stub 清理后基线快照（启动早于本批通知修复），通知域由上述 44 定向兜底。累计待提交：notification_service.py + test_notification_sms_signing.py + test_notification_token_cache.py + 本日志。

## 产品决策项执行（composite 语义统一 ✅ / approvals 复核闭环 ✅ / utils 8 文件裁决保留 ✅）
- **① composite_score 三字段语义 D 级 → 决策并完成**：全链路统一为 **0-100 归一化可比分量（norm×100）**，消除三套并存口径（全量写 raw+social×100 / 增量写 0-1 norm / 读又重算 avg）。改动 `services/composite_score_service.py`（LF，+20/−27）：_calculate_scores 三分量×100；recalculate_user_score 增量写×100；get_composite_scores 读透出存储分量并**删除 read-path Score 聚合重算**（省一次查询）。新测试 `tests/test_composite_score_units.py`（3 用例：产物 0-100/落库透传/读取不重算 assert_not_called）。**六文件 68 passed**（含 recalc/approvals/algorithm isolation/regression 20260817）。
- **② approvals detail/pending 字段 → 复核已闭环（无代码缺口）**：F17 文档 §4 已于 2026-08-29 更正并测试锁定（detail L44-46 断言 student_id、pending L78-81 断言 status；37 passed）。本次复核再次确认，无新增改动。
- **③ 8 个 utils 半成品删除 → 裁决：保留**（不执行删除）：validation（scores/users/device/import_export 运行引用）、startup_cleanup（run.py）、transaction_retry（import_export）均有**存活运行时引用**；structured_logger/validation_middleware/validators/smart_reconnect 运行时零引用但各有专属测试、删除需连带删测试且无收益（smart_reconnect 已 restore 无引用者另议）；__init__ 删除破坏包聚合。判定 8 文件均非安全删除目标，维持 HEAD 终态。
- **备注**：旧 0-100 前的历史行（raw/0-1 混存）将在下次全量重算后归一；读取已不兼容旧 raw（behavior）语义，UI 无该分量消费已验证。
- **全量复核（WZk7BP）收官**：**2080 passed / 8 skipped / 0 failed，PYTEST_EXIT=0，1218s**——stub 清理基线确认，与 09-04 基线一致。

## 剩余项逐步推进（① G5 真验完成推送 / ② utils 子集清理评估驳回 / ③ vitest 基线运行中）
- **① CI G5 OpenAPI 真验落地（cc8f8ae 推送核实）**：修复 run_regression.sh step2 **快照路径 bug**（脚本默认解析 backend/api-docs 不存在 → 恒 exit2 被跳过；真快照在根 api-docs/）→ 显式 `--snapshot $ROOT/api-docs/openapi.json` + `--strict`，rc 分档 0 OK / 2 后端未起或快照缺失跳过 / 其余漂移失败。ci.yml 回归前 nohup 起后端轮询 `/api/swagger.json`（90s 降级跳过、MQTT 不阻塞）。**本地实弹**：起后端 VERIFY_RC=0，469 vs 469 零漂移。⚠️ 真验过程清理了多轮残留 run.py 进程与 5000 端口（含历史遗留），已全清。
- **② utils 子集清理（structured_logger/validation_middleware/validators/smart_reconnect）→ 评估驳回**：穷尽核验推翻"低风险"假设——smart_reconnect 被 `utils/__init__.py:34` 导入（包初始化即执行，删除须 __init__ 手术）；structured_logger 有 test_utils.py 共享冒烟（非独立测试）；validators 与 validation.py:455 validate_request 形参同名易误伤。删除需 __init__ 改造 + 共享测试迁移，收益低、破坏风险高 → 维持 HEAD，后续如需动须单独立项走全回归。
- **③ 前端全量 vitest（89yWfo）**：threads 池全量复跑固化今日基线（约 8 分钟），完成后按三要素收口判定。

## utils 子集清理完成（1d8cdfd 推送核实）+ ⚠️ 并行大范围删除事件处置
- **清理落地**：删 4 模块（structured_logger/validation_middleware/validators/smart_reconnect）+ 3 专属测试；`utils/__init__.py` 摘除 smart_reconnect 导入与 __all__ 条目（此步须与 __init__ 手术同批，验证 import utils OK）；`test_utils.py` 行级摘除 structured_logger 顶层导入与方法（字符串整段匹配在此环境失配 → 改行级删除，8 passed）。9 文件 2011 行删除；全量 pytest（i1HDPz）复核新基线运行中。
- ⚠️ **并行会话并发删除（高危，两度发生）**：19:33 起工作区反复出现**非本会话**大范围删除（tests 目录 141 文件 → 全树 537+，含 api/、Dockerfile、env、utils/__init__.py 等），伴随 index.lock 频繁出现；已两次 `git restore` 全量恢复 HEAD，本批提交仅含 9 个显式路径（未用 git add -A）。**判断存在另一 WorkBuddy/自动化窗口在同一仓库执行破坏性删除，须用户停止该进程并确认后再继续本仓库任何大批次操作**。
