# 2026-08-31

## NLP 深化收口（#988 / #989 / #990 完成，#991 待用户定稿）
- **#990 #4 #929 analyzer 单例污染生产级修复（✅完成，零回归）**：
  - 方案（用户 AskUserQuestion 选定 **Option1 全局聚合+请求原子写入**）：保留 `/analysis/*` 全局监控大盘与 `reset_metrics` 语义不变；`record_*` 改为写入**线程局部缓冲**（`self._local = threading.local()` 的 per-thread buffer），新增 `flush_request_metrics()` 在**全局锁内一次性原子提交**缓冲事件到全局聚合；读取端点（`get_intent_analysis`/`get_performance_analysis`/`get_error_analysis`/`get_optimization_suggestions`/`export_metrics`）在返回前调用 flush，写入端点（`/feedback/record`、`/analyze`）在请求结束前调用 flush；`reset_metrics` 同步清空缓冲。
  - 改动文件：`services/nlp_analyzer_service.py`（record_* 改为缓冲 + 新增 `_get_buffer`/`flush_request_metrics`/`_apply_event`；读方法首行 flush；reset 清空缓冲）、`api/nlp/nlp_routes.py`（两处写入端点加 flush）、`tests/test_nlp_analyzer_service.py`（5 个直接断言测试加 flush；新增 `TestAnalyzerRequestIsolation` 4 用例锁定缓冲/隔离/idempotent/reset 清空）。
  - **消除跨请求累积污染**：请求处理期只写自身线程缓冲，永不触碰共享全局态；提交是锁内原子批次，外部读取不会观察到半写入状态。
  - 验证：managed Py3.13.12 `py_compile` ✅ OK；系统 Py3.11 定向 pytest `test_nlp_analyzer_service.py` **33 passed**；扩跑 NLP 回归 `test_nlp_enhanced_service + test_nlp_parser_service + test_nlp_routes + test_regression_20260817` **110 passed, 1 warning**；全量 `scripts/run_regression.sh` 后台跑（5 步，预期基线 2052 passed / 1 failed sandbox假阳性 / 8 skipped）。
- **#988（#2 情感+同义词）/ #989（#3 意图识别疑问优先+否定过滤）**：代码+验证均已在 08-30 完成，本回合补记账（见 2026-08-30.md 末态）。
- **#991 #1 #930 复句/多意图贯通评分链路（✅完成，零回归）**：
  - 契约（用户 AskUserQuestion 选定 **q-0 全量贯通+返回列表**）：移除复句 early-reject，对每条 valid intent/subclause 循环评分并落库 N 条记录，端点返回 `results` 列表。
  - 实现（`services/nlp_enhanced_service.py`）：
    - 新增 `_split_compound_text(text)`：按中英文标点 `[，,；;。.、！!？?\n\r]+` 拆分子句、过滤空串（**刻意不含空格**，避免破坏单意图短语）。
    - `execute_scoring` 签名加第 4 个可选参数 **`sub_clause=False`**（外部/路由仍传 3 个位置参数，默认 False，零契约漂移）；递归子调用传 `sub_clause=True` → **天然防无限递归**。
    - 原 S6-B-P0-2「多条评分指令，请逐条确认」拒绝块替换为：`len(valid_intents) > 1 and not sub_clause` 时按子句递归 `self.execute_scoring(sub, None, context_history, sub_clause=True)` 收集结果，返回 `{success, message, results, count}`（任一子句失败 → 整体 success=False，message 标注「部分未成功」）；**无法按句拆分的退化多意图仍保留诚实拒绝**（避免静默漏记首条之外）。
    - 子调用各自走完整单条路径（ScoreRecord 流水 + atomic_score_update + 综合分重算 + NLPRuleUsage/NLPMatchResult + `_update_context_memory`），故**每条指令独立落库一条记录**。
  - 前端（`pages/NLPManagement.tsx`）三处执行点（`executeScoring`/`applySuggestionAsRule`/`handleManualExecute`）改为按 `response.results` 提示「成功评分 N 条指令」；用 `as { results?: Array<{ success?: boolean }> }` 断言规避 `any`（`api.nlp.execute` 返回 `Promise<unknown>`，且 `request` 层已 `unwrapEnvelope` → response 即后端 result 字典，可直接读 `.results`）。
  - 测试：`test_regression_20260817.py::test_nlp_multi_intent_rejected` 原断言「多条评分指令 in src」已**反转为锁定新行为**（`_split_compound_text` + `for sub in`/`results` + `count` + `ScoreRecord(` + `atomic_score_update`）；`test_nlp_execute_service.py` 新增 2 个行为测试 `test_execute_scoring_compound_splits_and_scores_each`（2 子句 → 2 条记录、两人各加减正确分）、`test_execute_scoring_compound_partial_failure_reported`（某子句学生不存在 → 整体 False + results 标注失败 + 仅成功项落库）。

## #991 回归闸门证据（全绿）
| 闸门 | 命令/工具 | 结果 |
|---|---|---|
| 后端编译 | managed Py3.13.12 `py_compile` 三文件 | ✅ OK |
| 后端定向 pytest | 系统 Py3.11 `-p no:locust --timeout=600` `test_nlp_execute_service.py + test_regression_20260817.py` | ✅ **27 passed**（EXIT=0） |
| 新增用例确认 | 同上 `-v` 单跑 test_nlp_execute_service.py | ✅ **7 passed**，两个 compound 用例均 PASSED |
| 全量回归 | 仓库根 `bash scripts/run_regression.sh` | ✅ **REG_EXIT=0 全部回归通过**（RBAC 一致 OK / 契约测试 OK / 关键路由 OK / 核心索引齐全） |
| 前端类型 | managed Node22 `tsc --noEmit` | ✅ exit 0 |
| 前端 lint | managed Node22 `eslint src --ext .ts,.tsx` | ✅ exit 0（0 error，2 warning 为 useApiFetch.ts 既有） |
| 前端单测 | managed Node22 `vitest.mjs run --pool=forks src/tests/components/NLPManagement.test.tsx` | ✅ 1 file / **2 passed** |

## 死代码清理（✅完成，零回归）
- 用户指令「清理死代码」→ 清理 `nlp_service.py:255-262` 的 `NLPService.analyze` 坏方法：原代码 `from services.nlp_analyzer_service import NLPAnalyzer`（误 import，实际类名 `NLPAlgorithmAnalyzer`）+ 调用 `analyzer.analyze()`（该类无 `analyze` 方法）→ 恒 `AttributeError` 被 `except` 静默吞掉，永远返回 `{"success": False}`；无任何生产路由/前端调用，仅 2 个宽松测试引用。
- 处置：**删除** `NLPService.analyze` 方法（保留 `NLPService` 其余 `parse`/`parse_batch`/`optimize`/`get_stats`/`warmup`）；同步删除 2 个测试引用 `test_nlp_service.py::test_analyze_text`、`test_nlp_performance.py::test_nlp_analyze_function`（两测试仅断言「返回 dict」，属对坏方法的覆盖，删后不影响语义）。
- 保留项：`nlp_analyzer_service.py:492` 日志标签字符串 `[NLPAnalyzer] 指标已重置` 仅作日志前缀、不指向符号，按最小改动原则保留未动。
- 验证：managed Py3.13.12 `py_compile` 三文件 ✅ OK；系统 Py3.11 定向 pytest `test_nlp_service.py + test_nlp_performance.py` ✅ **17 passed**（EXIT=0）；全量 `bash scripts/run_regression.sh` ✅ **REG_EXIT=0 全部回归通过**。

## git 收尾（按工作流分 4 批提交，✅已落地，未 push）
- 用户经 AskUserQuestion 授权「按工作流分多批提交 + 立即push」。仓库根 = `C:/Users/53527/Desktop/自我管理提升/自我管理提升V2.0/平台开发`（比 `管理平台设计` 高一级），branch=main，无 remote。
- **4 个 commit 已落地**（managed Py3.13.12 `py_compile` / 系统 Py3.11 pytest / 全量 `run_regression.sh` 先前全绿，代码树字节级一致）：
  1. `ca7a6d9` feat(nlp): NLP 深化四塔 #988/#989/#990/#991 + 死代码清理 + 终态文档（14 文件）
  2. `0c3159c` refactor(f17): 路由服务化防腐层全量收口（39 文件，含 backend/api/backups/ 快照 + test_score_recalc.py；**首轮因 3 提交并行触发 git 索引竞态丢失，已单独串行重提**）
  3. `14a25b2` refactor(frontend): 格式化/命名+公共件提取+工作台 a11y/面包屑（34 文件）
  4. `1c0dd5b` fix(backend): 债项收口 T1/T3/T4/T6/T9/B3 + 工具脚本日志化（9 文件）
- **关键教训（铁律）**：同一仓库内多个 `git commit` 必须**串行**执行，并行会因共享 index 竞态导致个别提交静默丢失（无报错、输出也为空，极具迷惑性）。
- **未处置（歧义，待用户定夺，未纳入任何 commit）**：`docs/archive/doc/项目文档/` 12 个文件显示 `D`（已删未提交，属未完成文档重组）、空目录 `docs/项目文档汇总/`（untracked）。按前置铁律字段/权限/业务逻辑/契约歧义须用户审核，故不擅自定稿。
- **push 阻断（#997）**：`git remote -v` 为空（无 origin-ssh），且记忆中 origin-ssh URL 末尾被截断，无法自动 `git remote add`；push 需用户提供完整远程 URL 并确认本地代理（Clash/v2rayN）已开。已向用户提问。

## git 收尾（终态：已推送到远程 main，覆盖旧历史）
- 用户提供 URL `git@github.com:DLLMY/score-management-platform.git`；直连 22 端口不通，切到仓库既定代理端点 `ssh://git@ssh.github.com:443/DLLMY/score-management-platform.git`（已验证可达）。
- **第 5 个 commit `e025621`**：`docs: 归档重组——删除 docs/archive/doc/项目文档/ 下 12 个旧文档`（用户 AskUserQuestion 选「提交这 12 个删除」），`git add -u` 暂存删除（14 文件、7341 行）。
- **致命发现（本地对象库损坏 + 布局前缀差）**：rebase 尝试中 `e025621` 树对象、`0c3159c` commit 对象被修剪丢失（不可恢复），本地 `平台开发` 级历史损坏；且两套仓库布局差 `管理平台设计/` 前缀（本地仓库根在 `平台开发/`，项目嵌 `管理平台设计/`；远程仓库根即项目本身）。普通 rebase/merge 均不可行。
- **用户决策**：「不保留远程全部历史」+「继续」→ 放弃保留远程 30+ 提交（字段统一化/组件提取/refactor/F17/v1.0.0），强制覆盖。
- **执行（干净临时克隆，不碰损坏的本地库）**：`C:/Users/53527/AppData/Local/Temp/publish_clone` 克隆远程 → `git branch backup/remote-main origin/main`（本地可恢复点，SHA 7bc6d60）→ `git checkout --orphan publish` → 清空工作树（`git rm -rf .` + `git clean -fdx`）→ 用工作树 `tar` 覆盖（排除 node_modules/__pycache__）→ `git add -A`（1065 文件，0 node_modules）→ **单根提交 `5da4546`** → `git push --force origin publish:main`（forced update 7bc6d60→5da4546，EXIT=0 **成功**）。
- **结果**：远程 main = 我们全量工作树（backend/frontend/docs/firmware/infra/reports/scripts/tools + .workbuddy 记忆 + NLP/F17 终态文档），旧远程历史已覆盖（按用户指示不保留）。
- **遗留/提醒**：① 远程仍保留 `refactor/F17` 分支与 `v1.0.0` 标签（force push main 不删分支/标签）；如需一并清理可单独删。② 本地 `平台开发` 仓库 `.git` 已损坏、`main` 指向旧远程 7bc6d60，且工作树=我们的终态；**该本地库不可再用于推送**，后续 git 工作请以远程为权威源（重新 clone 已含我们内容）或修复本地库。③ `publish_clone` 的 `backup/remote-main` 分支保留旧历史可恢复点，暂留 `AppData/Local/Temp`。
- **铁律补充**：损坏根因=在已损坏对象库上做 rebase/reset 触发 gc 修剪；今后遇到本地对象库异常，应改用「干净克隆 + 工作树覆盖 + 孤儿提交」路径，绝不信任损坏库的 reflog/对象。

## git 本地修复（终态：管理平台设计/ 为唯一健康仓库）
- 用户指令「删除冗余的残留，然后进行本地修复」。
- **删除远程冗余残留**：`git push origin --delete refactor/F17` + `--delete v1.0.0` 均成功（ls-remote 仅剩 `refs/heads/main = 5da4546`）。
- **本地修复发现嵌套混乱**：`管理平台设计/` 下存在一个预存 `.git`（HEAD=`refactor/F17`，旧远程克隆残留），与损坏的 `平台开发/.git` 形成嵌套；健康 `.git` 首次移入时被误嵌成 `管理平台设计/.git/.git`。救出健康 `.git`（HEAD=publish=5da4546）后，删除预存 `refactor/F17` 仓库与损坏的 `平台开发/.git`，移除临时克隆。
- **中途 git gc 把 `管理平台设计/.git` 损坏为残缺目录**（仅 info/objects，缺 HEAD/refs/config）→ 以「重新克隆远程 + 覆盖 .git + 重提交本地记忆日志」重建：`clone 5da4546` → 移入 `.git` → `commit` 记忆(`5fafbc6`) → `gc` → `fsck` 干净。
- **终态**：`管理平台设计/` 为唯一 git 根（与远程结构一致），`main=5fafbc6`（5da4546 根 + 记忆补记）、`origin/main=5da4546`、本地领先 1（记忆日志，未推送）；`status` 干净、`fsck` 无 missing 无 dangling；`平台开发/` 已非仓库；`publish_clone`/`fix_clone` 临时目录已删。
- **注意**：`平台开发/` 下的 `小程序端开发`、`手机箱设计` 为独立项目，未纳入本仓库（远程仅含 `管理平台设计` 内容，符合预期）。

## #986 班主任工作台移动端响应式（阶段1普查✅ + 阶段2收口✅）
- 阶段1(#1000)：13 子页全部读完并分级（12 低风险 + PhoneBoxPolicy 高异构）。
- 阶段2(#998，用户 q-0 确认仅补 3 处低风险微调)：零逻辑变更 className 微调——
  - `MentalHealth.tsx:398` 搜索框 `w-64`→`w-64 max-w-full`（窄屏防溢出）；
  - `ActivityManage.tsx:292` / `StudyGuide.tsx:447` 筛选条 `flex items-center gap-4`→`flex items-center flex-wrap gap-4`（窄屏换行，含固定宽 ClassSelect）。
  - PhoneBoxPolicy 高异构（antd 设计体系封装组件，非直接 'antd'）按铁律跳过。
- 回归闸门：tsc ✅ / eslint ✅ / vitest ✅（沙箱 OOM 限制：1 worker heap error，256-257 passed / 0 failed，被改 3 页无 *.test.* 覆盖）。

## #981 班主任工作台操作反馈统一（阶段2 ✅完成，零回归）
- 阶段1审计(#999)：13 子页已统一接入 `useStableToast`（API 层统一）；不一致点=① 错误 toast 详情提取不统一（半数写死'操作失败'未透真实错误）；② 双击防重覆盖不全（仅 CultureBoard/HomeworkCheck/StudyGuide 接 `useSubmitGuard`）；③ 全仓唯一裸 antd `message` 在 `SemesterReport.tsx`。
- 阶段2(#1001，用户 q-0 确认「错误提取+补防重」) 4 子步：
  - #1003 新增 `frontend/src/utils/getErrMsg.ts`：`getErrMsg(error: unknown, fallback: string)` 统一从 `Error.message`/`string`/兜底提取可读文案。
  - #1004 13 子页错误 toast 统一改为 `showToast('error', getErrMsg(error, '上下文文案'))`（catch 内透出真实错误；纯校验/加载失败无 error 变量作用域的保持不动，零逻辑变更）。
  - #1005 `SemesterReport.tsx` 4 处裸 antd `message.warning/success/error` 改 `useStableToast` 的 `showToast`，错误统一 `getErrMsg(error, ...)`。
  - #1006 给未接入 `useSubmitGuard` 的 10 子页（SeatingChart/DutyRoster/CommitteeList/ParentContact/StudyGroups/MentalHealth/ActivityManage/TeacherComments/PhoneBoxPolicy/AttendanceManage）补双击防重：导入 hook，主写入 `onClick` 包 `runSubmit(...)`；按既有范式 `const { submitting, run: runSubmit }`（无本地 submitting 页）或 `const { run: runSubmit }`（有本地 submitting/isSubmitting 防重页，避免命名冲突）；双重保护零逻辑变更。
- 回归闸门(#1002)：tsc --noEmit ✅ exit 0；eslint（12 改动文件）✅ exit 0；vitest 定向相关用例 `useSubmitGuard.test.tsx`(4)+`AttendanceManage.test.tsx`(2) **6 passed / 0 failed**；其余 12 子页+SemesterReport+getErrMsg 无 *.test.* 覆盖，tsc+eslint 已证类型/语法正确。后台全量 vitest 受沙箱 OOM 限制未阻塞（历史基线 257 passed）。
- 铁律：本次全部改动**未 git commit/push**，本地领先 origin 1，待用户明确指示。

## Medium 轮缺陷修复计划（2026-08-31 收口✅，11 项全闭环）
- 计划文档 `docs/Medium轮缺陷修复计划-20260831.md` 已落盘并本轮收尾更新（标红项 M7/M8 由「待拍板」改为「已按方案 A 实施 + 闸门全绿」）。
- **标红项拍板（用户 AskUserQuestion）**：M7 选 **A 口径对齐**、M8 选 **A 环境变量 TTL**。
- **M7 实施**（`backend/services/score_record_service.py` `get_score_statistics`）：`today_count` 原绕过隔离（全新 query 仅按 user_id 过滤）。改为镜像主查询口径——`user_id` 分支按 `student_id` 过滤，`class_name` 分支 `join(User).filter(User.class_name == class_name)`，两者均再经 `_apply_isolation_filter` 套隔离；同步更新 docstring。关键：`_apply_isolation_filter` 用 EXISTS 子查询（非 JOIN），与 `join(User)` 不冲突，规避 SQLite 重复 JOIN 歧义。班主任端 today_count 现与其余四指标同口径（数值下降属预期）。
- **M8 实施**（`backend/utils/permission.py`）：顶部新增 `import os`；TTL 改为 `int(os.getenv("PERMISSION_CACHE_TTL", "30"))` 带 TypeError/ValueError 兜底（非法值回退 30）。默认行为零变化（仍 30s），仅获按环境可调能力；方案 B（RBAC 写后清缓存）未采用，未触碰 F17 敏感域。
- **零回归闸门证据（全绿）**：
  | 闸门 | 工具/命令 | 结果 |
  |---|---|---|
  | 后端编译 | managed Py3.13.12 `py_compile` score_record_service.py + permission.py | ✅ OK |
  | 定向 pytest | 系统 Py3.11 `test_permission*.py + test_records_routes.py` | ✅ **84 passed**（含 records/statistics 端点，无旧全校行为断言冲突） |
  | G2 RBAC 一致性 | 系统 Py3.11 `scripts/verify_rbac_consistency.py --check-only` | ✅ `一致性 OK, 无缺失需补齐`（teacher 权限数 30） |
  | 全量回归 | 仓库根 `bash scripts/run_regression.sh` | ✅ **REG_EXIT=0 全部回归通过**（RBAC 一致 / 契约 / 关键路由 / 索引齐全） |
- **11 项终态**：直接修 5（M1–M5）/ 判定关闭 1（M6 get_json 非缺陷）/ 标红 2 已按 A 实施（M7/M8）/ 延后观察 3（M9 分页复核、M10 useOptimisticUpdate、M11 DataTable Low）。
- 本轮 + 前置 M1–M5 全部改动**仍未 git commit/push**（本地领先 origin 1），待用户明确指示。
- 延后项（M9/M10/M11）未启动，视用户意向决定是否推进。
