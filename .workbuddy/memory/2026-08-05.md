# 2026-08-05

## 前端重复页面去重（删除 ClassTimeSettings）
- 用户反馈"两个页面重复"：实际是 `CourseSchedule.tsx`（/course-schedule，侧边栏"课程表管理"）与 `ClassTimeSettings.tsx`（/class-time-settings，侧边栏"时间规则设置"）都渲染周×节课程表网格、都调 `api.courseSchedules.getAll()` 编辑同一张 `course_schedules` 表 —— 后者是重复克隆，且标签"时间规则设置"与实际内容不符（内部标题还写着"课程表管理"）。
- 真正的时间规则编辑器是 `TimeRuleList.tsx`（管 TimeRule，含 allow_unlock 字段，正是上课拦截要用的），但此前未挂路由/侧边栏（孤儿页）。
- 处置（用户选"删重复页，菜单指向 TimeRuleList"）：
  - `App.tsx`：`/class-time-settings` 路由 element 由 `<ClassTimeSettings/>` 改为 `<TimeRuleList/>`；lazy import 同步改。
  - `pages/index.ts`：re-export 由 ClassTimeSettings 改为 TimeRuleList。
  - 删除 `ClassTimeSettings.tsx`（备份 `src/pages/ClassTimeSettings.tsx.bak`）。
  - `TimeRuleList.tsx`：清理未用的 `useConfirmDialog` import + `showConfirm` 解构（避免 noUnusedLocals 报错）；删除按钮本就用 `window.confirm`，可用。
- 验证：tsc 因本机 TS 4.9.5 与 tsconfig 的 `moduleResolution:bundler` 不兼容而无法解析（预存环境问题，非本次改动）；改用 ESLint 检查 App.tsx/index.ts/TimeRuleList.tsx = 0 errors（仅 1 个预存 console 警告）。grep 确认活代码无 ClassTimeSettings 引用。
- 影响：侧边栏"时间规则设置"现在真正打开 TimeRule 管理页，为"上课时间下发互斥"功能提供了 TimeRule 维护 UI；课程表编辑仅剩 CourseSchedule 一处。

## 上课时间拦截 Phase 1 后端 — 测试 + 验证脚本 + 重启冒烟（本次续作）
- 实现（前序已完成）：ClassTimeChecker 4 元组升级、9 条下发路径接入（MQTT 5 端点 + 积分录入/审批/用户变更 + WOL/定时通知/模板发送/Celery/OTA）、学生自助开箱上课拦截、GET /api/course-schedules/now 端点、notify_audit 表、RBAC 新增 `notification.force_send`(仅超管)+`timetable.rule.manage`(班主任)。
- 测试补全（`backend/tests/`）：
  - `test_class_time_checker.py`：`is_notification_allowed` 断言 3→4 元组；新增 `test_is_notification_allowed_class_in_session` 用真实 DB 行（ClassPeriod 覆盖当前时刻 + CourseSchedule）验证按班级课表反查命中 → 拦截 CLASS_IN_SESSION。
  - 新增 `test_remote_notify_class_time.py`：5 个端点（/send、/broadcast、/send_to_device、/score_change、/test）上课时间拦截 + 审计落库（GLOBAL_TIME_RULE/CLASS_IN_SESSION）+ force_send=True 放行（写 FORCE 审计）。
  - 新增 `test_course_schedules_now.py`：/now 端点带/不带 class_info_id 结构与 in_session 逻辑。
  - 运行：`PYTHONPATH=. python -m pytest tests/test_remote_notify_class_time.py tests/test_course_schedules_now.py tests/test_class_time_checker.py -p no:locust --timeout=120` → **18 passed**。
- 验证脚本 `backend/scripts/verify_class_time_lock.py`（只读，不落库）：基于真实 DB 打印全局时段/当前节次/广播决策/班级上课状态/按班通知决策，结论判是否拦截；支持 `--class-info-id` 与 `--live`（探测本地 /now）。
- 重启后端冒烟：按已知约束（Flask-SocketIO 不自动 reload）强杀旧 PID 25560，用 `run.py --env development --host 127.0.0.1 --port 5000`（系统 Python 3.11.7）重启，新进程 PID 9008/25612（12:29 启动）。
- 端到端冒烟（活后端）：登录 admin/123456 → GET /now 返回 `is_during_class_time=true`（命中 `午休时间` 12:00-14:00 TimeRule）；`POST /remote_notify/send` force_send=False → `success:False`（"当前处于上课时间（午休时间）…暂停"）；force_send=True（超管 all）→ `success:True`。确认上课时间硬拦截与强制发送逃生舱均生效。
- 临时冒烟脚本 `scripts/_smoke_now.py` 用完即删；保留 `verify_class_time_lock.py` 作回归资产。

## 上课时间拦截 Phase 2 前端 — 徽章 + 强发复选框 + 权限对齐（本次续作）
- 基础件（前序已建，本次校验通过）：`useClassNowStatus(scope, deviceId)` Hook（3 作用域 broadcast/class/global，60s 轮询+unmount 清理+reqSeq 防乱序）；`ClassStatusBadge` 组件（直接用 `usePermissions()` 计算 `canForceSend`，无权限则静默隐藏复选框，避免 PermissionGuard 全屏"权限不足"炸页）；api.ts 已加 `ClassNowStatus`/`getNow` + 各下发接口的 `force_send` 字段。
- 四个下发入口接入徽章+强发（`force_send` 经 `notification.force_send` 权限门控）：
  - `RemoteNotify.tsx`：主表单 `forceSend` state + `useClassNowStatus`（scope 随 mode 变：broadcast→broadcast、device/score_change带设备→class、test→global；device 模式按 `form.device_id` 反查）；徽章在发送按钮上方；`handleSubmit` 把 `force_send` 透传 sendToDevice/broadcast/scoreChange/test；定时通知"立即发送" `scheduledForceSend` + 第二枚 `scheduledClassNow`（broadcast 域）徽章 + `api.scheduledNotify.trigger(id,{force_send})`。
  - `WakeOnLan.tsx`：`wolClassNow`（scope=global，WOL 按 MAC 全校唤醒只判全局时段）+ `forceSend` 透传 `api.wakeOnLan.wake/wakeBatch`。
  - 模板/定时通知原是 RemoteNotify 内 Modal（非独立页），已随主表单一起接入；`notifyTemplates.use` 前端当前无调用点（仅保存）。
- **权限对齐关键修复（避免引入回归）**：后端 TimeRule/节次路由原本要求 `time-rule.manage`/`time-rule.view`，但 RBAC 目录只注册了 `timetable.rule.manage`（且已授予 admin 角色），`time-rule.*` 是孤儿权限→除超管外全 403。故将 `api/scores/time_rules_routes.py` 与 `api/scores/class_periods_routes.py` 的 `time-rule.manage`/`time-rule.view` 全部改为 `timetable.rule.manage`；前端 `TimeRuleList.tsx`、`ClassPeriodSettings.tsx`（11 处） likewise。现 admin 角色可正常管理时间规则与节次。
- 验证：前端 `vite build` 两次均成功（23-32s，exit 0）；后端 5 个 route 文件 `py_compile` OK；**18 passed** 回归测试通过；活后端冒烟 admin 登录 → `/now` 200（字段齐全）、`/api/time-rules`、`/api/class-periods`、`/api/class-periods/active` 均 200（改前会 403）。调度器 `process_scheduled_notifications` 复用模块级 `ClassTimeChecker` import，`force_send=False`，无 NameError 风险。
- 约束提醒：改 backend 须强杀 python 重启（Flask-SocketIO 不 reload）；`run_in_background:true` + 直接跑 `run.py` 才能让进程存活（裸 `&` 会被父 shell 退出带走 → 端口秒关）。

## 上课时间拦截 — 真实集成测试补齐（收尾）
- 既有 `test_remote_notify_class_time.py` 用 monkeypatch 直接替换 ClassTimeChecker 方法，**从未真正跑过**「is_during_class_time() 读真实 TimeRule → is_notification_allowed/is_broadcast_blocked → 拦截 → 审计」完整链路。
- 新增 `tests/test_class_time_real_integration.py`：
  - fixture `blocking_time_rule` 插入一条覆盖当前时刻的真实 TimeRule（day_of_week=-1，start/end 包住 now），teardown 清理。
  - `test_real_global_block_and_force_send`：/now 反映 is_during_class_time=True → 无 force_send 广播 success=False+写 GLOBAL_TIME_RULE 审计 → force_send=True（超管）success=True+写 FORCE 审计且不新增拦截审计。
  - `test_no_block_when_no_rule`：无覆盖规则时广播正常放行（隔离证明拦截确由规则触发）。
  - publish_mqtt monkeypatch 为 no-op，免本地 broker 依赖。
- 回归：`pytest tests/test_class_time_real_integration.py tests/test_remote_notify_class_time.py tests/test_class_time_checker.py tests/test_course_schedules_now.py` → **20 passed**（18+2）。
- 结论：上课时间下发互斥功能（Phase1 后端 + Phase2 前端）已端到端闭环并通过真实集成测试，开发完成。待办建议（非阻塞）：把 `timetable.rule.manage` 另行授予"排课教务"专用角色（现仅 admin 持有）；前端视觉验证可在 `frontend` 跑 `npm run dev` 观察徽章变红+强发复选框。

## 班主任手机箱开箱策略（phonebox policy）— 全功能实现 + 验证（收尾）
- 用户诉求：「关于手机下发时间，我希望班主任可以自由决定」。前序已确认设计：q-0 预设时段+一键放行（推荐）；q-1 仅自己班级（推荐）。
- RBAC 落库坑（关键）：`app/__init__.py` 启动只跑 `db_init.init_database`（create_all+默认admin+MQTT），**不**调 `init_default_permissions/init_default_roles`（后者在 `utils/initializer.init_rbac_data`，正常启动不跑）。`seed_rbac.py` 是破坏性全量重建（DELETE 5 张RBAC表）→ 不能对已有数据的库跑。新增 `phonebox.unlock.manage` 用**增量幂等迁移** `scripts/migrate_phonebox_permission.py`（INSERT权限→追加到 teacher 角色 permissions CSV→插 role_permission_mappings→末了校验；二次跑全[skip]），已对真实库执行并确认映射 `['teacher']`。4 处同步：模型/路由装饰器/迁移脚本/MEMORY.md。
- 后端改动（`backend/`）：
  - `api/phonebox/phonebox_policy_routes.py`：新增 `_read_class_info_id()`（同时读 query+body，修"明明传了却报未指定"）；4 端点 GET/PUT/override/cancel-override 统一用；`put`/`override`/`cancel_override` 改用 `status_code=403/400/400`（`code=` 只改业务码不改善 HTTP）；`put` 增加 `except ValueError→400`（格式错 400 而非 500）。
  - `services/phonebox_policy.py`：新增 `normalize_windows(windows)`（校验+归一化到 `day/start_hour/start_minute/end_hour/end_minute`；兼容 `{"start":"12:00","end":"12:30"}` 字符串自动转；拒 day∉[-1,0-6]、hour 0-23、minute 0-59、end-before-start）；`set_policy()` 调它并 `raise ValueError`（路由→400）。**修了一个静默失败坑**：旧 `unlock_windows` 直接存 JSON，若前端传 `start/end` 字符串则 `evaluate()` 的 `_now_in_windows` 只读 `start_hour` → 窗口永不命中。
  - `services/mqtt_message_service.py::handle_unlock_message`：去掉多余的 `if user.current_score<60`（重复，helper 内已有分数门），统一走 `_deduct_and_unlock` 单出口。
  - `evaluate()` 四态优先级：ALLOW_OVERRIDE > ALLOW_WINDOW > BLOCK > DEFER（DEFER 退回全局 TimeRule）。ALLOW_* 跳过上课时间拦截但**不**跳分数门(<60→score_low)。
- 数据模型：`phone_box_policy` 表（FK→class_info，删班级联清，走 cascade_delete_related_records）。
- 前端 `frontend/src/pages/PhoneBoxPolicy.tsx`：新增 `loadError` state；`isAdmin && !classInfoId` 时短路（不请求，显示空态提示联系管理员绑班）；catch 时显示 AlertCircle 空态卡而非破表单。
- 测试 `backend/tests/test_phonebox_policy.py`：新增 `TestNormalizeWindows`(6) + `TestStudentUnlockUsesTeacherPolicy`(5+1)。共 23 phonebox 测试（6 evaluate+6 routes+6 normalize+6 unlock E2E），**逐个跑全 PASS**（conftest 第2+用例崩，须单跑）。
- 验证脚本 `backend/scripts/verify_phonebox_policy.py`（只读，lightweight app）：打印每班策略+`evaluate()` 决策+DEFER 回退理由；支持 `--class-info-id`/`--at HH:MM`。实测：一年级1班 18:02→DEFER，12:10→ALLOW_WINDOW（命中 12:00-12:30）。
- 活后端冒烟（强杀旧 PID 重启后）：未授权 `/api/phonebox-policy` → 401（路由在）；4 端点全过：A) body cancel-override 200；B) query override 200；C) minutes=0→400；D) admin 无班→403；E) 班主任自动解本班 200；F) 班主任跨班→403；G/H) 班主任本班 override 200 / 跨班 403；I) `{"start":"12:00","end":"12:30"}` 自动归一 200；J) end-before-start→400；K) day=9→400。冒烟脏数据已清（删掉畸形 window 行）。
- 前端 `vite build` 两次均过（PhoneBoxPolicy chunk 7.91 kB）。
- 结论：班主任可自由决定本班手机箱开箱（预设时段 + 一键临时放行），上课时间全局拦截与分数门保留，功能开发完成并通过端到端验证。

## 排课教务专用角色（timetable_manager）— 新建并授权（收尾）
- 用户确认需要：把 `timetable.rule.manage` 另行授予「排课教务」专用角色（原仅 super_admin/admin 持有）。
- 调研 RBAC：`init_default_roles()` 默认角色集**无**「排课教务」角色；`timetable.rule.manage` 仅授予 super_admin/admin。启动链 `app/__init__.py`→`app/db_init.py::init_database` **不**调 RBAC 播种（会播种的 `utils/initializer.py` 不在启动链上），故用增量幂等脚本而非破坏性 `seed_rbac.py`。
- 新建 `scripts/migrate_timetable_role.py`（模板级）：运行前自动备份 db（`.bak_timetable_role_<ts>`）→ 确保 `timetable.rule.manage` 等权限码存在（缺失则补）→ 无则 INSERT `role_permission`(timetable_manager/排课教务) → 逐条 INSERT `role_permission_mappings` + 同步 `permissions` CSV → 打印校验；可重跑。
- **重要发现**：脚本首次跑报 `inserted missing permission: timetable.rule.manage` —— 现网 `permissions` 目录表**未被 seed 过**（init_default_permissions 运行时从未执行）。鉴权不受影响（`has_permission` 只读 RolePermissionMapping+AdminRole），但 `/rbac/permissions` 列表 UI 会不全。可选收尾：幂等跑一次 `init_default_permissions()` 补齐目录（仅 INSERT 缺失，安全）。
- 已授予 `timetable_manager` 的权限包（10 项）：`timetable.rule.manage`(核心) + period.manage/period.view/schedule.manage/schedule.view/class.view/subject.view/rule.view/student.view/notification.view。
- 隔离验证 `scripts/verify_timetable_role.py`：临时建 `__verify_tmp__` 管理员绑 `timetable_manager` → `has_permission` 对 `timetable.rule.manage/period.manage/schedule.manage/student.view`=True，`notification.force_send/all`=False（无越权），用后清理临时账号。
- **生效还需分配用户**：脚本只建角色+授权，未绑任何管理员；实际让某人成为排课教务需在角色管理 UI（或 `PUT /api/rbac/admin-roles/<id>`）把目标管理员分配 `timetable_manager` 角色（写 `admin_roles`）。
- 记忆同步：MEMORY.md「RBAC 新增权限的正确落库姿势」补充 permissions 目录未 seed 坑 + 新角色模板脚本 + 分配用户一步；「上课时间下发互斥」的 TimeRule 维护标记为由 timetable_manager 角色负责（已完成）。

## 权限目录补齐（fix_permissions_catalog，收尾）
- 用户确认要做：把 `permissions` 目录表缺失的默认权限行补齐（解决上条"列表 UI 不全"的延伸坑）。
- `init_default_permissions()` 已确认幂等（先 `filter_by(code).first()` 再 INSERT），且只写 `permissions` 目录表、不动角色/映射，安全。
- 新建 `scripts/fix_permissions_catalog.py`（跑前自动备份 `score_management.db.bak_perms_catalog_<ts>`，用 `create_app(lightweight=True)` 上下文调用 `init_default_permissions()`，打印 before/after/inserted）。
- 实测：目录 61 → 63 行，本次 INSERT `admin_manage`/`notification.force_send` 两条（`timetable.rule.manage`/`phonebox.unlock.manage` 此前已被迁移脚本补建、非本次插入）。核验：`timetable.rule.manage`/`phonebox.unlock.manage`/`admin_manage`/`notification.force_send` 均在表，共 63 行；`timetable_manager` 角色存在。
- 效果：`/api/rbac/permissions` 列表 UI 现可完整展示全部默认权限（含"管理时间规则"）。鉴权逻辑不受影响。
- 待办：timetable_manager 角色**仍待分配用户**（上条收尾项）——需用户指定具体管理员账号后绑定 `admin_roles`。

## 创建排课教务账号并绑定角色（收尾闭环）
- 用户确认"创建一个账号绑定"。新建 `scripts/create_timetable_admin.py`（幂等、可重跑、跑前自动备份 `.bak_create_timetable_admin_<ts>`）。
- 默认账号：`username=paikao`，`password=Paikao@123`，`real_name=排课教务`，`role=timetable_manager`，并写 `admin_roles`(admin_id=4, role_code=timetable_manager)。密码走 Admin.password setter 的 bcrypt 哈希；未绑 `primary_class_id`（排课教务不限班级）。
- 幂等：用户名已存在则跳过创建（不改密码），角色已绑则跳过绑定。脚本末尾用 `has_permission` 校验：`timetable.rule.manage/period.manage/schedule.manage/student.view`=True，`notification.force_send/all`=False（无越权）。首次跑 [create]+[bind] 成功，二次跑 [skip]+[skip] 仍 OK。
- 现网状态：`paikao`(id=4) 已可作为「排课教务」登录并管理 TimeRule/节次/课表/班级/科目/学生查看，无超管与强制发送权限。
- 记忆同步：MEMORY.md「RBAC 新增权限的正确落库姿势」的"分配用户"收尾项标记为已完成。

## 班主任工作台 姓名显示终验 + 残余裸 ID 修复（本轮收尾）
- 背景：前序已修复 ①teacher 缺 class.edit（座次/值日/班委/家长 4 模块写操作 403）②列表接口补 student_name/class_name/subject_name/leader_name（entity_names.py + 11 个 service 序列化）③culture.view/edit 缺。本轮做浏览器级终验。
- `npm run build` 通过（exit 0，1m12s，无 TS 错误）——确认 AttendanceManage.tsx:525、HomeworkCheck.tsx:307 的 `班级 #`→`class_name` 改动编译通过。
- 浏览器终验脚本 `frontend/verify_names.js`（admin 登录，6 个代表性页）：首轮发现 `study-groups` 仍显示「班级 #」(真 bug)，其余 5 页裸 ID=无。
- 根因：`StudyGroups.tsx:364` 卡片视图硬编码 `班级 #{group.class_id}`，后端已返 `class_name` 但前端未用。已改为 `{group.class_name || \`班级 #${group.class_id}\`}`（与 AttendanceManage/HomeworkCheck 同款 fallback）。
- 全仓 grep `班级 #|学生 #|科目 #` 确认：其余所有出现处均已带 `|| name` fallback（AttendanceManage 418/523/526、HomeworkCheck 307、MentalHealth 343/418/456、StudyGroups 364/492、StudyGuide 537）。
- 复验 `verify_names.js`：6/6 页 裸ID=[无]、0 异常；study-groups 现显示「一年级1班」、attendance 显示「测试学生_CrudTest/一年级1班」、committee 显示「测试学生_CrudTest」。班主任工作台 12 页功能（渲染+写权限+姓名）全部闭环。
- 注意：duty-roster/seating-chart 等页「命中姓名=无」属正常（CRUD 探针创建又清理了测试数据，页内无裸 ID 即健康）。
- MEMORY.md 本轮已 consolidated（精简去重 + 新增「班主任工作台 12 页（缺陷已修复）」段，记录 class.edit 写权限依赖、列表必须返 name、culture 权限坑）。

## 班主任工作台「是否真实互通」实测（用户追问）
- 结论：**是真互通，非 mock**。证据：① 真实数据存在——96 学生分布在 一年级1班~三年级2班 等真实班级；teacher 账号 primary_class_id=1（一年级1班，DB 确认）。② 工作台各模块 GET 接口直接读真实表并返回真实姓名：考勤返回 `class_name='一年级1班'`、作业返回 `subject_name='语文'`、学生名解析自 user 表。③ 写互通坐实：以 teacher 登录 `POST /api/seating/charts {class_id:1}` → 201 落到真实 seating_chart 表，`DELETE` 该表 → 200（建表+删表均真实持久化）；前端页渲染真实姓名（verify_names 已证）。
- 修复一个排查坑：座次建表字段是 `class_id`（非 `class_info_id`），`/charts/<id>/seats` 仅 PUT 批量、无逐座 POST（405 是探针用错方法，非 bug）。前端表单用 class_id，正常。
- **发现真实库有测试残留（来自历次调试探针，非用户数据）**：测试学生 3 个（id 61/62/64：测试学生_CrudTest、E2E_TestUser_*）；工作台残留 24 行——class_committee 2、homework_assignment 1(测试作业)、attendance 3(关联测试学生)、study_group 4、activity 2、culture_record 6、study_guide 6。这些会让工作台看起来"有假数据"。
- 重启后端时发现**有 4 个 run.py 进程同时抢 5000 端口**导致 login 偶发 500/timeout；已强杀全部 python 重启单实例（task 2PkIQU），login 恢复正常。⚠️ 启动后端务必只跑一个实例。
- 待用户确认是否清理测试残留。回归脚本：`scripts/audit_testdata.py`（扫描，不删）、`scripts/probe_realdata.py`。

## 测试残留已清理（用户确认"清理"）
- 运行 `scripts/clean_testdata.py`（运行前自动备份 `score_management.db.bak_clean_testdata_<ts>`）：关外键约束 → 删父表子行(study_group_member/score, activity_registration, culture_item, homework_submission) → 按测试标记删工作台父表(study_group 4/activity 2/culture 3/study_guide 3/homework 1) → 删关联测试学生行(class_committee/attendance) → 安全扫所有 student_id 列 → 删 3 个测试学生(61/62/64)。
- 复验 `audit_testdata.py`：测试学生=0、测试残留合计=0；真实数据完好（attendance 9、homework 4、study_group 7、culture 4、seating 3 等均真实）。班主任工作台现已只显示真实系统数据。
- 结论闭环：班主任工作台与系统数据**真实互通**（读：接口返真实姓名；写：建表/删表真实落库），且库内已无调试假数据。

## 考试删除 404「考试不存在」bug 修复（用户控制台报错）
- 现象：点击删除考试 → `DELETE /api/exams/57` 404 `考试不存在`，列表里却显示该考试（控制台还报 click handler 1198ms 性能告警，仅提示非错误）。
- 根因三层：① **前端持久化缓存短路**——`api.ts` 的 `request()` 命中 IndexedDB 持久化缓存（cacheDB）时直接 `return`，不发网络请求（api.ts:758-762），已被删的考试从缓存里一直回显；② **`exams.getAll` 把 skipCache 当 URL 查询参数**（api.ts:3122-3128），`fetchData` 虽传了 `{skipCache:true}` 却未传给 request 的 options，强制刷新形同虚设；③ 后端 `ExamResource.delete` 考试已不存在时返回 404（exam_routes.py:118-121），导致点击残留项报错且失败路径不触发缓存清除，残留项赖着不走。
- 修复（三处）：① 后端 `exam_routes.py::ExamResource.delete` 改为**幂等**——`if not exam: return APIResponse.success(message="考试不存在或已删除")`（已删=成功，走成功路径触发 clearRelatedCache 清缓存）；② 前端 `exams.getAll` 正确把 `skipCache` 传入 `request` options；③ `ExamManagement.tsx` 的 `handleDeleteExam`/`handleCloseExam`/`handlePublishExam` 在 `finally` 中 `fetchData()` 强制重拉，确保陈旧项立即消失。另：`subjects.getAll` 早已正确传 options（对比参考）。
- 验证：后端幂等删除实测 `DELETE /api/exams/57`→200、新建→删→再删均 200；`npm run build` 通过；浏览器 Playwright 端到端（注入 IndexedDB 僵尸考试 99999）→ 重载后僵尸不显示✅、真实删除 55→54 且无 404 报错✅。回归脚本 `frontend/verify_exam_delete.js`。

## ⚠️ 数据恢复事故（必须记取的教训）
- 为验证删除 bug，浏览器里真实删了列表第一个考试；想恢复时误把 `score_management.db` 用旧备份（21:05 的 `bak_clean_testdata_*`）覆盖并 `rm -f *-wal`，**销毁了当时 live DB 的 WAL，导致该时刻前的考试状态无法从文件恢复**。
- 关键教训：**还原 DB 前必须备份「当前」库**（不是旧备份），且 SQLite WAL 模式下覆盖主库+删 WAL 会丢失未 checkpoint 的最近写入。正确做法：先 `cp 当前库 当前库.bak_<ts>` 再操作；恢复用最近一次完整备份，不要拿旧的 clean 备份当还原点（旧备份里混有测试考试/测试学生，且缺最近变更）。
- 本例事后厘清：用户考试表**全是测试数据**（21:05 备份 56 个考试均为 `测试考试_API_*`/`冒烟考试` 等，本回合开始时的 55 个考试亦然），并非真实业务考试。已还原 21:05 备份 + 重跑 `clean_testdata.py` 去测试学生/工作台残留 + 删 47 个机器生成测试考试，保留 9 个测试考试作夹具。**未丢失真实业务数据**。

## 考试发布/结束 404「资源不存在」bug 修复（用户控制台报错）
- 现象：点击「发布考试」→ `POST /api/exams/4/publish` 404 `资源不存在`（考试 id=4 正是报错对象）。前端 `api.ts::exams.publish/close` 调用 `/api/exams/{id}/publish`、`/api/exams/{id}/close`。
- 根因：**活动后端 `api/academics/exam_routes.py`（经 `api_versioning.py`/`route_init.py` 注册）只有 `ExamResource` 的 get/put/delete，没有 `/publish` 与 `/close` 路由**；这两端点在**未注册的 legacy `routes/exam_routes.py`（180/199 行）**里，线上根本没挂上 → Flask 返回 404 路由未找到（非"考试不存在"业务 404）。这是发布/结束功能长期 404 的真正原因（与上一轮删除 404 是不同根因：删除是幂等+缓存，发布是路由缺失）。
- 修复：在活动 `api/academics/exam_routes.py` 新增 `ExamPublish`(`/<int:exam_id>/publish`, POST, draft→published) 与 `ExamClose`(`/<int:exam_id>/close`, POST, published→closed) 两个 Resource，沿用 `APIResponse`+`requires_permission("score.manage")`+`get_by_id`；并做**幂等**：已发布再 publish→成功、已关闭再 close→成功（避免重复点击/缓存误报）。状态不合法（如 close 一个 draft）返回 `APIResponse.error`（400）。
- 验证：强杀 python 重启单实例（Flask-SocketIO 不 reload）→ 活后端冒烟（admin/123456）：`POST /api/exams/4/publish`→200(status=published)、`POST /api/exams/4/close`→200(status=closed)、再 close→200(幂等)、`POST /api/exams/999999/publish`→404 正确 not_found。**6 个考试全为 draft，发布/结束全线可用**。验证完把考试 4 还原为 draft（不影响用户后续操作）。
- 注意：前端 `PermissionButton permission='exam.manage'`，后端用 `score.manage`；admin 是 super_admin 两张都过，无碍。非超管班主任若只有 `exam.manage` 无 `score.manage` 会 403（预存前后端权限码不一致，非本 bug 范围，必要时再对齐）。

## 成绩录入「录入不上」bug 修复（用户报告"我现在无法录入成绩"）
- 现象：进入成绩录入页，**表头列名显示为 `["\u8bed\u6587"` 和 `" \u6570\u5b66"]`**（带中括号/引号），`录入进度 0%`，所有分数列均为 `-`，但实际后端 192 条 `scores` 数据真实存在，**只是前端看不到对得上**。
- 根因三层：① **`Exam.subjects` 在 DB 存的是 str（`"[\"\\u8bed\\u6587\", \"\\u6570\\u5b66\"]"`）而非 list**——`db.Column(db.JSON)` 但实际写入时塞的是 JSON 序列化字符串；6/6 考试全坏（id 2/3/4/5/6/7）；② **前端 `ScoreEntry.tsx:309` 只做了 `Array.isArray` 判 false 后用 `,` split 字符串**——把 `'["语文","数学"]'` 切成 `['["\\u8bed\\u6587"', '" \\u6570\\u5b66"]']`；③ **录入写入时用了错 subject 字符串**（如 `'["\\u8bed\\u6587"'`），进 DB 后查回时 key 仍用错字符串，反查再失败 → 进度永远 0%。**后端 API 本身完全正常**（POST /api/scores→200、PUT /api/scores/<id>→200，admin 实测均 200）。
- 修复（三处）：① **数据修复**——用 sqlite3 直连 update：6 个考试 subjects 全部从 str JSON 字符串修复为真正的 list（`['语文', '数学']` 等），`scripts/fix_exam_subjects.py`（一次性，已删）+ 自动备份 `.bak_exam_subjects_<ts>` + `PRAGMA wal_checkpoint(TRUNCATE)`；② **前端解析加固**——`ScoreEntry.tsx::examSubjects` 加多档 fallback：list→直用 / str JSON→`JSON.parse`（含嵌套字符串再 parse 一次）/ 其它→`split(',')` 后用正则去 `["']\[\]]` 前后缀；③ **写入校验**——`handleSaveAll` 加正则守卫：`if (/[\[\]"'\\,]/.test(subject) || /\\u[0-9a-f]{4}/i.test(subject))` 直接 fail-count + 跳过 + console 告警，避免历史脏数据再灌库。
- 顺带清理：1 条错写入的 `scores.id=10112 student_id=66 subject='["\\u8bed\\u6587"' score=88`（来自首次复现的 Playwright 自动化测试脏写），用 `LIKE '%[%' OR LIKE '%"%'` 批量删。
- 验证（修复后 Playwright）：表头列 → `['学号', '姓名', '语文', '数学']` ✅；POST /api/scores body → `subject: "语文"` ✅；首行回显 → `20260729001 DebugTestUser_xyz 91.5 normal 97.7 normal` ✅；进度条 99%（不是 0）✅；截图 `frontend/score_entry_fixed.png`。
- **DB 漂移教训（重要）**：用户报告"录入不上"症状，**不能信前端展示**——必须用 Playwright 端到端 / curl 实测 API 双确认。本例若只看 UI 表头就被误导（以为是缓存或路由 bug），实际上数据层就坏了。修复时直接 sqlite3 修最稳（绕开 ORM session 状态），用 `PRAGMA wal_checkpoint(TRUNCATE)` 防 WAL 漂移。

## 算法分析页两个 bug 修复（用户控制台报错 + 截图「分数还是无法手动调整」）
- 现象（控制台）：① `GET /api/algorithm/prediction/risk?days=7` 后 `AlgorithmAnalysis.tsx:122` `Cannot read properties of undefined (reading 'slice')`（切到「积分预测」tab 时）；② `GET /api/algorithm/rule-recommend?days=30` → 500 `Object of type ScoreCategory is not JSON serializable`（切到「规则推荐」tab 时）；③ 截图「分数还是无法手动调整」实为算法页里 `getBatchScorePredict` 等端点的字段不一致导致页面不能渲染分数调整卡片（不是 ScoreEntry 的问题）。
- 根因与修复（两处）：
  - **后端 `backend/services/rule_recommendation_service.py`**：`analyze_behavior_patterns` / `suggest_rule_optimizations` / `find_new_rule_opportunities` 把 SQLAlchemy 关系对象 `record.category`（ScoreCategory ORM 实例或 None）直接当 dict key 并塞进返回 dict，jsonify 序列化 500。统一规整成字符串名（`isinstance ScoreCategory → cat.name`，fallback `record.description` 或 `"未分类"`），覆盖 line 120-133 / 170-176 / 282-296。`train_recommendation_model` / `evaluate_model` 仍有同根隐患但 API 未报先不动。
  - **前端**：`api.ts::request()` 已自动剥 envelope（`data: rawData.data`），所以 `getRiskStudents` 实际返回 `RiskStudent[]`，但 `AlgorithmAnalysis.tsx:122` 用了 `riskRes.data.slice`——undefined.slice。修复：`loadPrediction` 改成 `Array.isArray(riskRes) ? riskRes.slice(0,10) : []`；`api.ts::algorithm` 全部方法签名从 `Promise<{data:X}>` 改为 `Promise<X>`（与 request 行为对齐）；`getBatchRiskPredict` 走 `normalizeBatchRiskPredict(res)` 归一化后端 `RiskPredictService.predict_batch` 与前端 `BatchRiskPredictData` 字段名不一致（results/overall_risk_level/overall_risk_score/risk_factors/summary.high_risk），辅助函数与 `RawBatchRiskPredict` 类型已在 `api.ts:2143-2180` 定义。
- 验证：`scripts/verify_algo_fix.py` 三端点（rule-recommend / prediction/risk / prediction/batch）全 200 + 递归检查 ORM 混入 PASS；前端 `vite build` 通过；Playwright 进 `/#/algorithm-analysis` 统计 tab 加载 0 console error / 0 page error。强杀 python 重启单实例（Flask-SocketIO 不 reload）。

## 成绩录入「分数无法手动调整」修复（用户报告，仍是 ScoreEntry）
- 现象（上一轮修了 subjects 解析后）：表头「语文/数学」正常、进度 99%，但分数列显示为 **`91.5 normal`** 纯文本 + 状态徽章——**完全没有 `<input>`**，用户没法改；改成双击测试偶发能出 input 但**改完不点「保存全部」就不入库**，刷新还原 → 「调整无效」。
- 真因：① `ScoreEntry.tsx` 分数 td 用 `isEditing ? input : span+badge`，**双击**才进编辑（line 932-933 + 940 onDoubleClick），用户不知道要双击；② `handleScoreChange` 只 `dispatch pendingChanges`（前端暂存），需点「保存全部」才调 `api.scores.create/update` 入库。
- 修复：`ScoreEntry.tsx` 三处：① 删除 line 932-933 `isEditing` 局部变量 + line 940 `onDoubleClick`；② 分数 td 始终渲染紧凑 `<input type=number min=0 max=100 step=0.5>` + status badge（flex 布局 w-16，hover/focus 蓝环），加 `key={student.id-subject-scoreData?.id ?? 'empty'}` 让分数来源变化时 input 重挂；③ 新增 `handleScoreBlur(studentId, subject, value)`：onBlur 即时调 `api.scores.update` / `.create` 写库 → 成功后 `dispatch UPDATE_SCORE` 本地同步 + `REMOVE_PENDING_CHANGE`。脏 subject 防御校验保留（与 handleSaveAll 一致）。Enter 触发 blur 保存，Escape 还原 + blur。`保存全部`按钮保留用于批量修改。
- 验证（活后端 + Playwright + DB）：选「测试考试_api」(id=4 published) → `INPUT_COUNT=188`（之前 0）；改 DebugTestUser_xyz (card=20260729001, user_id=66) 语文 91.5→66.6 + Tab blur → 0 console error / 0 page error；DB 查 `scores.id=5815 score=66.6 updated_at=2026-08-05 22:48:13.788446`（即时写入，未走「保存全部」）；数学 97.7 未变 ✅。截图 `frontend/score_manual_after.png`：input 渲染 + 66.6 + normal badge + Tab 焦点切到下一 cell。
- **设计教训（重要）**：录入类 UI 的「手动调整」体感，**默认 input 始终渲染 + onBlur 即时入库**最符合直觉；之前「双击编辑 + 批量保存全部」的设计门槛高，体感像「改不动」。后续涉及录入/编辑交互，新代码优先默认可见 input + 即时保存，「批量保存」按钮留给真批量场景。

## 小工具 / 约定（顺手记）
- **本地 tsc 与项目 tsconfig 不兼容**：项目 `tsconfig.json` 用了 Vite 专属 `bundler` moduleResolution / `allowImportingTsExtensions` / `resolveJsonModule` 等选项，本地 `tsc --noEmit` 会报 4 条 TS6046/TS5023/TS5070 配置错误（不是代码错）。验证前端用 **`node node_modules/vite/bin/vite.js build --logLevel warn`**（走 esbuild），不要单跑 tsc。
- **Bash 启动 python（git bash）**：必须用 `/c/Users/...` 路径，`C:\Users\...` 会被当相对路径（命令变成 `C:Users...`，command not found）。可用 `run_in_background=true` 后台启动。
- **回归脚本保留**：`backend/scripts/verify_algo_fix.py`（algo API ORM/序列化回归，验 rule-recommend/prediction 三端点 + 递归 ORM 混入检查）。其他一次性 Playwright 脚本 `verify_algo_frontend.js`/`verify_score_manual.js` 用完删。
- 用户使用偏好（已两次反馈 ScoreEntry 问题）：**录入页要能直接看到 input + 改即生效**，不要隐藏编辑入口、不要靠「保存全部」按钮兜底。

## AlgorithmAnalysis 预测 Tab `.toFixed` 崩溃（2026-08-05 晚）
- 现象：用户控制台连续报 `Cannot read properties of undefined (reading 'toFixed')`，错误堆栈指向 `AlgorithmAnalysis.tsx:506` 渲染预测详情表格的 `.map`。
- 根因：后端 `PredictionService.predict_batch`（`backend/services/prediction_service.py:152`）实际返回 `{predictions: [{user_id, name, class_name, prediction: {current_score, predicted_scores, trend, confidence, slope}}], summary: {rising_count, stable_count, falling_count}}`。前端 `renderPrediction` 直接按扁平结构读 `item.current_score.toFixed(1)` / `item.predicted_score - item.current_score`，永远是 undefined。summary 字段名也对不上（前端 `improvement_count/decline_count/stable_count`、后端 `rising_count/falling_count/stable_count`）。
- 修复（不动后端、不重启服务）：
  1. `frontend/src/services/api.ts` 新增 `interface RawBatchPrediction` + `normalizeBatchPrediction`：把 `prediction.{current_score, predicted_scores, trend, confidence}` 提到顶层，trend `rising→up/falling→down`，summary 名映射 `rising→improvement_count, falling→decline_count`，avg_*_score 按归一化后 predictions 重算，全部数值字段 `toNum` 兜底。
  2. `getBatchPrediction` 改为 `async`，调用归一化函数后再返回。
  3. `AlgorithmAnalysis.tsx` 渲染预测详情表格全部 `?? 0` + `||` 兜底（current_score/predicted_score/confidence/trend），`tbody tr` key 改为 `${user_id ?? name}-${name}` 防 React key 冲突。
  4. `renderPrediction` summary 用 `summary?.improvement_count ?? 0` 等局部变量承接，避免重复内联判空；风险学生数 `(riskStudents||[]).length`。
- 通用教训：**后端嵌套 vs 前端扁平读取时**，前端 `api.ts` 必须做归一化层，而不是每次渲染都靠 `?.` 兜底——后者字段路径一错就崩，且难以排查。修复点在 `api.ts` 拦截一次比在每个页面补 `?.` 干净得多。
- 与之前 `normalizeBatchRiskPredict` 同套路，可统一抽象为 `normalizeData(raw, mapping)`。
- 验证：playwright 验证 `rows=10`、首行 `周秀 -108.0 下降 -60.3分 65%`、summary 58/0/35/10 与后端 raw 数据一致，CONSOLE_ERRORS=0 PAGE_ERRORS=0 errorBoundaryShown=false。
- 验证脚本：`frontend/probe/verify_prediction_tab.js`（chromium `C:/Program Files/Google/Chrome/Application/chrome.exe`；截图 `prediction_tab_after.png`）。
