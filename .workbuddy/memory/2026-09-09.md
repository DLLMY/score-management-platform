# 2026-09-09 工作日志

## D2 巨型页拆分 · 第十三拆 ExamManagement（非班主任，原 D2 扫描遗漏）
- 发现 `src/pages/ExamManagement.tsx` 为 **1032 行**、admin 级考试管理页（非 teacherWorkbench 路由组），原 2026-09-08 扫描遗漏（当时未列入 D2 >1000 清单）。属 D2 合法候选。
- 按「逻辑层 + 单一 View 大 Props」范式拆分：抽 `examManagement/` 目录 **2 文件**（types.ts 112 行 / ExamManagementView.tsx 392 行）。主文件 `ExamManagement.tsx` 收敛为逻辑层 **691 行**（原 1032）。
- 要点：`getStatusBadge`/`getImportanceBadge` 仅被 `columns` 闭包引用 → 保留逻辑层；`handleExport`/`handleImportFile`（`/api/exam-import/execute` 带 `exam_id`）经 `getAuthHeaders` 在逻辑层构造后下发；`ExamManagementViewProps` 42 字段；`DEFAULT_EXAM_FORM`/`filteredExams` 零变更。逻辑逐字节不变。
- 三闸门全绿：**tsc EXIT=0 / eslint 两文件 EXIT=0 / vitest run 277 passed·3 skipped EXIT=0**（后台 Ayk5pH）。
- 债清单 `docs/代码债重扫清单-20260908.md` D2 段落 + 汇总表更新：非班主任 >1000 大页现为 **十二页全拆** + NLPManagement 核验。

## 当前 D2 收口状态（2026-09-09）
- ✅ 非班主任 >1000 行大页：Analysis/RuleList/ScoreAnalysis/SubjectManagement/AlgorithmAnalysis/Dashboard/DeviceManagement/PermissionManagement/ScoreEntry/UserList/CourseSchedule/ExamManagement（共 12）+ NLPManagement（核验 T12 已拆）。
- 🔒 班主任页（铁律③·三页全批按序执行）：**ClassManagement ✅ / AttendanceManage ✅ / StudyGuide ✅（三页全部已拆，各三闸门全绿）→ D2 班主任页收口完毕**。
- ⚠️ >1000 阈值外的「大页」（未被原 D2 定义为债，待用户决策是否扩展范围）：RemoteNotify(937)/DeviceGroup(909)/StudentPortal(894)/Approvals(823)/ClassPeriodSettings(710)/Diagnostics(709) 等（其中 MentalHealth/ParentContact/CommitteeList/SeatingChart/DutyRoster 为班主任页，铁律③锁定）。
- 铁律①：本轮所有改动仍**未 commit/push**。

## D2 第十四拆 ClassManagement（班主任页·铁律③ #1，三闸门全绿）
- 用户 2026-09-08 经 AskUserQuestion 选定「三页全批，按序执行」（ClassManagement → AttendanceManage → StudyGuide），并附 `docs/班主任页拆分方案-铁律③待审.md` 零变更声明方案预审通过 → 执行。
- 范式：逻辑层 `ClassManagement.tsx` + `classManagement/` 7 子模块（types.ts / StatisticsCards.tsx / ClassFormModal.tsx / HeadTeacherModal.tsx / TeacherPreviewDialog.tsx / ImportModal.tsx / ClassManagementView.tsx）。主文件保留全部 state/effect/handler/useMemo：`useListFetch`/`useForm`×1/`useModal`×3/`useConfirm`/`useStableToast`；**3 处渲染期 ref 赋值保留**（`confirmRef.current=confirmFn`、`performRemoveHeadTeacherRef.current=performRemoveHeadTeacher`）、**2 处行级 exhaustive-deps 抑制随 useCallback 保留**（showTeacherPreviewDialog/showRemoveConfirmDialog）；`lastOperation` 撤销语义、assign 第三参 `true`、skipCache 双刷新、`api.classes.import`(xlsx)/原生 `fetch`(json) 分流、文件白名单 `.json/.xlsx/.xls` 逐字节不变；DataTable rowActions 保留 S1 注释 `/* class.delete 后端无此码，统一 class.manage */`。
- 视图层：ClassManagementView default export 主壳（Header+统计卡+搜索/工具条+DataTable+4 模态挂载）；ImportModal（241 行最大块）直接 import `api`/`downloadBlob`/`logger`/`PermissionButton`，模板下载内联 fetch + downloadBlob。`columns` useMemo deps=[]（仅 GraduationCap）保留逻辑层。
- 铁律③零变更声明逐条满足：权限码 / 字段命名 / 业务字典(`is_active`/`grade`) / 业务逻辑(撤销·导入分流·班主任分配) 均未改。
- 三闸门全绿：**tsc EXIT=0 / eslint 8 文件 EXIT=0 / vitest run 277 passed·3 skipped EXIT=0**（后台 r7nGeG）。债清单 D2 段落 + 汇总表已更新（班主任页进度 1/3）。
- 下一步：Task #52 StudyGuide（1110→逻辑层，studyGuide/ 子模块 types+StudyGuideView+Toolbar/GuidesTab/PlanCard/PlansTab/GuideModal/PlanModal；getProgressColor/getProgressBg/filteredGuides/filteredPlans 保留逻辑层）；运行三闸门后收口班主任页进度 3/3。

## D2 第十五拆 AttendanceManage（班主任页·铁律③ #2，三闸门全绿）
- 范式：逻辑层 `AttendanceManage.tsx` + `attendanceManage/` 8 子模块（types.ts / DraftBanner.tsx / StatisticsCards.tsx / PendingLeavesPanel.tsx / AttendanceTableCard.tsx / RecordModal.tsx / LeaveModal.tsx / AttendanceManageView.tsx）。主文件保留全部 state/effect/handler/useMemo（`useListData`(attendances)/`useAutoSave`(M3 草稿)/`useWorkbenchClass`(filterClassId)/`useClientFilter`/`useSubmitGuard`/`useStableToast`）；`getStatusBadge`+`columns` useMemo（deps=[getStatusBadge]）保留逻辑层构造后下发 `AttendanceTableCard`；`pendingLeaves`/`filteredAttendances` 派生值逻辑层计算下发；`runSubmit` 守卫原样下发给 RecordModal/LeaveModal/PendingLeavesPanel（保留 `runSubmit(() => handleX())` 调用语义）。视图层 `AttendanceManageView.tsx` default export 主壳（草稿恢复条/header+班级筛选+待审批角标+快速记录·请假申请按钮/统计卡/待审批面板/列表卡/双模态挂载）。
- 铁律③零变更声明逐条满足：权限码（attendance.record/batchRecord/applyLeave/approveLeave/getStats/getLeaves 均未改）/字段命名/业务字典（`status: present/absent/late/leave`、`leave_type` 字典）/业务逻辑（草稿恢复·批量记录·审批·分页）未改。
- 三闸门全绿：**tsc EXIT=0 / eslint 8 文件 EXIT=0（仅 2 条原函数遗留 unused-var 警告 leavesPages/attendancePages，非本拆引入）/ vitest run 277 passed·3 skipped EXIT=0**（后台 xi5hfw）。债清单 D2 段落 + 汇总表已更新（班主任页进度 2/3）。

## D2 第十六拆 StudyGuide（班主任页·铁律③ #3，三闸门全绿 → D2 班主任页收口完毕）
- 范式：逻辑层 `StudyGuide.tsx` + `studyGuide/` 8 子模块（types.ts / Toolbar.tsx / GuidesTab.tsx / PlanCard.tsx / PlansTab.tsx / GuideModal.tsx / PlanModal.tsx / StudyGuideView.tsx）。主文件保留全部 state/effect/handler/useMemo/useRef（`useListData`×2(guides/plans)/`useWorkbenchClass`(filterClassId)/`useSubmitGuard`/`useConfirm`+`confirmRef.current=confirmFn` 渲染期 ref 赋值保留/`useStableToast`）；`getProgressColor`/`getProgressBg`/`filteredGuides`/`filteredPlans` 派生值保留逻辑层计算后下发；`runSubmit` 守卫下发给 GuideModal/PlanModal（PlanModal 表单 `onSubmit` 保留 `void runSubmit(handlePlanSubmit)`，L8 注释保留）；双 Tab 切法、空列表 EmptyState 动作、404 自动刷新列表(v2)、编辑时班级不可改、发布 ToggleSwitch 全部逐字节不变。
- 铁律③零变更声明逐条满足：权限码（studyGuide.* 均既有权限域，未改）/字段命名/业务字典（guide_type/plan_type/target_audience/is_published）/业务逻辑（草稿·双 Tab·进度按钮·删除确认·分页）未改。
- 三闸门全绿：**tsc EXIT=0 / eslint 9 文件 EXIT=0（仅 2 条原函数遗留 unused-var 警告 guidePages/planPages，非本拆引入）/ vitest run 277 passed·3 skipped EXIT=0**（后台 REaO0z）。债清单 D2 段落 + 汇总表已更新（班主任页进度 3/3 → D2 班主任页收口完毕）。
- 铁律①：本轮所有改动（ExamManagement + ClassManagement + AttendanceManage + StudyGuide 四拆）仍**未 commit/push**。

## D2 战役总结（2026-09-09 收口）
- 非班主任 >1000 大页 12 拆 + NLPManagement 核验 + 班主任页 3 拆（铁律③）= 全部 Done，三闸门全绿基线 277 passed·3 skipped。
- 仅剩待用户决策：>1000 阈值外的「大页」（RemoteNotify/DeviceGroup/StudentPortal/Approvals/ClassPeriodSettings/Diagnostics 等）是否扩展 D2 范围（原 D2 未定义这些为债）。

## 下一步优化 · D7 死分页状态清理 + 1 处 exhaustive-deps（2026-09-09 续）
- 背景：D1–D6 + 后端兜底收敛战役均已收口（债清单 doc 标注「终态洁净」），92 文件改动全部未提交。先跑全项目三闸门作证：tsc EXIT=0 / eslint(89 变更文件) 9 warning(0 error) / vitest 277 passed·3 skipped EXIT=0。
- 发现：`useListFetch`/`useListData`（M9 服务端分页）迁移后，7 页遗留 9 处死 `useState` 分页计数 `const [xxxPages, setXxxPages] = useState(1)`（AttendanceManage×2/CultureBoard/DutyRoster/MentalHealth×2/StudyGuide×2/TeacherComments），读值无消费者，仅 `setXxxPages(resp.pages)` 单向写入 → eslint `no-unused-vars` 9 告警。另全 src eslint 仅 1 处 `exhaustive-deps`（`AlgorithmAnalysisShell.tsx:521` engagement 列 `useMemo` 缺 `setEngagementTrendUserId`，稳定 useState setter，`types.ts` 类型 `Dispatch<SetStateAction<...>>`）。
- 处置：① 删 9 处 `xxxPages` 的 `useState` 声明 + 唯一写入 `setXxxPages(resp.pages)`（顶层无条件 useState，删一处不影响 hook 顺序；无字段/权限/业务逻辑变更，铁律③不适用）；② `AlgorithmAnalysisShell.tsx` 该 `useMemo` 依赖数组 `[]` → `[setEngagementTrendUserId]`（setter 稳定，行为不变）。
- ⚠️ Edit 工具对部分 `setXxxPages`/`guidePages` 行静默未生效（报 success 但文件不变），导致删声明留写入 → tsc 报 `Cannot find name 'setXxxPages'`。改用 Python 字节级读改写（继承原 EOL）兜底删除，验证落盘正确。教训：批量删 setter 调用行时 Edit 不可靠，优先用字节级脚本。
- 三闸门全绿（复跑）：**tsc EXIT=0 / eslint 全 src EXIT=0（0 problems）/ vitest 277 passed·3 skipped EXIT=0**。债清单 doc 追加 D7 段 + 汇总表 D7 行。
- 铁律①：本轮 + 全部历史改动仍**未 commit/push**。

## D2 范围正式扩展至 >1000 阈值外 6 大页（2026-09-09 收口）
- 用户指令「把 D2 范围真正扩展到 >1000 阈值外 6 大页」：将此前作为「扩展备注」的 6 页**正式纳入 D2 主清单**（非班主任页，铁律③不适用）。6 页：RemoteNotify(937)/DeviceGroup(909)/StudentPortal(894)/Approvals(823)/ClassPeriodSettings(710)/Diagnostics(709)。
- 取证：重跑三闸门全绿 —— **tsc --noEmit EXIT=0 / eslint（6 页定向）EXIT=0 / vitest run 277 passed·3 skipped EXIT=0**。6 页视图目录齐整（remote-notify 6 / deviceGroup 7 / 其余 4 页各 types+View）。
- 文档：债清单 `docs/代码债重扫清单-20260908.md` 三处提升 —— ① D2「位置」行增补「范围扩展」候选（6 页行数）；② 收口汇总 line 76 🏁 并入「D2 扩展 6 页已正式纳入主清单并收口」；③ 「D2 扩展」子标题标注「已正式纳入 D2 主清单」；④ 汇总表 D2 行标题改为「已正式扩展至 >1000 阈值外 6 大页」。
- 铁律①：全部改动**未 commit/push**，待用户明确指示。

## 当前平台债项终态（2026-09-09）
- D1–D7 全部收口：D2（含 >1000 阈值外 6 大页正式纳入）巨型页拆分全收口 + D1 exhaustive-deps 保留 + D3 false-positive 跳过 + D4 batch_writer print + D5 student_routes 安全子集 + D6 formatNumber 桥接 + D7 死分页状态清理。三闸门基线 277 passed·3 skipped 全绿。
- 仅剩：全部改动未提交（铁律①），待用户确认 commit/push。

## 新一轮全量债项扫描（2026-09-09 续）
- 用户指令「开启新一轮全量债项扫描」。方法：Python 静态扫前端 `apps/frontend/src`（329 ts/tsx）+ 后端 `apps/backend`（521 py，生产 247/非生产 274），维度覆盖 console/any/TODO/eslint-disable/超大文件 + 后端 print/broad-except/noqa（区分生产 vs 测试/脚本/工具）。
- 基线复验：`tsc --noEmit` EXIT=0（今日）；eslint 全 src 0 problems、vitest 277·3 沿用上轮 → 当前零回归。
- 发现（E 系列，详见 `docs/代码债重扫清单-20260909.md`）：
  - E1 前端 `any` 类型 40 处/16 文件（中低）。
  - E2 `eslint-disable` 35 处（32 exhaustive-deps + 3 no-console）→ 实质 0 真债。
  - E3 后端 `# noqa` 243 处，其中 **F841 死变量 198 处**（真债，可安全批量清）。
  - E4 后端 broad/bare `except` 生产 103 处/49 文件（中高，集中 services，需审计收窄）。
  - E5 后端 `print()` 生产仅 27 处全在 1 个 migration 脚本 → 非债。
  - **E6 前端巨型页第二梯队：15 页 >600 行且含大量 JSX（className 45–137）且无视图子目录 = 从未拆分的单体巨页**（MentalHealth/ParentContact/CommitteeList/SeatingChart/DutyRoster/RankRuleList/StudyGroups/TimeRuleList/ImportConfigManagement/ActivityManage/OpsCenter/Settings/CultureBoard/Profile/HomeworkCheck）。这是 D2 真正下一波。
  - E6a 已拆但逻辑层过肥：10 页有子目录（D2 已拆）但主文件仍 614–1358 行（ScoreEntry 1176/UserList 1009/NLPManagement 1358 等），JSX 已下沉、className 低。
- 关键校正：先前「D2 全收口」指其命名清单页；实际仍有 15 个未拆分 monolith 大页 + 10 个逻辑过肥页，属 D2 范围外的新债。
- 建议执行序：E3(F841 清) → E6(15 页拆) → E4(审计) → E1(随拆收) → E2/E5/E6a 观察。
- 铁律①：本轮扫描仅产出文档，无代码改动；全部历史改动仍**未 commit/push**。

## 债项收口推进（2026-09-09 续，E3→E6→E4→E1 顺序）

### E3 后端 F841 死变量清理 — ✅ 已完成（task #66）
- `apps/backend` 生产代码 14 处真实 F841 死变量赋值清理（`# noqa: F841` 同步移除/修正）。批量安全清理，零逻辑变更。

### E6 前端 15 页 monolith 拆分 — ✅ 已完成（task #67，15/15）
- 按行数降序复用 D2「逻辑层 + 视图层」范式逐页拆分，每页即时 tsc + eslint 全绿。
- 拆分清单（逻辑层 .tsx 仅留 state/effect/handler/派生值 + 渲染 `<XxxView>`；JSX/纯渲染辅助函数下沉 `pages/<dir>/<Camel>View.tsx` 并 default export）：
  1. MentalHealth → mentalHealth/MentalHealthView
  2. ParentContact → parentContact/ParentContactView
  3. CommitteeList → committeeList/CommitteeListView
  4. SeatingChart → seatingChart/SeatingChartView
  5. DutyRoster → dutyRoster/DutyRosterView
  6. RankRuleList → rankRuleList/RankRuleListView
  7. StudyGroups → studyGroups/StudyGroupsView
  8. TimeRuleList → timeRuleList/TimeRuleListView（视图层 getDayLabel/formatTime 包 useCallback 清零 exhaustive-deps）
  9. ImportConfigManagement → importConfigManagement/ImportConfigManagementView（FormInstance 由逻辑层下发）
  10. ActivityManage → activityManage/ActivityManageView
  11. OpsCenter → opsCenter/OpsCenterView（子组件/columns 下沉）
  12. Settings → settings/SettingsView
  13. CultureBoard → cultureBoard/CultureBoardView（逻辑层删未用 EmptyState/LoadingSpinner）
  14. Profile → profile/ProfileView（loading 早返回 + 双 Modal 下沉；修正安全页修改密码按钮 onOpenPassword）
  15. HomeworkCheck → homeworkCheck/HomeworkCheckView（columns 升模块级常量，useMemo 依赖清零）
- ⚠️ 铁律③锁定页 ClassManagement/AttendanceManage/StudyGuide 未触碰（E6 涉 15 页均非锁定页）。
- **三闸门全绿收口**：tsc --noEmit EXIT=0 / eslint（每页定向）EXIT=0 / **全量 vitest run 277 passed·3 skipped EXIT=0**（后台 238s）。

### E1 前端 any 渐进类型化 — ✅ 已确认无需再改（task #68）
- 本轮 grep 复验：`apps/frontend/src` 生产代码 **零 `any` 类型用法**（仅 env.ts/api.ts/codeSplitting.tsx 三处注释提及）。
- 原因：先前 T5 信封/算法接口 `RawAlg → unknown`、env 对象类型化、codeSplitting `unknown → React.FC<P>`、api.ts 大改写已先行收敛。doc 原列 40 处/16 文件已在历史工作中消除，当前工作树零显式 any。

### E4 后端 broad/bare except 审计收窄 — ✅ 收口（保守高密度 pass，task #65）
- 规模：doc 原定生产 103 处/49 文件，集中 redis_cache_service/nlp_enhanced/nlp_ml/approval_service/permission/cache 等。
- 策略（严格保守，绝不盲删/绝不改控制流/仅 3 类安全变换）：
  - **A 补 traceback**：原 `logger.warning(f"...: {e}")` 无堆栈 → 加 `exc_info=True` 或 `logger.exception`；redis `_safe` 装饰器（line 41）一处改动覆盖全部 `@_safe` 方法。
  - **B 沉默加日志**：原 `pass`/空 return/`return False` 无日志 → 加 `logger.exception`/`logger.warning(exc_info=True)`；高频探针（ping/ensure_connection/flush/get_pool_status/readiness）用 `logger.debug` 避免刷屏。
  - **C 收窄**：仅当操作单一明确异常且 body 有意义处理时 → `int()`→`(ValueError,TypeError)`、`json.loads`→`(json.JSONDecodeError,TypeError)`（mqtt_message_service / scheduled_notify_service 已落）。
- 执行分两轮：① 首 subagent 18 散点文件；② 我补 remote_notify(5)+system_routes(3) 沉默路径（首 agent 漏补日志只删 `as e`）；③ 二 subagent 攻坚 6 高密文件（redis 24/nlp_enhanced 17/nlp_ml 19/approval 5/permission 4/cache 1）。
- 验收：**23 个生产文件改动，全部 `python -m py_compile` EXIT=0**；git 范围干净（无 tests/scripts/migrations/tools）。
- ⚠️ 越界修正：首 subagent 误改 3 个测试文件（删未用变量，非 except 收窄，违"排除 tests"铁律）→ 已 `git checkout` 回退，tests/ 保持原状。
- approval_service 5 处均为 `except Exception: db.session.rollback(); raise`（re-raise，已可见）→ 正确跳过未改。
- 变换分布：A≈35 处补 traceback / B≈31 处沉默加日志 / C≈4 处收窄 / 0 处删除或控制流变更。
- 残余：生产 raw broad-except 仍 566 处（含大量 1 处/文件散点），但绝大多数为已含日志 fail-safe 或 re-raise 模式，按保守铁律不盲改；doc 原定 103 目标中的**高密度集群已全部收口**。
- 验证闸门：py_compile（python 3.13）+ 抽检 git diff + 三处关键点（permission.py 补 log_warning 导入、cache.py 补模块 logger、redis `_safe` exc_info）复核均正确。全量 pytest 需 DB 不可跑，以行为保持 + 编译 + 评审替代。

### E 系列债项终态（2026-09-09）
- E3 ✅（14 文件 F841）/ E6 ✅（15/15 拆，vitest 277·3）/ E1 ✅（零 any，历史已收）/ **E4 ✅（保守高密度 pass，23 文件）**。
- E2(eslint-disable 实质 0 债)/E5(print 仅 1 migration 非债)/E6a(10 逻辑过肥页) → 观察项，按 doc 不主动排期。
- 铁律①：本轮全部改动（E3+E6 15 逻辑+15 视图+E1 零改动+E4 23 文件）仍**未 commit/push**，待用户明确指示。

### 下一步
- 待用户确认 commit/push（铁律①，绝不自动提交）。

## E4 长尾清零（2026-09-09 深夜，用户指令「清零 566 长尾」）
- 用户「继续清零 566 处 broad-except 长尾」。剩余 = 全生产 raw broad-except 减去已收口 23 文件 = **91 文件**（grep 曾误把 `.venv/` 第三方包计入 185，已排除 → 真 114 生产文件减 23 = 91）。
- 高体量文件 12 个（records_routes 12/diagnostics 9/scheduled_tasks 9/notification_service 9/nlp_service 8/alert_service 8/users_routes 8/logger 7/mqtt_tasks 7/nlp_optimizer 7/import_export_service 7/api_versioning 7/backup_utils 6/bert_service 6/approvals_routes 6/nlp_routes 6/config_loader 5/scheduled_notify_routes 5 等），其余多数 1-3 散点。
- ⚠️ **首轮重扫曾因 grep 未排除 `.venv` 误报 185**：`grep -rln` 必须加 `-vE '/(tests?|migrations|scripts|tools|\.venv|node_modules|__pycache__)/'`。
- 已分类高体量文件：绝大多数 except **已有日志**（`logger.error(f"...: {e}")` / `log_warning(f"...: {e}", exception=e)`）→ 仅需 **A 补 traceback**；真沉默吞极少（diagnostics check_* 把 `str(e)` 传回响应体已可见 → skip）；approval_service 5 处为 re-raise → skip；已用 `logging.getLogger(__name__).warning("...", exc_info=True)` 模式（nlp_service 283/nlp_optimizer 62·80·113/bert_service 124/nlp_routes 1414）→ skip 已含堆栈。
- **EOL 铁律实测**（python 字节级权威）：LF 文件 = records_routes/users_routes（Edit 工具安全）；**CRLF 文件 = notification_service/nlp_routes/alert_service/backup_utils/security_audit/scheduled_notify_routes**（须字节级脚本，禁用 Edit，否则全文件翻 LF）。
- batch1（api 路由 10 文件，8 改）已由首轮 subagent 完成：exam_import/import_routes/download/export/import_export/devices/firmware/wol 8 文件，A=22/B=2/跳过2/0回退，编译全过。
- 尝试派 batch2（14 api 路由文件）触发 **429 频率限制**（00:36 UTC+8 重置，约 48 分钟窗口）。策略：速率恢复后按「显式文件清单 + CRLF 保留指令 + 逐文件 py_compile」分批后台 subagent 收口，勿单批并发过多。
- 铁律①：本轮全部改动仍**未 commit/push**，待用户明确指示。
- 若需彻底清零 566 raw broad-except 长尾，可再派审计（多为已合理 fail-safe，收益低）。

### 手动推进补充（23:48）
- 23:47 再派 batch2 仍 **429** → 改自编辑 **LF 文件**（Edit 安全）完成 A 变换，编译全过：
  - `api/scores/records_routes.py` 12 处全补 `exc_info=True`（`%s` 风格 3 + f-string 9 + 854 多行）；`api/users/users_routes.py` 5 处（FTS 更新×2/移除/ScoreChange 批量/导入失败）。
- 后端已改累计 **33 文件**（原 23 + batch1 8 + records_routes/users_routes 2），全 `python -m py_compile` EXIT=0。
- 剩余 ~78 文件多为 CRLF + 1-3 散点，须字节级脚本或 subagent；00:36 速率窗口后按显式清单分批后台派发，勿并发过多。中途状态汇报给用户，恢复后继续。
