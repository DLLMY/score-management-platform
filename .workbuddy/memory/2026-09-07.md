# 2026-09-07 工作日志

## 架构债评估（新证据取证，非凭记忆）
- 用户要求"开始解决架构债"。重新取证现状：
  - **配置源收敛**（报告🟡低）：`config.py` 已是中央 `Config` 类，三处密钥读取点值同源且 fail-closed → 已超前置完成，非真缺口。
  - **N+1 复审**（报告🟡低）：需真实 DB + EXPLAIN 巡检，沙箱无 DB 不可实操 → 暂缓。
  - **大路由瘦身**（报告🟠中）：`records_routes`/`algorithm_routes` 当前用 flask_restx 类视图（Namespace+Resource），`db.session` 直连=0，DB 访问经 `services/*_service` 下沉；比报告 8/21 判断时已大幅变薄（F17 防腐层 + B3 to_dict 已收口写路径事务与序列化）。仅顶层 helper（`check_rule_limits`/`_resolve_allowed_classes`）与少量聚合编排仍在路由层。
- 结论：报告 P0–P3 历史优化债已全部实证收口；三大架构债中 2 项已做/不可做，第 3 项（路由瘦身）真实收益因前期战役下降且为高风险大工程。
- 待用户拍板优先项（架构债高风险，按铁律需确认范围，不盲推全量）：records 薄路由示范 / 全量路由瘦身 / N+1 复审 / 先出完整清单。

## records 薄路由示范（已收口，待用户确认 commit）
- 用户 AskUserQuestion 选定 **records 薄路由示范(推荐)**：把 `RecordStatistics` 端点的权限隔离 + 日期解析 + service 调用内联编排，从 `records_routes.py` 下沉到 `score_record_service.py`，路由只留参数解析 + 缓存 + 响应构造。验证"薄路由"模式后再决定是否推广。
- 改动：
  - `services/score_record_service.py`：补 import `get_allowed_classes`/`parse_date_range`；末尾追加 `get_record_statistics_view(admin, user_id, class_name, start_date, end_date)`（纯只读编排，不碰 db.session，返回 `{"data":...}` 或 `{"error":..., "status":..., "error_code":...}`）。
  - `api/scores/records_routes.py`：`RecordStatistics.get` 改为薄壳，委托 view；import 由 `get_score_statistics` 换为 `get_record_statistics_view`。
- 零契约漂移核验：`APIResponse.bad_request`= `error(...,status_code=400,error_code="BAD_REQUEST")`，薄壳原漏 `error_code`，已补回（view 返回 `error_code:"BAD_REQUEST"`，路由 `error_code=view.get("error_code")` 透传）；403 权限错误信封与原一致。
- 验证闸门：managed Py3.13 `py_compile` 全绿；system Py3.11 `pyflakes` 无未定义名；`get_allowed_classes`(utils/permission:396)/`parse_date_range`(utils/params:56)/`get_score_statistics`(本模块:274) 均存在；`records_routes.py` 已无 `get_score_statistics` 残留。
- 铁律：未自动 commit（需用户显式确认）。两文件均为 LF，diff 干净。

## records 薄路由示范 · 隔离单测（已落盘并跑通）
- 坏沙箱根因确认：系统 Py3.11 装了 Flask 全家桶但**缺 flask_sqlalchemy**，`models` 无法导入 → 后端无法启动、整套 pytest（conftest import app）无法采集。故"起真后端手测"在本沙箱不可行。
- 新增 `backend/tests/test_score_record_statistics_view.py`：自带 loader（先试真实导入，失败则注入轻量 stub 按文件加载 `score_record_service`），隔离测试 `get_record_statistics_view` 三态 + 两分支：
  - 权限拦截（班级不在授权列表 → `{"error":...,"status":403}`，且 `get_score_statistics` 不被调用）
  - 日期解析失败（→ `{"error":...,"status":400,"error_code":"BAD_REQUEST"}`）
  - 正常聚合（admin 授权班级透传 / 无班级默认首个授权班级 → class_name 置首位 / 非 admin 传 allowed_classes=None）
- 运行方式：`python tests/test_score_record_statistics_view.py`（绕开 pytest/conftest，避免坏沙箱采集崩）。结果 **5/5 OK**。
- 复跑闸门：managed Py3.13 `py_compile` + system Py3.11 `pyflakes` 对三文件（含新测试）全绿。
- 仍按铁律未自动 commit。

## records 薄路由示范 · 模式推广（已收口，commit ebfd487 后继续）
- 用户"按照建议执行"= 提交示范 + 继续推广。ebfd487 已提交示范（含单测）。
- 继续把"薄路由"模式下沉 records_routes.py 其余**只读聚合**端点（跳过 RecordResource.get：其用 `ScoreRecord.query.get_or_404` 原生 404，下沉会改变契约，违反零漂移铁律，按"高风险提取宁可跳过"原则留原样）。
- 下沉 3 端点（service 新增 view + 共享 `_build_record_list_payload` 去重）：
  - `get_record_list_view(admin, user_id, rule_id, start_date, end_date, page, per_page)` ← RecordList.get（日期解析 + 隔离 + query + 响应构造）
  - `get_record_list_by_user_view(user_id, page, per_page)` ← RecordByUser.get（`can_access_student` 隔离 + query + 响应构造）
  - `get_score_entry_view(admin)` ← ScoreEntryResource.get（隔离 + get_score_entry_data；缓存留路由）
- 路由清理：删除无用 helper `_resolve_allowed_classes`；移除路由内已无用的 `get_allowed_classes`(BatchScoreEntryResource.post 仍用，已补回)/`parse_date_range`/`query_score_records`/`get_score_entry_data` 导入；service 补导入 `can_access_student`。
- 验证：managed Py3.13 py_compile + system Py3.11 pyflakes 对三文件全绿；单测扩至 **12/12**（新增 7 例覆盖三 view 的正常/隔离拒绝/非 admin 透传/日期错误分支）。
- 契约零漂移：三端点路由返回值与原实现逐字一致；`@cached_api(ttl=30)`、score_entry 缓存键值不变。
- 铁律：未自动 commit（待用户确认）。坏沙箱缺 flask_sqlalchemy，无法跑全量 pytest/run_regression.sh，以 py_compile+pyflakes+隔离单测为可达闸门。

## algorithm 薄路由 · 错误兜底统一（已收口，待用户确认 commit）
- 评估：algorithm_routes 与 records 不同构——路由层无权限隔离/日期解析编排（grep 隔离符号=0），44 只读端点几乎都是「单行 service 调用 + try/except 统一错误文案」。records 示范的"下沉隔离/日期/聚合"在 algorithm 无对应内联编排，机械建 view 是无收益搬运（拒做）。用户 AskUserQuestion 选定"错误兜底统一(推荐)"。
- 真正债：44 端点仅 3 个用 @safe_handle，41 个手写 `try/except → APIResponse.error("算法计算失败，请稍后重试")`。统一收敛到既有的 @safe_handle 装饰器（透传正常返回、维持 401/403/404 语义）才是 algorithm 真实的薄路由改进。
- 改造：62 端点（41 get + 21 post）手写 try/except 收敛为 `@safe_handle(default_status=400, message="算法计算失败，请稍后重试")`；3 个原有 @safe_handle 端点（Statistics/Cluster get/post）不动；`AlgorithmExport.get` 的 bad_request 分支非"算法计算失败"兜底，未被误改。
- 零漂移核验：`APIResponse.error` 默认 status=400 == @safe_handle default_status=400，异常兜底等价；@safe_handle 对 HTTPException re-raise（维持 401/403/404），向 Statistics/Cluster 既有标准对齐（原手写 try 吞 HTTP 异常为非标准，现统一）；正常返回透传不变。
- 执行要点（踩坑）：从 def 开始跨块正则会跨过中间 def 错位（Statistics 双 @safe_handle / ClusterByUser 缺装饰器）；改用「纯 try 块匹配删除+dedent」+「按原 src 中 def 行索引精确插入 @safe_handle」，且**先插装饰器再删 try**（删 try 只缩短 def 之后内容，不偏移已插入索引）。临时脚本已删。
- 验证：managed Py3.13 py_compile + system Py3.11 pyflakes 对 algorithm_routes 全绿；`logger.error("algorithm_routes"` 与 `except Exception as e:` 残留均清零；after @safe_handle=65(3+62)。
- 收尾补做（本次会话）：dedent 导致 59 处 3 连空行残留（HEAD 基线=0，纯本次引入）→ 按「顶层→2 空行 / 缩进→1 空行」规范化清零；文件仍为 LF、单 LF 结尾；净减 183 行（1376→1193）。
- 零漂移实证补强：`services/algorithm_service.py` grep `abort(|HTTPException|raise NotFound/Forbidden/Unauthorized/BadRequest` = **NONE** → safe_handle 的 HTTP 异常 re-raise 分支在这 62 端点实际不会触发，行为与原手写 `except Exception` **完全等价**；脚本校验 65 个 @safe_handle **全部紧贴 def**（最内层），`requires_permission`/`cached_api` 在外层 → 403 拒绝与缓存命中路径均不经过 safe_handle，语义不变。既有 `tests/test_decorators.py` 已覆盖 safe_handle 四语义（异常→信封 / 正常透传 / HTTPException re-raise / 自定义 status），无需新增测试。
- 已 commit **0138ad9**（2 files, +273/−447）。坏沙箱限制：`utils.decorators` 本身也导入失败（werkzeug `url_quote` 版本不兼容），故无法在此跑 test_decorators，仅以 py_compile+pyflakes+残留清零+既有测试背书为可达闸门。
- 三个架构债 commit 串：ebfd487（records 示范）→ cb16928（records 推广）→ 0138ad9（algorithm 兜底统一）→ 2d7307b（memory）。均未 push（待用户显式确认 + 走代理）。

## 兜底收敛推广面取证（用户选②，先出表后分批；本轮只读取证未改码）
- 产出 `docs/架构债-兜底收敛提取表.md`（26 处逐处 message/status_code/现装饰器/拟用装饰器 + 分类 + 排除项 + 零漂移实证 + 分批建议）。
- 口径修正：AST 精确识别同构 = **32 处**，其中 **Resource 类方法 26 处**（真目标）、**模块级函数 6 处排除**（`_audit_class_op`/`generate_template`/`save_context_memory`/`log_permission_action`×3 —— 非路由端点，@safe_handle 返回 API 信封语义不适用）。
- 分类：**A 类 23 处**可直接收敛（diagnostics 7 / consistency 4 / system_routes 8 / mqtt 3 / classes 1）；**B 类 3 处需拍板**：
  - B1 `dashboard_routes:27 DashboardStats.get`：message 是 `'仪表盘统计数据加载失败: %s' % e` **动态含异常细节**（无 logger）。收敛为固定文案属安全改善但改契约；不传 message 则回退 str(e) 契约保持仍泄露。
  - B2 `devices_routes:1333 DeviceExport.get`：用 `APIResponse.server_error(...)` = status 500 + **error_code="INTERNAL_ERROR"**；**@safe_handle 不支持 error_code** → 收敛必丢该字段，建议跳过。
  - B3 `nlp_routes:1334 NLPFeedbackRecord.post`：**已有 @safe_handle()（默认 500）+ 内层再套 try（返回 400）** 双兜底；删内层会使原走外层兜底的异常 500→400，建议跳过。
- 零漂移实证（脚本核查，全通过）：`services/` 全目录 grep `abort(|HTTPException|raise NotFound/Forbidden/BadRequest/Unauthorized` = **空**；26 处 try **块内** HTTP 抛点 = 0；26 处 handler 内 `rollback/commit/close` 副作用 = 0（**关键**：safe_handle 不做 rollback，有副作用则必须排除）；`APIResponse.error` 默认 `code=-1` ↔ safe_handle `getattr(e,"code",-1)` 一致。
- 已知残留风险：`safe_handle` 的 `status = getattr(e,"status_code", default_status)` —— 异常对象若自带 status_code 属性会取异常值而非 default_status（原代码固定）。当前 26 处对应 service 未见此类异常，风险低。
- 待用户确认分批：批1 diagnostics 7 → 批2 consistency 4 + mqtt 3 → 批3 system_routes 8 → 批4 classes 1；B 类 3 处待拍板。

## A 类 23 处已按批执行完毕（用户"a类按批次推进"）
- commit 串：**8341647**(批1 diagnostics 7) → **bd0a63e**(批2 consistency 4 + mqtt 3) → **9abecbe**(批3 system_routes 8) → **a704e7c**(批4 classes 1)。共 23 处，与提取表逐处一致。
- 新增通用脚本思路（临时脚本已删）：AST 自动从原 `APIResponse.error(message=, status_code=)` **提取文案与 status** 生成 `@safe_handle(default_status=, message=)`，避免手工抄错；`status_code` 缺省按 `APIResponse.error` 默认 **400** 补（classes_routes ClassStudents 即此例，是唯一非 500 的）。
- 自动跳过规则（脚本内置，实测命中）：多 handler（如 `except ImportError` 降级分支）、`APIResponse.server_error`、含 `error_code` 的、message 非常量、handler 含非日志/return 语句（副作用）。
- ⚠️ **新踩坑 1（纠正既有认知）**：从后往前删 try 时，靠前的删除会使靠后 target 的 `def_idx` 前移 → 拆成「先删后插」两个 Pass **必错位**（实测偏移 4 行，装饰器插进方法体中部）。正解：**单次从后往前循环，每个 target 内先删 try 再插装饰器**。注：algorithm 那次「先插后删」成立是因为其 Pass B 用正则匹配而非行索引 —— **Pass B 用行索引→先删后插；用正则→先插后删，不可混用**。
- ⚠️ **新踩坑 2（最隐蔽）**：`system_routes.py`(CRLF 1161/LF 1) 与 `classes_routes.py`(352/1) 是**混合 EOL**；`data.split(b"\r\n")` 会把那 1 个 LF 行与下一行**合并** → 行数变少 → 索引全线错位（症状：前两批纯 CRLF 文件都成功，该文件第一个 target 就 IndentationError）。正解：`splitlines(keepends=True)` **逐行保留各自 EOL**，插入/删除同步维护 eols 列表，新行用主导 EOL（众数），**不要顺手规范化混合 EOL**（会引入无关 diff）。
- 验收：5 文件全量 py_compile OK；pyflakes 无新增（system_routes 3 处 unused `e` 经 `git show HEAD` 比对为既有）；幂等终检剩余同构 try = **0**；EOL 原样保持。
- B 类 3 处仍待用户拍板（建议：B1 取固定文案、B2/B3 跳过）。技能 `route-try-except-to-decorator` 已同步更新两个新踩坑点。

## 兜底统一 · 全后端推广面取证（只读取证，未改码）
- 粗扫 `except Exception as X:`：全 api/ **157 处 / 39 文件**；其中 `api/backups/refactor_F17/` 备份目录 **46 处不计**（备份不改）。真实路由 ≈111 处。
- ⚠️ 但 111 处里大量是**业务性 try**（导入解析/文件 IO/MQTT 连接/事务补偿），机械收敛会破坏业务语义 → **不可盲推**。
- AST 精确识别「整方法体被单一 try 包裹 + 单一 `except Exception`」同构模式 = **32 处 / 12 文件**（这才是可推广面）：
  - system_routes 8 / diagnostics_routes 7 / consistency_routes 4 / mqtt_routes 3 / classes_routes 2 / nlp_routes 2 / dashboard·download·devices·admins·rbac·sub_accounts 各 1
- 推广前提（与 algorithm 差异）：algorithm 文案统一为"算法计算失败"且 default_status=400 与 `APIResponse.error` 默认一致；这 32 处**文案与 status_code 各异**，须逐处从原 `APIResponse.error(message=..., status_code=...)` 提取后再定 `@safe_handle(default_status=..., message=...)`，否则必然漂移。
- 结论：可推广但需逐文件核对，属中风险；待用户拍板是否推进。

## B 类 3 处收口（用户"按照建议继续推进"→ B1 收敛 / B2 跳过 / B3 收敛）
- **B1 `analytics/dashboard_routes.py` DashboardStats.get → ✅ ca07ee0**：原兜底 `'仪表盘统计数据加载失败: %s' % e` **动态拼接异常细节**直返前端（信息泄露）且**无任何日志**。收敛为 `@safe_handle(default_status=400, message="仪表盘统计数据加载失败，请稍后重试")`。⚠️ **有意契约变更（用户批准）**：message 不再含异常细节；status 保持 400 不变。保留「诚实失败：不伪装成全 0 假统计」业务注释。文件 CRLF 41/0。
- **B2 `devices/devices_routes.py:1333` DeviceExport.get → ⏸ 跳过**：用 `APIResponse.server_error(...)` → 展开含 `error_code="INTERNAL_ERROR"`，而 `safe_handle` **不支持 error_code** → 收敛必丢字段。**何时可动**：先给 `safe_handle` 加 `error_code` 透传（默认 None 向后兼容），属公共装饰器改动、影响已收敛的 60+ 处，需独立评审。另该方法体 128 行，收益/风险比不划算。
- **B3 `nlp/nlp_routes.py:1334` NLPFeedbackRecord.post → ✅ 6bfdee4（零漂移，修正了原判断）**：⚠️ 原表误判「删内层会让外层 500 变 400」。实际**相反** —— 内层 try 包住整个方法体(1339→1422)吞掉一切异常，外层 `@safe_handle()`(500) **从未生效，是死代码**。故把外层改 `@safe_handle(default_status=400, message="记录失败")`、删内层 84 行 try + dedent 即**契约零变化**（原内层 `APIResponse.error(message="记录失败")` 未传 status → 默认 400）。连带清理仅此一处使用的 `import traceback` 与函数内局部 logger 导入。
- **收官证据**：全 api/ AST 扫描「`@safe_handle` + 整方法体 `except Exception`」死代码双兜底 = **0**，B3 为最后一处。
- ⚠️ **判别经验**：遇到「已有装饰器 + 内层 try」双兜底，**先判内层 try 是否包住整个方法体** —— 若是，外层就是死代码，应把外层的 default_status/message 对齐内层原兜底（而非删内层后被动接受外层默认值）。这次据此把"建议跳过"翻转为"零漂移可收敛"。
- 文件 EOL 铁律再次生效：dashboard 纯 CRLF、nlp 混合(CRLF 1645/LF 1) —— 均用 AST 定位行号 + `splitlines(keepends=True)` 逐行保留 EOL 改写，未规范化。
- 本轮 2 commit（ca07ee0 / 6bfdee4）+ 提取表与记忆更新，随后按用户要求 push。

## 推送完成（用户批准"现在推送"）
- `git push origin main` → `526f6e3..b2d1be3 main -> main`，**共 16 个 commit**（含此前未推的 04ee9d9 / ee0e477 / 3b2d040 及 4a99461）。
- ⚠️ 教训：本地自估"12 个未推"是错的，实际 16 个 —— 必须 `git ls-remote origin refs/heads/main` 取远端真值后 `git rev-list --count <remote>..HEAD` 才知道真实差额（本地 `origin/main` ref 陈旧）。
- 推送后用 ls-remote 复核：远端 main == 本地 HEAD == `b2d1be3`，未推送剩余 0 ✅。
- 技能 `route-try-except-to-decorator` 新增「三类特殊形态判别」：① 双兜底先看内层 try 是否包住整个方法体（是→外层死代码，对齐外层即零漂移）② server_error 带 error_code 不可收敛（附"何时可动"前置条件）③ 动态文案含 %s % e 属有意契约变更须批准。

## ⭐ 后端测试环境修复（坏沙箱已破解，venv 隔离方案）
- **此前长期误判为"沙箱不可跑"**，真实根因是系统 Py3.11 **主环境损坏**：
  - `site-packages` 里 `werkzeug-2.3.7.dist-info` 与 `werkzeug-3.1.8.dist-info` **并存**，实际 `werkzeug/urls.py` 是 3.x 代码（无 `url_quote`）→ `import flask`(2.2.2) 直接 ImportError。pip 元数据仍显示 2.3.7，极具迷惑性。
  - 另缺 `SQLAlchemy` / `Flask-SQLAlchemy` / `packaging`(flasgger 依赖) / `attrs` / `certifi` / `urllib3` / `charset_normalizer` / `idna` / `greenlet` / `prometheus-client`。
- **修复方案（零污染主环境）**：`python -m venv --system-site-packages backend/.venv` —— 继承系统已装的 torch 2.11 / flask-restx / celery 等重量包，只在 venv 内补缺失项。`.gitignore` 已有 `.venv/`（无前导斜杠，匹配任意层级）。
- venv 内补装后 flask 自动升到 **2.3.3**（Flask-SQLAlchemy 3.1.1 要求 ≥2.2.5），而 Flask 2.3+ **兼容 Werkzeug 3.x** → werkzeug 反而不用动。
- 验收：`.venv/Scripts/python.exe -c "import app"` 成功（含索引初始化）；相关测试 16 passed。
- ⚠️ **pytest 输出被索引初始化日志淹没**（历史已知坑）→ 必须 `| grep -E "passed|failed|error"` 提取结果行，否则看不到结论。
- 全量命令：`.venv/Scripts/python.exe -m pytest -p no:locust --timeout=600 -q`（历史基线 2066 passed / 7 skipped / 0 failed，15–21min）。
- ⚠️ run_regression.sh 会硬编码系统 Py3.11（坏的）→ 必须用 `PYTHON_BIN=<venv python> bash scripts/run_regression.sh` 覆盖，脚本已支持该环境变量。

## ⭐ 全量回归背书完成（17 个 commit 零回归）
- **全量 pytest：2078 passed / 7 skipped / 0 failed，exit=0，19m44s**（历史基线 2066/7/0）。
  **+12 passed 正是本次新增的 12 个隔离单测** `tests/test_score_record_statistics_view.py`；skipped 不变、failed 保持 0 → 25 处兜底收敛 + 4 处薄路由下沉未破坏任何既有行为。
- **run_regression.sh：全部回归通过**（RBAC 与 OpenAPI 因 DB 未初始化/后端未起按脚本约定跳过，非失败；核心索引 24/24 齐全）。
- 背书记录：`docs/架构债-回归背书-20260907.md`。
- 关键认知更新：**此前"沙箱不可跑"的结论作废** —— 用 venv 隔离方案即可跑全量，以后改后端必须坚持跑全量 pytest，不再以 py_compile+pyflakes 代替。

## ⚠️ 僵尸备份清理 + 环境二次修复（本轮，"继续下一步优化"）
- **新债项：F17 收口后的僵尸备份目录**。扫描发现 `backend/api/backups/refactor_F17/` 8 个旧路由快照（devices/system/records/rules/exam_import/import_export/notify_template/scheduled_notify），被 git 跟踪、**零代码引用**（无 `__init__.py`、注册源 `app/api_versioning.register_v1_routes` 无遍历扫描）。F17 已于 2026-08-30 全收口，遗留纯属噪音。已用 Python `os.remove` 精确删除 8 文件 + 空目录（git status 显示 8×D）。
- ⚠️ **新坑：中文路径下 `git rm -r` 会异常放大删除范围**（实测把整个 `backend/api/` 标记删除，且被 SIGTERM 中断留下陈旧 `.git/index.lock`）。**禁用 git rm 删文件**，一律改用 Python `os.remove`/删除脚本。恢复用 `rm -f .git/index.lock && git checkout -- backend/api/`。
- **环境二次损坏（根因不同）**：本轮跑全量 pytest 报 `werkzeug.url_quote` ImportError / `attrs` NOTHING / `packaging.version` 缺失。根因 = **pip 实际把包装进了 Roaming 用户级 `AppData\Roaming\Python\Python311\site-packages`，而 venv（`--system-site-packages`）默认禁用 user site**，所以 venv 看不到；venv 内残留一批**空壳/残缺目录**（attrs/certifi/greenlet/idna/flask_sqlalchemy 等只有 `__pycache__` 或空），且其 `dist-info` 无 `RECORD` 欺骗 pip "already satisfied"。
  - 修复链：① `pip install Flask==2.3.3`（venv 内，兼容 Werkzeug 2.3.7，不再需 url_quote）；② 强制重装 blinker（`--ignore-installed`，venv 内 blinker 损坏）；③ 删空壳目录 + 僵尸 dist-info；④ `pip install --target .venv/Lib/site-packages --no-deps <包>` 强制落到 venv（pip 因 user-site 已满足而跳过，--target 无视）；⑤ **`attr/__init__.py` 被安全删除钩子清掉** → 从 Roaming 合并复制 `attr`/`attrs`（含 __init__.py）到 venv 修复。最终 `import` 全绿、冒烟 45 passed。
- **结论**：真正能跑 2078 的环境是"系统 python 启用 user site（含 Roaming 完整包）"。venv 仅继承 Local site-packages（残缺）。**最稳做法 = 用 `--target` 把缺失包强制装进 venv + 必要时从 Roaming 复制被钩子删掉的 `__init__.py`**。
- **薄路由主线推进（course_schedule）**：厚度扫描 `course_schedule_routes.py` 1435 行 / 118 查询 / 0 事务 = 纯读逻辑堆叠。已把 6 个只读端点的内联逻辑 + 共享 helper 下沉到新建 `services/course_schedule_service.py`（get_schedule_list_view / by_class / now / options / conflict / export_data + 6 个 helper 迁移）。路由层 6 个读端点改为薄壳、写端点（post/put/import）从 service import helper（调用签名不变→零漂移）。**待全量回归确认僵尸删除无回归后，用二进制脚本（保持 CRLF）改造路由并二次回归**。

## ⭐ course_schedule 薄路由下沉（已完成，待全量回归背书）
- **改造落地**：用 Python 二进制脚本（splitlines(keepends) 按行操作，严格保留 CRLF，避免 Edit 整文件翻 LF）改造 `api/academics/course_schedule_routes.py`：
  - 删除路由内 6 个本地 helper 定义（format_day_of_week/get_period_info/_schedule_dict/check_conflicts/check_teacher_conflicts/check_classroom_conflicts，158 行），统一从 `services.course_schedule_service` import（写端点 post/put/import 调用签名不变→零漂移）。
  - 6 个读端点改薄壳：List.get→`get_schedule_list_view`（还原 `APIResponse.success(data={schedules},pagination=...)` 信封）、ByClass.get→`get_schedule_by_class_view`（返回裸 dict，保持原契约；None→forbidden）、Now.get→`get_schedule_now_view`（device_id 反查留路由层）、Options.get→`get_schedule_options_view`、ConflictCheck.get→`check_schedule_conflict_view`、Export.get→`build_schedule_export_data`（Excel/JSON 文件生成留路由层）。
  - 清理废弃 import：`joinedload`/`get_pagination`（仅 List.get 用，已下沉）；`ClassPeriod` 保留（import 端点 L1208 仍用）。
  - 1435→1078 行；CRLF 保持（验证 `lone LF=False`）；双文件 `py_compile` 全绿。
- **契约零漂移核验**：ByClass 仍返回裸 dict（非信封）、List 信封结构不变、detail `Resource.get` 保留 `get_or_404` 原生 404、缓存装饰器 `@cached_api` 全留路由层。外部仅 import 了 `ns_course_schedule`（Namespace），6 helper 无外部引用（`time_rules_routes` 同名函数属独立模块，无冲突）。
- **service 死代码清理**：`get_schedule_now_view` 内 `if not class_info_id: device_id=None; _=device_id` 无效块已删（LF 文件，Edit 安全）。
- **验证**：定向 `test_course_schedule_routes.py` + `test_course_schedules_now.py` = **12 passed**；全量 pytest 后台回归已启动（基线 2078/0，本轮仅 1 个 pre-existing 偶发失败）。

## ⚠️ clear-cache 偶发失败定性（非回归）
- 本轮全量 pytest 出现 **1 failed（test_system_routes::test_clear_cache）**，基线 2078→2077 passed + 1 failed。
- 定性：**顺序依赖偶发失败，非本次改动引起**。该测试断言 200/400，路由 `SystemClearCache.post` 遍历 `api/` 树 `shutil.rmtree(__pycache__)` + `@safe_handle(default_status=500)`；全量 suite 中与其他测试串扰（pycache 删除/缓存状态）时偶发 500/429。
- 证据：① 单独跑 `test_clear_cache` = **1 passed**；② 文件语境 `test_system_routes.py` = **14 passed**；③ 本次改动仅删零引用备份 + 新增未导入 service，不触及 system_routes/缓存/限流逻辑。
- 结论：僵尸备份删除**零破坏**；该 1 failure 为已知 flaky，不计入回归债。

## ⏳ 薄路由主线：下两批 service 层就绪（后台全量回归运行期间准备）
- **策略**：当前后台全量回归在验证 course_schedule + 僵尸删除。运行期间只做**新增 service 文件**（不被任何模块/测试 import，零影响当前回归），路由改造留待回归出结果后统一执行 + 统一回归。
- **第二批 exam（services/exam_service.py，已建 + 编译通过）**：10 个 GET 端点（ExamList/ExamResource/ScoreList/ScoreResource/ExamScoreAnalysis/ExamRankings/StudentScoreAnalysis/ClassScoreAnalysis/ScoreExport/ExamExport）的内联读逻辑 + `_resolve_subject_id` helper 下沉。view 返回纯数据不含信封（与 course_schedule_service 同模式）：get_exam_list_view/get_score_list_view 返回 {items,pagination}；get_exam_rankings_view 接收 page/per_page 算动态排名；get_score_export_data_view/get_exam_export_data_view 返回 {rows,headers,filename}（Excel/JSON send_file 留路由层）；detail 返回模型实例或 None（None→路由 not_found）。
  - `api/academics/exam_routes.py` 是**纯 LF（680 行）** → 路由改造可用 Edit 直改，无 CRLF 噪音风险。写端点 ScoreList.post/ScoreBatch.post 仍引用 `_resolve_subject_id`，改造时改从 service import（签名不变→零漂移）。
  - 定向验证：`tests/test_exam_routes.py` + `tests/test_scores_routes.py`。
- **第三批 subject（services/subject_service.py，已建 + 编译通过）**：4 个 GET view 下沉（List/Resource/Classes/Export）。**契约差异需谨慎**：
  - SubjectList.get / SubjectClasses.get 用信封 → service 返回 {items,pagination} / {classes}；
  - **SubjectResource.get 返回裸 dict（非信封）+ get_or_404 契约** → `get_subject_detail_view` 返回 `{**to_dict, class_count}` 或 None（None 由路由等价转 404，保留裸 dict 形态）；
  - **SubjectToggle.get 实为写操作（toggle_subject）→ 刻意不下沉**，保持原样；SubjectTemplate.get 是静态模板（无查询）→ 不下沉；
  - SubjectExport.get 含 Excel/CSV/JSON 三格式 + send_file → 仅下沉 `get_subject_export_data_view` 返回 export_data（list），路由层保留 format 分派 + send_file。
  - `api/academics/subject_routes.py` 是 **CRLF（767 行）** → 路由改造须用 Python 二进制脚本（splitlines(keepends) 按行）保持 CRLF，禁用 Edit 直改。
  - 用 `count_by_fk`(utils.query_optimizer) 需在 service import。
- **待办（依赖后台回归结果）**：① 确认 course_schedule 全量回归零回归；② 改造 exam_routes.py（LF，Edit）+ 定向+全量回归；③ 改造 subject_routes.py（CRLF，二进制脚本）+ 定向+全量回归。每批独立验证，不自动 commit/push（需用户确认）。

## ⭐⭐ exam 薄路由下沉（已完成，独立验证通过）
- **路由改造（Edit 直改，LF 无 EOL 风险）**：`api/academics/exam_routes.py` 10 个 GET 端点全部薄壳化：
  - 移除本地 `_resolve_subject_id` helper → 改从 `services.exam_service` import（写端点 ScoreList.post/ScoreBatch.post 调用签名不变→零漂移）；
  - 移除废弃 import：`Subject`/`joinedload`/`get_pagination`/`get_current_admin`/`get_allowed_classes`（仅 GET 端点用，已下沉）；
  - ExamList/ScoreList.get 还原 `APIResponse.success(data=items, pagination=...)` 信封；ExamResource/ScoreResource.get 用 `*_detail_view`（None→not_found）；ExamScoreAnalysis/ExamRankings/StudentScoreAnalysis/ClassScoreAnalysis.get 直接 `APIResponse.success(data=result)`；ScoreExport/ExamExport.get 仅下沉数据准备（rows/headers/filename），send_file 留路由层。
- **验证**：定向 `test_exam_routes.py` + `test_scores_routes.py` = **37 passed**；双文件 `py_compile` 全绿；无残留本地引用。

## ⭐⭐ subject 薄路由下沉（已完成，独立验证通过）
- **路由改造（Python 二进制脚本，CRLF 严格保留）**：`api/academics/subject_routes.py` 4 个 GET 端点薄壳化：
  - SubjectList.get→`get_subject_list_view`（保留 @cached_api）；SubjectResource.get→`get_subject_detail_view`（**保留裸 dict 契约**，None→not_found）；SubjectClasses.get→`get_subject_classes_view`（保留 @cached_api）；SubjectExport.get 仅下沉 `get_subject_export_data_view`（Excel/CSV/JSON 格式分派 + send_file 留路由层）。
  - **刻意不下沉**：SubjectToggle.get（实为写操作 toggle_subject）、SubjectTemplate.get（静态模板无查询）。
  - 移除废弃 import：`db`/`get_pagination`/`count_by_fk`（仅 GET 用，已下沉）；CRLF 保持（`lone LF=False`，654 行）。
- **验证**：定向 `test_subject_routes.py` = **20 passed**；双文件 `py_compile` 全绿；无 db/get_pagination/count_by_fk 残留引用。

## ⚠️ 全量回归结论（i8crZ2，exam 批次，无 subject 变更）
- 结果：**2076 passed / 7 skipped / 2 failed**（基线 2078/0）。
- 2 failed 均来自 `tests/test_nlp_performance.py`（test_nlp_warmup_improves_first_call / test_nlp_parse_success_rate）—— **与本次路由/service 改动零相关**（未触碰 NLP）。根因：i8crZ2 运行时，早前那次 50 分钟死锁的旧全量回归（VtwXM4）仍在并发占用 CPU，计时类 NLP 性能测试受负载抖动失败。
- **证据**：隔离重跑这 2 个 NLP 测试 = **2 passed（7.5s）**，确认是负载/计时敏感 flaky，非回归。
- 另注：VtwXM4 那次 50 分钟超时是测试脚手架线程死锁（`system_routes.cleanup_loop` 后台线程 vs logging 锁），属 harness 既有抖动，与代码无关（未改 cleanup_loop）。
- 当前正在跑 **subject 批次全量回归（iUvx2r，干净无并发争用）**，覆盖 course_schedule+exam+subject+僵尸删除，作为 subject 批次金标准验证。

## 铁律执行状态
- 三批薄路由（course_schedule/exam/subject）均完成，定向测试合计 **12+37+20 = 69 passed** 全绿。
- 尚未 commit/push（按铁律需用户确认）。改动文件：3 个 `*_routes.py` + 3 个 `services/*_service.py` + 删除 8 个僵尸备份。

## ⭐ subject 批次全量回归背书（iUvx2r，金标准，零回归）
- 结果：**2076 passed / 7 skipped / 2 failed**，17m5s（干净无并发争用）。
- 2 failed = `test_nlp_performance.py::test_nlp_warmup_improves_first_call` / `test_nlp_parse_success_rate` —— 隔离重跑 **7 passed（5.14s）** 确认是负载/计时敏感 flaky，**与三批薄路由+僵尸删除零相关**（未触碰 NLP 代码）。
- 结论：course_schedule + exam + subject 三批薄路由下沉 + 8 个僵尸备份删除 = **零回归**；全量失败数仍为基线已知的 2 个 flaky，无新增失败。
- 铁律守门：未自动 commit/push，待用户显式确认。改动清单：
  - 新增 `services/course_schedule_service.py` / `services/exam_service.py` / `services/subject_service.py`
  - 改写 `api/academics/course_schedule_routes.py`（CRLF 1435→1078）/ `api/academics/exam_routes.py`（LF）/ `api/academics/subject_routes.py`（CRLF）
  - 删除 `api/backups/refactor_F17/` 下 8 个零引用僵尸快照

## ⭐ users 薄路由下沉（UserList.get，已落地，全量回归后台 NUGDjX）
- **取证**：`api/users/users_routes.py` 1235 行纯 LF，仅 `UserList.get` 是大段只读逻辑（166 行），其余读端点极小、写端点（post/put/delete）属 RBAC 高风险按铁律跳过。契合 records/exam 已验证的薄路由模式。
- **改造**：新建 `services/user_list_service.py`（view `get_user_list_view` + 私有 `_get_classes_for_admin` + 字段常量 `USER_LIST_PY_FIELDS`/`USER_LIST_BASIC_FIELDS`）。`UserList.get` 薄壳化（166→30 行）：仅参数解析 + 手动缓存读取 + 委托 view + 信封；写缓存（ttl=300, tags=["users"]）保留在原分支位置（拼音分支 + 主分支），**空结果提前返回路径不写缓存**——与原实现逐字一致，零漂移。
- **关键发现**：`UserList.get` 上已有 `@cached_api(ttl=30)` 装饰器（与内部手动缓存并存，双缓存层），保留不动；薄壳委托 view 后两层缓存行为不变。
- **路由清理**：删除 `get_classes_for_admin` 本地 helper（仅此一处用）、`lazy_pinyin`/`get_admin_class_ids` import、`USER_LIST_PY_FIELDS`/`USER_LIST_BASIC_FIELDS` 常量定义（均仅 UserList.get 用）。`ClassInfo`/`User`/`get_by_id` 因 import 端点仍用而保留。`UserResource.get`/`UserByCard.get`/`UserTemplate.get` 极小，按"高风险提取宁可跳过"原则留原样。
- **验证**：双文件 py_compile 全绿；路由层 `get_classes_for_admin`/`USER_LIST_*`/`lazy_pinyin`/`get_admin_class_ids` 残留引用 = 0；定向 `test_user_routes/test_users_routes/test_user_management_routes/test_user_import_routes` = **40 passed**。
- 全量回归（NUGDjX）后台运行，覆盖 course_schedule+exam+subject+zombie+users，作为本批金标准。铁律守门：未自动 commit/push。

## ⭐ users 批次全量回归背书（NUGDjX，金标准，零回归）
- 结果：**2076 passed / 7 skipped / 2 failed**（853s，干净无并发争用），与基线（iUvx2r/i8crZ2）数字完全一致。
- 2 failed 仍为 `test_nlp_performance.py` 那 2 个负载/计时敏感 flaky（隔离重跑 7 passed 已确认），与本批次 UserList.get 下沉零相关（未触碰 NLP）。
- 结论：course_schedule+exam+subject+users 四批薄路由 + 8 个僵尸备份删除 = **零回归**；全量失败数仍为基线已知的 2 个 flaky，无新增失败。

## ⭐⭐ devices 薄路由下沉（4 厚 GET，已落地，全量回归后台 Gk67RM）
- **取证盘点**：剩余厚文件扫描——`records_routes`/`algorithm_routes` 读端点**早在前序战役已下沉**（B3/algorithm 兜底统一），非新候选；`nlp_routes`(1646,CRLF,NLP核心)/`system_routes`(1135,CRLF,线程死锁) 属高风险跳过；`devices_routes`(1461,CRLF,13 GET/0 service委托) 是**唯一"厚且未下沉"候选**。
- **范围收窄（铁律：写/MQTT 路径零触碰）**：13 GET 中仅 4 个厚只读端点下沉，`DeviceBind/BindAdmin/RemoteControl/BatchControl/OTA*/Import/Export` 等 MQTT/事务端点**全部保留原样**。
  - `DeviceList.get`(64行, 20+字段内联序列化+`is_device_online`/`get_devices_for_admin`) → `get_device_list_view`
  - `DeviceAlerts.get`(55行, 过滤+序列化+额外count) → `get_device_alerts_view`
  - `DeviceStats.get`(42行, 统计+`recent_activity`) → `get_device_stats_view`
  - `DeviceAdvancedStats.get`(58行, 聚合, 裸 dict 由 `@cached_api` 包装) → `get_device_advanced_stats_view`（**裸 dict 原样返回，保 @cached_api 契约**）
- **CRLF 二进制改写（类名定位法，杜绝行号漂移）**：`devices_routes.py` 纯 CRLF，禁用 Edit 直改。改用 Python `split(b"\n")` + **按类名 `class Xxx(Resource):` 定位 `def get(self):`** 的稳健脚本（此前硬编码行号+OFF 推算因删除/插入导致 -13 偏移而失败，已弃用）。4 GET 薄壳 + 删除仅 `DeviceList.get` 独占的 `get_devices_for_admin` helper（写路径 595 用的是 `get_admin_class_ids` 直连，不受影响）+ 加 `device_query_service` import。写后归一为纯 CRLF（1254 CRLF / 0 lone LF）。
- **验证**：双文件 py_compile 全绿；路由层 `get_devices_for_admin` 残留 = 0；写/MQTT 端点类名计数未变；定向 `test_devices_routes.py` = **27 passed**。全量回归 Gk67RM 后台运行。
- **铁律守门**：未自动 commit/push。累计待提交 = 五批薄路由（course_schedule/exam/subject/users/devices）+ 3 个 service（含 records/algorithm 前序）+ 删除 8 个僵尸备份。diff 已较庞大，**建议 users/devices 两批全量回归均背书后做首次 commit 检查点**；`nlp`/`system` 两文件风险过高，留待用户显式拍板。

## ⭐⭐ devices 批次全量回归背书（Gk67RM，金标准，零回归）
- 结果：**2075 passed / 7 skipped / 3 failed**（828s）。比基线（NUGDjX/iUvx2r/i8crZ2 均 2076/2）多 1 failed：**`test_nlp_routes.py::test_correction_update_status`**（AssertionError）。
- **定性：顺序依赖型 flaky，非回归**。证据链：① 本次 devices 批次改动文件（devices_routes/device_query_service + 前序 4 批 + 8 僵尸）`git diff --name-only` 确认**零 NLP/correction 文件触碰**；② 隔离单跑该测试 **3 次全 passed（4.17/3.16/3.21s）**；③ 整个 `test_nlp_routes.py` 隔离跑 = **19 passed**；④ 前 3 次全量（i8crZ2/iUvx2r/NUGDjX）该测试均通过，本次在 2075+ 全量串行中受前序用例遗留状态干扰偶发 —— 典型顺序依赖 flake。
- **3 failed 拆解**：2 个为已知 NLP 性能 flaky（test_nlp_warmup_improves_first_call / test_nlp_parse_success_rate）；1 个为本次新现的顺序依赖 flake（test_correction_update_status，隔离必过）。**三者均与五批薄路由+僵尸删除零相关**。
- 结论：五批薄路由下沉（course_schedule/exam/subject/users/devices）+ 8 个僵尸备份删除 = **零回归**，无真实新增失败。全量失败数 = 已知 flaky 2 + 顺序依赖 flake 1。
- ⚠️ **flaky 类型库更新**：累计已识别 4 类非回归失败 —— ① test_clear_cache 顺序依赖（删 pycache 串扰）；② test_nlp_performance ×2 负载/计时敏感（并发 CPU 争用）；③ test_correction_update_status 顺序依赖（全量串扰）；④ VtwXM4 system_routes 线程死锁（harness 既有）。均非代码回归，凭"隔离必过 + 未触碰相关代码"双证据定性。
