# 2026-09-07 工作日志

## 架构债评估（新证据取证，非凭记忆）
- 用户要求"开始解决架构债"。重新取证现状：
  - **配置源收敛**（报告🟡低）：`config.py` 已是中央 `Config` 类，三处密钥读取点值同源且 fail-closed → 已超前置完成，非真缺口。
  - **N+1 复审**（报告🟡低）：需真实 DB + EXPLAIN 巡检，沙箱无 DB 不可实操 → 暂缓。
  - **大路由瘦身**（报告🟠中）：`records_routes`/`algorithm_routes` 当前用 flask_restx 类视图（Namespace+Resource），`db.session` 直连=0，DB 访问经 `services/*_service` 下沉；比报告 8/21 判断时已大幅变薄（F17 防腐层 + B3 to_dict 已收口写路径事务与序列化）。仅顶层 helper（`check_rule_limits`/`_resolve_allowed_classes`）与少量聚合编排仍在路由层。
- 结论：报告 P0–P3 历史优化债已全部实证收口；三大架构债中 2 项已做/不可做，第 3 项（路由瘦身）真实收益因前期战役下降且为高风险大工程。
- 待用户拍板优先项（架构债高风险，按铁律需确认范围，不盲推全量）：records 薄路由示范 / 全量路由瘦身 / N+1 复审 / 先出完整清单。

## records 薄路由示范（已收口，待用户确认 commit）
- 用户 AskUserQuestion 选定 **records 薄路由示范(推荐)**：把 `RecordStatistics` 端点的权限隔离 + 日期解析 + service 调用内联编排，从 `records_routes.py` 下沉到 `score_record_service.py`，路由只留参数解析 + 缓存 + 响应构造。验证"薄路由"模式后再决定是否推广。
- 改动：
  - `services/score_record_service.py`：补 import `get_allowed_classes`/`parse_date_range`；末尾追加 `get_record_statistics_view(admin, user_id, class_name, start_date, end_date)`（纯只读编排，不碰 db.session，返回 `{"data":...}` 或 `{"error":..., "status":..., "error_code":...}`）。
  - `api/scores/records_routes.py`：`RecordStatistics.get` 改为薄壳，委托 view；import 由 `get_score_statistics` 换为 `get_record_statistics_view`。
- 零契约漂移核验：`APIResponse.bad_request`= `error(...,status_code=400,error_code="BAD_REQUEST")`，薄壳原漏 `error_code`，已补回（view 返回 `error_code:"BAD_REQUEST"`，路由 `error_code=view.get("error_code")` 透传）；403 权限错误信封与原一致。
- 验证闸门：managed Py3.13 `py_compile` 全绿；system Py3.11 `pyflakes` 无未定义名；`get_allowed_classes`(utils/permission:396)/`parse_date_range`(utils/params:56)/`get_score_statistics`(本模块:274) 均存在；`records_routes.py` 已无 `get_score_statistics` 残留。
- 铁律：未自动 commit（需用户显式确认）。两文件均为 LF，diff 干净。

## records 薄路由示范 · 隔离单测（已落盘并跑通）
- 坏沙箱根因确认：系统 Py3.11 装了 Flask 全家桶但**缺 flask_sqlalchemy**，`models` 无法导入 → 后端无法启动、整套 pytest（conftest import app）无法采集。故"起真后端手测"在本沙箱不可行。
- 新增 `backend/tests/test_score_record_statistics_view.py`：自带 loader（先试真实导入，失败则注入轻量 stub 按文件加载 `score_record_service`），隔离测试 `get_record_statistics_view` 三态 + 两分支：
  - 权限拦截（班级不在授权列表 → `{"error":...,"status":403}`，且 `get_score_statistics` 不被调用）
  - 日期解析失败（→ `{"error":...,"status":400,"error_code":"BAD_REQUEST"}`）
  - 正常聚合（admin 授权班级透传 / 无班级默认首个授权班级 → class_name 置首位 / 非 admin 传 allowed_classes=None）
- 运行方式：`python tests/test_score_record_statistics_view.py`（绕开 pytest/conftest，避免坏沙箱采集崩）。结果 **5/5 OK**。
- 复跑闸门：managed Py3.13 `py_compile` + system Py3.11 `pyflakes` 对三文件（含新测试）全绿。
- 仍按铁律未自动 commit。

## records 薄路由示范 · 模式推广（已收口，commit ebfd487 后继续）
- 用户"按照建议执行"= 提交示范 + 继续推广。ebfd487 已提交示范（含单测）。
- 继续把"薄路由"模式下沉 records_routes.py 其余**只读聚合**端点（跳过 RecordResource.get：其用 `ScoreRecord.query.get_or_404` 原生 404，下沉会改变契约，违反零漂移铁律，按"高风险提取宁可跳过"原则留原样）。
- 下沉 3 端点（service 新增 view + 共享 `_build_record_list_payload` 去重）：
  - `get_record_list_view(admin, user_id, rule_id, start_date, end_date, page, per_page)` ← RecordList.get（日期解析 + 隔离 + query + 响应构造）
  - `get_record_list_by_user_view(user_id, page, per_page)` ← RecordByUser.get（`can_access_student` 隔离 + query + 响应构造）
  - `get_score_entry_view(admin)` ← ScoreEntryResource.get（隔离 + get_score_entry_data；缓存留路由）
- 路由清理：删除无用 helper `_resolve_allowed_classes`；移除路由内已无用的 `get_allowed_classes`(BatchScoreEntryResource.post 仍用，已补回)/`parse_date_range`/`query_score_records`/`get_score_entry_data` 导入；service 补导入 `can_access_student`。
- 验证：managed Py3.13 py_compile + system Py3.11 pyflakes 对三文件全绿；单测扩至 **12/12**（新增 7 例覆盖三 view 的正常/隔离拒绝/非 admin 透传/日期错误分支）。
- 契约零漂移：三端点路由返回值与原实现逐字一致；`@cached_api(ttl=30)`、score_entry 缓存键值不变。
- 铁律：未自动 commit（待用户确认）。坏沙箱缺 flask_sqlalchemy，无法跑全量 pytest/run_regression.sh，以 py_compile+pyflakes+隔离单测为可达闸门。

## algorithm 薄路由 · 错误兜底统一（已收口，待用户确认 commit）
- 评估：algorithm_routes 与 records 不同构——路由层无权限隔离/日期解析编排（grep 隔离符号=0），44 只读端点几乎都是「单行 service 调用 + try/except 统一错误文案」。records 示范的"下沉隔离/日期/聚合"在 algorithm 无对应内联编排，机械建 view 是无收益搬运（拒做）。用户 AskUserQuestion 选定"错误兜底统一(推荐)"。
- 真正债：44 端点仅 3 个用 @safe_handle，41 个手写 `try/except → APIResponse.error("算法计算失败，请稍后重试")`。统一收敛到既有的 @safe_handle 装饰器（透传正常返回、维持 401/403/404 语义）才是 algorithm 真实的薄路由改进。
- 改造：62 端点（41 get + 21 post）手写 try/except 收敛为 `@safe_handle(default_status=400, message="算法计算失败，请稍后重试")`；3 个原有 @safe_handle 端点（Statistics/Cluster get/post）不动；`AlgorithmExport.get` 的 bad_request 分支非"算法计算失败"兜底，未被误改。
- 零漂移核验：`APIResponse.error` 默认 status=400 == @safe_handle default_status=400，异常兜底等价；@safe_handle 对 HTTPException re-raise（维持 401/403/404），向 Statistics/Cluster 既有标准对齐（原手写 try 吞 HTTP 异常为非标准，现统一）；正常返回透传不变。
- 执行要点（踩坑）：从 def 开始跨块正则会跨过中间 def 错位（Statistics 双 @safe_handle / ClusterByUser 缺装饰器）；改用「纯 try 块匹配删除+dedent」+「按原 src 中 def 行索引精确插入 @safe_handle」，且**先插装饰器再删 try**（删 try 只缩短 def 之后内容，不偏移已插入索引）。临时脚本已删。
- 验证：managed Py3.13 py_compile + system Py3.11 pyflakes 对 algorithm_routes 全绿；`logger.error("algorithm_routes"` 与 `except Exception as e:` 残留均清零；after @safe_handle=65(3+62)。
- 收尾补做（本次会话）：dedent 导致 59 处 3 连空行残留（HEAD 基线=0，纯本次引入）→ 按「顶层→2 空行 / 缩进→1 空行」规范化清零；文件仍为 LF、单 LF 结尾；净减 183 行（1376→1193）。
- 零漂移实证补强：`services/algorithm_service.py` grep `abort(|HTTPException|raise NotFound/Forbidden/Unauthorized/BadRequest` = **NONE** → safe_handle 的 HTTP 异常 re-raise 分支在这 62 端点实际不会触发，行为与原手写 `except Exception` **完全等价**；脚本校验 65 个 @safe_handle **全部紧贴 def**（最内层），`requires_permission`/`cached_api` 在外层 → 403 拒绝与缓存命中路径均不经过 safe_handle，语义不变。既有 `tests/test_decorators.py` 已覆盖 safe_handle 四语义（异常→信封 / 正常透传 / HTTPException re-raise / 自定义 status），无需新增测试。
- 已 commit **0138ad9**（2 files, +273/−447）。坏沙箱限制：`utils.decorators` 本身也导入失败（werkzeug `url_quote` 版本不兼容），故无法在此跑 test_decorators，仅以 py_compile+pyflakes+残留清零+既有测试背书为可达闸门。
- 三个架构债 commit 串：ebfd487（records 示范）→ cb16928（records 推广）→ 0138ad9（algorithm 兜底统一）→ 2d7307b（memory）。均未 push（待用户显式确认 + 走代理）。

## 兜底收敛推广面取证（用户选②，先出表后分批；本轮只读取证未改码）
- 产出 `docs/架构债-兜底收敛提取表.md`（26 处逐处 message/status_code/现装饰器/拟用装饰器 + 分类 + 排除项 + 零漂移实证 + 分批建议）。
- 口径修正：AST 精确识别同构 = **32 处**，其中 **Resource 类方法 26 处**（真目标）、**模块级函数 6 处排除**（`_audit_class_op`/`generate_template`/`save_context_memory`/`log_permission_action`×3 —— 非路由端点，@safe_handle 返回 API 信封语义不适用）。
- 分类：**A 类 23 处**可直接收敛（diagnostics 7 / consistency 4 / system_routes 8 / mqtt 3 / classes 1）；**B 类 3 处需拍板**：
  - B1 `dashboard_routes:27 DashboardStats.get`：message 是 `'仪表盘统计数据加载失败: %s' % e` **动态含异常细节**（无 logger）。收敛为固定文案属安全改善但改契约；不传 message 则回退 str(e) 契约保持仍泄露。
  - B2 `devices_routes:1333 DeviceExport.get`：用 `APIResponse.server_error(...)` = status 500 + **error_code="INTERNAL_ERROR"**；**@safe_handle 不支持 error_code** → 收敛必丢该字段，建议跳过。
  - B3 `nlp_routes:1334 NLPFeedbackRecord.post`：**已有 @safe_handle()（默认 500）+ 内层再套 try（返回 400）** 双兜底；删内层会使原走外层兜底的异常 500→400，建议跳过。
- 零漂移实证（脚本核查，全通过）：`services/` 全目录 grep `abort(|HTTPException|raise NotFound/Forbidden/BadRequest/Unauthorized` = **空**；26 处 try **块内** HTTP 抛点 = 0；26 处 handler 内 `rollback/commit/close` 副作用 = 0（**关键**：safe_handle 不做 rollback，有副作用则必须排除）；`APIResponse.error` 默认 `code=-1` ↔ safe_handle `getattr(e,"code",-1)` 一致。
- 已知残留风险：`safe_handle` 的 `status = getattr(e,"status_code", default_status)` —— 异常对象若自带 status_code 属性会取异常值而非 default_status（原代码固定）。当前 26 处对应 service 未见此类异常，风险低。
- 待用户确认分批：批1 diagnostics 7 → 批2 consistency 4 + mqtt 3 → 批3 system_routes 8 → 批4 classes 1；B 类 3 处待拍板。

## A 类 23 处已按批执行完毕（用户"a类按批次推进"）
- commit 串：**8341647**(批1 diagnostics 7) → **bd0a63e**(批2 consistency 4 + mqtt 3) → **9abecbe**(批3 system_routes 8) → **a704e7c**(批4 classes 1)。共 23 处，与提取表逐处一致。
- 新增通用脚本思路（临时脚本已删）：AST 自动从原 `APIResponse.error(message=, status_code=)` **提取文案与 status** 生成 `@safe_handle(default_status=, message=)`，避免手工抄错；`status_code` 缺省按 `APIResponse.error` 默认 **400** 补（classes_routes ClassStudents 即此例，是唯一非 500 的）。
- 自动跳过规则（脚本内置，实测命中）：多 handler（如 `except ImportError` 降级分支）、`APIResponse.server_error`、含 `error_code` 的、message 非常量、handler 含非日志/return 语句（副作用）。
- ⚠️ **新踩坑 1（纠正既有认知）**：从后往前删 try 时，靠前的删除会使靠后 target 的 `def_idx` 前移 → 拆成「先删后插」两个 Pass **必错位**（实测偏移 4 行，装饰器插进方法体中部）。正解：**单次从后往前循环，每个 target 内先删 try 再插装饰器**。注：algorithm 那次「先插后删」成立是因为其 Pass B 用正则匹配而非行索引 —— **Pass B 用行索引→先删后插；用正则→先插后删，不可混用**。
- ⚠️ **新踩坑 2（最隐蔽）**：`system_routes.py`(CRLF 1161/LF 1) 与 `classes_routes.py`(352/1) 是**混合 EOL**；`data.split(b"\r\n")` 会把那 1 个 LF 行与下一行**合并** → 行数变少 → 索引全线错位（症状：前两批纯 CRLF 文件都成功，该文件第一个 target 就 IndentationError）。正解：`splitlines(keepends=True)` **逐行保留各自 EOL**，插入/删除同步维护 eols 列表，新行用主导 EOL（众数），**不要顺手规范化混合 EOL**（会引入无关 diff）。
- 验收：5 文件全量 py_compile OK；pyflakes 无新增（system_routes 3 处 unused `e` 经 `git show HEAD` 比对为既有）；幂等终检剩余同构 try = **0**；EOL 原样保持。
- B 类 3 处仍待用户拍板（建议：B1 取固定文案、B2/B3 跳过）。技能 `route-try-except-to-decorator` 已同步更新两个新踩坑点。

## 兜底统一 · 全后端推广面取证（只读取证，未改码）
- 粗扫 `except Exception as X:`：全 api/ **157 处 / 39 文件**；其中 `api/backups/refactor_F17/` 备份目录 **46 处不计**（备份不改）。真实路由 ≈111 处。
- ⚠️ 但 111 处里大量是**业务性 try**（导入解析/文件 IO/MQTT 连接/事务补偿），机械收敛会破坏业务语义 → **不可盲推**。
- AST 精确识别「整方法体被单一 try 包裹 + 单一 `except Exception`」同构模式 = **32 处 / 12 文件**（这才是可推广面）：
  - system_routes 8 / diagnostics_routes 7 / consistency_routes 4 / mqtt_routes 3 / classes_routes 2 / nlp_routes 2 / dashboard·download·devices·admins·rbac·sub_accounts 各 1
- 推广前提（与 algorithm 差异）：algorithm 文案统一为"算法计算失败"且 default_status=400 与 `APIResponse.error` 默认一致；这 32 处**文案与 status_code 各异**，须逐处从原 `APIResponse.error(message=..., status_code=...)` 提取后再定 `@safe_handle(default_status=..., message=...)`，否则必然漂移。
- 结论：可推广但需逐文件核对，属中风险；待用户拍板是否推进。

## B 类 3 处收口（用户"按照建议继续推进"→ B1 收敛 / B2 跳过 / B3 收敛）
- **B1 `analytics/dashboard_routes.py` DashboardStats.get → ✅ ca07ee0**：原兜底 `'仪表盘统计数据加载失败: %s' % e` **动态拼接异常细节**直返前端（信息泄露）且**无任何日志**。收敛为 `@safe_handle(default_status=400, message="仪表盘统计数据加载失败，请稍后重试")`。⚠️ **有意契约变更（用户批准）**：message 不再含异常细节；status 保持 400 不变。保留「诚实失败：不伪装成全 0 假统计」业务注释。文件 CRLF 41/0。
- **B2 `devices/devices_routes.py:1333` DeviceExport.get → ⏸ 跳过**：用 `APIResponse.server_error(...)` → 展开含 `error_code="INTERNAL_ERROR"`，而 `safe_handle` **不支持 error_code** → 收敛必丢字段。**何时可动**：先给 `safe_handle` 加 `error_code` 透传（默认 None 向后兼容），属公共装饰器改动、影响已收敛的 60+ 处，需独立评审。另该方法体 128 行，收益/风险比不划算。
- **B3 `nlp/nlp_routes.py:1334` NLPFeedbackRecord.post → ✅ 6bfdee4（零漂移，修正了原判断）**：⚠️ 原表误判「删内层会让外层 500 变 400」。实际**相反** —— 内层 try 包住整个方法体(1339→1422)吞掉一切异常，外层 `@safe_handle()`(500) **从未生效，是死代码**。故把外层改 `@safe_handle(default_status=400, message="记录失败")`、删内层 84 行 try + dedent 即**契约零变化**（原内层 `APIResponse.error(message="记录失败")` 未传 status → 默认 400）。连带清理仅此一处使用的 `import traceback` 与函数内局部 logger 导入。
- **收官证据**：全 api/ AST 扫描「`@safe_handle` + 整方法体 `except Exception`」死代码双兜底 = **0**，B3 为最后一处。
- ⚠️ **判别经验**：遇到「已有装饰器 + 内层 try」双兜底，**先判内层 try 是否包住整个方法体** —— 若是，外层就是死代码，应把外层的 default_status/message 对齐内层原兜底（而非删内层后被动接受外层默认值）。这次据此把"建议跳过"翻转为"零漂移可收敛"。
- 文件 EOL 铁律再次生效：dashboard 纯 CRLF、nlp 混合(CRLF 1645/LF 1) —— 均用 AST 定位行号 + `splitlines(keepends=True)` 逐行保留 EOL 改写，未规范化。
- 本轮 2 commit（ca07ee0 / 6bfdee4）+ 提取表与记忆更新，随后按用户要求 push。

## 推送完成（用户批准"现在推送"）
- `git push origin main` → `526f6e3..b2d1be3 main -> main`，**共 16 个 commit**（含此前未推的 04ee9d9 / ee0e477 / 3b2d040 及 4a99461）。
- ⚠️ 教训：本地自估"12 个未推"是错的，实际 16 个 —— 必须 `git ls-remote origin refs/heads/main` 取远端真值后 `git rev-list --count <remote>..HEAD` 才知道真实差额（本地 `origin/main` ref 陈旧）。
- 推送后用 ls-remote 复核：远端 main == 本地 HEAD == `b2d1be3`，未推送剩余 0 ✅。
- 技能 `route-try-except-to-decorator` 新增「三类特殊形态判别」：① 双兜底先看内层 try 是否包住整个方法体（是→外层死代码，对齐外层即零漂移）② server_error 带 error_code 不可收敛（附"何时可动"前置条件）③ 动态文案含 %s % e 属有意契约变更须批准。

## ⭐ 后端测试环境修复（坏沙箱已破解，venv 隔离方案）
- **此前长期误判为"沙箱不可跑"**，真实根因是系统 Py3.11 **主环境损坏**：
  - `site-packages` 里 `werkzeug-2.3.7.dist-info` 与 `werkzeug-3.1.8.dist-info` **并存**，实际 `werkzeug/urls.py` 是 3.x 代码（无 `url_quote`）→ `import flask`(2.2.2) 直接 ImportError。pip 元数据仍显示 2.3.7，极具迷惑性。
  - 另缺 `SQLAlchemy` / `Flask-SQLAlchemy` / `packaging`(flasgger 依赖) / `attrs` / `certifi` / `urllib3` / `charset_normalizer` / `idna` / `greenlet` / `prometheus-client`。
- **修复方案（零污染主环境）**：`python -m venv --system-site-packages backend/.venv` —— 继承系统已装的 torch 2.11 / flask-restx / celery 等重量包，只在 venv 内补缺失项。`.gitignore` 已有 `.venv/`（无前导斜杠，匹配任意层级）。
- venv 内补装后 flask 自动升到 **2.3.3**（Flask-SQLAlchemy 3.1.1 要求 ≥2.2.5），而 Flask 2.3+ **兼容 Werkzeug 3.x** → werkzeug 反而不用动。
- 验收：`.venv/Scripts/python.exe -c "import app"` 成功（含索引初始化）；相关测试 16 passed。
- ⚠️ **pytest 输出被索引初始化日志淹没**（历史已知坑）→ 必须 `| grep -E "passed|failed|error"` 提取结果行，否则看不到结论。
- 全量命令：`.venv/Scripts/python.exe -m pytest -p no:locust --timeout=600 -q`（历史基线 2066 passed / 7 skipped / 0 failed，15–21min）。
- ⚠️ run_regression.sh 会硬编码系统 Py3.11（坏的）→ 必须用 `PYTHON_BIN=<venv python> bash scripts/run_regression.sh` 覆盖，脚本已支持该环境变量。

## ⭐ 全量回归背书完成（17 个 commit 零回归）
- **全量 pytest：2078 passed / 7 skipped / 0 failed，exit=0，19m44s**（历史基线 2066/7/0）。
  **+12 passed 正是本次新增的 12 个隔离单测** `tests/test_score_record_statistics_view.py`；skipped 不变、failed 保持 0 → 25 处兜底收敛 + 4 处薄路由下沉未破坏任何既有行为。
- **run_regression.sh：全部回归通过**（RBAC 与 OpenAPI 因 DB 未初始化/后端未起按脚本约定跳过，非失败；核心索引 24/24 齐全）。
- 背书记录：`docs/架构债-回归背书-20260907.md`。
- 关键认知更新：**此前"沙箱不可跑"的结论作废** —— 用 venv 隔离方案即可跑全量，以后改后端必须坚持跑全量 pytest，不再以 py_compile+pyflakes 代替。
