# 2026-09-11

## 续（承接 09-10 #104）前端 hooks 导入统一走 barrel — #105 ✅

用户指令「继续清理（如 useApiFetch/useStableToast/useSubmitGuard 未走 barrel 的一致性问题）」，并在选项中选定 **「补齐并统一走 barrel」**。

### 一、取证（关键：事实与预期相反）
| 项 | 数量 |
|---|---|
| barrel 导入（`from '.../hooks'`） | 44 处 |
| **直接路径导入（`from '.../hooks/useXxx'`）** | **117 处** |
| barrel 缺失导致被迫直接路径 | useStableToast 44 · useSubmitGuard 27 · fetchJson 4 · useMemoryUsage 1 |
| barrel 已有却仍直接路径（真不一致） | useWorkbenchClass 16 · usePermissions 10 · useAutoSave 5 · useListFetch 3 · 其余 8 |
| 同文件混用两种风格 | 28 个文件 |
- **根因**：`hooks/index.ts` 不完整（缺 4 个模块），开发者想用 barrel 也没有 → 只能写直接路径，久而久之直接路径成多数派。
- **额外缺口**：`useForm.ts` 的 4 个类型（`ValidationRule`/`ValidationRules`/`FormErrors`/`UseFormResult`）也**未导出**，有 6 处 `import type { FormErrors|UseFormResult } from '../../hooks/useForm'`。
- 安全性前置：无 default import 消费者；hooks 目录内无 import barrel（**无循环依赖**）；eslint 未启用 `import/no-duplicates`（仅 `import/first`，不受影响）。

### 二、动作
1. **补齐 barrel**：`hooks/index.ts` 31 → 37 行，新增 `fetchJson`/`useApiFetch`/`type ApiResult`、`useStableToast`、`useSubmitGuard`、`useMemoryUsage`、`useForm` 的 4 个类型。**现覆盖全部 20 个 hook 模块**。
2. **统一导入（脚本化，80 文件 / 176 条语句）**：把 `from '.../hooks/useXxx'` 合并为 `from '.../hooks'`；**同文件已有的 barrel import 一并纳入合并**（否则会产生同源多行 import）。
3. **3 处动态 import**：`await import('../../hooks/useAppState')` → `import('../../hooks')`（NLPManagement/RemoteNotify/UserList 三个冒烟测试）。
4. **10 个死 default export 清理**：useAutoSave/useClassNowStatus/useClientFilter/useKeyboardShortcut/useListData/useListFetch/usePermissions/useTableUrlState/useUndoRedo/useWorkbenchClass 的 `export default useXxx;` 全部**零 default 消费者** → 删除（与 #104 删 useShallowCompare/useDebouncedValue 的 default 聚合对象一致）。

### 三、闸门（三要素全过，零回归）
| 项 | 结果 |
|---|---|
| prettier --check | All matched files use Prettier code style! |
| tsc --noEmit | EXIT=0 |
| eslint src（含 js/jsx） | **EXIT=0（0 errors / 0 warnings）** |
| 全量 vitest | **VITEST_EXIT=0**；37 passed/1 skipped(38)；Tests **276 passed**/3 skipped；**0 failed**（与 #104 持平）|
| 三要素③ 文件数 | 磁盘 38 == 报告 38，差集空 |
| EOL（389 源文件） | 全 LF，0 CRLF/0 MIX |
| 幂等复检 | 剩余 direct 静态 import=0 / 动态 import=0 / hooks 内 default=0 |

### 四、坑与结论
- ⚠️ **首版转换脚本漏了"已有 barrel import"**：只匹配 direct 路径 → `CategoryList.tsx` 会生成 3 条同源 `from '../hooks'`。**修正：`is_hook_import` 必须同时匹配 `.../hooks` 结尾与 `.../hooks/use*`**，把已有 barrel 纳入合并。→ **脚本必须先 dry-run 预览再写盘**。
- ⚠️ **动态 `import()` 用 `grep "from '"` 查不到**，必须单独扫 `import\(`。否则 barrel 统一不彻底。
- ✅ **`vi.mock('../../hooks/usePermissions')` 经 barrel re-export 仍生效**：vitest mock 是模块级替换，barrel 的 `export ... from './usePermissions'` 本质也是导入该模块 → 被拦截。PermissionGuard.test.tsx **10 tests ✓** 实测通过。→ 改走 barrel **不需要**同步改 vi.mock 路径（改成 mock 整个 barrel 反而危险：只提供部分导出会导致其他导出 undefined）。
- 📌 **hooks 导入规范（本批确立）**：统一从 `'.../hooks'`（barrel）导入；**新增 hook 必须同步在 `hooks/index.ts` 导出**（含其类型），否则又会退化成直接路径。

### 铁律①
#90–#105 全部改动仍未 commit/push，待用户显式指示。

## 续（#106）跨目录 barrel 一致性审计 — components/ utils/ services/ ✅（仅审计，未改动）

用户指令「检查其他目录（components/、utils/、services/）是否有同类 barrel 不一致」。

### 一、结论速览
| 目录 | barrel | 导入风格 | 是否有"同类不一致" |
|---|---|---|---|
| `components/` | 有（根 81 符号 + 9 子 barrel） | **barrel 98 vs 直接路径 109** | **★ 有，与 hooks 完全同型** |
| `utils/` | **无** | 72 处全直接路径 | 无（天然一致，但无统一入口）|
| `services/` | **无** | 53 处全直接路径 | 无（同上）|
| `context/` | **无** | 7 处全直接路径 | 无（同上）|
| `stores/` `types/` | 有 | 7 / 79 全走 barrel | 无（一致）|

### 二、components/ 109 处直接路径明细（按模块）
| 模块 | 处数 | 符号 | 是否已在根 barrel |
|---|---|---|---|
| `data-display/DataTable` | 47 | **全为 `import type { ColumnType }`/`SortOrder`** | ✅ 已在 |
| `ui/ConfirmDialog` | 34 | `useConfirm` / `ConfirmProvider` | ✅ 已在 |
| `form/EntitySelect` | 18 | `ClassSelect`/`StudentSelect`/`SubjectSelect` | ❌ **缺失** |
| `form/ToggleSwitch` | 8 | `ToggleSwitch` | ❌ **缺失** |
| `ui/Pagination` | 1 | `Pagination` | ✅ 已在 |
| `PermissionGuard`（depth1） | 1 | `PermissionGuard` | ✅ 已在 |
→ **可直接改 83 处；需先补 barrel 26 处**（4 个符号）。

**根因同 hooks**：根 barrel 不完整 —— `components/form/`（无子 barrel）的 4 个导出、`workbench/WorkbenchBreadcrumb` 均未进根 barrel。

### 三、components/ 附带发现（3 类）
1. **6 个子 barrel 零消费者**：`ui`/`data-display`/`feedback`/`skeletons`/`special`/`charts`（`charts/index.tsx`）——无人 import；仅 `layout`/`image`/`lazy` 被根 barrel 以 `from './layout'` 形式使用。→ 子 barrel 层属**半死资产**（且根 barrel 混用"逐文件"与"子 barrel"两种 re-export 风格）。
2. **orphan 组件（有真实消费者但未进 barrel）**：`workbench/WorkbenchBreadcrumb`（**13 消费者**）、`form/EntitySelect`（19）、`form/ToggleSwitch`（8）。→ 与 #105 hooks 情形一致：缺导出 → 被迫直接路径。
3. **疑似死组件**（待确认）：`data-display/SearchInput.tsx`（仅被无人使用的 `data-display/index.ts` 引用）、`feedback/NetworkStatus.tsx`（0 引用；真身在 `special/GlobalStateComponents.tsx`）、`skeletons/UserListSkeleton.tsx`（仅被无人使用的 `skeletons/index.ts` 引用）。
4. **`special/GlobalStateComponents.d.ts` 与 `GlobalStateComponents.tsx` 并存**——旧遗留声明（注释"为 JSX 文件提供类型支持"），现被 `.tsx` 遮蔽，属冗余。

### 四、安全性前置（为后续执行备好）
- ✅ **无循环依赖**：components 子树内 0 处 import `components` barrel。
- ✅ **无模块级副作用**：components 全目录无顶层 `window/document/localStorage` 调用（DevTools 的门控在组件体内）。
- ⚠️ 传递依赖：components barrel → `hooks`(7) / `stores`(2)；import barrel 会连带加载 hooks 链（vitest 无 tree-shaking）。但已有 98 处走 barrel 且全量 vitest 绿，风险已实证可控。
- ✅ 全部相关文件 git 已跟踪。

### 铁律①
#90–#106 未 commit/push。本 #106 仅审计，未产生代码改动。

## 续（#106 执行）components 双层 barrel 统一 + 死组件清理 ✅

审计后经 AskUserQuestion，用户选定：① components barrel 不一致 → **「补齐 + 统一」**；② 附带 4 类问题 → **「附带问题全处理」**。已全部执行完毕，零回归。

### 一、动作清单
1. **补 5 个子 barrel**：
   - `ui/index.ts`：+`AdvancedSearch`、`ConfirmProvider/useConfirm` 及类型 `ConfirmOptions/ConfirmType/FilterField/AdvancedSearchProps/BatchAction/StatusTone/StatusBadgeEntry`。
   - `special/index.ts`：+`ClassStatusBadge`(+Props)、`ErrorBoundary` 族（ErrorBoundaryFallback/ErrorBoundaryWrapper）。
   - `feedback/index.ts`：**修正别名错误** —— 原 `export { default as SearchEmptyState } from './EmptyState'` 实际指向 EmptyState 组件；改为 `export { default as EmptyState, ErrorState, SearchEmptyState } from './EmptyState'`（该文件默认导出 EmptyState、命名导出 SearchEmptyState/ErrorState）。
   - `data-display/index.ts`：删除 `SearchInput`（死组件）。
2. **新建 2 个子 barrel**：`form/index.ts`（ClassSelect/StudentSelect/SubjectSelect/useClassOptions/EntityOption/ToggleSwitch）、`workbench/index.ts`（CurrentClassLabel/WorkbenchBreadcrumb）。
3. **根 barrel 重写** `components/index.ts`：由混用「逐文件 re-export + 子 barrel re-export」改为**统一从 9 个子 barrel 聚合**（ui/data-display/feedback/form/layout/image/lazy/special/workbench），保留 MemoComponents/PermissionGuard/PreloadProvider 顶层散文件。**charts 例外**：因依赖 recharts（重依赖），保留独立子入口不并入根 barrel，注释说明。
4. **96 个消费文件导入统一**：`from '.../components/<子目录>/<文件>'` → `from '.../components'`（189 条语句合并为 96 条）。
5. **删 6 个死文件/冗余**：`data-display/SearchInput.tsx`、`feedback/NetworkStatus.tsx`（真身在 special/GlobalStateComponents.tsx）、`special/GlobalStateComponents.d.ts`（被 .tsx 遮蔽）、`skeletons/` 整目录（DashboardSkeleton/UserListSkeleton/index.ts）。
6. **手动修正 5 处导入**（脚本路径 bug）：ApprovalsView/OpsCenterView 的 `formatDateTime` 路径 `./utils/format`→`../../utils/format`；useApprovalsLogic 的 `api` 是 **default export** → `import api from '../../services/api'`；AttendanceTableCard 的 `Attendance` 类型路径；HomeworkCheckView 的 `Pagination` 实为 **antd 组件**（ui/Pagination API 是 currentPage/totalPages，与用法 current/total/pageSize 不符）→ 改 `from 'antd'`。

### 二、导出符号集比对（防丢失）
HEAD 根 barrel 81 符号 → 当前 **93**，**零丢失**，新增 12（ClassSelect/StudentSelect/SubjectSelect/ToggleSwitch/WorkbenchBreadcrumb + 6 类型）。

### 三、⚠️ 事故与恢复（本批最严重）
- **转换脚本误删代码**：脚本用 `out.append(raw[:first.start()]); ...; out.append(raw[last:])`，把两条匹配 import 之间（含非 components 的 services/hooks/utils/context 导入）**整段删除** → tsc 爆 **70 处 TS2304/TS2552** 未定义名（12 文件：App.tsx、useApprovalsLogic.tsx、useExamManagementLogic.tsx 等）。
- **恢复 SOP（已固化进 skill）**：① **不以 `git checkout` 救**（会丢其它未提交改动）；② **以 tsc 报错名为权威判据**（HEAD 导入集差集会因 HEAD 早于重构产生 51 文件误报）；③ 全仓导出索引回退；④ HEAD 版本 import 映射优先；⑤ 桶归一化（hooks/components → barrel）；⑥ 修复 12 文件，残留 6 处手动 Edit。
- 复验：tsc EXIT=0；横向体检 import 路径 0 不可解析；副作用导入 `import 'x'` 0 丢失；13 项「正文仍引用但导入消失」经核实全为误报（字符串字面量/注释）。

### 四、闸门（全过，零回归）
| 项 | 结果 |
|---|---|
| prettier --check | All matched files use Prettier code style! |
| tsc --noEmit | EXIT=0 |
| eslint src | **EXIT=0（0 problems）** |
| 全量 vitest | **VITEST_EXIT=0**；37 passed/1 skipped(38)；Tests **276 passed**/3 skipped；**0 failed**（与 #104/#105 持平）|
| 三要素③ 文件数 | 磁盘 38 == 报告 38，差集空 |
| EOL（385 源文件） | 全 LF，0 CRLF/0 MIX |

### 五、结论
- utils/services/context **无 barrel** → 属「天然一致」，不强行引入（无统一入口但零不一致）。
- components 现为**双层 barrel**（根 → 9 子 barrel），与 hooks 单层 barrel 同属「消费者一律走 barrel」规范。
- 📌 **新增 components 子目录必须建 `index.ts` 并在根 barrel 同步聚合**；**依赖重的子目录（如 charts/recharts）保留独立子入口**，不进根 barrel。

### 铁律①
#90–#106 全部改动（16 D / 326 M / 15 R / 9 RM / 70 ??）仍未 commit/push，待用户显式指示。

## 续（#107）主线盘点：除班主任工作台外还有哪些开放主线 —— 只读核实

用户问「除了班主任工作台交互增强，是不是还有其他主线」。逐项取证（未改任何代码）：

### 一、开放主线（3 条 + 1 项阻塞决策）
| 主线 | 依据 | 现状（本轮实测） |
|---|---|---|
| **班主任工作台**（≠仅交互） | `docs/班主任工作台优化方案-待审核.md`（2026-08-29，待审） | **P0 权限词根错配仍未修**：5 页 /study-groups /mental-health /activity /culture /study-guide 前端仍用 `class.view` 门控（App.tsx 实测），后端要求专属词根 → 页面「假可用」。**P1 写路径班级隔离缺失仍未修**：`get_admin_class_ids` 在 `api/class_management/**` **零调用**（12 处调用全在 export/devices/report）。⛔ 两项均为安全级 |
| **班主任三页拆分** | `docs/班主任页拆分方案-铁律③待审.md` | ClassManagement(1438)/AttendanceManage(1062)/StudyGuide(1110) 待用户逐页确认 |
| **前端类型闸门 + 测试覆盖** | `docs/下一步开发计划-20260824.md`（标 🔴） | `tsconfig.json:12 "strict": false` **仍在**；E1 `any` 已随 D2/E6a 清零（实测 0 处）→ 具备开 `noImplicitAny` 前提；前端测试 38 文件，关键业务逻辑覆盖仍薄 |
| **M9 分页缺口** | `docs/M9分页复核-缺口清单.md` | **P0 已闭环**（实测 `/api/wol/devices`、`/api/devices/online` 均带 "M9 P0" 注释 + `paginate`）；**P1 抽查 attendance_service.list_attendance 已支持 page/per_page** → 基本可判收口，仅需正式复核 |

### 二、已收口（不再排期）
F17 路由服务化（写路径已关闭，「106 处」为过时估计）· E 系列 E1–E6 全清 · B 系 B007+B904 全清 · NLP 四塔 · 后端机械优化（UP031/UP035/SIM102/RUF100/RET505/F401/F841）。

### 三、⚠️ 重要纠正：铁律① 的真实状态
用 `git ls-remote`（权威）核实：**远端 `refs/heads/main` = 本地 HEAD = `2db39b4`** → **所有已提交内容均已推送**（此前记忆「2 个本地 commit 未 push」已不成立）。
真实问题是：**#90–#107 全部改动（436 项：16 D / 326 M / 15 R / 9 RM / 70 ??）仍堆在工作区，从未形成 commit**。这才是需要用户决策的阻塞项。

## 续（#108）工作区积压 → 提交切分方案 ✅（只读产出，未执行 commit）

用户选定「先解决主线 4（工作区积压）」。已产出 `docs/提交切分方案-20260911.md`。

### 一、关键结论：批次级切分不可行
- 无中间快照；后续批次反复改写同批文件（#106 统一了 96 个消费文件导入，而这些文件已被 #102 格式化 / #103–#105 hook 清理动过）→ **无法按文件边界还原 #90…#107 的批次归属**。hunk 级 `git add -p` 理论可行但 534 条 / 数千 hunk，实践不建议。
- **采用按路径前缀的领域/分层切分**（文件互斥、`git add <路径>` 可精确落地）。

### 二、切分结果（`git status --porcelain -uall` = 534 条）
| # | 提交信息 | 项数 |
|---|---|---|
| 1 | `chore(repo)`: ops/deploy 折叠入 ops/infra + 路径引用与忽略规则同步 | 29（24 重命名 + 5 引用同步） |
| 2 | `refactor(backend)`: F17 薄路由收尾 + 服务下沉 + 路由/服务层清理 | 108（api41+services60+models1+app4+startup2） |
| 3 | `refactor(backend)`: ruff 机械优化与工具层死代码清理 | 80（utils28+scripts32+tasks4+middleware4+tools3+config3+tests5+run1） |
| 4 | `refactor(frontend)`: barrel 统一 + 死代码清理（#104–#106） | 77（components30+hooks24+tests11+utils4+services4+context2+App+test-setup） |
| 5 | `refactor(frontend)`: 页面拆分与逻辑层瘦身（D2/E6a） | 202（pages/**） |
| 6 | `docs`: 代码债重扫清单 + 班主任页拆分方案 | 3 |
| 7 | `docs(memory)`: 工作记忆更新（#90–#107） | 5 |
| — | 合计 1–7 | **504** |

**不入库 27 项**（提交 8，仅清理）：0 字节 stray `600` / `上一轮（2026-09-08）D1–D7`、`apps/frontend/_tsc*.txt`/`_bash_marker.txt`、`tools/tmp/`（24 个 split_*/fidelity 脚本 + bak/*.tsx）、`.trae/documents/*.md`。
→ `tools/tmp/` 建议加 `.gitignore`（与提交 1 的 .gitignore 同文件，须并入提交 1，除非 `git add -p` 分拣）。

### 三、执行顺序与待决策
- 顺序 **1 → 2 → 3 → 4 → 5 → 6 → 7 → 8清理**（不可颠倒：提交 1 必须先取走 `docs/` 两个指南 + `.gitignore`，否则提交 6 的 `git add -A docs/` 会卷进路径同步改动）。
- 提交前补跑：`tsc --noEmit`（提交 4 前，防 #106 事故残留）+ eslint（含 js/jsx）；py_compile 全量。全量 vitest/pytest 已在 #104–#106 绿。
- 待用户拍板 3 点：① 后端拆 2 个还是合并 1 个；② `tools/tmp/` 删还是忽略；③ `.trae/` 随提交还是忽略。
- 回滚：`git reset --soft HEAD~1`（保留改动）或 `--soft <2db39b4>`；**全程不 push**。

### 铁律①
仍未执行任何 commit；方案确认后按表执行，且**不 push**。

## 续（#108 执行）提交切分落地 —— 7 个 commit 全部落地 ✅

用户指令「按顺序执行，同时 push」。已按方案执行，**并 push**（首次打破"只提交不推送"）。

### 一、落地结果
| # | commit | 内容 | 文件数 |
|---|---|---|---|
| 1 | `b1fe22d` | chore(repo): ops/deploy → ops/infra + 路径引用与忽略规则 | 30（24 重命名 + 6 引用同步） |
| 2 | `2d67852` | refactor(backend): F17 薄路由收尾 + 服务下沉（新增 operation_log_service.py） | 108 |
| 3 | `c387c76` | refactor(backend): ruff 机械优化与工具层死代码清理 | 80 |
| 4 | `e9272df` | refactor(frontend): barrel 统一 + 死代码清理（#104–#106） | 77（16 删除 + 3 新增 + 58 改） |
| 5 | `e17afc4` | refactor(frontend): 页面拆分与逻辑层瘦身（D2/E6a） | 202（128 新增 + 74 改） |
| 6 | `09277c7` | docs: 代码债清单 + 班主任拆分方案 + 提交切分方案 | 4 |
| 7 | （本提交） | docs(memory): 工作记忆更新（#90–#107） | 5 |

### 二、⚠️ 执行中发现的重要事实
- **仓库事前已有「预暂存」内容**：`ops/deploy → ops/infra` 的 24 条重命名在本次操作前就已被 `git add`（`git status` 首列即为 `R`/`RM`），而其中 9 个文件的**内容修改未暂存**（docker-compose context 路径 `context: ../..`、check_deploy.py 等）。→ 首次 commit 后残留 9 项，用 `git commit --amend --no-edit` 补入（未 push 时安全）。**教训：接管脏工作区前先 `git diff --cached` 看暂存区，别只信 `git status` 的未暂存视图。**
- **`git add -A <不存在的路径>` 会整体报错退出（exit 128）**，但**已处理的路径仍会留在暂存区**（本次 `git add -A ops/ docker-compose.yml …` 因根 docker-compose.yml 已被移走而报 pathspec 错误，`ops/` 部分仍生效）。→ 批量 `git add` 应只传**现存路径**；被移走的根文件其删除已由 `git add -A ops/` 连带暂存并被识别为 R100 重命名。
- **`.gitignore` 一旦加入 `tools/tmp/`**，`git status -uall` 立即少 24 条 → 计数基线会变（本次核账用「分桶重扫」而非固定数字）。
- 提交前闸门已跑：`tsc --noEmit` EXIT=0 + `eslint src --ext .ts,.tsx,.js,.jsx` EXIT=0。
- **无 git hooks**（`.git/hooks` 只有 sample）→ 提交不会自动触发 G2/G5/run_validation，须自行把关。

### 三、清理（提交 8）
删除 2 个 0 字节 stray 文件 `600`、`上一轮（2026-09-08）D1–D7`；`tools/tmp/`（24）+ `_tsc*.txt`（3）已由 `.gitignore` 覆盖。

### 铁律①（新态）
**已 push**。#90–#107 积压已全部落地为 7 个 commit 并推送，工作区归零。

## 续（#108 复盘）主线 1/3 实测复核 —— ⚠️ 三份"待审"文档全部陈旧，均已闭环

push 后按「然后按顺序继续推进」进入主线 1，**逐项实测后发现文档结论与事实相反**：

### 一、⚠️⚠️ 重大纠正：班主任工作台 P0/P1/P2 + 三页拆分 **全部已闭环**
| 条目 | 文档（2026-08-29）原述 | 实测（2026-09-11） | 硬证据 |
|---|---|---|---|
| **P0 权限词根错配** | 5 页 `class.view` 门控 → "假可用" | ✅ **已修** | `App.tsx`：study-groups→`study_group.view`、mental-health→`mental_health.view`、activity→`activity.view`、culture→`culture.view`、study-guide→`study_guide.view`；seating/duty/committee/parent 保持 `class.view`（与后端一致） |
| **P1 写路径越权** | class_management 零调用 `get_admin_class_ids` | ✅ **已修** | `utils/permission.py:212` `requires_permission` 内置 `_check_class_scope(permission)`，对 `_CLASS_SCOPE_PREFIXES`（12 个班级管理词根）自动 `ensure_class_access`/`ensure_student_access`，越权 403。**不需逐路由加装饰器**——挂在 `requires_permission` 上全局生效 |
| **P1 隔离冒烟** | — | ✅ **12 passed** | `tests/test_workbench_isolation_smoke.py`（**必须用 `apps/backend/.venv/Scripts/python.exe`**；系统 Py3.11 会假报 werkzeug `url_quote` ImportError） |
| **P2 api.ts 双风格** | 形参 camelCase | ✅ **已统一** | `services/api.ts` 形参 `classId|studentId` = 0 |
| **三页拆分（铁律③待审）** | ClassManagement/AttendanceManage/StudyGuide 待逐页确认 | ✅ **已拆完** | 1438→**691**、1062→**512**、1110→**441**；各建 `pages/<name>/`（View + 5–6 子组件 + types） |
| **M9 P0 无界 dump** | 2 端点全表 dump | ✅ **已闭环** | `wol_routes.py:345`、`devices_routes.py:405` 均 `get_pagination(default=50)` + 分页切片，注释标 `M9 P0` |
| **M9 P1 子资源** | 13 端点 service 内 `.all()` | ✅ **已闭环** | 抽查 8 个 service（mental_health/parent/duty/homework/culture/study_guide/activity/teacher_comment）list 方法均含 page/per_page；全仓 `get_pagination` 57 文件级使用点 |

→ 已给三份文档加**顶部状态横幅**（`班主任工作台优化方案-待审核.md`、`班主任页拆分方案-铁律③待审.md`、`M9分页复核-缺口清单.md`），标明"已闭环，勿再按待办引用"。

### 二、★ 教训（写入长期记忆）
**引用任何历史审计文档前必须先做一次实测复核**。本次因直接采信 `docs/` 快照 + `grep class.view` 的**行号**（未核对行号所属路由），得出"P0 仍未修"的错误结论并在报告中输出。**正确姿势：grep 必须带上下文（`-A3` 看 `path=`），并核对后端 `requires_permission` 实际词根**。文档生成日期 ≠ 当前状态。

### 三、主线 2 可行性已量化（唯一真正的开放项）
`tsc --noEmit --noImplicitAny` 仅新增 **38 个错误 / 15 文件**（`strict:false` 仍在 → 具备开关前提，E1 `any` 已清零）：
- TS7011×25（索引签名读限制）、TS7018×5、TS7010×2、其余 TS7053/7016/7015/7008/7006/7005 各 1
- 分布：`tests/components/ScoreChart.test.tsx`(6)、`services/__tests__/api.test.ts`(4)、`pages/dashboard/useDashboardLogic.ts`(4)、`test-setup.ts`(3)、`stores/index.ts`(3)、`services/performanceReportingService.ts`(3)、`pages/analysis/useAnalysisLogic.ts`(3)、`pages/ScoreAnalysis.tsx`(3)、`pages/permissionManagement/helpers.tsx`(2)、`pages/courseSchedule/useCourseScheduleLogic.tsx`(2)、及 5 个文件各 1
→ **规模可控，属真正可执行的下一步。**

### 铁律①（终态）
7 个 commit 已 push；本轮追加的文档状态标注 + 记忆更新随小提交推送，工作区再次归零。

## 续（#109）主线 2 收口 —— ✅ 前端 `noImplicitAny` 已开启，38 个错误全清

### 一、执行动作（15 文件 / 18 改动）
按上一轮量化的 38 个错误逐文件清掉，并把 `noImplicitAny: true` 正式写入 `apps/frontend/tsconfig.json`（`strict` 仍 false）：

| 错误族 | 文件 | 修法 |
|---|---|---|
| TS7011×~25（`.catch(() => null/[])` 缺返回类型） | useAnalysisLogic / useDashboardLogic / ScoreAnalysis / performanceReportingService / courseSchedule / api.test / ScoreChart.test | 补显式返回类型：`.catch((): null => null)`、`.catch((): Subject[] => [])`、`headers.get: (): string \| null => null` 等 |
| TS7016×1（`react-dom/client` 无类型声明） | src/index.tsx | **补装 `@types/react-dom@^18.3.7` 为显式 devDependency**（此前完全缺失，仅因 `noImplicitAny:false` 而静默） |
| TS7018×3（store 初始字面量无上下文类型） | stores/index.ts | 给 `persist` 初始化器加显式返回类型 `(set, get): PermissionState =>` |
| TS7018×1（`.map` 回调返回对象字面量） | nlp-management/useNLPManagementLogic.ts | 加 `(s): Rule & { rule_id: number } =>` 标注 |
| TS7010/7018/7008×3（jsdom stub） | test-setup.ts | 箭头补 `: MediaQueryList`；`root: Element \| null = null`；`takeRecords(): IntersectionObserverEntry[]` |
| TS7053×1（`distribution[info.level]`） | deviceManagement/helpers.tsx | 新增 `SignalLevel = 'excellent'\|'good'\|'fair'\|'poor'` 联合类型（零行为变更） |
| TS7015+TS7006×2（索引表达式/参数隐式 any） | permissionManagement/helpers.tsx | `Record<number,...>` → **`Record<ID,...>`**（`Admin.id: ID = number\|string`，原签名本就错） |
| TS7005×1（`const persistentCache = null`） | services/api.ts | 补类型 `{ data: unknown } \| null` |

### 二、两个根因型发现（已写入长期记忆）
1. ⚠️ **`.map()` 回调返回对象字面量时 tsc 上下文类型可能不生效**：`const mapped: Rule[] = arr.map((s) => ({...}))` 下，空数组属性被推断成 `any[]`（TS7018），**且多余属性完全查不出来**。加显式返回标注后同时暴露了 `rule_id` 这个 `Rule` 未声明、全仓无读取的多余属性（零行为变更处理：标注为 `Rule & { rule_id: number }`）。
2. ⚠️ **Edit 工具同文件并行编辑会静默丢写**：本轮 5 处编辑（courseSchedule 2 处 / dashboard 1 处 / perf 1 处 / permMgmt import / test-setup class）工具均报 success 但未落盘，复跑 tsc 才暴露。→ **同文件多次编辑必须串行 + 改后回读核验**。

### 三、附带修复：`package-lock.json` 与 `package.json` 脱节
`npm install` 顺带把 lockfile 对齐到 package.json，产生 2129 行删除。**核实为真实修复**：HEAD 的 `package.json` 早已不含 `web-vitals` / `eslint-config-react-app`（T10 eslint 清理时移除），但 lockfile 仍残留 → `npm ci` 本会失败。现 lockfile 与 package.json 一致。

### 四、闸门证据（全绿）
| 闸门 | 结果 |
|---|---|
| `tsc --noEmit`（读 tsconfig，已含 noImplicitAny） | **EXIT=0** |
| `tsc --noEmit --noImplicitAny`（改前 38 → 改后） | **0 errors** |
| prettier --check | All matched files use Prettier code style! |
| eslint（.ts/.tsx/.js/.jsx） | **EXIT=0（0 problems）** |
| 全量 vitest | **EXIT=0**；37 passed / 1 skipped(38)；Tests **276 passed** / 3 skipped；0 failed（与基线持平） |
| 三要素③ | 磁盘 **38 == 报告 38**，差集空 |
| EOL（17 个改动文件） | 全 LF，0 CRLF |
| vite build | ⚠️ 默认 outDir 被**沙箱 `safe-delete` 守卫**拦截（清 `dist/assets` 272 文件 > 阈值 50），非代码问题；改独立 outDir 复跑见下 |

| vite build（独立 outDir） | **EXIT=0**（默认 outDir 那次是被沙箱 `safe-delete` 守卫拦的，非代码问题） |

### 铁律①
#109 改动已 commit（`0a2b3f7`）+ push。

---

## #110 · 主线 2 第二格：开启 `strictNullChecks`（42 → 0）

### 一、量化（关键判断纠正）
`tsc --noEmit --strictNullChecks` 实测 **42 错误 / 20 文件** —— 与 `noImplicitAny`（38）**同量级**，而非上一轮我保守估计的「高一个量级」。
原因：第一格已把 `any` 清零，而 `any` 正是 null 检查的最大噪声源。**教训：代价必须 CLI 实测，不要凭感觉估。**
分布：TS2322×17 / TS2345×16 / TS18048×6 / TS18047×2 / TS2339×1。

### 二、★ 核心方法：先修「类型声明层」，派生错误成批消失
**19 处声明修改 → 直接消掉 25 个派生错误（42 → 17）**，剩余 17 处再逐个处理。

| 声明改动 | 消掉的派生错误 |
|---|---|
| `useClientFilter` 的 `predicate: (item: T) => boolean` → **`=> unknown`**（与 `Array.filter` 真实签名一致，比在 5 个调用点各加 `!!` 更根本） | 5 处 `boolean \| undefined`（ActivityManage / CultureBoard / HomeworkCheck / MentalHealth / TeacherComments） |
| `Activity/Culture/StudyGuideCreateInput.class_id: number` → **`class_id?: number`**（编辑态本就不传） | 3 处 |
| `RuleRecommendResult.rule_id: number` → **`number \| null`**（构造处写 `?? null`） | api.ts 4 处 |
| `newDeviceErrors` / `errors: Record<string,string>` → **`FormErrors<T>`**（hooks barrel 已导出，别手抄） | 2 处 |
| **删本地 `NlpCorrection` 重复定义 → `export type NlpCorrection = NLPCorrection`**（可选性 `?string` vs `string\|null` 不一致是根因） | 4 处（含 CorrectionsList 3 处：id 变必填后不再是 `number\|undefined`） |
| `EntryItem.permission?: string` → **`permission: string`**（两数组各项均显式声明） | 2 处 |
| `updateSeat(..., student_id: number)` → **`number \| null`**（可清空座位） | SeatingChart 2 处 |
| `handleFormChange(..., boolean\|null)` → 补 **`\| undefined`** | 1 处 |
| `NlpAnalysisData.components?: unknown[]` → **`Record<string, PerformanceRowStats>`** | AnalysisTab 1 处 |
| `WorkbenchOverviewViewProps.pendingHomework/unresolvedAlerts: number` → **`number \| null`** | 2 处 |

### 三、剩余 17 处的修法与原则
**14 处用类型断言 / 写法修正保持零行为变更**：
- `(s - avg) as number` → **`s - (avg as number)`**：断言必须包住「真正可能为 null 的那个变量」，而非整个表达式。
- `if (existing?.id)` 内用 `existing.id` → **提升为局部常量 `existingId`**（truthy 收窄即 number）。⚠️ 绝不能改成 `!= null` —— `id === 0` 会走错分支 = **真行为变更**。
- `{ score }`（`number\|null`）→ `{ score: score as number }`（与同函数 create 分支写法对齐）。注：该处原报错在 `score` 而非 `existing.id`，靠列号随前文长度变化判定（55→54）定位。
- `benchmarkResults.avg_accuracy` / `r.score_change` / `data.message` → `as number/string`（**保持原 NaN / 空值行为，不顺手"修正"显示值** —— 改显示值是产品决策）。
- api 层 `data: unknown` → 在调用点断言 `as NlpAnalysisData / NlpSuggestion[] / NlpOptimizerConfig`（不让 api 层反向依赖 pages 类型）。
- `onChange={() => handleToggleActive(state.editingUser)}` → `state.editingUser && handleToggleActive(...)`（箭头函数内收窄丢失；渲染期必非 null，等价）。

**1 处唯一必要运行时防御（已在提交信息逐条登记）**：
- `suggestion.suggestions.map(...)` 而 `suggestions?: string[]` → **`(suggestion.suggestions ?? [])`**。原代码字段缺失时**运行时崩溃**，改后渲染空态 —— 本批唯一改变运行时路径的点。

**1 处死代码删除（零行为变更）**：
- `const persistentCache: { data: unknown } | null = null; if (persistentCache) {...}` → 报 `Property 'data' does not exist on type 'never'`。原因是 const 只可能为 null → truthy 分支收窄成 `never` = **运行期不可达**。确认 if 体内仅 `logger.log`（isDev 下）+ `return`，删后行为完全一致。

**1 处 JSX 收窄修正（零行为变更）**：
- `{A?.list?.length > 0 && (<div>{A.list.map(...)}</div>)}` → **`{A?.list && A.list.length > 0 && (...)}`**：可选链不是对 `A` 的直接判空，会阻断后续收窄；undefined 同样短路为 false，等价。

### 四、⚠️ 本轮踩到的新工具坑（已写入 skill）
**`&&` 链断导致「假绿」**：`python _fix.py && cd ... && tsc | grep -c 'error TS'` —— 脚本 `sys.exit(fail=1)` 使 `&&` 后续**全部跳过**，但末尾 `;` 分隔的 `echo "REMAIN=$(tsc ...)"` 仍执行，读出 **「REMAIN=0」的假象**（实际还有 2 处）。
→ 对策：校验段一律用 `;` 串联；**任何「突然全绿」都必须复核原始输出**（`head -20` 看全量 + 打印 `PY_EXIT`）。

**同文件多处替换改用 Python 脚本批处理**：`EDITS=[(rel, old, new, expected_count)]` + 断言命中次数 + 断言替换前后 `\r\n` 计数不变。本批 3 个脚本共 32 处替换一次跑完，规避了 #109 的「同文件并行 Edit 静默丢写」。

### 五、闸门证据（全绿）
| 闸门 | 结果 |
|---|---|
| `tsc --noEmit --strictNullChecks` | **42 → 0** |
| `tsc --noEmit`（读 tsconfig，已含 strictNullChecks） | **EXIT=0** |
| prettier --check | All matched files use Prettier code style!（AnalysisTab 折行先 `--write` 再 `--check`） |
| eslint（.ts/.tsx/.js/.jsx） | **EXIT=0（0 problems）** |
| EOL（src 全量 372 个 ts/tsx） | **全 LF，0 CRLF** |
| 改动规模 | 18 文件 / +60 −46 |
| 全量 vitest | **EXIT=0**；37 passed / 1 skipped(38)；Tests **276 passed** / 3 skipped；**0 failed**（与 #109 基线持平） |
| 三要素③ | 磁盘 **38 == 报告 38**，差集空 |

### 六、确立的规范
- tsconfig 现为 `strict:false` + **`noImplicitAny:true` + `strictNullChecks:true`**。下一格候选：`strictFunctionTypes` / `strictBindCallApply` / `strictPropertyInitialization` / `noImplicitThis` / `alwaysStrict` / `useUnknownInCatchVariables` —— **每个都先单独 CLI 量化再决定**。
- 遗留待清理项（非本批范围）：`useNLPManagementLogic` 的幽灵属性 `rule_id`（`Rule` 未声明、全仓无读取）；`ruleList` L300 `showToast('success', data.message)` 无兜底文案（同函数 L304 有兜底，不对称）。
- skill `ts-noimplicitany-enable` 已升级为「分步开启 TS 严格检查 SOP（noImplicitAny → strictNullChecks）」，新增第二格全套内容 + 两个工具坑。⚠️ 其目录名已略窄于内容，后续可考虑改名（未擅自改，避免破坏引用）。

### 铁律①
#110 改动已 commit（`59b3065`）+ push。

---

## #111 · 清理多余属性 `rule_id`（类型契约收敛）

**核实过程（关键：区分同名属性归属不同实体）**：
`grep -rn "\.rule_id" src/pages/nlp-management/` 命中 8 处，逐一核对所属类型后发现**全部不是 `Rule`**：
- `ParseTab.tsx:112/114/126/129` 的 `rule` 来自 `parseResult.matched_rules` → **`MatchedRule`**（`types.ts:19` 早已声明 `rule_id: number | null`）；
- `useNLPManagementLogic.ts:193` 的 `r` 同为 `matched_rules` 元素；
- `useNLPManagementLogic.ts:138` 的 `s.rule_id` 是 **`Suggestion`** 字段（用于派生 `id`）；
- **`suggestedRules`（`Rule[]`）的渲染段 `ParseTab.tsx:243-266` 只读 `id / score_type / score_value / behavior_tags / behavior_description / behavior_keyword`，从未读 `rule_id`。**

→ #109 的判断成立：`Rule & { rule_id: number }` 中的 `rule_id` 确为无消费者的多余属性（信息已由 `id: s.rule_id` 承载，删除零丢失）。

**改动（1 文件 / 2 行）**：`useNLPManagementLogic.ts` 的 `mapped` 去掉 `rule_id: s.rule_id,`，回调返回类型由 `Rule & { rule_id: number }` 收敛回 `Rule`。

**教训**：判断「某属性无消费者」**必须按类型归属逐一核对**，不能只按属性名 grep 计数 —— 同名属性分散在不同实体上（`Suggestion.rule_id` / `MatchedRule.rule_id` / 误加在 `Rule` 上的 `rule_id`），只看命中数必然误判。

### 闸门证据
| 闸门 | 结果 |
|---|---|
| `tsc --noEmit`（含 noImplicitAny + strictNullChecks） | **EXIT=0** |
| prettier --check | All matched files use Prettier code style!（该文件先 `--write` 再 `--check`） |
| eslint（.ts/.tsx/.js/.jsx） | **EXIT=0（0 problems）** |
| 全量 vitest | **EXIT=0**；37 passed / 1 skipped(38)；Tests **276 passed** / 3 skipped；**0 failed**（与 #109/#110 基线持平） |
| 三要素③ | 磁盘 **38 == 报告 38**，差集空 |

### 铁律①
#111 改动已 commit + push。

---

## ③ 主线 3 复核：班主任聚合首页**同样早已闭环**（记忆再次修正）

记忆与 `docs/班主任工作台优化方案-待审核.md` 里「12 项条目无聚合首页/概览页」的结论**已过时**。实测证据：
- `src/App.tsx:156` 懒加载 `./pages/WorkbenchOverview`，`:692-695` 挂路由 `path='workbench'`；
- `App.tsx:73` 未匹配路径默认 `Navigate to='/workbench'`；
- `components/layout/Sidebar.tsx:463-464` 菜单项「工作台总览」→ `/workbench`；
- `workbenchOverview/types.ts` 含 `ENTRIES` 13 项 + `GLOBAL_ENTRIES` 4 项，注释明写「P2 修复：此前总览页无这些入口」；且该页已完成 D2 拆分（`WorkbenchOverviewView.tsx` 独立视图层）。

→ **至此「三主线 + 三份陈旧文档」全部经实测确认闭环，无待办开发项。**

⚠️ **收敛的元教训**：本项目多份审计文档（`班主任工作台优化方案-待审核` / `班主任页拆分方案-铁律③待审` / `M9分页复核-缺口清单`）与长期记忆条目**普遍滞后于代码**。引用前**必须实测复核**，且 `grep` 权限词根/属性名时**必须带 `-A`/`-B` 上下文核对所属实体**（本轮 P0 误判、`rule_id` 属性归属 均为同类错误）。

### 遗留（非阻塞，可选，需用户决策）
- `ruleList` L300 `showToast('success', data.message)` 无兜底文案（同函数 L304 有兜底，不对称）；补文案属**显示行为变更**，未擅自改。
- skill `ts-noimplicitany-enable` 目录名已窄于其内容（现已覆盖 strictNullChecks），改名会破坏引用，未擅自处理，留给用户决定。

---

## #112 ④ 逐格推进收尾：**strict 整体开启** + noImplicitOverride

### 关键：先 CLI 逐 flag 量化，再决定是否「逐个堆叠」
在 #109/#110（noImplicitAny、strictNullChecks 已开）基础上，`tsc --noEmit --strict` 全量仅 **18 错误**。逐 flag 拆解：

| flag | 单独开启新增错误 |
|---|---|
| **strictFunctionTypes** | **18** |
| strictBindCallApply / strictPropertyInitialization / noImplicitThis / alwaysStrict / useUnknownInCatchVariables | **0**（全部） |

→ **推翻「逐个 flag 堆叠」的原计划**：代价 100% 集中在一格，18 处修完即可直接 `"strict": true`。

### 18 处的三个根因（全在类型声明层，零运行时变更）
1. **App.tsx 13 处（TS2322）**：`createLazyComponent(importFn: () => Promise<{default: React.ComponentType<unknown>}>)`
   —— 泛型参数位于 props **逆变位**，`unknown` 不能赋给页面的 `{}` props，故所有**显式标注 `React.FC` 的页面**全被拒。
   - **判据（可复用）**：报错集里「显式标注 `React.FC` 的页面全报错、无标注的 `function Xxx()` 不报错」→ 就是容器签名过严，而非页面有问题（后者参数更少，逆变下可兼容）。
   - 修法：`React.ComponentType`（默认泛型 `{}`）—— `React.FC` 恰好匹配，且**零 `any`**。
2. **codeSplitting.tsx 3 处（TS2352/TS2345）**：同型 `ComponentType<unknown>`。
   - 核实中发现该文件是 **181 行零消费者死文件**（8 个 export 全无引用、utils 无 barrel、无动态 import、无测试）→ **直接删除**，3 个错误随之消失。
3. **5 处函数参数逆变（TS2322）**：props 声明比真实实现**更宽**或**结构化类型不精确**
   - `approvals/types.ts`：`setSort: (field, order: string|null) => void`，而来源 `useTableUrlState` 给的是 `SortOrder`（`'ascend'|'descend'|null`）→ 收窄为 `SortOrder`。
   - `courseSchedule/types.ts`：`handleSubjectChange: (e: {target:{value:string}}) => void` / `handleFileChange: (e: {target:{files:FileList|null}}) => void`
     → 改 `ChangeEvent<HTMLSelectElement>` / `ChangeEvent<HTMLInputElement>`（与实现签名及 `<select>` / `<input type=file>` 真实 onChange 一致）。

### 顺手开启 noImplicitOverride（4 处，零 JS 产物差异）
`strict` 之外单独量化：**noImplicitOverride 4** / noUnusedLocals 13 / noUnusedParameters 7 / noUncheckedIndexedAccess **133** / exactOptionalPropertyTypes **157** / noPropertyAccessFromIndexSignature **187**。
- 4 处均为 class 覆盖 `React.Component` 成员（`lazy/LazyComponent.tsx` + `special/ErrorBoundary.tsx` 的 `componentDidCatch` / `render`）→ 补 `override`。纯编译期标注，**JS 产物零差异**。
- 后三项（133/157/187）**明确不推**：会迫使大量 `?? 0` / 断言补丁，**改变运行时语义**，风险远高于收益。
- noUnusedLocals/Parameters（13/7）与 eslint `no-unused-vars` 职责重叠（多为 `jsx: react-jsx` 下无用的 `import React`），另立批次评估。

### 最终 tsconfig
```jsonc
"strict": true,              // 逐格收口：noImplicitAny → strictNullChecks → strictFunctionTypes → 其余子项一次清零
"noImplicitOverride": true,  // strict 之外已开启项
```
（原显式 `noImplicitAny` / `strictNullChecks` 两行已删除 —— 已被 `strict` 涵盖）

### 改动清单（7 文件，+17 −194）
`tsconfig.json` · `src/App.tsx` · `src/components/lazy/LazyComponent.tsx` · `src/components/special/ErrorBoundary.tsx` · `src/pages/approvals/types.ts` · `src/pages/courseSchedule/types.ts` · **`src/utils/codeSplitting.tsx`（删 181 行）**

### 闸门
| 闸门 | 结果 |
|---|---|
| `tsc --noEmit`（读 tsconfig） | **EXIT=0** |
| 逐 flag 复核（strict + override + 8 个 strict 子项） | **全部 0 错误** |
| prettier --check | All matched files use Prettier code style! |
| eslint（.ts/.tsx/.js/.jsx） | **EXIT=0（0 problems）** |
| 全量 vitest | **EXIT=0**；37 passed / 1 skipped(38)；Tests **276 passed** / 3 skipped；**0 failed** |
| 三要素③ | 磁盘 **38 == 报告 38**，差集空 |
| vite build | **EXIT=0**（改 tsconfig 后补跑；独立 outDir 避开沙箱 safe-delete 守卫） |
| EOL | 改动文件全 LF，0 CRLF |

### 附带收获：新识别一个可复用判据
「容器签名过严」的识别特征 —— **同一批 importFn 调用点中只有一部分报错，且报错集恰好等于「显式标注了 `React.FC` 的组件」**。根因必在容器的泛型参数位置（逆变位），而不在被传入的组件。

### 铁律①
#112 改动待 commit + push（沿用「按顺序执行，同时push」授权，见下节执行记录）。

---

## #113 未使用符号收口：noUnusedLocals / noUnusedParameters（2026-09-11 上午）

### 触发
「按照建议继续」→ 执行上一轮登记的建议项之一：清 noUnusedLocals(13) / noUnusedParameters(7)。

### ★ 关键纠正：不能交给 eslint
上一轮记录「两项与 eslint `no-unused-vars` 重叠 → 走 eslint 而非 tsc」**是错的**。实测：
- 对 `ConfirmDialog.tsx`（未用 `import React`）与 `cacheWarmupService.ts`（未用私有方法）单独跑 eslint，**0 problems**。
- 两条原因：① `@typescript-eslint/no-unused-vars` **覆盖不到类的私有成员**（本次 2 处）；
  ② `react/jsx-uses-react` 会把 `jsx: react-jsx` 下已无必要的 `import React` 标记为**已使用**（本次 11 处）。
⇒ 凡 `jsx: react-jsx` 或类私有成员场景，**tsc 是唯一兜底**。

### 量化 → 清理（20 处 / 18 文件，零运行时变更）
| 类别 | 数 | 处理 |
|---|---|---|
| 无用 `React` / `ReactNode` 导入 | 11 | 直接删（`React.ComponentType` 类型位由 UMD 全局提供） |
| 无引用私有成员 | 2 | `CacheWarmupService.fetchEndpoints`（整方法）、`RequestCoalescer.maxCacheTTL`（字段）删除 |
| 无引用解构属性 | 3 | `StudyGroupsView.groups/setErrors`、`UserDetailView.id` |
| 无引用 `.map` 解构项 | 1 | `ScoreRecords`：`([examId, exam]) =>` → `([, exam]) =>` |
| 无引用回调形参 | 2 | `SeatingChart` 两个 handler 的 `seat` → `_seat` |
| 未被使用的公共方法形参 | 1 | `performanceMonitor.recordError(name)` → `_name`（保留形参，调用点/测试仍传参） |

### tsconfig
`noUnusedLocals: false → true`、`noUnusedParameters: false → true`（附注释写清必须在 tsc 侧兜底的原因）。

### 闸门
| 闸门 | 结果 |
|---|---|
| `tsc --noEmit` | **EXIT=0** |
| eslint（.ts/.tsx/.js/.jsx） | **EXIT=0（0 problems）** |
| prettier --check | 全通过 |
| 全量 vitest | **EXIT=0**；37 passed / 1 skipped(38)；**276 passed / 3 skipped / 0 failed** |
| 三要素③ | 磁盘 **38 == 报告 38**，差集空 |
| EOL | 20 个改动文件全部 0 CRLF |

### 新增坑（已同步进 skill「第四格」）
⚠️ **未使用的「公共方法形参 / 回调形参」不要直接删** —— 删了调用点会报「传参错误」（`Expected 0 arguments, but got 1`）。
统一加 `_` 前缀：`noUnusedParameters` 对 `_` 开头形参豁免，且 API 表面 / 签名声明都不变。

## #114 收尾建议项
- ✅ `ruleList/useRuleListLogic.tsx:300` 成功分支补兜底：`data.message as string` → `data.message || '模板应用成功'`
  （与同函数失败分支 `|| '应用模板失败'` 成对；顺带去掉 1 处 `as` 断言）。属**显示文案变更**，已单独提交并在信息中标注。
- ✅ `ts-noimplicitany-enable` skill：由「三格」升级为「四格」，新增「第四格 noUnusedLocals/Parameters」整节；
  决策表中两项由 ⏸ 改 ✅。**目录名维持不改**（已评估：发现靠 `description` 字段，不靠目录名）。

### 提交
| commit | 内容 |
|---|---|
| `9afaa29` | chore(frontend): 清理 20 处无用符号并开启 noUnusedLocals/noUnusedParameters |
| `83c55ca` | docs(memory): 记忆整并压缩（14111→10794 字节）并登记 noUnused 闸门 |
| `db6b903` | fix(frontend): 规则模板应用成功的 toast 补兜底文案 |

远端 `git ls-remote origin refs/heads/main` = **db6b903** == 本地 HEAD。

### 追加（同批次收尾）
- `16c5a77` docs(memory): 记录 #113 未使用符号收口并修正 noUnused 决策
- `f362ed3` docs(memory): MEMORY.md 二次压缩 14111→10044 字节（-29%）
  —— 原因：上一版 11103 字节仍贴近注入截断阈值（14111 时已被截断并要求先整理），
  故再做一轮定向瘦身；关键条目自检全部保留。
- **最终远端 HEAD = `f362ed3`**（工作区干净）。

## #115 后端 ruff 真实缺陷批次（下午）
- 量化：后端 app 代码上用 ruff 选真实缺陷规则（F821/F822/F823/B006/B019/B023/F401 等），命中 **39 处**。
- 修复（零行为变更 / 修真 bug）：
  - **F821 ×5**：`course_schedule_routes / admins_service / algorithm_service / report_summary_service / db_optimizer` 缺 `logger` 定义（进入 except 分支即 NameError）→ 各补 `import logging` + `logger = logging.getLogger(__name__)`。
  - **F823 ×1**：`nlp_parser_service.execute_scoring` 函数内第 450 行 `from models import db` 使 `db` 成为函数局部变量，导致第 395 行 `db.session.add(rule)` 在 manual_correction 分支先执行时 UnboundLocalError → 删除该行（模块级第 11 行已导入 db）。
  - **F822 ×27**：`config.py` 的 `__all__` 误列 27 个 `Config` 类属性（如 `FLASK_APP`）为模块级名字 → 收敛为真实存在的 `["Config","config","get_config"]`（app 代码无 `from config import *`，零行为变更）。
  - **B006 ×1**：`textcnn_service.TextCNNClassifier.__init__` 可变默认参数 `filter_sizes=[2,3,4]` → `filter_sizes=None` + 函数内 `filter_sizes = filter_sizes or [2,3,4]`。
  - **B023 ×4**：`transaction_retry` 的 `_insert_batch`/`_update_batch` 闭包捕获循环变量 `batch` → 默认参数绑定 `def _x(batch=batch):`。
  - **F401 ×1**：`models/__init__.py` `TeacherComment` 导入未再导出 → 加入 `__all__`（外部用 `from models.teacher_comment import TeacherComment`，barrel 仅补导出）。
- **待用户决策（不擅改）**：**B019 ×1** `nlp_fast_parser.FastNLPParser.parse` 方法上 `@lru_cache` 内存泄漏——因 `parse` 内部调用 `self._is_simple_text`/`self._split_clauses`/`self._parse_single` 依赖 self，不能简单改 staticmethod（会改变缓存语义），留待用户定夺。
- **EOL**：9 个文件 CRLF、2 个 LF，全部用二进制安全脚本改，EOL 零翻转（config.py 初版一度混行尾，已二次修正为纯 CRLF）。
- **验证**：ruff 缺陷选择器归零（仅余 B019 待决策）· py_compile 10 文件全 OK · 定向 pytest（nlp_parser/textcnn/course_schedule/algorithm/report_summary/models）= **80 passed / 0 failed** · 全量 `run_regression.sh` = **全部回归通过**。
- **提交**：`d4a29c1` fix(backend): ruff 真实缺陷批次（10 文件）· `07af512` docs(memory): #115 记录 → 远端 HEAD=`07af512`，工作区干净。

## #116 后端 ruff 安全清理批次（下午）
- 量化：后端 app 代码 ruff 扩展扫描（F/E/B/SIM/C4/UP/ASYNC/C901/RET/ARG）共 ~2800 命中；
  筛出 RET/SIM/UP/C4/B033 安全子集 54 处，排除 E501(2027,归black)/C901(89,结构重构)/ARG*(80,签名兼容)。
- 修复（零行为变更）：
  - **47 处** ruff --fix 自动修复：RET501/507/503/502、SIM105、UP017、B033、SIM222。
  - **7 处** ruff 标 unsafe 的手动修复（多行块/需补 import）：service_init(SIM105×2+补 import contextlib) ·
    cache(SIM105+补 import contextlib) · rule_recommendation(SIM222×2,`"未分类" or "未分类"`→`"未分类"`) ·
    version_routes/app.__init__(RET503,before_request 处理器补显式 `return None`)。
- **EOL**：21 文件 CRLF/LF 保持原样（ruff --fix 与手动脚本均字节级保行尾，无 MIX）。
- **排除项（留待用户决策，不擅改）**：
  - **B905 ×4**（algorithm_service/device_service 的 `zip` 缺 `strict=`）：加 `strict=True` 会在迭代器不等长时
    由静默截断变抛异常——属行为变更，违反零行为变更铁律。
  - E501/C901/ARG* 非本批范围（见上）。
- **验证**：ruff 选中规则归零 · py_compile 21 文件全 OK · 全量 run_regression.sh = 全部回归通过（task JbLc1r）。
- **前端残留债**：`console.log`/debugger 共 9 处（any/TODO 已清零）——待 #117 批次。
- **提交**：`82b029f` style(backend): ruff 安全清理批次#116（21 文件 +53/-62）· 待 push + 本日志。

## #117 后端 ruff 安全清理批次（续）
- 量化：#116 之后剩余安全子集 RET504/508、SIM103/108、C403/414/416 共 26 处；排除 SIM115×1 误报后修 25 处。
- **SIM115 误报**：redis_cache_service.py:156 的 `open(log_path,...)` 句柄须长期持有供子进程（Redis-server）用，
  `with` 会提前关闭导致子进程写失败——**明确不修**。
- 修复（均逐行 diff 审阅确认语义等价）：RET504×8 内联 return · RET508×1 `elif`→`if`（前分支含 break）·
  SIM103×6 needless-bool 内联（transaction_retry 显式 `bool()` 保类型）· SIM108×6 if-else→三元 ·
  C403×2 `list(set([...]))`→`list({...})` · C414×1 `sorted(list(s))`→`sorted(s)` · C416×1 `list(row)` 替代推导。
- **EOL**：19 文件 CRLF/LF 保持，无 MIX。
- **验证**：ruff 选中规则归零（仅余 SIM115 误报）· py_compile 19 文件 OK · 全量 run_regression.sh = 全部回归通过（task dwrKbb）。
- **提交**：`4e4481d` style(backend): ruff 安全清理批次#117（19 文件 +31/-71）· 待 push + 本日志。

## 后端 ruff 债项收口结论
- 已闭环：#115（F821/F823/F822/B006/B023/F401）、#116（RET/SIM/UP/B033）、#117（RET/SIM/C4 剩余）。
- **待用户决策（行为变更，不擅改）**：B905×4（zip 缺 `strict=`，加 strict 会在不等长时由静默截断变抛异常）。
- **非本范围**：E501(2027,归 black) / C901(89,结构重构有行为风险) / ARG*(80,签名兼容性) / E402(73,部分惰性导入) / SIM115(1,误报)。
- 前端：`console.*` 9 处经核查全是 logging/errorMonitor 基础设施，**非调试残留，无债项**；`any`/TODO 已清零。

## C901/ARG/E402 债项分层与试点（转向高风险重构）

用户指令「转向 C901/ARG 等需要逐项评估的高风险重构」。ruff 0.16.0 全量扫 apps/backend：

- **C901 圈复杂度=103**（函数 >10）：services46/api27/utils8/scripts7/tests7/tools4/app1/middleware1/models1/startup1。分布 11-12:35 / 13-15:25 / 16-20:23 / 21-30:15 / 31+:5。最高 users_routes.post 45、academics_service 37、nlp_enhanced 36/29/28、security 35。
- **ARG 未用参数=506**：81%(409) 在 tests/，多为 parametrize/mock/SocketIO 回调**按名绑定→改名会废测试**，只能 # noqa。非测试仅 97，且多为框架/回调/接口签名（mqtt_manager paho 回调、excel/export 静态方法 filename 等）→ 只能保签名（前缀_/#noqa），删参需逐个审计调用方。结论：**ARG 非高价值重构，勿批量动**。
- **E402 模块级导入不在顶部=155**：多数为 if TYPE_CHECKING/延迟导入/sys.path 等合法写法→盲移破坏延迟加载/循环导入规避。结论：**逐文件甄别，勿批量移**。

### 试点（2 函数，二进制安全保行尾）
- operation_logs_routes.get() 抽模块级 _apply_operation_log_filters()：C901 11→~4；helper 自身 8(<10)。
- ota_negotiation_service.in_quiet_window() 抽 _hit_window(cur,start,end)：C901 12→~9。
- 验证：py_compile OK；ruff C901 两文件归零；operation_log 路由测试 2 passed；全量回归后台跑（task ORu9F7）。
- 行尾纪律：operation_logs_routes=CRLF(125) 用二进制脚本保 CRLF；ota_negotiation=LF(491) 保 LF。禁 Edit 直改。
- **全量回归已绿（REG_EXIT=0，全部回归通过）**：试点 2 函数行为零变更确认。C901 提取方法论+闸门（二进制保行尾/ruff 归零/针对性测试/run_regression）验证可用。
## C901 11-12 桶收口战役（子批1-5 + 试点）

用户指令 A=继续推进 #113（11-12 复杂度桶）。逐函数评估+二进制安全保行尾+全量回归闸门，共收口 **11 个函数**（全绿）：
- 试点：operation_logs_routes.get → _apply_operation_log_filters；ota_negotiation.in_quiet_window → _hit_window（task ORu9F7 绿）
- 子批1：anomaly.get_all_anomalies → _compute_score_change/_format_detected_at；validation.validate_request → _interpret_validator_result（Rw0Ppp 绿）
- 子批2：system_config.update_config → _CONFIG_FIELDS/_apply_config_fields；api_cache.cached_api → _extract_response_data/_safe_cache_get/_safe_cache_set（Qj0I6P 绿）
- 子批3：rule_recommendation.suggest_rule_combinations → _find_frequent_combinations/_build_combination_suggestions；nlp_parser.determine_intent → _intent_from_keywords（uWZE0Z 绿）
- 子批4：excel.export_large_dataset DRY → _style_header/_write_large_dataset_rows/_compute_column_widths/_write_small_dataset_rows（AUwMaJ 绿）
- 子批5：academics.update_import_config → _apply_import_config_fields；tools.collect_test_metrics → _collect_test_files/_count_source_modules（vZ0v0V 绿）

剩余 16 函数均为真热路径/闭包/线性if链/tests，按纪律**留单评或跳过**（RBAC requires_permission、审批 _execute_approve、MQTT handle_mqtt_message、积分 execute_rules、成绩导入 execute_score_import、NLP parse/_check_corrections、闭包 generate_template/register_handlers/wrapper、_convert_value 线性if、5个 tests 项）。11-12 桶实质收口，转入 #114（13-30 桶 63 函数）。
## C901 #114 战役（13-30 桶，重新扫描校准）

重新扫描后端确认：C901 真实剩余 **91 个**（原 103，已收口 11）。#114 真实分布：13-15桶23 / 16-20桶23 / 21-30桶15 / 31+桶5。

用户指令 A 转 #114。子批1（13复杂度低位，已绿 task HBtQ9p）：
- engagement.calculate_engagement (c=13→0)：抽 _calc_attendance_rate/_calc_homework_rate/_calc_activity_rate 三模块级纯函数
- excel_utils.validate_import_data (c=15→0)：抽模块级 _validate_row_by_type

纪律沿用：二进制保行尾 + ruff C901 归零 + ruff 默认计数前后一致 + 针对性测试 + 全量回归闸门。13-15桶剩余多为 NLP 核心(match_rule/semantic_match/_analyze_sentiment/_prepare_enhanced_training_data/_validate_name)、MQTT _process_ota_status、路由写路径(post/put)、基础设施 _try_auto_start_redis(语义敏感留单评)，逐批挑机械性强者。

用户中断后续（15:00 续，task slhgXG 绿）：原 #114 子批2 第二候选 resolve_relations 一并完成。
- risk_predict_service.detect_attendance_risk (c=13→~5)：抽模块级 _attendance_risk_real(attendance_rate,absent_count,leave_days) 与 _attendance_risk_proxy(features)（返回 score,factors 元组），主函数仅留路由分支+裁剪+定级。注意：**proxy 分支 `daily_record_count<0.5` 仅加分不 append factor** 的怪癖已逐字节保留。
- class_service.import_classes 内闭包 resolve_relations (c=15→~4)：拆为模块级 _resolve_admin_relation + _lookup_admin_by_name + _lookup_admin_by_id（各 c≤6）。**关键怪癖已保留**：原逻辑在「admin 找到但 role 不在 admin/teacher」时仍 append 错误——且仍执行 `resolved["head_teacher_id"]=admin.id`（else 块里 role 检查与赋值同级，非嵌套）。新 _lookup 在 role 异常时 append 错误后**不 return**、继续 `resolved["head_teacher_id"]=admin.id`，严格等价。

#114 子批2 结果：C901 88→87（2 函数收口）。验证：py_compile 2 文件 OK；ruff C901 2 文件归零；ruff 默认计数 2261 前后一致；针对性测试 test_risk_predict_service 25 passed / test_class_service 36 passed；全量 run_regression.sh=全部回归通过。
C901 11-12桶 16 剩余 + #114 子批2 后真实剩余 87（13-15桶居多）。下一步候选：nlp_fast_parser._validate_name(c15)/redis._try_auto_start_redis(c14)/api 路由 post/put 写路径（均逐函数评估，高风险抽取宁跳不强推）。

## #114 子批3（task h4f8GF 绿）：nlp_fast_parser._validate_name (c=15→~7)
- 抽两个方法：`_strip_intent_keywords(name, intent_keywords)`（首尾关键词剥离，命中即返回、未命中返回 None）与 `_trailing_is_invalid(text, end_pos, intent_keywords)`（后置中文非行为续接判定，True 触发剔除）。主函数仅留 intent_keywords 构造 + 单字动词兜底 + 两次调用。
- ⚠️ **CRLF 坑复发**：二进制安全脚本以 `newline=""` 读入后，old 锚点串**仍须** `.replace("\n","\r\n")` 才能命中，否则 OLD BLOCK NOT FOUND（此前只在「读出侧」归一化，忘了「匹配侧」）。
- 验证：py_compile OK；nlp_fast_parser C901 归零；总 87→86；ruff 默认 2261 不变；test_nlp_fast_parser 15 passed；全量回归绿。

## #114 子批4（task 4yT39t 绿）：tools/project_evaluation 两函数 (c=14 / c=17)
- `collect_api_coverage`(14) → `_scan_route_file` + `_match_namespace_desc` + `_collect_swagger_metrics`
- `collect_architecture_metrics`(17) → `_collect_layers` + `_collect_model_count` + `_collect_service_count` + `_count_route_files` + `_detect_service_layer_pattern`
- 做法：**按行号切片搬运**（不是重打字符串），gnarly 正则行原样复制，emoji 行靠切片整体保留（脚本里不出现 emoji，规避代理转义失败）。
- ⚠️ **dedent 量算错**：基准缩进 4 的块 dedent 应为 **0**（非 4）；`try:` 基准 12 的块目标是 4 → dedent **8**（非 12）。写成 4/12 会产生「部分 0 缩进 + 部分 4 缩进」混合 → SyntaxError。**dedent 量 = 原基准 − 目标基准**。
- ⚠️ **`&&` 链断假绿再次应验**：py_compile 失败后 `&&` 后段全跳过，只剩硬编码 `echo "All checks passed"` → 误判成功。校验链一律用 `;` + 显式 `echo EXIT=$?`（本批正是这样才抓到 SyntaxError）。
- ✅ **等价性用「输出快照 diff」证明**（tools 不在核心回归内）：harness 重定向 stdout 并 dump 返回 dict，前后两次对比。
  - ⚠️ **`design_patterns` 顺序不稳定是既存现象**：`list(set(...))` + 字符串 hash 随机化 → **同一份代码连跑两次输出都不同**。必须 `PYTHONHASHSEED=0` 重跑前后版本才可比（本批 pinned 后 stdout / stderr / 返回 dict 三者逐字节一致 → 零行为变更）。
- 验证：COMPILE_EXIT=0；C901 文件仅剩 `collect_security_metrics`(c=24)；总 86→84；ruff 默认 2261 不变；EOL 598/598 纯 CRLF（pure_lf=0）；全量回归绿。

#114 子批3+4 后真实剩余 **C901 84**。

## #114 子批5（task ez2lsn 绿）：phonebox_policy.normalize_windows (c=18)
- 拆 5 个模块级 helper：`_parse_hm_string` / `_parse_hm`（原为**嵌套闭包**，ruff 把闭包复杂度计入父函数，故仅上提不够）/ `_parse_day` / `_check_hm_range` / `_normalize_window_item`；主函数降为 c≈5。
- 文件是**纯 LF**（crlf=0，与多数 backend 文件不同）→ emit 不能加 `\r`。
- ✅ **等价性用「输入矩阵快照」证明**：35 个用例（合法/越界/非整数/无冒号/混合字段与字符串/多项第二项报错/结束早于开始）逐字节一致。
- 验证：COMPILE_EXIT=0；C901 文件归零；总 84→83；ruff 默认 2261；EOL 纯 LF 保留；test_phonebox_policy + test_regression_20260817 **44 passed**；全量回归绿。

## #114 子批6（task UG68l7 绿）：nlp_analyzer_service._apply_event (c=18)
- 按 `etype` 的 5 路分派 → 抽 `_apply_intent_event` / `_apply_performance_event` / `_apply_error_event` / `_apply_component_event` / `_apply_history_event`，主函数降为 c≈6。
- ⚠️ **差点漏搬代码**：先只读到 306 行，实际函数到 **308 行**（history 分支末尾还有 `if len(self.request_history) > self.max_history_size: pop(0)`）。→ **改写前必须用「下一个 def 行号」确认真实边界**，不能凭 Read 的 limit 想当然。
- ✅ **等价性用「事件矩阵 + 状态 dump」证明**：~550 个事件覆盖 5 分支 + 溢出裁剪（confidences>100 / slow_requests>100 / request_history>max），**两段 JSON 均 30058 字节、逐字节一致**。注意 dump 里含 `datetime.now()`，**必须正则抹平时间戳**否则必然假差异。
- 验证：COMPILE_EXIT=0；C901 文件归零；总 83→82；ruff 默认 2261；EOL 567 纯 CRLF；test_nlp_analyzer_service **33 passed**；全量回归绿。

## #114 子批7（task 8TDNY2 绿）：nlp_parser_service.extract_behavior (c=18)
- 抽 4 个方法：`_match_behavior_keywords`（含同义词）/ `_match_words_by_jieba` / `_count_sentiment` + `_count_sentiment_words` / `_count_sentiment_by_substring`。主函数降为 c≈4。
- 保留的语义细节：jieba 分支里 `if word in positive` 与 `if word in negative` 是**两个独立 if**（同词命中两次计两次），非 elif；`jieba.lcut` 在原代码里被调用 2 次，新代码同样 2 次（分词结果确定）。
- 验证：COMPILE_EXIT=0；C901 文件归零；总 82→**81**；ruff 默认 2261；EOL 495 纯 CRLF；test_nlp_parser_service **18 passed**；全量回归绿。

## 本轮结论
C901 **84 → 81**（3 个 c=18 函数收口）。三个子批均「快照等价证明 + py_compile + C901 归零 + ruff 默认计数不变 + EOL 保留 + 针对性测试 + 全量回归」。
**改写手法升级**：本轮改用「**从文件自身提取 OLD 块 + 断言首/尾行**」替代手写锚点串——既规避 CRLF 匹配问题，又保证不漏搬。剩余 81 个以 13-20 桶为主（api 路由 post/put 写路径、NLP 核心、MQTT），31+ 桶 5 个（#116 禁批量强推）。
⚠️ `import_export_service.bulk_import_users`(c=17) 有**潜在 NameError 怪癖**：except 分支用 `name if name else "未知"`，若第 88 行 `name = str(row[0])...` 本身抛错则 `name` 未绑定 → 处理器内再抛 NameError。**抽取时必须把字段提取留在主函数**，只把校验块下沉，否则会改变行为。剩余分布：13-20 桶为主（api 路由 post/put 写路径、NLP 核心 match_rule/semantic_match/_analyze_sentiment、MQTT _process_ota_status/handle_score_add、nlp_analyzer._apply_event(c18)/nlp_parser.extract_behavior(c18)/phonebox_policy.normalize_windows(c18)），31+ 桶 5 个（#116 禁批量强推）。

## #114 子批8-12（本轮，C901 81 → 76，5 个函数，5 轮全量回归全绿）

| 子批 | 函数 | 原 c | 手法 | 等价性证明 |
|---|---|---|---|---|
| 8 | `nlp_enhanced_service._analyze_sentiment` | 14 | 抽 `_occurrence_weights`(static) + `_accumulate_keyword_scores` | 文本矩阵约 1500 例（18 程度副词 × 正/负关键词 + 28 前缀 + 句子级），**86384 字节逐字节一致** |
| 9 | `nlp_ml_service._prepare_enhanced_training_data` | 14 | 抽 `_collect_match_result_samples`(static) + `_collect_rule_samples` | 假 rule/match 数据集 + `random.seed(0)`，**798 条 texts/labels 逐字节一致** |
| 10 | `nlp_enhanced_service.semantic_match` | 13 | 抽 `_build_rule_texts`(static) + `_collect_matched_rules` | 假 vectorizer + 确定性 cosine 桩 + get_by_id 桩，7 文本 × 4 intent 逐字节一致 |
| 11 | `nlp_enhanced_service.match_rule` | 13 | 抽 `_collect_keyword_rule_matches` / `_append_semantic_matches` / `_append_rule_if_new` / `_build_fallback_matches` | 4 个 collaborator 全打桩，10 用例覆盖 keyword/strong/semantic/ml/ensemble/fallback/unknown |
| 12 | `api/scores/rules_routes.py::RuleList.post` | 14 | 抽 5 个模块级函数：`_validate_rule_create_payload` + `_validate_rule_name/_score/_category/_limits` | **差分法**：备份文件切片 exec 成参照函数，25 组输入矩阵 orig vs new **mismatch_count=0** |

验证（每批一致）：py_compile 0 · C901 文件归零 · ruff 默认计数 **2261 全程不变** · EOL 原样（LF 仍纯 LF、CRLF 仍纯 CRLF）· 针对性测试 53/35/53/53 passed · 全量回归 5 轮均「全部回归通过」。

### 本轮踩到的新坑
1. **把复杂校验块整体搬进单个 helper 不降 C901** —— helper 自身也会被 C901 命中（rules_routes 校验链搬完仍是 c=15）。**必须按职责拆到各自 <10**，再由 orchestrator 串起。
2. **拆分后引入新 `RUF059`** —— 原方法级共享的 `error_msg`，拆分后在某子函数里"未使用" → 新报 `RUF059`（ruff 默认计数 2261→2262）。修复：**改 `_error_msg` 前缀**（实测可抑制）。**判据：ruff 默认计数 +1 就属这类「作用域收缩」副作用**，非语法问题。
3. **浮点累加顺序不可想当然** —— `_analyze_sentiment` 若把正/负累加分段求再相加，浮点非结合律会致末位差异 → 必须**传入并返回两个 running totals 原位累加**。
4. **DB 依赖函数的差分等价** —— 把备份文件里的原始代码块**切片 + `textwrap.dedent` + `exec` 成参照函数**，同一次运行内与抽出的 helper 做输入矩阵比对；比 before/after 两次运行更强（不受 hash 随机化影响，无需 pin seed）。
5. **独立 harness 导入路由模块需 env** —— `utils.security` 在非 pytest 下缺 `JWT_SECRET_KEY` 会直接 `RuntimeError`；harness 里 `os.environ.setdefault("JWT_SECRET_KEY", ...)` 即可。

## #114 子批13-21（本轮，C901 76 → 67，9 个函数，3 轮全量回归全绿）

| # | 函数 | 原 c | 手法 | 等价性证明 |
|---|---|---|---|---|
| 13 | `api/data/download_routes.py::download_score_template` + `generate_template` | 13 + 12 | 抽 `_resolve_template_subjects` / `_query_template_students` / `_build_score_template_workbook` / `_fill_score_sheet` / `_fill_notes_sheet` + `_NOTES_ROWS` | 打桩 `User.query` 三分支 + `get_by_id`（str/list/None/空 subjects），**10 组用例回读 xlsx 单元格逐字节一致** |
| 14 | `api/devices/devices_routes.py::DeviceRemoteControl.post` | 13 | 抽 `_send_device_restart` / `_publish_unlock_retry` / `_start_smart_unlock` + `_UNLOCK_SPECS` | 假 Thread **同步执行** target + `time.sleep` 空转，12 用例 **publish 序列逐字节一致** |
| 15 | `api/academics/course_schedule_routes.py::CourseScheduleResource.put` | 13 | 抽 `_check_schedule_update_forbidden` / `_resolve_schedule_update_fields` / `_resolve_teacher_name` / `_schedule_time_changed` / `_collect_time_conflicts` / `_check_change_conflicts` / `_resolve_final_color` / `_schedule_update_response` | 全 collaborator 打桩（含 `ns.payload` 换 stub），**21 用例（返回体 + update payload）逐字节一致** |
| 16 | `services/mqtt_manager.py::_process_ota_status` | 15 | 四状态分支 → `_handle_ota_started/_progress/_success/_failure`；**DRY** 共用 `_find_in_progress_firmware_update`；失败码元组→类常量 `_OTA_FAILURE_STATUSES` | 打桩 app/models/logger/datetime，**35 用例**（15 状态 × device/record 组合 + 非法 JSON）dump 日志/查询/session.add/commit/device 回写，逐字节一致 |
| 17 | `services/nlp_enhanced_service.py::_trigger_inductive_learning` | 16 | 抽 `_learn_name_pattern` / `_register_name_pattern` / `_learn_intent_keywords` | 裸实例 + 注入 state，**27 用例**（前缀长度 0/1/2/8/9、位置反向、空值、已有模式…）逐字节一致 |
| 18 | `services/import_export_service.py::bulk_import_users` | 17 | 抽 `_append_user_name_error` / `_append_user_class_error` / `_append_user_card_id_error` / `_validate_user_row` / `_record_user_row_failure` / `_record_user_row_exception` / `_commit_imported_rows` | **19 用例**含 NameError 怪癖路径，逐字节一致 |
| 19 | `services/redis_cache_service.py` | — | 本轮未动（语义敏感，留单评） | — |
| 20 | `services/nlp_enhanced_service.py::_check_corrections` | 12 | 抽 `_load_approved_corrections` / `_extract_correction_data`(static) / `_build_correction_map`(static) / `_apply_correction_map` | 打桩 NLPCorrection 两步查询 + db/datetime/logging，**13 用例**（含查询异常降级、缓存淘汰、重复 field_type 首条优先）逐字节一致 |
| 21 | `services/excel_service.py::_convert_value` | 11 | 拆 `_escape_formula_injection` + `_convert_non_str_value`（str 提前分流） | 纯函数**输入矩阵 31 例**（含公式注入 8 种前缀、Decimal/float/list/dict/空值）逐字节一致 |

验证（每批一致）：py_compile 0 · C901 目标文件归零 · ruff 默认计数 **2261 全程不变** · EOL 原样（LF 仍纯 LF、CRLF 仍纯 CRLF）· 针对性测试 3/27/9/49/53/4/87 passed · 全量回归 **3 轮**均「全部回归通过」。

### 本轮踩到的新坑（已写入长期记忆 + skill）
1. **模块级 helper 不能插在 class 中间** —— `CourseScheduleResource.put` 之后同类还有 `delete`，直接在其后插 `def` 会**提前终止 class** → 语法错误。正解：锚点选 **class 之前**的空行，脚本改为**双插入点**（`lines[:A] + helpers + lines[A:B] + method + lines[C:]`）。
2. **harness 的「实现观测」会产生假差异** —— devices 用例里 `thread_new.args` 旧版是闭包（`()`）新版是共享函数（带 topic/payload）；**过滤掉该字段后其它全部一致**。判据：差异只落在「被测代码内部结构」而非「外部可见行为」时，须逐字段判定是否属观测噪声。
3. **抽取新增局部 `from X import a, b` 会触发 `I001`** —— ruff/isort 按**大小写敏感**排序（`db, DeviceFirmwareUpdate` 应写成 `DeviceFirmwareUpdate, db`）。判据：ruff 默认计数 **一次性 +N**（本次 +4，与新增 import 块数吻合）。修法：只把**自己新增的那几处**改成排序后形式（**不要**跑 `ruff --fix`，否则会顺带修掉既有基线 I001 使计数低于基线）。
4. **快照先自检** —— bulk_import_users 首版 harness 的 `make_lookup` 里 `filter_by(**kw)` 的 `kw` 在 `first()` 中不可见 → 全部用例退化成 system 错误（`all-valid` 也 imported=0）。**快照必须先用「必然成立的期望值」自检**（如 all-valid 必须 imported=1），否则噪声快照会白跑前后对比。
5. **`isinstance` 链拆分时 `str` 可安全提前** —— `str` 不是 datetime/date/Decimal/bool/list/tuple/dict/float 的实例，把 str 分支提到最前不改变语义；反之 `datetime` 必须留在 `date` 之前（子类关系）。


---

## #114 子批（13-16 桶收口·第 2 批）：3 函数，C901 66 → 63

- ruff 默认 **2259 → 2258**（已定因，见坑 3）；3 文件纯 LF 保持；针对性 pytest **144 passed**；**2 轮全量回归均「全部回归通过」**。

| 函数 | 原 c | 手法 | 等价证明 |
|---|---|---|---|
| `api/reports/report_routes.py::ClassSemesterReport.get` | 16 | 抽 7 个模块级 helper：`_check_report_class_scope` / `_build_report_score_map` / `_build_report_headers` / `_build_report_rows` / `_build_report_summary_rows` / `_render_csv_report` / `_render_excel_report`；主方法只留参数校验 + 编排 | 路由 harness 全打桩（ClassInfo/User/Exam/Score 的 query、`ExcelUtils`、`send_file`、`build_class_summary`、`utils.permission.get_current_admin|get_admin_class_ids`），**210 例**（权限 6 态 × 早退 × 空考试不查 Score × fmt=csv/excel/CSV × 摘要三态）逐字节一致 |
| `services/mqtt_message_service.py::handle_unlock_message` | 16 | 抽类内 `_apply_teacher_unlock_policy` / `_blocked_by_class_schedule` + 模块级 `_build_policy_check_time`（DRY 两处重复的 check_time 计算）/ `_teacher_policy_reason`；重处置完分支改 `return True/False` | 全 collaborator 打桩（phonebox_policy.evaluate、ClassTimeChecker、log_warning、datetime），**57 例**（策略 6 态 × 6 种 hour/minute × 门禁 × 课表三态）逐字节一致 |
| `api/data/export_routes.py::ExportData.post` | 16 | 5 个导出类型各抽 `_export_*_data(export_format, timestamp)`，统一返回 `(output, filename, mimetype)`；主方法只留校验 + 分派 + 兜底 | 全打桩（export_service 9 方法、Query 轨迹、to_dict 入参、build_attachment_response、logger），**29 例**（校验 4 + 5 类型×2 格式×2 权限范围 20 + 空集 3 + 服务异常 2）逐字节一致 |

### 本轮新坑（判据优先）

1. ⚠️ **harness 快照里的列表必须在 append 时深拷贝**。`'api': api_rec` 存的是**共享列表引用**，而 `json.dumps` 在所有用例跑完后才执行 → 210 条结果全指向最后一条（表现为「所有用例观测值完全相同」，误导性极强）。正解 `'api': [list(x) for x in api_rec]`。**判据：所有用例的观测值完全一致 = 强信号，先怀疑共享引用。**
2. ⚠️ **被 `@requires_permission` 装饰的路由方法不能直接调用**：wrapper 在调用期访问 flask `request` → `RuntimeError: Working outside of request context`。必须**在 import 被测模块之前**把 `utils.permission.requires_permission` 置为透传（装饰器已在 import 期执行，import 之后再设无效）。
3. ⚠️ **ruff 默认计数「下降」也必须定因**，不能只判「不增加」。本批 -1 是 **DTZ005**：原来 `datetime.now()` 在「策略块」「课表块」各写一遍，DRY 后只剩 `_build_policy_check_time` 一处。**定因手法**：把备份文件 `cp` 到**与被改文件同目录**（保证配置作用域一致）改名后跑 ruff，两边输出排序 diff 再逐条对规则计数。
4. ⚠️ **harness 里 `str()` 出的桩对象带内存地址** → before/after 假差异。正解：dump 后 `re.sub(r"0x[0-9a-fA-F]+", "0xADDR", raw)` 归一化，比手工过滤字段更稳且对任何路径都生效。
5. ⚠️ **按行号切片的尾部边界是开区间，最易把「待替换行」顺手切进来**。本批两次踩到：`lines[211:215]` 把要删的 `check_time = None` 带进来（重复赋值）；`lines[163:179]` 把 `return` 带进来，后面又追加一次 `return True`（产生不可达语句）。**判据：拼装后新方法里出现语义重复的相邻行 = b 多算 1。**
6. ⚠️ **Read 的行号直接拿去写断言必须逐条复核**。本轮 3 个脚本共 4 处断言因差 1 行或缩进误判失败（`not_found` 在 46 而非 47、`download_name` 在 138 而非 139、`if not user` 在 147 而非 148、`check_time = None` 缩进 24 而非 20）。**低成本做法**：先跑一段脚本把「所有待断言行号」的真实内容 `repr` 打印出来，再一次性写对断言。
7. ⚠️ **把「提前 return 的权限校验」抽成 helper 时，必须显式补 `return None` 兜底**：原实现是 `if _admin and ...:` 包住内部 return，抽出后漏掉兜底会让主方法把 `None` 当错误响应返回。消费端统一写 `if scope_error is not None: return scope_error`。
8. ✅ **类内新增方法没有「class 被提前终止」风险**（只有**模块级** def 不能插在 class 中间）。`handle_unlock_message` 非类末方法，故新方法插在其后即可；模块级 helper 才需要双插入点。

### 复核口径变更（重要）

**MEMORY.md 的 ruff 基线已重新校准**：唯一口径 = `ruff check apps/backend`（**含 tests/scripts/tools/migrations**），当前 **默认 2258 / C901 63**；旧记录 2261/67 已废（历史批次改动顺带消掉 2 处，本轮复核时发现并校准）。

### 工作区

未 commit（按规范等用户确认）。`git status --short -- apps` = 29 个已改文件（26 个历史批次 + 本轮 report_routes / mqtt_message_service / export_routes），**无未跟踪残留**（探针文件已删）。
---

## #114 子批 22-24：records_routes 双 post + score_rule_service.import_rules（C901 63→60）

### 收口明细
| # | 函数 | 原 c | 文件行尾 | 手法 | 等价证明 |
|---|---|---|---|---|---|
| 22 | `api/scores/records_routes.py::ScoreEntryResource.post` | 19 | LF | 抽 `_resolve_score_entry_change` / `_compute_rank_change` / `_notify_rank_change` / `_notify_score_change` / `_invalidate_score_caches` / `_recalc_composite_score`（模块级，插在 class 之前） | 路由 harness 全打桩，**27 例**逐字节一致 |
| 23 | `api/scores/records_routes.py::BatchScoreEntryResource.post` | 19 | LF | 抽 `_resolve_batch_entry_rule` / `_build_batch_record` / `_validate_batch_entry` / `_recalc_composite_scores_after_batch` | 同上 27 例（同一次快照覆盖两个 post） |
| 24 | `services/score_rule_service.py::import_rules` | 19 | LF | 抽 `_validate_rule_name` / `_validate_rule_score` / `_validate_rule_category_id` / `_validate_rule_non_negative_int`（daily_limit 与 min_interval 同构 DRY）/ `_build_rule_import_failure` / `_build_rule_import_system_failure` / `_build_rule_from_row` | 打桩 ScoreRule.query / db.session / get_by_id，**25 例**逐字节一致 |

### 关键坑（本轮新增，已入 skill §3 末条 + §7.8/§7.9）
1. ⚠️ **「同函数内条件赋值的局部变量」抽取后异常位置会漂移**：原 `if 排名变化: from api.monitoring.mqtt_routes import publish_mqtt` 使该名为**局部变量**，未绑定时下方引用抛 `UnboundLocalError: cannot access local variable 'publish_mqtt' where it is not associated with a value`，且抛点在**引用处**（其前已执行的 `get_by_id` / `is_notification_allowed` 副作用都发生过）。首版我改成「参数 + 开头 `raise NameError`」→ 快照 diff 出现 5 例：异常文案不同（`NameError` vs `UnboundLocalError`）+ `ctc`/`get_by_id` 观测缺项（抛点提前）。**正解**：helper 收 `publish_mqtt_fn`，内部 `if publish_mqtt_fn is not None: publish_mqtt = publish_mqtt_fn`，把未绑定引用留在原位置 —— 类型/文案/位置三者自动一致且与 Python 版本无关。
2. ⚠️ **既有 `except` 体内再抛异常要当契约保留**：`_build_rule_import_system_failure` 内 `rule_data.get("name")` 对非 dict 输入会二次抛 `AttributeError` **逃出函数**（harness `not_dict` 用例），orchestrator 的 `except` 盖不住 except 体自身的再抛。
3. ⚠️ **harness 导入 api/** 前须设足长密钥三件套**：`JWT_SECRET_KEY` / `FLASK_SECRET_KEY` / `SECRET_KEY` 各 ≥32 字节（否则 load_config 直接 `sys.exit`，表现为 EXIT=1 但 stderr 全是配置报告）。
4. ⚠️ **harness 输出不要走 stdout**：app 初始化会往 stdout 打索引优化报告 → 污染 JSON。改为脚本内写 `argv[2]` 指定的文件。
5. ✅ 行号切片「非替换行」也要成对保留：本次反复用 `lines[492:494]`（空行+注释）模式，把注释与空行从原件搬进新块，避免丢注释。

### 验证一致项
- `C901`：63 → **60**（3 函数齐消）；`ruff check apps/backend` 默认 **2258 不变**（无 I001/RUF059 副作用）。
- 两文件纯 LF 保持（字节计数 `crlf=0`）；`py_compile` 全过；目标文件 C901 归零。
- 针对性 pytest（test_records_routes / test_rules_routes / test_scores_routes / test_rule_engine_service / test_composite_score_service / test_score_recalc）= **70 passed**。
- 全量回归 `scripts/run_regression.sh` = **全部回归通过**（REG_EXIT=0）。
- 工作区 `git status --short -- apps` = **31 个已改文件**（28 历史 + 本轮 3），无未跟踪残留；HEAD 仍 `8b45381`，未 commit。

### 下一批候选（16-20 桶剩余）
`class_service.import_classes`(19) · `mqtt_message_service.handle_score_add`(19) · `course_schedule_routes._parse_course_import_input`(20) · `classes_routes.post`(20) · `user_list_service.get_user_list_view`(20) · `nlp_enhanced_service.extract_entities`(19，NLP 语义敏感建议单评)。

---

## #121 + #122：class_service.import_classes + mqtt_message_service.handle_score_add（C901 60→58）

### #121（已完成，本次前序）class_service.import_classes (c=19→0)
- 二进制安全改写（`.workbuddy/tmp_scan/refactor_class_import.py`）：orchestrator + 4 个模块级 helper（`_HEAD_TEACHER_MISSING` sentinel / `_rule_failed` / `_validate_import_item` / `_resolve_import_relations` / `_process_single_class`）。
- **默认计数漂移定因**：+1 SIM102 → 改 sibling-if 后 +2（位置在 `_validate_import_item` regex 嵌套 if）→ 合并到 if/elif 头后 SIM102 清，却 +4（SIM114×3 + RUF019×1）→ 抽 `_rule_failed` 消 SIM114、sentinel 消 RUF019，回到基线。
- 等价 harness 13/13；test_class_service 通过；全量回归全部通过。

### #122（本次完成）mqtt_message_service.handle_score_add (c=19→0)
- 二进制安全改写（`.workbuddy/tmp_scan/refactor_handle_score_add.py`）：orchestrator（保留幂等回包 + User 未找到 early return + rule_id/rule_name/score_change 分派）+ 4 个类内 helper（`_apply_score_in_session` / `_handle_score_by_rule_id` / `_handle_score_by_rule_name` / `_handle_score_by_score_change`）。
- **默认计数漂移定因（skill §5）**：改写后默认 2258→2259（+1 F401 `models.db` 导入未用）→ 发现 `_apply_score_in_session` 用 `db.session` 却未在该法内导入 `db`（运行时 NameError 隐患）。两处修法成对：`orchestrator` 删未用 `db` 导入、`_apply_score_in_session` 补 `from models import db` → 仍 +1 I001 → ruff 权威写法为**带别名 `SystemConfig as _SysCfg` 单成行在前** + `from models import db` 另起一行（不合并）。最终默认 **2258 不变**。

### 验证一致项（#122）
- py_compile OK；C901 文件 `handle_score_add` 归零；**全后端 C901 58（−1）**；默认 **2258 不变**；文件**纯 LF（CRLF=0）**保真。
- **差分等价 harness**（`.workbuddy/tmp_scan/snap_handle_score_add.py`，切片 bak 原 func + 当前 func/helpers 同次 exec 比对）：**14/14 用例全一致**（幂等/未找到/rule_id 有效+失效+限流+提前返回/rule_name 有效+多匹配+无匹配/score_change 合法+非法/无参/rule_id 优先），发布序列+落库字段+并发重试调用+recalc 调用+会话 add/flush+终态分数逐项相等；SELF_CHECK_OK。
- 针对性 pytest `test_mqtt_message_service.py` = **44 passed**；全量 `scripts/run_regression.sh`（PYTHON_BIN=venv）= **全部回归通过**（REG_EXIT=0；RBAC 一致 / 契约 2 passed / 关键路由 33 passed / 索引齐全；OpenAPI 因后端未运行跳过，非阻塞——本改动仅触内部 MQTT handler，不影响 HTTP 契约面）。

### 工作区
未 commit（按规范等用户确认）。`mqtt_message_service.py` 显示 ` M`；本轮 #114 战役累积改动均在工作区，HEAD 仍 `8b45381`。**改后端须强杀全部 python 再重启**方能在运行实例生效（SocketIO 不 reload）。

### 下一批候选（16-20 桶剩余，#119）
`course_schedule_routes._parse_course_import_input`(20) · `classes_routes.post`(20) · `users_routes.post`(20) · `user_list_service.get_user_list_view`(20) · `nlp_enhanced_service.extract_entities`(19，NLP 语义敏感建议单评) · `multi_intent_detection`(17)。_HIGH 风险（安全校验/DB 迁移语义）宁跳不强推；机械性强者优先。

---

## #119 · user_list_service.get_user_list_view 收口（C901 58→57）

### 完成（本轮）
- 二进制安全改写（`refactor_user_list_view.py`）：orchestrator + 9 个模块级 helper（`_empty_user_list` / `_user_list_item` / `_user_list_result` / `_apply_class_scope` / `_match_pinyin_users` / `_handle_pinyin_search` / `_apply_user_keyword` / `_apply_user_score_range` / `_apply_user_sort`）。
- **关键细节（零行为变更）**：
  - 原实现把 `is_pinyin` 算在 `if search:` 内、新版本提到顶部 `is_pinyin = bool(search and search.lower().isascii() and search.lower().isalpha())`；对 `not search` 路径 keyword 分支 `not is_pinyin` 与原 `not (search and ...)` 等价（search=None 时两者均 False→不拦截 keyword）。
  - `_apply_class_scope` 保留原「`allowed_classes==[]` 空结果提前返回」「class_id 越界」「class_name 不在允许集」「非 admin 按 class_name.in_ 过滤」四个分支语义；`None` 分支仅 `class_id` 时 `filter(class_info_id==class_id)`，与原一致。
  - 缓存写入位置（拼音分支 `get_cache_service().set(..., ttl=300, tags=["users"])` 与主分支）严格保真；空结果路径不写缓存。

### 验证（全绿）
- py_compile OK；目标文件 C901 归零；**全后端 C901 57（−1）**；默认 **2258 不变**；文件**纯 LF（CRLF=0）**保真（仅预存 I001@10，bak 同样存在 → 非本批引入）。
- **差分等价 harness**（`.workbuddy/tmp_scan/snap_user_list_view.py`，切片 bak 原 func + 当前 9 helper+orchestrator 同次 `exec` 比对）：**15/15 用例全一致**（superadmin 全量/按班级/中文搜索/积分区间/按分降序/按时间升序、非 admin 三态权限/班级越界、拼音"zhao"、卡号"c001"、关键词"张"、拼音无匹配）+ SELF_CHECK_OK（total=5/缓存 1 次/pages=1）。
  - ⚠️ 踩坑：① `_UserMeta`/`_ClassMeta` 未挂 `FakeColumn` 属性（`name`/`class_name`/`card_id`/`phone`/`current_score`/`created_at`/`class_info_id`/`id`）→ AttributeError；② `FakeColumn` 不支持 `|`（OR）组合 → 引入 `_Expr` 对象（`__or__`/`__and__`），`_eval` 递归求值；③ 共享 `FakeQuery` 在多次 `run` 间累积 `filter/order` → 每次 `run` 前重置 `_UserMeta.query`/`_ClassMeta.query` 为新 `FakeQuery`；④ `slice_to_eof` 切 `_empty_user_list` 取新版 9 helper + orchestrator（须从首个 helper 起切，否则漏 orchestrator）。
  - 注：`pinyin_search`(search="zhao") 两版均返回 0 —— 这是原实现**既有行为**（纯字母触发 `card_id/phone LIKE "%zhao%"` 预筛，无命中即空），非回归；等价 harness 仅证两版一致，不改此行为。
- 针对性 pytest（`test_users_routes.py` + `test_user_management_routes.py`）= **17 passed**。
- 全量 `scripts/run_regression.sh`（**绝对** PYTHON_BIN=venv，相对路径会被脚本中途 `cd apps/backend` 破坏）= **全部回归通过（REG_EXIT=0）**：RBAC 一致（权限目录空跳过）/ 契约 2 passed / 关键路由 33 passed / 索引齐全；OpenAPI 因后端未起跳过（非阻塞）。
  - ⚠️ **纪律补充**：run_regression.sh 必须用**绝对路径** `PYTHON_BIN`，否则中途 `cd` 后相对路径失效 → pytest 步骤 `No such file or directory` 假失败。

### 工作区
未 commit（按规范等用户确认）。`user_list_service.py` 显示 ` M`；#114 战役累积改动持续堆在工作区，HEAD 仍 `8b45381`。

### 下一批候选（#119 剩余，16-20 桶）
`course_schedule_routes._parse_course_import_input`(20) · `classes_routes.post`(20) · `users_routes.post`(20) · `nlp_enhanced_service.extract_entities`(19) · `multi_intent_detection`(17)。`users_routes.post` 含 c=45/@829 与 c=25/@489 两处超 20 桶（归 #120 21+ 桶），安全/DB 语义高风险宁跳不强推。

---

## #119 续 · course_schedule_routes._parse_course_import_input 收口（C901 57→56）

### 完成（本轮）
- 二进制安全改写（`refactor_parse_course_import.py`，备份 `course_schedule_routes.bak.py`）：模块级 orchestrator（c≈4）+ 4 个模块级 helper。
  - `_parse_course_import_multipart(field_mappings, default_values)`：文件存在/格式校验（`file not in request.files` / 空 filename / 非 xlsx|xls → `APIResponse.bad_request`），通过则委 `_parse_excel_file`。
  - `_parse_excel_file(file, ...)`：openpyxl 读表头→col_map→逐行 `_build_mapped_item`→ `class_name & subject_name` 双非空才 append。
  - `_build_mapped_item(row_data, ...)`：字段映射循环，**`source_val is None` 且 `required` → `break`**（保留原早退语义，partial dict 在 `_parse_excel_file` 里因缺 class_name/subject_name 不被 append）。
  - `_convert_field_value(source_val, target_field, field_type)`：boolean/integer/day_of_week(`_DAY_TEXT_MAP`)/else 类型链（用 `if/if/if/return` 兜底等价原 `if/elif/elif/else`）。
- orchestrator：`multipart/form-data` → 调 multipart helper，`isinstance(result, APIResponse)` 早退；`application/json` → `request.json["data"]`；else → 不支持格式错误。与原返回语义（list 或 APIResponse）逐字一致。
- **首次脚本报错（自纠）**：`NEW_BLOCK = ("..." + NL 隐式续行缺 `+`) 触发 SyntaxError` → 改用 `NL.join(list_of_lines) + NL` 列表拼接，干净保 CRLF。⚠️ 教训：括号内多行字符串拼接若每行已 `+ NL` 收尾，下一行必须以 `+` 起头，否则缺逗号/运算符即 SyntaxError——**多行字符串拼接优先用 list+join**。

### 验证（全绿）
- py_compile OK；目标文件 C901 归零（`_parse_course_import_input` 消失，文件内仅余无关 `_validate_course_import_item`(22)/`post`(16)）；**全后端 C901 56（−1）**；默认 **2258 不变**；文件**纯 CRLF（crlf 1171→1186）**保真（增量=4 helper 间 2 空行）。
- **差分等价 harness**（`.workbuddy/tmp_scan/snap_parse_course_import.py`，切片 bak 原 func + 当前 4 helper+orchestrator 同次 `exec`）：**11/11 用例全一致**（multipart 无文件/空文件名/xlsx 2 行有效/break 跳过缺必填行/缺选填字段走 default/xls 后缀/bad csv 后缀、json 有效/缺 data key/data 为 None、其它 content-type）+ SELF_CHECK_OK（2 行有效→day_of_week 0/period_number int 1/elective True/floor "3"；break 行被跳过剩 1 行）。
  - ⚠️ harness bug 自纠：`mp_no_filename` 用了 `FakeFile("x.xlsx")`（非空文件名）误入 xlsx 路径需 worksheet → 改 `FakeFile("")` 触发 `not file.filename` 早退。属测试桩构造错误，非代码问题。
- 针对性 pytest `test_course_schedule_routes.py` = **9 passed**。
- 全量 `scripts/run_regression.sh`（**绝对** PYTHON_BIN=venv）= **全部回归通过（REG_EXIT=0）**：RBAC 一致 / 契约 2 passed / 关键路由 33 passed / 索引齐全；OpenAPI 因后端未起跳过（非阻塞）。

### 工作区
未 commit（按规范等用户确认）。`course_schedule_routes.py` 显示 ` M`；#114 战役累积改动持续堆在工作区，HEAD 仍 `8b45381`。

### 下一批候选（#119 剩余，16-20 桶）
`classes_routes.post`(20) · `users_routes.post`(20) · `nlp_enhanced_service.extract_entities`(19) · `multi_intent_detection`(17)。`users_routes.post` 含 c=45/@829 与 c=25/@489 两处超 20 桶（归 #120 21+ 桶），安全/DB 语义高风险宁跳不强推；NLP 两函数语义敏感建议单评。
