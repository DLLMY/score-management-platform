# 2026-09-19 工作日志

## D 战役启动（综合评估建议项 10 条全推进）
执行序（用户拍板）：M2(修复部署阻断#1) → M1/L2/L3/H1(代码/报告) → H2/H3/L1(文档) → M3/M4(大特性, 先基础设施+高频页分批)。
任务编号：#269 M2 / #270 L2 / #271 M1 / #272 L1 / #273 L3 / #274 H1 / #275 H2 / #276 H3 / #277 M3 / #278 M4。

## #269 D-M2 免鉴权健康检查端点（已收官）
- 根因：评估报告「部署阻断#1 健康检查路径不匹配」= Dockerfile `HEALTHCHECK curl -f http://localhost:5000/api/health` 探活路径不存在；实际路由是 `/api/system/health`（带 `@requires_permission("system.view")`，无令牌返 401），容器持续重启。
- 新增 `apps/backend/api/system/health_routes.py`：`health_bp`（普通 Blueprint，幂等注册于 `app/api_versioning.py::register_v1_routes`，模式同 download_bp/migration_bp）。
  - 路由：`/api/health`、`/api/healthz`、`/healthz`（三个独立视图函数，消除 route_registry 同名 endpoint 误报）。
  - 免鉴权（无 `@requires_permission`），DB 探活 `db.engine.connect() + SELECT 1`（复用 `_check_database_health` 模式），返回 `{status,db,timestamp}`（含 success/code/message 使全局 ResponseMiddleware 跳过二次包裹），健康 200 / DB 不可达 503。
- 验证：4 项单测（`tests/test_health_endpoint.py`）全过；真实 app 启动冒烟确认 `/api/health`→200 且 `data.status=ok`，`/api/system/health` 无令牌仍 401（鉴权未被放开）；ruff 新文件零新增。
- 提交：随本批 D-M2 提交并 push origin main。

## #270 D-L2 开发环境一键配置脚本（已收官）
- 新增仓库根 `setup_dev.py`（跨平台 onboarding 脚本，区别于 `ops/infra/one_click_deploy.py` 生产部署）。
- 三支柱：① 依赖安装（后端 .venv + `pip install -r requirements.txt` [+`-ml.txt`]；前端 `npm install`，`--install` 才联网）；② `.env` 模板复制（后端/前端 `.env.example`→`.env`，已存在跳过）；③ DB 初始化（`create_app(lightweight=False)` + `db.create_all()` 建表 + `init_admin.py` 种子）。
- 幂等 + 可跳过单步（`--skip-*`）；`--check` 仅体检不改动；结尾 `os._exit(0)` 避免非守护调度线程挂起。
- 验证：py_compile 通过；ruff 新文件零新增；`--help` 正常。不联网执行 install（留给用户环境）。

## #271 D-M1 日志自动归档（代码已落地，待全量回归+提交）
- 根因/缺口：后端日志此前仅用 `RotatingFileHandler` 按尺寸轮转（app.log/error.log/security*.log），
  但轮转产物为裸文件、从不压缩、从不清理 → 长期运行磁盘只增不减。
- 新增 `apps/backend/utils/log_archiver.py`：
  - `GzipRotatingFileHandler`（重写 `rotation_filename`/`rotate`，轮转瞬间把旧日志压缩为 `<base>.N.gz`，
    归档收拢到 `<日志目录>/archive` 子目录；公开 API 与 `RotatingFileHandler` 完全一致，零回归替换）；
  - `enforce_retention`（删超期 .gz，默认 30 天）；`archive_existing_rotated`（把历史遗留裸 `*.log.N` 补压为 .gz）；
  - `setup_log_archiving(app)` 幂等启动钩子（导入即补压一次 + 生产环境注册 APScheduler 每日清理，开发/测试仅打 info）。
- 接入：
  - `utils/logger.py` 与 `utils/security_audit.py` 的 5 处 `RotatingFileHandler` 全部替换为 `GzipRotatingFileHandler`；
  - `config.py` 新增 `LOG_RETENTION_DAYS=30` / `LOG_ARCHIVE_SUBDIR='archive'`；
  - `app/__init__.py::create_app` 末尾调用 `setup_log_archiving(app)`；
  - 新增运维脚本 `scripts/archive_logs.py`（手动补压 + 清理，支持 `--retention-days`/`--dry-run`）；
  - 新增回归测试 `tests/test_log_archiver.py`（Gzip 轮转产物带 .gz / enforce_retention 删超期 / 历史补压）。
- 实现中修复的 2 个真实 bug（测试暴露）：① `enforce_retention` 误用 `os.getmtime` → `os.path.getmtime`；
  ② `GzipRotatingFileHandler` 的 `archive_dir` 原本硬编码全局 `LOG_DIR`，改为派生自 handler 自身 `filename` 目录。
- 验证闸门：py_compile 通过；ruff 新文件零新增（基线 BLE001/DTZ005/I001 等不动）；
  定向 pytest（test_log_archiver + test_logger）= 20 passed / 0 failed；全量回归 `apps/backend/tests` 后台运行中（待 RC=0 后提交并 push origin main）。
- 注意：切换 Gzip 轮转后，既有 `logs/` 下裸 `*.log.N` 由 `setup_log_archiving` 启动补压 + 运维脚本兜底，无需手工迁移。

## 事故：git 仓库损坏与恢复（D-M1 全量回归隔离时踩坑）
- 起因：为隔离 D-M1 是否引入回归，尝试 `git stash push -u` 暂存 7 个 D-M1 文件；该 python 子进程被沙箱 SIGTERM 打断。
- 后果：`git stash` 中断把 `.git/refs` 整个目录删掉 + `.git/objects/pack` 下 `.pack` 数据文件丢失（仅剩 `.idx`）+ 近期提交松散对象（45ef720/1f3d7ec 等前缀目录）消失 → git 报 `not a git repository` / `bad object HEAD`。
- 恢复：① 重建 `.git/refs/heads` 与 `refs/remotes/origin`；② 把 `refs/heads/main` 与 `refs/remotes/origin/main` 松引用写回真正本地 tip `45ef72009384398214a10969a8de713f1f4b78f4`（依据 `.git/logs/HEAD` 与 push 记录）；③ 删 orphan `.idx` 与 `multi-pack-index`；④ `git fetch origin`（M2/1f3d7ec、L2/45ef720 均已 push）重新拉回对象库 → 全恢复，`status` 精确显示 7 个 D-M1 改动。
- **铁律：本仓库禁止再通过被 shim 破坏的 bash 跑任何 git 写操作（stash/commit/reset）。git 命令一律用 `os.chdir(REPO)+subprocess.run(['git','--git-dir=.git','--work-tree=.',...])` 二进制直跑；commit/push 走专门的 `.py` 脚本。**

## 全量回归中发现的 2+1 个【既有】失败（与 D-M1 无关）
- `tests/test_api_contract.py` 2 失败：`exams.import POST /api/exams/import` 后端无此路由（后端实为 `/api/exam-import/execute` 等）。纯前端/后端契约缺口，D-M1 未碰 exam 路由或前端。属预存在，修复须用户定夺端点。
- `tests/test_system_routes.py` 1 个 300s 超时：海量 `mqtt_service.py:58 log_task` 线程并发 `logger.error` 全卡在 logging Handler 共享锁（`self.lock.acquire()`），属遗留锁顺序问题。`GzipRotatingFileHandler` 与 `RotatingFileHandler` 的 emit/锁语义一致，D-M1 未改变该行为 → 非 D-M1 引入。

## #271 D-M1 收官（已提交并推送）
- commit `222b56a`（`45ef720..222b56a main -> main`），8 文件 +353/−7；`ls-remote` 远端 HEAD 已同步。
- 验证闸门全部达成：py_compile 通过；ruff 新文件零新增；定向 pytest（test_log_archiver+test_logger）=20 passed；test_system_routes 单独 14 passed。
- 全量 `pytest apps/backend/tests` 单批次跑出现 2 类【既有】异常（非 D-M1 引入）：test_api_contract 2 失败（/api/exams/import 路由缺口）+ test_system_routes 1 个 300s 超时（mqtt 日志锁跨测试累积争用，单独跑全绿）。已分别判定与本变更无关。

## 处理 2 个既有失败（D-M1 全量回归暴露）
- **失败1** test_api_contract 2 失败（exams.import 契约孤儿）：前端 `apps/frontend/src/services/api.ts:5380` 的 `exams.import` 默认端点 `url || '/api/exams/import'` 后端无路由；真实端点为 `POST /api/exam-import/execute`（exam_import_routes.py:156，FormData: file+exam_id，返回 success_count/errors）。已改默认值为 `'/api/exam-import/execute'`（纯修默认，零行为破坏）。静态契约测试已通过。
- **失败2** test_system_routes 全量跑 1 个 300s 超时（死锁）：根因=`services/mqtt_service.py::log_mqtt_message_async` 每次发布起一个守护线程并占用一个 DB 连接，全量单进程测试下数百线程耗尽 SQLAlchemy 连接池（pool_size=20+max_overflow=40=60），与日志锁形成死锁（请求线程拿不到连接、日志线程拿不到锁）。修复：模块级 `threading.Semaphore(5)` 限流并发 DB 操作到很小值，连接池永不被 MQTT 日志线程耗尽。
- 验证：`test_mqtt_service + test_system_routes + test_api_contract` 同进程跑 = **27 passed / 18.21s / RC=0**（此前同场景 300s 超时）；单独 test_api_contract = RC=0。全量 `pytest apps/backend/tests` 后台运行中确认零回归。

## #277 D-M3 i18n 国际化（已提交并推送）
- 基础设施：`i18next@^23` + `react-i18next@^14`；新增 `src/i18n/`（index.ts 初始化 + localStorage 记忆 + languageChanged 回写、locales/zh-CN.ts、locales/en-US.ts 1:1 键对齐）；`index.tsx` 副作用挂载。
- 高频页 Login：Login.tsx 5 处错误文案 + LoginView.tsx ~20 处标签/占位符/标题/强制改密弹窗 `t()`，加原生 `<select>` 语言切换器。
- 修复 2 处既有回归（阻塞闸门，非 M3 引入）：8 个 T12 拆分漏建 barrel（components/hooks 无 index.ts 致 TS2307）；ImportExportPanel.tsx prettier 不合规。
- 登录测试改 import ./i18n 并强制 zh-CN，消除 3 处占位符断言失败。
- 四闸门全绿；commit `09ead06`（19 文件）已 push origin main，`ls-remote` 远端 HEAD 同步。

## #278 D-M4 移动端响应式（已收官，待提交推送）
- 基础设施（补齐响应式 Hook，此前仅有一次性读取的 mobileUtils）：新增 `src/hooks/useMediaQuery.ts`（SSR 安全、监听 matchMedia change 重渲染）+ `src/hooks/useIsMobile.ts`（断点 (max-width:767px) 与 Sidebar 抽屉断点同源），均加入 `hooks/index.ts` barrel。
- 高频页 DashboardView.tsx 真实移动端修复：顶栏窄屏堆叠（`flex-col sm:flex-row` 替代 `justify-between` 防横向溢出）+ 右簇 `flex-wrap gap-2 sm:gap-4` + 标题 `min-w-0`；页面内边距 `p-6`→`p-4 sm:p-6`；统计卡片栅格 `grid-cols-2 lg:grid-cols-4`（手机/平板 2 列、桌面 4 列）。
- 四闸门全绿：tsc 0 error / eslint 0 error（4 个既有 warning）/ prettier 全绿 / vitest 276 passed 3 skipped。
- 注意：布局外壳(Sidebar/Header)此前已用 Tailwind 断点类做基础响应式，M4 不重复造轮子，仅补 JS 层 Hook + 修 Dashboard 真实溢出点。
- **M4 收口 = D 战役 10/10 全部完成**（M1/M2/L2/L3/H1/H2/H3/L1/M3/M4）。

## #279 后端全量回归复盘（D 战役零回归判定）
- **拆穿假信号**：早先 `FULL_RC=2` 实为 bash shim 破坏 `cd`（`cd: null directory`）→ `&&` 短路 → pytest 没跑；改用 Python 子进程（显式 cwd）重跑。
- **真信号（用 Python 子进程跑，正确方式）**：`collected 2113 items`；进度到 94% 时因 **conftest app fixture 累积后台线程（performance_monitor/log_performance_summary 多个 `time.sleep(300)`、APScheduler、config-watcher、cleanup_loop）导致 werkzeug 路由 compile 死锁 → 300s Timeout 中断**，RC=1 且未打印 summary。
  - 根因=一次性全量跑触发累积死锁；印证记忆铁律「勿用 -n 4 xdist；**串行 3 批分跑**」。
- **D 战役范围（health/log_archiver/mqtt/exam-import）测试全绿** → D 战役（M3/M4 纯前端）零回归达成。
- **2 个稳定历史回归（非 D 战役引入，是更早战役的遗留）**：
  1. `test_devices_routes::TestDeviceWriteEndpoints::test_create_device_missing_device_id_returns_error` → `Failed: DID NOT RAISE IntegrityError`。根因 commit `4eaf431`（设备/管理员创建 UNIQUE 冲突转 400）改异常处理，把 device_id 缺失的约束异常吞掉/转 400，测试期望的 IntegrityError 不再抛出。**引入点 4eaf431，非 D 战役**。
  2. `test_rule_engine_service::TestRuleMatcher::test_match_rules_below_threshold` → `assert [] == [{'rule':...}]`（低于阈值的规则没被过滤）。根因 commit `cb474b0`（C901 收官：rule engine 抽取）改变阈值比较逻辑；典型「C901 抽取破坏边界断言」场景。**引入点 cb474b0，非 D 战役**。
- `test_nlp_performance` 原 2 个 F **复跑全绿（7 passed）** → 确认为 flaky 假失败（记忆已知）。
- 结论：D 战役零回归 ✅；但整库有 2 个历史遗留后端失败 + 1 个 timeout 基础设施问题（应按铁律分批跑避免），需在后续「历史回归清理」任务中修复。

## #280 T4 综合分重算异步化（收口完成，已提交推送）
- 评估结论：异步基础设施早已预置（`services/score_recalc.enqueue_or_recalc_user_score` 异步优先/同步兜底 + `tasks/score_tasks.recalc_user_score` Celery 任务带重试），且全部写路径（records_routes:227/665、approvals_routes:371、score_record_orchestration:270/359、mqtt_message_service:787、nlp_enhanced_service:3523）均已收口到 `enqueue_or_recalc_user_score`，契约未变（响应信封 + `composite_score` 状态标记）。
- **唯一缺口**：`CELERY_ASYNC_SCORE_RECALC` 开关从未被任何配置/env 设置 → 生产也一直走同步回退，T4 目标「写路径仅入队 + 延迟不随用户数线性增长」从未真正生效（开关休眠）。
- 收口动作（零回归、默认关闭）：
  1. `config.py` 新增 `CELERY_ASYNC_SCORE_RECALC = os.getenv("CELERY_ASYNC_SCORE_RECALC","False").lower()=="true"`（默认 False，env 可开启）。
  2. `app/config_init.py` 显式 `app.config["CELERY_ASYNC_SCORE_RECALC"] = config.CELERY_ASYNC_SCORE_RECALC`（init_config 非 blanket from_object，不显式注入不生效）。
  3. 新增 `tests/test_score_recalc_async_on.py` 闸门：开关开启时写路径仅 `.delay()` 入队、绝不同步重算。
- 验证：T4 相关 15 用例全绿（含新闸门）；records/approvals 路由 26 用例全绿；两配置文件 py_compile OK 且 CRLF 守恒（bare_lf=0）。
- 提交 `b1546d6` 已 push origin main，`ls-remote` 远端 HEAD 同步。
- ⚠️ 生产启用 T4 异步需用户显式在部署 env 置 `CELERY_ASYNC_SCORE_RECALC=true` 且确保 Celery worker（queue=score）+ broker 已起；未起 worker 时自动回退同步（不静默漂移）。此开关为部署决策，未默认开启。

## #281 T4 正式点亮（env 接线，已提交推送）
- 用户"开始推进"→ 把开关补进 `.env` 模板并正式点亮。先行核验 `score_recalc.enqueue_or_recalc_user_score`：`.delay()` 异常被 `except Exception` 捕获→同步回退，broker 不可达**绝不静默丢重算**（仅连接延迟）。
- `run.py:35` 关键事实：`START_CELERY` 在生产 env 默认 `"1"`（worker 随 run.py 自动起、监听 score 队列）→ 生产置 true 即真正异步。
- 三文件插入（纯 LF，crlf=0 守恒）：
  - `.env.example` / `.env.development` → `CELERY_ASYNC_SCORE_RECALC=false`（安全默认 + 注释说明 worker 前提）。
  - `.env.production` → `CELERY_ASYNC_SCORE_RECALC=true`（注释标明 run.py 生产默认 START_CELERY=1）。
  - 校验 `load_dotenv` 在 config.py/config_init.py/run.py 均生效，env 会被读入 `os.environ`，`config.py` 的 `os.getenv(...)` 正确取到。
- 提交 `e825226` 已 push origin main，`ls-remote` 远端 HEAD 同步。diff 中 2 deletions 实为两模板文件末行补 `\n`（原无 EOF 换行），无原有行被删。
- T4 闸门复跑 15 passed / RC=0，零回归。T4 全链路（代码接线 b1546d6 + 部署点亮 e825226）**正式收官**。

## #282 T1–T12 全量收口审计（下一步开发计划-20260824 全部关闭）
- 用户"需要"→ 按优先级核下一项，发现 `docs/下一步开发计划-20260824.md` 的 **T1–T12 已全部收口**（不止 T4）：
  - **T1** 综合分重算不静默失败 ✅（历史）；**T2** 前端测试补强 ✅（37 测试文件 / 276 passed·3 skipped / RC=0，测试文件均已 commit）；**T3** 列表强制分页 ✅（历史）；**T4** 异步化 ✅（本会话 b1546d6+e825226）；**T5** any→0 ✅（全仓 `any` 类型用法 grep 0 处，api.ts 仅剩 T5 规划注释）；**T6** security fail-closed ✅（历史）；**T7** ML 解耦 ✅（requirements.txt：catboost/gensim 已移除，torch/transformers/xgboost/lightgbm/shap 迁 requirements-ml.txt）；**T8** 死依赖清理 ✅（package.json 无 web-vitals、无 eslintConfig react-app 残留；"配置单一 Config 类收敛"仍属 §2.6 余债未强求）；**T9** 大路由瘦身 ✅（records_routes 1050→687、algorithm_routes 1242→45；78b05db 抽 11 编排函数到 score_record_orchestration、fbee156 algorithm 拆 6 part；②③ 待 run_regression 复跑终证）；**T10** CRA eslint ✅（package.json 无 eslintConfig）；**T11** 静默失败页 ✅（FrontendTelemetry/SecurityAudit 已有错误态+重试，全仓 `return null` 静默失败已无）；**T12** 巨型页拆分 ✅（历史）。
- 结论：该计划文档 12 项任务**全部 done**。剩余非 T 余债（§2.6，低优先）：config.py/config_init.py/security.py 配置源分散、后端少量 `except:pass` 日志化、~13 低频模型无 to_dict。
- 下一步方向待用户定：① 对 §2.6 余债小批次收口；② 重新取证扫描当前代码库新债（文档为 08-24，期间经历 D 战役/F17/06 差异等大量改动）；③ 用户另有新优先级。前端 tsc+eslint 全绿认证已后台启动（task zXsAIE）。

## #283 重新扫描刷新计划（用户选 ②）
- 用户"2"→ 选②重新取证扫描。实测扫描 `scan_debt_20260919.py`（结果 `scan_debt_20260919_result.json`）产出 `docs/下一步开发计划-20260919.md` 取代旧 08-24 文档。
- 实测关键数字：
  - 前端：src 文件 485；≥500 行大文件 **24**（api.ts 6445 / types/index.ts 1614 / Sidebar 939 / App 853 / ImportExportPanel 803 / Header 798 / stores/index 605 …）；`any` 类型用法 **0**；`return null` 53（绝大多数为合法早返回/条件渲染，**非** T11 静默失败，不批量处理）；`eslint-disable` 53；console 残留 3；测试文件 32（vitest 实测 276 passed·3 skipped）。
  - 后端：py 547；≥300 行路由 **25**（nlp_routes 1636 / devices_routes 1429 / course_schedule 1223 / users_routes 1207 / system_routes 1139 …）；`silent except:pass` **222 但生产仅 9 处**（其余在 tests/）；`bare except` 生产 2（api_versioning:171,175）；`TODO/FIXME` 1（utils/validation.py:265）；**路由内联 dict 序列化 866 处**（B3 to_dict 残量）；**83 模型中 32 缺 to_dict**；config getenv 散落（config.py 56 + config_init 8）；**F17 路由写路径残留 0**（全收口）；**C901 生产 0**（唯一 1 处在 test_api_contract.py）。
- 新计划债务分级：P0 四闸门守卫；P1 = D1(B3 to_dict 收尾 32 模型/866 处) + D2(超大路由拆分续作,T9 残量)；P2 = D3(silent except→logger 生产 9 处) + D4(配置源收敛) + D5(bare except 2 处)；P3 = D6(console3/todo1/eslint-disable 跟踪递减)。伪债甄别段：return null 53 / any 0 / F17 写路径 0 / C901 生产 0 均已收敛，不排期。
- 推荐推进顺序：D3/D5/D6（低风险清理）→ D1（B3 收尾小步）→ D4（配置收敛）→ D2（超大路由拆分，最后、需 plan+分批）。

## #284 D3 生产 silent except:pass 日志化（代码已落地，待提交）
- 范围收窄：222 处 silent except 中 213 在 tests/ 不排期；生产 9 处里 4 处为合法控制流兜底（`utils/datetime_utils.py:61,68` 日期格式 trial loop、`services/nlp_enhanced_service.py:1928` 实体跳过、`startup/run.py:17` stdout reconfigure `AttributeError`）**不改**（改则刷屏告警）。实改 **5 处结构型 best-effort 块**：
  - `apps/backend/api/student/student_routes.py:376` MQTT 下发兜底 → `logger.warning(f"下发解锁指令到 phonebox/unlock/{box} 失败: {e}")`（`logger=logging.getLogger(__name__)` 已定义）。
  - `apps/backend/app/service_init.py:232` `finally` db.session.remove 兜底 → `log_warning(f"清理 db.session 失败(已忽略): {e}")`。
  - `apps/backend/services/system_config_service.py` 顶部补 `from utils.logger import log_warning`（原文件无 logger import，避免 ImportError）+ `update_config` 缓存失效 try → `log_warning(f"设备白名单缓存失效失败(已忽略): {e}")`（`# pragma: no cover` 保留）。
  - `apps/backend/tasks/scheduler.py:71,87` 两处 `finally` db.session.remove 兜底 → `log_warning(...)`。
- 字节级脚本改写（`_d3_edit.py`：先逐文件核验 old 字节计数==expected 再 replace），CRLF 守恒（service_init crlf=360、scheduler crlf=94 不变；LF 文件仅 +1 import 行）。控制流语义 100% 保留（仍是 best-effort，不 re-raise）。
- **D5 / D6 经实测确认为伪债（不改动）**：
  - D5 生产 2 处 `app/api_versioning.py:171,175` 实测已是 `except Exception as e: logger.warning(...)`（注释「此前 except:pass 吞掉冲突」已记录历史修复），零裸 `except:`。
  - D6 `console.*` 3 处均合法基础设施（`utils/logger.ts` console-bridge、`errorMonitor.ts` console hook、`README.md` 示例）；`utils/validation.py:265` 的 "TODO" 实为 MAC 正则 `r"^([0-9A-Fa-f]{4}\.){2}[0-9A-Fa-f]{4}$"` 注释，无真 TODO/FIXME。
- 验证闸门：py_compile 4 文件 OK；定向 pytest `test_student_endpoints.py` **24 passed**(68.56s) + `test_student_insights.py` **6 passed**(24.70s)；全局 `pytest --collect-only` **2114 collected / RC=0**（证无导入错误/循环依赖，尤其 system_config_service 新 import）。EOL 守恒已核验。
- 提交：待用户确认（计划 §四「不自动 commit，须用户确认」）。建议随批提交 4 后端文件 + `docs/下一步开发计划-20260919.md` + 本内存日志，push origin main 后 `ls-remote` 核实。
