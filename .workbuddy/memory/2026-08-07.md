# 2026-08-07 工作日志

- 清理 3 个未注册监控蓝图死代码（用户：「直接删除无用死代码」）：
  - 删除 `backend/api/monitoring/logs_routes.py`(logs_bp)、`mqtt_monitor_routes.py`(mqtt_monitor_bp)、`websocket_routes.py`(ws_bp)，及 legacy 副本 `backend/routes/logs_routes.py`、`routes/websocket_routes.py`（备份 /tmp/deadcode_bak/）。这些蓝图从未被 `api_versioning.py::register_v1_routes` 注册（生产长期 404），且 `api/monitoring/__init__.py` 的 `__all__` 仅悬空列出、从未 import。
  - 修复 `api/monitoring/__init__.py` `__all__`：移除 3 个悬空项（logs_bp/mqtt_monitor_bp/ws_bp），仅留 ns_notifications/ns_alerts/ns_operation_logs/ns_mqtt。
  - 验证：后端重启无 import/traceback；`api.monitoring` 正常导入（__all__ 已清理）；`/api/mqtt/status` 仍 200（由已注册 ns_mqtt 提供，非死蓝图）；`/api/logs/error`、`/ws/rooms` 正确 404；48 passed（rank+student+reports+teacher_tools+envelope）。
  - 结论：前端 `/api/mqtt/status` 等调用完全由 ns_mqtt 支撑，删除死蓝图零影响。如需启用这些监控端点须显式补到 register_v1_routes（try/except 包裹）。
  - 按约定未 git 提交。

- 继续清理死代码（用户：「扫一遍其他潜在死代码（legacy routes/ 整包、prediction/anomaly/risk/rule/composite）」）：
  - **5 个重复命名空间路由文件删除**：`api/prediction_routes.py`(ns_prediction)、`api/anomaly_routes.py`(ns_anomaly)、`api/risk_routes.py`(ns_risk)、`api/rule_routes.py`(ns_rule)、`api/composite_routes.py`(ns_composite)。它们从未在 `api_versioning.py` 注册（仅 211-215 行注释占位），无测试/前端/__init__ 引用；功能 100% 被已注册的 `ns_algorithm`（/algorithm/prediction、/algorithm/anomaly、/algorithm/risk-predict、/algorithm/rule-recommend、/algorithm/composite-score、/algorithm/warning）覆盖。删除 `api_versioning.py` 第209-215 行过时注释块。
  - **整包删除 legacy `backend/routes/`**：37 个文件，全仓库零外部 import（仅包内互相 import），确为旧 blueprint 风格镜像，生产长期未注册。删除前已 `tar czf /tmp/legacy_bak/routes_legacy_*.tar.gz` 备份。前端无调用这些路径。
  - 注意：一次 Bash 双执行导致 5 个 api 文件被删但每文件 cp 备份失败；所幸项目有 git 仓库（`git status` 显示 ` D api/*.py`，可 `git checkout` 恢复），routes/ 另有 tar 备份，均安全可恢复。
  - 验证：`create_app()` OK（913 routes，无断链）；重启后端 swagger 仍 451 路径、ns_algorithm 子集全在、5 个独立命名空间已 GONE、users/student/login/rank/class 仍存活；48 passed（rank+student+reports+teacher_tools+envelope）。
  - 按约定未 git 提交。

- 扩大死代码扫描到 services/ 层（用户：「扩大扫描到 services/ 层（部分 service 可能仅被已删路由引用而变为孤儿）」）：
  - 用 AST import 分析（非 grep）遍历全部 68 个 services/ 模块，交叉核对「已删路由 import 的 service」vs「当前活跃 importer」。
  - **结论：删除操作未产生任何孤儿 service**。已删路由（routes/ 整包、5 个 algorithm 重复 ns、3 个监控蓝图）import 的 20 个 service 全部仍有活跃消费者（api/algorithm_routes.py + ns_algorithm、各 api/* 活路由、mqtt_manager/mqtt_service/websocket_service 等）。已删文件是活路由的 legacy 镜像，删除安全。
  - **但发现 9 个既存孤儿**（与本次删除无关，删除前已死）：仅被测试引用或完全无引用——`lightweight_model_service`、`score_rule_service`、`mqtt`(包)、`mqtt/mqtt_cache`、`mqtt/mqtt_client`、`mqtt/mqtt_interface`(**完全无引用，连测试都没有，最强孤儿**)、`mqtt/mqtt_message_client`、`mqtt/mqtt_message_processor`、`mqtt/mqtt_queue`。这些是旧 MQTT 实现被 `mqtt_service`/`mqtt_manager` 取代后的遗留，外加两个独立模块。
  - 未删除这 9 个既存孤儿（超出「已删路由」范围，且含测试引用），需用户另行确认是否清理（连同其测试一并删）。
  - 扫描用的临时文件 `_svc_scan.py`/`_deleted_scan/` 已清理，仓库无残留。

- 按用户确认「全部删除(含测试)」清理 9 个 services/ 既存孤儿：
  - 删除整个 `services/mqtt/` 子包（__init__.py + mqtt_cache/mqtt_client/mqtt_interface/mqtt_message_client/mqtt_message_processor，共 6 文件）——旧 MQTT 实现，已被顶层 `mqtt_service.py`/`mqtt_manager.py`/`mqtt_message_service.py`/`mqtt_management_service.py` 取代。
  - 删除顶层孤儿 `services/mqtt_queue.py`、`services/lightweight_model_service.py`、`services/score_rule_service.py`。
  - 删除配套测试：`test_mqtt_cache/test_mqtt_client/test_mqtt_message_client/test_mqtt_message_processor/test_mqtt_queue/test_lightweight_model_service/test_score_rule_service.py`（7 个）。
  - 编辑 `tests/test_api_integration.py`：移除模块级 `from services.mqtt.mqtt_client import MQTTClient` 的 try/except 块 + 删除 `test_mqtt_client_initialization` 方法（引用已删模块）。
  - 编辑 `di/__init__.py`：移除悬空 DI 注册 `score_rule_service = providers.Singleton("services.score_rule_service.ScoreRuleService")`（惰性字符串引用，启动不报错但指向已删模块）。
  - 验证：`create_app()` OK（918 routes，无断链）；grep 确认无活跃代码 import 已删模块（monitoring_service 的 `mqtt_queue_size` 指标名、mqtt_service 的 `mqtt_client = mqtt_manager` 别名均非 import，保留）；前序 108 passed（api_integration + 3 个保留 mqtt 测试 + envelope/student/reports/teacher_tools）。
  - 复扫：services/ 模块 68→59，ORPHAN 0。仓库无临时残留。
  - 按约定未 git 提交。

- 提交与推送（用户：「现在可以提交git」→「需要，一并操作」）：
  - `git add -A` 后 `git restore --staged` 排除两个 `api-docs/openapi.json.bak_*` 备份（第三个 `...180545` 原本被 git 跟踪，单独提交删除 `a06c6b7`）。
  - 提交 `0d0f811` chore: 清理死代码并补齐 API 契约与功能模块（92 文件 +4154/−16995）；随后 `a06c6b7` 删备份。
  - `git push origin main` 成功（099d999..a06c6b7），工作树干净。

- 全项目评估（用户：「现在评估整个项目」，10:25）：
  - 规模：后端 433 .py / 98,668 LOC；前端 179 .tsx/.ts / 63,674 LOC；git 107 commits（2026-08 月 6 commits）。README/Dockerfile/docker-compose/CI 齐备。
  - 架构分层基本健康，双 JWT 隔离真实生效，ORM 全覆盖无注入风险，信封统一。死代码清理到位（已删模块零残留引用）。
  - **新发现（已逐项验证，写入 MEMORY.md「代码健康待办」）**：
    1. CI 当前红：`.github/workflows/ci.yml:54-55` 跑已删的 `test_mqtt_message_processor.py`/`test_mqtt_client.py`（实际 `test_mqtt_message_service.py`/`test_mqtt_service.py`）。
    2. 未注册裸 Blueprint：`api/system/migration_routes.py`(migration_bp→/api/migration/*)、`version_routes.py`(version_bp→/api/version) 零 import → 生产 404（同已删监控蓝图病）。
    3. security.py 潜伏 NameError：`get_request_data`/`get_request_param`(L410/428) 用 `request` 但顶部无 `from flask import request`；当前零调用方，属坏死代码。
    4. 前端死代码：`services/api.js`(32KB) 0 引用 + 孤儿页 ClassAssignment.js/MQTTDebug.js/UserManagement.js/ClassTimeSettings.tsx.bak；vite extensions 顺序一改会静默切回旧实现（高危）。
    5. utils/ 8 个孤儿（batch_operations/cache_middleware/data_sync_events/db_pool_manager/exception_handler/monitoring_service/performance/swagger_config；initializer 有 1 importer 待确认）。
    6. 前端信封拆包重复 3 处（api.ts L613/680/719）只判 success/data 忽略 code；`test_api_envelope.py` 对缺 success 端点仅 warning 不 fail。
    7. 仓库噪音：instance/ 20+ `.db.bak_*` + corrupt/rebuilt 库；backend/ 根散落 verify_algo*.py、test_*.py、11 张 PNG → 建议归档 scripts/archive + .gitignore。
  - 已顺手清理超限的 MEMORY.md（合并去重 + 写入上述待办），本日志未丢失。

- 按建议修复评估发现的代码健康问题（用户：「按照建议修复」，10:40）：
  1. **CI 红已修**：`ci.yml` MQTT 测试改为实际存在的 `tests/test_mqtt_message_service.py tests/test_mqtt_service.py`（原引用的 message_processor/client 已于 services 清理时删除，故 CI 永久失败）。
  2. **裸 Blueprint 注册**：`register_v1_routes` 末尾 `app.register_blueprint(migration_bp)` + `version_bp`（try/except 包裹）。冲突：version_bp 的 `/api/versions` 与 `api_versioning.py:45` 的 `api_version_manager` 同名路由重复 → 删除 version_bp 的 `list_versions` 路由（保留 version_manager 权威实现）。`/api/version`、`/api/v1/compatibility`、`/api/migration/*` 现生产可用；create_app 926 routes，check_route_duplicates 无重复。
  3. **security.py 补 import**：顶部加 `from flask import request`（`get_request_data`/`get_request_param` 原属坏死代码，调之必 NameError，现可调用）。
  4. **前端死代码删除**：`services/api.js` + 4 孤儿页（ClassAssignment.js/MQTTDebug.js/UserManagement.js/ClassTimeSettings.tsx.bak）。已用 AST + vite `resolve.extensions`(`.tsx,.ts,.jsx,.js`) 确认 api.js 因 .ts 优先实际由 api.ts 解析（rbacApi.ts 的 `import {request} from './api'` 命中 api.ts 的 `request` 导出），删除安全。
  5. **utils/ 9 孤儿删除**：batch_operations/cache_middleware/data_sync_events/db_pool_manager/exception_handler/monitoring_service/performance/swagger_config/initializer（AST 扫描确认活跃+测试零引用；`utils/__init__.py` 未导出它们；`performance_monitor` 是另一模块未动）。
  6. **信封拆包 DRY**：api.ts 抽出 `unwrapEnvelope(rawData, skipDataExtract)` 替代 3 处内联（L612/679/717），行为不变；`code` 增强判定因担心行为回退暂未加（后端业务失败均置 success:false，前端已识别）。
  - 验证：前端 build ✓、eslint 0 error（317 console warning 既有）、vitest 149 passed/3 skipped；后端 RBAC --check-only 一致 OK；pytest 子集（envelope+2 mqtt+student+reports+teacher_tools）跑中。
  - 仓库噪音项（instance/.db.bak_*、根散落脚本/PNG）未处理，保留在 MEMORY.md 待办。

- 后期功能端到端可用性验证（用户：「功能是否完整可用」，11:00）：
  - 启动临时后端（端口 5001，因 5000 被一个无法从当前 shell 结束的旧进程占用）+ 前端 3000 代理 5001（`REACT_APP_API_URL` 环境变量，未改 vite.config）。
  - **后端学生 API 全链路实测（python urllib 直连 5001）**：登录 200（双因子 card_id+name）、me/score/records/notifications/leaves/rank 全 200；请假 POST 201 创建成功；手机箱 POST 返回 403 `allowed:false,decision:defer`（该班无策略，正常业务拦截）；学生 token 打 admin /api/users → 401（双 JWT 隔离生效）。注：库里仅 id=1(陈洋) is_active=0（数据状态，非 bug），92 个学生 is_active=1 可登录。
  - **前端 Playwright 实跑（chromium）**：
    - 学生端：首测发现 **StudentPortal.tsx TDZ 崩溃** `Cannot access 'loadMyRank' before initialization`（loadMyRank useCallback 定义在引用它的 useEffect 之后，const 暂时性死区），ErrorBoundary 捕获→整页「页面加载失败」白屏。**从写完起一直不可用**，vitest(149)/build/lint 无 StudentPortal 组件测试故未覆盖。
    - **已修复**：将 `loadMyRank` 的 useCallback 移到 useEffect 之前（StudentPortal.tsx L54）。重跑 PASS：登录→Portal→积分/通知/请假/手机箱/排名 5 Tab 全渲染、0 console error、请假提交无报错。
    - TeacherTools（教师效率工具/批量录入成绩）、SemesterReport（班级学期报告导出）实跑均完整渲染、无崩溃、无 console error。
  - 临时验证文件（e2e_*.mjs / *.png / *_run_5001.log）已清理；仅 StudentPortal.tsx 一处修复落地。
  - 结论：**所有后期功能（学生端/老师效率/学期报告/排行榜）现已完整可用**，学生端白屏 bug 是关键回归，已修复。
  - 后端 5001 + 前端 3000 验证实例仍在运行（非标准端口，因 5000 旧进程无法结束）。

- 提交并推送（用户：「按照建议操作」，11:22）：
  - 提交 `6899752` fix: 评估修复(CI/蓝图/安全/死代码) + 修复学生端白屏（22 文件，+121/−5046）：含 ci.yml MQTT 测试引用修正、api_versioning 注册 migration/version 蓝图、删 version_routes 冗余 list_versions、security.py 补 import、删 utils 9 孤儿、删前端 api.js+4 孤儿页、api.ts 抽 unwrapEnvelope、StudentPortal.tsx 前移 loadMyRank 修复 TDZ 白屏。pre-commit 验证（Python 语法）通过。
  - **push 成功**：用户开启代理后 `git push origin main` 成功（`a06c6b7..6899752 main -> main`），远程 main 已更新；评估修复 + 学生端白屏修复全部同步至 GitHub。

- 前后端算法契约一致性评估（用户：「现在再次评估所有算法是否和前端出发配合正确」，晚）：
  - **评估范围（四模块对照 api.ts ↔ backend/api/algorithm_routes.py + 各 service）**：
    1. NLP 模块：32 个方法，路由/字段/枚举全对。
    2. 手机箱策略模块：4 路由（list/create/update/delete）全对。
    3. 分析排名模块：无 404、字段对齐。
    4. 算法路由层：55 个方法全部有后端路由对应。
  - **结论**：路由层与字段层整体对齐；唯一隐患是 **3 组单用户预测接口"假契约"**——前端直接 `as Promise<XxxResult>` 断言，但后端返回结构与前端类型不一致，缺 normalize 兜底（batch 接口此前已补，单用户漏了）。
    - prediction/<id>：后端 `predicted_scores`(数组)/`trend`(rising|falling|stable|insufficient_data)/`current_score`/`confidence`，无 `predicted_score`/`name`。
    - score-predict/<id>：后端 `predicted_score`/`features.current_score`/`confidence`/`class_name`，无 `subject`/`trend`。
    - anomaly/<id> + sudden|trend|group/<id>：后端 `anomalies[]`/`anomaly_type`/`details`/`score_change`/`date` 或 `{has_anomaly,...}`，结构与 `AnomalyResult` 不一致。
    - risk-predict/<id>：后端 `overall_risk_level`/`overall_risk_score`/`risk_factors[]`/`recommended_actions`，无 `risk_level`/`risk_score`/`contributing_factors`。
  - **方案（用户选"前端补 normalize(推荐)"）**：api.ts 新增 7 个单对象 normalize 函数（normalizeUserPrediction/ScorePredict/RiskPredict/Anomaly/SuddenChange/TrendAnomaly/GroupAnomaly，复用 TREND_MAP/toNumSafe），并改 7 个单用户方法（getPrediction/getUserAnomaly/getSuddenChange/getTrendAnomaly/getGroupAnomaly/getScorePredict/getRiskPredict）改调之。
  - **为何线上未崩**：AlgorithmAnalysis.tsx 页面只消费 batch 接口（已有 `?? 0`/`|| 'stable'` 兜底），单用户接口属潜在隐患，未触发白屏。
  - 提交 `e8b3aab` fix(frontend): 单用户算法接口补 normalize 兜底，对齐后端返回字段（136 insertions/+，14 deletions/−），已 push（`9c87cee..e8b3aab`，origin/main 同步）。前端 `tsc --noEmit` api.ts 相关 0 错误。
  - 待办：`.workbuddy/skills/` 仍 untracked（含 backend-reallink-smoke 等沉淀 skill），按约定待用户确认是否入库。
