# 三目录清理验证报告（backend / frontend / deploy）

> 验证方式：静态只读分析（Glob/Grep/pyflakes 抽样 + 交叉复核关键断言），**未修改/删除任何文件**
> 验证时间：2026-08-18 ｜ 分支：`refactor/F17`

## 一、总览

| 目录 | 清理状态 | 结论摘要 |
|------|----------|----------|
| `backend/` | ⚠️ **基本完成（遗留代码质量类问题）** | 无用文件层面 0 残留；但发现 2 处真实运行时 bug + ~40 处未用导入 + API 层调试 print |
| `frontend/` | ❌ **未完成（5 个死 .js + 2 处悬空引用）** | 5 个 `.js` 文件确认零引用可删；1 处 `alert` 待改 Toast；1 处 icon 悬空引用 |
| `deploy/` | ❌ **未完成（失效引用 + 配置漂移 + 离线依赖断裂）** | 4 处脚本引用不存在的 `.bat/.ps1`；ngrok.exe 缺失；config.json 与文档漂移 |

---

## 二、backend/ 检查结果

### 1) 不再被引用的文件
- ✅ 死命名模式（`.bak/_old/_backup/_tmp/.archive/.orig/~/_disabled/_copy/archive/dead_code/unused`）：**0 命中**
- ✅ 孤儿模块抽样（textcnn/bert/wol/frontend_telemetry/engagement/unlock_validator/class_time_checker/data_consistency_checker）：均被非测试代码引用，**无孤儿**
- ✅ `backend/backups/`（616MB）、`migration_backups/`：`git check-ignore` 已确认被 `.gitignore` 忽略、`git ls-files` 0 跟踪，**仅磁盘占用、无需处理**
- ✅ 根目录 `app.py` / `sweep_endpoints.py` / `token.txt` 已清除，无密钥/证书残留

### 2) 未使用的函数/类/变量/导入
- 🔴 **真实 bug ①（已交叉验证）**：`api/data/import_export_routes.py:841,844` 使用 `os.path.basename`，但文件头部导入区**没有 `import os`** → `DELETE /api/import-export/backup/<filename>` 端点运行时 `NameError`（S8 路径穿越修复引入）
- 🔴 **真实 bug ②（已交叉验证）**：`migrations/add_foreign_keys.py:6-7`、`add_rbac.py:7`、`fix_database_schema.py:3` 存在**无 `#` 前缀的中文裸行**（`-course_schedule表…`、`- 为admin表…`）→ 若执行将 SyntaxError/NameError（遗留迁移脚本，未被运行应用导入）
- 🟡 pyflakes 全仓：约 **40 处 `imported but unused`**，集中于 api 路由层：`phonebox_policy_routes.py:13`、`approvals_routes.py:4`、`rules_routes.py:3,624`、`admins_routes.py:5,18`、`admin_notifications_routes.py:11`、`security_routes.py:3`、`rbac_routes.py:3,33`、`sub_accounts_routes.py:3,5,7`、`users_routes.py:1,31,983`、`user_management_routes.py:3`、`diagnostics_routes.py:2`、`import_routes.py:3`
- 🟡 未用局部变量：`admin_classes_routes.py:60,107`、`import_export_routes.py:521/664/763`、`scheduled_notify_routes.py:239,312`、`rbac_routes.py:299,455`、`admins_routes.py:460`
- ✅ `pdb`/`breakpoint()`：0
- ⚠️ `config.py:395` 的 `__all__` 未定义变量为动态生成，属 pyflakes 误报

### 3) 注释掉的死代码 / 调试残留
- ✅ 无大段 `# def/class` 注释死代码
- 🟡 API 层调试 `print` 遍布（建议改 logging），样例：
  - `api/monitoring/mqtt_routes.py` — `print("=== MQTT connect API called ===")`
  - `api/nlp/nlp_routes.py`、`api/scores/records_routes.py`、`api/scores/scheduled_notify_routes.py`、`api/data/export_routes.py`、`api/users/users_routes.py` 各有调试 print
  - （启动/配置类 print：service_init/db_init/config_loader 可接受，保留）
- ✅ TODO/FIXME/HACK/XXX 实质 0（仅 `utils/validation.py:264` 正则内 `XXXX` 误匹配）

### 4) 不再使用的配置项/依赖/资源
- ✅ `requirements.txt` 重依赖（torch/transformers/sklearn/xgboost/lightgbm/catboost/shap/gensim/reportlab/xlsxwriter/prometheus_client/celery 等）经 grep 均有 import，无明显未用依赖
- ✅ 无 `.key/.pem/secret/token` 残留

---

## 三、frontend/ 检查结果

### 1) 不再被引用的文件
- ✅ 死命名模式（`.bak/_old/_backup/_tmp/*.tmp/*.orig/~/*/archive/dead_code/unused/*.disabled`）：**0 命中**
- 🔴 **5 个遗留 `.js` 确认零引用（已交叉验证 `grep` 全 src 无其它文件 import）**，均可删除：
  | 文件 | 原因 |
  |------|------|
  | `src/hooks/usePerformance.js` | 已被同名 `usePerformance.ts` 取代，`hooks/index.ts` 仅 re-export `.ts` |
  | `src/hooks/useWebSocket.js` | 全仓无 import，功能由 `stores/useWebSocketStore` + `WebSocketContext.jsx` 取代 |
  | `src/stores/asyncStore.js` | 全仓无 import，被 `stores/index.ts` 取代（含 console.warn） |
  | `src/utils/toast.js` | `useToast` 全仓无 import，被 `ToastContext.tsx` 取代 |
  | `src/setupTests.js` | CRA/Jest 遗留，项目用 vitest + `test-setup.ts` |
- ✅ 保留（仍被引用）：`utils/webVitals.js`（`index.tsx:5` 引用）、`services/__tests__/api.test.js`（vitest 用例）→ 建议后续转 `.ts`
- ✅ 63 个页面均在 `App.tsx` 懒加载注册，无孤儿页面

### 2) 未使用的函数/类/变量/导入
- ✅ ESLint `no-unused-vars` 已清零（exit 0）
- 🟡 含 `eslint-disable` 共 17 文件 / 约 28 处，**全部是 `exhaustive-deps` 与 `no-explicit-any`**，无 unused 压制；密集于 `NLPManagement.tsx:350,392,425…`（9 处）
- ✅ `@ts-ignore`/`@ts-nocheck`：0（仅 `test-setup.ts:84` 合法 `@ts-expect-error`）

### 3) 注释掉的死代码 / 调试残留
- ✅ `debugger`：0；无裸 `console.log`（仅 `utils/logger.ts:5-9` 封装、`asyncStore.js:98/110` 待删文件内）
- 🟡 `pages/Analysis.tsx:424` `window.alert('暂无数据可导出…')` → 建议改 Toast
- ✅ 无整段功能被注释（仅 `ImportExportPanel.tsx:765` 一处说明性注释）

### 4) 不再使用的配置项/依赖/资源
- ✅ 运行时依赖（antd/recharts/socket.io-client/web-vitals/zustand/lucide-react/react-router-dom）均被 import；无未用依赖
- 🟡 **悬空引用**：`index.html:20` 引用 `/icons/icon-152x152.png`，但 `public/` 中**该文件缺失**（补图或移除 `<link>`）
- 🟡 孤立资源：`public/docs/MQTT协议说明.md`、`public/docs/系统使用指南.md` 未被页面引用（疑似孤立文档）

---

## 四、deploy/ 检查结果

### 1) 不再被引用的文件
- ✅ 无备份/旧版脚本副本
- 🔴 **4 处脚本引用不存在的文件（已交叉验证目标均不存在）**：
  | 引用处 | 缺失目标 |
  |--------|----------|
  | `check_deploy.py:140,219` | `一键部署.bat` |
  | `service_manager.py:518` | `install_dependencies.bat` |
  | `start_server.bat:43` | `install_env.ps1` |
  | `server_deploy.ps1:86,99` | `deploy.bat` |
- 🟡 孤立文件：`redis/redis.msi`（安装器残留，无脚本引用）、`redis/redis-cli.exe`（仅 server 被调用）、`ngrok/ngrok.yml.example`（模板未引用）
- 🔴 **离线依赖断裂**：`ngrok/` 下仅 zip（`ngrok-v3-stable-windows-amd64.zip`）+ yml + start.bat，**无 `ngrok.exe`**（zip 未解压即启动会失败）

### 2) 未使用的函数/类/变量
- ✅ 4 个 `.py` 全部函数可达（`one_click_deploy.py` 的 `start_ngrok`/`check_redis_status`、`service_manager.py` 各方法、`.ps1` 的 `Build-Frontend`/`Download-NSSM` 均被调用），无失效 GOTO/标签
- ⚠️ 澄清：`one_click_deploy.py:312/330/342` 的 `from app import create_app`、`FLASK_APP=app` 指向 `backend/app` 包，**仍有效，勿误删**

### 3) 注释掉的死代码 / 调试残留
- ✅ .py 无注释掉的 def/import/os.system 块，无 pdb；print 均为正常输出
- 🔴 **安全残留**：`one_click_deploy.py:181` 明文打印默认密码；`:256/270/289` 硬编码 FLASK/JWT/CSRF 开发密钥写入 .env 模板
- ✅ .bat/.ps1 无整段 REM/:: 注释掉的命令

### 4) 不再使用的配置项/依赖/资源
- 🟡 `config.json` 未用字段（grep 无引用）：`project_name:2`、`production:14-18`（backend_host/backend_workers/frontend_build_path）、`development:19-22`
- 🔴 **配置双向漂移**：`CONFIG_GUIDE.md:14,20,23,202,209` 写 `redis_port`/`startup_delay.redis`/`server_mode`/`laptop_mode`，真实 `config.json` 均无；反之 production/development 字段文档未提
- 🔴 **文档间失效链接**：`README.md`/`快速开始.md` 引 `DEPLOYMENT_UPDATE_SUMMARY.md`；`BRANCH_STRATEGY.md:413`、`RELEASE_GUIDE.md:365` 引 `CICD_GUIDE.md` 与 `../DEVELOPMENT_GUIDELINES.md`（均不存在）；`DEPLOYMENT_GUIDE.md:239` 引 `backend/backup_db.py`（不存在）

---

## 五、汇总：仍需清理的内容清单

### P0 — 真实运行时 bug（建议立即修）
1. `backend/api/data/import_export_routes.py:841,844` — 补 `import os`（当前该端点删除备份必崩）
2. `backend/migrations/add_foreign_keys.py:6-7`、`add_rbac.py:7`、`fix_database_schema.py:3` — 中文裸行补 `#`（或删除废弃迁移脚本）

### P1 — 可直接删除的死文件（已确认零引用）
3. `frontend/src/hooks/usePerformance.js`、`useWebSocket.js`
4. `frontend/src/stores/asyncStore.js`、`frontend/src/utils/toast.js`、`frontend/src/setupTests.js`
5. `deploy/redis/redis.msi`、`deploy/redis/redis-cli.exe`、`deploy/ngrok/ngrok.yml.example`（确认无引用后）
6. `frontend/public/docs/*.md`（确认无页面引用后）或移入 docs/

### P2 — 失效引用修正（不改则部署向导误导）
7. `deploy/check_deploy.py:140,219`、`service_manager.py:518`、`start_server.bat:43`、`server_deploy.ps1:86,99` — 改指真实入口（`one_click_deploy.py`/`deploy.ps1`/`start_server.bat`/`stop_all.bat`）或删提示
8. `deploy/ngrok/` — 补解压 `ngrok.exe`（或改由 download_deps.py 拉取）
9. `frontend/index.html:20` — 补 `icon-152x152.png` 或移除 `<link>`
10. 9 个 .md 中指向不存在文档的链接（`CICD_GUIDE.md`/`DEPLOYMENT_UPDATE_SUMMARY.md`/`backend/backup_db.py` 等 5+ 处）统一修正

### P3 — 代码质量与配置对齐（不阻塞，建议排期）
11. backend ~40 处 `imported but unused`（api/*_routes.py，pyflakes 可一键修）+ 9 处未用局部变量
12. backend API 层调试 `print` → `logging`（mqtt_routes/nlp_routes/scores/*_routes 等 6 文件）
13. `frontend/pages/Analysis.tsx:424` `alert` → Toast
14. `deploy/config.json` 与 `CONFIG_GUIDE.md` 对齐（删未用字段或补文档）；`one_click_deploy.py:181/256/270/289` 明文密码/硬编码密钥弱化
15. `frontend` 17 文件 28 处 `eslint-disable`（exhaustive-deps/any）择机收敛；7 个 `.js`（5 删 2 转 .ts）

### ✅ 已确认无需处理
- `backend/backups/`（616MB）与 `migration_backups/` 已 gitignore，未跟踪，仅磁盘占用
- `deploy/ngrok/*.zip`、`redis/redis-server.exe/redis.windows.conf` 为离线部署依赖，被脚本引用，保留
- 无密钥/证书/token 残留；requirements 无未用依赖
