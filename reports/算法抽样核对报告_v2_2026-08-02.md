# 算法端点抽样核对报告 v2（风险分层触发 + 预测服务缺陷修复）

- 日期：2026-08-02
- 项目：`管理平台设计`（Flask + React 学生积分管理平台）
- 依据：v1 报告收尾建议——“下一步若需触发风险 `high` 层级，建议补导入**出勤记录**与**多时点成绩趋势**数据”
- 本轮结论：**high 层级已成功触发（12 人）**；在补数据过程中另发现并修复 `prediction_service.py` 的 **3 个真实缺陷**（其中 1 个会导致前端页面崩溃，1 个会让预测值膨胀到 10¹⁰ 量级）。

---

## 1. 关键澄清：风险模型不读 `attendance` 表

v1 报告建议“补导入出勤记录”，本轮通读 `services/risk_predict_service.py` 后确认该建议的前提不成立，需要修正：

`RiskPredictService.get_risk_features`（第 37 行起）的数据源**只有 `ScoreRecord` 与 `User` 两张表，全程没有查询 `attendance` 表**。所谓“出勤维度”是一个**代理指标（proxy）**，由行为记录的密度与波动率推算：

| `detect_attendance_risk` 判据（第 319 行起） | 加分 | 实际含义 |
|---|---|---|
| `daily_record_count < 0.3` | +0.4 | 行为记录过于稀疏 |
| `daily_record_count < 0.5` | +0.2 | 行为记录偏少 |
| `score_volatility > 5` | +0.2 | 积分波动剧烈 |
| `total_records == 0` | +0.3 | 30 天窗口内完全无记录 |

**因此，往 `attendance` 表灌数据对风险分层没有任何影响。** 正确做法是构造具备特定统计特征的 `score_record` 时序。这是本轮方案与 v1 建议的实质差异，也是必须先纠正的认知偏差。

三个维度的加权口径（`predict_risk`，第 385 行）：

```
overall = 0.4×academic + 0.35×behavior + 0.25×attendance
overall ≥ 0.7 → high；≥ 0.4 → medium；否则 low
```

要触发 `high`，需要学业与行为两个高权重维度同时接近饱和。

---

## 2. 数据补充：分层构造行为时序

新建脚本 `backend/seed_behavior_v2.py`，按目标层级**反向构造**满足判据阈值的数据，写入 **3,307 行** `score_record`（2,363 行落在 30 天风险窗口内，944 行为窗口外的解锁记录）。

| 队列 | 计划人数 | 构造手法 | 命中的判据 |
|---|---|---|---|
| high | 12 | 30 天内仅 14 条记录（密度 0.47）；仅 2 条正向；负向在 `[-20,-11]` 与 `[-6,-1]` 间交替 | 密度 < 0.5（行为 +0.15、出勤 +0.2）、波动率 > 5（出勤 +0.2）、`no_positive_days ≥ 7`（+0.3）、`negative_rate > 0.5`（+0.25） |
| medium | 25 | 20 条记录，最后 7 天无正向 | `no_positive_days ≥ 7`（+0.3） |
| low | 63 | 25–29 条记录，85% 为正向 | 全部判据不命中 |

配套两项：

- **当前积分分层**（`pick_current_score`）：high 取 4–18、medium 取 26–46、low 取 52–92。目的是让 high 队列落入 `class_percentile < 30`，触发学业维度 +0.25。
- **解锁记录**（`gen_unlocks`）：high 25–40 条、medium 8–22 条、low 0–6 条，全部放在 31–90 天前。刻意置于风险窗口之外——不干扰风险打分，只驱动综合分的合规维度。

脚本内嵌了 `detect_*` 逻辑的离线复现，提交前先行自校验：预测 high=12 / medium=24 / low=64，仅 1 例（user 4，medium→low）与规划有偏差。所有记录以 `operator='system_seed'` 标记，可通过 `DELETE FROM score_record WHERE operator='system_seed'` 干净回滚。

---

## 3. 发现并修复 3 个 `prediction_service.py` 缺陷

补完数据后 `/api/algorithm/prediction/risk` 仍返回 0 人。排查确认这**不是数据问题，而是三个独立的代码缺陷**。

### 3.1 缺陷一：趋势拟合对象错误（口径错误，导致漏报）

```python
# 修复前
trend = PredictionService.calculate_trend(score_changes)   # 每次变动量序列
# 修复后
trend = PredictionService.calculate_trend(cumulative_scores)  # 累计积分序列
```

`calculate_trend` 用最小二乘拟合斜率。喂给它增量序列，拟合出的是**变动量的加速度**，而非积分本身的升降。实测三组反例：

| 场景 | 修复前判定 | 应为 |
|---|---|---|
| 每天稳定 −5 分 | `stable`，slope = 0 | `falling`，slope ≈ −5 |
| 每天稳定 +3 分 | `stable`，slope = 0 | `rising`，slope ≈ +3 |
| 先跌后稳（净下滑） | **`rising`**，slope = +0.75 | `falling` |

即“持续稳定下滑”这类最该被预警的学生，恰恰被判为 `stable` 而全部漏掉。同文件的 `RiskPredictService.get_risk_features`（第 76–91 行）用的正是累计序列，两处口径原本就不一致——本次修复也统一了口径。

### 3.2 缺陷二：`get_risk_students` 返回值违反前端契约（会导致页面崩溃）

前端 `RiskStudent` 接口（`frontend/src/types/index.ts:678`）要求 `risk_score` / `risk_level` / `warning_count`，但该方法原本只返回 `user_id, name, class_name, current_score, predicted_change`。后果：

- `AlgorithmAnalysis.tsx:973` 调用 `risk_score.toFixed(1)` → **运行时崩溃**（`undefined` 无 `toFixed`）；
- `Analysis.tsx:210-212` 按 `risk_level` 统计高/中/低 → **恒为 0**。

补齐字段，并采用与 `RiskPredictService` 一致的分档：

```python
severity   = abs(slope) / abs(threshold)          # 受过滤条件所限必然 ≥ 1
risk_score = round(min(1.0, 0.7 * severity / 1.2), 2)
# ≥0.7 → high；≥0.4 → medium；否则 low
warning_count = 预测窗口内低于当前积分的天数
```

旁证：仓库中既有用例 `test_get_risk_students_with_risk` 此前一直以 `KeyError: 'risk_level'` 失败，修复后转为通过——说明这是**存量契约缺口**，非本轮引入。

### 3.3 缺陷三：预测序列指数级爆炸

```python
# 修复前
predicted_cumulative = current_score + sum(predicted_scores) + predicted_change
predicted_scores.append(round(predicted_cumulative, 2))
```

`predicted_scores` 存的是**每日累计水平**，却又被 `sum()` 当作增量再加一遍，于是每天翻一倍，整体按约 `2^(n-1)` 膨胀。实测：起点 −100、日均 −3、预测 30 天 → **−5.5×10¹⁰**。

```python
# 修复后：只在前一日水平上叠加当日衰减增量
predicted_cumulative = current_score
for i in range(1, days + 1):
    predicted_cumulative += avg_change * (0.9 ** (i / 7))
    predicted_scores.append(round(predicted_cumulative, 2))
```

同一场景修复后为 **−171.88**，落在理论上界 `current_score + avg_change × days = −190` 之内。置信区间同步改为以区间中点为中心、带宽 `2σ√n`（原写法同样受 `sum()` 污染）。

### 3.4 未发现问题的模块

- `risk_predict_service.py`：斜率本就基于累计序列，口径正确，**无需修改**。
- `score_predict_service.py`：采用带截断的乘性模型，不存在爆炸风险，**无需修改**。

---

## 4. API 端最终验证（三处修复 + 新数据，后端重启后）

### 4.1 综合分 `/api/algorithm/composite-score`

```
recalculate → 200；熵权 behavior=0.4883  academic=0.2597  social=0.2520
GET → 200，返回 100 条
综合分 min=15.2  max=96.9  avg=63.0  std=20.4  NaN=0
有学术分的学生：100/100
```

| 排名 | 姓名 | 综合分 | 学术 | 行为 |
|---|---|---|---|---|
| #1 | 何伟 | 96.89 | 94.65 | 91 |
| #2 | 黄洋 | 92.54 | 88.40 | 89 |
| #3 | 黄军 | 88.64 | 78.89 | 92 |
| #99 | 张静 | 15.64 | 61.22 | 11 |
| #100 | 王伟 | 15.17 | 66.11 | 9 |

末位两名学术分尚可（61/66）但综合分垫底，符合熵权下行为维度权重最高（0.4883）的设定，分布无坍缩。

### 4.2 风险预测 `/api/algorithm/risk-predict/batch`

```
status=200，返回 101 条
层级分布：{'high': 12, 'medium': 25, 'low': 64}
样例：周秀 [high] overall=0.78 | 学业=0.9 行为=0.9 出勤=0.4
```

**high 层级成功触发，实测 12 人与规划完全一致。** 分维度看，学业与行为均达 0.9（接近饱和），出勤代理项 0.4，加权得 `0.4×0.9 + 0.35×0.9 + 0.25×0.4 = 0.775 ≈ 0.78`，手工复算与接口输出吻合。

### 4.3 高危名单 `/api/algorithm/risk-predict/high-risk`

```
status=200，高危学生 12 名，与 batch 口径一致
干预建议正常下发：“关注学习状态，提供学习辅导”
```

### 4.4 趋势预测 `/api/algorithm/prediction/risk`

```
status=200，下降风险学生 12 名（修复前为 0）
层级分布：{'high': 12}
契约字段缺失：无
胡帆 [high] 风险分=1.0  预测变化=-236.17  预警天数=30
周伟 [high] 风险分=1.0  预测变化=-229.33  预警天数=30
赵芳 [high] 风险分=1.0  预测变化=-222.48  预警天数=30
```

三项均达预期：从 0 人恢复为 12 人（缺陷一修复）、契约字段齐全（缺陷二修复）、预测变化回到 −236 这一合理量级而非 −5.5×10¹⁰（缺陷三修复）。

需要说明：此处 12 人 `risk_level` **全为 high**，是因为构造的 high 队列日均扣分达 −20~−11，`severity = |slope|/|threshold|` 远超 1.2 上限而饱和。这是极端造数所致，非分档逻辑缺陷；真实数据下会自然散布。

---

## 5. 回归测试

```
tests/test_prediction_service.py    20 passed
tests/test_risk_predict_service.py  24 passed
────────────────────────────────────────────
                                    44 passed in 7.77s
```

三处修改均未破坏既有断言。已确认无用例对具体预测数值做硬断言（仅校验长度与字段存在性），故缺陷三的修复不涉及用例改写。

**遗留问题（非本轮引入）**：`tests/test_score_predict_service.py` 14 个用例因 `NameError: name 'ScorePredictService' is not defined` 失败，属该测试文件自身的 import 缺失，与本轮改动无关，建议单独处理。

---

## 6. 变更清单

| 文件 | 类型 | 说明 |
|---|---|---|
| `backend/services/prediction_service.py` | 修改 | 3 处缺陷修复：趋势拟合口径、契约字段补齐、预测序列爆炸；均附中文注释说明原因 |
| `backend/seed_behavior_v2.py` | 新增 | 分层行为时序造数脚本，内嵌离线自校验，`operator='system_seed'` 可回滚 |
| `backend/verify_all_algo.py` | 新增 | 四端点一键验证脚本（综合分 / risk-batch / high-risk / prediction-risk），含契约字段缺失检查 |
| `backend/diag_trend.py`、`diag_predict_explode.py` | 已删除 | 临时诊断脚本，完成举证后清理 |

---

## 7. 结论与后续建议

**本轮目标达成**：v1 遗留的“high 层级无法触发”已解决（12 人），且证实其根因中有相当部分并非数据缺失，而是 `prediction_service.py` 的代码缺陷。四个算法端点当前输出均可解释、可手工复算。

后续建议，按优先级：

1. **补 `test_score_predict_service.py` 的 import**，让 14 个用例恢复运行——目前这块是测试盲区。
2. **为 `get_risk_students` 的分档阈值补单测**，覆盖 severity 饱和（本轮已实测到）与临界值场景，防止回归。
3. **考虑让出勤维度真正接入 `attendance` 表**。当前用行为记录密度作代理，语义上是“老师有没有给他记分”，而非“学生有没有到校”，两者相关但不等价；若产品语义要求真实出勤，需改造 `get_risk_features`。
4. **前端加一层防御**。本轮缺陷二能直接把页面打崩，说明前端对后端返回缺乏兜底；建议在 `AlgorithmAnalysis.tsx` 等处对 `risk_score` 等字段做可选链或默认值处理。
