# 项目优化与回归总结（2026-08-06）

> 基于《按改进建议修复》六项改进的完整落地 + 二次评估（73 → 79 分）+ 后续三轮深化。
> 结论先行：**全量测试 0 失败、运行全绿、三套契约校验就位、改进建议全部完成**。

---

## 一、今日改进总览

| # | 改进项 | 优先级 | 状态 | 落地成果 |
|---|---|---|---|---|
| 1 | 补齐 MQTT / 通知 stub（P0） | P0 | ✅ | `tasks/mqtt_tasks.py` 三个 handler 真实实现（复用 `mqtt_message_service` 业务 + celery app context）；`tasks/notification_tasks.py` 真实 MQTT 发布（修 `float("time.time()")` 必崩 bug） |
| 2 | 建立 API 契约（P1） | P1 | ✅ | 后端契约测试 `test_api_envelope.py`（遍历端点 0 5xx + shape 快照）；前端冒烟 `smoke.spec.ts`（8 核心页 0 报错）；OpenAPI 漂移校验 `verify_openapi_contract.py` |
| 3 | 提升前端测试覆盖（P1） | P1 | ✅ | 全量 e2e 21/21 全绿（4 个旧 spec 加固）；8 页冒烟回归 |
| 4 | RBAC 自动 seed + 校验脚本化（P1） | P1 | ✅ | `verify_rbac_consistency.py`（ast 解析 seed 字面量不执行，`--apply` 幂等补齐绝不删） |
| 5 | 清理死代码（P2） | P2 | ✅ | 顶层 `app.py` → 零逻辑占位；遗留标记 6 → 0（真实技术债为 0） |
| 6 | 安全加固（P2） | P2 | ✅ | `created_by` 从 token 取真实责任人；业务 CRUD 操作审计（rules/classes/notify 补全）；CI 配置（含 RBAC 校验步骤） |

## 二、修复清单（本轮累计）

### 真实 500 故障（7 个，全部已修复并验证 200）
| # | 端点 | 根因 |
|---|---|---|
| 1 | `/api/rules/template/download` | `send_file` 未 import → NameError |
| 2 | `/api/users/template/download` | `csv`/`send_file` 未 import → NameError |
| 3 | `/api/diagnostics/cpu` | 调用不存在的 `check_cpu()` |
| 4 | `/api/diagnostics/memory` | 调用不存在的 `check_memory()` |
| 5 | `/api/export/categories` | 调用不存在的 `export_categories_to_excel/pdf()` |
| 6 | `/api/diagnostics/performance` | 慢请求记录含 datetime 不可 jsonify（数据驱动型，仅生产数据触发） |
| 7 | （同 6 类） | `get_slow_requests/get_slow_queries` 源头归一 isoformat |

### 配置 / 契约漂移（3 类）
| 项 | 发现 | 修复 |
|---|---|---|
| RBAC | DB 中 teacher 角色缺 `notification.send`（发通知会 403） | `--apply` 幂等补齐（26→27 权限） |
| OpenAPI 文档 | 快照 441 路径中 125 条过期（28%）：61 新增端点缺失 + 64 旧端点残留 | 实时同步快照（备份保留），复查 0 漂移 |
| Playwright | `dev:vite` 脚本缺失 + 4 个旧 spec flaky（12/9） | 补脚本 + 串行加固 + 断言贴实现事实 → 21/21 |

### 测试套件归零
- 全量 pytest：1622 passed / 11 failed / 6 skipped → **0 失败**（修复 4 个文件的旧契约断言 + 并发类测试基建限制明确 skip）

## 三、当前项目健康状态（2026-08-06 18:08 实测）

| 组件 | 状态 |
|---|---|
| 后端（5000） | ✅ 登录 200，修复端点全 200 |
| MQTT | ✅ connected（10 主题） |
| 前端（3000） | ✅ 200 |
| 后端契约测试 | ✅ 2 passed |
| 关键路由 pytest | ✅ 28 passed |
| RBAC 校验 | ✅ 一致性 OK |
| OpenAPI 校验 | ✅ 0 漂移 |

## 四、回归使用指南（改完代码必跑）

```bash
# 后端回归（RBAC + OpenAPI + 契约测试 + 关键路由）
bash scripts/run_regression.sh

# 追加前端 e2e 冒烟（需后端 5000 已启动）
bash scripts/run_regression.sh --full

# 单项校验
python backend/scripts/verify_rbac_consistency.py --check-only   # RBAC（--apply 幂等补齐）
python backend/scripts/verify_openapi_contract.py                # OpenAPI（--update 同步快照）
python -m pytest tests/test_api_envelope.py -p no:locust         # 契约测试（需系统 Python 3.11）

# 前端全量 e2e（21 项）
cd frontend && npx playwright test --project=chrome --workers=1

# 后端全量 pytest（约 35 分钟，需系统 Python 3.11）
cd backend && python -m pytest -p no:locust --timeout=300
```

## 五、遗留 / 下一步（可选）

| 项 | 说明 |
|---|---|
| CI 实际接入 | `ci.yml` 已就绪（backend 子集 + RBAC + frontend build/lint），需推送到 GitHub 验证 |
| 前端单测 | 10 个 CRA 遗留单测文件为死资产（无框架无引用），可用 vitest 复活或清理 |
| OpenAPI 共享类型 | 文档级契约已就位；如需 TS 共享类型可基于 openapi.json 生成 |

---
*关键约定备忘：后端必须用系统 Python 3.11；改后端后强杀全部 python 重启；禁跑 seed_rbac.py（清库重建），一律走幂等增量脚本；列表端点 data 必须数组；改测试断言前先 grep 前端实现事实。*
