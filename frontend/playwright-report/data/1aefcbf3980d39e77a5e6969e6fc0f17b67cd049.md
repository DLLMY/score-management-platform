# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: permissions.spec.ts >> 权限控制 >> 侧边栏菜单应根据权限显示
- Location: tests\e2e\permissions.spec.ts:22:7

# Error details

```
Test timeout of 30000ms exceeded.
```

```
Error: locator.fill: Test timeout of 30000ms exceeded.
Call log:
  - waiting for getByPlaceholder('请输入用户名')

```

# Page snapshot

```yaml
- generic [ref=e2]:
  - generic [ref=e4]:
    - generic [ref=e5]: ⚠️
    - heading "页面加载失败" [level=2] [ref=e6]
    - paragraph [ref=e7]: 请刷新页面重试
    - button "刷新页面" [ref=e8] [cursor=pointer]
  - button [ref=e9] [cursor=pointer]
```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test';
  2  | 
  3  | test.describe('权限控制', () => {
  4  |   test('未登录用户应重定向到登录页面', async ({ page }) => {
  5  |     await page.goto('/dashboard');
  6  |     await expect(page).toHaveURL(/login/);
  7  |     await expect(page.getByRole('heading', { name: '积分管理平台' })).toBeVisible();
  8  |   });
  9  | 
  10 |   test('登录后应保存用户信息到localStorage', async ({ page }) => {
  11 |     await page.goto('/');
  12 |     await page.getByPlaceholder('请输入用户名').fill('admin');
  13 |     await page.getByPlaceholder('请输入密码').fill('123456');
  14 |     await page.getByRole('button', { name: '登录' }).click();
  15 |     await expect(page.getByRole('navigation')).toBeVisible({ timeout: 15000 });
  16 | 
  17 |     const adminData = await page.evaluate(() => localStorage.getItem('admin'));
  18 |     expect(adminData).toBeTruthy();
  19 |     expect(JSON.parse(adminData!).username).toBe('admin');
  20 |   });
  21 | 
  22 |   test('侧边栏菜单应根据权限显示', async ({ page }) => {
  23 |     await page.goto('/');
> 24 |     await page.getByPlaceholder('请输入用户名').fill('admin');
     |                                           ^ Error: locator.fill: Test timeout of 30000ms exceeded.
  25 |     await page.getByPlaceholder('请输入密码').fill('123456');
  26 |     await page.getByRole('button', { name: '登录' }).click();
  27 |     await expect(page.getByRole('navigation')).toBeVisible({ timeout: 15000 });
  28 | 
  29 |     const sidebar = page.getByRole('navigation');
  30 |     await expect(sidebar.getByText('系统管理')).toBeVisible();
  31 |     await expect(sidebar.getByText('权限管理')).toBeVisible();
  32 |     await expect(sidebar.getByText('操作日志')).toBeVisible();
  33 |   });
  34 | 
  35 |   test('退出登录后应清除localStorage', async ({ page }) => {
  36 |     await page.goto('/');
  37 |     await page.getByPlaceholder('请输入用户名').fill('admin');
  38 |     await page.getByPlaceholder('请输入密码').fill('123456');
  39 |     await page.getByRole('button', { name: '登录' }).click();
  40 |     await expect(page.getByRole('navigation')).toBeVisible({ timeout: 15000 });
  41 | 
  42 |     await page.getByRole('button', { name: /退出/ }).click();
  43 |     await expect(page).toHaveURL(/login/);
  44 |     const adminData = await page.evaluate(() => localStorage.getItem('admin'));
  45 |     expect(adminData).toBeNull();
  46 |   });
  47 | });
  48 | 
```