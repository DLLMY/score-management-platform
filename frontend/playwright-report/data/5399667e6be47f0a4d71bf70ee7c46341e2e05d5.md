# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: login.spec.ts >> 登录页面 >> 页面应正常显示登录表单
- Location: tests\e2e\login.spec.ts:4:7

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: getByRole('heading', { name: '积分管理平台' })
Expected: visible
Timeout: 10000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 10000ms
  - waiting for getByRole('heading', { name: '积分管理平台' })

```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test';
  2  | 
  3  | test.describe('登录页面', () => {
  4  |   test('页面应正常显示登录表单', async ({ page }) => {
  5  |     page.setDefaultTimeout(20000);
  6  |     await page.goto('/', { waitUntil: 'domcontentloaded' });
  7  | 
  8  |     const heading = page.getByRole('heading', { name: '积分管理平台' });
  9  |     const usernameInput = page.getByPlaceholder('请输入用户名');
  10 |     const passwordInput = page.getByPlaceholder('请输入密码');
  11 |     const loginButton = page.getByRole('button', { name: '登录' });
  12 | 
> 13 |     await expect(heading).toBeVisible({ timeout: 10000 });
     |                           ^ Error: expect(locator).toBeVisible() failed
  14 |     await expect(usernameInput).toBeVisible({ timeout: 10000 });
  15 |     await expect(passwordInput).toBeVisible({ timeout: 10000 });
  16 |     await expect(loginButton).toBeVisible({ timeout: 10000 });
  17 |   });
  18 | 
  19 |   test('页面标题应为积分管理平台', async ({ page }) => {
  20 |     page.setDefaultTimeout(20000);
  21 |     await page.goto('/', { waitUntil: 'domcontentloaded' });
  22 |     
  23 |     const title = await page.title();
  24 |     console.log('Page title:', title);
  25 |     
  26 |     expect(title).toBe('积分管理平台');
  27 |   });
  28 | 
  29 |   test('页面应包含用户名输入框', async ({ page }) => {
  30 |     page.setDefaultTimeout(20000);
  31 |     await page.goto('/', { waitUntil: 'domcontentloaded' });
  32 |     
  33 |     const usernameInput = page.getByPlaceholder('请输入用户名');
  34 |     await expect(usernameInput).toBeVisible({ timeout: 10000 });
  35 |     
  36 |     await usernameInput.fill('testuser');
  37 |     const value = await usernameInput.inputValue();
  38 |     console.log('Username input value:', value);
  39 |     
  40 |     expect(value).toBe('testuser');
  41 |   });
  42 | 
  43 |   test('页面应包含密码输入框', async ({ page }) => {
  44 |     page.setDefaultTimeout(20000);
  45 |     await page.goto('/', { waitUntil: 'domcontentloaded' });
  46 |     
  47 |     const passwordInput = page.getByPlaceholder('请输入密码');
  48 |     await expect(passwordInput).toBeVisible({ timeout: 10000 });
  49 |     
  50 |     await passwordInput.fill('testpassword');
  51 |     const value = await passwordInput.inputValue();
  52 |     console.log('Password input value:', value);
  53 |     
  54 |     expect(value).toBe('testpassword');
  55 |   });
  56 | });
```