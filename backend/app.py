# ============================================================
# DEPRECATED / 死代码占位（已清理，勿再添加逻辑）
# ============================================================
#
# 本文件是历史遗留的顶层入口，已被同目录 `app/` 包完全取代：
#   - `run.py` 的 `from app import app` 解析到 `app/__init__.py::get_app()`，
#     永远不会执行本文件（Python 导入时目录包优先于同名 .py 模块）。
#   - MQTT / 调度器等启动逻辑在 `app/service_init.py`，改启动逻辑请改那里。
#
# 本文件仅作占位保留（兼容 FLASK_APP=app.py 等路径引用），不含任何可执行逻辑。
# 请勿在本文件添加任何代码 —— 添加了也不会生效，只会再次误导后人。
# ============================================================
