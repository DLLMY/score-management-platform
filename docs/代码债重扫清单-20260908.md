# 管理平台代码债重扫清单（2026-09-08）

> 触发：用户指令「重扫管理平台新债」，在 F17 / 班主任工作台 / T1–T12 / Medium 缺陷均已收口之后，做一轮 fresh 扫描。
> 结论：**管理平台主体已处于终态洁净状态**，下方为唯一仍可收口的新鲜债项，整体为 P3 / 低风险的代码卫生与可维护性项，无功能性/安全性阻塞。

---

## 0. 扫描口径与已确认洁净项

| 维度 | 结果 |
|---|---|
| 前端 `console.*` 散落 | 无（仅 `utils/logger.ts`、`utils/errorMonitor.ts` 设计内委托 console，非债） |
| 后端/前端 `TODO`/`FIXME`/`XXX` | 后端 0（仅 `utils/validation.py:264` 正则注释误命中）；前端 1（测试内 `NOTE`，非债） |
| 前端类型级 `any` | 0 |
| 后端 `print()` | 仅 `tools/project_evaluation.py`（CLI 报告，合理）、`scripts/migrate_*.py`（迁移脚本，合理）、`utils/batch_writer.py:447`（见 D4） |
| 硬编码 endpoint / IP | 无（前端 baseURL 集中在 `services/api.ts`；后端仅 `__init__.py:27` https 升级逻辑，合法） |
| F17 写路径残留 | 0（`api/` 全为读/统计，`services.` 调用 372 处，路由已薄壳化） |

---

## 1. 待办清单（按严重度）

### D1 · `exhaustive-deps` eslint 抑制集群 — ✅ Medium（已逐处核验，结论：全部合法，零改动）
- **真实分布（2026-09-08 grep 实测）**：共 **22 处** = 11 个 file-level 整文件抑制（`Analysis/DeviceManagement/Dashboard/CourseSchedule/ExamManagement/FirmwareManagement/NLPManagement/RemoteNotify/RuleList/ScoreEntry/UserList` 顶部 `/* eslint-disable */`）+ 11 个单行抑制。原清单"NLPManagement 9 处"为误报，实为 1 个 file-level。
- **逐处核验结论（全部合法有意省略，无需补全依赖，不动业务逻辑）**：

  | 位置 | 模式 | 判定 |
  |---|---|---|
  | EntitySelect:111 | `[options,value]` 但用 `props.*` | 有意：仅 options/value 变更触发 |
  | Header:103 | 挂载期 poll `[]` | 合法：mount-only interval |
  | useShallowCompare:123/135 | useMemo 内自实现比较 | 合法：deps 已自包裹 |
  | useAutoSave:53 | `[data,previousData]` | 有意：忽略 saveDraft/key 身份 |
  | ClassManagement:407/493 | `*.current` ref 兜底 | 合法：ref 模式 |
  | useClientFilter:24 | `predicateRef.current` | 合法：ref 模式 |
  | RankBoard:75 | `[tab]` 条件加载 | 有意：selectedClass 由 onSelectClass 兜底 |
  | UserList:472 | `[fetchTrigger]` 手动刷新 | 合法：trigger 模式 |
  | WorkbenchOverview:254 | `[filterClassId]` 调 loadMetrics | 合法：loadMetrics 依赖同一值 |

- **file-level 11 处**：均为开发期有意的全文件级约定（避免大文件内大量重复注释）。逐页移除会爆出大量 warning 且无功能收益，违反「高风险提取宁可跳过」原则 → **保留**。
- **风险**：无（未改代码）。
- **闸门**：`eslint` 跑 22 处所在文件全绿（ESLINT_EXIT=0，已验证 2026-09-08）。

### D2 · 巨型页拆分候选 — P3（大工作量，班主任页须铁律③审核）→ **需分页执行 + 用户逐页认领**
- **位置**（`pages/*.tsx` 行数，2026-09-08 实测）：DeviceManagement 2031、PermissionManagement 1657、ScoreEntry 1571、UserList 1490、CourseSchedule 1476、ClassManagement 1438 🔒、AlgorithmAnalysis 1431、NLPManagement 1358、SubjectManagement 1198、Dashboard 1170、StudyGuide 1110 🔒、ScoreAnalysis 1104、RuleList 1100、Analysis 1091、AttendanceManage 1062 🔒。
- **📌 范围扩展（2026-09-09 用户确认「把 D2 范围真正扩展到 >1000 阈值外 6 大页」）**：D2 范围正式扩展到 **>1000 阈值外的「大页」**（700–937 行区间，体量虽未破千但同样属巨型页、认知负担重），新增 **6 候选**：**RemoteNotify 937 / DeviceGroup 909 / StudentPortal 894 / Approvals 823 / ClassPeriodSettings 710 / Diagnostics 709**。此 6 页非班主任页，铁律③不适用；沿用「逻辑层 + 视图层」范式与三闸门（tsc/eslint/vitest 全绿）。详见下方「D2 扩展 6 页」收口记录。
- **说明**：>1000 行页集中。按域拆子组件/子 hook 可显著降低认知负担与回归面。
- **🔒 班主任相关页（铁律③：字段/权限/业务逻辑变更须用户审核）**：ClassManagement、AttendanceManage、StudyGuide（其余班主任页 HomeworkCheck/ParentContact/MentalHealth/TeacherComments/SeatingChart/StudyGroups/CultureBoard/DutyRoster/CommitteeList 不在此 >1000 行清单内，但改动同样走铁律③）。
- **建议执行顺序（先非班主任低风险页热身，再进班主任页需审核）**：
  1. **Analysis (1091)** — 纯统计展示，拆分图表子组件风险最低，推荐首拆。
  2. **RuleList (1100) / ScoreAnalysis (1104)** — 规则列表 / 分数分析，域边界清晰。
  3. **SubjectManagement (1198) / AlgorithmAnalysis (1431)** — 学科 / 算法分析。
  4. **Dashboard (1170)** — 聚合首页，拆指标卡子组件。
  5. **DeviceManagement (2031) / PermissionManagement (1657) / ScoreEntry (1571) / UserList (1490) / CourseSchedule (1476) / NLPManagement (1358)** — 最大体量，最后分批。
  6. **🔒 ClassManagement / AttendanceManage / StudyGuide** — 班主任页，须铁律③用户审核后执行。
- **每页执行范式（铁律）**：① 读全文件厘清 state/effect/handler 边界；② 抽子组件/子 hook（props 单向、零逻辑变更）；③ `tsc --noEmit` + `eslint` + `vitest run`（该页用例）；④ 单页单 PR、零回归才提交；⑤ 班主任页加 🔒 审核闸门。
- **风险**：中（拆分易引入回归）；绝不一次全拆。
- **闸门**：`tsc --noEmit` + `eslint` + `vitest run`（该页用例，全量不加 `--pool`）。
- **状态**：2026-09-08 **启动执行中** ——
  - ✅ **Analysis.tsx（1091→~230 行）已首拆**：抽 `analysis/AnalysisSections.tsx`（BasicStatsGrid / AlgorithmInsightCards / ScoreDistributionPanel / ClusterDistributionPanel / RiskWarningPanel / TopUsersRanking / ScoreTrendPanel / CorrelationPanel / NeedAttentionPanel 共 9 个纯展示子组件），逻辑层（state/effect/handler/useMemo）零变更，仅向下传 props。闸门：`tsc --noEmit` EXIT=0、`eslint` 两文件 EXIT=0、`vitest run` 277 passed/3 skipped EXIT=0。
  - ✅ **RuleList.tsx（1101→~385 行逻辑层）已拆分**：抽 `rule/RuleSections.tsx` 导出 `RuleView`（纯展示 + 三类模态框：规则增改/Excel 导入/模板应用），`RuleList.tsx` 保留全部 state/effect/handler/useMemo，仅 `return <RuleView {...props}/>`。共享接口（Rule/Category/FormData/FormErrors/RuleTemplate）留在 `RuleList.tsx`，`RuleSections.tsx` 以 `import type` 引入（类型循环安全）。**eslint 两文件 EXIT=0**；`tsc --noEmit` / `vitest run` 因本轮回话末段 Bash/PowerShell/TaskOutput 工具层临时故障未复跑，待恢复后补验（预期零回归，逻辑逐字节不变）。
  - ✅ **ScoreAnalysis.tsx（1104→~230 行逻辑层）已拆分**：抽 `scoreAnalysis/ScoreAnalysisSections.tsx`（导出 `ScoreAnalysisView` + 全部类型接口 SubjectStats/ExamAnalysis/ClusterResult/CompositeScoreResult/WarningResult/AlgorithmData 等 + `CLUSTER_COLORS` 常量 + `renderScoreDistribution`/`renderSubjectBarChart` 两个渲染函数），`ScoreAnalysis.tsx` 保留全部 state/effect/handler/useMemo（`handleExport` 留在逻辑层），仅 `return <ScoreAnalysisView {...props}/>`。闸门：`tsc --noEmit` EXIT=0、`eslint` 两文件 EXIT=0、`vitest run` 全绿（EXIT=0）。
  - ✅ **SubjectManagement.tsx（1198→~270 行逻辑层）已拆分**：抽 `subjectManagement/SubjectManagementSections.tsx`（导出 `SubjectManagementView` + 类型接口 FormData/AdminUser/StatusFilter + `defaultForm` 常量 + `presetColors` 常量），`SubjectManagement.tsx` 保留全部 state/effect/handler/useMemo（含三个模态框所需的 handler、useForm/useSubmitGuard/useModal 状态）。为降 props 复杂度，把"关闭编辑教师模态框"三处内联 set 收敛为 `closeEditTeacherModal`、"重置搜索"内联收敛为 `handleReset` 两个复合 handler 传视图层。闸门：`tsc --noEmit` EXIT=0、`eslint` 两文件 EXIT=0、`vitest run` 全绿（EXIT=0）。
  - ✅ **AlgorithmAnalysis.tsx（1431→759 行逻辑层）已拆分**：抽 `algorithm-analysis/AlgorithmAnalysisShell.tsx`（`AlgorithmAnalysisShell` + `AlgorithmAnalysisShellProps`）。原文件 12 个 Tab 子组件已在 `./algorithm-analysis/`，本拆仅把"主壳 JSX（头部/搜索/班级筛选/刷新/标签导航/5 个特殊控制栏/警示/错误/内容分发/加载遮罩）+ 4 个列 useMemo（predictionDetailColumns/scorePredictColumns/attributionColumns/engagementColumns）"移到视图层；逻辑层保留全部 state/effect/handler/useMemo，仅 `return <AlgorithmAnalysisShell {...shellProps}/>`。`shellProps` 透传 `AlgorithmAnalysisDeps`（不含列定义）+ 壳层专用 setter/回调（activeTab/setActiveTab/tabNavRef/setSearchKeyword/setPredictionDays/setAnomalyDays/setRecommendDays/loadPrediction/loadAnomaly/loadStatistics），Shell 内部 memo 列定义后合并回 `deps` 下发给各 Tab（与原闭包行为一致）。闸门：`tsc --noEmit` EXIT=0、`eslint` 两文件 EXIT=0（仅 1 条与原始一致的 exhaustive-deps 警告）、`vitest run` 277 passed/3 skipped EXIT=0。
  - ✅ **Dashboard.tsx（1170→~620 行逻辑层）已拆分**：抽 `dashboard/DashboardView.tsx`（`DashboardView` + `DashboardViewProps`）。逻辑层保留 `useReducer`+`dataReducer`/`initialState`、全部 fetch*/handler/useMemo（`filteredUsers`/`classes`/`classGroups`）、WebSocket 订阅与定时刷新；仅 `return <DashboardView .../>`。视图层持有 5 个 memo 组件（`AnimatedNumber`/`StatCard`/`UserCard`/`DeviceCard`/`LiveClock`）+ 5 个纯函数/常量 helper（`getRankColor`/`getScoreColor`/`getLevel`/`getUserCluster`/`CLUSTER_COLORS`）+ 主壳 JSX；`UserCard` 原闭包内 `getUserCluster` 重构为模块级 `(clusters, userId)` 入参，由逻辑层下发 `state.algorithmData.clusters` 经 props 注入；`loading` 骨架屏判断移到视图层。逻辑层导出 `DashboardState`/`ClusterData`/`ClusterStudent` 供视图 `import type`。闸门：`tsc --noEmit` EXIT=0、`eslint` 两文件 EXIT=0（仅 2 条 unused-import 警告，已随拆分清理）、`vitest run` 277 passed/3 skipped EXIT=0。
  - ✅ **DeviceManagement.tsx（2031→802 行逻辑层）已拆分**：按用户确认的「子模块拆分」策略，抽 `deviceManagement/` 目录共 **16 文件**（types.ts / helpers.tsx / DeviceColumns.tsx / StatisticsCards.tsx / DeviceListView.tsx / DeviceMonitorView.tsx / AddDeviceModal.tsx / DeviceDetailModal.tsx / BindDeviceModal.tsx / ControlDeviceModal.tsx / DeviceSettingsModal.tsx / OTAModal.tsx / BulkOTAModal.tsx / OTAProgressModal.tsx / ImportModal.tsx / DeviceManagementView.tsx）。主文件收敛为纯逻辑层（全部 state/effect/handler/useMemo/useForm/useModal/useSubmitGuard/WebSocket 订阅零变更），仅 `return <DeviceManagementView {...props}/>`。`deviceColumns` 经模块级 `createDeviceColumns(handlers)` 构造；`getSystemStateText/getSignalStrength/getSeverityIcon` 提升为模块级 helper；9 个模态框各自承接 props，含 `runSubmit` 守卫包装 `submitAddDevice/submitBindDevice/submitRemoteControl/submitUpdateSettings/submitOTAUpgrade/submitBulkOTAUpgrade`；`useForm` 泛型 `handleChange` 经 `(field, value: unknown)` 适配器透传视图层（DeviceSettings/OTA 两异构表单需包装，NewDevice/Bind 直接透传）。逻辑逐字节不变。闸门：`tsc --noEmit` EXIT=0、`eslint` 全绿（EXIT=0）、`vitest run` 277 passed/3 skipped EXIT=0。
  - ✅ **PermissionManagement.tsx（1657→614 行逻辑层）已拆分**：按用户「子模块拆分」同策略，抽 `permissionManagement/` 目录共 **3 文件**（types.ts / helpers.tsx / PermissionManagementView.tsx）。`helpers.tsx` 提 `getRoleLabel`/`getRoleBadgeColor`/`PERMISSION_CATEGORIES` 为模块级 + 4 个列工厂 `createAdminColumns(adminRolesMap,roles)`/`createClassColumns`/`createRoleColumns`/`createLogColumns` + `filterPermissions`/`groupPermissions` 纯函数派生；主文件收敛为纯逻辑层（全部 state/effect/handler/useMemo 零变更），仅 `if(loading) return <LoadingSpinner/>` 与 `return <PermissionManagementView {...props}/>`（含 25+ props：5 Tab 数据/handler/列 + 5 模态框状态/handler/form setters + `runSubmit`/`submitting`）。视图层持有 tab 导航 + 5 Tab（用户/班级/角色/权限/日志）+ 5 模态框全部 JSX。逻辑逐字节不变。闸门：`tsc --noEmit` EXIT=0、`eslint` 全绿（EXIT=0）、`vitest run` 277 passed/3 skipped EXIT=0。
  - ✅ **ScoreEntry.tsx（1571→1176 行逻辑层）已拆分**：按「逻辑层 + 单一 View 大 Props」范式（useReducer 异构页，避免 DeviceManagement 16 文件过碎），抽 `scoreEntry/` 目录共 **2 文件**（types.ts / ScoreEntryView.tsx）。`types.ts` 沉淀完整接口（ClassInfo/Exam/ScoreItem/ExamData/PendingChange/ScoreEntryDraft/ImportResult/CellPosition/ScoreEntryState/ScoreEntryAction/BatchFailure/ScoreEntryViewProps）；主文件保留 `scoreEntryReducer`/`initialState`、全部 stateful handler（`handleScoreBlur`/`focusCell`/`runBatched`/`handleSaveAll`/`handleImport`/`handleConfirmAll`/`handleBatchDelete`/`handleBatchReset`/`handleBatchConfirm`/`exportTemplate`/`getStatusBadge`/`handleKeyDown` 等）、`columns` 动态 useMemo（输入/粘贴/方向键导航，依赖 stateful handler 保留在逻辑层构造后下发，保证键盘/粘贴交互逐字节不变）、`examSubjects`/`visibleSubjects`/`getEntryProgress`/`filteredStudents` useMemo，以及 `cancelBatchRef`/`batchProgress`/`batchFailures` 本地状态；仅 `return <ScoreEntryView {...props}/>`。视图层持有草稿恢复条/header 操作区/筛选 Card/进度保存全部确认区/batchProgress 条/batchFailures 条/DataTable/3 个 Modal（导入/批量操作/导入结果）全部 JSX；批量保存「取消」按钮经 `onCancelBatch` 回调跨层触发逻辑层 `cancelBatchRef.current=true`，状态不泄漏视图层。逻辑逐字节不变。闸门：`tsc --noEmit` EXIT=0、`eslint` 全绿（EXIT=0）、`vitest run` 277 passed/3 skipped EXIT=0。
  - ✅ **UserList.tsx（1490→1009 行逻辑层）已拆分**：按「逻辑层 + 单一 View 大 Props」范式，抽 `userList/` 目录共 **2 文件**（types.ts / UserListView.tsx）。`types.ts` 沉淀 `PaginationState`/`FormData`/`Rule`；主文件保留 `useReducer`+`State`/`Action`/reducer、全部 stateful handler（`handleOpenModal`/`handleSubmit`/`handleDelete`/`handleToggleActive`/`handleQuickScore`/`handleBatchDelete`/`handleBatchScore`/`handleExport`/`handleClearSelection`/`handleSelectionChange`/`handleClearFilters`/`handleRetry`/`fetchUsers` 等）、`userColumns` useMemo（状态徽章/评分/编辑/启禁/删除操作列，闭包捕获 handler 保留在逻辑层构造后下发）、`classes`/`selectedUsersArray`/`selectedUsersData`/`debouncedSearchTerm` useMemo；仅 loading/error/empty 三分支 + `return <UserListView {...props}/>`。视图层持有 header/搜索+班级筛选/高级筛选卡/BatchActionBar/DataTable/3 个 Modal（增改/导入/快捷评分）全部 JSX，`batchActions`/`advancedSearchFields` useMemo 下沉视图层；`State`/`Action` 经 `export type UserListState=State; UserListAction=Action` 供视图 `import type`。逻辑逐字节不变。闸门：`tsc --noEmit` EXIT=0、`eslint` 全绿（EXIT=0）、`vitest run` 277 passed/3 skipped EXIT=0。
  - ✅ **CourseSchedule.tsx（1476→688 行逻辑层）已拆分**：本页为全 D2 最高「逻辑-in-JSX」耦合页（渲染块 784 行内嵌模板下载 fetch/错误导出 fetch/冲突策略 radio setState 等真实逻辑 + 14 个 setter 直连）。采用「逻辑层 + 单 View」范式，`courseSchedule/` 目录共 **2 文件**（types.ts / CourseScheduleView.tsx）。`types.ts` 沉淀 `FormData`/`ConflictResult`/`WeekDay`/`ImportResult`/`TeacherItem` + 46 字段 `CourseScheduleViewProps` 接口；主文件保留全部 state（含 `showModal`/`closeModal`/`showImportModal` 经 useModal、`exportFormat`/`exporting`、13 个数据/UI setter）+ handler（`fetchData`/`fetchSchedules`/`handleSubmit`/`handleDelete`/`handleAdd`/`handleEdit`/`handleImport`/`exportSchedule`/`checkConflicts` 等）+ `columns` useMemo（闭包捕获 `getPeriodTime`/`handleAdd`/`handleEdit`/`handleDelete`/`weekDays` 保留在逻辑层构造）+ `activePeriods`/`filteredSchedules`/统计 useMemo；`FormData`/`ConflictResult`/`WeekDay` 类型改为从 `./courseSchedule/types` `import type`（`FormData` 与全局 DOM FormData 冲突 → 必须 type-only import）。视图层持有 header/统计卡/班级下拉/DataTable 时间表/增改 Modal（含冲突警告+教师教室备注）/导入 Modal（模板下载+配置+策略 radio+文件拖拽+结果）全部 JSX（verbatim 移动，逐字节不变），`api`/`logger`/`downloadBlob`/`getAuthHeaders` 由视图直接 import（其 JSX 内嵌的真实逻辑）。闸门：`tsc --noEmit` EXIT=0、`eslint` 全绿（EXIT=0）、`vitest run` 277 passed/3 skipped EXIT=0。
  - ✅ **NLPManagement.tsx（1358 行）已核验：T12 已视图拆分完毕，不再另拆**：该页在 T12 已按「主壳 + `NLPDeps` 透传」范式拆分（ParseTab/RulesTab/TrainingTab/StatisticsTab/AnalysisTab + CorrectionModal/RuleFormModal/BatchImportModal/CorrectionsList 全部抽到 `./nlp-management/` 子模块，头部注释明示「T12 拆分」）。主文件 1358 行构成 = ~50 useState + ~35 useCallback handlers + 4 个 columns useMemo（`ruleColumns`/`trainingResultColumns`/`performanceColumns`/`correctionColumns`，render 闭包绑定 `editingRule`/`trainAllResult`/`correctionStatusFilter` 等 ~50 个组件 state）+ ~100 行 `deps: NLPDeps` 装配 + ~60 行主壳导航 JSX。主壳 JSX 仅 60 行，无大块可再下沉；剩余 bulk 为纯逻辑层，属 D2 铁律「逻辑保留零变更」范畴；强行拆 columns 需宽泛 factory 接口绑定 50+ state、异构度高、收益低（违反「复杂度超收益则拒绝」）。结论：NLPManagement 已达 D2 目标态（视图全下沉、主文件纯逻辑），**不再另拆**。
  - ✅ **ExamManagement.tsx（1032→691 行逻辑层）已拆分**：本页为原 D2 扫描遗漏的非班主任 >1000 行页（admin 级考试管理，非 teacherWorkbench 路由组）。按「逻辑层 + 单一 View 大 Props」范式，抽 `examManagement/` 目录共 **2 文件**（types.ts / ExamManagementView.tsx）。`types.ts` 沉淀 Exam/ClassItem/Subject/ExamFormData/SubjectFormData 接口 + `ExamManagementViewProps`（42 字段）；主文件保留全部 state/effect/handler/useMemo（`useForm` 双表单 / `useModal` 双模态 / `useAutoSave` 草稿 / `useClientFilter` 派生过滤 / `columns` useMemo 闭包捕获 `getStatusBadge`/`getImportanceBadge`/5 个操作 handler）、`filteredExams`/`DEFAULT_EXAM_FORM` 零变更，仅 `if(loading) return <LoadingSpinner/>` 与 `return <ExamManagementView {...props}/>`；`getStatusBadge`/`getImportanceBadge` 因仅被 `columns` 闭包引用保留逻辑层。`handleExport`/`handleImportFile`（`/api/exam-import/execute` 带 `exam_id`）经 `getAuthHeaders` 在逻辑层构造后下发视图。逻辑逐字节不变。闸门：`tsc --noEmit` EXIT=0、`eslint` 两文件 EXIT=0、`vitest run` 277 passed/3 skipped EXIT=0。
  - ✅ **ClassManagement.tsx（1438→逻辑层，班主任页·铁律③ #1）已拆分**：本页为 🔒 班主任页，用户于 2026-09-08 经 AskUserQuestion 选定「三页全批，按序执行」（ClassManagement → AttendanceManage → StudyGuide），并附「铁律③零变更声明」方案（`docs/班主任页拆分方案-铁律③待审.md`）预审通过，故执行。按「逻辑层 + 子模块」范式，抽 `classManagement/` 目录共 **7 文件**（types.ts / StatisticsCards.tsx / ClassFormModal.tsx / HeadTeacherModal.tsx / TeacherPreviewDialog.tsx / ImportModal.tsx / ClassManagementView.tsx）。`types.ts` 沉淀 `FormData`(`id/name/grade/description/is_active`+index)/`TeacherPreview`(`teacher:Admin, classInfo:ClassInfo`)/`ImportResult`/`ClassManagementViewProps`（~47 字段，含 `showHeadTeacherModal: boolean`）；主文件保留全部 state/effect/handler/useMemo（`useListFetch`/`useForm`×1/`useModal`×3/`useConfirm`/`useStableToast`/`useAutoSave` 未用/`columns` useMemo 空依赖仅用 `GraduationCap` 图标），**3 处渲染期 ref 赋值保留**（`confirmRef.current=confirmFn`、`performRemoveHeadTeacherRef.current=performRemoveHeadTeacher`）、**2 处行级 `// eslint-disable-next-line react-hooks/exhaustive-deps` 随 useCallback 保留**（对应 `showTeacherPreviewDialog`/`showRemoveConfirmDialog`）；`lastOperation` 撤销语义、assign 第三参 `true`、skipCache 双刷新、`api.classes.import`(xlsx)/原生 `fetch`(json) 分流、文件白名单 `.json/.xlsx/.xls` 全部逐字节不变；DataTable `rowActions` 保留 S1 注释 `/* class.delete 后端无此码，统一 class.manage */`。视图层 `ClassManagementView.tsx` default export 主壳（Header+统计卡+搜索/工具条+DataTable+4 模态挂载）；`ImportModal.tsx`（241 行最大块）直接 import `api`/`downloadBlob`/`logger`/`PermissionButton`，模板下载 `onClick` 内联 `fetch(api.importConfig.downloadTemplate('classes'))`+`downloadBlob`。逻辑逐字节不变。**铁律③零变更声明逐条满足**：权限码（`class.manage`/`class.view`/`class.import`/`class.teacher.assign`/`class.teacher.remove`/`class.delete`）未改、字段命名统一、业务字典（`is_active`/`grade`）未改、业务逻辑（撤销/导入分流/班主任分配）未改。闸门：`tsc --noEmit` EXIT=0、`eslint` 8 文件 EXIT=0、`vitest run` 277 passed/3 skipped EXIT=0。
  - ✅ **AttendanceManage.tsx（1062→逻辑层，班主任页·铁律③ #2）已拆分**：范式：逻辑层 `AttendanceManage.tsx` + `attendanceManage/` **8 子模块**（types.ts / DraftBanner.tsx / StatisticsCards.tsx / PendingLeavesPanel.tsx / AttendanceTableCard.tsx / RecordModal.tsx / LeaveModal.tsx / AttendanceManageView.tsx）。主文件保留全部 state/effect/handler/useMemo（`useListData`(attendances)/`useAutoSave`(M3 草稿)/`useWorkbenchClass`(filterClassId)/`useClientFilter`/`useSubmitGuard`/`useStableToast`）；`getStatusBadge`+`columns` useMemo（deps=[getStatusBadge]）保留逻辑层构造后下发 `AttendanceTableCard`；`pendingLeaves`/`filteredAttendances` 派生值逻辑层计算下发；`runSubmit` 守卫原样下发给 RecordModal/LeaveModal/PendingLeavesPanel（保留 `runSubmit(() => handleX())` 调用语义，含批量出勤/缺勤、审批批准/驳回）。视图层 `AttendanceManageView.tsx` default export 主壳（草稿恢复条/header+班级筛选+待审批角标+快速记录·请假申请按钮/统计卡/待审批面板/列表卡/双模态挂载）。逻辑逐字节不变（含 M3 空草稿静默清理、M9 P1 服务端分页、M11 批量按班级生效）。
  - 铁律③零变更声明逐条满足：权限码（attendance.record/batchRecord/applyLeave/approveLeave/getStats/getLeaves 均为既有权限域，未改）/字段命名/业务字典（`status: present/absent/late/leave`、`leave_type: personal/sick/bereavement/maternity/other`）/业务逻辑（草稿恢复·批量记录·审批·分页）未改。
  - ✅ **StudyGuide.tsx（1110→逻辑层，班主任页·铁律③ #3）已拆分**：范式：逻辑层 `StudyGuide.tsx` + `studyGuide/` **8 子模块**（types.ts / Toolbar.tsx / GuidesTab.tsx / PlanCard.tsx / PlansTab.tsx / GuideModal.tsx / PlanModal.tsx / StudyGuideView.tsx）。主文件保留全部 state/effect/handler/useMemo/useRef（`useListData`×2(guides/plans)/`useWorkbenchClass`(filterClassId)/`useSubmitGuard`/`useConfirm`+`confirmRef.current=confirmFn` 渲染期 ref 赋值保留/`useStableToast`）；`getProgressColor`/`getProgressBg`/`filteredGuides`/`filteredPlans` 派生值保留逻辑层计算后下发；`runSubmit` 守卫下发给 GuideModal/PlanModal（PlanModal 表单 `onSubmit` 保留 `void runSubmit(handlePlanSubmit)` 语义，L8 注释保留）；双 Tab（指导文章/改进计划）切法、空列表 EmptyState 动作、404 自动刷新列表（v2）、编辑时班级不可改、发布 ToggleSwitch 全部逐字节不变。视图层 `StudyGuideView.tsx` default export 主壳（header+新建按钮/Toolbar(搜索+班级筛选+面包屑+Tab 切换)/内容区(LoadingSpinner·GuidesTab·PlansTab)/双模态挂载）。
  - 铁律③零变更声明逐条满足：权限码（studyGuide.getGuides/getPlans/createGuide/updateGuide/deleteGuide/createPlan/updatePlan/deletePlan/updatePlanProgress 均为既有权限域，未改）/字段命名/业务字典（`guide_type`/`plan_type`/`target_audience`/`is_published`）/业务逻辑（草稿·双 Tab·进度按钮·删除确认·分页）未改。
  - 🏁 **D2 非班主任 >1000 行大页全部处理完毕**：Analysis/RuleList/ScoreAnalysis/SubjectManagement/AlgorithmAnalysis/Dashboard/DeviceManagement/PermissionManagement/ScoreEntry/UserList/CourseSchedule/ExamManagement 十二页已拆（各主文件收敛为逻辑层，三闸门全绿）+ NLPManagement 核验为 T12 已拆不另动；**D2 扩展 6 页（>1000 阈值外「大页」）RemoteNotify/DeviceGroup/StudentPortal/Approvals/ClassPeriodSettings/Diagnostics 已于 2026-09-09 正式纳入 D2 主清单并收口（三闸门全绿，见下）**。🔒 班主任页进度（铁律③·三页全批按序执行）：**ClassManagement ✅ / AttendanceManage ✅ / StudyGuide ✅（三页全部已拆，各三闸门全绿）→ D2 班主任页收口完毕**。

  - ✅ **D2 扩展 6 页（>1000 阈值外「大页」，已于 2026-09-09 正式纳入 D2 主清单）**：用户指令「把 D2 范围真正扩展到 >1000 阈值外 6 大页」确认收口。锁定 6 候选：RemoteNotify(937)/DeviceGroup(909)/StudentPortal(894)/Approvals(823)/ClassPeriodSettings(710)/Diagnostics(709)。沿用「逻辑层 + 视图层」范式、三闸门（tsc/eslint/vitest 全绿），铁律①（不 commit/push）继续生效；此 6 页非班主任页，铁律③不适用。
    - ✅ **RemoteNotify(937)/DeviceGroup(909)**：前序已拆并验证（各主文件收敛逻辑层 + `remote-notify/`/`deviceGroup/` 子模块）。
    - ✅ **StudentPortal(894→逻辑层)**：抽 `studentPortal/` 2 文件（types.ts / StudentPortalView.tsx default export 主壳，含 6-tab 与 SVG 折线参与度趋势内联 IIFE）。逻辑层保留全部 state/handler/useListFetch/`tab` effect，仅 `return <StudentPortalView {...~30项props}/>`；新增 `LeaveFormData` 命名接口 + `setLeaveFormMerged` 合并包裹（视图层以 Partial 增量更新，避免整替换丢字段）。
    - ✅ **Approvals(823→逻辑层)**：抽 `approvals/` 2 文件（types.ts / ApprovalsView.tsx default export 主壳）。逻辑层 `export interface Approval/CreateForm/FailedBatch` + `getStatusBadge`/`getTypeLabel`/`columns` useMemo 构造后下发；新增 `setCreateFormMerged` 合并包裹；`sortOrder` 经 `sortOrder as SortOrder` 桥接 DataTable。
    - ✅ **ClassPeriodSettings(710→逻辑层)**：抽 `classPeriodSettings/` 2 文件。`minuteOptions`/`getPeriodType` 纯展示常量下沉视图；逻辑层移除 `React.FC` 注解，保留 `if(loading)` 早返 + 全部 handler；`setFormDataMerged` 合并包裹。
    - ✅ **Diagnostics(709→逻辑层)**：抽 `diagnostics/` 2 文件（types.ts / DiagnosticsView.tsx 含子组件 HealthStatus/HealthCard/MetricCard/SlowRequestsTable/ErrorList/SystemInfoComponent + 主壳）。逻辑层 4 个 fetch handler + `refreshAll` + 30s 轮询 effect；`getAuthHeaders`/`parseEnvelopeSafe` 留逻辑层。
    - **关键修复（本轮回话 Bash 恢复后补闸门发现）**：4 页落盘代码视图层以 `setXxxForm({ field: v })` Partial 形式调用，但逻辑层原直传原始 `useState` setter（`Dispatch<SetStateAction<T>>`）→ 类型不可 assign，且运行时整替换会丢字段。统一在逻辑层加 `setXxxMerged = (data: Partial<T>) => setXxx(prev => ({ ...prev, ...data }))` 合并包裹；ApprovalsView 的 `sortOrder` 经 `as SortOrder` 桥接（`SortOrder` 来自 `../../components/data-display/DataTable` 本地导出，非 antd）。
    - **闸门（本轮续拆全绿）**：`tsc --noEmit` EXIT=0、`eslint` 上述 8 文件 EXIT=0（0 problems）、`vitest run` **277 passed / 3 skipped** EXIT=0（与基线一致，零回归）。

### D3 · `debounce`/`throttle` 重复定义 — ❌ 核验后判定 false positive（跳过）
- **位置**：`utils/mobileUtils.ts:233/247` 与 `services/requestOptimizationService.ts:570/581`。
- **结论（2026-09-08 比对）**：二者**语义根本不同，非重复**：
  - `mobileUtils.debounce/throttle`：通用 timing 工具，`(func, wait) => wrappedFunc`，无 key。
  - `requestOptimizationService.debounceRequest/throttleRequest`：基于 `private debounceMap/throttleMap` 的**请求级 keyed 去重/合并包装**，`(key, request, options) => Promise<T>`。
  - 后者从 `requestOptimizationService` 类取方法、从不 import 前者；二者无实现重复，仅命名相似。
- **处置**：**跳过**。强行合并需高参数化复杂度、收益低，符合「异构高、复杂度超收益则拒绝」原则。无需改动。
- **风险**：无（不改动）。

### D4 · `utils/batch_writer.py:447` 用 `print()` 而非 logger — ✅ Low（已收口）
- **位置**：`apps/backend/utils/batch_writer.py:447` `print(f"处理了 {stats['total_processed']} 条消息")`。
- **说明**：运行时工具应以 `current_app.logger` / 项目 logger 输出，便于统一采集。
- **处置**：`print(...)` → `logger.info(...)`（复用文件已有 logger 或 `app.logger`）。
- **风险**：零功能影响。
- **闸门**：`py_compile` + 定向 pytest（batch_writer 相关）。

### D5 · 后端 broad `except Exception` + `# noqa: BLE001` — Low（可选加固）→ **✅ 安全子集已收口，其余按铁律延后**
- **真实范围（2026-09-08 grep 实测）**：`# noqa: BLE001` 的 broad except 共 **55+ 处、横跨 30+ 文件**，含 `tests/conftest.py`、`run.py`、`app/service_init.py`、`app/startup_cleanup.py`、`utils/decorators.py`（集中式 `@safe_handle` 错误处理核心）、`services/*`、`scripts/*`、`tasks/*`。远超原描述的"路由层几处兜底"。
- **处置（收窄到安全子集）**：
  - ✅ `api/student/student_routes.py` 4 处（analytics 端点 engagement/risk/score_trend/participation_trend 异常，**原服务端完全无日志、静默吞入返回 dict**）：补模块级 `logger` + 4 处 `logger.exception(...)`，HTTP 响应契约逐字节不变。闸门：py_compile OK、定向 `test_student_insights.py` 通过、全量 `run_regression.sh` **已通过（REG_EXIT=0, vJCe31, 2026-09-08）**。
  - ⏭️ `api/scores/scheduled_notify_routes.py` 2 处（MQTT publish 内层 per-item 兜底）：外层已有 `logger.warning`/`logger.error` 聚合日志，**并非静默吞掉**，D5 目的已满足，故不强行加（避免 per-topic 全堆栈噪声）。
  - ⛔ 其余 50+ 处（conftest / run.py / service_init / startup_cleanup / decorators / 各 services）：**按铁律延后**——① conftest 与启动期 logger 可能尚未初始化，`logger.exception` 自身可能报错或污染测试输出；② `utils/decorators.py` 是集中式 `@safe_handle` 错误处理核心，改动超出"低风险卫生"范畴；③ 跨系统关键文件大面改动违反"小批量可审阅"原则。
- **风险**：安全子集低；其余延后未动。
- **闸门（安全子集）**：`py_compile` + 定向 pytest（`test_student_insights.py`）+ `bash scripts/run_regression.sh`（后台 vJCe31）。

### D6 · `formatNumber` 工具散落 — ✅ Low（已收口，re-export 桥接，零删除）
- **位置**：`utils/format.ts`（formatDateTime/formatDate）与 `utils/formatNumber.ts`（formatNumber）分两文件。
- **处置（2026-09-08）**：`formatNumber.ts` 的 5 个函数（formatNumber/formatPercent/formatCurrency/formatCompact/formatScore）原样并入 `utils/format.ts`；3 个引用页（ScoreRecords/ScoreAnalysis/StudentPortal）import 收敛到 `'../utils/format'`；`formatNumber.ts` 改为纯 re-export 桥接（保留文件，**规避 `git rm` 灾难风险**）。
- **风险**：零回归；`tsc --noEmit` 通过、`vitest run` 全绿（277 passed / 3 skipped，含 `format.test.ts` 9 tests）。
- **闸门**：`tsc --noEmit` + `vitest run`（已通过）。

### D7 · 列表迁移（M9 P1）遗留死 `useState` 分页状态 + 一处 `exhaustive-deps` — ✅ Low（已收口）
- **位置①（死状态）**：`pages/` 下 7 页共 9 处 `const [xxxPages, setXxxPages] = useState(1)`（AttendanceManage×2 / CultureBoard / DutyRoster / MentalHealth×2 / StudyGuide×2 / TeacherComments），读值 `xxxPages` 在 `useListFetch`/`useListData`（M9 服务端分页）迁移后不再被任何渲染或逻辑消费，仅 `setXxxPages(resp.pages)` 单向写入、无读者。eslint `no-unused-vars` 在 89 个前端变更文件中报 9 条 warning（0 error）。
- **位置②（exhaustive-deps）**：`algorithm-analysis/AlgorithmAnalysisShell.tsx:521` 的 engagement 列 `useMemo` 依赖数组 `[]` 缺失 `setEngagementTrendUserId`（稳定 `useState` setter，`types.ts` 类型为 `Dispatch<SetStateAction<...>>`）；属 D2 拆分后新出现的告警。
- **处置（2026-09-09）**：① 删除 9 处 `xxxPages` 的 `useState` 声明 + 其唯一写入 `setXxxPages(resp.pages)`（顶层无条件 `useState` 删一处不影响其余 hook 顺序；无字段/权限/业务逻辑变更，铁律③不适用）；② 将 `setEngagementTrendUserId` 补入该 `useMemo` 依赖数组（setter 稳定，行为与原 `[]` 完全一致）。**注**：Edit 工具对部分 `setXxxPages`/`guidePages` 行静默未生效，改用 Python 字节级读改写（继承原 EOL）兜底，验证落盘正确。
- **风险**：零功能影响（仅清死状态 + 消 1 告警）。
- **闸门**：`tsc --noEmit` EXIT=0、`eslint` 全 `src` EXIT=0（**0 problems**）、`vitest run` **277 passed / 3 skipped** EXIT=0（与基线一致，零回归）。

---

## 2. 处置建议与最终状态（2026-09-08）

| 项 | 结论 | 代码改动 | 闸门 |
|---|---|---|---|
| D1 exhaustive-deps 集群 | ✅ 逐处核验：22 处全合法，含 11 file-level 约定 → **保留，零改动** | 无 | eslint 22 文件全绿 |
| D2 巨型页拆分 | 🔧 **全部收口（已正式扩展至 >1000 阈值外 6 大页）**：非班主任大页 Analysis ✅ + RuleList ✅ + ScoreAnalysis ✅ + SubjectManagement ✅ + AlgorithmAnalysis ✅ + Dashboard ✅ + DeviceManagement ✅ + PermissionManagement ✅ + ScoreEntry ✅ + UserList ✅ + CourseSchedule ✅ + ExamManagement ✅ + NLPManagement ✅(核验 T12 已拆不另动)；**D2 扩展 6 页（已纳主清单）** RemoteNotify ✅ + DeviceGroup ✅ + StudentPortal ✅ + Approvals ✅ + ClassPeriodSettings ✅ + Diagnostics ✅；🔒 班主任页铁律③（三页全批按序执行）：**ClassManagement ✅（7 子模块）/ AttendanceManage ✅（8 子模块）/ StudyGuide ✅（8 子模块）全部已拆，三闸门全绿 → D2 班主任页收口完毕** | Analysis 9 子组件 + RuleList/ScoreAnalysis/SubjectManagement/AlgorithmAnalysis/Dashboard 视图层 + DeviceManagement 16 子模块 + PermissionManagement 3 子模块 + ScoreEntry/UserList/CourseSchedule/ExamManagement/StudentPortal/Approvals/ClassPeriodSettings/Diagnostics 各 2 子模块 + RemoteNotify/DeviceGroup 子模块 + ClassManagement 7 子模块 + AttendanceManage 8 子模块 + StudyGuide 8 子模块 | tsc/eslint/vitest 全绿（vitest 277 passed / 3 skipped） |
| D3 debounce/throttle | ❌ false positive（语义不同）→ 跳过 | 无 | — |
| D4 batch_writer print | ✅ 已收口（`print`→`logger.info`） | 1 行 | py_compile OK |
| D5 broad except | ✅ 安全子集已收口（student_routes 4 处 +logger）；余 50+ 处按铁律延后 | 6 行 | run_regression.sh REG_EXIT=0 |
| D6 formatNumber | ✅ 已收口（合并+re-export 桥接，零删除） | 合并+3 import+桥接 | vitest 277 passed |
| D7 死分页状态+exhaustive-deps | ✅ 已收口（删 9 处 xxxPages 死 useState+写入；补 1 处稳定 setter 依赖） | 9 处删除 + 1 行依赖补全（8 文件） | tsc/eslint(全 src 0 problem)/vitest 全绿（277 passed/3 skipped） |

**铁律遵守**：本轮所有改动**均未 commit/push**，待用户明确指示。每批独立零回归后才推进。
