# 提交切分方案（#90–#107 积压收口 · 2026-09-11）

> **性质**：只读分析产出，**未执行任何 git 写操作**。
> **背景**：`git ls-remote` 核实远端 `refs/heads/main` = 本地 HEAD = `2db39b4` → **历史提交均已推送**；但 #90–#107 全部改动堆在工作区，从未形成 commit。
> **规模**：`git status --porcelain -uall` 共 **534 条**（含未跟踪目录展开）；不含展开为 **436 项**。
> **铁律①**：本方案待用户确认后才执行 commit。

---

## 0. 关键结论（先说不可行的部分）

| 方案 | 可行性 | 原因 |
|---|---|---|
| **按批次切分（#90…#107 各一个 commit）** | ❌ **不可行** | 无中间快照。后续批次反复改写前批次的同一批文件（例：#106 统一了 96 个消费文件导入，而这 96 个文件里多数已被 #102 格式化、#103–#105 的 hook 清理动过）。**无法按文件边界还原批次归属**。 |
| **按领域/分层切分** | ✅ **本方案采用** | 按路径前缀切分 → 文件互斥、可 `git add <路径>` 精确落地、每个 commit 语义自洽。 |
| **按 hunk 切分（`git add -p`）** | ⚠️ 理论可行、实践不建议 | 534 条 / 涉及数千 hunk，人工分拣成本远高于收益，且极易出错。 |

> 因此本方案按 **8 个提交**（其中 1 个为清理、不产出 commit 内容）切分。

---

## 1. 提交清单总览

| # | 提交信息 | 项数 | 风险 | 闸门 |
|---|---|---|---|---|
| 1 | `chore(repo): ops/deploy 折叠入 ops/infra 并同步路径引用与忽略规则` | 29 | 低（纯路径/重命名） | 无需（无代码语义变更） |
| 2 | `refactor(backend): F17 薄路由收尾 + 服务下沉 + 路由/服务层清理` | 108 | 低 | pytest 已绿 |
| 3 | `refactor(backend): ruff 机械优化与工具层死代码清理` | 80 | 低 | pytest 已绿 |
| 4 | `refactor(frontend): barrel 统一 + 死代码清理（#104–#106）` | 77 | 中（曾发生误删事故，已修复） | tsc/eslint/vitest 已绿 |
| 5 | `refactor(frontend): 页面拆分与逻辑层瘦身（D2/E6a）` | 202 | 中 | tsc/eslint/vitest 已绿 |
| 6 | `docs: 代码债重扫清单 + 班主任页拆分方案` | 3 | 无 | — |
| 7 | `docs(memory): 工作记忆更新（#90–#107）` | 5 | 无 | — |
| 8 | `chore(cleanup): 移除临时工具产物与空文件` | — | 无 | — |
| | **合计（1–7）** | **504** | | |

> ⚠️ 后端拆成 #2/#3 是**按目录分层**切分（`api/`+`services/`+`models/`+`app/`+`startup/` vs `utils/`+`scripts/`+`tasks/`+`middleware/`+`tools/`+`config/`+`tests/`+`run.py`）。若您更倾向"后端一个大 commit"，把 #2/#3 合并即可。

---

## 2. 逐提交明细与落地命令

### 提交 1 · `chore(repo): ops/deploy 折叠入 ops/infra 并同步路径引用与忽略规则`

**项数 29** = 24 条重命名 + 5 个引用同步文件。

- **重命名（24）**：`ops/deploy/**` → `ops/infra/**`，含 `docker-compose.yml`、`docker-compose.mqtt.yml` 从仓库根移入 `ops/infra/`。
- **引用同步（5）**：`.gitignore`（`ops/deploy/*` → `ops/infra/*`）、`README.md`、`docs/CONFIGURATION_GUIDE.md`、`docs/STARTUP_GUIDE.md`、`apps/frontend/public/docs/系统使用指南.md`。

```bash
git add -A ops/ docker-compose.yml docker-compose.mqtt.yml
git add .gitignore README.md docs/CONFIGURATION_GUIDE.md docs/STARTUP_GUIDE.md "apps/frontend/public/docs/"
git commit -m "chore(repo): ops/deploy 折叠入 ops/infra 并同步路径引用与忽略规则"
```

> ✅ 重命名已被 git 识别为 `R`/`RM`（内容大部分未变），diff 可读性高。

---

### 提交 2 · `refactor(backend): F17 薄路由收尾 + 服务下沉 + 路由/服务层清理`

**项数 108**：`apps/backend/api/`(41) + `services/`(60) + `models/`(1) + `app/`(4) + `startup/`(2)。

- 含 **2 个新增**：`services/operation_log_service.py`（操作日志只读聚合下沉，F17 防腐层延伸）、`tests/test_operation_log_routes.py`（契约基线，属 `tests/` → 归提交 3）。

> 说明：`tests/` 目录按分层归入提交 3。

```bash
git add -A apps/backend/api/ apps/backend/services/ apps/backend/models/ apps/backend/app/ apps/backend/startup/
git commit -m "refactor(backend): F17 薄路由收尾 + 服务下沉 + 路由/服务层清理"
```

---

### 提交 3 · `refactor(backend): ruff 机械优化与工具层死代码清理`

**项数 80**：`apps/backend/utils/`(28) + `scripts/`(32) + `tasks/`(4) + `middleware/`(4) + `tools/`(3) + `config/`(3) + `tests/`(5) + `run.py`(1)。

已收口的规则批次（均零行为变更）：
`F401`(未用 import 15) · `F841`(死变量 7) · `B007`(未用循环变量 25) · `B904`(raise ... from,4) · `UP031`(printf 风格→f-string) · `UP035` · `SIM102` · `RUF100` · `RET505` · `PEP585` 类型现代化。

```bash
git add -A apps/backend/utils/ apps/backend/scripts/ apps/backend/tasks/ apps/backend/middleware/ apps/backend/tools/ apps/backend/config/ apps/backend/tests/ apps/backend/run.py
git commit -m "refactor(backend): ruff 机械优化与工具层死代码清理"
```

---

### 提交 4 · `refactor(frontend): barrel 统一 + 死代码清理（#104–#106）`

**项数 77**：`src/components/`(30) + `src/hooks/`(24) + `src/tests/`(11) + `src/utils/`(4) + `src/services/`(4) + `src/context/`(2) + `src/App.tsx` + `src/test-setup.ts`，另含 **16 个删除**。

- **删除 16**：9 个死 hook（useAdvancedSearch/useDeviceDetection/useOptimisticUpdate/usePWA/usePerformance/usePreload/useRouteChangeAbort/useShallowCompare/useSplitState）+ 6 个死组件（data-display/SearchInput、feedback/NetworkStatus、special/GlobalStateComponents.d.ts、skeletons/ 整目录 3 文件）+ 1 个独占测试。
- **新增 3**：`components/form/index.ts`、`components/workbench/index.ts`、`hooks/useMemoryUsage.ts`。
- 含 hooks 单层 barrel + components 双层 barrel 统一、96 个消费文件导入收敛、api.ts 等 prettier 规范化。

```bash
git add -A apps/frontend/src/components/ apps/frontend/src/hooks/ apps/frontend/src/tests/ \
           apps/frontend/src/utils/ apps/frontend/src/services/ apps/frontend/src/context/ \
           apps/frontend/src/App.tsx apps/frontend/src/test-setup.ts
git commit -m "refactor(frontend): barrel 统一 + 死代码清理（#104–#106）"
```

> ⚠️ 本提交含 #106 的脚本误删事故修复结果（12 文件恢复），**commit 前建议再跑一次 tsc**（约 40s）。

---

### 提交 5 · `refactor(frontend): 页面拆分与逻辑层瘦身（D2/E6a）`

**项数 202**：`apps/frontend/src/pages/**` 全部（含 45+ 个新建子目录，如 `pages/<name>/<Name>View.tsx`、`columns.tsx`、`useXxxLogic.ts(x)`）。

```bash
git add -A apps/frontend/src/pages/
git commit -m "refactor(frontend): 页面拆分与逻辑层瘦身（D2/E6a）"
```

---

### 提交 6 · `docs: 代码债重扫清单 + 班主任页拆分方案`

**项数 3**（均在提交 1 之后，因 `docs/` 已被提交 1 取走两个指南）：

- `docs/代码债重扫清单-20260908.md`（新）
- `docs/代码债重扫清单-20260909.md`（新）
- `docs/班主任页拆分方案-铁律③待审.md`（新）
- ➕ `docs/提交切分方案-20260911.md`（本文档自身，随本提交入库）

```bash
git add -A docs/
git commit -m "docs: 代码债重扫清单 + 班主任页拆分方案"
```

---

### 提交 7 · `docs(memory): 工作记忆更新（#90–#107）`

**项数 5**：`.workbuddy/memory/2026-09-08.md`(M)、`MEMORY.md`(M)、`2026-09-09.md`、`2026-09-10.md`、`2026-09-11.md`（后三者新增）。

```bash
git add -A .workbuddy/memory/
git commit -m "docs(memory): 工作记忆更新（#90–#107）"
```

---

### 提交 8（无 commit 内容）· 清理不入库产物

以下 **27 项**不进入版本库，其中 26 项为未跟踪文件（删除即无痕，无需 commit）：

| 类别 | 路径 | 处置 |
|---|---|---|
| 空文件残留（0 字节，疑似重定向误建） | `600`、`上一轮（2026-09-08）D1–D7` | **删除** |
| 临时输出（我上一轮排查留下） | `apps/frontend/_tsc.txt`、`_tsc_body.txt`、`_bash_marker.txt` | **删除** |
| 临时工具脚本 + 拆分前备份 | `tools/tmp/`（24 文件：`split_*.py`、`fidelity.py`、`analyze_*.py`、`bak/*.tsx`） | **删除**，并在 `.gitignore` 追加 `tools/tmp/` |
| IDE 计划文档（已跟踪，本次有改动） | `.trae/documents/cleanup-startup-scripts.md` | 随提交 1 或单列 `chore(ide)` |

> ⚠️ `.gitignore` 追加项与提交 1 是同一文件：**建议把 `tools/tmp/` 忽略规则并入提交 1 的 `.gitignore` 改动**（同文件无法按内容拆两个 commit，除非用 `git add -p` 分拣）。

```bash
# 删除临时产物（未跟踪，无需 commit）
rm -f 600 "上一轮（2026-09-08）D1–D7" apps/frontend/_tsc.txt apps/frontend/_tsc_body.txt apps/frontend/_bash_marker.txt
rm -rf tools/tmp/
# 若希望保留 tools/tmp 作为工具目录，则改为在 .gitignore 追加：tools/tmp/
```

---

## 3. 执行顺序（不可颠倒）

```
1（含 .gitignore）→ 2 → 3 → 4 → 5 → 6 → 7 → 8清理
```

- 提交 1 必须最先：它取走 `docs/` 的两个指南 + `.gitignore`，否则提交 6 的 `git add -A docs/` 会连带把路径同步改动卷进去。
- 清理（8）放在最后：确保 `tools/tmp/` 不会误入任何提交。
- 各提交之间**互不依赖编译**，但为审计清晰建议**顺序提交**。

---

## 4. 提交前建议补跑（约 3 分钟）

| 项 | 命令 | 期望 |
|---|---|---|
| 前端类型 | managed Node 22.22.2 + `node_modules/typescript/bin/tsc --noEmit` | EXIT=0 |
| 前端 lint | `node_modules/eslint/bin/eslint.js src --ext .ts,.tsx,.js,.jsx` | EXIT=0 / 0 problems |
| 后端导入完整性 | `python -m py_compile` 全量（系统 Py3.11） | 全绿 |

> 全量 vitest（38 文件）与 pytest（2066 用例）**已在 #104–#106 跑过且全绿**；如您要求，可在提交 5 后补跑一次全量 vitest 作为最终背书。

---

## 5. 回滚方案

| 场景 | 处置 |
|---|---|
| 某提交内容不对 | `git reset --soft HEAD~1`（保留改动回工作区，不丢内容） |
| 提交信息写错 | `git commit --amend`（**仅在未 push 时**） |
| 需要整批撤销 | `git reset --soft <2db39b4>` → 回到当前状态 |

> 全部提交**不做 push**，除非您显式指示。

---

## 6. 待您确认的 3 个决策点

1. **后端是否拆成提交 2/3 两个**（按 `api/services` 与 `utils/scripts` 分层），还是合并为一个大提交？
2. **`tools/tmp/`**：删除，还是保留目录但加入 `.gitignore`？
3. **`.trae/`**：本次改动随提交 1 一起提交，还是加入 `.gitignore` 不再跟踪？

---

> **铁律①**：本方案为只读分析产物；确认后我按上表顺序执行 commit，全程**不 push**。
