# 班主任页拆分方案（D2 扩展 · 铁律③待审）

> 触发：D2 非班主任 >1000 行大页已全拆（12 页 + NLPManagement 核验）。剩余 🔒 班主任页 ClassManagement / AttendanceManage / StudyGuide 按铁律③「字段命名 / 权限体系 / 业务逻辑变更须经用户审核」执行。
> 本文为**拆分方案 + 零变更声明**，执行前须用户逐页确认。三页均无文件级 `/* eslint-disable */`（ClassManagement 有 2 处行级抑制，须逐字节保留）。
> 统一范式：主 `.tsx` 收敛为**逻辑层**（state/effect/handler/useMemo/useReducer/渲染期 ref 赋值原样保留），JSX 下沉到同目录 `小驼峰` 子模块；逻辑**逐字节不变**，仅 `return <XxxView {...props}/>`。

---

## 0. 三页横向快照

| 维度 | ClassManagement | AttendanceManage | StudyGuide |
|---|---|---|---|
| 总行数 | 1438 | 1062 | 1110 |
| 逻辑 / JSX 行数 | 678 / 757 | 505 / 552 | 432 / 674 |
| useState / useReducer | 17 / 0 | 18 / 0 | 17 / 0 |
| useRef（含渲染期赋值） | 3（2 处渲染期赋值） | 0 | 1（1 处渲染期赋值） |
| useCallback / useMemo | 16 / 5（columns deps `[]`） | 15 / 2（columns deps `[getStatusBadge]`） | 15 / **0** |
| 自定义 hook | useListFetch / useForm / useModal×3 / useStableToast / useConfirm | useListData / useAutoSave / useClientFilter / useSubmitGuard / useWorkbenchClass / useStableToast | useListData×2 / useSubmitGuard / useWorkbenchClass / useStableToast / useConfirm |
| JSX 形态 | 单壳 + 4 模态（无 Tab） | 单壳 + 条件面板 + 2 模态（无 Tab） | 单壳 + **双 Tab** + 2 模态 |
| JSX 内副作用 | **1 处**：模板下载 `fetch(api.importConfig.downloadTemplate)` + `downloadBlob` + `logger` | 无 | 无 |
| 视图层需直 import | `api` / `downloadBlob` / `logger` + UI | 仅 UI 组件 | 仅 UI 组件 |
| 权限码 | `class.manage`×8、`class.view`×1 | **0** | **0** |
| 拆后逻辑层预估 | ≈745 行 | ≈555 行 | ≈480 行 |

---

## 1. ClassManagement（1438 → ≈745 逻辑层）

### 1.1 方案
- 新建 `src/pages/classManagement/` 目录（对齐 `deviceManagement/` 子模块风格）：
  - `types.ts`：`FormData` / `TeacherPreview` / `ImportResult` / `ClassManagementViewProps`（含 30+ props）
  - `ClassManagementView.tsx`：主壳（Header + 统计卡 + 搜索/工具条 + DataTable + 4 模态挂载）
  - `StatisticsCards.tsx`：3 × StatCard（值来自 `classList.total` / `totalStudents` / `classesWithTeacher`）
  - `ClassFormModal.tsx`：班级新增/编辑模态（124 行，props 传 `formData/errors/handleChange/handleChangeEvent/validateAll/onSubmit/closeModal`）
  - `HeadTeacherModal.tsx`：分配班主任模态（含当前班主任卡 + 教师搜索 + `filteredTeachers.map`）
  - `TeacherPreviewDialog.tsx`：确认分配对话框（props 传 `teacherPreview/isLoading/confirmAssignHeadTeacher`）
  - `ImportModal.tsx`：**241 行最大块**，须直接 `import api from '../../services/api'`、`import { downloadBlob } from '../../utils/download'`、`import logger from '../../utils/logger'`（`showToast` 经 props 下发；`getAuthHeaders` 不在视图层）
- **`columns` useMemo（空依赖、零 handler 引用，仅用 `GraduationCap` 图标）保守保留逻辑层**，逻辑层保留 `GraduationCap` import。

### 1.2 铁律③零变更声明
- **权限码**：8 处 `class.manage` + 1 处 `class.view` 原样保留；第 816 行注释 `/* S1: class.delete 后端无此码，统一 class.manage */` 与语义**不动**（不得"修正"为 `class.delete`）。
- **字段命名**：`ClassInfo`/`Admin`/`FormData`/`ImportResult` 全字段名及 API 参数（`page/per_page/keyword/skipCache/config_id`）不变。
- **业务逻辑（全部原样）**：① 教师过滤 `a.role === 'teacher'`；② 分配 `api.adminClasses.assign(teacherId, classId, true)`（第三参 `true` = 设为班主任）；③ 撤销语义（`previousTeacherId` 存在则重 assign 否则 remove）；④ 分配/移除后 `skipCache:true` 刷新 + 重复 `refetch({skipCache:true,params:{page:1}})`+`setPage(1)`（现状双刷新保留）；⑤ 导入分流 `.xlsx/.xls`→`api.classes.import`、`.json`→原生 `fetch`+`getAuthHeaders`、URL 拼 `?config_id=`；⑥ 文件白名单 `.json/.xlsx/.xls`；⑦ 导出 `api.classes.export(...)` + `exporting` 早退防连点；⑧ 分页边界 `totalPages=Math.max(1,Math.ceil(total/newPageSize))` 越界不更新；⑨ `student_count` 兜底 `!=null ? student_count : '--'`，`totalStudents` 缺失按 0 累加；⑩ `performRemoveHeadTeacher` 经 ref 间接调用（与两处 exhaustive-deps 抑制配套）。
- **lint 抑制**：第 407 / 493 行 `// eslint-disable-next-line react-hooks/exhaustive-deps` 随 useCallback 整体留逻辑层，不补全依赖、不移位。
- **渲染期赋值**：`confirmRef.current = confirmFn`（76）、`performRemoveHeadTeacherRef.current = performRemoveHeadTeacher`（550）保留逻辑层且顺序不变。

---

## 2. AttendanceManage（1062 → ≈555 逻辑层）

### 2.1 方案
- 新建 `src/pages/attendanceManage/` 目录：
  - `types.ts`：`QuickRecordForm` / `LeaveFormData` / `AttendanceDraft` / `AttendanceManageViewProps`
  - `AttendanceManageView.tsx`：主壳（草稿条 + Header + 统计卡 + 待审批面板 + 记录列表卡 + 2 模态挂载）
  - `DraftBanner.tsx` / `StatisticsCards.tsx`（5 × StatCard）/ `PendingLeavesPanel.tsx` / `AttendanceTableCard.tsx` / `RecordModal.tsx`（184 行，含批量按钮字面量数组）/ `LeaveModal.tsx`
- **视图层无需 import `api`/`downloadBlob`/`logger`**，纯 props 下发（最干净一页）。
- **`getStatusBadge`（返回 JSX 的 useCallback）+ `columns`（deps `[getStatusBadge]`）保留逻辑层**，逻辑层保留 `UserCheck` import。
- `useListFetch` 的 fetcher 内含 `setAttendanceTotal/setAttendancePages/setRecordForm` 副作用 → fetcher 整体留逻辑层。

### 2.2 铁律③零变更声明
- **权限码**：全页 0 处 → 声明「权限体系零涉及、零新增」。
- **字段命名**：`QuickRecordForm`/`LeaveFormData`/`Attendance`/`AttendanceStats`/`LeaveApplication` 全字段及分页信封（`total/pages/records/leaves`、`page/per_page`）不变。
- **业务逻辑（全部原样）**：① `status` 枚举 `present/absent/late/leave`+`unknown` 兜底及其配色/文案映射；② `period` 枚举 `上午/下午/晚上`；③ `leave_type` 枚举 `personal/sick/bereavement/maternity/other`↔事假/病假/丧假/产假/其他；④ 默认日期 `new Date().toISOString().split('T')[0]`；⑤ 批量记录规则（M11：只校验 `class_id`+`date`+`period`，`api.users.getAll({class_id,per_page:500,skipCache:true})`→`filter(role==='student')`→空则 warning→`api.attendance.batchRecord`）；⑥ 双层防重（handler `if(submitting)return` + JSX `runSubmit`）；⑦ 校验文案原样；⑧ 审批 `api.attendance.approveLeave(id, approve)` 布尔语义 + toast 文案；⑨ `per_page:50` 固定、`>50` 才渲染 Pagination、切 `filterClassId` 重置 `attendancePage=1`；⑩ 草稿 key `'attendance-entry'`、空草稿静默清理、`clearDraft()` 时机；⑪ 出勤率 `(rate*100).toFixed(1)%`；⑫ fetcher 内 `setRecordForm` 副作用；⑬ `leavesPages`/`attendancePages` 死变量保留。

---

## 3. StudyGuide（1110 → ≈480 逻辑层）

### 3.1 方案
- 新建 `src/pages/studyGuide/` 目录（对齐 `nlp-management/*Tab.tsx` 风格，**双 Tab 切法**）：
  - `types.ts`：`GuideFormData` / `PlanFormData` / `StudyGuideViewProps`
  - `StudyGuideView.tsx`：主壳（Header + 工具栏 + 内容区 + 2 模态挂载）
  - `Toolbar.tsx`：搜索 + 班级筛选 + Tab 切换器
  - `GuidesTab.tsx` / `PlanCard.tsx` / `PlansTab.tsx` / `GuideModal.tsx` / `PlanModal.tsx`
- **视图层无需 import 副作用依赖**，纯 props 下发。
- **`getProgressColor`/`getProgressBg`（渲染期纯函数、阈值 80/50/20）+ `filteredGuides`/`filteredPlans` 保留逻辑层**，作为 props 下发（最保守，逻辑逐字节不变）。
- `useListData` 两个 fetcher 留逻辑层。

### 3.2 铁律③零变更声明
- **权限码**：全页 0 处 → 声明「权限体系零涉及、零新增」。
- **字段命名**：`GuideFormData`/`PlanFormData`/`StudyGuide`/`ImprovementPlan` 全字段及分页信封不变。
- **业务字典（中文枚举即业务口径，原样）**：`guideTypes=['学法指导','学习方法','应试技巧','心理辅导','其他']`、`planTypes=['培优','补差','专项提升','综合提升']`、`audiences=['全班','优生','后进生','中等生','个人']`；默认值 `guide_type:'学法指导'`/`target_audience:'全班'`/`is_published:true`/`plan_type:'培优'`。
- **业务逻辑（全部原样）**：① 班级绑定语义（编辑 `class_id: guideForm.id ? undefined : selectedClassId`，编辑禁用 ClassSelect）；② 创建前置校验「未选班级则 toast 返回」；③ **`is_published` 未进 `StudyGuideCreateInput` payload（现状不一致，保留不修）**；④ 计划判定用 `editingPlanId` 非 `planForm.id`；⑤ 进度 5 档 `[0,25,50,75,100]`→`updatePlanProgress`；⑥ 配色分档阈值 `>=80/>=50/>=20/else` 两函数一致；⑦ 删除 404 容错（同构两处，判定 `status===404||response.status===404||message.includes('不存在')`→`showToast('info','已被删除')`，`finally` 必 `fetchGuides/fetchPlans`）；⑧ 校验文案原样；⑨ **过滤不对称**（`filteredGuides` 按班级、`filteredPlans` 不过滤，现状保留）；⑩ `per_page:50`、`>50` 才渲染、切 `filterClassId` 重置双游标；⑪ `guidePages`/`planPages` 死变量保留；⑫ `confirmRef.current=confirmFn` 渲染期赋值保留顺序。

---

## 4. 统一闸门（每页执行后）
1. `tsc --noEmit` EXIT=0（managed Node 22.22.2-2）
2. `eslint` 主文件 + 各子模块 EXIT=0
3. `vitest run`（全量，不加 `--pool`）基线 277 passed / 3 skipped EXIT=0
4. 用户审阅 `git diff` 确认「字段/权限/业务逻辑零变更」后，铁律①解除再 commit（每页单批）

## 5. 待确认
- 三页方案是否全部批准？批准顺序？
- 子模块粒度：ClassManagement / StudyGuide 是否接受「主壳 + 多子模块」切法（而非单一大 View）？
