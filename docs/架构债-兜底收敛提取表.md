# 架构债 · 手写 try/except 兜底收敛提取表

> 目标：把路由层「整方法体被单一 try 包裹 + 单一 `except Exception` + 统一兜底」的同构样板，
> 收敛为既有 `@safe_handle(default_status=…, message=…)` 装饰器。
> 前置战役：`0138ad9` 已在 `algorithm_routes.py` 收敛 62 处（同法）。
> **本表为取证阶段产物，尚未改码；待确认后分批执行。**

识别方式：AST 精确匹配（docstring 后仅 1 条语句且为 `Try` + 单一 handler + `type is Exception` + 无 `orelse/finalbody`），
已排除 `api/backups/**` 备份目录，已排除 6 个模块级辅助函数（非路由端点）。

- 全 `api/` 粗扫 `except Exception as X:` = 157 处（备份目录 46 处不计）
- AST 精确识别同构 = **32 处**，其中 Resource 类方法 **26 处**（本表对象）、模块级函数 6 处（排除）

---

## 一、A 类：可直接收敛（23 处）

原兜底形态均为 `logger.error(...) + return APIResponse.error(message=固定文案, status_code=固定值)`，
无副作用、无动态文案 → 可逐字等价替换为装饰器。

### 1. `api/system/diagnostics_routes.py`（7 处，全部同文案）

| 行 | 端点 | HTTP | 现外层装饰器 | 原 message | 原 status | 拟用装饰器 |
|---|---|---|---|---|---|---|
| 139 | `HealthCheck.get` | GET | `requires_permission('system.view')` | 诊断失败，请稍后重试 | 500 | `@safe_handle(default_status=500, message="诊断失败，请稍后重试")` |
| 173 | `PerformanceInfo.get` | GET | `requires_permission('system.settings')` | 诊断失败，请稍后重试 | 500 | 同上 |
| 200 | `ErrorInfo.get` | GET | `requires_permission('system.settings')` | 诊断失败，请稍后重试 | 500 | 同上 |
| 260 | `MemoryInfo.get` | GET | `requires_permission('system.settings')` | 诊断失败，请稍后重试 | 500 | 同上 |
| 275 | `CPUInfo.get` | GET | `requires_permission('system.settings')` | 诊断失败，请稍后重试 | 500 | 同上 |
| 290 | `DiskInfo.get` | GET | `requires_permission('system.settings')` | 诊断失败，请稍后重试 | 500 | 同上 |
| 305 | `ClearCache.post` | POST | `requires_permission('system.settings')` | 诊断失败，请稍后重试 | 500 | 同上 |

### 2. `api/system/consistency_routes.py`（4 处，全部同文案）

| 行 | 端点 | HTTP | 现外层装饰器 | 原 message | 原 status | 拟用装饰器 |
|---|---|---|---|---|---|---|
| 36 | `ConsistencyCheck.get` | GET | `requires_permission('system.settings')` | 一致性检查失败，请稍后重试 | 500 | `@safe_handle(default_status=500, message="一致性检查失败，请稍后重试")` |
| 60 | `ConsistencyReport.get` | GET | `requires_permission('system.settings')` | 一致性检查失败，请稍后重试 | 500 | 同上 |
| 76 | `ConsistencyFix.post` | POST | `requires_permission('system.settings')` | 一致性检查失败，请稍后重试 | 500 | 同上 |
| 94 | `ConsistencyStatus.get` | GET | `requires_permission('system.settings')` | 一致性检查失败，请稍后重试 | 500 | 同上 |

### 3. `api/system/system_routes.py`（8 处）

| 行 | 端点 | HTTP | 现外层装饰器 | 原 message | 原 status | 拟用装饰器 |
|---|---|---|---|---|---|---|
| 637 | `SystemPerformance.get` | GET | `requires_permission('system.view')` | 获取性能指标失败 | 500 | `@safe_handle(default_status=500, message="获取性能指标失败")` |
| 775 | `FrontendPerformance.post` | POST | `rate_limit('frontend_performance')` | 接收失败 | 500 | `@safe_handle(default_status=500, message="接收失败")` |
| 811 | `FrontendPerformanceBatch.post` | POST | `rate_limit('frontend_performance_batch')` | 接收失败 | 500 | 同上 |
| 857 | `FrontendError.post` | POST | `rate_limit('frontend_error')` | 接收失败 | 500 | 同上 |
| 886 | `SystemStats.get` | GET | `requires_permission('system.view')`, `cached_api(ttl=60)` | 获取系统统计失败 | 500 | `@safe_handle(default_status=500, message="获取系统统计失败")` |
| 996 | `FrontendMetricsList.get` | GET | `requires_permission('ops_center.view')` | 获取前端指标失败 | 500 | `@safe_handle(default_status=500, message="获取前端指标失败")` |
| 1049 | `FrontendErrorList.get` | GET | `requires_permission('ops_center.view')` | 获取前端错误失败 | 500 | `@safe_handle(default_status=500, message="获取前端错误失败")` |
| 1099 | `SystemMetricsList.get` | GET | `requires_permission('ops_center.view')` | 获取系统指标失败 | 500 | `@safe_handle(default_status=500, message="获取系统指标失败")` |

### 4. `api/monitoring/mqtt_routes.py`（3 处）

| 行 | 端点 | HTTP | 现外层装饰器 | 原 message | 原 status | 拟用装饰器 |
|---|---|---|---|---|---|---|
| 316 | `MQTTDisconnect.post` | POST | `requires_permission('manage_devices')` | MQTT disconnect error | 500 | `@safe_handle(default_status=500, message="MQTT disconnect error")` |
| 383 | `MQTTUnlock.post` | POST | `requires_permission('manage_devices')` | Send error | 500 | `@safe_handle(default_status=500, message="Send error")` |
| 420 | `MQTTCommand.post` | POST | `requires_permission('device.manage')` | Send error | 500 | 同上 |

### 5. `api/academics/classes_routes.py`（1 处）

| 行 | 端点 | HTTP | 现外层装饰器 | 原 message | 原 status | 拟用装饰器 |
|---|---|---|---|---|---|---|
| 145 | `ClassStudents.get` | GET | `requires_permission('class.view')`, `cached_api(ttl=30)` | 操作失败，请稍后重试 | **400**（`APIResponse.error` 默认，未显式传） | `@safe_handle(default_status=400, message="操作失败，请稍后重试")` |

---

## 二、B 类：需拍板（3 处，存在契约变化）

### B1. `api/analytics/dashboard_routes.py:27` `DashboardStats.get`

- 原兜底：`return APIResponse.error(message='仪表盘统计数据加载失败: %s' % e)`（**无 logger**，status = 400 默认）
- 问题：message 是**动态拼接异常细节**，历史上项目已修过「不直返异常细节」。
- 选项：
  - **a（推荐）** `@safe_handle(default_status=400, message="仪表盘统计数据加载失败")` — 固定文案，安全改善；**契约变化**：响应 message 不再含异常细节。
  - b 不传 message → safe_handle 回退 `str(e)`，**契约保持**但仍泄露异常细节。
  - c 跳过。

### B2. `api/devices/devices_routes.py:1333` `DeviceExport.get`

- 原兜底：`APIResponse.server_error(message='设备操作失败，请稍后重试')`
  → 展开为 `error(message, code=-1, status_code=500, error_code="INTERNAL_ERROR")`
- 问题：`@safe_handle` **不支持 `error_code` 参数**，收敛会**丢失 `error_code: "INTERNAL_ERROR"` 字段** → 契约漂移。
- 选项：
  - **a（推荐）跳过**，保持原样。
  - b 扩展 `safe_handle` 增加 `error_code` 透传（改动公共装饰器，影响面大，需单独评估）。

### B3. `api/nlp/nlp_routes.py:1334` `NLPFeedbackRecord.post`

- 现状：**已有 `@safe_handle()`（默认 500）**，内部又套了一层 try/except → 双兜底冗余。
- 内层兜底：`return APIResponse.error(message='记录失败')` = **400**（`error` 默认）。
- 问题：删掉内层 try 后统一由外层兜底，**原先走外层兜底（500）的异常也会变成 400** → 行为变化。
- 选项：
  - a 删内层 try + 外层改 `@safe_handle(default_status=400, message="记录失败")`，接受 500→400 变化。
  - **b（推荐）跳过**（该端点已有装饰器兜底，收益仅为去重，却引入状态码变化）。

---

## 三、排除项（6 处模块级函数，非路由端点）

`@safe_handle` 返回的是 API 响应信封，语义不适用于非路由辅助函数：

| 文件 | 行 | 函数 |
|---|---|---|
| `api/academics/classes_routes.py` | 29 | `_audit_class_op` |
| `api/data/download_routes.py` | 29 | `generate_template` |
| `api/nlp/nlp_routes.py` | 131 | `save_context_memory` |
| `api/system/admins_routes.py` | 78 | `log_permission_action` |
| `api/users/rbac_routes.py` | 38 | `log_permission_action` |
| `api/users/sub_accounts_routes.py` | 38 | `log_permission_action` |

---

## 四、零漂移实证（已核查，均通过）

| 核查项 | 结果 |
|---|---|
| `services/` 全目录 `abort(` / `HTTPException` / `raise NotFound\|Forbidden\|BadRequest\|Unauthorized` | **无任何命中** → `safe_handle` 的 HTTP 异常 re-raise 分支在这 26 处实际不触发，与手写 `except Exception` 行为等价 |
| 26 处 try **块内**是否藏 `abort(` / `raise HTTP` | **0 处** |
| 26 处 handler 内是否有 `rollback` / `session.commit` / `.close()` 副作用 | **0 处**（`safe_handle` 不做 rollback，若有则必须排除） |
| `APIResponse.error` 默认 `code=-1` ↔ `safe_handle` 的 `code=getattr(e,"code",-1)` | **一致** |
| 装饰器位置 | 新 `@safe_handle` 一律**最内层**（紧贴 `def`）；`requires_permission` / `cached_api` / `rate_limit` 留外层 → 403 / 429 / 缓存命中路径不经过装饰器 |
| EOL | 待逐文件确认（CRLF 文件禁用文本脚本改写） |

### 残留风险（需知情）
`safe_handle` 内部 `status = getattr(e, "status_code", default_status)`：若抛出的异常对象**自带** `status_code` 属性，
会采用异常值而非 `default_status`，而原代码是固定 500/400。普通 `Exception` 无该属性 → 取 `default_status`；
自定义业务异常若带该属性则行为不同。当前 26 处对应 service 未见抛此类异常，风险低，但属已知差异。

---

## 五、分批执行结果（A 类 23 处已完成）

| 批次 | 范围 | 处数 | commit | 状态 |
|---|---|---|---|---|
| 批 1 | `diagnostics_routes.py` | 7 | `8341647` | ✅ 已提交 |
| 批 2 | `consistency_routes.py` + `mqtt_routes.py` | 4 + 3 | `bd0a63e` | ✅ 已提交 |
| 批 3 | `system_routes.py` | 8 | `9abecbe` | ✅ 已提交 |
| 批 4 | `classes_routes.py` | 1 | `a704e7c` | ✅ 已提交 |
| 待定 | B1 / B2 / B3 | 3 | — | ⏸ 待拍板 |

每批均独立跑闸门：`py_compile`(managed Py3.13) + `pyflakes`(system Py3.11) + 残留清零 + `@safe_handle` 计数 + 空行规范化。

### 执行中额外保留（原表未列出，脚本按规则自动跳过）
- `diagnostics_routes.py` `ErrorInfo.get` 内 `except ImportError:` 降级分支 —— 多 handler 属业务性 fallback，收敛会丢降级逻辑。
- `mqtt_routes.py` 另 2 处 `except Exception` 非整方法体 try（业务性）。
- `system_routes.py` 3 处（pyflakes 报 unused `e`，经 `git show HEAD` 比对为**既有问题**，非本次引入）。

### 终检
- 5 文件全量 `py_compile` OK；`pyflakes` 无新增问题。
- 幂等终检：重跑识别，5 文件剩余同构 try = **0**。
- 所有文件保持原 EOL（含 2 个混合 EOL 文件未被规范化）。
