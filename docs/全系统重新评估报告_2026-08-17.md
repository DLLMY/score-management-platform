# 全系统重新评估报告（2026-08-17 10:11）

> 基于前 4 轮修复后现状（权限对齐 / 功能完整度 P0-P3 / 系统级 F1-F15+F18 / 交互闭环 / 字段统一）重新评估。
> 方法：3 个子代理并行（功能与业务逻辑 / 字段与数据流转 / 异常场景）+ 主代理对全部 P0 疑点**人工复核**（3 个 P0 必崩项均已逐一验证属实）。
> 修复基线：上一轮 F1-F20 闸门全绿（OpenAPI 0 漂移 / eslint 0 / build 0 / vitest 160+3 / 后端回归 4 passed）。

---

## 维度 1：功能模块完整性

**结论：17 个业务域 CRUD 覆盖完整，前端调用与后端路由抽查 10/10 匹配，无前端调用缺失。**

| 检查项 | 结果 |
|---|---|
| 核心 CRUD + 业务操作 | ✅ 全覆盖（用户/班级/成绩/考试/设备/MQTT/算法NLP/通知/监控/导入导出/学生端/班主任工作台/手机箱） |
| 前端调用 ↔ 后端路由 | ✅ 抽查 10 方法全部有对应端点 |
| 后端有前端无入口 | ✅ 无（backups/ 空目录由 system_routes 承载，非缺口） |

**但存在"链路空转"**（功能配置了、页面有了，执行方缺失）：
- 开锁限额/黑名单（用户管理页可配置）→ `UnlockValidator` 从未被开锁路径调用（死链）
- 考勤"请假"状态 → 无任何写入口
- 综合评分开锁维度 → 数据源描述词与统计词不匹配，恒中性

## 维度 2：业务逻辑漏洞（🔴 P0 已复核属实）

| # | 问题 | 证据 | 影响 |
|---|---|---|---|
| **P0-1** | **审批列表/待办接口恒 500**：列表推导循环变量 `a`，却引用未定义 `r` | approvals_routes.py:117、:412（`"student_id": r.student_id`） | 有 ≥1 条审批时 GET /api/approvals 与 /pending 必崩（单测空库未触发） |
| **P0-2** | **开锁限额/黑名单全链路未生效 + 门户开箱免费**：`_deduct_and_unlock` 硬编码只查 current_score<60 扣 10 分，不调 UnlockValidator；门户开箱（student_routes.py:314-322）只发空载荷 MQTT，**不扣分/不记流水/不累计次数** | mqtt_message_service.py:239-261、student_routes.py:314-322 | 配置的日/周限额与黑名单形同虚设；刷卡扣分而门户免费，激励不一致 |
| **P1-3** | **考试状态机与成绩可编辑性脱钩**：closed 后仍可增删改成绩；排名接口筛 `status="published"` 而实际成绩只有 pending/confirmed → **排名恒空**；`:133` date 分支死代码 | exam_routes.py:218/328/342/425 | 发布关闭语义失效；考试排名功能不可用 |
| **P1-4** | **请假审批与考勤统计断裂**：approve 不生成 leave 考勤记录，统计却按 status=leave 算 | attendance_service.py:101-128 | 已批准请假不计入考勤率 |
| **P2-5** | **审批改分不联动综合评分/缓存**（records score-entry 有，approve 没有） | approvals_routes.py:246-261 | 同积分链路两套行为 |
| **P2-6** | **数据隔离遗漏**：考试/成绩分析、考勤全部、通知列表无班级过滤 | exam_routes、attendance_service、notifications_routes.py:31-64 | 班主任可跨班读成绩/考勤 |
| **P2-7** | 手机箱无策略时 UI 显示"已开启"而实际返回 DEFER（界面误导） | phonebox_policy_routes.py:117 vs phonebox_policy.py:151 | 行为与展示矛盾 |
| **P2-8** | 综合评分增量/全量归一化基数不同；无记录学生 index() 抛 ValueError | composite_score_service.py:399-446 | 结果不可比 + 潜在崩溃 |

## 维度 3：数据字段规范

| # | 问题 | 证据 |
|---|---|---|
| F1 | **死列**（无读写入口）：ScoreRecord.reason/period_id/subject_id、Score.rank（恒 None）、Approval.leave_type/start_date/end_date | models/__init__.py:190-192/648、:299-301 |
| F2 | **精度截断**：User.current_score Integer vs ScoreRecord.score_change Float——0.5 分变动被截断，与流水汇总不一致 | models:127 vs :188、records_routes.py:275 |
| F3 | **枚举漂移**：'pending' 同时承载 Approval/Notification/Score 三语义；box_a/b_status 默认 'closed' 而前端判 'opened'；Score 兼容脏值 'normal' | models:271/293/649、:398、exam_routes.py:356 |
| F4 | **updated_at 无 onupdate** 且改分路径不刷新 user.updated_at | models:137/179、records_routes.py:275/839 |
| F5 | **级联缺失**：规则删除不清理 ScoreRecord.rule_id 引用（对照考试已级联）→ IntegrityError/孤儿 | rules_routes.py:281 vs exam_routes.py:149 |
| F6 | **CompositeScore.behavior_score 语义漂移**：存归一化值，读时被 current_score 覆盖 | composite_score_service.py:302/459 |
| F7 | 积分钳制不一致：审批/MQTT 钳 min/max，records 手动创建不钳 → 可超 max | approvals:49-54 vs records:265-284 |
| F8 | 设备状态双路径（telemetry vs control 两套心跳处理）；heartbeat 写不存在的 battery_level/temperature | mqtt_manager.py:546 vs mqtt_message_service.py:263、heartbeat_service.py:128 |

## 维度 4：数据流转断链

| # | 断链 | 证据 |
|---|---|---|
| D1 | **开锁统计数据源断裂**：写端英文 "Points deducted for unlock"，读端统一 `like("%开锁%")` 中文统计 → 排行"近30天开锁"恒 0、综合评分开锁维度失效；且 :174 方向反转（开锁越多合规越低） | mqtt_message_service.py:253 vs analysis_service.py:114/176/227/348、composite_score_service.py:93/429/174 |
| D2 | **综合评分增量断链**：仅 score-entry 单路径触发重算；手动创建/批量/删除回滚/审批/MQTT 加分均不触发 | records_routes.py:699 vs :265/:731/:916、approvals:231、mqtt:481 |
| D3 | **审批闭环不完整**：approve 不建 Notification，学生端收不到审批结果；不清 statistics 缓存 | approvals_routes.py:241-261 |
| D4 | **MQTT 限额双轨**：设备侧按分值 sum 拦截、Web 按次数 count 拦截，口径不一致 | mqtt_message_service.py:59-64 vs records_routes.py:48-53 |
| D5 | MQTT 用户缓存过期（60s），Web 加分不刷新设备缓存 | mqtt_manager.py:65 |

## 维度 5：异常场景（🔴 3 个 P0 必崩已复核属实）

| # | 场景 | 当前行为 | 证据 |
|---|---|---|---|
| **E1** | 审批列表非空 | **恒 500**（未定义 r） | approvals_routes.py:117/412 |
| **E2** | 盒子刷卡带 rule_id 加分 | **恒 500**（ScoreRecord 无 source/source_info 字段，TypeError） | box_routes.py:125-126 vs models:184-198 |
| **E3** | 积分记录导出 | **恒 500**（无 new_score 属性，AttributeError） | export_routes.py:139、:354 |
| E4 | MQTT 规则首笔使用（无历史记录） | AttributeError 被吞，设备无回包（判空顺序颠倒，records 已修此处漏） | mqtt_message_service.py:73 |
| E5 | 刷卡开箱并发 | 读改写无锁，多 worker 双扣/越限；无幂等键 | mqtt_message_service.py:239-261 |
| E6 | 成绩并发提交 | check-then-act 无 DB 唯一约束兜底 → 重复成绩 | models:639-675 |
| E7 | 审批与人手改分并发 | 无锁覆盖丢更新 | approvals:246 vs records:273 |
| E8 | 批量导入同批重复 card_id | 预检不查本批 → commit IntegrityError 无 rollback，脏 session | users_routes.py:719-754 |
| E9 | Redis 宕机 | 仅 get/set/delete 等 8 方法降级，hget/hgetall/lpush/zrange 等裸抛 ConnectionError | redis_cache_service.py:320-393 |
| E10 | 远程通知部分路径 | send_to_device/score_change/test 忽略 publish 返回值，断连仍 success:true | remote_notify_routes.py:286/347/400 |
| E11 | 手动 POST /records | 不限额不限频，current_score 可扣成负 | records_routes.py:265-284 |
| E12 | 统计接口非法日期 | fromisoformat 未捕获 → 500 | records_routes.py:407-410 |
| E13 | 成绩无范围校验 | 负分/超满分可直接写（仅导入路径校验） | exam_routes.py:229-241 |

---

## 改进建议（按优先级 + 预期收益）

| 级别 | 建议 | 动作 | 预期收益 |
|---|---|---|---|
| 🔴P0 | R1 修 3 个必崩 | approvals:117/412 改 `a.student_id`；box:125-126 删 source/source_info（或加列）；export:139/354 删 new_score | 审批页/刷卡加分/记录导出恢复可用（3 行级改动） |
| 🔴P0 | R2 开锁链路统一 | 刷卡与门户统一走 UnlockValidator + 扣分记账 + 描述词统一（中文"开锁扣分"）；门户开箱补扣分/记流水/累计 | 限额/黑名单生效；激励一致；开锁统计与综合评分恢复 |
| 🟠P1 | R3 成绩唯一约束 | Score 加 (exam_id, student_id, subject_id) UniqueConstraint + 捕获 IntegrityError 转 400 | 并发/重复录入根治 |
| 🟠P1 | R4 综合评分重算全覆盖 | records post/batch/delete、approvals、MQTT 加分统一触发 recalculate + 缓存失效 | 综合分/排名实时一致 |
| 🟠P1 | R5 并发原子化 | current_score 改 SQL 原子累加（`update().values(current_score=current_score+Δ)`） | 消除读改写竞态 |
| 🟡P2 | R6 数据隔离补漏 | 考试成绩/考勤/通知列表套 get_allowed_classes | 班主任权限边界一致 |
| 🟡P2 | R7 字段规范 | 清死列、current_score 改 Float（或记录取整）、枚举收敛（box 状态、pending 语义）、updated_at onupdate、规则删除级联清理 | 数据一致性与可维护性 |
| 🟡P2 | R8 异常兜底 | MQTT min_interval 判空顺序、Redis 全方法降级、远程通知余下路径校验返回值、批量导入批内去重+rollback、成绩范围校验、日期 try | 异常不崩、不假成功、不脏库 |
| 🟢P3 | R9 考试状态机 | 成绩增删改校验 Exam.status；排名筛 confirmed；修复 date 死代码 | 状态语义真实生效 |

### 总体结论
- **功能覆盖完整**（维度 1 无缺口）；**但业务链路存在 3 个必崩缺陷（E1/E2/E3，均为 1-2 行低级笔误/越界字段）与开锁/综合评分两大体系性断链**。
- 字段规范与异常兜底以"死列、枚举漂移、无锁并发、降级不全"为主，无高危安全项（前轮已修 JWT/伪认证/限流）。
- 建议优先 R1（必崩）→ R2/R3/R4（链路与数据正确性）→ R5-R8（健壮性）→ R9（状态机）。

---

## 修复记录（2026-08-17 10:37 起，用户「全部修复」，R1-R9 全部落地）

> 全闸门：后端 py_compile 23 文件 OK / 定向 pytest（回归+unlock+scores+approvals+records+attendance+mqtt）/ OpenAPI 0 漂移 / RBAC OK。

### R1 修 3 个必崩（P0）
| 项 | 修复 |
|---|---|
| 审批列表恒 500 | approvals_routes.py:117/:412 `r.student_id` → `a.student_id`（上一轮 F8 插双字段时变量名误写，有审批即崩） |
| 刷卡加分恒 500 | box_routes.py 删 `source/source_info`（ScoreRecord 无此字段，TypeError） |
| 记录导出恒 500 | export_routes.py 删 3 处 `new_score`（ScoreRecord 无此属性，AttributeError） |

### R2 开锁链路统一（P0）
- `_deduct_and_unlock`（刷卡统一出口）改走 `UnlockValidator.validate_unlock`（黑名单/分数门槛/日/周限额）+ `record_unlock`（扣 10 分+日/周计数+流水）
- **User 模型加 `weekly_unlock_count`/`week_start_date` 列**（原内存属性→周限额永不落库）+ 迁移脚本
- **描述词统一中文"开锁扣分"**（原英文 "Points deducted for unlock" 与统计 like("%开锁%") 断裂 → 排行/综合分开锁维度恒 0）
- **门户开箱补扣分记账**（原只发空载荷免费开箱）：班主任策略放行后再过 UnlockValidator，通过才扣分记账+下发；失败返回原因码中文提示
- `_check_time_window` 修复兜底恒放行 bug；`validate_unlock` 加 `skip_time_window` 参数（班主任放行路径不双重时段拦截）
- 保留 composite 开锁维度"开锁多为负向"语义（手机管理场景合理默认，未做未确认的反转）

### R3 成绩唯一约束（P1）
- Score 模型加 `__table_args__` UniqueConstraint(exam_id, student_id, subject_id)
- 迁移脚本 `migrate_score_unique_constraint.py`（现有库 0 重复，已建索引）
- 单条/批量/更新路径捕获 IntegrityError → 400（并发窗口兜底）

### R4 综合评分重算全覆盖（P1）
- records 手动创建 / 批量录入 / 删除回滚 后均触发 `recalculate_user_score`
- approvals 通过后触发（reject 不改分不触发）；MQTT 刷卡加分后触发（低频路径，失败不影响主流程）

### R5 并发原子化（P1）
- 新增 `utils/score_utils.atomic_score_update`（SQL `UPDATE current_score=<expr>` 原子累加，带 min/max 钳制 + 顺带刷新 updated_at）
- records 手动/批量/删除、approvals 通过、MQTT 刷卡加分全部改用原子累加（消除读改写竞态）

### R6 数据隔离补漏（P2）
- 考勤列表/请假列表（attendance_service）、考试成绩列表（exam_routes ScoreList）、通知列表（notifications_routes）套 get_allowed_classes/get_admin_class_ids——班主任权限边界一致

### R7 字段规范（P2）
- 删死列：ScoreRecord.reason/period_id/subject_id、Score.rank（模型层；2 处读取改动态排名/None；DB 列保留免 DROP COLUMN 迁移风险）
- **User.current_score Integer → Float**（0.5 分变动不再截断）；DB 无需迁移（SQLite 动态类型）
- User/Score.updated_at 加 onupdate=datetime.now（ORM 自动刷新）
- 规则删除前把引用流水的 rule_id 置 NULL（原直接删 → 有流水的规则删除即 IntegrityError）
- CompositeScore 读端 behavior_score 返回归一化列值（原被 current_score 覆盖，语义漂移）
- **Device 模型补 battery_level/temperature 列**（心跳上报即 AttributeError）+ 迁移脚本
- 枚举收敛评估：box 默认 'closed' 与前端判断一致（非 bug）；'pending' 三语义收敛改造成本高风险大，保留（历史兼容）

### R8 异常兜底（P2）
- MQTT check_rule_limit 判空顺序修复（首笔使用不再 AttributeError 无回包）
- Redis 14 个方法加 `_safe` 降级装饰器（hget/hgetall/lpush/zrange 等连接断开不裸抛）
- 远程通知余下 3 路径（send/score_change/test）校验 publish 返回值，断连如实报失败
- 批量导入学生：批内 card_id 去重 + commit 兜底 rollback（原同批重复 → 脏 session）
- 统计接口日期 try（非法日期 400 而非 500）；手动 POST /records 钳制 min/max（原可扣成负/超上限）

### R9 考试状态机（P3）
- 成绩增/删/改校验 `Exam.status == "closed"` 拒绝（原 closed 后仍可改）
- 排名接口筛 `confirmed`（原筛 published 恒空）
- 成绩范围校验（0 ~ full_score，单条/批量/更新全路径）
- Exam 更新 date 字段可写（原 date 分支死代码不可达）

### 残留说明
- composite 开锁维度方向语义（开锁多 → 合规低）保留为产品默认，如需反转请产品确认
- 'pending' 枚举三语义（Approval/Notification/Score）未收敛（跨模块历史兼容，改造成本高）

---

## 回归验证记录（2026-08-17 11:31 起，第 4 轮修复后复核）

> 方法：全局引用残留扫描 + 开锁链路/原子化/隔离边界复核 + 存量残留项核查。**发现并修复 6 处遗漏**（均为上一轮 R1-R9 未覆盖的连带项）。

### 复核发现与修复
| # | 级别 | 问题 | 修复 |
|---|---|---|---|
| V1 | 🔴 高 | **Score.to_dict 仍访问已删 rank 列**（R7 删列漏改 to_dict）→ 有成绩数据时 scores 列表/详情/排名接口必 AttributeError | 删 to_dict rank 键 |
| V2 | 🟠 中 | **mqtt 2 条加分路径（rule 名匹配 / score_change）仍是 Python 读改写**（R5 只改 rule_id 路径）→ 并发竞态残留 | 改 atomic_score_update + min/max 钳制 |
| V3 | 🟠 中 | **P2-8 recalc ValueError**：学生无 composite 记录时 `active_user_ids.index()` 崩；且增量基数（composite 表）与全量（全量活跃用户）不一致 → 结果不可比 | 无记录返回 None + 基数统一为全量活跃用户 |
| V4 | 🟠 中 | **P1-4 请假审批断裂**：approve_leave 只改状态不生成考勤记录 → 已批准请假不计入考勤统计 | 批准后为请假日期范围幂等生成 Attendance(status='leave') |
| V5 | 🟡 低 | **R2 边界：daily_unlock_limit 历史 NULL** → `x < None` TypeError | 默认 5 处理 |
| V6 | 🟡 低 | **P2-7 手机箱 UI 误导**：无策略时 allow_self_unlock=True 但实际 evaluate=DEFER | 无策略改为 False |

### 验证闸门
- 新增 6 条回归测试（Score.to_dict / approve_leave 考勤 / recalc 无记录 / daily NULL / 策略序列化 / mqtt 无读改写）
- 定向 pytest（回归+unlock+attendance+mqtt）全绿后更新全量闸门

### 存量保留项（复核确认，未恶化）
- F16 models 拆包 / F17 883 处路由直连 db / F19 URL kebab 统一：大型架构重构，明确保留待专项
- 'pending' 三语义（Approval/Notification/Score）：跨模块历史兼容，保留
- composite 开锁维度"开锁多 → 合规低"方向：产品语义默认，如需反转请产品确认
