# 技术债台账（TECH_DEBT_LEDGER）

> 创建：2026-09-23 ｜ 范围：全仓源码注释中的"债标记"
> 结论先行：**原评估所称"86 个债标记长期滞留"经全仓取证不成立**。真实遗留债仅 **1 项**，其余均为良性/意图性注释。

---

## 1. 扫描方法与规模

- 范围：全仓 `.py/.ts/.tsx/.js/.jsx` 源码（排除 `node_modules/.git/__pycache__/.venv/dist/build/coverage/.workbuddy` 等）。
- 信号词：占位 / 兜底 / 兼容 / 遗留 / 复制 / 临时 / 硬编码 / 简化 / 尚未 / 未实现 / 待实现 / TODO / FIXME / XXX / HACK / 技术债 / techdebt 等。
- 命中"不完美"信号注释共 **246 行**（含"占位"类 46 行、"兜底"68 行、"兼容"67 行等）。

## 2. 逐类定性（关键：几乎全部良性）

| 信号 | 命中 | 定性 | 说明 |
|------|------|------|------|
| 占位 | 46 | ✅ 意图性 | 多为**有意的 UI 空态 / 骨架屏 / 兜底占位文案**：`format.ts` 空值回退 `--`、LazyImage/OptimizedImage 占位符、workbenchOverview 未就绪渲染占位、EntitySelect 空列表占位文案。属良好实践，**非债**。 |
| 兜底 | 68 | ✅ 设计 | 描述**防御性 fallback 路径**（网络失败降级、token 限频降级、路由缺字段兜底），属健壮设计文档。 |
| 兼容 | 67 | ✅ 迁移说明 | CRA→Vite 迁移期兼容垫片（扩展名解析顺序、envPrefix、代理白名单），均为**已落地**说明。 |
| 复制 | 18 | ✅ 字面量 | 绝大多数是**字面"复制"操作**：复制 `.env` 模板、复制固件到目录、剪贴板复制卡号/密钥——非代码重复债。 |
| 临时 | 15 | ✅ 意图性 | 临时目录、临时 ID（仅列表渲染定位）、"临时放行"功能、测试临时库——均为有意设计。 |
| 硬编码 | 16 | 🟡 多为修复记录 | 多数描述**已移除的硬编码假数据**（ScoreAnalysis 移除伪造 0.68、Profile 移除虚构登录记录、analysis 移除硬编码假数据）——是修复记录；少数指向已收敛常量（P2-7 已修复弱口令回退、unlock_validator 已命名常量）。 |
| 简化 | 11 | 🟡 已知限制 | 意图性范围说明（简化版 Apriori / 规则预测 / 缓存匹配），功能可用、精度有限，**非债**。 |
| 尚未 | 4 | ✅ 状态描述 | 描述运行态（如"尚未签发过"、心跳"尚未被标 off"），非债。 |
| 未实现 | 2 | 🟡 测试笔记 | `test-setup.ts` 说明 jsdom 未实现 `scrollIntoView` 的测试桩——测试环境笔记，**非产品债**。 |
| 待实现 | 1 | 🔴 **真实债** | `notification_service.py:150` 腾讯云短信功能待实现（见 §3）。 |
| XXX | 1 | ✅ 无害 | `validation.py:265` 是 MAC 正则 `XXXX.XXXX.XXXX` 的注释，非债。 |

> **误判根因**：原"86"估计把 `placeholder`（258 处，多为 HTML 属性 / CSS `::placeholder`）与上述良性中文注释一并计入；这些是 UI 占位符与工程笔记，并非待清理债。

## 3. 真实遗留债（已升级为跟踪条目）

| ID | 位置 | 描述 | 建议处置 | 优先级 |
|----|------|------|----------|--------|
| **DEBT-001** | `apps/backend/services/notification_service.py:150` | 腾讯云短信功能未实现，统一兜底报错（注释："避免'腾讯云短信功能待实现'误导"） | 若需短信通道：对接 SMS 服务商 SDK 并补充单测；否则在产品文档中明确标注"不支持短信"，消除误导 | **P3** |

## 4. 已知限制（记录在案，非债，按需升级）

- `rule_recommendation_service.py:588` 简化版 Apriori；`score_predict_service.py:180` 规则预测模型；`cache.py:105/211` 简化匹配——功能可用，精度有限。
- `apps/firmware/esp32-old-c3/` 与 `esp32/` 并行存在——历史遗留分支，待确认可删后归档（见文档归档计划）。

## 5. 处置原则（避免误删良性注释）

1. **保留**所有"占位 / 兜底 / 兼容"意图性注释：它们是设计意图文档，误删会丢失上下文。
2. **新债标记统一前缀**：今后新增债标记一律用 `// DEBT: <描述>` 或 `# DEBT: <描述>`，便于精确检索与跟踪，不再被"占位/临时"等泛化词淹没。
3. **禁止假数据**：前端已落实"缺数据即诚实空态，不伪造数值"纪律（ScoreAnalysis / Profile / analysis 已移除硬编码假数据），沿用。

---

*本台账取代"清理 86 债标记"的原始假设；后续债项统一在此跟踪。*
