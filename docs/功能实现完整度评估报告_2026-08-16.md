# 全项目功能实现完整度评估报告（2026-08-16）

> 审查对象：后端（67 路由文件 / 60+ services，Flask-RESTful）+ 前端（56 页面，React+TS）+ 端到端链路。
> 方法：3 个子代理分维度评估（后端完成度 / 前端完成度 / 核心链路闭环）+ 主代理对全部关键缺口**人工复核**（grep/读码确认路由真相）。基于前几轮修复后的现状（权限/交互/闭环/字段/重复页面均已处理，不计入本次问题）。
> 结论：静态审查 + 逻辑推演。

---

## 0. 整体完成度总结

| 维度 | 完成度 | 说明 |
|---|---|---|
| 后端核心功能 | **约 95%** | 67 路由文件、60+ services 绝大多数为真实实现（DB/模型/网络均有实际调用），**未发现空壳端点**（pass/NotImplementedError） |
| 前端页面功能 | **约 90%** | 56 页面全注册全可达，核心页面（Dashboard/成绩/算法/NLP/远程通知/权限）功能齐全 |
| 核心业务链路 | **闭环** | 登录/成绩录入-批量-确认/设备开箱/算法分析/NLP 解析/远程通知/通用导入导出全部前后端打通 |
| **未实现/缺口** | 集中在**外围能力**：成绩/考试导出、短信推送、告警触发、NLP 训练真实性、2 处前端路径错位 | |

**总评：核心业务完整可交付；缺口集中于"导出、推送、训练真实性"等外围与增强功能。**

---

## 1. 逐链路闭环结论（子代理 C + 复核）

| # | 链路 | 状态 |
|---|---|---|
| 1 | 登录（管理端/学生端 JWT + RBAC + 路由守卫） | ✅ 闭环 |
| 2 | 成绩主链路（录入/批量/确认） | ⚠️ **导出与导入有缺口**（见 P0） |
| 3 | 考试管理 | ⚠️ **导入/导出/模板路径错位**（见 P1） |
| 4 | 通用导入导出（ImportExportPanel → /api/import_export/*） | ✅ 闭环 |
| 5 | 设备开箱（学生端 → phonebox → MQTT 下发） | ✅ 闭环 |
| 6 | 算法分析（58 端点 + export） | ✅ 闭环 |
| 7 | NLP 解析/规则/模型 | ✅ 闭环（真实性缺陷见 P2） |
| 8 | 远程通知（模板/定时/单发/广播 → MQTT） | ✅ 闭环 |
| 9 | 导入配置（list/create/update/delete） | ⚠️ **setDefault 路径顺序错**（见 P1） |

---

## 2. 问题清单（按优先级）

### 🔴 P0 — 功能缺失 / 核心路径 404
| # | 问题 | 证据 | 影响 |
|---|---|---|---|
| P0-1 | **成绩导出功能后端未实现** | 后端 `ns_scores` 仅 `/`、`/batch`、`/<id>`、`/confirm-all`（exam_routes.py:194-347），**无 export 端点**；前端 ScoreEntry.tsx:458 `fetch('/api/scores/export?exam_id=...')` 必 404 | 成绩页"导出"按钮不可用 |
| P0-2 | **考试导出功能后端未实现** | `ns_exam` 仅 `/`、`/<id>`、`/publish`、`/close`，无 export；ExamManagement.tsx:281 `/api/exams/export` 必 404 | 考试页"导出"不可用 |
| P0-3 | **成绩导入断链（前端路径错）** | api.ts:3922 `/api/scores/import`，后端无此路由；实际导入能力在 `/api/exam-import/execute`（exam_import_routes.py:317） | ScoreEntry 导入 404 |

### 🟡 P1 — 路径错位 / 404
| # | 问题 | 证据 | 影响 |
|---|---|---|---|
| P1-1 | **考试导入/模板路径错位** | ExamManagement.tsx:434-436 指向 `/api/exams/import\|export\|template`；后端实际为 `/api/exam-import/execute\|template`（exam_import_routes.py:317/470） | 考试导入/模板按钮 404 |
| P1-2 | **导入配置 setDefault 参数顺序反** | 前端 `/api/import/configs/${id}/set-default`（api.ts:4434）vs 后端 `/configs/set-default/<id>`（import_routes.py:210） | 设为默认配置 404 |

### 🟠 P2 — 伪实现 / 桩 / 真实性缺陷
| # | 问题 | 证据 | 影响 |
|---|---|---|---|
| P2-1 | **NLP 预热硬编码缓存** | nlp_optimizer.py:159-166 将 4 条固定文本解析结果写死进缓存，/api/nlp/parse 命中返回伪造数据（nlp_routes.py:164-172） | 解析结果绕过真实模型 |
| P2-2 | **NLP 无数据伪造训练指标** | nlp_ml_service.py:1381-1405、nlp_rule_service.py:264-267 无训练数据仍写 accuracy=0.85/f1=0.85/completed | 训练历史/评估展示虚假精度 |
| P2-3 | **腾讯云短信 stub** | notification_service.py:190-191 `return {"success": False, "message": "腾讯云短信功能待实现"}` | 短信通知不可用 |
| P2-4 | **告警通知触发桩** | alert_service.py:141-145 邮件/短信/Webhook 仅记日志（注释"可扩展"） | 告警不推送 |
| P2-5 | **成绩/风险预测"伪训练"** | score_predict_service.py:172-211 启发式公式+feature_importance 硬编码；risk_predict_service.py:822 权重写死；train（:376-468/:727-864）仅统计不产出模型 | 预测可信度有限 |
| P2-6 | **综合评分排名不落库** | composite_score_service.py:477-485 `db_session_scope(): pass` 空提交，模型无 rank 字段 | 排名更新无效 |
| P2-7 | **MQTT 硬编码凭据回退** | api/monitoring/mqtt_routes.py:229-254 缺配置时回退 Broker + "phoneboxtest"/"123456" | 生产安全风险 |

### 🟢 P3 — 前端占位 / 死按钮 / 低风险
| # | 问题 | 证据 |
|---|---|---|
| P3-1 | **HelpCenter 文档卡死按钮** | HelpCenter.tsx:129-141 三个文档卡片 button 无 onClick |
| P3-2 | **api.ts 3 处休眠封装指向不存在路由** | :4192 `/exams/{id}/scores`、:4289 `ungrouped-devices`、:4290 `by-device/{id}`（无页面调用，低风险） |

### 前端"部分实现"页面
- **ExamManagement**：考试 CRUD/发布/关闭齐全，导入导出缺（P0-2/P1-1）
- **ScoreEntry**：录入/批量/确认齐全，导出导入缺（P0-1/P0-3）
- **HelpCenter**：FAQ 可用，文档入口死按钮（P3-1）

---

## 3. 逐模块完成度摘要

| 模块 | 完成度 | 缺口 |
|---|---|---|
| 认证/RBAC/用户/管理员 | 已实现 | — |
| 班级/教务/课表/节次 | 已实现 | — |
| 成绩/积分（录入/规则/审批/排行） | 部分实现 | 导出后端缺、导入路径错（P0） |
| 考试 | 部分实现 | 导出缺、导入路径错（P1-1） |
| 设备/MQTT/WOL/OTA | 部分实现 | 硬编码凭据回退（P2-7） |
| 算法（聚类/归因/参与度/画像/推荐） | 已实现（规则+统计真实） | 预测 train 不产出模型（P2-5） |
| NLP（解析/规则/模型/训练） | 部分实现 | 预热硬编码、伪造指标（P2-1/2-2） |
| 导入导出（通用/excel） | 已实现 | setDefault 顺序错（P1-2） |
| 监控运维（指标/诊断/审计/日志） | 已实现 | — |
| 消息推送（通知/远程/定时/模板） | 已实现 | 短信 stub（P2-3） |
| 班主任工作台（考勤/作业/心理/活动/文化/小组/学法） | 已实现 | — |
| 手机箱（策略/开箱/MQTT 下发） | 已实现 | — |
| 学生端（查分/请假/通知/开箱/排行/成长） | 已实现 | — |

---

## 4. 建议修复顺序

1. **P0（先补核心 404）**：后端补 `ns_scores/export` 与 `ns_exam/export` 端点（复用 excel_service 流式导出）；修正 api.ts:3922 成绩导入指向 `/api/exam-import/execute`（或补 /api/scores/import 转发）
2. **P1**：ExamManagement 三按钮路径改 `/api/exam-import/*`；api.ts:4434 setDefault 改 `/configs/set-default/<id>`
3. **P2（清理伪实现）**：删除 NLP 预热硬编码缓存、无数据训练改为"未训练"标记、腾讯云短信接真实 SDK 或显式禁用、告警触发接 webhook、预测 train 标注"规则模型"、composite rank 落库、MQTT 移除硬编码凭据
4. **P3**：HelpCenter 文档按钮接真实文档链接或移除；api.ts 删除/修正 3 处休眠封装

---

## 5. 备注

- 本次为**完成度评估**，未执行代码修改；报告含修复建议，若需按序修复请告知。
- 已排除前几轮已修复项（权限体系、交互 21 项、闭环 18 项、重复页面、字段统一、teacher score.edit 等）。
- NLP/算法为"规则+统计真实计算、机器学习训练部分简化"——已如实标注，生产若依赖预测精度需专项投入。

---

## 6. 修复记录（2026-08-17 00:39 起，用户「按照建议全部修复」）

> 全闸门：后端 py_compile 10 文件 OK / 定向 pytest 31 passed（修 2 处过时测试）/ OpenAPI 契约 0 漂移 / RBAC OK / eslint 0 / vite build 0 / vitest 160 passed 3 skipped。

### P0 已修复
| 项 | 修复 |
|---|---|
| P0-1 成绩导出 | 后端 `ns_scores/export`（exam_routes.py，GET+score.view，按 exam_id/student_id/subject_id 筛选，复用 export_service 流式 xlsx，含学生姓名/学号/班级列） |
| P0-2 考试导出 | 后端 `ns_exam/export`（GET+score.view，考试列表 xlsx） |
| P0-3 成绩导入断链 | 前端 api.ts `importScores` 改 `/api/exam-import/execute`（FormData file+exam_id 与后端完全兼容） |

### P1 已修复
| 项 | 修复 |
|---|---|
| P1-1 考试导入/模板 | ExamManagement 删 `importUrl`，新增考试下拉（importExamId）+ `onDataImport`（构造 file+exam_id 调 execute）；templateUrl 改 `/api/exam-import/template`；**权限码修正**：`exam.import/export/template`（后端不存在→非超管按钮被隐藏）→ `score.entry/score.view` |
| P1-2 setDefault 顺序 | api.ts `/configs/${id}/set-default` → `/configs/set-default/${id}` |

### P2 已修复（7 项）
| 项 | 修复 |
|---|---|
| P2-1 NLP 预热硬编码缓存 | nlp_optimizer.py 删除 common_queries 4 条写死缓存（保留真实 warmup_texts 解析预热） |
| P2-2 伪造训练指标 | nlp_ml_service `_train_without_data` + nlp_rule_service 无数据分支：accuracy 0.85 → 0.0，status 改 `untrained`（不再伪报 completed） |
| P2-3 腾讯云短信 stub | 删除 tencent 分支与 stub 函数（产品配置层无腾讯云凭据字段，渠道不可达）；统一兜底"仅支持 aliyun" |
| P2-4 告警触发桩 | alert_service `_trigger_notification` 写入管理员通知中心（recipient_type='admin'，high 优先），真实副作用 |
| P2-5 预测伪训练 | score_predict 返回加 `model_type:"rule-based"`；train message 诚实标注"规则模型非 ML 产物"；risk_predict 权重注释标注启发式 |
| P2-6 composite 排名空提交 | 增量更新 `db_session_scope(): pass` → merge+commit 真实落库；删除空操作 `_update_rankings`（排名本就动态 COUNT，消除虚假日志） |
| P2-7 MQTT 硬编码凭据 | mqtt_routes 移除 phoneboxtest/123456 回退；无配置时明确 400 报错"请先配置 MQTT 凭据" |

### P3 已修复
| 项 | 修复 |
|---|---|
| P3-1 HelpCenter 死按钮 | 3 张文档卡接真实链接：README→`public/docs/系统使用指南.md`、API→`/api/docs/`（Swagger）、MQTT→`public/docs/MQTT协议说明.md`（后两者从仓库复制） |
| P3-2 api.ts 休眠封装 | 删除 `uploadScores`（:4192）、`getUngroupedDevices`（:4289）实现+接口；修正 `getByDevice` → `/api/device-group/device/${id}/groups`（后端真实路径） |

### 顺带修复
- tests/test_score_predict_service.py 2 处过时 `user_id=` → `student_id=`（ScoreRecord 字段早已统一，测试与模型不一致致 2 失败）

### 残留说明
- 腾讯云短信：产品未配置腾讯云凭据（SMS 配置层仅阿里云字段），移除 stub 分支为**诚实报错**；如需腾讯云渠道需先加配置字段与 SDK 接入。
- 预测模型仍为规则/统计模型（未做真实 ML 训练）——已如实标注 `model_type: rule-based`，专项 ML 训练需标注数据与训练管线，属后续专项。
