# 全项目模块字段统一化审查报告

> 审查日期：2026-08-16
> 审查范围：全项目 56 个前端页面 + 后端 68 个模型 / 全部 API 命名空间（教务管理 16 页复用已交付的《教务管理模块字段统一审查报告》，本报告覆盖其余 40 页 + 全部后端契约交叉核对）
> 审查方法：5 组 Explore 子代理字段级并行扫描（表格列/表单/筛选/详情 × 字段名/控件/选项集）+ 后端模型与 API 契约交叉核对（以模型列名与 API 主字段为"规范基准"）

> **实施状态（2026-08-16）**：
> - ✅ **P0（E4）已修复**：`culture/activity/study_guide` 后端三个服务新增班级存在性校验（`get_by_id(ClassInfo, class_id)` 不存在返回 400）；前端 CultureBoard/ActivityManage/StudyGuide 新增班级 Select（加载 `api.classes.getAll()`，默认第一个班级，编辑时不可改），不再提交 `class_id=0`，未选班级前端拦截。
> - ✅ **P1（E5/E6）已修复**：CommitteeList/DutyRoster/SeatingChart 表单 `class_id` 隐藏写死 1 改为班级 Select（默认第一个班级，编辑时不可改）；`HomeworkAssignment` 类型补 `class_name`。
> - ✅ **P1（E7）确认无需改**：聚类输出 `user_id`（引用语义合规），前端 `Analysis.tsx:189` 已 `Number(user.id)` 兼容。
> - ⏳ 待办（P1 续）：引用型字段 Select 化（C1/C2/C3：考勤/班委/值日/座位/学习小组/学法计划/审批的学生/班级/科目手输 Input(number) → Select）——涉及 7+ 页，需学生/科目列表数据源。
> - ⏳ 待办（P2）：星期编码统一 0 基（E8/A7：RemoteNotify 定时 1 基→0 基 + DutyRoster 英文代码→0-6）；枚举值体系英文代码化（B2/B3：activity_type/period）；布尔开关控件统一（D3）；日志级别枚举对齐（B7）。
> - ⏳ 待办（P3）：分页返回结构统一（A13）、per_page 对齐（D5）。

---

## 一、审查范围（40 页，按模块分组）

| 模块 | 页面 |
|------|------|
| 学生成长/家校/德育 | StudentLogin、Profile、RankBoard、RankRuleList、SemesterReport、HomeworkCheck、CultureBoard、TeacherTools、ClassCompare |
| 考勤/班级事务/活动 | AttendanceManage、CommitteeList、DutyRoster、SeatingChart、CategoryList、Approvals、ActivityManage |
| 心理/学习互助/分析 | MentalHealth、StudyGroups、StudyGuide、Analysis |
| 设备/话机/固件/网络 | DeviceManagement、DeviceGroup、FirmwareManagement、WakeOnLan、Diagnostics、PhoneBoxPolicy、RemoteNotify、Notifications |
| 系统/权限/运维/监控 | Dashboard、OpsCenter、PermissionManagement、OperationLogs、SecurityAudit、SystemMetrics、FrontendTelemetry、DataSyncPage、Settings、HelpCenter |

## 二、不一致字段完整清单

### A. 语义相同但命名不一致（跨模块）

| # | 语义 | 出现命名 | 涉及位置 | 规范基准 | 统一方案 |
|---|------|---------|---------|---------|---------|
| A1 | **学生引用** | `card_id`(登录/录分)、`id`(User 主键)、`user_id`(排行榜/审批/聚类)、`student_id`(考勤/班委/值日/座位/心理/学习小组/学法/作业/家长)、`studentId`(localStorage) | 全部模块 | `User.id` / 业务码 `card_id` | **引用场景统一 `student_id`**（与子模型 FK 列名一致）；`user_id` 仅在"用户"域（审批/通知）保留；`card_id` 仅登录/导入业务码；`studentId` 统一 `student_id` |
| A2 | **班级引用** | `class_id`(数字手输/隐藏)、`class_name`(筛选值/展示)、`name`(option value)、`cls.id` | 全部模块 | FK `class_info_id` / 前端参数 `class_id` | 前端提交与筛选**统一 `class_id`(Select 选 id)**；展示派生 `class_name`；禁止 `class_name` 字符串提交（见 E4/E5） |
| A3 | 启用/状态布尔 | `is_active`(班委/分类/排行规则/文化墙/分组/固件/角色/权限)、`is_published`(学法/活动)、`is_completed`(作业/值日)、`is_resolved`(心理预警)、`alert_enabled`(设备)、`is_read`(通知)、`is_online`(设备)、`connected`(MQTT)、`healthy`(数据同步) | 各域 | 各域语义不同 | 保留各域语义字段名（域内含义明确）；**控件统一**（表单 Switch、列表 Tag）；`is_published`/`is_completed` 为域内状态不改名，报告标注 |
| A4 | **风险/严重度** | `risk_level`('low'/'medium'/'high' 字符串)、`severity`(number 1-5 阈值判断)、`severity`('critical'/'error'/'warning' 字符串)、`stress_level`(1-5) | 分析/心理/设备告警/数据同步 | 语义不同域 | **同域统一**：风险= `risk_level`(low/medium/high)；告警= `severity`(critical/error/warning)；心理= `severity`(number) 保留——三域命名已区分，统一 Tag 配色（红/黄/绿） |
| A5 | 类型/分类 | `type`(审批/通知/问题)、`category`(文化墙/权限/模板)、`activity_type`(活动)、`leave_type`(考勤)、`guide_type`/`plan_type`(学法)、`alert_type`(心理)、`operation_type`/`target_type`(日志)、`event_type`(审计)、`metric_type`/`metric_name`(遥测) | 各域 | 各域枚举不同 | 域内统一 `*_type` 命名（现有已基本规范）；**值体系统一**：全部英文代码 + 前端字典映射（见 B2） |
| A6 | 时间字段 | `created_at`(多数)、`timestamp`(诊断/运维)、`updated_at`(指标)、`modified`(备份)、`start_date`/`end_date`(请假/活动/计划/审计筛选)、`approve_time`/`completed_at`/`read_at`/`started_at`/`resolved_at` | 各域 | 创建时间=`created_at` | 创建时间全站统一 `created_at`（诊断/指标保留域内 `timestamp`/`updated_at`，标注来源）；业务起止 `start_date`/`end_date` 统一 |
| A7 | **星期编码** | 排课/时间规则/话机策略 `day_of_week` 0 基（-1=每天, 0=周一…6=周日）；RemoteNotify 定时 `repeat_day_of_week` **1 基（1=周一…7=周日）**；DutyRoster `day_of_week` **英文代码 monday~sunday** | 3 套编码 | 全站统一 0 基（0=周一…6=周日, -1=每天） | **统一为 0 基**：DutyRoster 英文代码改 0-6 数字 + 字典；RemoteNotify 定时 1 基→0 基（后端 `now.weekday()+1` 同步改 `now.weekday()`，注意 weekday() 已返回 0 基） |
| A8 | 在线状态 | `is_online`(boolean，以心跳时效计算)、`status`('online'/'offline'/'error'，可能陈旧) | 设备模块 | `is_online`(计算属性) | 双轨保留兼容，**统一使用 `is_online`**；`status` 仅 API 兼容输出，前端禁用 |
| A9 | 强制语义 | OTA 推送表单 `force`、固件上传/列表 `is_mandatory` | 设备模块 | 域内字段 | 不同操作域（推送 vs 上传），保留；报告标注 |
| A10 | 小组/个人积分 | StudyGroups `score`(小组)、User `current_score`/`score`(兼容) | 学习小组 | 个人=`current_score` | 小组积分 `score` 为组域字段保留；个人积分统一 `current_score` |
| A11 | 管理员姓名 | `real_name`(Admin/Profile 编辑/权限管理)、`name`(User/学生) | 系统模块 | 域内字段 | Admin 用 `real_name`、学生用 `name`，跨域不混用 |
| A12 | 通知类型三套 | `type`('success'/'info'/'warning'/'error')、`severity`('critical'/'error'/'warning')、`type`('normal'/'score_change'/'class_reminder') | 通知中心/设备告警/远程通知 | 各域 | 保留各域（通知中心 `type`、设备告警 `severity`、远程通知 `type`），统一 Tag 配色 |
| A13 | 分页返回结构 | 响应含 `pages`+`total`(SecurityAudit/遥测) vs 仅 `total`(OperationLogs) | 系统模块 | 统一 `total`+`pages` | 后端补齐 `pages`；前端统一读取 `pages`（低优先级） |
| A14 | 聚类键 | `ClusterStudent.user_id` vs `User.id` | 分析 | `user_id` | 后端聚类输出补 `user_id` 别名，消除运行时 Number 强转依赖 |

### B. 固定选项集字段 · 未统一用下拉 / 值体系混用

| # | 字段 | 现状 | 统一方案 |
|---|------|------|---------|
| B1 | 星期（三套编码） | 见 A7 | 统一 0 基 0-6（-1 每天）+ 字典映射 |
| B2 | 枚举值体系混用 | `activity_type` 中文值（文体活动/运动会…）、`leave_type`/`status`/`type` 英文代码 | **全部英文代码 + 前端字典**（activity_type 改 `sports/arts/volunteer/other` 等代码，前端映射中文） |
| B3 | 考勤时段 `period` | 中文值（上午/下午/晚上） | 改英文代码 `morning/afternoon/evening` + 字典 |
| B4 | DutyRoster 区域 `area` | 英文代码 ✓ | 已合规 |
| B5 | 策略 `strategy`(座位) | Select ✓ | 已合规 |
| B6 | 角色 `role` vs `roles[]`/`role_code` | PermissionManagement 管理员表 `role`(单值) 与 RBAC `roles[]`/`role_code` 并存 | 新代码统一 `role_code`/`roles[]`；`role` 兼容输出 |
| B7 | 日志级别 `severity` | SecurityAudit=info/debug/warning/error/critical；DataSync=critical/error/warning/info（缺 debug） | 统一 5 级枚举，DataSync 补 debug |
| B8 | 图标/颜色 | RankRuleList 图标 15 项/颜色 10 HEX；DeviceGroup icon 6 项/color 6 HEX | 各域固定选项，Select ✓ 合规 |

### C. 需要自由输入但实现不当（应改为 Select / 手动输入规范）

| # | 字段 | 现状 | 判定 | 统一方案 |
|---|------|------|------|---------|
| C1 | **学生引用手输** | AttendanceManage/CommitteeList/DutyRoster/SeatingChart/StudyGroups(成员/组长)/StudyGuide(计划)/HomeworkCheck/Approvals 的 `student_id`/`user_id` 全部 **Input(number) 手输** | ❌ 引用型禁止手输 | 改为 **Select(学生列表)**；至少错误提示（选不到人时拒绝） |
| C2 | **班级引用手输/隐藏写死** | HomeworkCheck `class_id` Input(number)；CommitteeList/DutyRoster/SeatingChart 隐藏 `class_id=1` | ❌ 引用型禁止手输 + 写死 | 改为 **Select(班级列表)**（见 E4/E5） |
| C3 | 科目引用手输 | HomeworkCheck `subject_id`/StudyGuide `subject_id` Input(number) | ❌ | 改为 Select(科目列表) |
| C4 | TeacherTools 批量录分科目 | `scoreSubject` 自由输入 | ⚠️ 科目是固定实体 | 改 Select(科目列表) |
| C5 | RemoteNotify 模板分类 `category` | 自由文本 Input | ⚠️ 建议固定选项 | 改 Select（预设分类）或保留输入（低优先级） |
| C6 | 名称/描述/内容类 | title/description/content/notes/reason/plan_content 全部 Input/TextArea ✓ | 合规 | 保留 |

### D. 跨页面一致性差异

| # | 差异 | 涉及页面 | 统一方案 |
|---|------|---------|---------|
| D1 | 学生引用标识混乱（A1） | 全模块 | 统一 `student_id` 展示 + Select 选择 |
| D2 | 班级筛选值：RankBoard/TeacherTools/ClassCompare/Analysis 用 `class_name` 字符串提交 | 多页 | 统一 `class_id`(Select value=id)（同教务 D1 方案） |
| D3 | 布尔开关控件：RankRuleList/CategoryList 用 Checkbox、CultureBoard/StudyGuide/ActivityManage 用 Switch、班委/值日仅展示 | 多页 | 表单统一 Switch；列表统一 Tag |
| D4 | 显示名：考勤"学生/班级"回退字段、学生 ID/用户 ID 混用 | 多页 | 统一"学生""班级"显示名 + 字典 |
| D5 | per_page 硬编码差异（8/20/50/100/500） | 运维/日志/遥测等 | 统一默认 20（运维 8 为卡片布局可保留，标注） |
| D6 | Dashboard 等级/聚类枚举 Tag | Dashboard | 与 RankBoard/Analysis 对齐文案 |

### E. 审查发现的真实功能问题（本批新增，P0/P1）

| # | 问题 | 影响 | 修复 |
|---|------|------|------|
| **E4 (P0)** | **CultureBoard/ActivityManage/StudyGuide 创建时提交 `class_id=0`**（文化墙/活动/学法文章），后端 culture/activity/study_guide POST `class_id` required 且**无班级存在性校验**直接落库 | 记录写入 `class_id=0`（不存在班级）→ 前端按真实班级查询永远查不到，脏数据 | 前端改为 Select 选真实班级（默认当前管理员班级/全部）；**后端加班级存在性校验**（`get_by_id(ClassInfo, class_id)`，不存在返回 400） |
| **E5 (P1)** | CommitteeList/DutyRoster/SeatingChart 表单 `class_id` 隐藏写死 1 | 创建记录班级固定为 1（若 id≠1 错误归属） | 改 Select 选班级 + 默认当前管理员班级 |
| E6 (P1) | HomeworkCheck 前端类型缺 `class_name`（后端 homework_service 已输出 `names.klass(a.class_id)`） | TS 类型缺口，运行时正常 | 前端类型补 `class_name` |
| E7 (P1) | Analysis 聚类键 `ClusterStudent.user_id` vs `User.id` 不一致 | 依赖运行时 Number 强转，若类型不一致聚类关联失败 | 后端聚类输出统一 `user_id` |
| E8 (P2) | RemoteNotify 定时星期 1 基（后端 `now.weekday()+1`）vs 话机策略/时间规则 0 基 | 跨模块星期编码不一致 | 统一 0 基（前端 1-7→0-6，后端 `+1` 移除）；DutyRoster 英文代码同步 0-6 |
| E9 (P2) | TeacherTools 群发通知提交 `user_ids` 来自 `s.id`（学生行 id）但后端可能期望 `user_id` | 需核对契约 | 与 A1 统一为 `student_id`/`user_id` 后校对齐 |

## 三、统一方案（字段字典 + 修复优先级）

### 3.1 统一字段字典（核心跨模块字段）

| 语义 | 统一字段名 | 类型 | 表单控件 | 列表控件 | 备注 |
|------|-----------|------|---------|---------|------|
| 学生引用 | `student_id` | int | Select(学生) | 文本(姓名) | FK `user.id`；`user_id` 仅用户域 |
| 班级引用 | `class_id` | int | Select(班级) | Tag(班级名) | 展示派生 `class_name` |
| 创建时间 | `created_at` | datetime | — | 文本 | 全站统一 |
| 启用状态 | `is_active` | bool | Switch | Tag(启用绿/禁用灰) | 域内语义保留命名 |
| 风险等级 | `risk_level` | enum low/medium/high | Select | Tag(绿/黄/红) | 告警域 `severity` |
| 星期 | `day_of_week` | int 0-6（-1 每天） | Select | 文本 | **全站 0 基** |
| 分页 | `page`+`per_page` | int | — | — | 返回含 `total`+`pages` |
| 枚举值 | 英文代码 | string | Select | Tag | 前端字典映射中文 |

### 3.2 实施优先级

| 优先级 | 事项 | 涉及 | 风险 |
|--------|------|------|------|
| **P0** | E4：culture/activity/study_guide 前端 class_id Select 化 + 后端班级存在性校验 | 3 页前端 + 3 个后端路由/服务 | 中 |
| P1 | E5：Committee/Duty/Seating class_id Select 化；C1/C2/C3：引用型字段 Select 化（学生/班级/科目） | 7+ 页前端 | 中 |
| P1 | E6：HomeworkCheck 类型补 class_name | 1 页 | 低 |
| P1 | E7：Analysis 聚类键统一 user_id | 后端 1 处 | 低 |
| P2 | E8/B1/A7：星期编码统一 0 基（RemoteNotify/DutyRoster） | 2 页 + 后端 1 处 | 中 |
| P2 | B2/B3：枚举值体系英文代码化（activity_type/period）+ 字典 | 2 页 | 低 |
| P2 | D3：布尔开关控件统一（Checkbox→Switch） | 2 页 | 低 |
| P3 | A13 分页返回结构、D5 per_page 对齐、B6/B7 枚举对齐 | 系统模块 | 低 |

> 说明：A3/A4/A5/A9/A10/A11/A12 为各域语义字段（域内含义明确），保留命名并统一控件与配色，不强行跨域改名（避免破坏 API 契约）。
