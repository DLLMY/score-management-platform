# 下一步开发计划（2026-09-19 扫描刷新版）

> 本计划替代《下一步开发计划-20260824.md》中已过期的 T1–T12 排期。原 12 项任务已全部收口；本文档基于 2026-09-19 的**实测扫描**重新量化当前真实技术债，按 P0–P3 给出可审阅的推进清单。

## TL;DR

- **原 T1–T12 全部已收口**（含本会话收尾的 T4 综合分重算异步化 `b1546d6`+`e825226`）。旧 backlog 已清空。
- **四闸门当前基线（本会话实测）**：
  - 前端 vitest：**37 文件 / 276 passed · 3 skipped，RC=0**
  - 前端 tsc：`--noEmit` RC=0
  - 前端 eslint：`src` 全量 RC=0
  - 后端 pytest：历史基线 **2084 passed / 7 skipped / 0 failed**（本会话因 conftest 后台线程累积导致全量 300s 超时，未重跑；作为入场闸门，任何重构后须复跑确认归零）
- **重新扫描识别出 5 类真实残留债（D1–D6）+ 4 类"伪债"（误报/已收敛，不排期）**。

## 一、已收口确认（无需再排期）

| 原任务 | 状态 | 实测证据 |
|---|---|---|
| T1 综合分重算不静默失败 | ✅ | 历史记录 + `test_score_recalc.py` |
| T2 前端测试补强 | ✅ | 本会话 vitest **276 passed·3 skipped** |
| T3 列表端点强制分页 | ✅ | 历史记录 |
| **T4 综合分重算异步化** | ✅ | 本会话 `CELERY_ASYNC_SCORE_RECALC` 接线 + 生产 `.env` 点亮 |
| T5 前端 any→0 | ✅ | 全仓 `any` 类型用法 grep **0 处** |
| T6 security fail-closed | ✅ | 历史记录 |
| T7 ML 解耦 | ✅ | requirements.txt 已移除 catboost/gensim；torch/transformers/xgboost→`requirements-ml.txt` |
| T8 死依赖清理 | ✅ | package.json 无 web-vitals、无 eslintConfig 残留 |
| **T9 大路由瘦身** | ✅（部分） | records_routes 1050→**687**、algorithm_routes 1242→**45**（78b05db/fbee156 编排下沉拆分）；但其余超大路由仍在（见 D2） |
| T10 CRA eslint 清理 | ✅ | package.json 无 eslintConfig |
| T11 静默失败页 | ✅ | FrontendTelemetry/SecurityAudit 已有错误态+重试；全仓精确"fetch error→return null"已无 |
| T12 巨型页拆分 | ✅ | `pages/**` ≥600 行文件 = 0（最大 591） |

## 二、当前真实债项（实测量化）

### P0 闸门守卫（入场条件，非新债）
- 任何 D1–D6 改动**必须**通过四闸门（前端 tsc/eslint/vitest + 后端 pytest）零回归后才可提交。
- 后端全量 pytest 因超时基础设施须**串行 3 批分跑**（禁 `-n 4` xdist：2 个 nlp_performance 假失败），判据：失败=0。

### P1 高价值结构性债
**D1 — B3 `to_dict` 收口残量**
- 实测：83 个模型中 **32 个缺 `to_dict`**；路由内联 dict 序列化 **866 处**（users 58 / devices 51 / course_schedule 46 / system 44 / nlp 41 / import_export 39 / remote_notify 38 / rules 36 …）。
- 性质：响应形状不一致风险 + 重复序列化逻辑。低业务逻辑风险（to_dict 为加法式）。
- 验收：① 新增 `to_dict(fields=None)` 不破坏既有响应形状（对象级断言，非 `inspect.getsource`）；② 每批 ≤5 模型 + 对应路由内联收敛；③ 跑 pytest 全绿；④ 最终 inline_dict_total 显著下降（目标 <200）。

**D2 — 超大路由拆分残量（T9 续作）**
- 实测 `api/**/*_routes.py` ≥300 行共 **25 个**，最大：
  - `nlp/nlp_routes.py` **1636**
  - `devices/devices_routes.py` **1429**
  - `academics/course_schedule_routes.py` **1223**
  - `users/users_routes.py` **1207**
  - `system/system_routes.py` **1139**
  - `devices/firmware_routes.py` 884 / `data/import_export_routes.py` 744 / `scores/records_routes.py` 687 …
- 性质：可维护性 + 合并冲突风险。
- 验收：① 单路由文件 ≤600 行（沿用 fbee156 拆 part 模块 + 78b05db 编排下沉范式）；② 路由注册入口 `app/api_versioning.py` 不变；③ 四闸门全绿。
- 风险：高，必须 plan 后分批，每批单路由，回归全绿。

### P2 可观测性与健壮性
**D3 — 生产代码 `silent except:pass` 收敛**
- 实测 222 处，其中**生产代码仅 9 处**（`tests/` 占其余 213 处，不排期）：
  - `services/nlp_enhanced_service.py:1928`、`api/student/student_routes.py:376`、`services/system_config_service.py:72`、`app/service_init.py:232`、`startup/run.py:17`、`tasks/scheduler.py:71,87`、`utils/datetime_utils.py:61,68`
- 验收：改为 `except Exception as e: logger.warning(...)`，保留原 best-effort 语义；4 闸门全绿。
- 风险：低。

**D4 — 配置源散落收敛（⚠️ 范围已修订，原"64 处 getenv=债"为误判）**
- **修订结论**：架构**已是正确集中式**——`config.py::Config` = Web 配置**唯一权威源**（其内 ~56 处 `getenv` 是设计正确，非债）；`app/config_init.py::init_config` = **注入桥**（把 `config.*` 灌入 `app.config`，T4 的 `CELERY_ASYNC_SCORE_RECALC` 已按此模式正确做，不重复）。故"散落 64 处"不成立。
- **真正会漂移的二级读取器（已逐一定性）**：
  1. `app/config_init.py::_check_redis_connectivity` —— 重读 4 个 Redis 键 + 硬编码默认值。**已收敛**（batch-1：改用 `config.REDIS_*`），零语义风险、零测试交互。
  2. `utils/config_validator.py` —— 重读 ~13 个键 + 硬编码默认值。**保留不收敛**：它是**运行时**校验，现有 `test_config_validator.py` 靠 `patch.dict(os.environ)` 在运行时改值断言；若改 `config.*`（导入期解析）会**破坏测试语义**。属"看似可收敛实则不可"的坑。
  3. `utils/log_archiver.py` —— 主路径已用 `Config.*`，仅 `except ImportError` 兜底为独立脚本运行。**无需改**（已正确）。
  4. `app/config_init.py::validate_secret_keys` 重读 `FLASK_SECRET_KEY`/`JWT_SECRET_KEY` —— **保留**：与 `config.py`、`utils/security.py` 共三点 fail-closed 读取，改动任一处须同步其余两处（见 config.py 头部说明）。
- **本批落地**：D4-batch-1 已提交（redis 探针 → `config.*`，字节级 CRLF 守恒 292→292，探针实测 `[R1] Redis 连通性 OK`）。
- **剩余**：无（真实可安全收敛项仅 batch-1 一处；其余均属"刻意保留"）。验收：① 配置单一来源已在 `Config`；② 注入桥白名单显式；③ 导入冒烟 + 探针实证通过。风险：**低**（修订后）。

**D5 — `bare except` 收口**
- 实测生产代码 2 处：`app/api_versioning.py:171,175`（其余在 tests/scripts）。
- 验收：改 `except Exception`，不吞异常；pytest 全绿。风险：低。

### P3 零碎清理
**D6 — 杂项**
- `console.*` 残留 3 处；`TODO/FIXME` 1 处（`utils/validation.py:265`）；`eslint-disable` 注释 53 处（跟踪递减，不强制清零，按 PR 收敛）。

## 三、"伪债"甄别（不排期，避免误改）

| 扫描信号 | 实测数 | 结论 |
|---|---|---|
| `return null` 出现 | 53 | **绝大多数为合法早返回/条件渲染**（hooks、`null` 校验、条件分支），非 T11 式"fetch error→静默空白"。仅明确"请求失败即 `return null`"的页面才需改，须逐文件甄别，**不批量处理**。 |
| `any` 类型用法 | 0 | T5 已收口，确认。 |
| F17 路由写路径残留 | 0 | 服务化迁移已全收口（`f17_write_paths_in_routes=[]`）。 |
| C901 圈复杂度（生产） | 0 | 唯一 1 处在 `tests/test_api_contract.py`，生产复杂度已收敛。 |

## 四、推荐推进顺序与风险

**建议节奏（低风险先行，高价值随后）**：
1. **D3 / D5 / D6**：零碎清理，零破坏，可一次小批收口。
2. **D1**：B3 `to_dict` 收尾，小步可审阅（每批 ≤5 模型），低风险高一致收益。
3. **D4**：配置源收敛，中等风险，需全量 pytest。
4. **D2**：超大路由拆分，高投入高风险，**必须 plan + 分批 + 四闸门**，留到最后。

**跨任务铁律（沿用历史经验）**：
- 改格式/命名/结构**保持原有逻辑与功能不变**，仅做等价重构。
- 禁 Edit 直改 CRLF 文件（后端多数 .py 为 CRLF），须字节级脚本守恒行尾。
- 提交经 Python `git -C` 子进程（绕过沙箱 bash shim 的 `cd` 损坏），push 后 `ls-remote` 核实。
- 不自动 commit，须用户确认。

## 五、四闸门验收表（每次改动后必填）

| 闸门 | 命令（managed 运行时） | 判据 |
|---|---|---|
| 前端 vitest | `node node_modules/vitest/vitest.mjs run --pool=forks` | passed↑、failed=0、RC=0 |
| 前端 tsc | `node node_modules/typescript/bin/tsc --noEmit` | RC=0 |
| 前端 eslint | `node node_modules/eslint/bin/eslint.js src --ext .ts,.tsx` | RC=0 |
| 后端 pytest | `apps/backend/.venv/Scripts/python.exe -m pytest`（串行 3 批） | failed=0、RC=0 |

---
*生成依据：`.workbuddy/tmp_scan/scan_debt_20260919.py` 实测 + 本会话四闸门复测。扫描数据见 `.workbuddy/tmp_scan/scan_debt_20260919_result.json`。*
