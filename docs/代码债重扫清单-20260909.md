# 代码债重扫清单（新一轮 · 2026-09-09）

> 上一轮（2026-09-08）D1–D7 已全部收口。本轮为**全新全量扫描**，旨在发现 D1–D7 之外的改进型债项，并复核 D2 巨型页拆分的真实覆盖度。

## 0. 扫描方法 & 当前基线

- **方法**：Python 静态扫描。前端 `apps/frontend/src`（329 个 `.ts/.tsx`）、后端 `apps/backend`（521 个 `.py`，其中生产 247 / 非生产 274）。维度：
  - 前端：`console.*`、`: any`/`as any`、`TODO/FIXME/HACK`、`eslint-disable`、超大文件（>600 行）、`eslint-disable` 规则分布。
  - 后端：`print()`、`broad/bare except`、`# noqa`（均区分 **生产 vs 测试/脚本/工具**）。
- **当前健康基线（代码未改动，沿用上轮验证）**：
  - `tsc --noEmit` → **EXIT=0**（今日 2026-09-09 复验）
  - `eslint` 全 `src` → **0 problems**（上轮 D7 后）
  - `vitest run` → **277 passed / 3 skipped**，EXIT=0
- **结论**：仓库当前零回归、可构建。本轮发现均为**改进型债项**（非回归），按需挑选执行，不强制全做。

---

## 1. 候选债项（E 系列）

### E1 · 前端 `any` 类型泄漏 — 中低
- **规模**：生产 **40 处 / 16 文件**（测试 9 处 / 3 文件，忽略）。
- **Top**：UserList(7)、NLPManagement(6)、codeSplitting(4)、App(3)、ScoreAnalysis(3)、webVitals(3)、env(2)、useNetworkStatus(2)、ExamManagement(2)、api.ts(2)。
- **处置**：渐进式类型化，随 D2 拆分同批顺手收；非紧急。可后续逐步开启 `no-explicit-any`。

### E2 · 前端 `eslint-disable` 抑制 — 低（实质 0 真债）
- **规模**：35 处 / 23 文件。规则分布：`react-hooks/exhaustive-deps` 32、`no-console` 3（仅 logger 合理使用）。
- **处置**：依 D1 政策 `exhaustive-deps` 多为 intentional，维持现状。**无行动项**。

### E3 · 后端 `# noqa` 抑制泛滥 — 中
- **生产 243 处 / 90 文件**。规则分布：`F841`(未使用变量) **198**、`E402`(import 非顶部) 21、`E501`(行过长) 19、`E712` 3、`F401`(未用 import) 2。
- **真债**：**F841 198 处 = 死变量赋值**，应清理（删 unused 赋值或显式 `_ =`）。其余多为 import 顺序/行长，低优先。
- **处置**：用 ruff/flake8 `--select F841` 定向批量清理（安全、零行为变更）。

### E4 · 后端 broad/bare `except` — 中高（生产吞异常风险）
- **生产 103 处 / 49 文件**。集中：`redis_cache_service`(9)、`nlp_enhanced_service`(8)、`nlp_ml_service`(7)、`approval_service`(5)、`student_routes`(4)、`nlp_optimizer`(4)、`permission`(4)…
- **处置**：**审计收窄**为具体异常类型；关键路径 fail-fast 或 `logger.error` 后重抛。**不可盲目删除**（部分是有意 fail-safe）。建议单文件评审，不批量自动修，配 pytest 回归。

### E5 · 后端 `print()` — 非债（跳过）
- 生产仅 27 处，**全部**位于 `models/migrations/001_add_class_info_fk.py`（迁移脚本，非运行时生产代码）。忽略。

### E6 · 前端巨型页第二梯队（未拆分 monolith）— 高（新 D2 波次）
- **规模**：**15 页 >600 行且含大量 JSX（className 45–137），无对应视图子目录** → 从未拆分。
- **清单（行数 / className）**：

  | 页面 | 行数 | className |
  |---|---|---|
  | MentalHealth | 817 | 119 |
  | ParentContact | 795 | 130 |
  | CommitteeList | 745 | 109 |
  | SeatingChart | 742 | 91 |
  | DutyRoster | 702 | 102 |
  | RankRuleList | 683 | 121 |
  | StudyGroups | 679 | 96 |
  | TimeRuleList | 676 | 89 |
  | ImportConfigManagement | 674 | 45 |
  | ActivityManage | 666 | 83 |
  | OpsCenter | 645 | 65 |
  | Settings | 640 | 115 |
  | CultureBoard | 631 | 86 |
  | Profile | 626 | 137 |
  | HomeworkCheck | 617 | 79 |

- **处置**：复用 D2「逻辑层 + 视图层」范式，三闸门（tsc/eslint/vitest）逐页拆分。
- ⚠️ **铁律③**：此 15 页**非** ClassManagement/AttendanceManage/StudyGuide 锁定页，铁律③不适用；但拆前需确认 MentalHealth/ParentContact 等业务归属，避免误触班主任工作台交互（参照铁律③「字段/权限变更须用户审核」精神）。

### E6a · 已拆但逻辑层过肥 — 中低
- **规模**：10 页已建子目录（D2 已拆）但主文件仍 **614–1358 行**：ScoreEntry 1176、UserList 1009、NLPManagement 1358、DeviceManagement 802、RemoteNotify 800、AlgorithmAnalysis 759、ClassManagement 693、ExamManagement 691、CourseSchedule 688、PermissionManagement 614。className 均低（0–33）→ JSX 已下沉，剩纯逻辑过肥。
- **处置**：可后续抽 handler/composition（如 `useXxxPageLogic` 自定义 hook 或拆 service 调用）；非紧急，列待办。

---

## 2. 汇总表

| 债项 | 规模 | 严重度 | 处置 | 闸门影响 |
|---|---|---|---|---|
| E1 `any` 类型 | 40 处 / 16 文件 | 中低 | 渐进类型化 | 需 tsc |
| E2 `eslint-disable` | 35 处（32 exhaustive-deps） | 低（0 真债） | 维持 | 无 |
| E3 `noqa`/F841 | 243（198 F841）/ 90 | 中 | 批量清 F841 | 需 ruff/flake8 |
| E4 broad-except | 103 / 49 | 中高 | 审计收窄 | 需 pytest |
| E5 `print()` | 27（migration） | 非债 | 跳过 | — |
| **E6 未拆 monolith** | **15 页** | **高** | D2 范式拆分 | tsc/eslint/vitest |
| E6a 逻辑过肥 | 10 页 | 中低 | 抽 hook/service | 需 tsc |

---

## 3. 建议执行顺序

1. **E3（F841 清理）**：批量、零行为变更、低风险高回报，最先做。
2. **E6（15 页拆分）**：沿用 D2 范式，三闸门逐页；按行数降序排期（MentalHealth→HomeworkCheck）。
3. **E4（broad-except 审计）**：单文件评审收窄，配 pytest 回归。
4. **E1（`any` 类型）**：随 E6 拆分同批顺手收。
5. E2 / E5 / E6a：观察项，不主动排期。

> **铁律①**：全部改动（含本文档）不 commit/push，待用户明确指示再提交。
