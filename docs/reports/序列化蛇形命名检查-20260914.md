# 序列化蛇形命名合规检查报告（推荐 #6）

> 扫描对象：`apps/backend/api/**` 下所有 `APIResponse.success/error/paginate(data={...})` 中 `data` 字典的键名。
> 约定：后端经 envelope 输出到前端的字段应为 **snake_case**（前端在 api.ts 层做 camelCase 转换）。
> 下列键名被识别为 **camelCase**（含小写+大写过渡），可能破坏前后端字段映射，需核对是否应为 snake_case 或由前端统一转换。

## 结果：未发现 camelCase 键

全部 `APIResponse` 序列化 `data` 字典键名均为 snake_case，符合前后端字段约定。
